# T-015実機検証記録（忍者）

対象: f881005〜2d5f3dc（部品選択リストへのサムネイル追加）。隠密静的レビュー合格済み
（`docs/archive/ecad2-t015-review-onmitsu.md`、要修正候補#1はP-009起票済み・#2は2d5f3dcで解消）。

## 観点別結果

| # | 観点 | 判定 | 所見 |
|---|------|------|------|
| 1 | 部品選択リスト各項目にサムネイル表示 | OK | 固定5種+自作パーツ全件にサムネイル表示。24px角、テキスト左に配置。 |
| 2 | サムネイルが項目毎の図形と一致・区別できる | OK(前提に食い違いあり) | a接点/b接点/コイル/セレクトSW/端子台の5種は目視で区別可能。**ただし部品選択リストの固定種は「ORa/ORb」ではなく「セレクトSW」**（下記「範囲外の気づき」参照）。 |
| 3 | 自作パーツのサムネイルが実定義形状を反映 | OK | テスト自作パーツ（菱形6線定義、`role=coil`）を自作フォルダへ配置し確認。サムネイル・配置後のキャンバス描画とも定義通りの菱形で一致。 |
| 4 | リスト選択→配置の既存フロー回帰なし | OK | リスト項目クリック→「要素の配置」ダイアログ(種別プリセット済み)→デバイス名入力→OK→配置、の一連が正常動作。ツールバー直接配置(F5等)と同一の`TryPlaceElement`経路で回帰なし。 |
| 5 | 起動時間の体感悪化なし | OK | 部品選択リスト表示(自作パーツボタン押下→リスト展開)の応答時間は実測187ms(固定5+自作1件)。体感上問題なし。 |
| 6 | リスト表示のスクリーンショット採取 | 済み | `t015_list1.png`(固定5種のみ)・`t015_list2_custom.png`(自作込み)をscratchpadに保存。サイズ・余白は殿帰宅後の判断材料として提出。 |
| 7 | 回帰スモーク(新規/開く/保存・矢印・Esc・配置) | OK | 新規(未保存確認ダイアログ標準3択・即1シート生成)/保存(.gcad)/開く(復元内容一致)/矢印セル移動/Esc選択解除/F5配置、いずれも正常動作。 |

## 範囲外の気づき（家老判断待ち、拙者からは修正しない）

1. **部品選択リストの固定種とツールバー固定種の不一致**: ツールバーには「OR a接点配置(Shift+F5)」「OR b接点配置(Shift+F6)」ボタンがあるが、部品選択リスト(`PartPaletteViewModel`が読み込む`BasicPartTemplates.All()`)には ORa/ORb が定義されておらず、代わりに「セレクトSW」が含まれる。T-007(88ea0fd)由来の既存構成で、T-015実装(f881005〜2d5f3dc)による変更ではない。隠密のスコープ再定義文書(`docs/archive/ecad2-t015-scope-redefinition-options-onmitsu.md` 9-13行)は両者とも「固定6種」と表記しており、この食い違いには触れていない。家老の検証観点2「ORa/ORbの見た目が区別できる」は部品選択リスト上では検証不能（該当要素が存在しないため）。
2. **要素配置時に機器表(`DeviceTableGrid`)へデバイス名が反映されない**: `PlaceElementAtSelectedCell`(`MainWindowViewModel.cs`、T-026由来コミット934247e)は`sheet.Elements.Add`と`MarkDirty()`のみ行い、`Document.Devices.ByName`への登録や`DeviceTable.Refresh()`を呼んでいない。プロパティパネル経由でデバイス名を編集した場合(`SelectedElementDeviceName`のsetter)のみ機器表に反映される。部品選択リスト・ツールバー直接配置のどちらでも同一の欠陥で、T-015固有ではない。

## スクリーンショット

`%TEMP%\claude\C--ECAD2\c62bb213-5b11-418b-80e8-5942a95caa6e\scratchpad\` 配下:
`t015_list1.png` / `t015_list2_custom.png` / `t015_placed.png` / `t015_placed2.png` / `t015_after_open.png` 等

## 検証中の手違い（記録として残す）

自作パーツ検証準備の過程で、.NETの実MyDocuments(OneDriveリダイレクト先)と異なる場所に誤ってテストフォルダを作成し、後片付けのrm -rfをAuto Modeの安全機構が事後拒否する事象が発生（詳細は殿・家老へ別途口頭報告済み、以後は拙者は当該フォルダに触れず殿確認に委ねた）。検証自体は正しい場所への再配置後に完了。テスト用自作パーツファイルは検証完了後に単一ファイル削除で後片付け済み。
