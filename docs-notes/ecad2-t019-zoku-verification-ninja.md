# T-019追加増分(案B)実機検証記録（忍者）

対象: ae2db4b（濃紺状態(HasProject=false)でのツール系操作を無効化・案内表示）。
隠密レビュー合格済み。

## 観点別結果

| # | 観点 | 判定 | 所見 |
|---|------|------|------|
| 1 | 起動直後(濃紺)でツールバー8ボタンがグレーアウトし押しても反応しない | OK | UI Automation実測で8ボタン全てIsEnabled=False、スクリーンショットでも視覚的にグレーアウトを確認。 |
| 2 | 同状態でF5〜F8・Shift+F5/F6でステータスバーに案内表示 | OK | 6キー全てで「シートがありません。新規作成（Ctrl+N）から始めてください」を確認。ツールはSelectのまま(PlaceElementへ遷移しない)。 |
| 3 | Ctrl+N後はボタン有効化・キー配置とも正常(回帰なし) | OK | 8ボタン全てIsEnabled=True化。F5配置で「要素の配置」ダイアログが正常表示(回帰なし)。 |
| 4 | Sheets=0の.gcadを手作りして「開く」→濃紺に戻り同ガードが効く | OK | 手作りJSON(`sheets:[]`)を読み込ませたところ濃紺状態に復帰、8ボタンのうち代表3件(選択/F5/自作パーツ)でIsEnabled=False再現を確認。 |
| 5 | 回帰スモーク=配置・保存・未保存確認3択 | OK | 新規→F5/F6配置→保存(.gcad書き出し確認)→再度配置→Ctrl+Nで未保存確認3択(はい/いいえ/キャンセル)表示、いずれも正常。 |

## 範囲外の気づき

なし。

## スクリーンショット

`%TEMP%\claude\C--ECAD2\c62bb213-5b11-418b-80e8-5942a95caa6e\scratchpad\` 配下:
`t019b_startup.png`(起動時グレーアウト) / `t019b_after_ctrln_f5.png`(有効化後配置) /
`t019b_after_open_empty.png`(Sheets=0読込後の濃紺復帰) / `t019b_unsaved_confirm.png`(未保存確認3択)
