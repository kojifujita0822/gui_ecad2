# T-021 増分(v)追加修正（コミット340f53d+7e70c3d）実機検証記録

忍者による実機検証。対象: 母線可視スクロール追加修正（殿直接指示、2026-07-05）。
静的レビュー: `docs/ecad2-t021-increment-v-review-340f53d-onmitsu.md`（隠密、要修正#1確認済み合格）。

## 検証環境

- Ecad2.App起動、ビルド警告0件・エラー0件。
- シート1（既存.gcadサンプル、20列グリッド、行1〜10、X0/X1/Y0接点あり）。

## 観点別結果

| # | 観点 | 判定 | 所見 |
|---|------|------|------|
| 1 | 矢印キーで左端(列0)到達時に左母線が見える位置までスクロール | OK | 行5→列0移動、スクショで左母線が選択セルの左に十分な余白を持って視認可能（`obs1-left-edge.png`） |
| 2 | 右端(列19)到達時に右母線が見える | OK | 行5→列19移動、右母線（P24ラベル付き縦線）が視野内（`obs2-right-edge.png`） |
| 3 | 列端に留まったまま上下移動しても手動横スクロール位置が引き戻されない（修正#1の確認） | OK | 列19で手動スクロールバー(PageLeft)操作により右母線を画面外へ押し出した状態からDownキー（行のみ変化・列19不変）を押下。スクロール位置は変化せず、右母線は画面外のまま維持された（`obs3-manual-scroll-before.png`→`obs3-after-down.png`）。隠密指摘#1（列不変での行移動時にwiden判定が誤発火し横スクロールが引き戻される回帰）は解消を確認 |
| 4 | 中央域の通常追従・端クランプの回帰なし | OK | 列10（中央）へ移動、選択セルは視野内に正常表示（`obs4-center.png`）。縦方向はグリッドが行1〜10のみで画面内に収まるため追従確認は意味をなさず（行10で頭打ち、`obs4-vertical.png`） |
| 5 | ズーム150%での位置整合回帰なし | OK | Ctrl+ホイール合成操作でズーム150%に変更後、列0（`obs5-zoom150-left.png`）・列19（`obs5-zoom150-right.png`）とも母線とセル位置の整合は正常 |

## 回帰の有無

観点1〜5すべてOK、回帰なし。

## 範囲外検出

- ~~ズーム操作（Ctrl+ホイール合成）直後に選択セルが「未選択」に戻る現象を観測~~（`obs5-zoom150-select.png`取得前）。**殿による実機確認の結果、この現象は再現せず**（2026-07-05訂正）。今回自作したUI Automation経由の合成操作（`keybd_event`+`mouse_event`+`SetCursorPos`）固有の副作用であり、アプリの実挙動ではないと判断。実装側の要修正事項ではない。
- 検証中、アプリウィンドウがプライマリモニタからセカンダリモニタへ移動している事象を確認（原因不明、殿操作の可能性あり）。検証はウィンドウ位置を都度再取得して対応、問題化せず。

## 使用ヘルパー

- 既存 `ecad2-ui-automation` スキルの標準ヘルパー一式。
- Ctrl+ホイール合成ヘルパーを本セッションscratchpadに再作成（前任忍者作成分は消失前提、引き継ぎ記載どおり）。スキル化は殿承認済み（2026-07-04）・未実施のまま。機能要件：`keybd_event`でCtrl押下→`mouse_event`でMOUSEEVENTF_WHEEL送信→Ctrl解放。
