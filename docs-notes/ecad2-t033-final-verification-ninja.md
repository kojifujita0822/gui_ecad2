# T-033 最終実機確認（増分3+4+Tab除外まとめて、忍者）

> 対象コミット `025e8d6`（増分4、種別選択シンボル化）+`77c2f81`（増分3、拡張表示ボタンTab除外）。
> 隠密レビュー `docs/archive/ecad2-t033-review-onmitsu-7.md`（クリーン、これが通ればT-033完全Done）を受けての
> 最終実機確認。

## 結論：**全観点OK**

| 観点 | 判定 |
|---|---|
| (1) 増分3: 拡張表示ボタン配置・押下無反応・Tab除外 | OK |
| (2) 増分4: 種別選択シンボル表示（閉/展開）・UIA名・種別切替→実配置 | OK |
| (3) 回帰: バー位置セル直下・配置確定→機器表反映・Esc | OK |

---

## 観点1: 増分3（拡張表示ボタン）

- **配置**: キャンセルボタンの右に「拡張表示」ボタンが表示されることを確認。
  証跡: `docs-notes/images/t033-final2-bar-closed-ninja.png`
- **押下無反応**: `IsEnabled=True`（隠密申し送り通り、UIA上は有効だが実処理未実装＝正しい仕様。
  誤診断しないよう注意）。押下してもバー・ステータスバーとも変化なし。
  証跡: `docs-notes/images/t033-final2-expand-noop-ninja.png`
- **Tab循環でスキップ**: `デバイス名 → OK → キャンセル → 種別選択(ComboBox) → デバイス名`（循環）で、
  拡張表示ボタンはTabオーダーから正しく除外されていることを実測確認。

## 観点2: 増分4（種別選択シンボル表示）

- **閉状態**: 種別選択ComboBoxがテキストなしでシンボル（a接点記号）のみ表示。
  証跡: `t033-final2-bar-closed-ninja.png`
- **ドロップダウン展開状態**: a接点・b接点・コイル・セレクトSW・端子台の5項目、すべてシンボルのみで
  表示（**殿目視用、セレクトSW新グリフの構図確定材料**）。
  証跡: `docs-notes/images/t033-final2-combo-open-ninja.png`
- **UIA名（正式名称）**: ComboBox自体のName="a接点"（選択中の項目名）、ドロップダウン各項目の
  ListItem.Nameも "a接点"/"b接点"/"コイル"/"セレクトSW"/"端子台" と正式名称で取得できることを確認。
- **種別切替→実配置**: セレクトSWへ切替（座標クリック、SelectionItemPatternが提供されずInvokePattern
  も不発だったため直接クリックで選択——UIA制約であり実装バグではない）→機器名「SW1」入力→OK確定
  →実際にセレクトSW記号（ダイヤ形）が配置され、機器表に「SW1 / Other」が反映されることを確認。
  証跡: `docs-notes/images/t033-final2-selectsw-chosen-ninja.png` / `-selectsw-placed-ninja.png`

## 観点3: 回帰

- **バー位置セル直下（増分2回帰）**: 行1・行2・行10いずれもセル直下に正しく表示。回帰なし。
  証跡: `t033-final2-bar-closed-ninja.png`、`docs-notes/images/t033-final2-row10-ninja.png`
- **配置確定→機器表反映**: 上記セレクトSW配置で確認済み。
- **Esc**: 行10で配置バー表示中にEscを押下→バーが正しく閉じ、セル枠のみ残ることをスクリーンショットで
  確認（文言だけでなく画面で裏取り）。
  証跡: `docs-notes/images/t033-final2-esc-ninja.png`

## 範囲外検出・不明事項

なし。

---

**総合所見**: 増分2（位置バグ）・増分3（拡張表示Tab除外）・増分4（種別シンボル表示）いずれも実機で
異常なし。T-033はこれにて完全Doneとして差し支えないと考える（セレクトSW新グリフの構図自体の最終確定は
殿目視待ち、`t033-final2-combo-open-ninja.png`を参照されたい）。
