# T-058増分2 実機確認記録（忍者、2026-07-14）

対象コミット: `5e1bbc5`（出力パネルAvalonDock化、案C）、`79a60b2`（タイトル空白バグ修正）。
2回の家老采配にまたがる検証記録（1周目でタイトル空白バグを発見、侍修正後の2周目で決着）。

## 1周目：出力パネルAvalonDock化（コミット`5e1bbc5`）

| 観点 | 判定 | 根拠 |
|---|---|---|
| (1) 新規作成・DRC実行・出力パネル動作の再現性確認 | OK | シート追加・DRC実行(0件警告)とも正常動作 |
| (2) 二重タイトル表示の解消 | **一部NG** | 二重表示という意味では解消されていたが、**タブタイトルが完全に空白**（文字が一切描画されない）という別の不具合を発見。拡大スクショ・UIA（Text要素のName空）の両方で確認 |
| (3) Ctrl+Alt+Rで両パネルの正常リセット | 未確認 | (2)の問題を優先し確認保留（家老了承） |
| (4) 検索バー(Ctrl+F)実行時のタイトル「検索結果」切替（重点観点） | **NG** | 殿実機操作で確認。検索バー表示後もタブタイトルは空白のまま。グリッド内容（`DataGrid.Visibility`）は正しく切替わるが、タブタイトル（`LayoutAnchorable.Title`のBinding）のみ機能していないことを確定 |

原因: `LayoutAnchorable`（AvalonDockの`LayoutContent`→`LayoutElement:DependencyObject`）は
`FrameworkElement`ではなく、`LayoutAnchorablePane.Children`もDependencyPropertyでないため
WPFのInheritanceContext確立経路を通らず、`Title="{Binding Find.IsVisible, ...}"`のDataContextが
解決できない（忍者は実機所見のみ報告、原因特定は侍・隠密による。コミット`79a60b2`の記述と符合）。

## 2周目：タイトル空白バグ修正（コミット`79a60b2`、Bindingからコードビハインド直接更新方式へ切替）

| 観点 | 判定 | 根拠 |
|---|---|---|
| (1) タブタイトルが「出力」と正しく表示 | OK | UIA（Text要素Name='出力'）+ スクショ視覚確認とも一致 |
| (2) 検索バー(Ctrl+F)実行時に「検索結果」へ切替（先の(4)NGの再確認） | **OK** | 殿実機操作（スクショ）で確認、出力パネルタブが明確に「検索結果」表示に切替、列ヘッダーも連動 |
| (3) 通常DoD再確認（新規作成・DRC実行・二重表示なし） | OK | 左パレット「シート」・出力パネル「出力」とも単一表示、二重表示なし |
| (4) Ctrl+Alt+Rリセット一言確認 | OK | 殿実機操作、ステータスバー「パネルレイアウトを既定に戻しました」表示を確認 |

## 総括

1周目で発見した致命的バグ（タブタイトル完全空白、検索結果切替も機能せず）は、2周目の修正
（Binding方式→コードビハインド直接更新方式）により完全に解消。全DoD達成、増分2は完全決着と判断。

範囲外の気づき: なし。回帰: 通常動作（シート追加・DRC実行）に影響なし。
