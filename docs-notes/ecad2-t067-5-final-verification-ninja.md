# T-067(5) 最終実機確認（忍者、2026-07-19）

## 経緯
2件の修正（(1)選択ハイライトのDashStyle対応、(2)rowInRangeガード対象外化）を経ての最終確認。
前段の検証記録は`ecad2-t067-5-contextmenu-verification-ninja.md`（初回、観点2誤NG判定含む）・
peerメッセージ履歴（誤NG判定の訂正経緯）参照。

## 判定サマリ（全観点OK）

| 観点 | 判定 | 所見 |
|---|---|---|
| (a) 選択中のまま線種変更が即座に見えるか | OK | 破線選択後、選択ハイライト自体が破線化され即座に反映（選択解除不要） |
| (b) 最下行付近の枠境界線での右クリック | OK | rowInRangeガード対象外化により、グリッド範囲外まで届く境界線でも正しく反応 |
| (c) 範囲外・枠なし座標（回帰確認） | OK | グリッド完全外側=無反応、グリッド内側・枠外=従来通り行操作メニュー、両方意図通り |

## 詳細

### (a) 選択中の即時反映
枠を右クリック→「線種」→「破線」選択→選択解除を挟まずCopyFromScreenで撮影。選択ハイライト
（オレンジ赤）自体が破線パターンで描画されており、変更が即座に視覚反映されることを確認
（証跡`t067-5-final-a-selected-dashed.png`）。

### (b) 境界ケース
ズーム40%でグリッド全体を表示し、下辺がグリッド罫線最下端（Y=459、`t067-5-boundary-frame.png`
参照）を超えるY=467まで届く枠を作成。この座標は前回検証で無反応だった同一座標——今回は
正しく「線種」▶「削除」メニューが表示された（証跡`t067-5-final-b-rightclick.png`）。一次ソース
確認（`MainWindow.xaml.cs` 1653行目）でも`HitTestFrame`がrowInRangeから独立して評価されるよう
修正されていることを確認済み。

### (c) 回帰確認
- グリッド完全外側（相対610,550、余白領域）を右クリック→メニュー無し、無反応（正常）
- グリッド内側・枠に触れない座標（相対700,380）を右クリック→従来通り「行4の前に行を挿入」
  「末尾に行を追加」「行4を削除」の行操作メニューが正しく表示（証跡
  `t067-5-final-c-gridinside-noframe.png`）

## 結論
T-067(5)は最終確定。全観点OK、回帰なし。

## 証跡ファイル
- `t067-5-final-a-selected-dashed.png`
- `t067-5-final-b-boundary-frame.png`, `t067-5-final-b-rightclick.png`
- `t067-5-final-c-outofrange.png`, `t067-5-final-c-gridinside-noframe.png`
