# T-079（P-058）実機再検証記録（忍者）

- 対象コミット: `2442882`（T-079修正、`fd67ed9`のT-065/T-066 隠密指摘4件修正の上に積む）
- 前回検証: `docs-notes/ecad2-t065-t066-verify-ninja.md`（③④がNG/保留だった回）
- 検証環境: `Start-Ecad2App`（セカンダリモニタ）

## 結論（観点別、DoD：①②③④全てOK）

### ①機器配置直後(配置ツールアクティブなまま)にCtrl+S押下しても機器表エントリが消失しないこと — OK

前回③NGの再現手順（シート追加→a接点配置→機器名確定→ツール切替せずCtrl+S）をそのままなぞって
確認。「名前を付けて保存」ダイアログが開いた状態のまま機器表を確認しても行が消えず（1行のまま）、
Escキャンセル後も正しく残っていることを確認。

### ②選択ツールに切り替えてからのCtrl+Sでも同様に消失しないこと — OK

2件目のa接点配置後、選択ツールへ切替→Ctrl+S。ダイアログ表示中・キャンセル後とも2行のまま
維持されることを確認。

### ③実際に保存完了(ファイル名入力→保存ボタン)させても機器表とキャンバスの整合が保たれること — OK

Ctrl+S→ファイル名入力(`t079test`)→Enterで保存完了。`sample/t079test.gcad`の実在を確認。保存後、
機器表はX1・X2の2行とも正しく残り、キャンバス上のラベル表示（X1/X2）とも一致（乖離なし）。

### ④型式セルを値を変えずEnter/Tabで抜けた場合、未保存マークが立たないこと — OK

①②③検証によりIsDirty判定が信頼できる状態になったため、今回検証可能と判断し実施：
1. 新規ドキュメント→a接点配置(X1)→型式に"AAA"入力・Tab確定→保存（IsDirty=falseへリセット）。
2. 型式セル("AAA")を再度ダブルクリックで開き、値を変えずTabで抜ける。
3. 「新規作成」を実行し、未保存確認ダイアログが**出現しないこと**を確認（IsDirty=falseのまま
   ＝MarkDirty不発）。

### ⑤型式列編集(A/B)・行追加削除不可(A)・ドキュメント情報ダイアログ(⑤⑥)に新たな崩れがないこと — OK

- 型式列編集・機器名/種別列編集不可は①②の検証過程で従来通り機能していることを確認。
- グリッド最下部の新規行プレースホルダなし・行削除不可も従来通り。
- ドキュメント情報ダイアログ：8フィールド構成（社名/タイトル/図番/客先/設計者/製図者/検図者/日付）
  健在、Revisions欄なしを再確認。社名編集→OK確定→再オープンで値が反映されていることを確認。

## 総括

T-079修正（`NotifySelectedElementChanged()`呼び出し追加）により、前回検証で発見した③の重大な
機器表エントリ消失バグは解消を確認。①〜④全てOK、DoD達成。

## 不明点

- なし。

## 範囲外の気づき

- なし。
