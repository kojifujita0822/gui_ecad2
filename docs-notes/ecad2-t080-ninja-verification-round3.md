# T-080追加往復 最終実機確認記録(忍者)

検証日: 2026-07-12
対象コミット: `924a7c4`(手元main、push無し。侍修正`516a81d`+テスト`924a7c4`、
隠密再レビュー`docs/ecad2-t080-issue2-3-fix-review-onmitsu.md`ブロッキングなし)
ログ: `%TEMP%\ecad2-diag.log`(固定・追記式、引き続き使用)
前提: 最新mainで`dotnet build`から建て直し。制御回路シート(シート1)にa接点・コイル・縦分岐線を
配置して検証環境を構築(行数40に拡張、スクロール/ズーム/リサイズ観点用)。

---

## 観点別結果

### 観点1: 課題2本丸-縦コネクタ選択保持(最重要) — OK

縦コネクタを選択状態(赤ハイライト)にしたまま、殿が制御回路シートの右母線右側を物理ダブルクリック。
殿の直接目視証言:「エディタ開いた、赤ハイライトは保持された」。ログでも`OpenRungCommentEditor`が
正しく発火し、その後のクローズ(チャット復帰による非アクティブ化、想定内)まで正常に確認。
**前回NGだった課題2の本丸は修正されている**。

### 観点2: 課題2続き-断ちすぎ無しの確認 — OK

- コメント域への単発クリック(縦コネクタ選択中): 選択状態(赤ハイライト、「操作対象: 始点」表示)は
  維持され、`SelectedCell`も変化しないことを確認(ログ`HitTestRungCommentRow=0`のClickCount=1で
  選択状態不変)
- コメント域外の通常グリッドセルクリック: 縦コネクタの選択が正しくクリアされ、新しいセルが選択
  されることを確認(従来どおりの動作、断ちすぎなし)

### 観点3: 課題3-実機5観点(スクロール/ズーム/リサイズ) — OK(全5観点確認)

1. **水平スクロール左端のままF2**: エディタがキャンバス可視域内(右母線右側)に正しく表示される
   ことを確認(修正前はウィンドウ右端の機器表パネル付近に描画され見えなかった)
2. **水平スクロールされた状態**: 引き続き正常表示(退行なし、観点1で確認済みと同一条件)
3. **垂直方向(行35、シート下端付近)**: 行数を40に拡張し、下端付近でも可視域内に正しく表示される
   ことを確認
4. **ズーム率150%**: 可視域内に正しく表示されることを確認
5. **ウィンドウリサイズ(900x500に縮小)**: リサイズ直後もプロセス生存・エディタが可視域内に正しく
   表示されることを確認

### 観点4: 要注意3目視-スクロールバー分の位置ズレ — 経過観察(所見のみ)

ウィンドウ縮小時(900x500)、`RungCommentBox`のBounds(X=565〜705)と垂直スクロールバーのBounds
(X=691〜708)を比較したところ、**エディタ右端がスクロールバー左端と約14px重なる**ことを数値で
確認した。隠密指摘の要注意3(`ActualWidth`がスクロールバー込みのサイズであること由来の軽微な
視覚ズレ)の予想どおり。課題3本体(画面外に完全に消える不具合)は解消済みであり、これは際どい
ケースでの軽微な重なりに留まる。ブロッキングとはせず経過観察として記録する。

### 観点5: 回帰(ダブルクリック起動・F2・Esc/Enter/Tab・空文字削除) — OK

- ダブルクリック起動: 観点1で確認済み(OK)
- F2: 複数回(row=0〜3)確認、いずれも正常(OK)
- Esc取消: 文字入力後Escでキャンセル→`CommitRungCommentEditor`ログなし、コメント反映されず(OK)
- Enter確定: 文字入力後Enterで確定→`Commit`→`Close`→フォーカス復帰、見た目にも正しく反映(OK)
- Tab確定: 同様に正常動作を確認(OK)
- 空文字確定=削除: 既存コメントを全選択削除しEnterで確定→コメントが削除され、他行のコメントは
  保持されることをスクリーンショットで確認(OK)

---

## スクショ所見

観点1〜5を通じ、ハイライト・コメント表示・ステータスバー文言に矛盾は見当たらなかった。

## 回帰

観点5で確認、いずれも回帰なし。

## 範囲外検出

特になし(前回round2で発見した課題2・3は今回の采配で解消確認済み、要注意3は上記観点4で経過観察)。

## 検証環境

- Ecad2.Appプロセス: 最新main(`924a7c4`)で`dotnet build`から新規起動
- シート: 新規プロジェクトへ「シート1」(制御回路、a接点+縦分岐線+コイル配置、行数40に拡張)を作成
- 検証終了後、侍の次回ビルドに備えEcad2.App.exeは終了予定
