# T-082 シート並び替え機能 忍者実機検証記録

検証日: 2026-07-13（殿ご帰還後、往復3周+隠密事後レビュー通過を受けての最終防波堤）
検証者: 忍者
対象コミット: 043bc1f（往復3周目修正）+ b5f5db1（レビュー完了記録）

## 前提・準備

- Ecad2.App再起動（旧プロセス消失、クラッシュログ`ecad2-crash.log`は空・原因不明だが再現なし）
- 新規文書作成、シート4枚（シート1〜4、自動採番名）を用意
- キーボードショートカット自体の検証（Alt+上下）・ドラッグ&ドロップ（座標クリック系操作）は
  `ninja.md`のMUST運用に基づき殿に操作を代行いただいた。忍者はUI Automation経由の非占有操作
  （ボタン押下・要素選択・ステータスバー読取・スクリーンショット）で結果を観測した。

## 観点別結果

| 観点 | 判定 | 根拠 |
|---|---|---|
| (1) ドラッグ&ドロップ並び替え | OK | 殿操作でシート4を一覧先頭へドラッグ、UI・保存ファイル双方で反映確認 |
| (2) Alt+上下キー並び替え | OK | 殿操作でシート1をAlt+↓×3で末尾へ、Alt+↑×3で先頭へ復帰、正常動作 |
| (3) 両操作ともDocument.Sheetsへ反映 | OK | 保存JSON（`sample/t082-ninja-test.gcad`）の`sheets`配列順序がUI表示と一致 |
| (4) 保存/読込で順序保持 | OK | 保存→「開く」で再読込、順序（シート4,1,2,3）が完全一致 |
| (5) PageNumber再採番 | OK | JSON上で`pageNumber`が新しい表示順に対応し1,2,3,4へ再採番済み |
| (6) 端（先頭/末尾）での移動 | OK | Alt+上下の境界（4回目押下）で何も起きずCanExecuteガードが機能 |
| (6) シート1枚時 | **未実機確認** | `CanMoveSheet`の`Sheets.Count > 1`ガード（コード確認済み）と端でのno-op実証からの類推にとどまる。単独シートでの直接実機確認は省略 |
| (6) 保存読込往復 | OK | (4)と同一 |
| (a) Alt+上下の実機反応 | OK | 殿操作で正常に反応（「問題なかった」） |
| (b) ドラッグ視覚表現 | OK | カーソル変化・挿入位置の挿入線・ドラッグ元半透明化の3点、殿確認で「問題なかった」（スクショでも該当行のハイライト帯を確認） |
| 重点観点：移動対象=選択中シート自身での選択セル保持 | OK | シート1選択中・選択セル(行1/列0)ありの状態でAlt+上下を4往復させても選択セルが「未選択」化せず保持 |
| P-069：連続Alt+下での選択ハイライト瞬間消失 | 発生せず | 殿目視で「問題なかった」。理論的懸念は現状再現せず |
| DRC連動 | OK | CR1配置→DRC実行で診断発生→ドラッグ並び替え→「DRC結果が削除されました。DRC再実行してください。」表示+出力パネルクリアを殿操作・スクショで確認 |

## スクショ所見

`t082-setup.png`（初期4枚）・`t082-after-altkeys.png`（Alt往復後、選択セル行1/列0保持を確認）・
`t082-after-drag.png`（ドラッグ後、シート4先頭・CR1配置・DRC削除案内）・`t082-after-reload.png`
（再読込後、順序一致）のいずれもステータスバー・機器表・シート一覧表示に矛盾なし。

## 回帰の有無

なし。4シート運用・要素配置・DRC実行・保存/読込いずれも既存機能が正常動作。

## 範囲外検出の有無

なし。

## 保留・不明事項

- シート1枚時の並び替え試行（no-op確認）は実機で直接確認していない。コード上のガード
  （`SheetNavigationViewModel.cs:262 CanMoveSheet`の`Sheets.Count > 1`）と、4枚シートでの
  端到達no-op動作の実証からの類推で「安全」と考えるが、断定はしない。
