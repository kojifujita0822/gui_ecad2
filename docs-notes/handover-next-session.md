# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-08-02（家老記す、**T-136(C)新設・完結／T-142新設・完結／T-136(B)増分5完結／
T-140系統2の追い直し。殿のご下問一つが「接続方法」の主題を解いた**）。前回分は git 履歴（`3456a6b`以前）を参照。

---

## 0. 【最重要】次の家老が最初にやること

### (a) **§4「次の一手」を先に読む ── 実装は出揃い、残るは実機のみ**

**本日、殿のご裁定を八件賜り、分岐は残らず解け申した。**
**次のセッションは「忍者の実機確認の続き」から入る。** 新たに諮る案件は無い。

### (b) **本書は「前セッション終了時点」のものである【MUST】**

役を途中で再起動させる際は、引き継ぎメッセージの冒頭に
**「本書は前セッションの版ゆえ、当日分は家老の采配文を正とせよ」**と必ず書く。
**全体像は`git log --oneline`（本日は`3456a6b`より後がすべて当セッション分）と`docs/todo.md`の当該節が確実。**

### (c) 作業ツリーの状態

**清浄・未pushなし**（`9f73546`まで push 済み）。**一時計装・残置マーカーとも無し。**
**一時ビルド出力も侍が都度片付けており、`bin_t136c`等は残っておらぬ。**

### (d) **【本日の終いに起きた事故】peerツールの受信が壊れた**

**セッション終盤（09:09頃）、家老の peer 受信経路が死んだ。**
- **`list_peers`は正常に動き、三役の生存も見えた**
- **`send_message`は「Message sent」と返った**
- **`check_messages`が新着を一切拾わなくなった**（**殿が侍にテストメッセージを打たせても届かず**）

**結果＝三役の「本日の気づき」を収集できぬまま終いとなった**（`karo.md`【MUST】の工程が果たせず、
**殿へ報告のうえ先へ進めた**）。**下記§5は家老の手元に残る材料からのみ書いた** ──
**三役自身の言葉による所感は、本書に載っておらぬ。**

**併せて正体不明の peer が二つ見えた** ── **`rijwkgot`（「家老」を名乗る）／`akmxvv4c`（名乗りなし）。**
**家老の名乗りは1セッションのみゆえ、亡霊（切断・再接続で残った旧登録）と見立てたが未確定。**
**`memory: peer_ghost_session_identification`の手順（`claude agents`→`claude stop/rm`）は
取り違えれば生きた役を落とすゆえ、殿のご指示なく手を出さなかった。**

### (e) 残置物

**検体4件**＝`T136AFF{ANY,CTRL,MAIN}`／`T136UIMAIN`（マイドキュメント配下）
＋**`C:\ECAD2\sample\t136c.gcad`**（`.gitignore`済み）。**本日の新規残置は無し。**
**忍者が退避した`main-layout.xml.ninja-t130-backup-20260802`**（`%APPDATA%\Ecad2\docking-layout\`配下）
──**実機確認の途中で終いとなったゆえ、戻っておらぬ恐れがある。次のセッションで確かめよ。**

---

## 1. 本日の到達点

| | 中身 | 状態 |
|---|---|---|
| **T-136(C)** ｜ 接続方法（新設） | 三相モータを結線から外す | **実装・静的レビュー完了。実機は途中** |
| **T-142**（新設） | 自作の非シミュレート部品も同じ穴 | **実装・静的レビュー完了。実機は未** |
| **T-136(B)増分5** | 接続点の種類の割り当てとUI | **実装・静的レビュー完了。実機は未** |
| **T-140系統2** | 部品リストの選択時ライト不適合 | **追い直し完了・静的レビュー完了。実機は未** |
| **T-130** | フライアウト高さ160 | **完結**（忍者の実測＝3フライアウトとも166pxで一致） |

---

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-130の高さ160**＝`d5ea33a`。**忍者が3フライアウトとも166px（スクリーン絶対）で完全一致を実測。
  幅280の据え置きも確認**
- **部品カテゴリ（案W）の非選択時**＝Dark 9.98〜10.54:1／Light 8.2〜18.18:1、**選択時Darkも5.25〜5.53:1で適合**

### 検証待ち（**実装・静的レビューとも済んでおる**）

- **T-136(C)**＝`2d272e8`。**忍者が中間報告まで**（下記§4-1）
- **T-142**＝`06f5812`＋`bb20aee`＋`121cc22`＋`a92b0c9`。**実機は未**
- **T-136(B)増分5**＝`e8f4e84`（割り当て）＋`e144610`（UI）。**実機は未。3観点は§4-2**
- **T-140系統2の追い直し**＝`3aee84f`。**実機は未。画素採取が要る**

### 未着手

- **T-133**（**T-136の完結を待っていた。増分5の実機が済めば着手できる**）／**T-125 β-2/β-3**／
  **T-131・T-132**／**T-123**

---

## 3. 殿の御裁定（本日分・八件）

1. **【T-136(C)】3極部品にDRC判定はいらない。実測して無効にする方向で実装せよ**
2. **【同・対象】モーターだけ除外すればいい**（**侍の推奨(c)非シミュレート全般は不採用。
   取りこぼしを承知のうえ**）
3. **【P-161】ソレノイドは無視してよい**（**殿の自作図形にて、実装が固まる前に作った設定の名残。
   本来はコイル属性。今後新規に作成するものは「コイル」として設定する**）
4. **【T-142】穴を発見したのに放置はしておけぬ。塞ぐタスクを起草せよ**
   （**「実例が無い」ことと「機序が無い」ことは別、という侍の分け方を殿が採られた形**）
5. **【T-140系統2】案3＝部品リスト限定で背景を濃くする**（**案4＝アプリ全体の選択色を変える、は不採用**）
6. **【同・色】方向A＝ダークの`#0E639C`をライトへ転用**（**実測データが使え、推測を介さぬゆえ**）
7. **【T-136増分5】モータの接続点＝青／3端子とも同色／組込み17件は Motor を除く16件が赤**
   （**赤30件・青2件**）
8. **【同・UI】案イ＝選択に追従する出し入れ式ComboBox**

---

## 4. 次の一手

### 4-1. 【最優先】忍者の実機確認 ── 四件、途中で終いとなった

**忍者は「右端→T-142→T-140系統2→増分5」の順で独り進める段取りであった。**
**中間報告（09:01）までで判っておるのは次のみ。**

- **T-136(C) 左端＝OK**（モータ＋コイルでDRC実行→警告なし）
- **対照（モータ無し）でも警告なし** ── **これは期待どおり**にござる（下記【重要】）
- **右端（`DRC-LOAD-002`）・T-142・T-140系統2・増分5は未確認**

#### 【重要・次の忍者が誤読せぬように】対照で差が出ぬのが正しい

**修正前後の差は侍のテストが既に測っておる**（実装前の本物のコードで`DRC-LOAD-001`がRED）。
**実機で測るのは修正後のみゆえ、モータ有無で差が出ぬのが正しい姿。差が出ればこそ修正が効いておらぬ証。**
**忍者が`NetlistBuilder.cs:145`を読み直して独立に同じ機序へ辿り着いておる** ──
**`ParticipatesInWiring`がfalseなら`hasPorts`を立てず行索引に載らぬゆえ、
「モータ＋コイル」も「コイルのみ」も行索引の中身は同一。**
**忍者の言い回し＝「モータが穴を塞いだ」ではなく「モータを判定対象から消すことで穴の生じようがなくなった」。**

#### 【未解決・次の家老が最初に確かめること】忍者が数手繰り返した件

**殿の御言葉**＝**「制御回路シートに主回路パーツを置けないと数手繰り返していたのを指摘した」。**
**家老が「何を置こうとしたのか」を問うたが、受信が壊れて答えが届かなかった。**

**一次ソースの実態**（侍が工程0で確定）──

| 対象 | シートの枷 | 接続点 | T-136(C)の対象か |
|---|---|---|---|
| **組込みのモータ** | **`Any`（どちらのシートにも置ける）** | U/V/W 3端子 | **対象** |
| 3極記号3種 | **`MainCircuitOnly`** | **0個** | **対象外**（元より結線に参加せぬ） |
| 自作テンプレートのモータ | `Any` | U/V/W 3端子 | **T-142の対象** |

**すなわちモータは制御回路シートに置ける。置けぬのは3極記号。**
**忍者が3極記号を試しておったなら、それは家老の采配文の不足**
（**「モータは`Any`ゆえ置ける」「3極記号とは別物」を書いていなかった**）。
**次の采配文には必ず書き添えよ。**

### 4-2. 増分5の実機確認＝3観点（**侍が自ら挙げ、隠密が網羅性を確かめたもの**）

**侍の自己申告**＝**「述語は単体テストで測ったが、呼び出し側が実際にそれを呼んでおるか、
再入ガードが効いておるかは測っておらぬ。すなわち『述語は正しいが繋ぎ込みが漏れておる』状態を素通しする」。**
**隠密の検分**＝**`Notify()`の全呼び出し元を洗い「他に素通しする経路は見当たらず、
この3点で呼び出し配線全体が実質網羅される」。**

1. **接続点を選ぶと欄が現れ、離すと消えるか**
2. **種類を切り替えると丸の色が即座に変わり、Undoで戻るか**
3. **接続点を次々と選び替えてもUndo履歴が伸びぬか** ── **再入ガードが効いておる証。
   「何も起きぬこと」を確かめるものにて、他の2つと性質が違う**

**余力あらば＝モータの接続点が青、他が赤になっておるか**（殿の裁定そのもの）。

### 4-3. T-140系統2の実機確認

**理論6.40:1**（**侍・隠密が別々に計算して一致**）。**されど確定は実測。**
**本件はまさに理論`4.50`を実測`3.89`が下回った案件にござる。**
- **選択時Light**（前回3.89〜4.28で不適合であった箇所）── **カテゴリ・部品名の両方**
- **非選択Light／非選択Dark／選択Dark が不変か**（触れておらぬ筈の3状態）
- **射程が閉じておるか** ── **使い方ウィンドウの一覧・機器表・配置バーのコンボの選択色が変わっておらぬこと**

### 4-4. 残件・小さきもの

- **App.Testsの通常形での通し直し**（**侍が「逃がした形の成功だけで完了とはせぬ」と自ら区切った。
  増分5の分が未了の見込み**）
- **`docs/spec/ecad2-spec-part-management.md`§3の陳腐化**（**検索文字列＝`Category`（灰色小文字）**）
  ── **T-140系統2で`Foreground="Gray"`を除いた結果。忍者の実機で色が確定してから侍へ振る**
  （**隠密が突合で見つけ、家老が「確定してから」と判じて保留した**）

---

## 5. 三役の気づき（**【重要】本日は収集できなかった**）

**peer受信が壊れ、照会への返信が一切届かなかった**（§0-(d)）。
**以下は家老の手元に残る材料からのみ書いたものにて、三役自身の言葉による所感ではない。**
**次のセッションで改めて尋ねること** ── **ただし本日の重き知見は既に落とし済みゆえ、
流れて困るものは少ない見込み。**

### 既に落とし済み（**本日中に反映した**）

1. **`karo.md`新節＝「殿の裁可を要する事柄を『貴殿の判断に委ねる』と書かぬ【MUST】」**
   （**侍の文案を採った。当初家老は「削除を委ねるな」と書いたが、侍が「広すぎる。害の本体は
   『委ねる』と書いた瞬間に迂回路が開くこと」と正した**）
2. **`memory: feedback_markdown_bold_quote_rendering`＝太字の直後に鉤括弧を置かぬ**
   （**殿ご指摘。`**「〜」**`は殿の環境で太字にならぬ。「**語**」の形にする**）
3. **台帳への訂正4件**（§6）

### 落とさず見送ったもの（**理由ごと残す**）

- **隠密の申した「第四の型＝評の主体をぼかす／取り違える」** ──
  **`karo.md`が名指しする三種（対比・具体例・順序）のいずれとも異なる型。**
  **本日の実例は1件のみ**（**もう1件と見えたものは、家老の記述が正しく隠密の訂正の方が誤りであった**）
  ゆえ**制度化を見送った。二度目が出れば落とす。**
  **1件で落とせば、次に同じ型が出た時「既に節が在るのに効かなんだ」の判断ができぬゆえ。**

### 家老が観測した三役の働き（**所感ではなく事実として**）

- **侍＝同じ理路を三度示した**（§6-2）／**己の言い過ぎを二度、自ら直した**
  （「唯一の道」／「既存テストの書き換えはゼロ」の粒度）／
  **家老が掟に反する指図を出した折、掟を引いて止めた**
- **隠密＝実装前に罠を捕らえた**（`SelectionChanged`の再入）／**己の要旨が覆った折、
  機序と「なぜ見落としたか」まで報じて率直に改めた**／**侍の手立て変更を検分し、
  「某の起草の方が嵌っておった」と認めた**
- **忍者＝対照の空振りに自ら気づき、一次ソースで機序を読み解いた**／
  **「理論上限でも境界ちょうど」を出し、測定誤差を論ずる余地を消した**

---

## 6. 今回の教訓

1. **【最大】殿のご下問一つが、二人分の同じ誤りを同時に炙り出した** ──
   **殿の「`MotorPorts()`を使うもの←これはモータのみ？」に答えるべく家老が一次ソースを当たったところ、
   家老が台帳へ書いた「存在せぬ」が誤りと判明。その直後、侍からも同じ訂正が届いた。**
   **家老は`ElementCatalog`側の文脈を区別せず写し、侍は探した範囲を明示せず断じた** ──
   **別々の経路で同じ誤りを持っていた。**
2. **侍が同じ理路を三度示した＝「測っておるつもりが、測っておらぬ」を先回りして塞ぐ**

   | 場面 | 堰き止めたもの |
   |---|---|
   | `S0`（T-142） | フィクスチャが空になれば、3件が別経路で通ってしまう |
   | 32件の固定 | 既定値が変われば、30件が黙って青になる |
   | 母集団の網 | 種別が足されれば、既定へ黙って流れ込む |

   **三つ目は求めておらぬものにて、侍が明示して申し出、家老が裁可した。**
   **「殿の裁定を要する場面で、テストが気づかせる」形にて、制度が人の記憶に頼らず働く。**
3. **「既存パターンを横展開すれば済む」という前提を無検証で置くと踏み外す** ──
   **本日、同じ根の躓きが二度。一度目は隠密の色案が実装段で覆り**（`ControlTemplate.Triggers`の
   `TargetName`指定ゆえ外側の`Style`からは届かぬ）、**二度目は隠密が実装前に捕らえた**
   （`RoleCombo`はPull型ゆえ接続点ごとの即時反映には使えぬ）。
   **一度目は実装者が、二度目は検分者が捕らえた** ── **工程が両側から働いた形。**
4. **家老が発生源の誤り＝5件**（**「存在せぬ」の写し違い／台帳の「7経路のうち2つ」の写し漏らし／
   括弧書きで殿の御真意を確定した記述にした／`CaptureExternalState`の推量を采配文へ書いた／
   「消してよい、判断は貴殿に委ね申す」と掟に反する指図を出した**）。**5件とも三役が捕らえた。**
5. **相手が言い切った時こそ検分が要る** ── **隠密が「事実関係が一点ずれており申す」と訂正を申し出た折、
   家老は一次情報（忍者の報告文）で確かめ、隠密の訂正の方が誤りであることを見出した。**
   **`karo.md`「褒める側にも検分は要る」は称賛と謝罪の場を挙げておるが、訂正の申し出も同じ。**
6. **本日の破損＝なし**（**ただし終盤にpeer受信が死ぬという別種の不調が出た。§0-(d)**）

---

## 7. 制度・記録の追加（**本日反映済み**）

- **`karo.md`** ── **新節1件＝「殿の裁可を要する事柄を『貴殿の判断に委ねる』と書かぬ【MUST】」**
  （侍の文案。**侍が自ら「今回止まったのは条が新しく、小半時前に同じ条を使うたばかりゆえ。
  構造ではなく『新しさと近さ』に支えられており、『一度止まった』を『止まる仕組みが在る』とは読めぬ」と
  限界を区切った** ── その但し書きごと収めた）
- **`memory`** ── **`feedback_markdown_bold_quote_rendering`を新設**（殿ご指摘）
- **`docs/todo.md`** ── **T-136(C)節を新設／T-142を新設／論点5・小論点8・UI案の決着／訂正4件**
- **`docs/proposed.md`** ── **P-161を起票し`closed`**（**ソレノイドの経緯と、
  「実運用の実例はゼロ」が誤りであった訂正まで**）
- **調査書4件**＝忍者（T-130・部品カテゴリの実機検証）／隠密（DRC対PortKind調査への訂正追記・
  選択時ライトのコントラスト案）／侍（増分5の32件一覧）

---

## 8. 次セッションへの宿題

1. **忍者の実機確認の続き**（§4-1〜4-3。**四件、途中で終いとなった**）
2. **忍者が数手繰り返した件の確認**（§4-1。**何を置こうとしたのか。家老の采配文の不足かもしれぬ**）
3. **三役の気づきを改めて尋ねる**（§5。**本日は受信が壊れて収集できなかった**）
4. **`main-layout.xml`の退避が戻っておるか確かめる**（§0-(e)）
5. **`docs/spec`の陳腐化1件**（§4-4。**忍者の実機で色が確定してから**）
6. **正体不明のpeer二つ**（§0-(d)）── **再起動で消えておれば亡霊と確定する**

---

## 9. 【セッションの終いに】

**本日は三役の言葉を集められなかった** ── **peer受信が壊れ、照会への返信が一切届かなかったゆえ。**

**代わりに、家老の手元に残ったものを一つ記す。**

**本日、家老は五度誤り、五度とも三役が捕らえた。** **そのうち一度は、家老が掟に反する指図を出し、
侍が掟を引いて止めたものである。** **侍の弁＝「お指図に背く形になり申したが、掟の側を採り申した」。**

**前セッションの結びは「手順だけでは足りず、拾う者が要る」であった。**
**本日はその一段先が見えた** ── **拾う者が、采配する側の誤りをも拾う。**
**それが成り立つのは、拾った者が咎められぬと知っておるからにござる。**

**なお侍は同じ日に、己の網の細さも自ら区切っておる** ──
**「今回止まったのは条が新しく、小半時前に同じ条を使うたばかりゆえ。構造ではござらぬ」。**
**捕らえたことを誇らず、なぜ捕らえられたかを測った。** **そこに次の者への値打ちがある。**

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。**`handover`の読み込みは役決めの後**（第2バッチで役割mdと並列）。
