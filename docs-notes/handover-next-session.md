# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-25〜26（家老記す、**T-068完全決着（増分0〜3-c）・T-124 v0.5リリース完了・
T-125（App層全体の見直し）起票と隠密の構造調査第1段まで**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**本日のコミットはすべてpush済み**（最終＝`45a70bc`、gitタグ`v0.5`も付与・push済み）。次回セッション
開始時、家老は念のため`git log origin/main..HEAD`で混入チェックを行う。**セッション末に家老が
docs・役割md・スキルを更新した分が未コミットで残っている可能性があるため、`git status`も確認すること。**

**検証用一時ファイルは片付け済み**（殿裁可2026-07-25）。計13件を
`C:\ECAD2\sample\t068-verification-parts\`へ**移動**した（`Move-Item`使用＝消さずに移すのみ、
`rm`禁止【MUST】遵守）。`図形\自作\`は空になり、**殿の実運用パーツライブラリ（`図形\`直下15件）
には一切触れていない**。次回必要になれば移動先から戻せる。

---

## 1. 目的とDoD

前回の「次の1手」（T-068増分3-cの決着＝残欠陥のWチェックと忍者再検証の残り）を引き継ぎ、
**T-068を完全決着させ、殿の指示によりv0.5リリース（T-124）まで到達**した。さらに殿の新規指示で
**T-125（App層全体の見直し・リファクタリング）を起票**し、隠密の構造調査第1段（γ・δとも）まで
進めた。

殿は途中で外出され「続きは任せた」との仰せ。**判断を要する分岐は【報告】として記録し、家老の
裁量で進められる分は進める**方針で運用した。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-068 完全決着（増分0〜3-c）**。増分3-cの残欠陥は下記3節1の経緯を経て「保存時のみクランプ」
  （殿裁定＝案3）で決着。忍者の実機確認で本修正3観点＋従来観点3・4・7・8すべてOK。
- **T-124 v0.5リリース完了**。csproj `0.4.0`→`0.5.0`、フレームワーク依存publish 2箇所
  （bin配下＋`C:\ECAD2\version\v0.5`）、忍者の実機実測で起動・終了正常＋
  `FileVersion=0.5.0.0`/`ProductVersion=0.5.0+6ab8e07f49277cb...`確認、gitタグ`v0.5`付与・push済み。
  **`ProductVersion`にコミットハッシュが入るため「どのコミットから作った配布物か」が追跡できる**
  （侍の気づき、以後の報告に書き添える値打ちあり）。
- **T-125 隠密の構造調査 第1段完了（γ・δとも）**。調査書3件push済み：
  `docs/ecad2-t125-app-layer-structure-survey-onmitsu.md`（全体）／
  `docs/ecad2-t125-vm-clustering-survey-onmitsu.md`（γ＝VM側）／
  `docs/ecad2-t125-view-clustering-survey-onmitsu.md`（δ＝View側）。
  - **最重要＝`MainWindowViewModel.cs`＋`MainWindow.xaml.cs`がT-045完了時3,002行→現在7,527行
    （約2.5倍）**。T-045の整理効果は16日間の機能拡張に吸収された。
  - **`Window_PreviewKeyDown`が単一メソッドで607行**（キャンバスマウス3メソッドと併せ4メソッドで
    1,170行＝`MainWindow.xaml.cs`全体の約28%）。**キーボードファーストという本プロジェクトの理念の
    中核が、構造としては最も脆い**。
  - **T-045成果の明暗**：【明】`ConfirmDrag<T>`/`CancelDrag<T>`が7種へ展開・定着（当時は
    「PoCのみ・展開見送り」だった）、`ForceCancelIfAny`4種→7種、`IDispatcherService`は再発ゼロ。
    【暗】**`ValidatePlacement`がWireBreak/ConnectionDotへ16日間未適用**（T-045増分C計画時に
    「着手時に家老へ確認」とされたまま誰も確認せず流れた＝家老の落度）、部品種別弁別4箇所重複、
    `Sheet.Elements`不変条件保証も未着手。
- **`proposed.md`のpending棚卸し完了**（隠密、`docs/ecad2-proposed-pending-review-20260725-onmitsu.md`）。
  pendingは26件→21件（裁可反映3件・埋もれ是正2件）。

### 実施したが未検証 / 判断待ち

- **【最優先の判断事項】P-129＝保存時クランプが新たな重複生成経路になっている**（3節2参照）。
- **T-125の増分α〜δの実装**——殿裁定は下りている（下記4節）が、**侍の計画起草はまだ**。

### 未着手

- **T-068増分4（インポート/エクスポート、低リスク）**：優先度低め。P-126（原本の「組込み図形を
  読み込み」「開く」ボタン）を設計時の検討事項に含めること（殿裁可済み）。
- **T-123（正式リリース準備・インストーラー化、Inno Setup 7.0.2）**：T-068完了後着手とされていた
  条件は満たされた。着手時期は殿の判断待ち。
- **T-122（ドロップゾーンオーバーレイの表示条件・判定範囲見直し）**：Approved、着手条件
  （T-102完了後）は既に満たされている。

## 3. 試して失敗したアプローチと結果（今回の教訓）

1. **【最大の教訓】「欠陥」と断じた前提そのものが誤りだった**——忍者が「`heightCells=1`のパーツに
   `RowOffset=-2`」を発見→侍・隠密が独立に「setterにクランプが無い」と原因特定（Wチェック一致）
   →修正→隠密が副作用（Undo復元時の中間状態で誤クランプ）を発見→**侍が2周目で原本を確かめ、
   原本も枠変更時・保存時ともクランプしていないと判明**。**三者とも一次ソースで正確に働いたが、
   誰も原本を見なかった。** 結果、修正1周・revert1回・殿への上申1回を要した。
   - **制度化済み**：`karo.md`へ「**『欠陥』と判定する前に原本・設計意図の確認を指示する【MUST】**」、
     `samurai.md`のPR-04節へ項目6「**『漏れ』と断ずる前に、それが意図的な不在かを確かめる**」。
   - 侍の総括＝**「踏襲したはずのものに*無い*処理を『バグ修正』として足そうとしておる。これは
     筋が通らぬ」**。`memory: feedback_verify_design_intent_before_quickfix`の「一次ソース」は
     **踏襲元の原本まで含む**と読み広げる（侍の提起）。
   - **同日2件目**（増分3-b2でも侍が「開発者向け」と見なしたステータス表示が実は原本のユーザー
     向けガイダンスだった）。
   - **原本を見た引き金は家老の「俯瞰の問い」**（侍いわく「自発ではござらぬ」）。`karo.md`の
     モグラ叩き検知節へ「**俯瞰の問いには『原本の確認』を明示的に含める**」を追記済み。
2. **【次回最優先の判断事項】決着直後に新論点＝P-129**：**`ClampPortsToFrame`は重複チェックを
   持たぬため、新たな重複生成経路になっている**。「BoundaryOffset同一・RowOffset違い」の接続点
   2個を持つ部品の枠を縮めて保存すると完全同一座標へ収束（忍者が実機で確認）。
   **隠密判定＝「原本に無い新しい穴」**——原本はクランプ処理自体を持たぬ。既知の分岐F
   （ドラッグ移動時の重複、原本由来・殿裁定で踏襲）は**意図的に重ねた場合のみ**生じるのに対し、
   本件は**枠を縮めて保存するだけで意図せず**生じる（**発生の意図性という軸で性質が異なる**）。
   実害＝`NetlistBuilder`の最左・最右ポート決定が厳密比較ゆえ左右2接続点が1点に縮退し、
   **断線でなく「本来分離されるべき左右ネットが同一ネットへ統合される誤結線（短絡に近い）」**。
   **家老裁定＝v0.5リリースは進めた**（仮リリースゆえ／発生条件が限定的／実運用パーツは全件
   クリーン）。**殿が「先に直せ」と判じられればv0.5.1を出す。対処案3つは`proposed.md` P-129参照。**
3. **単調写像の議論で三段の応酬**：侍「逆順だと並びが崩れる」→隠密「`Math.Clamp`は単調・
   `OrderBy`は安定ソートゆえ順序に依らず同一」→**侍の反例で決着**（`Math.Clamp`は単調**非減少**
   ゆえ、**同値へ潰れると安定ソートの基準順序が前後で変わる**）。**しかも「先頭=NetA・末尾=NetB」
   規約ゆえ電気的意味を持つ**。隠密はPythonでの機械的再現で確認し**自ら結論を撤回・訂正**、
   「**試した反例が『クランプ前に既に正しい順序だったケース』に偏っていた＝反証の網羅性不足**」と
   自己分析。**PR-27が『検算・反証を組み立てる側』にも効くと判明し、`onmitsu.md`へ新節として
   制度化済み**（`memory: feedback_counterexample_search_bias_monotone_argument`）。
4. **「テストしにくい」は設計の匂い**（侍の気づき、`samurai.md`へ新節）：`FrameworkElement`派生を
   STAスレッドで包む手法を編み出して4件のテストを実現したが、殿の裁定で「保存時のみ適用」へ設計が
   変わった途端**Core層の純粋関数となりテスト8件＋壊す実測3観点まで到達**。侍いわく
   **「STAで包んでいた頃より遥かに堅い。『テストしにくい』は設計の匂いであった」**。
5. **家老の落度3件**：(a)「欠陥」判定前に原本確認を指示しなかった（上記1） (b)殿の裁可を
   `proposed.md`へ反映し忘れた（P-126/127/128、殿の問いで発覚。**加えてP-078/P-079が殿裁定から
   12日間`pending`のまま埋もれていた**＝隠密の棚卸しで2度目の発覚） (c)T-045増分C計画時の
   「着手時に家老へ確認」が16日間流れた（上記2節）。**(b)は`karo.md`へ「殿の裁可を得たら、その場で
   `proposed.md`の判断欄を更新する【MUST】」として制度化済み**。
6. **一時ファイルの件数を過少報告**：引き継ぎメモに「計3件」と記していたが実数は13件だった。
   殿へ提示した一覧も一度11件と誤り、その後の追加で13件に。**台帳の数字は都度実測で裏を取ること。**

## 4. スコープ境界・制度（既存＋今回の追加）

- 検証パイプライン既定順＝侍実装→隠密静的レビュー→忍者実機確認／書き込みは侍一元化／コミットは
  パス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】／環境依存文字・
  簡体字・rmコマンド禁止【MUST】（すべて継続適用）
- **本日の制度化5件（いずれも書き込み済み）**：
  1. `samurai.md` PR-04節へ**項目5**（既存の別経路への適用漏れも見る）・**項目6**（「漏れ」と
     断ずる前に意図的な不在かを確かめる＝**最重要**）
  2. `samurai.md`へ新節「**『テストしにくい』は設計の匂い**」（STA包み込みの勘所も併記＝
     **例外は必ずキャプチャして再throw、握り潰すと全件GREENに見える**）
  3. `karo.md`へ「**『欠陥』と判定する前に原本・設計意図の確認を指示する【MUST】**」
  4. `karo.md`へ「**殿の裁可を得たら、その場で`proposed.md`の判断欄を更新する【MUST】**」
     ＋モグラ叩き検知節へ「俯瞰の問いには『原本の確認』を明示的に含める」追記
  5. `onmitsu.md`へ新節「**検算・反証を組み立てるときの入力値チェック【MUST】**」（PR-27の射程拡張）
- **`ecad2-ui-automation`スキルへ追記**：UIAツリーに現れないキャンバスの座標較正
  （既知定数`cellMm=9.0`/`marginMm=30.0`/`MmToDip=96÷25.4`からの理論値と画素走査の突き合わせ、
  T-068時点のオフセット＝横+20.6px/縦+126.6px。**セッションごとに1回較正し直すのが安全**）
- **T-125の制度**：**効果測定の指標をDoDに含める【MUST】**（殿裁可）。着手前の数値は確定済み
  （VM側＝3,395行・283メンバー・最大クラスタ48・ドラッグ7種／View側＝4,132行・143ハンドラ・
  `case`49箇所・最大単一メソッド607行）。**T-125完了から2〜3週後に隠密へ追跡調査を命じ、同じ指標で
  測り直す**——本日の調査（T-045の16日後追跡）がまさにその実例。
- **`proposed.md`新規起票2件**：P-129（保存時クランプによる重複生成＝殿の判断待ち）／
  P-130（パーツエディタダイアログの原因不明の消失、再現性未確認・記録のみ）

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、`git log origin/main..HEAD`と`git status`で混入チェック（**セッション末の
   家老の更新分が未コミットで残っている可能性あり**）。
2. **殿の判断を仰ぐ【報告】事項**（帰参時に一覧でご覧いただけるよう記帳済み）：
   - **P-129（保存時クランプが端子短絡を生みうる）＝最優先**。**我らが自ら開けた穴**ゆえ原本踏襲を
     盾にできぬ。対処案3つ＝(1)保存時バリデーションで拒否 (2)クランプ時に自動でずらす
     (3)現状維持（**隠密・家老とも正当化が弱いと見る**）。**「先に直せ」なら v0.5.1 を出す**
   - **T-125の実装着手**——殿裁定は「**増分α/βの実装とγ/δの事前調査を並行させる二本立て**」。
     **侍の計画起草を命じ、増分の中身を殿にご覧いただいてから着手**する段取り
   - **P-100/P-101**（Breaker3P種別・主回路向きの設定UI皆無）＝殿裁定「T-067/T-068と合わせて判断」の
     条件が揃った。**P-050**（GroupFrame座標オーバーライドの無害根拠がT-067で崩れた、まず実害確認
     調査）・**P-077**（ヒットテスト重複、PR-22候補の成立条件が既に満たされた）も隠密が
     「いま拾う価値あり」と判定
   - **P-122**（シートパネル位置ずれ）の着手時期／**T-123**（インストーラー化）の着手時期／
     **T-122**（ドロップゾーンオーバーレイ）の着手時期
   - **P-130**（ダイアログ消失、再現性未確認）は記録のみで可
3. **T-125の計画起草を侍へ采配**（隠密の調査書3件が入力。侍は既に読了済み）。増分素案＝
   α（境界ガード横展開＋部品種別弁別の共通ヘルパー化、**小・T-045の未達分の後始末**）／
   β（P-077の左右クリックヒットテスト共通化、中）／γ（`MainWindowViewModel`の責務分割、大）／
   δ（`MainWindow.xaml.cs`のハンドラ群責務分離、大）。**隠密の切り出し候補4件**＝
   A（ドラッグ状態機械の統合）・B（SelectedElement系＝最も切り出しやすい）・C（ImageInsert内部整理）・
   D（行操作/シート設定コマンド）。**隠密の見立て＝「型ごとに7分割」でなく「ドラッグ状態機械という
   関心事を横断統合」する方が既存パターン（`ConfirmDrag<T>`）と整合する**。
4. `proposed.md`のpending項目の定期確認（本日21件へ整理済み。条件待ち4件＝P-063/P-070/P-052/P-055）。

---

## 本日の三役の気づき（家老が収集、`karo.md`の【MUST】手順）

- **侍**：(1)原本を見た引き金は家老の俯瞰の問い、後進への手がかりは「**既存コードに『無い』ことを
  欠落と決めつける前に、それが意図的な不在かを確かめよ**」 (2)**己が編み出したSTA手法を捨てたことが
  最大の学び**＝「テストしにくい」は設計の匂い (3)壊す実測を計5回行い、マーカーコメント＋grep 0件
  確認が唯一の防波堤 (4)publishは版数だけでなく**生成物のファイル件数をv0.4と突き合わせた**
- **忍者**：(1)ダイアログ消失は**前兆なし・直後の「次のPowerShell呼び出し1発目」でFromHandleが即fail**、
  T-080と型が近く「**複数のPowerShell呼び出しの連続実行自体が引き金**」の疑い。次回同型に遭ったら
  「直前操作は正常だったか」「呼び出し回数が多かったか」の2点をまず記録すると2件目認定の手がかりに
  なる (2)重複生成を見つけたのは**観点1用に作った検体が`BoundaryOffset`を揃え`RowOffset`だけ変える
  配置だったため一石二鳥で炙り出せた** (3)24対41の食い違いは**実装バグより先に数え方の違いを疑った**
- **隠密**：(1)反証の網羅性——**境界条件を意図的に1つは試す手順の固定化**、机上で埒が明かぬ時は
  迷わず機械的再現へ（**本来は自分から先に疑って切り替えるべきだった**） (2)埋もれ発見の手がかりは
  「判断列の先頭ラベルだけで読み飛ばさず括弧内まで目を通す」＋**過去の教訓の蓄積**（P-104/P-107/P-109の
  前例を知っていたゆえ`pending`という文字面そのものを疑った） (3)構造調査は
  **`git show <commit>:<path> | wc -l`での時系列比較**が最も説得力を持った。クラスタリングは
  grepでメンバー抽出し`_dragging*`等の命名規則から逆引き——**侍の一貫した命名規則あってこそ
  機械的分類ができた** (4)**家老が侍の反例をそのまま右から左へ伝えたことで先入観なく独立に検算し
  直せた。あの仲介がなければ鵜呑みか意固地かに偏りかねなかった**

**家老の所見**：本日、三役がそれぞれ**自分の誤りを自分で見つけて訂正**した——侍が原本調査で前提の
誤りを見つけて実装を止め、隠密が反証の網羅性不足を認めて撤回し、忍者は誤検出を自ら突き止めて記録に
残した。**誰も守りに入らず、一次情報へ立ち返って自分の結論を検め直している。** 家老の采配の誤り
3件も、すべて三役の働きが炙り出したものであった。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
