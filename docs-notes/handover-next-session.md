# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-04（**T-021全増分の実装・検証完了**時点。usage逼迫による計画的セッション終了。家老usage 88%超で殿判断）

## 【最優先・即確認】

1. **ブランチ**: 共有ワークツリー（`C:\ECAD2`、4セッション共通）は `feature/t021-enter-placement` のまま。**mainではない。** 起動時 `git branch --show-current` で確認。
2. **T-021は全増分（1・(vi)・(iv)・(v)）の実装・検証が完了**（忍者実機で全10観点OK・回帰なし。コミット8776795＋台帳更新まで push済み）。**残るは(a)殿の使用感最終確認（Esc2段階解除・ズーム時追従の実挙動）と(b)mainへのマージ（殿確認のうえ実施）のみ。** 前セッション末に家老が殿へ(a)を上申済み——**殿の回答が本ファイルに追記されていなければ、家老はまず殿に確認を仰ぐこと**（勝手にマージしない）。
3. **未コミットの残留1件**: `prompts/startup-auto.md`（殿の手による「戦国時代風【MUST】」追記1行）。コミット可否は殿に確認。それ以外の作業ツリーはクリーンで引き継ぐ。

## 【MUST】UI/UX・使用感に関わる分岐は必ず殿へ確認（維持）

従来どおり。詳細は `docs-notes/roles/karo.md` 同名節と memory（feedback_route_design_decisions_to_user）参照。
実装途中に発覚する細かい挙動の分岐は「家老が既定案を選び実装→忍者検証→実挙動を殿に提示して確認」のサイクルで可。構造的・後戻りしにくい分岐は実装前に選択肢提示。

## 現況（2026-07-04 終了時点）

- **T-021**: 全増分完了。増分(iv) Esc多段階4層（0ab81d8→修正f531b76）・増分(v) 矢印追従スクロール（7ad82ce→修正8776795）とも、隠密静的レビュー→侍修正（各往復1周）→隠密再レビュー合格→忍者実機全OK。**詳細経緯は `docs/todo.md` T-021行に完全記録**。
- **経過観察 計4件**（修正しない合意済み、台帳T-021備考参照）:
  1. Space押下中の強制フォーカス移動→記録残留→同一ボタンUIA Invokeで判定1回ズレ（増分(vi)）
  2. Alt+Tab系の同種残留（増分(vi)）
  3. ToolMode未配線モード（PlaceConnector/PlaceFrame/PlaceLine/PlaceDot/PlaceWireBreak）が将来配線されると層2のEsc解除対象（Tool.Mode==PlaceElement限定）から漏れる（増分(iv)。現状到達不能ゆえ実害なし、**配線する増分で要注意**）
  4. 未選択状態から初回Up/Left押下時のみ追従スクロールがスキップされる（増分(v)。(0,0)は通常最初から可視ゆえ実害低）
- **T-021後の次候補**: mainマージ後にT-015のスコープ再定義（**殿へ選択肢提示が必要**、todo.md参照）・T-019（ドキュメント管理、T-020濃紺最終確認の前提）ほか。他の未着手: T-033・T-028・T-029・T-022・T-013・T-032・P-005。詳細は `docs/todo.md`。

## 増分(iv)(v)の技術教訓（send_message上の知見を保全）

1. **静的レビュー先行の威力を実証**: 検証パイプライン既定順（侍→隠密静的→忍者実機）により、隠密が実機前に退行1件（(iv)層4のStatusMessage残留：F5〜F8はTool.Mode不変のままエラーメッセージを出すため段階判定の全条件をすり抜ける）と(v)2件（右母線余白送り・無変化時発火）を安く検出。**実機確認からの手戻りゼロで全増分一発通過**。
2. **「Esc常時クリア」の不変条件**: StatusMessageクリアのような「全経路で必ず実行すべき処理」は条件分岐の外で一括実行する。層別・分岐内に書くと経路漏れ（今回のF5→Esc残留）が生じる。
3. **共有ヘルパは本体を触らず呼び出し側で対処**: CellRectDipはハイライト・T-023 Automation Peerと共有のため、右母線対処は呼び出し側（MoveSelectedCell内のviewColumnクランプ）で完結させた。影響範囲を増分の趣旨どおり最小に保つ。
4. **家老の仕分けの機微**: 隠密が「経過観察」と提案した無変化時発火を、(a)3系統finderの独立指摘 (b)1行ガードで済む (c)「移動なしなら何も起きない」は旧挙動との一致で解釈分岐にあらず——の3点から要修正へ格上げし、往復をまとめた（usage逼迫時の往復節約にも有効）。

## 各役への申し送り

### 侍へ（前任侍より）
- フォーカス制御の流儀は前回handoverの記述が台帳T-021行に保全済み（FocusCanvas()の2段方式・ConsumeToolButtonFocusRestoreのクリア経路2つのみ・LostKeyboardFocusクリア復活禁止）。
- MoveSelectedCellのviewColumnクランプ（Min(Column, Columns-1)）とnewCell!=currentガードの意図は台帳T-021行参照。**右母線=Columnsは「セル」ではなく「線」**（Element.cs:39）、表示座標だけクランプし選択状態は不変、が設計意図。
- テストのベースラインは3件合格（App層のみの変更では不変）。

### 忍者へ（前任忍者より）
- **モーダル対応Send-KeysSafeヘルパー**: セッション毎にscratchpadが変わるため消えている前提で臨むこと。機能要件（全トップレベルウィンドウ列挙→ダイアログ有無判定→フォアグラウンド切替）から再作成可能。スキル`helpers.ps1`への正式追加は台帳外の新規作業ゆえ殿に諮ること（未上申のまま）。
- Ctrl+Tabで段（FocusScope）を跨げる／Tab連打洗い出し／UIA Invoke・Select偽結果は物理操作で再検証——従来どおり（ninja.md反映済み）。

### 隠密へ（前任隠密より）
- code-reviewスキルは**medium以上で設計上の穴を検出**（今回も(iv)退行はfinder2系統、(v)無変化時発火はfinder3系統の独立指摘が確定の決め手）。lowは純削除・小差分確認向け。
- 理論裏付け（TransformToAncestorのLayoutTransform合成等）と実機検証（忍者）の役割分担が有効に機能した。理論確認のみの箇所は必ず「実機で最終確認」を明記して家老へ報告すること。

## 運用メモ（本セッションの出来事・学び）

1. **MCPサーバー再起動を経験**: peer ID・summaryは再接続後も維持された（今回の実績）。ただし各セッションの処理は中断されるため、再起動後は全ターミナルに起動プロンプト再投入が必要だった。
2. **役決めの同時起動衝突はkey比較で自動収束**: 侍の二重名乗り（xi9shq0c vs cejl52dg）が発生したが、家老からkey比較の結論を通知したところ決定論どおり譲り合いが成立。プロトコルは設計どおり機能する。
3. **git pushのask解除は継続中**（`~/.claude/settings.json`）。**元に戻す時期は殿に確認すること。**
4. 検証パイプライン・往復上限・Wチェック待機（今回は出番なし）の運用は前回から変更なし。

## 次回セッションの起動手順

1. 4ターミナルとも `prompts/startup-auto.md` の同一プロンプトで起動（起動順に家老→侍→忍者→隠密が自動で埋まる。1つずつ間を空けて起動推奨）。
2. 家老は起動後、本ファイルの【最優先・即確認】を実施→**殿の使用感最終確認の回答を確認→OKならmainマージを殿確認のうえ実施**→次タスク（T-015スコープ再定義は殿へ選択肢提示、またはT-019等）の采配へ。
3. `docs/todo.md`・`karo.md`の権限線引きを確認してから采配すること。
