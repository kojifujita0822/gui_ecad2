# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-28（家老記す、**T-134/T-135完全Done・T-133を増分4の手前で保留・T-136を新規起票**）。
前回分は git 履歴（`cba9e0e`以前）を参照。

---

## 0. 【最重要】次の家老が最初にやること

### (a) **本書は「前セッション終了時点」のものである【MUST】**

**本日、役の途中再起動で二度、同じ事故が起きかけた**——**新しい隠密も新しい侍も、本書の古い記述
（「T-134のレビューが未了」「気づき14件の検分が未了」）を読んで、済んだ仕事に着手しかけた。**
**いずれも着任早々に「どちらを先に当たるべきか」と問うたゆえ防げた。**

**How to apply**：**役を途中で再起動させる際は、引き継ぎメッセージの冒頭に
「本書は今朝の版ゆえ、当日分は家老の采配文を正とせよ」と必ず書く。**
**全体像は`git log --oneline -30`（本日は`cba9e0e`より後がすべて当日分）と`docs/todo.md`の当該節が確実。**

### (b) 作業ツリーの状態

**本日のコミットはすべてpush済み**。`git log origin/main..HEAD`と`git status`で混入チェックを行うこと。
**一時計装・残置マーカーとも無し**（侍が確認）。

**殿の`main-layout.xml`について**——**本日2度、書き換わった**。
1度目は忍者の検証が上書き（**復元済み・ハッシュ一致確認**）、2度目は**殿ご自身の御操作の結果**
（`AutoHideWidth`等の既定値追加のみ、配置・寸法は不変。**忍者の判断で復元せず**）。
**【教訓】「WARNINGゆえ無傷」は状態依存の幸運にすぎぬ**——**検証中に「名前を付けて保存」を挟むと
未保存でなくなり、確認ダイアログが出ず正規終了が通って上書きされる**（忍者がスキルへ落とし済み）。

### (c) 検体の残置3件

**`T133INC1H2`／`T133INC1H3`／`T133P148H3`**（マイドキュメント配下、**実体は`OneDrive\ドキュメント\Ecad2\図形\自作\`**）。
**T-133増分4で高さ2以上の検証に使うゆえ残置**。他3件は殿の御許可を得て削除済み。

---

## 1. 本日の到達点

| | 中身 | 状態 |
|---|---|---|
| **T-134** | Undo対象の穴6件 | **完全Done**（往復1周＝文書情報のRedo消失）。**副産物でP-055も解消** |
| **T-135** | 別セルクリックでの巻き戻し（P-144） | **完全Done**（計画・実装・22テスト・実機5観点、**すべて本日中**） |
| **T-133** | その他図形の移植 | **増分0・1・2・3とP-148が完全Done。増分4の手前で保留**（下記T-136を待つ） |
| **T-136** | **部品属性の新設（シート種別の枷＋接続点の種類）** | **新規起票。計画未着手。T-133に優先** |

**本日のコミット＝25件超**（侍が前任12件＋現任2件、家老が11件超）。

---

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-134**＝`fec4117`＋`97adba2`。**Wチェックで侍・隠密の診断が独立に一致**
  （`DocumentInfo.Date`のみnull許容→同値ガードの`==`で`null != ""`→Redo破棄）。忍者の実機9観点すべてOK
- **T-135**＝`d7e0875`。`ClearOrJoinTargetDraftIfAny`を性質で二分し、
  **Esc・文書差し替え＝巻き戻す／セル移動・モード切替＝畳むのみ**。忍者の実機で
  **別セルクリック2行→2行／Esc 3行→2行**と**逆の結果**が出て「両方を非破壊にした形でない」ことを実証
- **T-133増分0〜3・P-148**＝`6efb876`/`5756870`/`ee4d0d9`/`46e4f79`/`b05c59d`/`c413ee0`/`8d238d4`。
  **忍者の実機実測＝枠の半径が高さ1で0.5・高さ2で1.5・高さ3で2.5、接続点の枠外0件**
- **【T-136の根拠】`CreatesComponent`の関門は7経路のうち2つしか守っておらぬ**（隠密の実測）——
  **母線への直接union（`NetlistBuilder.cs:160-161`）・横配線の結合（`:212-216`）・ノード生成・行索引は素通り**。
  **すなわち「非シミュレート」を選んでも、接続点が左端・右端にあれば母線へ無条件に繋がる**

### 判断待ち・未着手

- **【最優先】T-136の計画起草**（侍へ）。**殿裁定＝(A)シート種別の枷と(B)接続点の種類を一つのタスクとし、
  両方の完了後にT-133へ戻る**
- **【殿へ諮る・T-136着手前に必須】「赤丸／青丸」が現在の選択状態の表現と衝突する**——
  **現在は選択中＝`OrangeRed`／非選択＝`DodgerBlue`**（`PartEditorCanvas.cs:699-700`）。
  **ここへ「赤＝種類A／青＝種類B」を重ねると見分けが付かぬ**。
  **隠密の案＝(P-1)選択の表現を色以外へ移す（外周リング等）を推す／(P-2)種類を赤青以外で表す
  （殿裁定(3)の変更を要す）／(P-3)明度で分ける（推さぬ）**
- **【次の家老が再考すべき一件】T-133を保留とした家老の判断は、一歩浅かった疑いがある**——
  家老は「T-133裁定4（3極記号の主回路限定）とT-136(A)が二重になる」と判じたが、**隠密は
  「T-133裁定4は`ElementKind`への制約、殿裁定(4)は`PartDefinition`への制約にて対象が違う。
  粒度も違う（ポート単位／部品単位）。組込みと自作で別々になるのは`CreatesComponent`・`Ports`が
  既に`PartResolver`で二分岐しておるのと同じ形ゆえ不自然ではない」**と見ておる。
  **殿は家老の説明を前提に保留を裁定なされたゆえ、前提が変われば裁定も変わりうる**
- **T-133の要判断2件**＝**§4-5（モータの高さがKind経路2/PartId経路1で食い違う）**・
  **§4-6（主回路シートの前提が実装に無い）**。設計書に記載済み
- **P-149**（要素ドラッグ掴みの行一致比較）／**P-146**（PDF2ページ目のMediaBox）／
  **P-147**（specが先行調査を取りこぼす）——いずれもpending
- **T-125 β-2/β-3**／**T-131・T-132**／**T-123・T-122**——未着手

---

## 3. 殿の御裁定（本日分・T-136の前提）

**【羅列の形で賜った5件＝設計の背景】**
1. **原本GuiEcadにも3極部品を制御回路シートへ置けてしまうバグがあり、未修正**
2. **その場合、原本でもDRCが機能せず中心接続点を左右母線へ引いてしまう状態であった**
3. **この仕様を引き継いだまま模倣しているゆえ、ecad2にほころびが出ている**
4. **【仕様変更】自作部品に「制御シート用」「主回路用」の設定を新設**
5. **制御部品で高さ2以上・接続点が多数のものは「電源2本＝左右配置で母線接続」
   「制御回路数本＝上下配置で母線へは接続せぬ」**

**【接続点の種類について5件】**
1. **2種**（電源／非DRC？ **名称は後日**） 2. **現状の部品は殿がご自身で設定** 3. **赤丸／青丸**
4. **後方互換は無視** 5. **接続方法は別途設計**

**【その他】** T-133は増分4の手前で保留／A・Bとも完了後にT-133／検体3件の削除を許可（rmも許可）

---

## 4. 今回の教訓

1. **【最大】家老が発生源の誤りが22件出た**——数の転記6件、制度文の要約3件、裁定の前提2件、
   役の取り違え1件、他。**すべて三役が捕らえた。**
   **最後の1件は「前任の侍と現任の侍の働きを一人の勘定へ合わせた」**もので、
   **現任の侍が「覚えがない」と申し立てて露見した**——**家老には前任も現任も『侍』としてしか見えぬ。**
2. **Wチェックが本日の決め手を二度作った**——(a)T-134観点4で侍・隠密の診断が**一字一句一致**
   (b)忍者（実機側）と隠密（静的側）が**采配される前に**同じ領域（行削除と高さ2）を指した
3. **テスト設計の分離が、誰も報じておらぬ穴を掘り当てた**——隠密の境界値設計が`Enter`確定を検証点として挙げ、
   侍の実装時RED証明が実際に穴の実在を確定させた（**設計単独の手柄ではない**——隠密自身の訂正）
4. **殿の御教示が、実装の誤りを暴いた**——「上下に接続点を設けるものは左右母線には必ず接続しない」に対し、
   隠密が「現状は**繋がぬべきものを繋いでおる**」と実証。**御言葉と実装の突合が最も効く**
5. **「褒める側にも検分は要る」が新設された**（`memory`）——忍者の「5経路」を侍と家老が二人とも
   検算せず通した。**対に「数は『何を1と数えたか』とセットで出す」も忍者が新設**

---

## 5. 制度・記録の追加（すべて反映済み）

- **`karo.md`**——(1)**殿裁定の射程を報告経路の外へ広げる際は破壊的か否かで線を引く【MUST】**
  （侍3件・隠密2件の検分を経て修正、**二役の見立てが割れた経緯も残す**）
  (2)**既存テストを触らずに済んだ実装はレビューの重さも変えてよい**
- **`samurai.md`**——(1)横展開の漏れは機械に検出させられぬか先に問う【MUST】
  (2)裁定そのものでなく裁定の「根拠」を回帰テストの対象にする【MUST】 (3)公開ラッパーはテスト可能性で決まる
  (4)式を変えると退化入力の扱いが黙って変わる (5)仕様上の上限値まで測る (6)褒める側にも検分は要る
  (7)**退化入力のテストは範囲の「外側」で測らねば検出力が出ぬ**
- **`onmitsu.md`**——(1)数を二度書いたら二度目は数え直せ (2)**射程の区切りそのものが検算を要る**
  (3)「別系統がある」と己で指摘したら以後の全ての数え上げで確かめよ (4)型（nullable性）まで見よ
  (5)diffが追加のみであること自体が根拠になる 他
- **`ninja.md`**——症状を報ずるときは同じ状態から抜ける他の経路も数え上げる【MUST】
- **`memory`新設2件**——`feedback_praise_also_needs_scrutiny`（褒める側にも検分）／
  `feedback_disclose_counting_unit`（数は単位とセット、忍者が新設）
- **`ecad2-ui-automation`スキル**——PDF直読（`FlateDecode`）／`graceful close`を見たらレイアウトを確かめよ／
  UIA`InvokePattern`の新実例／「動かなかった」は回帰と見分けがつかぬ 他

---

## 6. 次の1手

1. **【最優先】殿へ「赤丸／青丸の衝突」を諮る**——T-136の着手前に必須（§2参照）
2. **T-136の計画起草を侍へ**——参考＝`docs/ecad2-port-type-impact-survey-onmitsu.md`（隠密、影響範囲）。
   **規模＝増分1つ分（関門を除く）、永続化は改修不要、描画は1箇所、UI雛形あり**
3. **T-133保留の是非を再考**（§2の「一歩浅かった疑い」）。**必要なら殿へ諮り直す**
4. **T-133再開時**＝増分4の設計書と忍者の期待値表が揃うており申す。
   **ただし期待値表は仕様変更で式が変わりうる**——本文の戒めに従い、式を先に確かめること

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。**`handover`の読み込みは役決めの後**（第2バッチで役割mdと並列）。
