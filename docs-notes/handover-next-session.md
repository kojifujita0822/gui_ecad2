# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-24（家老記す、**台帳陳腐化の大量発見・修正、T-102/T-111/T-119/T-121完了、
T-068計画確定・T-122/T-123起票、殿のご指示で本日はここで終了**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**作業ツリーはクリーン**。本日の全コミットはpush済み（`git log`で`7be7aa2`まで）。次回セッション
開始時、家老がpush前混入チェック（`git log origin/main..HEAD`）を行うが、通常は既にクリーンな
はず。

---

## 1. 目的とDoD

前回セッション終了時点の「次の1手」（T-119往復4周目の強調色対処検討、T-121設計調査依頼、
proposed.md定期棚卸し）を引き継ぎ着手。途中、殿より逐次「タブデザインが見本と違いすぎる」
「T-121実機確認」「T-102/T-068も進めよう」等の追加指示・裁定を受けながら、多数のタスクを
検証パイプライン完了まで進めた。加えて、台帳（`docs/todo.md`）のステータス欄が実態（既に完了
済み）と食い違う「陳腐化」を多数発見し、その都度修正した。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **台帳陳腐化の大量修正**：T-111・T-114〜T-118・T-120・T-110・T-077・T-083・T-089・T-067・
  T-098・T-102の計12件、見出しが`Approved`/`In-progress`のまま実態は完了済みだった不整合を発見・
  `Done`へ修正。うち2件（T-111・T-102）は本日の家老自身の編集ミス（見出しだけ変えて本文を同期
  し忘れる）で、バックグラウンド監査エージェント（`general-purpose`）による全体棚卸しで発覚・
  即修正した。**今後も同種の見落としが起きうるため、ステータス変更時は必ず本文も同時に更新する
  こと（memory: `feedback_verify_design_intent_before_quickfix`とは別軸の自戒事項）**。
- **T-119（タブ形状Aeroテーマ化）完全決着**：往復4〜7周目。左肩のみ非対称な曲線→対称なS字への
  修正、曲率不足→強化、右側四角枠との継ぎ目解消、その副作用（縁線消失）の修正、と4往復を経て
  全解消。強調色未反映（往復3〜4周目由来）は指定アプローチ適用済みで効果なしと判明しモグラ叩き
  相当と判断、**殿裁定＝据え置き（経過観察）**で決着。忍者最終実機確認OK。
- **T-121（配置ツールバータイトルバー常時非表示）完全決着**：実装自体は小規模だが、実機確認で
  重大回帰3件（A:フロート後オーバーレイ残留・B:Tabキーで曲線部へフォーカス誤動作・C:フロート幅
  未フィット）が連鎖発覚。特にAの対処（即時Collapse代入）が新たな重大回帰（オーバーレイの
  「フロート中表示継続」という本来の設計を破壊）を生み、殿の実機ご指摘で発覚→侍が即差し戻し→
  隠密の独立調査で「差し戻しこそ正解」と確認、という往復があった。最終的に3件とも解消・忍者
  実機確認OK。
- **T-102（OR自動配線の合流先を明示選択）完了**：殿裁定（案A・T-041パターン踏襲の2点指定UI）を
  実装、`BuildOrJoinCandidates`新設（旧baseRow探索と完全後方互換）。RED先行証明済み（新API依存
  テストにつき「根本原因自体が旧コードで再現できるか」という代替観点で証明、memory:
  `feedback_red_proof_new_api_limitation`参照）。隠密静的レビュー・忍者実機確認（4観点）とも
  OK、回帰なし。
- **T-111（ステータスバー表示改善）完了**：侍実装のみ完了し検証パイプラインが取り残されていた
  ことを家老が発見、隠密静的レビュー・忍者実機確認とも完遂。
- **proposed.md定期棚卸し完了（隠密）**：P-119以降の新規分・古いpendingとも精査、殿の裁定を
  要する新規項目は無しと判定（詳細`docs/ecad2-proposed-periodic-review-20260724-onmitsu.md`）。
- **出力破損#19（隠密、1回目）**：`docs-notes/output-corruption-log.md`へ記録済み、離脱プロトコル
  未発動（1回のみのため）。長時間連続調査・報告往復の終盤という文脈が#16（家老）と共通、との
  所見あり。
- **rmコマンド違反2件目（侍、RED先行証明の一時テストファイル削除）**：忍者の実例と同型の
  「git未追跡の使い捨てファイルだから安全」という自己判断による例外扱いが原因。侍が自ら即座に
  自己申告、memory更新済み（`feedback_rm_command_batch_at_end.md`）。実害なし。
- **memory新設2件・更新1件**：`feedback_verify_design_intent_before_quickfix`（設計意図を検証
  せず憶測で直さない、T-121往復7周目実例）・`feedback_red_proof_new_api_limitation`（新API依存
  テストのRED証明限界、T-102実例）・`feedback_stay_keyword_keep_app_running`へ追記（stay後も
  自動操作を速やかに控えるべきという忍者の自己反省）。
- **ecad2-ui-automationスキルへ忍者の気づき3点追記**：SelectedCellDisplayの行+1オフセット・
  UIA合成ドラッグの近距離不安定（追加実証）・DRC結果パネル行クリックのジャンプ機能。
- **T-123（正式リリース・インストーラー化）技術選定完了**：Inno Setup採用（GuiEcad本体の実績
  確認済み、MSIXは実際には不採用だったと判明）。最新版Inno Setup 7.0.2（2026-07-13リリース）を
  公式サイトで確認済み、着手時に再確認する旨記帳。

### 実施したが未検証（次回セッションで継続）

- **T-122（T-103ドロップゾーンオーバーレイの表示条件・判定範囲見直し）調査段階のみ、実装未着手**。
  殿より「フロート中ずっと表示されるのはおかしい」「ドロップ判定範囲が画面全体なのはなぜ、配置
  ツールバーのエリアだけでいい」との指摘を受け、隠密が両論とも技術的に対処可能と確認済み
  （(1)WM_MOVING受信起点への表示切替、(2)Visual Tree探索でPlacementToolBarの元の親ペインの
  実座標を基準にする方式）。殿裁定＝両方OK、進めてよい。侍が一次ソース（`GetDropAreas`/
  `FindVisualChildren<LayoutAnchorablePaneControl>()`）を読解した段階で本日は中断（**作業ツリー
  完全クリーン、コード編集は一切未着手**）。次回は「既存`FindVisualChild<T>`（単一マッチ）に
  述語版オーバーロードを追加→PlacementToolBar/MainToolBarを含む`LayoutAnchorablePaneControl`を
  探索→WM_MOVING(0x0216)受信でオーバーレイのVisible化+座標再配置」という設計から着手する
  （侍の申し送り）。
- **T-068（自作パーツ管理・編集UI）計画確定のみ、実装未着手**。隠密プラン完了（`docs/ecad2-t068-part-editor-plan-onmitsu.md`）、殿裁定4点確定（ツールセット=GuiEcad同等9種踏襲／複製機能=
  見送り／画面形態=モーダルダイアログ／端子入力=キャンバスドラッグ）。増分5分割案（増分0=PoC
  〔高リスク〕→増分1=プロパティ編集〔低〕→増分2=端子編集〔低〜中〕→増分3=形状編集キャンバス
  本体〔高リスク〕→増分4=インポート/エクスポート）のうち、次は増分0（PoC、形状編集キャンバスの
  操作感検証）の詳細設計から着手する（`docs/README.md`の高リスク領域方針＝検証優先に従う）。

### 未着手

- **T-123（正式リリース・インストーラー化）**：技術選定（Inno Setup）のみ確定、着手はT-068完了後。
- proposed.mdの一部pending項目（P-080/P-100/P-101はT-068本格着手時に再検討、P-077は3件目実例
  待ち、P-045は将来のバッチ清掃候補）——今回の棚卸しでは動かす材料なしと判定済み、次回の定期
  棚卸しで再確認。

## 3. 試して失敗したアプローチと結果（今回の教訓）

1. **T-121往復7周目：既存コードの設計意図を検証せず憶測で応急処置し、新たな重大回帰を生んだ**。
   「オーバーレイ残留」バグへの対処として`Visibility=Collapsed`を直接代入した際、「ドラッグ中
   だけの目印だろう」と憶測し、実際の設計（フロート中はずっと表示し続け再ドッキングで消える）を
   一次ソースで確認しなかった。殿ご自身の実機操作で「通常のドラッグでもオーバーレイが出ない」と
   いう新規回帰を発見されるまで気づかれなかった。侍が即座に差し戻し、隠密も独立に同一結論へ到達
   （memory: `feedback_verify_design_intent_before_quickfix`に記録済み）。
2. **家老自身の技術仮説も誤りだった（率直に訂正）**：上記回帰の原因について、家老は「WPFの
   ローカル値がTriggerより優先されるため」という仮説を立てたが、隠密の一次ソース確認で
   「そもそもTrigger自体が存在せず全てコードビハインドの直接代入」と判明、仮説は成立しないと
   判明した。結果的に侍の診断（即時代入が継続表示という設計自体を破壊した）が正しく、家老は
   自分の仮説に固執せず訂正した。
3. **隠密自身のレビュー承認にも見落としがあった**：症状1（オーバーレイ残留）への即時Collapse
   修正を、隠密は当初「タイミング的に上書きできる」という技術的検証のみで承認したが、「そもそも
   この上書きが設計意図を破壊していないか」という観点を欠いていた。事後、隠密自身が率直に
   訂正した。
4. **家老の台帳更新が中途半端になる事故が複数回発生**：見出し（ステータス欄）だけを`Done`へ
   変更し、本文（完了内容の記述）を同期し忘れるミスがT-111・T-102で発生。バックグラウンド監査
   エージェントによる全体棚卸しで発覚・修正。**ステータス変更時は必ず本文も同時に確認・更新する
   こと**を次回以降徹底する。
5. **侍のrm -f誤用（2件目）**：RED先行証明の一時テストファイル削除で、「git未追跡の使い捨て
   ファイルだから安全」という自己判断により`rm -f`を使用（禁止事項【MUST】違反）。忍者の
   同型事例（2026-07-22）に続く2件目。侍が即座に自己申告、実害なし。
6. **忍者：「stay」後も自動操作を継続し殿の実操作と競合するリスクを生んだ**：T-121検証中、
   殿の実機操作と自動操作（Tab連打等）が交錯する局面で、自動操作を速やかに控えるべきところ
   継続してしまった。memory更新済み。

## 4. スコープ境界・制度（既存、継続適用＋今回の追加）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機確認／書き込みは侍一元化／
  コミットはパス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ
  【MUST】／環境依存文字禁止【MUST】／簡体字禁止【MUST】／rmコマンド禁止【MUST】（継続適用）
- バグ対応のWチェック（侍修正＋隠密独立調査の並行）は本日もT-121の重大回帰対応で機能した
  （家老が突合、一致を確認してから確定する運用は有効）
- **新規制度化（本日）**：
  1. memory新設2件（`feedback_verify_design_intent_before_quickfix`・
     `feedback_red_proof_new_api_limitation`）・既存memory更新1件（`feedback_stay_keyword_keep_app_running`）。
  2. `ecad2-ui-automation`スキルへ忍者の気づき3点追記（座標系オフセット・合成ドラッグ距離の
     追加実証・DRC結果パネルの罠）。
  3. 台帳ステータス変更時の本文同期を徹底する自戒（制度文書への正式な追記はまだ、次回検討）。

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、**作業ツリーはクリーン**（§0参照）。push前混入チェックを一応行う。
2. **T-122**：侍へ続きの設計・実装着手を采配（`FindVisualChild<T>`述語版オーバーロード追加→
   WM_MOVING検知方式、侍の申し送りどおり）。
3. **T-068増分0**：形状編集キャンバスのPoC詳細設計から着手（高リスク領域、検証優先の方針で）。
4. **T-123**：T-068完了まで着手保留、頭に留め置くのみ。
5. proposed.mdのpending項目の定期確認（次回の頃合いで、`memory:
   project_proposed_pending_periodic_review`参照）。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
