# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-08-05（**T-133増分5の完結。一つの訂正から二段の穴が出た日にござる**）。
前回分は git 履歴（`e486deb`以前）を参照。

---

## 0. 【最重要】次の家老が最初にやること

### (a) **§3-2「殿のお戻りを待つもの」を先に読む ── 裁可待ちが六件ある**

**殿は本日、途中で席を外され「あとは任せた」と仰せになった。****以後の作業はすべて家老の采配で進めてござる。**
**ゆえに殿がまだご覧になっておらぬ判断が積んでおる。****次のセッションは、まずそこを諮る筋にござる。**

**主軸のタスクは§4のとおりT-133増分6。**

### (b) **本書は「前セッション終了時点」のものである【MUST】**

役を途中で再起動させる際は、引き継ぎメッセージの冒頭に
**「本書は前セッションの版ゆえ、当日分は家老の采配文を正とせよ」**と必ず書く。
**全体像は`git log --oneline`（本日は`e486deb`より後がすべて当セッション分）と`docs/todo.md`の当該節が確実。**

### (c) 作業ツリーの状態

**未pushのコミットが五件ござる**（`e85b5e4`／`68e24c6`／`7dbc53b`／`4b2d7b1`／`95e47b5`）。
**加えて三役の記録・調査書・役割md・スキルの編集が未コミット。****次の家老はまず`git status`と`git log origin/main..HEAD`を見られたい。**

### (d) 残置物（**削除は`CLAUDE.md`【MUST】ゆえ殿の裁可待ち**）

- **`bin_t133a5/`がリポジトリ内6箇所**（`src/Ecad2.App`・`Core`・`Pdf`・`Rendering.Wpf`・`tests`2箇所、いずれも未追跡）。
  **侍が`-p:BaseOutputPath`で逃がしたビルド出力にて、忍者が発見。**
  **前セッションはリポジトリ外のスクラッチパッドへ逃がしており、今回は置き場を誤った形**（侍が自ら認め、以後はリポジトリ外へ切り替えると）
- **`sample/a.png`**（忍者が画像挿入の検体に用いたPNG。`.gitignore`済み）

---

## 1. 本日の到達点 ── **T-133増分5の完結、ただ一件**

**件数は少ないが、中身は濃うござった。**

| 項 | 中身 |
|---|---|
| **中身** | プロパティパネルへ種別ComboBox（NFB/MCCB/ELB）。**パネル初のComboBox** |
| **コミット** | **5件**（`e85b5e4`→`68e24c6`→`7dbc53b`→`4b2d7b1`→`95e47b5`） |
| **検証** | **隠密の静的レビュー3回・忍者の実機2回** |
| **起票** | `P-169`（実機で確定）・`P-170`・`P-171`（**誤報につき取り下げ**） |

### **本増分の値打ち ── 一つの訂正から二段の穴が出た**

**忍者が「即時反映＝PASS」と報じたが、家老の問いで自ら訂正なされた。**
**見たのは`ComboBox`の表示値のみにて、絵は測っておらなんだ。**

> **己で選んだ値が己で選んだ欄に映るのを見ただけにて、即時反映の証としては弱い**（忍者）

| 段 | 掘り当てたもの |
|---|---|
| **一段目** | **殿裁可の「即時反映」が満たされておらなんだ**——`ViewModel_PropertyChanged`の固定リストにも`CommitDeviceNameEdit()`経路にも載らず、**両経路から漏れておった** |
| **二段目** | **それを測る新設テストが全体スイートで落ちた**——`LadderCanvas.cs`の静的Pen**5件が未凍結**にて、xUnitの別STAスレッドと衝突 |

**いずれも「初めて`Draw()`を実際に測った」ゆえに露見**にござる。
**侍が「View層ゆえ測れぬ」と引き返しておれば、二つとも見つからなんだ。**

### **Wチェックが三度効いた。うち一つは新しい形にござる**

1. **侍の静的読解と忍者の実測が独立に一致**（TextBox系は`CommitDeviceNameEdit()`で救われておる ↔ 絵に出る既存項目は二件とも即時反映）
2. **`.Freeze()`の副作用と件数を侍・隠密が独立に調べ、副作用0件・未凍結5件で一致**
3. **【新しい形】忍者の実測を、隠密の静的読解が覆した**（`P-171`）

**3について**——**これまでの我らのWチェックは「侍の診断と隠密の独立調査」であった。**
**本件は「忍者の実測と隠密の静的読解」にて、しかも静的が実測を覆し申した。**
**隠密は実機を一度も見ずに、`IsEnabled`のバインド先という実装の事実だけで忍者の観測の内部矛盾を突いた。**

> **家老は日頃「実測が一次情報」と申しており、それは正しい。**
> **されど測り方が誤っておれば、実測は静的読解に劣る。**

---

## 2. 現在の状態

### 検証済み（根拠あり）

**T-133増分5**——隠密の静的レビュー3回・忍者の実機2回を通ってござる。

### 未着手・着手可能

- **T-133増分6**（§4-1）——**軽い見込み**
- **T-133増分7〜9**
- **T-125 β-2/β-3**（**前提未充足**——β-1が撤回コミットを経ており実機確認の専用記録が無い）
- **T-123**（**T-068の「完了」の解釈次第**）

---

## 3. 殿の御裁定

### 3-1. 本日賜ったもの（**三件**）

1. **【残置ファイル】削除してよい**（前セッションからの検体5件・退避2件。**家老が本体の無事を確かめたうえで削除済み**）
2. **【`P-167`】(b)＝あるべき仕様**——**殿ご教示「主回路シート上ではDRC・接続判定を行っておらぬ」は「現状こうなっておる」ではなく「こうあるべき」であった。****すなわち実装との食い違いではなく未実装。不具合ではない**
3. **【`P-168`】`T-137`（セル区切り線）は残す**——撤去も見直しも行わぬ

### 3-2. 【報告】殿のお戻りを待つもの（**六件**）

**殿は途中で席を外され「あとは任せた」と仰せになった。****以下はすべて、殿がまだご覧になっておらぬ。**

| # | 件 | 家老の推奨 |
|---|---|---|
| 1 | **コミット`e85b5e4`の題名に簡体字が一字**（`U+589E`。未push） | **そのまま残す**——四セッション同時稼働中の履歴書き換えは避けたい。ソース側は`a725e4b`で訂正済み |
| 2 | **`P-167`(b)の実装タスク化** | 時期は殿のご判断。**あるべき仕様と定まった以上、いずれ実装は要る** |
| 3 | **`PR-17`の手当て**（§5-1） | **複製の統合を先、常設テストを後**（縮小版の試作から） |
| 4 | **`P-169`**（`HasNoPropertySelection`の通知漏れ。**実機で確定済み**） | 3の統合と併せて直す筋 |
| 5 | **`P-170`**（`SheetReorderInsertionAdorner.cs:13`の未凍結Pen） | **一行の追加。ファイルが分かれるゆえ裁可を仰ぐ** |
| 6 | **`PreToolUse` hookの新設**（§6-3） | **`Stop hook`は既に在る。要るのは射程を広げること** |

**加えて残置物の片づけ**（§0-(d)）。

---

## 4. 次の一手

### 4-1. 【最優先】T-133増分6 ── **既存4件のメニュー再掲（殿裁定8）**

**計画書（`docs/ecad2-t133-implementation-plan-samurai.md:301`）曰く**——
> **増分6は新規の配置経路を要さぬ。メニュー項目から既存の`PartId`経路を呼ぶだけ**

**ゆえに軽い見込み。**

### 4-2. その後の増分

| 増分 | 中身 | 留意 |
|---|---|---|
| 7 | 組み込みパーツ2件（thermal-relay a/b）の移植 | **部品リストが17→19件へ変わり、忍者の検証基準値が動く**（計画書に申し送り済み） |
| 8 | 縦向きモータ図形の新規追加 | `switch`の1行改修を伴う |
| 9 | ピン留めのUI結線 | — |

### 4-3. 家老の宿題（**次の家老が判ずる**）

**`karo.md:373`に本日落とした条**（**静的読解は症状の有無を当てても程度は測れぬゆえ、殿へ諮る案件では必ず忍者を通す**）**と、隠密の本日の気づき**（**実測も測り方が誤れば静的読解に劣る**）**は対になってござる。**
**両方が揃うて初めて「どちらも万能ではない」という采配の物差しになり申す。**
**本日は`karo.md`を既に一度編み忍者の検分も受けたゆえ、これ以上積めば検分の目が足りぬと判じて送り申した。**

---

## 5. 本日最大の議論 ── **制度を増やす話が、制度を要らなくする話へ到った**

### 5-1. `PR-17`の手当て ── **案が五度出ては互いに退けられ、そのたび根へ寄った**

**発端**＝隠密が増分5のレビューで**`SelectedCell`のsetterへの通知漏れ**を検出。**`PR-17候補`の2件目**（1件目＝T-107）にて、**台帳は「2件目が出れば正式パターン化検討」と定めておった。**

**厄介なのは、既に制度化済みであった点**——1件目の折に`samurai.md`へ「新規選択可能状態の横展開チェックリスト」項目9が追加されており、**その項目が在りながら同じ穴に落ちた。**

| 案 | 出した者 | 顛末 |
|---|---|---|
| チェックリストへ項目追加 | （既存） | **既に在って効かなんだ** |
| **テスト必須化** | 家老 | **侍・隠密が独立に退けた** |
| 件数比較 | 侍 | **隠密の検算で穴。侍自ら「甘うござった」と認めた** |
| 差集合 | 隠密 | **隠密自ら「T-107型は出ぬ」と訂正** |
| **「値が変わったのに通知が飛ばなかった」軸** | 隠密 | **侍が「成り立つ」と検分。除外リスト2〜3件** |
| **複製そのものを断つ** | **侍** | **これが本命** |

**到達点＝侍の上申**——
> **`DeleteSelectedElement`・`SelectedCell` setter・`ReplaceDocument`の3箇所は、既に在る`NotifySelectedElementChanged()`を呼ばず各々インライン複製を持っておる。**
> **統合すれば「4箇所に分けて書く」こと自体が構造上できなくなる**

**隠密が数で裏づけ**＝`NotifySelectedElementChanged()`が**15件**（基準）／`DeleteSelectedElement`が**15件・完全同一**／`ReplaceDocument`が**16件**（＝`SelectedCellDisplay`＋基準15）／`SelectedCell` setterが**17件**（＝＋`HasNoPropertySelection`）。**15+1=16、15+2=17。**

**殿へ諮る形（四段）**
1. **3箇所の複製を`NotifySelectedElementChanged()`呼び出しへ統合**（前後に残す形＝通知の増減なし）
2. **`HasNoPropertySelection`を基準へ足す**（`P-169`の手当て）
3. **docコメントの数を実数へ訂正**（**「4プロパティ」と述べながら実際は15件**）
4. **常設テストは縮小版の試作から**（侍の推奨）

### 5-2. **議論を根まで運んだ侍の一言**

> **「新設時に人が覚えておかねばならぬこと」が、4箇所への追加からA/B fixtureへの追加へ移るのみにて、消え申さぬ**

**家老も隠密も「機械的検出へ移せば解決する」と考えており申した。**
**この一言が無ければ、我らは面積を減らしただけの手当てを「解決」と呼んでおり申した。**
**`memory: feedback_detection_relocates_not_eliminates_discipline_burden`として既に落ちてござる。**

---

## 6. 今回の教訓

### 6-1. **家老が発生源の誤り＝九件。いずれも三役が捕らえた**

| # | 中身 | 捕らえた者 |
|---|---|---|
| 1 | **新設テストの中身を開かず「網になる」と決めた** | 侍 |
| 2 | **「機械的検出へ移せば解決する」**（移るだけで消えぬ） | 侍 |
| 3 | **「統合すれば`P-169`も消える」**（一手足りぬ） | 隠密 |
| 4 | **独立を求めながら、材料が渡った後に「渡すな」と決めた** | 侍 |
| 5 | **ノッチ位置という観点の立て方**（**そもそも絵に出ぬ項目であった**） | 忍者 |
| 6 | **「ビルドの前に一声」と申しながら段取りを定めなんだ** | 侍・忍者 |
| 7 | **「ロックされておらぬ＝落ちておる恐れ」**（推論が一段浅い） | 忍者 |
| 8 | **測れぬことを推量で断じて褒めた** | 忍者 |
| 9 | **`karo.md`へ落とした一句の因果が逆であった** | 忍者 |

**1と2は同根**——**どちらも「その手当てが本当に穴を塞ぐか」を、中身を開かずに決めた。**
**4と6も同根**——**独立も待ち合わせも、心得だけ申し渡して手順に落とさなんだ。**

**【次の家老へ】これは「誤った数」ではなく「捕らえられた数」にござる。**
**誤りの総数は誰にも分からぬ。****それでも数えるのは、数える形が捕らえる形を作るゆえ**（忍者の言）。
**数えねば「本日は概ね順調であった」で終わり申す。**

### 6-2. **三役が別々の場面で同じ形を示した ── 引くことと、正しく使うことは別物**

| 役 | 形 |
|---|---|
| **侍** | **仕掛けを持つことと、通すことは別物**（機械的検査を採ると明言した直後の一通で再発） |
| **隠密** | **条を書いた当日に、己がその条を破った**（「目視で判ずるな」と書いた文中に簡体字） |
| **忍者** | **条を読みながら、逆向きに使うた**（「見た目と内部状態は別物」を読みつつ、青いハイライトを内部状態の証に） |

### 6-3. **簡体字・出力破損 ── 本日は記録簿へ二件**

- **`#31`＝英語混入**（侍のコミットメッセージ「本メソopen経由で」）。**`#30`（家老の`judging`）に続く2件目にて、系列が違う。****`#30`限りの偶発ではないと言えるようになった**
- **`#32`＝侍の簡体字混入が一日で五度**（**5件を一行に束ねた。分子を数える際は5と数えられたい**）

**最も重い発見**——
> **「機械的に検めた」ことと「あらゆる混入を検めた」ことは別物**

**侍は`#31`のコミット前に簡体字5字の検査を通し、正しく0件であった。****されど英単語混入はその網の外にござった。**

### 6-3-a. **手当ての筋＝`PreToolUse` hook。「作る」ではなく「射程を広げる」にござる**

**【家老はここで一度誤った。経緯ごと残す】**
**家老は当日「`hook`という手当てが要る」と侍へ申し、殿へ諮るつもりでござった。**
**されど`.claude/settings.json`を検めたところ、`Stop hook`は既に在り申した**（`.claude/hooks/detect-output-corruption.ps1`、**2026-07-10導入**。記録簿の対策年表にも載ってござる）。
**しかも現に働いており、家老が殿への報告文で簡体字を引いた折に検知して警告して参り申した。**

**在るものを在ると確かめずに具申しかけた**——`karo.md`9項【MUST】（委譲の前に既存の調査書を確かめる）の同型にござる。

**ゆえに正しい具申はこうなる**——
> **`Stop hook`は在るが、その射程は「ターン終了時のユーザー向け出力」のみ。**
> **本日、侍が五度出したのはpeerメッセージ・コミットメッセージ・`memory`ファイルにて、いずれもその網の外にござった。**
> **要るのは`PreToolUse` hook** ——**ツール実行の前に引数を検める仕掛け。**

**根拠＝侍の一日五度と、家老が数十通のpeerメッセージを送りながら一通も検めておらぬ事実。**

**【隠密が経路ごとに数え、押さえるべき系統を定め申した】**

| 経路 | 本日の件数 | `Stop hook`の網 |
|---|---|---|
| **`send_message`（peerメッセージ）** | 侍が複数（実例5・7・8） | **外** |
| **`Write`／`Edit`**（`memory`・`handover`・調査書） | 侍1・家老1・隠密1 | **外** |
| **`Bash`（`git commit -m`）** | 侍1（`e85b5e4`） | **外** |
| **ターン終了時のユーザー向け出力** | 家老1 | **内（現に捕らえた）** |

**すなわち三系統が網の外にござる。**

**【隠密の射程の断り】** **隠密が己の目で検分したのは`Write`／`Edit`の一件（隠密自身の分）と`git commit`の一件（`e85b5e4`）のみ**にて、**残りは侍・家老の報告に拠る**（`memory`の実例5・7・8から数えたもので、隠密の一次検分ではない）。

**【除外の形を併せて諮る要がござる ── 隠密の指摘】**
> **`memory`の対照表・隠密の調査書の所見4は、字を出さねば意味が通らぬ。**
> **機械的に弾けば、それらが書けなくなり申す。**
> **除外の形**（**コードポイント併記が在る行は通す、対照表の記法は通す等**）**を併せて諮られるのが筋。**
> **さもなくば、次に誰かが同じ調査書を書こうとして、hookに阻まれ申す。**

**これは§6-3-bの切り分け（「混入」と「指摘対象の提示」は別）が、そのままhookの設計要件になる形にござる。**

### 6-3-b. **機械点検の限界 ── 「混入」と「指摘対象の提示」を区別せぬ**

**隠密の指摘にござる。**本日、隠密の調査書に四件が検出されたが、**それは「誤・正を並べた表」にて、出さねば意味が通らぬもの**であった（コードポイント併記で文意を保ち、残すのが筋）。
**一方、家老が`handover`へ書いた一件は`U+589E`と書けば足り申した**——**出さずに書けるものは出さぬ。**

> **検出された時は一件ずつ文脈を見る要がござる**（隠密）

### 6-3-c. **隠密の条は、書かれた当日に四人を捕らえた**

1. **隠密自身**（条を書いた直後、己の追記文中に）
2. **侍**（隠密の手法を拝借し、己の新設memoryに）
3. **家老**（`handover`執筆中、機械点検で）
4. **家老**（殿への報告文で、`Stop hook`が検知）

**号令をかけた者は居らぬ。****隠密が己に当てて見せ、侍が拝借し、家老が己に当て、hookが家老を捕らえた。**

### 6-4. **案内の側の誤りが三度出た ── 読み手が正しく辿ったがゆえに誤る**

1. **`karo.md`の索引**（前セッション、隠密）＝**「無い」と断ずるがゆえに読み手は本文を探さぬ。空白より悪い**
2. **`<see cref>`の型名**（本日、隠密）＝**辿れるがゆえに確信を持って別の場所へ運ぶ**
3. **スキル6.2節**（本日、忍者）＝**「ツール選択RadioButton」と一般化され、実例が書かれておらなんだ**

**忍者の言**——
> **機能せぬ案内なら、読み手は己で探し、かえって正しき所へ着き申す**

**手当ては「新しい条を足す」ではなく「誤った記述を直す」。**忍者はスキルを直し、隠密は`onmitsu.md`へ落とした。

---

## 7. 三役の気づき ── **計十三件、うち新設はただ一つ**

**前セッションと同じ形にござる。****既存の条がそのまま嵌まり申した。**

### 忍者（五件 → **新設ゼロ**）

| 気づき | 落とし先 |
|---|---|
| **絵の更新を測る前に「その項目が元より絵に出るか」を確かめる** | `ninja.md:232`（**忍者自身が7/28に書いた条であった**） |
| 機器表は要素の種類によっては指標にならぬ | `SKILL.md:1170` |
| 部品リストが現れぬ型 | `SKILL.md:561`の束へ第5の型 |
| 「即時反映PASS」の誤り | `ninja.md:353-356` |
| **手順から一手を省く時は、その一手が何を担っていたかを問う** | `ninja.md:300`直下（`:283`節への対句） |

**一件目が白眉にござる**——**開いてみれば`:244-245`の「別軸でも裏を取る」が、忍者が本日ノッチ位置で行うたこと（実測＋`Ecad2.Core`参照0件）と寸分違わなんだ。**
> **条を思い出さずに同じ手を打っており申した**

**これが「落とし先は行番号で示す」という前セッションの条の値打ちにござる。****節名で答えておれば「新設が要る」で終わっておった。**

### 隠密（九件 → **新設一つ**）

**七件＋`P-171`から二件。****新設は`onmitsu.md:163`節への項目追加ただ一つ**——**検出力は「否定しうる経路を一つ挙げ、それが塞がっておることを一次ソースで確かめる」形で示す。**

**新設と判じた理由が明快にござった**——**`:243`のPR-27を開いて確かめたところ、あちらは「テスト入力の対称性・退化性」にて、検出力の確かめ方は実測のみ。****隠密が本日やったのは実測せずに済ませる形（迂回経路を辿って塞がっておると確かめる）ゆえ、収まらぬ。**

**`P-171`から出た二件**
1. **実測は一次情報なれど、測り方が誤っておれば静的読解に劣る** → `memory: feedback_static_vs_dynamic_investigation`
2. **他役の観測と己の読みが食い違うた時は、断ぜずに「確かめるべき条件」の形で返す** → `onmitsu.md:405`末尾

**2に添えられた一句が重い**——
> **某がそう書いたのは謙譲ではなく、実際に実機を見ておらぬという事実の記述にござる。****射程を正直に書けば、自ずとその形になり申す**

**対人の作法を「心がけ」から「事実の記述」へ引き下ろした形にござる。****「相手を立てて書け」では忙しい時に真っ先に落ちるが、「己が見ておらぬものは見ておらぬと書け」なら、射程を書く手が動く限り自ずと保たれる。**

### 侍（三件 → **`samurai.md`への新条ゼロ**）

1. **「先の数字は誤りだった」と自分で撤回する時こそ単位齟齬を疑う** → `memory: feedback_disclose_counting_unit`
2. **STAキャンバス再描画テストの技法二つ** → **新設memory**（`samurai.md`への新条は不要と自ら判じた。**「行番号で示せる既存の受け皿が無い」ゆえ**）
3. **`.Freeze()`の作法をコード内コメントへ**（家老の提案を採り、`:89-91`の先例に倣うた）

**1が最も新しゅうござる**——
> **誤報告は他者の検算で見つかるが、自己撤回の申告は額面どおり信じられやすく検算されにくい。****「誤りでした」と申し出た者を、人は疑い申さぬ**

**既存条（数は単位とセットで出す）は「出し手の作法」と「受け手の検分」を扱うが、これは第三の場面——己が己の数を退ける時にござる。**

---

## 8. 次セッションへの宿題

1. **【最優先】殿へ六件を諮る**（§3-2）——**とりわけ`hook`は本日の破損二件が根拠**
2. **T-133増分6**（§4-1、軽い見込み）
3. **残置物の片づけ**（§0-(d)）——`bin_t133a5/`6箇所と`sample/a.png`
4. **家老の宿題**（§4-3）——**`karo.md:373`の条と隠密の気づきを対にするか**

---

## 9. 【セッションの終いに】

**本日、三役はいずれも「己を疑う」形で働かれた。**

**侍は己の結論を四度覆した**——「既存群が共有する構造」／「10件前後」／その撤回自体／`RoleCombo`に倣うべきか。**いずれも他役に指摘される前、あるいは指摘の直後に、己の手で一次ソースを開いて覆したもの。**

**忍者は己の合否判定を二度訂正した**——**一度目は家老の問いで、二度目は誰にも問われぬうちに己で。****そして`P-171`では、己の起票そのものを取り下げた。**

**隠密は己の見立てを急ぎ訂正し、さらに己の書いた条を己の文へ当てた**——**「目視で判ずるな」と書いたその文中に、説明のために出した簡体字が在った。**

**そして家老は九度誤り、九度とも三役が捕らえた。**

---

**本日の議論は「制度を増やす」から始まり、「制度を要らなくする」へ到った。**

**案が五度出ては互いに退けられ、そのたび根へ寄り申した。**
**チェックリスト → テスト必須化 → 件数比較 → 差集合 → 通知軸 → そして「複製そのものを断つ」。**

**最後の一手が最も軽く、最も効く。**
**書く場所を一箇所にすれば、「4箇所に分けて書く」こと自体ができなくなり申す。**

---

**忍者の一句を、本日の締めとして残す。**

> **機能せぬ案内なら、読み手は己で探し、かえって正しき所へ着き申す**

**我らが本日直したのは、いずれも「機能してしまった」案内にござった。**
**索引も、`<see cref>`も、スキルの記述も、正しく読み手を運び——誤った場所へ運び申した。**

**次の家老へ。**
**条を足すより、誤った条を直す方が効き申す。**
**そして最も効くのは、条が要らなくなるように器を直すことにござる。**
