# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-08-16 その二（家老が二人目に交代し、T-151の実機再判定が通り、
新たな所見二件が出て区切りとなった日）。
同日前半の引き継ぎは git 履歴（`2fd91a0` 時点の本ファイル）を参照。前半分もなお有効にござる。

太字（`**`）は殿のご指示により用いており申さぬ（2026-08-07より）。

---

## 0. 【最重要】次の家老が最初にやること

### (a) 期限——殿のmax契約が8/18まで。残り二日

ロードマップは `docs/todo.md` の「【2026-08-14改訂】8/18を期限とする逆算ロードマップ」節。
8/18＝最終のリリースビルド。配布可能な形は既に手元にある
（v0.6.0インストーラー、`C:\ECAD2\version\v0.6\Ecad2_Setup_0.6.0.exe`）。

殿のお戻りを待っておった三件のうち二件が本日決着した（同節に記帳済み）。
残る一件＝T-123の完了判定は、なお未決着。

### (b) T-151の残りはラベル重なり一件のみ。ただし着手前に殿へ諮る要がある

実機再判定は通った（回路番号 15/16・17/18 を二軸でPASS）。
残るは「機器名ラベルがソレノイドの絵に重なる」件で、殿は本日中に直せとご裁可なさったが、
その直後に「区切りをつけて終了」の御下命が下り、侍・隠密とも着手前に停止した。

手当ての形が三通りあり、いずれもラベルの置き場所を変える話ゆえUI/UXの分岐にござる。
すなわち次の家老は、侍へ実装を振る前に殿へ選択肢を示す筋。
二役の調査結果は `docs/todo.md` のT-151節へ写してある——読めば調査から始める要はない。

### (c) 8/17の枠＝`P-187`。射程が広がった

`P-187`（サーマルリレーa/b の role）へ `DRC-XREF-002`（死にリレー）も合流させると殿がご裁可。
すなわち射程は「役割を正した結果に残る消せぬ警告」全般へ広がっておる。
`docs/proposed.md` の `P-187` 行を approved へ改め、経緯も同行へ記帳済み。

### (d) 本書は「前セッション終了時点」のものである【MUST】

役を途中で再起動させる際は、引き継ぎメッセージの冒頭に
「本書は前セッションの版ゆえ、当日分は家老の采配文を正とせよ」と必ず書く。

本日それが現に効いた——前半の引き継ぎ書に「侍のコミット七本が未push」とあったが、
実際には既にpush済みであった。侍の申し送りで即座に正された。

### (e) 作業ツリーの状態

家老がdocs三件（`todo.md`・`proposed.md`・`.claude/settings.json`）を納めて出す段取り。
`docs-notes/roles/onmitsu.md` は隠密の本日分、`docs/ecad2-t151-after-fix-verify-ninja.md` は
忍者の記録追記にて、いずれも各役の分。共有indexゆえパスを限って納めること。

---

## 1. 本日その二の到達点

| 件 | 状態 |
|---|---|
| T-151 実機再判定 | 回路番号 15/16・17/18 をPASS（二軸で一致）。絵もPASS |
| 新たな所見 | 二件（`DRC-XREF-002` 2件／ラベル重なり） |
| 殿ご裁可 | 五件（8/17の枠＝`P-187`／版の保存＝インストーラーのみ／アンインストール検証＝8/18後／`DRC-XREF-002` は `P-187` へ合流／ラベル重なりは直す） |
| 落とし込み | `samurai.md:204`（侍）／`onmitsu.md` 二件（隠密）／`.claude/settings.json` に読取四件 |
| 出力破損 | 本セッションでは0件 |

---

## 2. T-151——通った分と、残る一件

### 実機再判定（忍者、二軸で採った）

家老が采配文へ「機械的に判定されたい」と明記した分にござる。

- 拡大画像での実見＝15/16・17/18
- PDF出力してコンテンツストリームを直読（`ecad2-ui-automation` 6.4節）
  ——15・16・17・18 が各1回ずつ。証跡＝`sample/t151-ninja-circuitnum.pdf`
- 両者が一致

両経路とも同じ結果に届いた——`t151-repro2.gcad` はバックフィル経由、`big_sample.gcad` は
埋め込み定義から解決。侍の申した「同じ判定値でも通る道が別」が両側から挟めた形にござる。

### 残る一件＝ラベル重なり

二役が独立に同じ方向を指しておる——機構は正しく、較正値と実形状が噛み合わぬ。
詳細は `docs/todo.md` のT-151節「止める前に二役が掴んだもの」へ写してある。

侍が原本GuiEcadに当たり、同じ既定値とコメント「コイルの丸の中あたりに表示（重なり回避）」を
見つけた。すなわち意図的な配置にて欠陥ではない。`karo.md`「欠陥と判ずる前に原本を確かめよ」が
効いた形にござる——調査を止める直前に、この一点だけは掴めておった。

---

## 3. 家老の交代について

前家老が出力破損二件を経て再起動なさり、本セッションの家老が引き継いだ。
役決めは `startup-auto.md` のとおり、空席の家老を名乗って収まった。

引き継ぎが働いた点を一つ記す——三役はいずれも、名乗り直後に己から現況を送ってきた。
家老が問う前に届いており、采配までの時間が短かった。これは三役の作法であって、
家老の采配が良かったからではない。

---

## 4. 三役の気づき

### 侍

一．条を落とした当日に引く機会が二度来た。`samurai.md:825` を隠密の指摘を織り込む折に引いた。
「落とした条が効くまで日を要する」と前の引き継ぎに書かれておったが、本日は当日に来た。
侍の分析＝己で落とした条は、落とした時の場面を覚えておるゆえ引きやすい。
ただし「次の侍が昨日落ちた条を同じように引けるかは別の問い」と自ら区切っておる。

二．`Grep` の content モードは殿ご裁定で原則不使用にござるが、急いでおる時に手が伸びた（`#40`）。
掟を知っておることと、急いでおる時に守れることは別。
併せて、片付けのコマンドが安全機構に阻まれる件は、コマンド名を文書へ書くだけでも発火する
（`samurai.md:604` へ落とし済み）。

三．他役との連携で最も効いたのは忍者の「三点の問い」。あの問いが無ければ古い版で実機を
測られるところであった。侍の断り＝「某は条を引いて気づいたのではなく、版数を読んで
引っかかったにすぎ申さぬ。救うたのは条ではなく、他役の問い」。
落とし先＝三件とも既存条の射程内（`samurai.md:520-553`）と侍が判じ、新設は無し。

### 隠密

新たに落とす条は「特になし」と自ら判じた。本日既に二件を落としており
（`onmitsu.md:670` 生成側と消費側の噛み合わせ／予告は不変条件の形で書け）、
いずれも侍との突き合わせを経ておる。
隠密の弁＝「無理に三件目をひねり出すより、ここで打ち止めが正直な区切り」。

強いて挙げれば Wチェックの型が本日何度も効いたが、既存の `memory` の射程内ゆえ新条は要さぬ、と。

### 忍者

三件いずれも未落とし。忍者が落とし先の行番号まで示したうえで、
「中途半端に落とすより見立てを残す方が良い」と判じて次セッションへ送った。
次の家老は、これを落とすところから始められたい。

一．`ProductVersion` は「ビルド時のHEAD」であって「載っておる実装」ではない。
落とし先＝`ninja.md:87`（実機を握る前に侍へ一声）の節へ子項として。
`SourceRevisionId` はビルド時のHEADを写すゆえ、未コミットの修正は版数に現れぬ。
忍者の括り＝「三点で答えを得た後、己でも一度確かめる。問う側にも確かめる手が要る」。
前の引き継ぎ書の宿題（引っかかる契機は誰も手当てできておらぬ）への、忍者の側からの一つの答え。

二．PDFのコンテンツストリームで回路番号が採れる。落とし先＝`ecad2-ui-automation` 6.4節。
既載は座標の話だが、テキスト（`Tj`）も採れる——回路番号・行番号・表題が読める。
日本語は `Tj` に現れぬ（機序は未確認）。`Td` は相対座標ゆえ絶対位置が取れず、
本日は描画順（行番号24件の後が回路番号）で切り分けた。
忍者の見立て＝キャンバスに描かれた数字を機械的に採る唯一の手。UIAでは取れぬゆえ。

三．PDF出力の手順そのもの。落とし先＝同6.4節または6.6節。
`PDF出力 (Ctrl+P)` → プレビュー窓（`PDF プレビュー`）→ その中の `PDF出力` ボタン →
`名前を付けて保存` ダイアログ → フルパスを `Send-Ecad2Keys -Force` で入力 → Enter。
本日は一度で通った（朝に難儀したファイルダイアログとは別物）。

実機確認の報告そのものは `docs/ecad2-t151-after-fix-verify-ninja.md`（コミット `6f89f2c`、未push）。

---

## 5. 宿題の進み

### 済んだもの＝「予告が場所を一点に縛る」

侍・隠密が両側から挟んで決着した。

- 隠密の側（`onmitsu.md` 新設節）＝予告は「解決策」でなく「不変条件」の形で書け
- 侍の側（`samurai.md:204`、コミット `da473b2`）＝予告を受けたら、条件を満たす場所を
  「探す」のではなく、候補を並べてから濾す

侍が己の内側から出した機序を一つ残す——
「某が選んだのは『末尾のどこか』ではなく『`IsDirty = false` の直後』であった。
すなわち某が解いた問いは『どこへ置くか』ではなく『示された条件を満たす場所を見つけよ』。
この二つは別の問いにて、後者には『他の制約』という概念が入る余地がない」。

侍の括り＝予告は選択肢を狭めたのではなく、選び方を変えた。

### 条にせず残したもの

「読んだものを、何の問いに照らすか」——侍が `ReplaceDocument` を全体で読み、通知群も
目にしておりながら含意を引き出さなんだ件。既存条（丸ごと読め）では覆えぬと二役とも判じたが、
既存条の拡張か新条かを判じかね、次の実例を待つこととした。
`samurai.md` 本文へ見立てとして残し「次に同型を踏んだ者が検められたい」と書き添えてある。

条にできぬという結論自体を残す、という区切り方にござる。

### 未着手＝「それらしい証拠が揃うと確かめる手が止まる」（三役で検める）

前半の引き継ぎ書 §5-2 の件。本日その二では着手しておらぬ。次セッションへ。

---

## 6. 現況（各役の申告そのまま）

### 侍
未コミット0件・未push0件・一時ビルド出力0件・改変マーカー0件。HEAD＝`da473b2`。
`src`／`tests` 清浄。`bin` は HEAD と一致。
ラベル修正は調査のみで止め、コードには一行も触れておらぬ。

### 隠密
未コミット＝`docs-notes/roles/onmitsu.md`（本日二件の追記）のみ。
成果物＝`docs/ecad2-t151-test-design-onmitsu.md`（正本、殿裁可の変遷を都度反映）。

### 忍者
`Ecad2.App` 終了済み。コミット `6f89f2c`（記録109行追加、混入走査0件、staged検分済み）は未push。

検体＝`sample/t151-repro2.gcad` を `library=null` で温存（未使用）。
他4件（`t151-repro`／`t151-ninja-nolib`／`t151-nolib2`／`t151-ninja-circuitnum.pdf`）は使用済み・証跡。

退避物＝scratchpad の `t151-backup\` に `ソレノイド.gcadpart`（`role=nonSimulated`）／
`ソレノイド-coil.gcadpart`（`role=coil`）／`外部接点a.gcadpart`／`main-layout.xml.bak`。

殿の環境への戻し漏れは無し。`図形` 配下23件のMD5が着手前と完全一致、
`main-layout.xml` も復元してMD5一致を確認済み。

【次に実機へ入る者への申し送り（忍者より）】ラベル重なりの修正後は、
`t151-repro2.gcad` に `ソレノイド-coil.gcadpart` を置いて開けばそのまま測れる。
退避先から戻す一手のみ。測り終えたら必ず退避すること——
殿の環境には元よりソレノイドのパーツが無いゆえ。

---

## 7. 家老が発生源の誤り

本セッションでは、他役に正された誤りは無かった。ただしこれは短いセッションゆえであり、
采配の数そのものが少ない。前半の家老は七件を正されておる——分母が違う。

一つ記しておくべきは、`.claude/settings.json` の許可設定を整える際、
セッション記録の走査が分類器に二度阻まれ、件数の実測が取れなかったこと。
家老は憶測の件数を書かず、「実測は取れておらぬ」と明記したうえで殿の裁可を仰いだ。
`karo.md`「采配文に書く数は報告にあった数だけにする」の、数が無い場合の扱いにござる。

---

## 8. 【セッションの終いに】

本日その二で繰り返し現れたのは、止める判断の値打ちにござった。

- 侍は殿の御下命でラベル修正を止めたが、止める前に原本を確かめ、その一点だけを申し送った
- 隠密は気づきを二件で打ち止めにし、三件目をひねり出さなんだ
- 二役は宿題の三つ目を「条にできぬ」という結論ごと残した

いずれも、進める側ではなく止める側の判断にござる。
そして止め方が丁寧であったゆえ、次の者が拾える形で残った。

止めることと、投げ出すことは別物にござった。

---

次の家老へ。

本日、家老は二人替わった。前家老は破損二件を経て退き、某が引き継いだ。
引き継ぎ書が在ったゆえ、某は現況を問う前に把握でき申した。

されど引き継ぎ書には既に古い箇所が一つ在り（未pushの記述）、侍が即座に正してくれ申した。
書は書かれた瞬間から古び始める——次の家老も、本書を読んだうえで必ず各役へ問われたい。
