# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-11（家老記す、殿PC不調につき早じまい）

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**現在のブランチは `t062-net10-migration`**。docs/todo.md更新＋隠密レビュー書
（`docs/ecad2-t062-migration-review-onmitsu.md`）をコミット・push予定（本メモ直後に実施）。
mainへのマージはまだ行っていない（T-062の実機回帰が主要操作フロー未検証のため）。

---

## 1. 目的とDoD

**進行中の本丸はT-062（.NET 10移行）の検証パイプライン完走**。
- 侍実装：完了（commit `143e1c0`、6csprojのTFM更新のみ、機能変更なし）
- 隠密静的レビュー：完了（4観点ともクリーン、`docs/ecad2-t062-migration-review-onmitsu.md`）
- 忍者実機回帰：**起動確認のみ完了・退行なし**（dotnet build 0警告0エラー→起動正常、ステータス
  バー初期値も正常）。**主要操作フロー（配置・結線・保存/読込・Undo/Redo）は未検証のまま中断**。

DoD未達＝主要操作フローの実機回帰が残る。それが済めば家老がマージ・push→T-072（v0.3）着手可。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- T-062のTFM更新自体・PDFsharp互換・ビルド/テスト全合格（Core64件・App419件）・隠密静的レビュー
  4観点クリーン・忍者による起動確認（退行なし）——いずれも上記の通り

### pending（殿判断待ち、急ぎでない）

- P-045〜P-050（既存）、P-055（T-051実機確認の副次発見、SelectedCell見た目上の消失＝Redoで復元可）
- **P-056（新規、本セッション）**：本日15:44頃、殿PCでキーボード入力のみが完全に不通となる
  フリーズが発生し、殿がスタートメニュー経由（マウスは生存）で手動再起動。直前の忍者セッションの
  最終操作が、Ecad2.Appの「開く」ダイアログへ`SendKeys::SendWait`でファイルパス＋Enterを送る
  手順で、その完了ログが残らぬまま途切れていた。**技術的仮説（未確定・再現未実施）**：.NET
  `SendKeys`のジャーナルフック方式がシステム全体のキーボード入力を巻き込んで凍結させた疑い。
  `ecad2-ui-automation`スキルは元々SendKeysを「フォーカス強奪注意」としているが、システム規模の
  入力凍結は初の疑い事例。**再発防止（SendKeysの使用箇所見直し・クリップボード経由への置換等）の
  要否は殿裁定待ち**、現状はpending記録のみで着手せず。

### 【最重要・要再開】T-062の主要操作フロー実機回帰、中断中

忍者による実機回帰は起動確認までで停止（殿のPC不調につき本セッションを終了したため）。
次回は同観点（配置・結線・保存/読込・Undo/Redo）から仕切り直す。**ファイルを開く操作の実機確認では
SendKeysによるパス直接入力を避け**、既存の別手段（最近使ったファイル一覧・UIボタン操作等）を
優先する方が無難（P-056の疑いが晴れるまでの暫定回避）。

## 3. 試して失敗したアプローチと結果

- 今回新規の失敗事例なし。T-062の実装・レビューは往復1周もなくクリーン確定。中断は環境要因
  （PC不調）であり実装・レビューの手戻りではない。

## 4. スコープ境界・制度（既存、継続適用）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／コミットは
  パス限定形／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】
- T-062のブランチ運用：作業前に専用ブランチ作成→検証パイプライン完走後に家老がmainへマージ・push
  （T-045と同様、mainを直接汚さずリスク回避）
- 便乗拡大禁止（P-045〜056も着手せず記録のみが正）

## 5. 次の1手（再起動後の家老がやること）

1. **役割自動決定**後、**采配の全量再送**（アイドルpeerは采配を拾わない鉄則）
2. 殿のPC状況（環境不調が解消したか）を確認してから、T-062主要操作フローの実機回帰を忍者へ
   再采配する（急がず、殿の様子を見てから）
3. P-056（SendKeys起因の疑い）について、殿から再現検証や対策の指示があればそれに従う。指示が
   なければ記録のみで進める
4. T-062の実機回帰完走後、家老がmainへマージ・push→T-072（v0.3）着手判断
5. 他の着手可能なタスク（T-052〜054は既にDone、T-063〜071は棚卸し起票済み・未着手）は、T-062完了
   後の優先順位どおり進める

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。**ブランチは`t062-net10-migration`のまま。T-062は主要操作フローの実機回帰待ちで
中断中（殿PC不調のため）。**
