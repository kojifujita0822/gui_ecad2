# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-27（家老記す、**T-125増分α決着／T-130原因究明と幅修正／新規タスク3件起票**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**本日のコミットはすべてpush済み**。次回セッション開始時、家老は念のため`git log origin/main..HEAD`と
`git status`で混入チェックを行う。

### 【要注意】T-130の一時計装が`main`に残置されている

**`src/Ecad2.App/Diagnostics/T130LayoutTrace.cs`（本体）＋`MainWindow.xaml.cs`への打点が
入ったままである。** **意図的な残置**であり、**P-141として除去タスクを起票済み**。

**【訂正2026-07-27】「打点8箇所」は誤り。実出現は13箇所**（`MainWindow.xaml.cs`に11、
`T130LayoutTrace.cs`に2）。**侍が除去の着手前に数え直して捕らえた**——**8を前提に作業すれば
5箇所が取り残されるところであった**。**家老が書いた数であり、家老の落度にござる**
（`onmitsu.md`「転記と再現は別物」は**書く側にこそ効く戒め**）。
**なお本件は同日4度目の数え上げ誤りであり、発生源が家老であったのは初にござる。**

- **マーカー`T-130 診断ログ用の一時改変`で一括検出できる**
- **除去の条件＝殿が「高さ（ツールバーを覆う）」の是非を裁可され、追加観測が不要と定まった時**
- **リリースビルドを行う前には必ず除去すること**
- 除去の際は**他の一時計装と横断で洗い出す**（`memory: feedback_temp_instrumentation_removal_discipline`）

### 殿の実運用ファイル

**`main-layout.xml`は無傷**。忍者が検証のたび退避・復元し、最後に差分ゼロを確認済み。
検証で使った控えは`sample\t130-diag-ninja\`・`sample\t130-layout-backup\`配下（`sample/`はgitignore対象）。

---

## 1. 目的とDoD

**殿の裁可により本日はT-125（App層全体の見直し）から入った。** 途中、殿ご自身の実機確認により
T-130（シートパネル位置ずれ）が動き、そちらが本日の主戦場となった。

| | 中身 | 状態 |
|---|---|---|
| **T-125 増分α** | 境界ガードの補填（配線分断・接続点・自由線） | **Done**（往復2周、検証パイプライン完了） |
| **T-125 増分β** | ヒットテスト優先順位の共通化 | **計画起草済み**。β-1のみ着手可、β-2/β-3は殿裁可待ち |
| **T-130** | シートパネルのAutoHideフライアウト異常 | **幅＝Done**／**高さ＝殿の判断待ち** |
| **T-131・T-132** | 主回路系パラメータUI／シート設定項目追加 | **起票＋設計方針の殿裁可済み、実装未着手** |
| **新規起票** | T-130／T-131／T-132、P-136〜P-141 | 完了 |

---

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-125増分α**——`ValidatePlacement`の横展開という当初の前提は**侍が原本検分でことごとく覆した**
  （横展開不可／欠落は3経路のみ／弁別ロジックは2箇所でrule of three未達）。**殿裁可＝C-1（配線分断に
  行上限も課す）／α-3見送り／拒否時はステータスバーへ理由を出す**。往復2周を要し、**2周目で真因が
  「入口ではなく尺度」と判明**（`MarginMm=20.0`が範囲外セルの負値を吸収し、mm下限ガードが常に素通り）。
  最終＝Core 280・**App 861**全合格、隠密のフル観点レビュー指摘なし、忍者の実機確認**9通りすべて拒否**
- **T-130 幅**——`AutoHideWidth=0`ゆえ`AutoHideMinWidth`既定100.0が使われていた。**殿裁可＝保存値が
  無い時だけ190pxを入れる**。忍者の実機確認＝**幅を500pxへ変えて正規終了→`main-layout.xml`に
  `AutoHideWidth="500"`が書き出され→再起動で尊重される**（殿裁定の眼目が成立）
- **T-131・T-132の設計方針**——いずれも**原本GuiEcadの調査を経て殿裁可済み**。
  T-131＝P-100はプロパティパネル内ComboBox（原本に在り＝実装漏れ）／P-101は**配置前選択メニュー**
  （原本は事後編集UIを**意図的に**持たぬ）。T-132＝列数は**下限2・上限40**（原本の20では既定値40と
  矛盾）、入力検証は**ecad2方式＝エラー表示しOKを弾く**

### 判断待ち・未着手

- **【最優先の判断待ち】T-130の高さ**——**フライアウトが全高に広がりツールバーを覆う**
  （実測＝フライアウトY=117・高さ686、ツールバーは概ねY=95〜135）。**AvalonDock標準の振る舞い**
  （`managerSize.Height`）ゆえ、対処するなら**T-110増分1の単一DockingManager統合という設計判断に
  触れる（規模大）**。**この判断が済むまで一時計装は残置**
- **【判断待ち】他パネルも同じ穴**——機器表・プロパティ・出力も保存XMLに`AutoHideWidth`を持たぬ。
  忍者の実測＝**細さの実害は見て取れず、シートパネルほど目立たぬ**（縦置き／横置きの差か、留保つき）
- **【判断待ち】T-125増分βの案C（P-077）**——**幅広部品を左端以外でクリックした時の扱い**。
  侍が三案を立て**案C（`SelectedCell`を要素のアンカーへ正規化）を推奨**。**案Aを却下した決め手＝
  UIA完全一致2件と食い違い、忍者の検証手段そのものを壊す**。殿へ「(1)今のまま選択できぬ (2)選択でき
  カーソル枠は左端へ寄る (3)選択でき枠はクリック位置に留まる」の形で諮る段取り（侍推奨=(2)＝案C）
- **T-125増分β-1＝家老裁量で着手可と裁定済み**（`return`欠落補填＋`OpenFrameLabelEditor`経路の非対称）。
  **未着手**。β-2（チェーン共通化）・β-3（当たり判定の一本化）は殿裁可を要す
- **T-131・T-132の実装**——**T-131は忍者の実機症状確認（ecad2側の現況）が未了**
- **T-123（インストーラー化）／T-122（ドロップゾーン）**——Approved、着手時期は殿の判断待ち
- **T-125 γ/δ**——隠密の事前調査2段完了。**α/β完了時点で構造を見てから要否を再評価**が殿裁定
- **`proposed.md` pending**——本日**P-136〜P-141の6件を起票**。うちP-140は対処不要と判断済み

---

## 3. 試して失敗したアプローチと結果（今回の教訓）

1. **【最大の学び】T-130の観測は3周を要し、躓きは毎回「見立て」の側にあった**——
   **1周目＝見る対象を誤った**（`LayoutAnchorable`自身の`PreviousContainer`。実際は親の
   `LayoutAnchorGroup`が持つ）。**2周目＝契機を誤った**（`DockingManager.LayoutChanged`は名に反し
   `Layout`依存関係プロパティ差し替え時しか発火せぬ）。**3周目＝見ている層を誤った**（モデルばかり
   採り、症状が現れる表示の層を素通り）。**そして3周目の値は既にログに採れており**、忍者が
   「フライアウトの実座標が採れる」と報じながら症状と結びつけず、家老も同じログを読みながら
   気づかなかった。**採ることと読むことは別物であった。殿が写真をお示しくださらねば、なお当てもなく
   計装を積み上げていた。**
   → **制度化済み**＝`samurai.md`「計装を足す前に既に採れている値を読み尽くせ【MUST】」／
   `ninja.md`「症状は数値で添える／検証前に症状を数値へ翻訳する【MUST】」／
   `memory: ecad2_symptom_to_number_translation`（新設）
2. **隠密の仮説が空振りした**（`CollectGarbage`が`PreviousContainer`を強制クリアする筋）——
   **殿が症状を目にされたのにログには消失が皆無**という形で否定された。隠密の自己分析＝
   **「複雑だが筋の通る理論」を「単純な説明」より先に検証してしまった**。要因は(a)家老の初動指示が
   過去の成功パターン（T-099(c)）へ誘導していた (b)症状の文言からモデル層寄りの解釈へ早々に固定した。
   → **制度化済み**＝`memory: feedback_investigation_layer_and_scope`へ**「理論の複雑さの順に検証する」**
   を追記。**家老は初動指示で過去の成功パターンへ誘導する際、それが枠を狭めうることに留意する**
3. **調査書の数え上げ誤りが本日3度出た**——弁別ロジック「4箇所」→実は2箇所／境界ガード「9/9」→実は
   7/9／当たり判定「3実装」→実は5箇所（UIA経路2件が**家老の台帳・隠密の調査書・侍の計画書すべてから
   漏れていた**）。**うち2度は隠密の調査書に発し、いずれも過去の別調査書からの転記**であった。
   **3度目は「βの3系統も改めて裏を取れ」という家老の指示で侍が着手前に捕らえ、手戻りが出なかった。**
   → **制度化済み**＝`onmitsu.md`調査ワークフロー2項へ「**転記と再現は別物**」「数を報告する前に個々の
   項目を列挙してから合計を出す」「数字が偶然一致した時こそ疑う」／
   `memory: feedback_preliminary_survey_not_final`を「調査書の引用部分も二次情報」へ拡張
4. **侍が「己が却下した理由を、己の実装で踏んだ」**（T-130幅修正1周目）——`AutoHideMinWidth`を190固定
   にする案を「リサイズの自由度を奪う」として却下しておきながら、**同じ問題を「起動毎に巻き戻す」
   別の形で再導入していた**。**隠密のレビューが捕らえ、殿裁定を動かした。**
5. **【殿指摘】照会文に明記せねば各役は`send_message`で返信しない**——セッション終了時の気づき照会に
   隠密が応答せず発覚。調べると**`ninja.md`には該当節があったが`samurai.md`・`onmitsu.md`には無く**、
   **役割mdの穴と家老が明記しなかった落度が重なっていた**。`memory:
   feedback_always_send_findings_to_peer`（殿指摘2026-07-18）の**再発**——**メモリだけでは防げず、
   両側の手順まで落として初めて効く**。
   → **制度化済み**＝`samurai.md`・`onmitsu.md`へ同節を追加／`karo.md`へ
   **「照会文には『必ず`send_message`で返信せよ』と明記する【MUST】」**
6. **家老の落度**：(a)**T-130の初動采配で「AutoHide復帰処理系を疑え」と過去の成功パターンへ誘導し、
   隠密の枠を狭めた**（上記2） (b)**同じログを読みながら`Screen`欄の意味に気づかなかった**（上記1）
   (c)**気づき照会に明記を欠いた**（上記5） (d)隠密の所見「AutoHide中のScreen x=2000は画面右寄り」を
   **忍者の起動報告（ウィンドウL=1960、セカンドモニター）と突合して否定できた**——
   **絶対座標をモニタ構成抜きで読むと偽の差分が見える**

---

## 4. スコープ境界・制度（今回の追加）

### 役割mdへ
- **`samurai.md`**——(1)境界検証チェックリスト**項目0**＝**「そのガードは入力の出所に合った尺度か」**
  ＋テストは変換を通した形で書く＋効果測定は「入力の出所に合った尺度のガードを持つ数」で数える
  ＋**`readonly struct`の宣言済み既定引数はパラメータレス生成では効かぬ**／既定値は「宣言」でなく
  「実際に構築される経路」を辿って確かめよ (2)新設節**「述語を切り出したら呼び出し側にもテストを置く
  【MUST】」** (3)項目6へ**「漏れと意図的な不在は同一タスク内で混在しうる」** (4)対称性節へ**数値の
  実例**（非正方形にしたから4件RED、正方形なら0件） (5)診断ログ連携節へ**「計装を足す前に既に採れて
  いる値を読み尽くせ【MUST】」** (6)復元漏れ節へ**「戻すときは`git checkout`でなく`Edit`で」**
  (7)ロック下ビルド節へ**`--filter`併用でRED証明が数秒**  (8)**家老からの照会には必ず`send_message`
  で返信する【MUST】**
- **`ninja.md`**——**「症状は数値で添える／検証前に症状を数値へ翻訳する【MUST】」**（報告フォーマット節）
- **`onmitsu.md`**——(1)調査ワークフロー2項へ**転記と再現は別物**ほか3点 (2)**家老からの照会には必ず
  `send_message`で返信する【MUST】**
- **`karo.md`**——(1)非同期委譲の作法**9項＝調査委譲前に既存調査書を`Glob`で確かめる【MUST】**
  (2)Wチェック**4項＝両者に「相手の結論を見たか否か」を報告へ明記させる【MUST】**
  (3)**照会文には「必ず`send_message`で返信せよ」と明記する【MUST】**

### スキル（`ecad2-ui-automation`）へ
- 6.0節＝**ボタンが`IsEnabled=False`ならまずシート種別を疑う**（配線分断は制御回路シート専用）
- 6.4節＝**重複ガードの警告文言で「記入されたか」を機械判定できる**（撮影に頼らぬ）
- 6.6節＝**「名前を付けて保存」は折りたたみだとファイル名欄が存在しない**
- 3節＝**AutoHideフライアウトのスプリッターは310pxの長距離ドラッグを要する**／
  **検証後の`Stop-Ecad2App`のWARNINGは「常態」**

### memoryへ
- **新設**＝`ecad2_symptom_to_number_translation`（症状は数値へ翻訳してから検証に入る）
- **拡張**＝`avalondock_hidden_invariants_survey_entry_points`（観測の罠2件を追加、計5箇所へ）／
  `feedback_preliminary_survey_not_final`（調査書の引用部分も二次情報）／
  `feedback_investigation_layer_and_scope`（理論の複雑さの順に検証する）／
  `feedback_always_send_findings_to_peer`（両側の手順へ落として初めて効く）

### 既存制度
検証パイプライン既定順／書き込みは侍一元化／コミットはパス限定形／pushは家老のみ／UI/UX分岐は必ず殿へ／
環境依存文字・簡体字・`rm`禁止／報告は簡素に／気づきには落とし先を添える——すべて継続適用。

---

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、`git log origin/main..HEAD`と`git status`で混入チェック
2. **【最優先】殿へ諮る事項**
   - **T-130の高さ**——ツールバーを覆う件。**一時計装（P-141）の除去可否もこれに連動する**
   - **他パネル（機器表・プロパティ・出力）の幅**も同様に直すか
   - **T-125増分βの案C（P-077）**——幅広部品の非アンカークリック。三案を提示済み、侍推奨＝案C
3. **殿裁可を待たず進められるもの**
   - **T-125増分β-1**（`return`欠落補填＋枠ラベル経路の非対称）＝**家老裁量で着手可と裁定済み**。
     隠密が特定したシナリオ（要素A選択→枠Bラベル編集→終了→要素A位置クリック）を**RED証明で再現させる**
   - **T-131の実機症状確認**（忍者）——ecad2側の現況がなお推測のまま
4. **T-132は規模小・設計方針も裁可済み**ゆえ、手が空けば着手できる
5. `proposed.md`のpending棚卸し（本日6件増えた。P-140は対処不要と判断済み）

---

## 本日の三役の気づき（家老が収集。**すべて落とし先へ落とし済み**）

- **侍**：(1)**計装を足す前に既に採れている値を読み尽くせ**——T-130の3周目の計装は、読めば要らなかった
  (2)**Wチェックでは「相手の結論を見たか否か」を明記する**——`MarginMm`の食い違いを家老が裁けたのは、
  両者が独立と分かっていたから (3)**一時改変を戻すなら`git checkout`でなく`Edit`で**——checkoutでは
  「戻し漏れ」も一緒に消えて気づけぬ (4)**`-p:BaseOutputPath`＋`--filter`でRED証明が1回20〜50ms**
- **忍者**：(1)**「採る」と「読む」が切れた真因は、症状が言葉のままだったこと**——言葉と数値は直に
  突き合わせられぬ。**着手前に症状を数値へ翻訳しておけば、ログを見た瞬間に照合が成る**。加えて
  **「`GroupPrev`を見よ」と対象を指定されたゆえ`Screen`欄は「ついでに出ておる値」に成り下がっていた**
  (2)**重複ガードの警告文言は「記入されたか」の機械判定に使える** (3)「名前を付けて保存」は
  折りたたみだとファイル名欄が無い (4)検証後の`Stop-Ecad2App`のWARNINGは常態
- **隠密**：(1)**理論の複雑さの順に検証する**——単純な説明から潰し、複雑な理論は後 (2)**転記と再現は
  別物**——過去の調査書から数字を引くときは一次ソースで再現してから使う (3)**数字が偶然一致した時
  こそ疑う**

**家老の所見**：本日は**「我らの見立てが誤っていた」と分かった回数**が、そのまま成果であった。侍は
計画起草で己の前提を3つ覆し、また「己が却下した理由が己の実装に跳ね返っていた」と認めた。隠密は
自分の仮説を二度、実測で撤回した。忍者は自分の報告の誤りを二度、自ら申告した。**家老も、初動采配で
隠密の枠を狭め、同じログを読みながら気づかず、照会文への明記を欠いた。**

そして**決着をつけたのは殿の写真2枚**であった。**三役が3周かけて当てられなかったものが、一次情報
ひとつで割れた。** 我らが積み上げていたのは計装であって、観察ではなかった——これが本日最大の教訓に
ござる。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
