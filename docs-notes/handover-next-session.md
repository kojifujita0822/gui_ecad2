# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-21（家老記す、**T-101/T-106/T-107(本実装+増分1+増分2)を完了・code-reviewスキル
起動不可の原因確定とmarketplace版導入まで完了し、区切りよくセッション終了**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**現在のブランチは `main`、origin/mainより7コミット先行、いずれも未push**：

```
ba0ebc7 feat(app): T-101 - 配置ツール選択中ツールの恒久ハイライト表示
52d71cd fix(app): T-101 - 自作パーツ選択中もF11ボタンのハイライトを維持
48008f5 fix(app): T-106 - 配置ツールバータブの文字色・非選択背景をダークモード対応
0c3a7d4 fix(app): T-106(5)(6)(7) - ComboBox/未定アイコンボタン/ScrollBarのダークモード対応
bd87250 feat(core,app): T-107 - 機器コメント表示・入力機能の新設
4aecb35 fix(app): T-107 - SelectedElementCommentの選択切替時通知漏れを修正
6553fab feat(core,app,pdf): T-107増分2 - 機器コメントをデバイス単位で共有
```

**未コミット差分5件**（家老が本セッションで編集、いずれもdocs/docs-notes配下）：
`docs-notes/pattern-recurrence-log.md` / `docs-notes/roles/onmitsu.md` /
`docs-notes/roles/samurai.md` / `docs/proposed.md` / `docs/todo.md`

**未追跡ファイル6件**（各役の調査書、コミット対象）：
`docs/ecad2-t101-review-onmitsu.md` / `docs/ecad2-t101-verify-ninja.md` /
`docs/ecad2-t106-review-onmitsu.md` / `docs/ecad2-t106-verify-ninja.md` /
`docs/ecad2-t107-review-onmitsu.md` / `docs/ecad2-t107-verify-ninja.md`

**次回セッション起動後、家老は最優先でこれらをコミット・pushすること**（パス限定形、
push前混入チェック必須、karo.md既定）。

---

## 1. 目的とDoD

前回セッション終了時点の「次の1手」（T-101から順次着手）を引き継ぎ、T-101・T-106・T-107
（本実装+増分1+増分2）を完了させ、途中で発覚したcode-reviewスキル起動不可の根本原因を確定・
対応するのが本セッションの目的だった。T-105・T-100新規発見6は未着手のまま持ち越し。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-101完了**：配置ツール選択中ツールの恒久ハイライト表示（背景色+枠線、殿裁定）。侍実装
  （ba0ebc7）→隠密静的レビュー（2点指摘、うちF11ハイライト維持は殿確認のうえ52d71cdで修正）
  →忍者実機確認（DoD(1)〜(6)全OK、色は画素採取で理論値と完全一致）。DoD(5)（F11経由の部品選択
  リストからの組込部品選択でハイライトが移らない件）はT-021既存仕様の帰結と確定、殿確認済み
  でスコープ外。Done。
- **T-106完了**：配置ツールバーのタブ文字色・非選択背景、インラインバーのComboBox・未定
  アイコンボタン・スクロールバー、いずれもダークモード非対応だった件を段階的に発見・対応
  （侍実装48008f5+0c3a7d4→隠密静的レビュー→忍者実機確認、DoD(1)〜(7)全OK）。既定Aero2
  テンプレートのコピー+色のみDynamicResource化という方針、一次ソース（AvalonDock本体+VS2013
  テーマ）確認で競合皆無と確定。Done。
- **T-107完了（本実装+増分1+増分2）**：ラダー図本体への機器コメント表示・入力機能。GX3の
  未定ボタン1/2の正体が「連続して回路を入力」「連続してコメントを入力」と判明した殿指摘が
  発端。表示位置=機器名は記号の上・コメントは記号の下（同一セル内、GX3の上下2分割構造）、
  表示色=緑、入力欄=プロパティパネル内DeviceNameBoxと同型位置。侍実装（bd87250）→隠密静的
  レビューで**T-079/P-058と同型の重大欠陥発見**（選択切替時、新設`SelectedElementComment`が
  4箇所のOnPropertyChanged発火経路から漏れ、古い値が誤って別要素へコミットされる）→侍修正
  （4aecb35、RED先行証明済み）→隠密再レビュー（他の全`SelectedElementXxx`派生プロパティも
  機械的に突き合わせ、他に見落としなしと確認）→忍者実機確認（DoD(1)〜(9)全OK）。この過程で
  `samurai.md`「新規選択可能状態の横展開チェックリスト」へ項目9を制度化、
  `pattern-recurrence-log.md`へPR-17候補として記帳。忍者の実機確認中、**殿より「同一デバイス名の
  複数要素間でコメントが共有されるべきでは」との追加指摘**があり実測したところ共有されない
  仕様と判明→殿裁定「デバイス単位で共有すべき（GX3準拠）」→**T-107増分2**として`Device.Comment`
  新設・`Element.Comment`廃止の設計変更を侍へ追加采配、実装完了（コミット6553fab、13ファイル
  228insertions/40deletions、ビルド0警告0エラー、テスト+8件失敗0）。
- **code-reviewスキル起動不可の原因確定**：隠密がclaude-code-guideへ照会し、Claude Code
  v2.1.215のバージョンアップに伴う恒久的仕様変更（公式changelog「Disabled automatic /verify
  and /code-review invocation」）と確定。`onmitsu.md`へ制度化・memory記録済み。**殿裁定により
  対応として、marketplace版code-reviewプラグイン（`code-review@claude-plugins-official`、
  Anthropic公式・無料）を`~/.claude/settings.json`の`enabledPlugins`へ家老が追加した**（本
  セッション内の走行中セッションには未反映、次回セッション起動時から有効になる見込み）。

### 実施したが未検証（次回セッションで継続）

- **T-107増分2の検証パイプライン未完了**：侍実装（6553fab）は完了、隠密静的レビュー・忍者
  実機確認は次回セッションへ持ち越し。隠密への采配文には「呼び出し元3系統（画面/PDF出力/
  PDFプレビュー）へのdevices引数配線漏れがないか重点確認」と指定済み。
- **marketplace版code-reviewプラグインの実際の動作**：次回セッション起動時、隠密が最初に
  `code-review`スキルが実際にSkillツール経由で起動できるようになったか確認する必要がある
  （`onmitsu.md`該当箇所に次回起動時1回限りの確認事項として明記済み、確認後は家老へ一報し
  当該注記は削除してよい）。ビルトイン版との名前衝突時の優先順位は公式ドキュメント未記載で
  不明のまま、実際に動くかどうかは試してみないと分からない。

### 未着手

- **T-105**（GroupFrame矢印キー移動対応、Proposed）：殿裁定済み優先順位2番目のまま、着手時
  UI/UX分岐（キー割当・他要素との一貫性）は殿確認必須【MUST】。
- **T-100新規発見6**（ダークモードで要素配置時、シートパネル・部品選択パネルが一瞬
  ライトモードに戻る現象）：優先順位3番目のまま。**新情報（2026-07-21、殿直接指摘・画像
  添付）＝配置ツールバーでツールを選んでからOK確定するまでの間、シートパネル・部品選択
  パネルは一瞬ではなく持続的に白いままと確認**（従来の「配置確定直後の一瞬」という理解とは
  異なる）。持続現象であれば診断ログ注入を待たず通常のスクリーンショット・画素採取でも
  再現・観察できる可能性が高い、次回着手時の重要な手がかり（todo.md T-100節に詳細記載）。
- T-098・T-077・T-068・T-046・T-102（従来どおり未着手）。

## 3. 試して失敗したアプローチと結果（今回の教訓）

今回は大きな手戻りは無かった。強いて言えば：

1. **T-106のスコープが3回段階的に拡大した**（タブ文字色→ComboBox/未定アイコンボタン→
   スクロールバー、いずれも殿の実機観察による連続指摘）。都度「まとめて対応するか個別対応
   するか」を殿へ確認したことで、モグラ叩き的な往復は避けられたが、事前に「ダークテーマ
   対応漏れの全体棚卸し」を一度やっておけば1回の采配で済んだ可能性がある（侍所見も同旨）。
2. **todo.md編集時にEditのold_string不一致が複数回発生**（全角括弧・改行の微妙な差異が原因と
   推測）。文字列を編集する前に必ず直前のGrep/Readで正確な現在の文字列を確認してから
   Editすることを徹底すると手戻りが減る。

## 4. スコープ境界・制度（既存、継続適用＋今回の追加）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／コミットは
  パス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】／
  環境依存文字禁止【MUST】／rmコマンド禁止【MUST】（継続適用、今回の逸脱なし）
- **新規制度化（本日）**：
  1. `samurai.md`「新規選択可能状態の横展開チェックリスト」へ項目9追加（選択中要素の派生
     表示プロパティ`SelectedElementXxx`の選択切替時通知漏れ、T-107発端）。
  2. `pattern-recurrence-log.md`へPR-17候補として記帳（2件目が出れば正式パターン化検討）。
  3. `onmitsu.md`「侍実装のコードレビュー」節へ「機械的突き合わせは現在の行番号を取り直して
     から照合する」を追記（T-107再レビューでの行番号ズレ実例）。
  4. `onmitsu.md`のcode-review恒久無効化の注記に、次回起動時1回限りのmarketplace版確認手順を
     追記（確認完了後は当該注記ごと削除してよい）。
  5. `ecad2-ui-automation`スキルへ4件追記（Disabledボタンへのinvokeエラー・PrintWindow撮影
     ヘルパー化の余地・SheetNavList ScrollBarのUIA検出不可・実機確認前のコード先読み効果）。

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、**最優先で未コミット5件・未追跡6件をコミットし、7コミットをpush**
   （§0参照。コミット前にstaged内容を確認、パス限定形）。
2. 采配の全量再送（アイドルpeerは拾わぬ鉄則）。
3. **隠密へ最優先で2点依頼**：(a) marketplace版code-reviewプラグインが実際に起動できるように
   なったか確認（`onmitsu.md`該当箇所参照、結果を家老へ一報） (b) T-107増分2（コミット
   6553fab）の静的レビュー継続（devices引数配線漏れ重点）。
4. 隠密レビュー完了後、忍者へT-107増分2の実機確認を采配（同一デバイス名の複数要素間で
   コメントが実際に同期表示されるか、PDF出力・機器表への反映も含む）。
5. T-107増分2完了後、**T-105（GroupFrame矢印キー移動対応）**（殿裁定済み優先順位2番目）へ
   着手。UI/UX分岐（キー割当）は着手時に殿確認必須【MUST】。
6. 完了後、**T-100新規発見6の検証**（殿裁定済み優先順位3番目）。「持続する」という新情報を
   活かし、診断ログ注入を待たず通常の画素採取での再現を先に試す価値あり（§2「未着手」参照）。
7. その後、T-098・T-077・T-068等の中規模タスクへ順次着手。
8. proposed.md pending項目の定期確認（P-109・P-111(rejected)・P-113(統合済み)・P-114(統合
   済み)等、変化なしの見込みだが念のため確認）。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
