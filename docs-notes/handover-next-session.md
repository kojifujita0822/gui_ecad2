# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-25（家老記す、**T-122完了・T-121タブヘッダー回帰疑いは誤検出と確定・
T-068増分0〜2完了（増分1は往復4周のメニュー展開バグ調査を経て決着）、殿のご指示で本日は
ここで終了**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**作業ツリーはクリーン**。本日のdocs関連コミット（`75a74c5`）まで完了。ソースコード側は
T-068増分2まで（`e5d54e4`）反映済み。次回セッション開始時、家老がpush前混入チェック
（`git log origin/main..HEAD`）を行う。

---

## 1. 目的とDoD

前回セッション終了時点の「次の1手」（T-122采配、T-068増分0のPoC詳細設計着手、proposed.md
定期棚卸し）を引き継ぎ着手。侍・忍者・隠密が順次参陣し4役体制で稼働、T-122→T-121回帰疑い→
T-068増分0→増分1→増分2と連続して采配・検証パイプラインを回した。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-122（ドロップゾーンオーバーレイの表示条件・判定範囲見直し）完了**：DoD2点（WM_MOVING
  受信起点への表示切替・PlacementToolBarの元の親ペイン実座標への判定範囲狭小化）とも実装、
  隠密静的レビュー（指摘なし）・忍者実機確認（観点1〜3・回帰確認とも OK）完遂。
- **T-121タブヘッダー再表示、回帰疑い→誤検出と確定**：T-122実機確認中に忍者が「フロート化→
  再ドッキング後、常時非表示のはずのタブヘッダーが復活」と報告、Wチェック（侍実測＋隠密独立
  調査）を実施。侍（診断ログ、Header自体はstyleMatch=True・Collapsed終始）・隠密（一次ソース、
  タブストリップ=Items.Count==1判定の標準機構と特定）・忍者（追加確認で自己訂正、起動直後から
  実は表示済みだったと判明）の3者が完全一致、**T-121の回帰ではなく既存の標準機構**と確定。
- **T-068増分0（PoC、形状編集キャンバス操作感検証）完了**：隠密が6論点（描画応答性/折れ線操作/
  弧作図/回転/Undo単位/座標変換・ヒットテスト）を`poc/t068-part-editor-poc/`で実装・評価、
  UI/UX分岐3点（折れ線確定方法/頂点ハンドル編集/弧の開き角）はいずれも殿裁定＝案A（GuiEcad
  踏襲）。6論点すべて「増分3実装に進めて問題ない」と判定、技術的障壁なし。
- **T-068増分1（プロパティ編集：名前/幅高さ/役割＋新規作成/保存/削除UI導線）完了**：UI導線＝
  殿裁定・案B（メニュー「パーツ(_P)」新設）、役割選択肢＝殿裁定・案A（GuiEcad同一8種）。実装
  完了後、**往復4周にわたる重大バグ調査**を経て決着（詳細は3節）——最終的に`CustomPartsMenu_
  SubmenuOpened`が`RoutingStrategy.Bubble`により子孫からの誤発火を受けていたと確定、発火源
  チェック追加で解消。隠密静的レビュー（PR-13型3件目のCanEditDiagramガード漏れも修正済み）・
  忍者最終実機確認（観点1〜4全てOK、3件のバグとも解消・回帰なし）完遂、最終整理でコード正味減
  （コミット`a09e86d`）。
- **T-068増分2（端子編集：RowOffset/BoundaryOffsetのリスト形式追加・削除）実装完了**：GuiEcad
  原本は端子編集をキャンバス上ドラッグ（増分3相当）で行うため、殿裁定「端子=キャンバスドラッグ」
  と隠密プランの「リスト形式」に食い違いがあると侍が発見、**殿裁定＝案A（まずリスト形式で仮
  実装、増分3着手時にキャンバス上ドラッグへ正式統合）**で解消。既存`DeviceTableGrid`パターン
  流用、バリデーション（ポート2点以上必須）・保存時並べ替え（BoundaryOffset昇順）実装。
  build/test exit 0（Core131+App808）、回帰なし（コミット`e5d54e4`）。
- **memory新設2件・スキル/role.md追記**：`feedback_before_after_comparison_both_states_
  required`（before/after比較は両方確認）・`feedback_verify_each_fix_before_stacking_next`
  （対症療法は都度効果検証）。`feedback_avoid_grep_content_mode`・`feedback_static_vs_dynamic_
  investigation`・`feedback_simplified_chinese_character_contamination`へ実例追記。
  `ecad2-ui-automation`スキルへ動的MenuItem展開の罠・標準MessageBoxボタンのInvokePattern
  非対応を追記。`ninja.md`へ「セッション終了時の気づき照会への応答【MUST】」新節追記。

### 実施したが未検証（次回セッションで継続）

- **T-068増分2、忍者実機確認は次回セッション以降に持ち越し**。実装（コミット`e5d54e4`）・
  build/testは完了しているが、隠密静的レビュー・忍者実機確認とも未実施。次回セッションで
  検証パイプライン（隠密静的レビュー→忍者実機確認）から着手する。

### 未着手

- **T-068増分3（形状編集キャンバス本体、高リスク）**：PoC（増分0）は完了・技術的障壁なしと
  判定済み、増分1・2完了後の順番として次に控える。
- **T-068増分4（インポート/エクスポート、低リスク）**：優先度低め、後回し可。
- **T-123（正式リリース準備・インストーラー化）**：T-068完了後着手、引き続き保留。

## 3. 試して失敗したアプローチと結果（今回の教訓）

1. **T-121「タブヘッダー復活」は忍者の誤観測だった**：起動直後の生の状態を実際には確認せず、
   別操作（フロート化直後）で見た画像と混同して「フロート化前は非表示」と報告。隠密の追加
   確認依頼を受けて初めて起動直後を実測し、実は表示済みだったと自己訂正。before/after比較は
   両方の状態を実際に確認してから断定すべし（memory: `feedback_before_after_comparison_both_
   states_required`）。
2. **T-068増分1のメニュー展開バグ、往復1〜2周目のUpdateLayout系対処は効果不明のまま重ねて
   しまった**：HasItems=false問題（1件目）は解消したが、2〜3周目に試した「対象階層を広げた
   UpdateLayout呼び出し」は実際には効いておらず、最終的に真因（`SubmenuOpened`のBubbleイベント
   誤発火）を発火源チェックで解消した段階で不要と判明、最終整理で除去（正味コード減）。対症
   療法を重ねる際は各修正が実際に効いているか都度検証すべき（memory: `feedback_verify_each_
   fix_before_stacking_next`）。
3. **家老自身の誤字混入2件**：(a)peerメッセージで「并配」→「采配」の誤字、送信直後に自己発見
   し即座に訂正メッセージ送信。(b)AskUserQuestion選択肢ラベルで「仮実装」→「仸実装」・
   「侍推奨」→「侶推奨」の誤字、殿指摘で発覚・訂正。「侍」の誤字化はこれで2回目（1回目は
   「侪」）——AskUserQuestionの選択肢ラベルという同一発生源で3回目の混入、パターンとして
   定着しつつある（memory: `feedback_simplified_chinese_character_contamination`）。
4. **T-068増分2着手前、隠密プランと殿裁定の食い違いを侍が発見**：隠密プランは端子編集を
   「リスト形式」と記述していたが、殿裁定は「端子=キャンバスドラッグ」。侍が着手前調査で
   気づき、殿確認へ回して解消。UI/UX分岐は殿裁定済みの既定方針に見えても、実装段階で細部の
   食い違いが顕在化しうる一例。

## 4. スコープ境界・制度（既存、継続適用＋今回の追加）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機確認／書き込みは侍一元化／
  コミットはパス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ
  【MUST】／環境依存文字禁止【MUST】／簡体字禁止【MUST】／rmコマンド禁止【MUST】（継続適用）
- バグ対応のWチェック（侍修正＋隠密独立調査の並行）は本日もT-121回帰疑い・T-068増分1の重大
  バグ調査（往復4周）で機能した（3者一致・完全一致による確定運用が有効と再確認）
- **殿裁定（2026-07-24）＝T-068増分1メニューバグは根が深い可能性を踏まえ、往復2周超のゲート
  適用を待たず3周目以降も継続してよいと事前許可**（本件限定のゲート免除、実際に4周目で真因
  確定・解消に至った）。
- **新規制度化（本日）**：
  1. memory新設2件・既存memory3件へ実例追記（3節参照）。
  2. `ecad2-ui-automation`スキルへ動的MenuItem展開・標準MessageBoxボタンの罠を追記。
  3. `ninja.md`へ「セッション終了時の気づき照会への応答【MUST】」新節追記（家老から気づきを
     尋ねられたら必ずsend_messageで返信、殿指示2026-07-25）。
  4. `pattern-recurrence-log.md` PR-13へ3件目再発（T-068増分1のCanEditDiagramガード漏れ）を
     記帳、実効性再点検の要否は次回検討課題。

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、**作業ツリーはクリーン**（§0参照）。push前混入チェックを一応行う。
2. **T-068増分2**：忍者実機確認から着手（実装は完了済み、隠密静的レビューも未実施のため先に
   回す）。検証観点＝端子タブでのRowOffset/BoundaryOffset追加・削除・編集、バリデーション
   （ポート2点未満は`NonSimulated`以外拒否）、保存時並べ替え。
3. **T-068増分3（形状編集キャンバス本体）**：増分2完了後、PoC（増分0）の知見を踏まえて着手。
   高リスク領域ゆえ、増分2までと同様に慎重な検証パイプラインを維持する。
4. **T-123**：T-068完了までは引き続き着手保留、頭に留め置くのみ。
5. proposed.mdのpending項目の定期確認（次回の頃合いで、`memory:
   project_proposed_pending_periodic_review`参照）。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
