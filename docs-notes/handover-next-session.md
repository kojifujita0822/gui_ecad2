# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-10（家老記す、本日の店じまい）

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**現在のブランチは `main`**。作業ツリーはクリーン（`.gitignore`へ`version/`追加のコミット後）。
**push保留なし**——本日の全コミットはその都度origin/mainへpush済み、最新は`e4bd421`まで、
加えて`.gitignore`更新1件が続く見込み。**gitタグ`v0.2`を付与・push済み**（初の仮リリース）。
テストは323件全合格（Core14+App309）が最新基準。

---

## 1. 目的とDoD

**進行中の本丸はT-055（行数拡張+母線名）の増分3**。計画書=
`docs/ecad2-t055-implementation-plan-samurai.md`。増分1・2は完全Done、**増分3（任意位置挿入・
削除、最高リスク）が残る**。着手前の段取り検討がまだ——PoC推奨と計画書に明記あり。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-050完全Done**・**T-055増分1完全Done**・**T-055増分2完全Done**（いずれも隠密レビュー＋
  忍者実機確認まで完了、詳細は`docs/todo.md`各節および`docs/todo-archive.md`）
- **T-056起票**（キャンバスのグリッド線表示切替機能、殿直接指示。優先度=T-052〜054より後）
- **T-057完全Done**：v0.2.0仮リリースビルド作成（csproj Version設定のみ、`722bf92`）。
  **gitタグ`v0.2`付与済み**。フレームワーク依存型（.NET 8ランタイム前提）
- **リリース方針の殿裁定**：本リリース（正式版）はインストーラー形式、それまでは仮リリース
  （フレームワーク依存の簡易配布で足りる）。インストーラーの技術選定は将来課題
- **家老・侍ともSonnet 5試行中**（本日開始）。侍は本日破損ゼロで完遂（旧Opus 4.8系列は
  1日7件+フリーズ1回、対比は`docs-notes/output-corruption-log.md`対策年表参照）。見守り指標=
  破損頻度・誤采配回数・殿訂正頻度
- **実機確認スキル改修**：`ecad2-ui-automation`をフォーカス非占有化（PrintWindow方式）、殿裁可済み
  push済み。**副次知見=別トップレベルウィンドウ（ダイアログ等）はメインウィンドウ固定の
  Save-Ecad2Screenshotでは写らず、恒久対応は別途要検討**（実害なし、都度個別対応で凌いでいる）
- **RDPループバック分離プランは非推奨で決着**（忍者起草、`docs-notes/ecad2-rdp-loopback-isolation-plan-ninja.md`）。
  理由=Windows 11 Proのシングルセッション制限で殿の現行セッションが強制ログオフされる致命的障壁。
  **代替=殿ご自身がキャンバスクリック・キーボードショートカット操作を必要な折に代行**（忍者が
  在席確認→依頼の運用、ninja.mdへの手順追記は忍者の宿題として未着手）

### pending（殿判断待ち、急ぎでない）

- P-045・P-046・P-047（既存）
- **P-048（新規）**：T-055増分1の行削除クランプがセッタ経由の副作用（記入中ドラフトのクリア等）と
  連動しうる。限定条件のみ、実害小

## 3. 試して失敗したアプローチと結果

- 過去分は`docs/todo-archive.md`参照。本日新規の失敗事例なし（T-055増分1・2とも往復はあったが
  いずれも制度どおりの検出・修正フローで解消、"失敗"ではなく想定の往復）

## 4. スコープ境界・制度（既存、継続適用）

- RED先行証明【MUST】・テスト設計と実装の分離【MUST】（バグ修正・往復案件）・
  テストコード静的レビュー+Stryker棚卸し（往復2周以上のクローズ時）
- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／
  コミットはパス限定形／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】
- 便乗拡大禁止（P-048も着手せず記録のみが正）
- **新規**：`.gitignore`へ`version/`追加（殿の手動publish生成物が未追跡で出現したため、本日対応）

## 5. 次の1手（再起動後の家老がやること）

1. **役割自動決定**後、**采配の全量再送**（アイドルpeerは采配を拾わない鉄則）
2. **T-055増分3の段取り検討**を最優先で進める——計画書はPoC推奨と明記。まず隠密へ事前調査
   （右クリックコンテキストメニュー実装パターン・5種要素のシフト処理の設計案）を采配するのが
   自然な滑り出し
3. T-056（グリッド表示切替）はT-052〜054の後、まだ着手時期でない
4. 忍者のninja.md手順追記（殿代行操作の運用）は手隙の折に催促してよい
5. モデル切替（家老・侍ともSonnet 5）の見守りを継続。誤采配・殿訂正・破損頻度を素直に記録すること

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。**ブランチはmainのまま。家老=Sonnet 5試行中・侍=Sonnet 5。v0.2タグ済み。**
