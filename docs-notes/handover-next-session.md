# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-25（家老記す、**T-068増分2完全決着・増分3を6段に分けて3-a/3-b1/3-b2/3-b3まで決着・
最終増分3-cは実装/静的レビュー完了、忍者再検証の途中で殿より終了のご下命**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**作業ツリーはクリーン、本日のコミット計30件すべてpush済み**（`origin/main`と一致）。次回セッション
開始時、家老は念のため`git log origin/main..HEAD`で混入チェックを行う。

**検証用一時ファイル4件が残置されている**（rm禁止【MUST】ゆえ削除せず、**殿の判断待ち**）。
`sample/`配下はgit管理外ゆえリポジトリへの影響はない：
- `C:\ECAD2\sample\t068-3b3-redraw-test.gcad`（3-b3の再描画トリガ検証）
- `T068増分3a検証部品.gcadpart.bak`（3-cの並べ替え検証のバックアップ）
- `T068増分3c再描画検証部品.gcadpart`（忍者によるとファイル名は誤り、正しくは「再検証部品」）
- `T068増分3c退化検証部品.gcadpart`

---

## 1. 目的とDoD

前回の「次の1手」（T-068増分2の検証パイプライン、増分3着手）を引き継ぎ、**T-068増分2の完全決着と
増分3の大半**を進めた。殿は途中で外出され、**事前裁可3点**（(1)増分3のUI/UX分岐はGuiEcad踏襲で家老が
仮決定し実装まで進めてよい (2)増分2の往復2周超も事前許可 (3)増分3が止まれば侍をT-102へ回す）を賜って
いたため、殿御不在中も手を止めずに進行できた。

**事前裁可の使用実績**：(1)は繰り返し行使（画面構成・ステータス文言・テーマ連動ほか）、(2)は使わず
済んだ（増分2は往復ゼロで決着）、**(3)は家老の誤りで成立しない選択肢だった**——T-102は既にDone
（2026-07-24）でロードマップ表の更新漏れを家老が検分せず殿へ提示してしまった（是正済み）。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-068増分2（端子編集）完全決着**：隠密静的レビュー（要修正なし）・忍者実機確認（観点1〜4すべて
  OK・回帰なし）。バリデーション両側・並べ替えのbefore/after両状態を実測。
- **増分3を6段に細分**（家老采配、高リスク領域ゆえ各段で検証パイプラインを回す方針）：
  3-a（器の再構築）／3-b1（Core層の下拵え）／3-b2（キャンバス本体移植）／3-b3（文字ツール＋保存経路）
  ／3-c（接続点統合）。**当初は3-b一括の予定だったが、性質の異なる作業の同居を避けて3分割した**
  （増分1で大きく作って往復4周を招いた前例を踏まえた判断）。
- **増分3-a 完全決着**：タブ廃止・単一画面構成へ再設計、440x420固定/`NoResize`→800x600・リサイズ
  可能化。隠密が「レイアウト組み替えによる結線切れは**原理的に起き得ぬ**」と判定（本ダイアログは
  DataContext非依存・`x:Name`直接参照方式ゆえBAMLのconnectionIdで解決される）、忍者の実機確認
  （観点1〜5すべてOK）で裏づけ。
- **増分3-b1 決着**：`PartDrawing.DrawPrimitive`のpublic API切り出し（`DiagramRenderer`無改変）、
  `Core/Model/PartShapeGeometry.cs`新設、**テスト65+8件追加**。View層不触ゆえ実機確認は行わず隠密の
  静的レビューのみで確定（家老裁定）。
- **増分3-b2 決着**：PoC 7ツールをキャンバス本体へ移植。**PoC 676行→本実装511行（165行減）、委譲
  15箇所**——裁可5・6（Coreへの一元化）の狙いが実測で裏づけられた。座標変換の2段`PushTransform`合成は
  **侍が設計し・隠密が数式展開で検算し・忍者が実機で画素走査（線は完全一致、矩形も誤差2px以内）**と
  三者が別々の手段で同じ結論に至った。
- **増分3-b3 決着**：文字ツール・`MergeCollinearLines`保存時適用・**既配置パーツの再描画トリガ**
  （侍が着手前調査1.9で見抜いた積み残し、3経路とも実機確認）・キャンバス背景のテーマ連動。
  `MergeCollinearLines`テスト17件追加（Core221）。
- **増分3-c 実装・静的レビュー完了**（忍者再検証は途中、3節参照）：接続点ツール統合・端子リスト廃止、
  Core239。ポート関連の幾何ロジックも`PartShapeGeometry`へ切り出し（家老裁可）。

### 実施したが未検証（次回セッションで継続）

- **【次回最優先】増分3-c 忍者再検証の残りと、観点2の未解決の疑問点**（3節・5節参照）。

### 未着手

- **T-068増分4（インポート/エクスポート、低リスク）**：優先度低め、後回し可。隠密の分岐G（原本の
  「組込み図形を読み込み」「開く」ボタン）は**P-126として起票済み**、増分4の設計時に「原本のこの
  2機能を含むか」を必ず検討事項に含めること。
- **T-123（正式リリース準備・インストーラー化）**：T-068完了後着手、引き続き保留。

## 3. 試して失敗したアプローチと結果（今回の教訓）

1. **【次回最優先の宿題】`heightCells=1`のパーツに`RowOffset=-2`が保存されていた**（忍者が3-c再検証
   中に発見）。新規追加時のクランプは機能しているようだが、**既存の接続点が基準枠（幅高さ）変更時に
   再クランプされていない疑い**（`WidthCells`/`HeightCells`のsetterは`Draw()`のみでポート再クランプ
   無し）。**クリーンな再現は未完了、疑いを疑いのまま記録**（断定も放置もせず次の者が追える形に）。
   家老所見＝「新規追加時はクランプするが後から枠を縮めると既存値が取り残される」筋は**PR-03
   （SetProperty早期return罠）・PR-05（状態リセット漏れ）と同じ匂い**。次回、侍・隠密を交えて詰める。
2. **静的レビューを2度通った実装のバグを実機が捕まえた**：`Tool_Checked`のswitch式に`"Port"`ケース
   だけが欠落し接続点ツールが起動不能。XAML側Tag・キャンバス処理・`ToolLabel`/`ToolGuide`には正しく
   追加済みで**この1箇所のみの漏れ**。**PR-28候補として記帳**、根本解決案は**P-128として起票**
   （`Tag`をenum値そのものにしswitch式自体を消す＝「手順で防ぐ」から「構造で防ぐ」への転換）。
3. **隠密の振り返り＝3増分にわたり同じ穴が空いていた**：「3-b2・3-b3・3-cいずれのレビューも
   `ToolLabel`・`ToolGuide`という**表示系2箇所への確認を繰り返すのみ**で、`Tool_Checked`（入力系）を
   一度も横断的に洗い出していなかった。**前回確認した箇所の再確認に終始し、確認範囲外を見落とす
   構図**」——PR-18と同型。**自ら言語化したことでPR-28候補が立った**。
4. **家老の誤り2件**：(a)T-102が既にDoneなのにロードマップ表の更新漏れを検分せず、殿へ「侍をT-102へ
   回す」という成立しない選択肢を提示（是正済み） (b)3-b1のDoDで5関数を列挙したが`Translate`・
   `CenterOf`・下請け群が漏れており、**侍が気づいて差し戻したから事なきを得た**。後者を受けて
   `karo.md`へ「DoDに具体名を列挙するときは『代表例。網羅は実装者判断』と明記する【MUST】」を追加。
5. **侍が「開発者向け」と見なしたステータス表示は、実は原本のユーザー向け動的ガイダンス**だった
   （隠密が原本を裏取りして判明）。侍の善意の改善が**実際には機能後退**。侍が「UI文言ゆえ独断とせず
   申告した」ことで家老の裏取り指示が入り、事なきを得た。**踏襲は逐語コピーではない**という但し書きも
   同時に判明——原本の弧のガイド文言は実装に無い機能（弧の向き変更）を案内しており、侍が実装実態に
   即して意訳したのを隠密が「原本コピーより優れた判断」と評価。
6. **簡体字混入、最も鮮烈な実例**：隠密の気づき報告に「**气**を配っても出る」——**簡体字混入について
   語っているまさにその文中で混入**。同日「增分」も出しており「以後意識する」と述べた直後の再発。
   memoryへ記録済み。**「知っていること」と「実行できること」は別**であり、PR-27の教訓と同型。

## 4. スコープ境界・制度（既存＋今回の追加）

- 検証パイプライン既定順＝侍実装→隠密静的レビュー→忍者実機確認／書き込みは侍一元化／コミットは
  パス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】／環境依存文字・
  簡体字・rmコマンド禁止【MUST】（すべて継続適用）
- **規模を理由に隠密の静的レビューを飛ばさなかった**（1行修正でも通した。`memory:
  feedback_verification_pipeline_no_skip`＝殿の明示訂正事項）
- **新規制度化（本日、いずれも家老裁量）**：
  1. **PR-27候補**（テスト入力の対称性・退化性による検出力の消失）を`pattern-recurrence-log.md`へ
     記帳。同一タスク内8件ゆえ「候補」だが**制度化は先行実施**。`samurai.md`へ新節、`onmitsu.md`へ
     レビュー観点（**原案は隠密の具申**、家老がレビュアー視点2点を補筆）。
  2. **PR-28候補**（enum新値追加時の全分岐行き渡り漏れ）を記帳。**書き手（侍）と読み手（隠密）の
     双方から独立に同じ観点が提起された**。
  3. `samurai.md`へ**「一時改変の復元漏れを防ぐ【MUST】」**新節（マーカーコメント必須＋`git diff
     --stat`は未追跡ファイルの差分を出さぬという罠。侍の実体験より）。
  4. `karo.md`へ**「DoDの列挙は代表例と明記する【MUST】」**（上記3節4(b)）。
  5. `ecad2-ui-automation`スキルへ**罠5件追記**（タブ切替式ツールバーのUIA消失／メニューPopup中の
     座標クリック不発／**RadioButton切替が反映されぬことがある**／ウィンドウ相対Y=150までは
     キャンバスに届かぬ／ダイアログを閉じた後`GetWindowRect`が(0,0,0,0)）。
  6. memory更新＝`ecad2_gpu_hw_render_blank_screenshot`（**3回再発**の履歴・迂回策）、
     `feedback_simplified_chinese_character_contamination`（上記3節6の実例）。
- **`proposed.md`新規起票3件**：P-126（原本の組込み図形読込・開くボタンが増分5分割案から欠落）／
  P-127（GPU HW描画不全が3回再発、根治調査の要否）／P-128（`Tag`のenum化）。**いずれも殿の判断待ち**。

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、`git log origin/main..HEAD`で混入チェック（**作業ツリーはクリーンのはず**）。
2. **【最優先】T-068増分3-cの決着**：
   - **まず3節1の未解決の疑問点**（既存ポートが基準枠変更時に再クランプされない疑い）を侍・隠密へ
     Wチェックで振る。実害があれば3-cの範囲内欠陥として修正采配。
   - 併せて**忍者の再検証の残り**（観点3＝重複チェックの非対称／観点4＝ヒットテストの優先／観点7＝
     選択状態の二重化／観点8の残り＝矩形ツール以外の回帰確認）。手順案は
     `docs/ecad2-t068-increment3c-verification-ninja.md`にある。
3. **これが通ればT-068完結**（増分0〜3-c）。増分4（インポート/エクスポート）は優先度低め。
4. **殿へお伺いする【報告】事項**（帰参時に一覧でご覧いただけるよう台帳へ記帳済み）：
   - **P-128（`Tag`のenum化）＝家老推奨・隠密の技術検証で穴なし**。本日のPR-28候補への最も筋の良い
     回答であり、承認されれば当該パターンは構造的に消滅する
   - **P-127（GPU描画不全の根治調査の要否）**＝家老の見立ては「撮影を要さない検証手段への代替を軸に
     対症療法併用」
   - **P-126（増分4の設計時に原本の2機能を含むか）**
   - **増分3のUI/UX仮決定11点**（GuiEcad踏襲）——とりわけ**Ctrl+Z/Y・Escapeキーのみ純粋な踏襲では
     ない**（原本に無くecad2独自拡張として維持）
   - **キャンバス背景のテーマ連動**（GuiEcad踏襲の是非ではなくecad2内部の一貫性の問題として家老が
     仮決定）
   - **`MergeCollinearLines`の副作用**＝わざと2本に分けて描いた線が保存時に併合され本数が減って
     見える。**原本と同じ挙動ゆえ仕様として据え置いたが、忍者が証跡（スクショ2枚＋保存ファイルの
     中身）を確保済み**——実機で驚かれる前にお示しするため
   - **検証用一時ファイル4件の要否**（0節参照）
   - **P-122**（シートパネル位置ずれ、T-110完了から3日未着手）の着手時期
5. `proposed.md`のpending項目の定期確認（本日P-104/P-107/P-109は既に決着済みと判明、代わりに
   T-110系4件が長期pendingと隠密が指摘）。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
