# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-21（家老記す、**T-107残り分・T-105・T-100(新規発見6)・T-098・T-077(增分1〜6)・
T-108・T-109を完了、T-110は隠密のプラン作成着手前で中断。侍・忍者・隠密は殿の指示で先に
セッション終了、家老のみ引き継ぎ処理中**）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**作業ツリーはクリーン、origin/mainと完全に同期済み**（最終コミット`bafa3a8`までpush済み、
未コミット・未追跡ファイルなし）。次回セッション開始時、追加のコミット作業は不要。

---

## 1. 目的とDoD

前回セッション終了時点の「次の1手」（T-107増分2の検証パイプライン継続から順次着手）を引き継ぎ、
本セッションでは非常に多数のタスクを完了させた。途中、殿から新規のUI/UX改善指摘（ダークモード
対応漏れ・表示日本語化・パネルタイトル色）が連続して入り、その都度対応した。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-107完了**（增分2の検証パイプライン）：隠密静的レビュー・忍者実機確認とも完了、指摘なし。
- **T-105完了**：GroupFrame矢印キー移動対応。キー割当=案A（無修飾矢印キー）殿裁定、侍実装→
  隠密レビュー→忍者実機確認、DoD(1)〜(5)全OK。
- **T-100完了**（ハッチング模様除去+新規発見6）：新規発見6（ダークモードで配置バー表示中に
  パネルが白化する現象）の根本原因を隠密が特定（PR-20の4例目、`IsMainContentEnabled`による
  IsEnabled継承とAero2 ListBoxテンプレートのStaticResource固定色の組み合わせ）。殿裁定で
  方式1（ControlTemplate差し替え）採用、侍実装→検証パイプライン完了。
- **T-098完了**：シート追加時のPageNumber採番方式見直し。既存最大PageNumber+1方式へ変更、
  RED先行証明済み、検証パイプライン完了。
- **T-077完了（增分1〜6）**：「ヘルプ」→「使い方」画面新設。非モーダルWindow・FlowDocument
  自作変換（案B）・左目次+右コンテンツ（案1）・F1ショートカット、全て殿裁定どおり実装。
  增分3で`docs/spec`全11領域の陳腐化点検を実施し重大な陳腐化多数発見・修正（特にpdf-testmode
  領域は「両機能とも未着手」前提のまま放置されていた最も深刻な例）。增分5でユーザー指摘に
  基づき表構文のFlowDocument変換対応を追加、实装中に無限ループの重大バグを自己発見・修正。
  增分6でステータスバーのツール値説明を追記。
- **T-108完了**：ダークモード対応漏れの全点検。AddSheetDialogのRadioButton文字色を発端に、
  App層全11XAMLファイルを機械的棚卸し。優先度高2件（PdfPreviewDialog Window未指定・
  StatusBar暗黙的スタイル不在）を追加発見・修正。新規パターン**PR-24**（暗黙的スタイル不在に
  よるAero2テーマスタイル本体Setterの継承阻害）を2例判明で確定・制度化。
  **【重要な運用注意】T-109起票時のEdit操作でT-108の見出し行が誤って上書きされ、その後の
  完了記録が一切台帳に記帳されないまま3コミット進んだ事故が発生**（後に発見・復旧済み、
  `docs/todo.md`は現在正しい状態）。原因と再発防止策は`memory:
  feedback_edit_old_string_heading_only_risk`参照——見出し行1行だけをold_stringに指定して
  新規セクションを挿入すると、直後の既存本文が意図せず新規セクションに呑み込まれる。
- **T-109完了**：ステータスバー「ツール」表示の日本語化。訳語は殿裁定で案B（統一感重視）、
  8値全て（選択/要素配置/縦コネクタ記入/グループ枠記入/自由線記入/接続点記入/配線分断記入/
  画像配置）確定。忍者実機確認で、うち2値（接続点記入/配線分断記入）は即時配置処理のため
  実機で表示される機会が構造的に無いと判明（T-041以来の既存設計、実装バグではない）。

### 実施したが未検証（次回セッションで継続）

- **T-110（4分割DockingManagerの単一統合）、プラン作成が前例精読の段階で中断**：殿裁定の
  経緯＝「シート」「出力」タブのみアクティブ色（青）という非対称性の質問→隠密調査で複数
  `DockingManager`（4つ）構成の構造的帰結（バグではない）と判明→対処案3択（現状維持／
  ActiveContent同期処理追加／単一統合）を提示→**殿裁定「将来的なリスクも考慮すると単一統合が
  いい、大分作業が後退するがプランを作成してほしい」**。隠密は`docs/ecad2-t104-toolbar-tabswitch-design-onmitsu.md`
  （前例）を精読した段階で終了、依頼内容1〜5（4つのDockingManager詳細構造調査・統合方式検討・
  影響範囲洗い出し・高リスク領域評価・PoC計画）はいずれも未着手。次回は依頼内容1（`MainWindow.xaml`
  の4つのDockingManager定義・ペイン一覧・テーマ適用処理`ApplyDockingManagerThemes`等の実地
  調査）から着手する。**高リスク領域（`AnchorablePaneControlStyle`系、T-099(c)で3周のモグラ
  叩き経験）に深く関わる見込みのため、プラン→PoC→増分実装＋各増分で忍者検証、の順を厳守
  すること**（`docs/todo.md` T-110節に依頼内容の詳細記載）。

### 未着手

- T-068（自作パーツ管理・編集UI、規模大）・T-046（CI化のみ残置）・T-102（優先度低）・
  T-028/T-022/T-013/T-032（Proposed、未Approved）。

## 3. 試して失敗したアプローチと結果（今回の教訓）

1. **todo.mdのEdit操作でセクション見出しが意図せず上書きされる事故**（§2参照）。新規タスク
   見出しを既存タスクの直前へ挿入する際、old_stringに見出し行だけを指定すると、既存本文が
   新規セクションへ呑み込まれる形になり、後続の完了記録が長期間記帳されないまま進んでしまう
   リスクがある。挿入後は必ず`grep -n "^### T-"`でセクション境界を確認する運用を徹底する
   こと（`memory: feedback_edit_old_string_heading_only_risk`）。
2. **家老の予備調査を過信しない**：T-109で家老が「PlaceConnectorに対応する既存ラベルは
   見当たらない」と予備調査したが、隠密の裏取りで「縦分岐線記入」という既存ラベルが実際には
   存在すると判明。予備調査は当たりをつけるものに過ぎない（`memory:
   feedback_preliminary_survey_not_final`）。
3. **`docs/spec`の陳腐化は一度の棚卸しで終わらない**：T-077增分3で「T-075完了直後のタスク
   のみが高リスク」という見立てで4領域を先行点検したが、実際にはpdf-testmode領域（T-060/
   T-061由来）が最も深刻で、当初の見立てを覆す結果になった（`memory:
   project_docs_spec_staleness_recurs`）。

## 4. スコープ境界・制度（既存、継続適用＋今回の追加）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／コミットは
  パス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】／
  環境依存文字禁止【MUST】／rmコマンド禁止【MUST】（継続適用、今回の逸脱なし）
- **新規制度化（本日）**：
  1. `docs-notes/pattern-recurrence-log.md`のPR-24（暗黙的スタイル不在によるAero2テーマ
     スタイル本体Setterの継承阻害）を正式パターン確定、`onmitsu.md`「値は正しいが反映されない」
     系調査の早期エスカレーション節へ第4パターンとして追記。
  2. `samurai.md`へ「ループ処理の終了保証チェックリスト」（無限ループ防止）・
     「WPFのFlowDocument系はSTAThread不要でテスト可能」を追記。
  3. `karo.md`「非同期委譲の作法」へ項目7「DoDは実装上到達・観測可能な状態かも確認する」を
     追記（T-109実例：8値対応済みでもうち2値は実機で構造的に到達不能だった）。
  4. `ecad2-ui-automation`スキルへ複数追記（保存ダイアログのフォルダ引き継ぎ罠・GridSplitter
     ドラッグのフォアグラウンド化要件等）。
  5. `docs/proposed.md`へP-116〜P-118を新規記録（いずれもpending、範囲外の気づき）。

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、作業ツリーはクリーンのためコミット・push作業は不要（§0参照）。
2. 采配の全量再送（アイドルpeerは拾わぬ鉄則）。
3. **隠密へT-110のプラン作成継続を最優先で依頼**：依頼内容1（4つのDockingManager詳細構造
   調査）から着手、`docs/todo.md` T-110節の依頼内容2〜5も順次進める。高リスク領域ゆえ拙速を
   避け、プランがまとまってから殿へ増分計画を提示すること。
4. T-110のプランが固まり殿裁定を得たら、PoC先行で侍へ实装采配。**この際、検証パイプライン
   （侍→隠密→忍者）を各増分で必ず挟むこと**（規模が大きいタスクほど省略厳禁）。
5. T-110完了後、T-068（自作パーツ管理・編集UI、規模大）等の中規模タスクへ順次着手。
6. proposed.md pending項目の定期確認（P-109・P-116・P-117・P-118等）。

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
