# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-28（家老記す、**T-134完了・T-135起票・T-133の裁定7件が出揃う**）。
前回分は git 履歴（`d0d1e55`以前）を参照。

---

## 0. 【最重要】次の家老が最初にやること

### (a) 気づき13件が**まだ落ちておらぬ**【MUST】

**usage逼迫（90%）でセッションを終えたため、本日の気づきを落とし先へ落とせなんだ。**
**`karo.md`「気づきは落とし先を必須とする【MUST】」に反する状態にござる。**
**本書へ書いただけでは落ちたことにならぬ**——**次の家老が最初に落とすこと。** 一覧は §6。

### (b) 作業ツリーの状態

**本日のコミットはすべてpush済み**（最新＝`b048b80`）。`git log origin/main..HEAD`と`git status`で
混入チェックを行うこと。**`main`は綺麗、一時計装・残置マーカーとも無し**（侍が確認）。

**殿の実運用ファイル`main-layout.xml`は無傷**（忍者が確認）。

### (c) 検体の残置1件

**`マイドキュメント\Ecad2\図形\自作\T133PDFOVER.gcadpart`**（488バイト）。
**次セッションで対照撮影に使う**（忍者、10分で撮れる由）。**他3件は殿の裁可を得て削除済み。**

---

## 1. 本日の到達点

| | 中身 | 状態 |
|---|---|---|
| **T-134** | Undo対象の穴6件を塞ぐ | **実装完了・コミット済み**（`fec4117`）。**レビューと実機が未了** |
| **T-133** | その他図形の移植 | **調査・計画とも完了、殿裁定7件が出揃う**。**着手はT-134の後** |
| **T-135** | 別セルクリックでの巻き戻し（P-144） | **新規起票**（殿裁定＝続けて直す）。**計画未着手** |

---

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-134実装＝`fec4117`**（9ファイル・740挿入/6削除）。**通常形で`dotnet build src/Ecad2.sln`成功
  （0エラー）、Core 280・App 930 全合格**。**RED先行証明＝改変10通りを実測、帰属はテスト名で分離**
- **殿裁定(U-1)＝配置時のみ積む／合流先確定では積まぬ／Esc経由の取消時だけ破棄**
  （`UndoManager`へ破棄API新設）。**侍・隠密が独立に「配置時のみ」で一致（Wチェック）**
- **PR-27対策の検出力を数値で実証**＝`Pos`の行と列を入替えて**14件RED**（対称座標なら0件）。
  **侍が`samurai.md`項目5へ2例目として記帳済み（`b048b80`）、射程3点を自ら区切っておる**
- **隠密の静的レビュー（部分）**＝`UndoManager.cs`・`MainWindowViewModel.cs`のdiff全体を検分。
  **観点3（殿裁定どおりか）・観点4（既存23箇所へ影響なし＝diffが追加のみで`-`行が無い）とも確定**。
  **テスト9ファイルは未見**
- **【隠密の未確認1件は家老が解消済み】`ApplyDocumentInfo`の同値ガードは8フィールドだが、
  `DocumentInfo`は9フィールド（`Revisions`が9番目）**——**されど穴は無い**。
  `ApplyDocumentInfo`は`Revisions`を代入しておらず（`:286-293`）、**侍が`:275`に
  「Revisionsは編集対象外ゆえ比較にも含めない」と明記済み**。隠密は定義に届かなんだだけ
- **T-133の接続点検証＝両輪とも完了**。**真因＝座標系が2つ食い違う**（基準枠の描画＝上端基準／
  接続点の`RowOffset`＝中心行0の上下）。忍者の実測式＝**`clamp(Round(cellY), -(h-1), +(h-1))`**、
  **「0にならぬ帯」が枠の高さの約6割**。**高さ1は`rowLimit=0`で潰れ「正しく見える」だけ**
- **殿ご下問（PDF出力で切られるか）＝切られぬ。ただし紙には出ぬ**。
  **`PushClip`は`src`全体で2箇所のみ**（複数ページ分割時に自由直線と枠を囲むだけ）、
  **要素は`PopClip`の後に描かれる**。忍者のPDF実測＝**MediaBox（A4縦）を越える座標が
  切り詰められずそのまま書かれておる**。**紙に出ぬのはPDF仕様上の帰結**（忍者は画像として未実測と区切る）
- **殿ご意向＝「0.5セルほどのはみ出しは問題視せぬ。描画部が印刷されるなら不問」**——**本件は不問で決着**

### 判断待ち・未着手

- **【最優先】T-134の残り2工程**＝**隠密の静的レビュー（テスト9ファイル未見）→忍者の実機確認**。
  - **隠密へ渡す最重点観点＝既存テスト7件（`T085/T086/T088×2/T096/T097/T107`）の修正が
    検出力を損なっておらぬか**（侍の自己申告のみ。配置がUndo対象になり落ちたため
    `UndoManager.Clear()`を挟んで起点を揃えた）
  - **忍者の見どころ**＝配置してCtrl+Zで消えるか／Esc取消後にCtrl+Zが空振りせぬか／
    別セルクリックでは逆に破棄されぬか／文書情報ダイアログで何も変えずOKしてもRedo履歴が消えぬか
- **隠密の軽微所見1件**＝`DiscardLastSnapshot`はRedoスタックに触れぬゆえ、
  **Redo可能な状態でOR配置→EscするとRedo履歴が失われたまま**。**P-135/P-140と同じく記録に留める筋**
- **T-135（P-144）＝計画未着手**。侍の中断点＝`ClearOrJoinTargetDraftIfAny`の**実呼び出しは3箇所＋定義1**
  （`:112/478/3139/3261`のうち`:3150`が定義）。**docコメントは「5箇所」と述べるが実は4箇所——数え直すこと**
- **T-133＝計画済み・未着手**（`docs/ecad2-t133-implementation-plan-samurai.md`、**10増分**）。
  **増分0・1は隠密の高さ影響範囲調査を待たずに着手できる**
- **T-133の保留1件**＝**縦向きモータの意匠**（絵を殿にご覧いただいてから確定。増分8の着手時）
- **忍者の対照撮影が未達**——**用紙内に収まる検体（列0）が撮れておらぬ**。`T133PDFOVER`で10分
- **T-125 β-2/β-3**／**T-131・T-132**／**T-123・T-122**——いずれも未着手

---

## 3. T-133の殿裁定（全7件。台帳`docs/todo.md` T-133節が正本）

| # | 中身 |
|---|---|
| 1 | 配置導線＝**案B**（`ElementKind`経路の新設） |
| 2 | **配置バーは通さぬ**＝メニュー→キャンバス単クリック |
| 3 | **縦方向の占有判定もT-133の中で正す** |
| 4 | 3極記号は**主回路シート限定**＝メニュー無効化（予防）＋`ValidatePlacement`での拒否（防御・サイレント） |
| 5 | **モータの直交＝縦向きモータ図形を新規追加**（**端子が横に3つ**。既存ポート定義と合致、ポートには触れぬ） |
| 6 | **基準枠の基準点＝中央**（`PartEditorCanvas.cs:674`） |
| 7〜9 | 組み込みパーツ2件＝**移植する**／既存4件＝**メニューへ再掲**／ピン留め＝**範囲に含める** |
| 11〜13 | 占有行＝**H-2（中心基準3行）**／組み込みパーツは**部品リストに出さぬ**（17件のまま）／`DeviceClass`は**現状維持** |

---

## 4. 今回の教訓（§3相当）

1. **【最大】家老が発生源の誤りが5件出た**——(a)台帳「1/16刻みゆえ`0.9375`で置ける」
   (b)制度化文の実例を2つ合成 (c)パターン台帳の機序断定「原本を2度写す」
   (d)母数の異なる数を「のうち」で繋ぐ（27=16+7が成り立たぬ） (e)「非対称を測ったのは本日が初」（実は2例目）。
   **いずれも「一次を確かめずに、もっともらしい因果を足した」同じ形。**
   **すべて隠密・侍が捕らえた**——**発生源を知る当人にしか見えぬ。**
2. **その対策として`karo.md`へ新項を落とした**＝**「`Read`させるだけでなく『踏み込みすぎておらぬか
   検められたい』と明示的に検分を請う【MUST】」**。**この一手で当日4件が返ってきた。**
   **読むことと、誤りを探す構えで読むことは別。**
3. **「49箇所」が引き継ぎ書→采配文→報告と渡り歩き、誰も数え直さなんだ**——**実は23箇所**
   （残り26はコメント中の言及）。**本書に数を書くときは再現手段を必ず添えること。**
4. **Wチェックが2度効いた**——(a)侍と隠密が独立に「配置時のみ積む」で一致
   (b)隠密が`ApplyDocumentInfo`の全フィールド網羅を疑い、家老が一次ソースで「穴なし」と確定。
   **どちらも材料を渡さなんだゆえに値打ちが出た。**
5. **忍者が2度、自らの誤りを訂正して結論を強くした**——(a)「クラス既定40」は生成箇所が`20`を明示
   (b)その訂正により**「縮小が働かぬまま線が用紙外へ出た」＝はみ出しは縮小判定の勘定に入らぬ**が実証された

---

## 5. スコープ境界・制度（今回の追加、すべて反映済み）

- **`karo.md`**——(1)**【新設】差し込みを受けたら`output-corruption-log.md`の分母表へ1行足す【MUST】**
  (2)**【新設】`Read`させるだけでなく検分を請う【MUST】**
- **`onmitsu.md`**——(1)**数を書いたら再現手段を添えよ（己への検算になる）** (2)**「対象外」の理由を
  一次ソースの証跡で選り分ける**（裁定によるもの／時系列の産物） (3)**既定値を持つ仕組みは
  「明示されたもの」だけを数えない** (4)**`grep`の生の一致数≠実呼び出し数**
- **`samurai.md`**——項目5へ**非対称入力の数値実例2例目**（侍が自ら記帳、`b048b80`）
- **`pattern-recurrence-log.md`**——**PR-29候補を新設**（同型の食い違いが最初から複数箇所に並行して作られる）、
  **PR-26候補へ訂正**（P-125は「漏れ」でなく殿裁定の射程内。実例を7件側へ差し替えるのが筋）
- **`output-corruption-log.md`**——**「差し込み依頼の記録（分母）」を新設**（殿ご指示）。
  **【前提】差し込みと外来文字混入の関係は未検証**。**本日の差し込み記録は未記入——次の家老が埋めること**
- **`memory: feedback_code_comments_are_secondary_source`**——「49箇所」を23へ訂正し、
  **「grepの生の一致数≠実呼び出し数」**を追記

---

## 6. 【未落とし】本日の気づき13件（次の家老が最初に落とす）

### 侍（4件）
1. **【最重要】「救済になるか」は、積む中身が何かまで見ねば判らぬ**——P-144の見立てが崩れたのは
   積むのが「配置前」ゆえ。**「対象にすれば戻せる」は、戻る先を見て初めて言える**
   → **`memory`新設**（`feedback_preliminary_survey_not_final`の系列）
2. **計装を新たな経路へ足すと、既存テストの前提が構造的に壊れる**——Undo・ログ・通知など
   **「今まで起きなかったことが起きる」変更では必ず起きると先に見込むべき**
   → **`samurai.md`**（「回帰テストのRED先行証明」節の近くへ新項）
3. **独立した改変は、テスト名が分かれていれば同時に壊して測ってよい**（4回のビルドが1回で済む。
   **混ざるもの＝`Pos`入替は単独で測る**という条件つき） → **`samurai.md`**（同上節）
4. **他役の要約に己の仕事が含まれておったら、記帳の前に検分する**——**家老側（検分を請う）の対**
   → **`samurai.md`。新節か既存節への追記かは家老判断を仰ぐ**

### 隠密（5件）
1. **diffが「追加のみ」であること自体が根拠になる**——実装者が「推測」と留保した観点を、
   レビュアーは別の証拠で昇格させられる → **`onmitsu.md`レビュー節**
2. **同値ガードを見たら、比較しているフィールドが型の全フィールドか数える**——
   **「ガードがある」と「ガードが正しい」は別** → **`onmitsu.md`レビュー節（新規観点）**
3. **`src`配下のgrepが`obj/`の生成ファイルを掴むことがある**——**一次ソースに届いておらぬのに
   届いたつもりになる** → **`onmitsu.md`調査ワークフロー**
4. **「検められたい」と請われた時は、賛否だけでなく数の整合まで当たる**——母数の取り違えは
   **文章を読むだけでは出ず、引き算して初めて出た** → **`onmitsu.md`**
5. **【己の誤り】P-144の救済を「構造が地続き」で判じ、時間軸を検めなんだ**——
   **積むのが「配置前」か「巻き戻し前」かで結論が真逆**。**救済の可否を論ずるときは
   スナップショットの時点を必ず問う** → **`memory`**

### 忍者（4件＋副次1）
1. **【最重要・新手法】PDFはコンテンツストリームを直読できる**——`FlateDecode`をPowerShellの
   `DeflateStream`で展開（**zlibヘッダ2バイトを飛ばす**）。理論値と**0.05mm以内**で一致。
   **PrintWindow撮影がPDFプレビューの色を誤る問題を丸ごと迂回できる**
   → **`ecad2-ui-automation`スキルへ新節**
2. **クラス既定値もまた二次情報である**——`GridSpec.Columns = 40`（クラス既定）を見て報じたが
   **生成箇所が`20`を明示指定**。**既定値も生成箇所を当たらねば実仕様ではない**
   → **`memory: feedback_code_comments_are_secondary_source`へ「既定値」の軸を追記（家老判断待ち）**
3. **合成ドラッグの距離則は作図にも効く**——線ツールは**136px/40刻みでは引けず340px/68刻みで成立**
   → **スキル0節または3節**
4. **`SelectionItemPattern.Select()`では部品配置が始まらぬ**——入口は**ListBoxItemの物理クリック**。
   **6.2節「UIAはClickハンドラを迂回する」の新実例** → **スキル6.2節**
5. **キャンバスのクリック有効範囲は`BoundingRectangle`より狭い**——Boundsは888px幅だが
   **実際に効いたのはrel x≒900（列17）まで** → **スキル6.4節**
   （副次）**自作パーツの保存には接続点2つ以上が要る**（`ErrorText`でバリデーション）→ **スキル**

---

## 7. 次の1手

1. **【最優先】§6の気づき13件を落とす**（本書へ書いただけでは落ちておらぬ）
2. **T-134の残り2工程**＝隠密の静的レビュー（**既存テスト7件の検出力を最重点観点に**）→忍者の実機確認
3. **T-135（P-144）の計画起草を侍へ**——**ただしT-134が忍者の実機確認まで通ってから実装**
   （同じ経路に触れるため）。**対処案(1)(2)のどちらかは殿が仰せでない——侍の見立てを添えて諮る**
4. **T-133へ着手**（T-134・T-135の後）。**増分0・1は調査待ちなしで入れる**
5. **忍者の対照撮影**（`T133PDFOVER`、10分）
6. **`output-corruption-log.md`の分母表へ本日の差し込み件数を記入**（家老の【MUST】、本日分が未記入）

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。**本日、殿ご指示により起動手順を改めた**——**`handover`の読み込みは役決めの後**
（第2バッチで役割mdと並列）。**役決めの経路に長文の読み込みを混ぜぬこと。**
