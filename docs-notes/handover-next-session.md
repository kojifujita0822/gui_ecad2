# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-05（**殿就寝中の夜間進行完了**時点。T-021マージ済み・T-019暫定完了。全役は殿裁定待ちで待機中）

## 【最優先・即確認】

1. **ブランチ**: **mainに戻っている**（T-021はfeature/t021-enter-placementからfast-forwardマージ済み、2026-07-05）。起動時 `git branch --show-current` で確認。featureブランチはマージ済みのまま残置（削除は殿確認のうえ）。
2. **殿起床後（または次セッション冒頭）に諮る事項5点**（すべて殿裁定待ち、下記「現況」も参照）:
   1. **T-021使用感の感想**——Esc2段階解除・ズーム時追従・母線可視スクロールの実挙動（マージ自体は殿の委任「マージも任せます」により実施済み）
   2. **T-020濃紺空状態の意匠最終判断**——T-019完了によりCtrl+N（新規）で濃紺画面へ到達可能になった。殿の実機目視で最終判断
   3. **T-019のUI/UX分岐**——(a)未保存確認フロー（Dirty判定）の要否・様式=**最優先**（暫定は確認なし破棄） (b)「新規」の初期状態（Sheets=0濃紺 vs 即1シート生成、GX Works3流儀との整合） (c)エラーMessageBox文面の最終確認
   4. **T-015スコープ再定義**——隠密整理の3案から選択（`docs/ecad2-t015-scope-redefinition-options-onmitsu.md`、本命=案1:右パネル部品選択リストへサムネイル追加）
   5. （小件）git pushのask解除（`~/.claude/settings.json`）を元に戻す時期
3. **作業ツリーはクリーンで引き継ぐ**（全記録コミット・push済み、最終コミット403df7a）。

## 現況（2026-07-05 夜間進行完了時点）

- **T-021**: **Done・mainマージ済み**。夜間に母線可視スクロール追加修正（殿直接指示：左端/右端到達時に母線が見える位置まで1セル分広くスクロール）を実施——340f53d→隠密レビューCONFIRMED1件（列不変の行移動でwiden毎回発火）→7e70c3d（列変化ガード、往復1周）→隠密再レビュー合格→忍者実機5観点OK。詳細は台帳T-021行。
- **T-019**: **暫定仕様で実装・検証完了、In-progress維持**（UI/UX分岐の殿裁定待ちのため）。新規/開く/保存＋メニュー・ツールバー・Ctrl+N,O,S接続まで動作、忍者実機8観点OK・回帰なし。経緯詳細は台帳T-019行（隠密レビュー3件→1周解消、エラー文面のWチェック→2周目解消）。
- **T-015**: 隠密がスコープ再定義の選択肢3案を起草済み（実装未着手）。殿裁定待ち。
- **T-028→T-033**: T-028への殿注文3点（カーソル直下表示・小型横長化＋横並び・はい/いいえボタン見切れ修正）は、殿裁定によりT-033（非モーダル浮動インライン化）へ統合。T-028は元の「拡張表示ボタン」のみに戻した。T-033の着手時期は引き続き後日殿と相談。
- **殿裁定の進行順（2026-07-05）**: T-021マージ → T-019 → T-015（マージとT-019は完了・暫定完了）。
- **経過観察一覧 `docs/observations.md` 新設済み**（殿裁定どおり）: #1〜#4=T-021既存4件、#5〜#6=母線可視スクロールの理論上縁ケース2件を追記済み。
- **P-007**: 忍者の気づき（ズーム直後の選択解除）→自作検証ヘルパー固有の副作用と判明しwithdrawn。
- **ズームヘルパーのスキル化完了**（殿承認済み2026-07-04の積み残し）: `Invoke-Ecad2CtrlScroll`を既存`ecad2-ui-automation`スキルへ追加（helpers.ps1+SKILL.md、403df7a）。**次セッションからscratchpad再作成は不要**。Send-KeysSafeの正式スキル化は引き続き未上申。

## 技術教訓（夜間進行分）

1. **Wチェック並行方式の2例目実績**: T-019エラー文面欠陥で侍診断と隠密独立調査が完全一致（双方catch節のex.Message直埋め込み・変換層未実装）、誤診なく1周で確定。原因自明に見える欠陥でも突合の儀式コストは低く、確定の速さに寄与。
2. **隠密レビューの新観点**: 「例外がMessageBoxに変換されるか」だけでなく「**変換後の文面が技術的でないか（中身）**」まで見る（今回の見逃しからの自己分析、隠密自身が明文化済み）。
3. **「残留状態のクリアは分岐の外で一括」の再適用**: T-019のReplaceDocumentクリア漏れ3件（DRC出力・StatusMessage・Tool）はT-021の「Esc常時クリア」教訓と同型。文書差し替え等の大域遷移では「旧状態で残ってはならないものの一覧」を先に洗うのが有効。
4. **忍者の新教訓**: `Send-Ecad2Keys`はメインウィンドウをフォアグラウンド化するため**モーダル子ウィンドウ表示中の使用は避ける**（Escが届かずダイアログ残存→誤検出しかけた実例、検証記録の自己訂正参照）。

## 各役への申し送り

### 侍へ（前任侍より）
- ~~T-019の暫定実装は「確認なし破棄」~~→**殿裁定反映済み（2026-07-05）**：未保存確認3択（ConfirmDiscardIfDirty、New/Open/Closingの3入口）・新規=1シート生成・起動濃紺#24325A・保存はHasProjectガード+IsEnabledの二重防御まで実装完了（fdc994d）。Dirty判定はMarkDirty明示呼び出し方式（変更操作6箇所+シート操作、呼び忘れリスクはP-008参照）。
- T-015のサムネイル実装：DrawPreviewの_lib一時差し替えは**UIスレッド単一・使い捨てインスタンス前提**（コメント明記済み）。Entries/SelectionEntriesは並行管理（同期漏れ注意、`docs/ecad2-t015-review-onmitsu.md` #5）。PartLibrary構築はPartPaletteViewModel.Libraryに一本化済み（2d5f3dc）。
- SelectedCellの直接代入+手動通知列挙（ReplaceDocument内）は将来リスクの設計メモ（`docs/ecad2-t019-review-onmitsu.md` #4）。setter側に通知を足すときは要追随。
- フォーカス制御の流儀（T-021）は台帳T-021行に保全済み。
- **前任侍の退出時の勘所2点（2026-07-05、原文保全）**：(1)**ReplaceDocumentは「文書に紐づく状態」リセットの単一責務点**（StatusMessage/Tool/DRC結果/IsDirty集約済み）。今後この種の状態を追加する際は必ずここへリセットを追加すること——隠密指摘で2度同じ穴（通知漏れ）を踏んだ経緯あり。(2)**CurrentSheetIndexの既定値0が「シート無し」とも「先頭シート選択中」とも解釈できる意味論的重複が通知漏れの温床**（NotifyHasProjectChanged/NotifyCurrentSheetChangedの個別対応で凌いだが、番兵値化(-1)は見送ったまま）。今後シート操作系を触る際はこの罠に注意。

### 忍者へ（前任忍者より）
- ズームヘルパーはスキル化済み（`ecad2-ui-automation`のInvoke-Ecad2CtrlScroll）。再作成不要。
- モーダル対応Send-KeysSafeヘルパーは引き続きセッション毎に消える前提（スキル化は未上申）。モーダルダイアログ表示中はSend-Ecad2Keysを使わず物理クリックで閉じること（上記教訓4）。
- UIA Invoke・Select偽結果は物理操作で再検証——従来どおり。

### 隠密へ（前任隠密より）
- code-reviewスキルはmedium以上で設計上の穴を検出、low は純削除・小差分確認向け——従来どおり。
- エラー通知系のレビューでは「変換の有無」だけでなく「変換後の文面の中身」まで確認する（上記教訓2）。

## 運用メモ（継続事項）

1. **usage申告ルール**（殿採用済み）: 各役はusage 75〜80%超で家老へ一行申告。家老は起動時の役割指示にこのルールを含める。
2. **peerメッセージは要約して送る**（殿指示）: 要点＋「詳細は◯◯参照」のパス添え。
3. **git pushのask解除は継続中**（`~/.claude/settings.json`）。戻す時期は殿に確認（上記確認事項5）。
4. 検証パイプライン既定順（侍実装→隠密静的→忍者実機）・往復2周上限・Wチェック並行方式は変更なし、いずれも今回も有効に機能。
5. 起動は各ターミナルで `claudepeers` と打つだけ（引数なしで自動的に"開始"が渡る）。

## 次回セッションの起動手順

1. 4ターミナルとも `claudepeers` で起動（起動順に家老→侍→忍者→隠密が自動で埋まる。1つずつ間を空けて起動推奨）。
2. 家老は起動後、本ファイルの【最優先・即確認】の5点を殿に諮ることから始める（T-019の未保存確認フローが最優先）。
3. `docs/todo.md`・`karo.md`の権限線引きを確認してから采配すること。
