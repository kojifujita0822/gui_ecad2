# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-10（家老記す、本日の店じまい その2）

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**現在のブランチは `main`**。作業ツリーはクリーン。**push保留なし**——本日の全コミットは
その都度origin/mainへpush済み、最新は`1c7e4d3`＋本メモ更新前後のtodo.md編集1件。
テストはCore45件・App344件、全合格が最新基準（旧基準323件から+66、T-055増分3で増加）。

---

## 1. 目的とDoD

**進行中の本丸はT-055（行数拡張+母線名）の増分3**（任意位置への行挿入・削除）。
計画書=`docs/ecad2-t055-implementation-plan-samurai.md`。実装・隠密レビュー2周・忍者実機確認
（6観点中5観点OK）まで完了したが、**1論点の殿裁定待ちで完全クローズしていない**。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-055増分3の実装・レビュー・実機確認は完了**（詳細は`docs/todo.md`T-055節）：
  - 隠密事前調査（`docs/ecad2-t055-increment3-precheck-onmitsu.md`）＝独立PoCフェーズ不要の所見
  - 実装`33a59f9`（ContextMenu基盤・RowOps・InsertRowBeforeCommand/DeleteRowAtCommand）
  - 隠密レビュー往復1周（要修正2件・cleanup1件）→修正`e9d062a`→再レビュークリーン確定
    （`docs/ecad2-t055-increment3-round1-review-onmitsu.md`）
  - 忍者実機確認6観点中5観点OK（`docs-notes/ecad2-t055-increment3-realmachine-verification-ninja.md`）
- **ecad2-ui-automationスキル改修**：右クリック機能（`Invoke-Ecad2CanvasRightClick`）新設、
  トラブルシュート2点追記（ダイアログ検出はEnumWindows推奨・ContextMenu表示中のMainWindowHandle罠）
- **ninja.md「殿代行操作の運用」節を追記完了**（忍者の宿題、キーボードショートカット検証・
  座標クリックの2観点限定、コミット`40de2de`）
- **殿指示「判断待ちで止まるなら先のタスクに進む、保留事項は【報告】ラベルで明示」**を運用開始
  （本セッション新規、家老memoryにも記録済み）

### pending（殿判断待ち、急ぎでない）

- P-045・P-046・P-047・P-048（既存）
- **P-049（新規）**：`InsertRowBeforeCommand`/`DeleteRowAtCommand`のCanExecuteに行番号範囲チェック
  不足。現状唯一の呼び出し元がガード済みで実害なし、将来の脆さとして記録のみ
- **P-050（新規）**：GroupFrameの`Visual*Mm`座標オーバーライドがRowシフトに未追随。枠ドラッグ・
  GuiEcadインポートとも未実装で到達経路なし、将来のドラッグ機能実装時の潜在バグとして記録のみ

### 【最重要・殿裁定待ち】T-055増分3を完全クローズできない1論点

**GroupFrame内部削除のHeight--ロジックが実機で到達不能（デッドコード疑い）**。計画書に明記された
「削除対象行が枠の内部→Height--（内部詰め）」というCore層ロジックは単体テストでは動作するが、
コマンド層の`IsRowOccupied`判定がGroupFrame範囲全体を「占有」とみなすため、実際のUI操作からは
削除自体が拒否されHeight--分岐に到達しない（忍者実機確認で発覚）。

これは既存の未確定論点「**削除対象行そのものに要素がある場合、拒否か要素ごと削除か**」
（計画書109行目の開かれた論点。GuiEcadは要素ごと削除方式）と**表裏一体**——この論点が確定して
いないため、GroupFrameだけ実質「拒否」寄りの挙動になり計画書のHeight--ロジックと矛盾している。

殿へ選択肢2つを提示済み（本セッション内、回答はまだ）：
1. 現状維持（全面拒否）—Height--分岐はデッドコードとして明記 or 削除
2. GuiEcad同型（要素ごと削除）採用—IsRowOccupiedのガードをGroupFrame内部行に限り緩め、他4種の
   拒否/削除方針も併せて確定

## 3. 試して失敗したアプローチと結果

- 過去分は`docs/todo-archive.md`参照。本日新規の失敗事例なし（増分3は往復1周のみでクリーン確定、
  4役体制の采配・検証パイプラインとも制度どおり機能）

## 4. スコープ境界・制度（既存、継続適用）

- RED先行証明【MUST】・テスト設計と実装の分離【MUST】（バグ修正・往復案件）・
  テストコード静的レビュー+Stryker棚卸し（往復2周以上のクローズ時）
- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／
  コミットはパス限定形／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】
- 便乗拡大禁止（P-049〜050も着手せず記録のみが正）
- **新規運用**：殿判断待ちで手が止まる場合は保留にして先のタスクへ進める。保留事項は
  【報告】ラベルを付けて明示し、殿が戻った際に一目で拾えるようにする

## 5. 次の1手（再起動後の家老がやること）

1. **役割自動決定**後、**采配の全量再送**（アイドルpeerは采配を拾わない鉄則）
2. 上記「殿裁定待ちの1論点」の回答が既にあれば、それに従い増分3の残タスク
   （削除対象行に要素がある場合の分岐実装）を侍へ采配。GroupFrameの扱いも同時に確定させること
3. 回答がまだなら、他の着手可能なタスク（T-052〜054、承認済み未着手）へ進める判断を検討
4. T-056（グリッド表示切替）はT-052〜054の後、まだ着手時期でない
5. モデル切替（家老・侍ともSonnet 5）の見守りを継続。誤采配・殿訂正・破損頻度を素直に記録すること

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。**ブランチはmainのまま。T-055増分3は1論点の殿裁定待ちで足踏み中。**
