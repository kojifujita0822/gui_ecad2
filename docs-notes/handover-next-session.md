# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-12（家老記す、T-082往復2周目完了・隠密フル観点再レビュー進行中の区切りで
セッション終了=殿指示、usage 71%）

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**現在のブランチは `main`**。家老が2ad2825（侍の往復2周目修正）まで一括push済み
（push前に`git log origin/main..HEAD`の混入チェックを経ること）。

---

## 1. 目的とDoD

目下の本丸は**T-082（シート並び替え）のクローズ**。DoD=隠密フル観点再レビュー通過→
忍者実機確認（観点は台帳T-082節=端移動・シート1枚時・ドラッグ視覚フィードバック・
保存読込往復。加えて侍の未検証申告2点=Alt+上下の実機反応・ドラッグ視覚の見た目、
修正1〜3の実機挙動）→台帳Done化。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-082実装+往復2周**：実装9c147ce（DoD(1)〜(6)）→往復1周目1b42fd3（隠密指摘の
  修正1=所見L型再発・修正3=SelectedSheet通知欠落+テスト補強3点、RED証明=git stash実測）→
  往復2周目2ad2825（修正2=殿裁定案A: 並び替えでクリアすべき診断が在る時のみDRC結果破棄+
  「DRC結果が削除されました。DRC再実行してください。」表示、RED証明=修正ブロック一時
  コメントアウト方式で代替）。build/test全合格569件（侍report、三区分報告で受領）
- **殿裁定4件（いずれも台帳T-082節へ記録・push済み）**：ドラッグ視覚表現=案A標準
  フィードバック／GX Works3にシート概念なし（踏襲参照先は不存在）／修正2=案A破棄+表示／
  レビュー深度の既定=1周目軽く・2周目以降フル（karo.md「レビュー深度の既定」節へ制度化、d67629c）
- **T-083起票済み**（ダークモード搭載、AvalonDock連動・T-058と同時実装、殿直接指示）：
  3層構成の事前検分つき（作図色=Core下地`DrawingTheme.Dark`完備でApp結線のみ／UIクローム=
  .NET 10 Fluent ThemeMode候補・成熟度は着手時に隠密裏取り必須／AvalonDock VS2013テーマ連動）

### 実施したが未検証・未完了

- **隠密のT-082フル観点再レビュー**：采配済み（2026-07-12、対象=1b42fd3+2ad2825）だが
  **結果未着のままセッション終了**。次セッションはまず`docs/`にレビュー書（ecad2-t082-*review*
  の2枚目）が出ているかGlobで確認し、無ければ**隠密へ再采配**（アイドルpeerは采配を拾わぬ鉄則）
- **忍者実機確認**：未着手（再レビュー通過後に采配）

### 未着手

- ロードマップ順: T-064/T-069→T-061→T-067/T-070→T-058/T-083→T-068（順3〜5は事前調査+
  殿裁定済みで即実装可。T-058はPoCから、T-068のみ事前調査未実施）
- T-077（ヘルプ「使い方」画面）: 中断中、`docs-notes/handover-onmitsu2-t077-partial.md`から再開可
- **P-066**（シート削除時のPageNumber非再採番、採番規約の非対称）: 起票済みpending、殿判断待ち
- P-061〜P-065: 引き続きpending

## 3. 試して失敗したアプローチと結果（今回の教訓）

- **1周目レビューの観点上乗せは深度過剰を招く**：T-082の1周目采配で家老が制度（テストコード
  深掘りは往復2周以上のみ）を超えてテスト網羅性点検を含め、殿から「ここまで必要？」の指摘。
  裁定=1周目は軽く（DoD整合+code-reviewスキル+既知トラップ狙い撃ちのみ）・2周目以降フル、
  karo.mdへ制度化済み。以後の采配はこの既定に従う
- **「既存規約に合わせよ、無ければ差し戻せ」の采配文言が有効**：修正2で侍が規約不在+
  ユーザー可視の挙動差を確認して正しく差し戻し、殿裁定で決着。挙動差を伴う修正の推測実装を防げた
- **T-058のバージョン前提は状況が変わった**：当時の「4.74.1で開始」裁定はecad2がnet8だった頃の
  判断。T-062完了（.NET 10）により5.0.0系（net10明示対応）直接採用の選択肢が浮上——PoC着手時に
  隠密へ最新確認を振ってから殿に諮る

## 4. スコープ境界・制度（既存、継続適用）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／コミットは
  パス限定形【MUST】（未追跡ファイルは先に`git add <パス>`が要る）／pushは家老のみ・混入チェック
  必須／UI/UX分岐は必ず殿へ【MUST】／環境依存文字禁止【MUST】
- **レビュー深度の既定（新設2026-07-12）**=1周目軽く・2周目以降フル（karo.md参照)
- Grep contentモード（-A/-B/-C含む）は全役回避継続（殿指示2026-07-12、破損記録簿#9#10）。
  Read直読・Glob・files_with_matches/count主体、行番号取得はBash rg -n

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、**采配の全量再送**（アイドルpeerは拾わぬ鉄則）
2. **T-082再レビューの帰趨確認**：`Glob docs/ecad2-t082-*` でレビュー書2枚目の有無を確認。
   無ければ隠密へフル観点再レビューを再采配（対象=1b42fd3+2ad2825、観点は台帳T-082節と
   9118406コミットの采配記録参照）。指摘ゼロなら忍者実機へ、指摘ありなら往復3周目=
   **2周超過ゆえ殿上申**（karo.md掟）
3. 忍者実機（観点=台帳T-082節+侍の未検証申告2点）→全観点OKで台帳Done化・アーカイブ移送
4. クローズ後はロードマップ順=T-064/T-069采配から。P-066の殿判断も適宜伺う

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
