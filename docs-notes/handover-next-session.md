# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-07（usage逼迫によるセッション終了前、家老記す。T-044の隠密再レビュー中の状態で引き継ぐ）

## 【次回冒頭の掃除・殿依頼済み】

`C:\Users\kojif\.local\bin\claude.exe.old.1783377108279`（旧2.1.195退避、240MB）を削除すること。
前回（2026-07-07朝）の家老セッションが自動更新の30秒前に起動し旧バイナリを掴んでいたため削除不能だった。
当該セッション終了後は `Remove-Item` 一発で消える。7/6のinstall_failedは孤児セッション由来で解消済み・更新系は健全。
亡霊peerが出たら正式クリアは `claude agents` → `claude stop <id>`（Stop-Processは応急、rosterから復活する）。

## 【最優先・即確認】

1. **ブランチ**: main。作業ツリーはクリーン・push済みで引き継ぐ想定（終了直前に要`git status`確認）。
2. **T-044（OR自動配線の冗長縦分岐抑止）が唯一のIn-progress**。状態:
   - 実装0d3425a（家老docsコミットへの混入搭載、内容は侍検証済みで正）→隠密レビューで**連鎖OR重大バグCONFIRMED**（判定がsheet.Connectors未考慮→3階層並列で3段目が母線直結union=回路等価性破壊）→**往復1周目修正d04c9a3**（Connectors条件追加+連鎖テスト3件、計37件合格）。
   - **GuiEcad比較調査済み**（殿発意、`docs/ecad2-t044-guiecad-diff-survey-onmitsu.md`）: 前作にOR自動配線は無い（手動のみ）が、ネットリスト層の「既存コネクタ全走査」原則と侍修正の条件式が完全一致＝方針整合を確認。
   - **次の仕事＝隠密のd04c9a3正式再レビュー（onmitsu-2、采配済み・未完了）**→クリーンなら忍者実機確認（殿スクショ事例=左母線際ORの二重接続解消・連鎖OR3階層・回帰）→殿目視でクローズ。
3. **本日の完了**: T-033完全Done（全5増分=非モーダル骨格/殿注文3点+位置バグ根治/拡張表示ボタン/種別シンボル化/7種統一・殿目視OK）・T-043完全Done（ORa/ORbサムネイル統一、往復2周・殿目視OK）。
4. **T-041**: Approved、着手はT-044完了後。
5. **殿の予告済み修正**: 「行-1・列-2までの選択範囲は意図的仕様、その範囲が描画範囲に含まれてしまっているのはバグで修正予定」（殿教示2026-07-07）。正式起票は殿の指示待ち。P-022/P-024はこの教示で論点再解釈済み（proposed.md参照）。
6. **pending**: P-012〜P-017・P-020〜P-028（P-018/P-026消化済み。新規=P-027リサイズ時描画オフセット・P-028母線到達判定3箇所散在のCore集約案）。P-025リファクタの適期=T-041前は変わらず。

## 本日の裁定・仕様確定（要記憶）

- **T-044仕様（殿確定）**: 左縦分岐省略=配置行と基準行の**両方**でOR左接続点と母線の間に要素が無い場合のみ（トポロジー等価保証）。列0は自明包含。右合流側は常時維持。既存データ補正なし。
- **T-037の暗黙保持ルール廃止（殿裁定）**: ドロップダウン7種化に伴い「接点系切替でisOr保持」は廃止、表示どおりの動作へ（ORはORa/ORb項目の明示選択でのみ）。
- **配置バー可視ラベル削除は容認**（殿裁定）・**拡張表示ボタンは当面Tab除外**（T-028で復帰、XAMLコメントあり）。

## 運用ルール（追加・教訓）

1. **共有git indexのコミット巻き込み**: 他役のstaged中ファイルを自分のコミットが巻き込む（実例=0d3425a）。コミット直前に`git diff --cached --name-only`＋**`git commit -- <パス>`のパス限定形**が鉄則。
2. **実機確認は必ずセカンダリモニタ**（殿指示、MUST化済み）: `.claude/skills/ecad2-ui-automation`のStart-Ecad2Appは既定でセカンダリへ自動移動（-Screen Auto）。
3. **殿の在席連携**: 忍者検証後はアプリ起動維持のまま殿へ引き渡し（殿が直接確認する運用）。UIA限界で殿の実操作が要る時、殿不在なら家老へも連絡。
4. 従来ルール継続: 検証build前`git status --short`／pushは家老経由（push前に`git log origin/main..HEAD`で混入確認）／peerメッセージ要約／検証既定順（侍→隠密静的→忍者実機）／修正往復2周上限／Wチェック並行／UI/UX分岐は必ず殿へ／診断ログ路線（T-038、往復に数えず）は原因不明の位置・座標系バグに有効（T-033で実証=Margin残留の自己参照ループを1周で根治）。

## 各役への申し送り

### 隠密へ
- 前任がd04c9a3の正式再レビュー（onmitsu-2）を受領済みのまま未完了。観点=(1)Connectors条件追加の正しさ (2)テスト実効性（3階層連鎖・ネットリスト検証） (3)省略条件他性質の非退行 (4)便乗変更なし (5)WireBreak考慮の一瞥。結果はdocs/ecad2-t044-review-onmitsu-2.mdへ。code-reviewスキル併用必須。

### 忍者へ
- 出番は隠密クリーン後の**T-044実機確認**: 殿スクショ事例の再現（左母線際OR=二重接続が消え母線接続のみ）・連鎖OR3階層（3段目が2段目と並列）・上の行に接点ありケース（縦分岐維持）・回帰スモーク。検証後アプリ起動維持で殿へ。

### 侍へ
- T-044は隠密・忍者の結果待ち。次はT-041（F9/sF9手動配線、キー操作方式は実装時に案起草→殿UI/UXゲート）。

## 次回セッションの起動手順

1. 4ターミナルとも「開始」で起動（1つずつ間を空けて）。冒頭掃除（claude.exe.old削除）を家老が実施。
2. 家老はT-044の隠密再レビュー状況を確認→未完なら再采配（メッセージ队列は再起動で消えるため全量再送）。
3. P-022/P-024の描画範囲バグ修正の正式起票は殿の指示を待って行う。
