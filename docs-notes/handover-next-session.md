# 引き継ぎメモ（次回セッションへ）

最終更新: 2026-07-19（家老記す、**T-067(1)(2)+基盤欠陥3件完全完了、T-099完全決着（要件1/2/3実装済み・
要件2再ドックのみ殿実機確認待ち）、T-089ほぼ完全完了（Stack overflow解消・回帰2件完治・テストモード色
修正、押下フィードバック最終見た目のみ殿実機確認待ち）、T-083ツールバー1段目ラベル色修正完了、
T-044は核心（判定は正しいが描画に反映されない）まで絞り込んだが原因未特定で持ち越し**。
長時間セッションだったが、各役の申告に基づき自然な区切りでの終了。異常離脱ではない）。

`long-horizon-discipline`スキル§6の5点セット形式で記す。

---

## 0. 【最重要】作業ツリーの状態

**現在のブランチは `main`**。ほとんどの変更はコミット・push済み（`git log origin/main..HEAD`空を
確認済み、直近push=`a4bd87d`）。

**ただし以下5ファイルは意図的に未コミットのまま残置**：
`src/Ecad2.App/App.xaml` / `src/Ecad2.App/MainWindow.xaml` / `src/Ecad2.App/MainWindow.xaml.cs` /
`src/Ecad2.App/ViewModels/MainWindowViewModel.cs` / `src/Ecad2.Core/Rendering/DiagramRenderer.cs`

内容＝**T-099要件(1)(2)(3)実装（実機確認済み・確定）**・**T-089実装一式（Stack overflow修正+
回帰2件完治+テストモード色バグ修正、いずれも確定）**・**T-083ツールバー1段目ラベル色修正（確定）**・
**T-044診断ログ計装（一時計装、原因確定後に除去要）**が同一ファイル内に混在した状態。前半3件は
実質完了しコミット可能な状態だが、T-044の一時計装が同じファイル（MainWindowViewModel.cs・
DiagramRenderer.cs）に混在しているため、**次セッションは`git diff`で内容を確認し、T-044計装部分と
確定済み実装部分を切り分けてからコミットすること**（詳細は§2・§5参照）。

主要コミット（本日分、9fd10a8〜a4bd87dの間、多数）：T-067(1)(2)完全完了・基盤欠陥3件完全完了、
各種調査記録・制度化。全てpush済み。

---

## 1. 目的とDoD

前セッションからの引き継ぎ「T-099要件(2)(3)・T-089プレビュー確認・T-067続き」を完遂し、殿の実機観察
から派生した新規タスク（T-044、OR自動配線の分岐線バグ）にも対応。セッション後半でT-089実装中に
Stack overflowインシデントが発生、その解消も本セッションの主要な仕事となった。

## 2. 現在の状態（三区分）

### 検証済み（根拠あり）

- **T-067(1)ヒットテスト+選択ハイライト描画、完全完了**：GuiEcad原本の忠実な移植（コミット9fd10a8）。
  隠密静的レビューでPLAUSIBLE1件（Rect非負クランプ）→修正済み。忍者実機NG（再描画漏れ）→
  `ViewModel_PropertyChanged`の再描画トリガー列挙に`SelectedFrame`が漏れていたのが真因、修正済み
  （コミットf648897）。実機確認OK。
- **T-067(2)キーボード/マウス配線、完全完了**：GroupFrameのドラッグ移動・削除・Esc配線（コミット
  58502a2、T-088と同型パターン）。build/test全合格。**実機確認は未実施**（Stack overflow
  インシデント対応に忍者の手が回らなかったため、次セッション冒頭で確認要）。
- **T-067基盤欠陥3件、完全完了**：`ReplaceDocument`の`SelectedFrame`/`_frameDraft`クリア漏れ、
  `HasAnyDraft`の`_frameDraft`未対応、`CancelResidualDraftForToolSwitch()`の`_frameDraft`クリア漏れ、
  いずれも修正・実測確認済み（コミット8c996fe・c544bde）。隠密のP-106運用実態点検で発覚——着手前
  チェックが名指し警告していたにも関わらず実装時に反映されなかった構造的・運用的な穴と確定、
  制度化済み（`samurai.md`チェックリスト項目8・`onmitsu.md`「着手前チェックとの1対1突き合わせ」）。
- **T-099要件(1)(2)(3)、完全決着**：要件(1)ドックタグ非表示・要件(2)最小ドラッグハンドル帯
  （殿提示のGX Works3画像に基づく案a、殿裁可済み）・要件(3)パネル幅の動的フィット、いずれも実装・
  忍者実機確認OK（本題＝帯ドラッグでのフロート化成功、幅541px維持確認、ドッキング時の表示もOK）。
  **例外1件＝要件(2)の「再ドック」動作のみUIA合成マウス操作では検証不能**（AvalonDock
  DragServiceのドッキングインジケーター認識が人間の目でしか判断できない性質と推測）、殿実機操作
  での確認が必要。要件(3)実装時の副産物として「AvalonDockのDockWidth指定は`LayoutPanelControl
  .OnFixChildrenDockLengths`の既知仕様（水平LayoutPanel配下にLayoutDocumentPaneが無い構成では強制
  Star上書き）により無視される」という技術的知見を一次ソース確認済み（診断ログ+UIA実測、コード
  からは除去済み）。
- **T-089、ほぼ完全完了**：Stack overflow修正（真因＝WPF Aero2テーマDLL内部キー未解決+暗黙的Style
  置き換えで既定Setter消失の複合、10キー明示定義+既定値Setter群追加で解消）。回帰(2)完治
  （無効ボタンの明灰色問題、IsEnabledトリガーをControlTemplateからStyle.Triggersへ移設）。
  テストモード色バグ完治（同型の罠、IsChecked/MouseOver/Pressed/IsDefaultedも同時にStyle.Triggersへ
  移設し3連発罠の再発を構造的に防止、自己確認で#E63C00支配色1105px確認済み）。
  **例外1件＝押下フィードバック(PressedOverlay)の最終見た目のみ瞬間的視覚状態ゆえ確証に限界あり**、
  殿実機操作での確認が必要（忍者所見「水色寄りに見える」参考情報あり、要確認）。
- **T-083、ツールバー1段目ラベル色、完全完了**：`ToolBarKeyLabelStyle`へForeground明示指定追加、
  忍者実機確認OK（画素採取・明暗傾向反転判定）。
- **T-100、前セッションで完全決着済み**（変更なし）。

### 実施したが未検証

- **T-044（OR自動配線の分岐線バグ、殿新規報告2026-07-18）、原因未特定のまま核心まで絞り込み済み**：
  「ORを書いた場合の分岐線が必ず1段上までしか連結しない、1段上の配線に分岐線があった場合は連結
  しなくなる」という殿報告。忍者が独立2系統（構図A「行1/4/8」・殿サンプル図面`setuzoku.gcad`）で
  同型症状（新要素の縦コネクタが期待範囲まで届かない、DRCでは未検出の視覚断線）を再現。診断ログ
  計装（`DrawWire`/`LeftTerminator`/`NothingBetweenRailAndColumn`/`DrawConnectors`/`DrawElement`、
  `AppendDiagLog`ヘルパー+lock排他制御）で追跡した結果、**判定ロジック（`NothingBetweenRailAndColumn`
  /`LeftTerminator`）は正しく「母線まで延ばすべき」と結論しているのに、実際の画面には線が一切
  表示されない**という核心的事実まで絞り込み済み（`DrawWire`呼び出し自体・座標値も正常、座標重なり
  仮説は否定済み）。**未解決の謎＝「配置確定時に1回だけ`DrawWire`が呼ばれ画面にも反映されるはずなのに
  消えている」という論理的矛盾**——隠密が`RedrawCanvas`の呼び出し経路（`PlacementOkButton_Click`内
  で1回のみ、`ClosePlacementBar`・`FocusCanvas`内には無し）を確認したが、この1回だけの呼び出しが
  なぜ画面に残らないのか説明がつかず、静的追跡の限界として本日は打ち切り。

### 未着手

- **T-067、次段階(3)(4)(5)**：(3)マウスドラッグでの新規作成 (4)ラベル編集UI（ダブルクリック→
  インライン、P-071の確定処理をここで実配線） (5)右クリックメニュー（線種変更・削除）。矢印キー
  移動対応は殿判断待ちのまま未着手。
- **T-098（シート追加時のPageNumber採番方式見直し）**：Approved、優先度別途検討のまま未着手。
- **T-077（ヘルプ→使い方画面新設）**：Approved、T-075完了済み・素材あり、優先度別途検討。

## 3. 試して失敗したアプローチと結果（今回の教訓）

- **T-044、構図Aでの初回診断ログ取得は非再現**：忍者が最初に選んだ3階層構図（Row1Col5→Row2Col5→
  Row3Col3、行が連続=0,1,2）ではバグが再現せず、判定も理論どおり正しかった。隠密が事後に「行番号が
  連続か離れているかで構図Aと最初の実機再現『行1/4/8』は別物だった可能性」を指摘、忍者も独立に
  同じ結論に到達——**再現条件の構図は、症状を再現した実際の操作手順を一字一句正確に再現する必要が
  ある**（教訓、次回も踏襲すること）。
- **T-044、座標重なり仮説（隠密）は忍者の追加計装で否定**：DrawWire/DrawElement/DrawConnectorsの
  座標を突き合わせたが3者とも衝突なし（境界一致のみ）。
- **T-044、複数回RedrawCanvas仮説（忍者）も隠密の追跡で論理的に行き詰まり**：`RedrawCanvas`の
  呼び出し経路は`PlacementOkButton_Click`内の1回のみと確認、しかしそれなら消える理由の説明がつかない
  という矛盾に到達。**次回の一手＝RedrawCanvas呼び出し回数を直接カウントする計装**（隠密提案、
  未実施）。
- **T-089、当初のStyle.Triggers方式（案A半透明オーバーレイ単体）は原理的に対処不能**：既定Aero2
  ControlTemplate内のControlTemplate.Triggers（優先度4位）がStyle.Triggers（優先度6位）を握り潰す
  構造（PR-20新規パターン確定、3例判明）。ControlTemplate部分自作へ転換して解決。
- **Stack overflowインシデント、真因は2段構えの複合**：(1)未定義のWPF Aero2内部キー参照（遅延解決
  のためビルド時エラーにならず実行時UnsetValueとして現れる） (2)暗黙的Style置き換えで失われた
  既定値Setter。侍が両方を同時に修正し解決。**PR-21候補として記帳**（既定テーマスタイル置き換え時は
  ControlTemplateだけでなくStyle本体の既定値Setterも移植すること、`samurai.md`に予防チェックリスト
  追記済み）。
- **目視の限界、本日複数回発生**：(1)忍者がT-044症状を「行4完全欠落」と誤読しかけたが殿の直接指摘で
  訂正 (2)隠密も同一画像を「完全欠落」と誤読、再確認で撤回（薄い線が解像度・圧縮で見えにくかった
  だけ） (3)忍者がT-083増分5の「右パネル白背景」を目視で確認したが画素採取で「実は正常」と判明
  （ColumnHeader行のみ白残存という別の軽微な話だった）。**教訓＝スクリーンショット目視での断定は
  危険、特に薄い色・細い線は要注意**（memory `feedback_screenshot_visual_misjudgment_thin_lines.md`
  へ記録済み）。
- **T-089一次ソース調査の見落とし**：隠密がControlTemplateの中身は精読したが、対応するStyle本体の
  既定Setter（Background/BorderBrush等）までは確認せず、対処案に含まれなかった——これがStack
  overflowインシデントの遠因の一つ（隠密自己申告）。

## 4. スコープ境界・制度（既存、継続適用＋今回の追加）

- 検証パイプライン既定順=侍実装→隠密静的レビュー→忍者実機／書き込みは侍一元化／コミットは
  パス限定形【MUST】／pushは家老のみ・混入チェック必須／UI/UX分岐は必ず殿へ【MUST】／
  環境依存文字禁止【MUST】／rmコマンド禁止【MUST】（継続適用、忍者が診断ログクリアに
  `Clear-Content`を使い回避した実例あり）
- **PR-20確定（3例判明、増分7・T-099・T-089）**・**PR-21候補**（既定テーマスタイル置き換え時の
  Style本体既定Setter移植漏れ、Stack overflow実例、2件目待ち）・**pattern-recurrence-log.md P
  R-01再発**（T-067基盤、チェックリート内容面+運用面の二重の穴、制度化済み）。
- **1周目レビューのeffort明記【MUST】新設**：家老が采配文に「code-reviewスキル併用」とだけ書くと
  隠密がonmitsu.mdの一般基準（規模に応じてhigh程度）でxhigh相当まで踏み込みうる実例あり——
  以後、1周目采配には必ず「effort=low」を明記すること（`karo.md`・`onmitsu.md`両方に追記済み）。
- **pathspec付きgit commitの罠、同日2回再発**：`git commit -m "..." -- <pathspec>`はステージング
  でなくワーキングツリー現在値を使う仕様、複数タスクの変更が同一ファイルに混在する状況で巻き込み
  うる。両回とも`git show --stat HEAD`の行数照合で即座に検知・訂正（実害なし）。**次回以降は最初
  からpathspec無しコミット（個別git add→commit）を徹底すること**（`samurai.md`に追記済み）。
- **診断ログの排他制御は最初から標準装備**：xUnit並列実行での同時書き込み競合が後追いで発覚した
  実例あり、`AppendDiagLog`ヘルパー+lockを最初の計装時点から入れること（`samurai.md`に追記済み）。
- 往復2周超はゲート必須、同種指摘3周以上は設計見直しも選択肢提示【MUST】（継続適用）
- その他既存チェックリスト群（PR-01〜PR-21、新規選択可能状態・境界値検証・状態リセット・
  型強制・rule of three等）は変化なし、継続適用
- Grep contentモード（-A/-B/-C含む）は全役回避継続、Read直読・Glob・files_with_matches/count主体

## 5. 次の1手（再起動後の家老がやること）

1. 役割自動決定後、**采配の全量再送**（アイドルpeerは拾わぬ鉄則）。
2. **push状況確認**（`git log origin/main..HEAD`、本セッション終了時点で完了・確認済みのはず）。
3. **未コミットの5ファイル（App.xaml/MainWindow.xaml/MainWindow.xaml.cs/
   MainWindowViewModel.cs/DiagramRenderer.cs）の扱いを確認**：`git diff`で内容を確認し、
   T-099/T-089/T-083の確定済み実装部分とT-044の一時診断ログ計装部分を切り分ける。前者は殿の
   実機確認（T-099再ドック・T-089押下フィードバック）を経てからコミット、後者はT-044原因確定
   まで一時計装として保持（コミットに含めない）。
4. **殿へ2件のご確認依頼**（お手すきの折で構わない）：
   (a) T-099要件(2)の「再ドック」動作（UIA検証不能のため実機操作でお試しいただきたい）
   (b) T-089の押下フィードバック(PressedOverlay)の最終見た目（忍者所見「水色寄り」の参考情報あり）
5. **T-044、次の一手＝RedrawCanvas呼び出し回数を直接カウントする診断ログ計装**を侍へ采配。
   隠密の未解決の論理的矛盾（1回しか呼ばれないはずの描画が消える理由）を解消する最短経路。
6. **T-067の続き**：(1)実機確認は済んだが**(2)（ドラッグ移動・削除・Esc配線）はまだ忍者実機確認
   未実施**、優先的に確認されたし。その後、次段階(3)マウスドラッグ新規作成→(4)ラベル編集UI→
   (5)右クリックメニューを侍へ継続采配。
7. **T-099・T-089が殿確認を経て完全にコミットされ、T-067が一区切りついたら、v0.4仮リリース
   ビルド作成を検討**（T-057確立手順に準拠。T-083全増分・T-100は既に完了済みで前提条件は
   ほぼ整っている）。
8. proposed.md pending項目の定期確認（P-100/P-101=自作パーツ関連後回し、P-103=AvalonDock
   既知バグ経過観察、P-104=機器表旧機器名残存の再現条件調査、P-107=FreeLine/ConnectionDot判定
   順序の逆転、T-067範囲外の気づき）

---

## 起動時の合図

各ターミナルとも「開始」で起動（1つずつ間を空けて）。役割は`prompts/startup-auto.md`のstep0〜6で
自動決定する。
