# T-077（ヘルプ「使い方」画面）調査 中断時点の引き継ぎ

**状態**：T-081優先采配（殿直接指示2026-07-12）により調査を一旦中断。DoD4点中、(1)(4)は調査完了、
(2)(3)は未着手。再開時はこのメモから続行する。

## (1) docs/spec全11領域の転用適性 棚卸し — 完了

Exploreエージェントに委譲・完了済み（下記をそのまま採用可）。

**区分別集計**：そのまま平易化できる＝6件（placement／menu-toolbar／sheet-document／wiring／
drc-output／statusbar）、操作説明が薄く追記が要る＝4件（undo-redo／device-table／canvas-display／
part-management）、開発者向けすぎて対象外＝1件（pdf-testmode——実装未着手ゆえ操作できる機能が無い）。

詳細な11件個別判定（根拠・該当章番号つき）はエージェント完了ログに全文あり。再開時は本メモの
担当（隠密2）へ再委譲するか、以下要点から書き起こす：
- 「そのまま」6件も、開発者向け節（裁定経緯・忍者/隠密呼称・タスク番号・コミットハッシュ・不明点）は
  転用前に除外要、という共通留意点あり。
- pdf-testmode.mdは対象外だが、T-060（PDF出力UI結線）はその後完全Done（2026-07-12）ゆえ、
  再開時はこの区分自体の再判定が必要（棚卸し実施時点でT-060は未Doneだった可能性——要再確認）。

## (2) 「使い方」コンテンツ構成案の叩き台 — 未着手

## (3) アプリ内表示形式の選択肢整理 — 途中（材料のみ収集済み）

自ら確認した前例（本セッションでの直接調査）：
- `AboutDialog`（T-074前例）：モーダル`Window`、280x130、StackPanel、20+11行の極小規模。
- `PdfPreviewDialog`（T-060前例）：モーダル`Window`、900x700、Grid3行（ツールバー/コンテンツ/ボタン）、
  157+40行。内容量が多い画面の直近前例として有用。
- T-058（AvalonDock導入、殿裁定済み・PoC未着手）：「パネル」形式を選ぶ場合、既存4区画レイアウト
  （左パレット/中央キャンバス/右機器表・プロパティ/下部出力）への新規パネル追加はAvalonDock PoC前は
  コスト増の可能性（`docs/spec/ecad2-spec-menu-toolbar.md`8節参照）。
- 設計規約「モーダルダイアログの非ネスト方針」（`docs/ecad2-ui-ux-design-brief.md`44行、R7）が
  ダイアログ形式選択時の制約として関係しうる。

## (4) GuiEcad原本のヘルプ前例照合 — 完了、重要な発見あり

**結論：前例あり。** GuiEcadの「ヘルプ」メニューには「使い方...」という項目が実在し、`OnMenuHowTo`
（`C:\Users\kojif\Desktop\生産物\gui_ecad\src\GuiEcad.App\MainPage.Menu.cs:456-557`、約102行）として
実装されている（Aboutダイアログ＝`OnMenuAbout`とは別実装・別内容）。

- 表示形式：モーダル`ContentDialog`（WinUI3）、`ScrollViewer`（MaxHeight=600, MinWidth=460）でスクロール可、
  単一OKボタン。
- コンテンツ構成：6章（モード切替／作図／機器名・コメント／選択・移動・削除／シート・表示／
  ファイル・出力）＋「主なショートカット」表（2列Grid、11行）。文章＋箇条書きの混在。
- UI要素：`TextBlock`・`StackPanel`・`Grid`の組み合わせで全てコードから動的生成。RichTextBox・
  WebBrowser・外部ファイル起動は不使用。
- 専用XAMLファイルなし、1メソッドに集約（MainPage.Menu.cs内）。

**再開時の使い方**：ecad2でWPF移植する際、この章立て構成（機能カテゴリ見出し＋箇条書き／
ショートカット一覧テーブル）とモーダルダイアログ＋動的UI構築という方針をそのまま参考にできる。
表示形式の選択肢整理（3節）にこの前例を「規模の目安」として組み込むこと。

## 再開時の次の一手

1. (2)コンテンツ構成案の叩き台を起草（GuiEcadの6章構成を土台に、docs/spec転用可能6件+追記要4件を
   マッピング）。
2. (3)を仕上げる：ダイアログ/パネル/別ウィンドウの利点欠点表を完成させ、AboutDialog・PdfPreviewDialog・
   GuiEcad`OnMenuHowTo`の3前例を規模の目安として明記。殿への選択肢提示材料として整える。
3. pdf-testmode.mdの区分再確認（T-060完了後の状態を反映）。
4. `docs/ecad2-t077-help-usage-design-onmitsu2.md`として正式報告書に統合。
