# 不具合パターン再発 台帳

「パターン再発検知」制度（`docs-notes/roles/onmitsu.md`・`docs-notes/roles/karo.md`参照）を、
記憶やその場のgrepという偶然に頼らず確実に運用するための台帳。2026-07-13、殿のご指摘で新設
（T-069往復3周目レビューで`CommitDeviceNameEdit()`呼び忘れ3回目を検出できたのは、たまたま
コード内コメントに過去の経緯が残っていたからに過ぎず、再現性が無いとの所感による）。

## 使い方

- **レビュー・調査で不具合を見つけたら、まず本台帳の「パターンの型」列と照合する**（記憶・grepは
  補助手段に留める）。一致する型があれば「パターン再発」として報告し、本台帳の該当行へ再発履歴
  （タスクID・日付）を追記する。
- 一致する型が無いが、過去の別タスクと根本原因が同じと判断できる場合は、**新規パターンとして
  本台帳へ追加**する（初出時点では「再発」ではなく「新規パターン候補」として記録し、2件目が
  出た時点で正式に「パターン」と確定してよい）。
- 追記は**検知した役が自著してよい**（隠密が主だが、忍者・侍が気づいた場合も可）。追記後は
  家老へ一報し、家老が制度化の要否を検討する（`karo.md`「パターン再発の制度化検討」参照）。
- 「制度化状況」列は、対処が完了したら更新する（pending→制度化済み等）。制度化済みでも再発する
  ようなら、その制度化が不十分だった証拠として扱い、再検討する。

## 記録様式

| ID | パターンの型（根本原因） | 初出 | 再発履歴（タスクID） | 制度化状況 |
|---|---|---|---|---|

## 記録

| ID | パターンの型（根本原因） | 初出 | 再発履歴（タスクID） | 制度化状況 |
|---|---|---|---|---|
| PR-01 | 新規選択可能状態の横展開漏れ——状態管理の入口（選択排他setter・Escキー条件・矢印キードラフト分岐・削除コマンドOR連鎖・右クリックヒットテストチェーン）が複数箇所に分散し、新しい`Selected*`状態を追加する際にそのいずれかへの反映が漏れる | T-082（2026-07-12、`CurrentSheetIndex`のクロスカット処理漏れ） | T-069（2026-07-13、`SelectedElement`/`HitTestElement`不整合）・T-064（2026-07-13、`SelectedImage`の横展開漏れ5件）・T-064追加往復（2026-07-13、`ReplaceDocument`が`SelectedConnector`等4種はクリアするが`SelectedImage`だけクリア漏れ。画像挿入ドラフトのクリア漏れ修正のわずか6行上に同種の別漏れが現存していた実例）・T-064矢印キー（2026-07-13、矢印キーハンドラに`SelectedImage`を見る分岐が無く既定のセル移動処理へフォールスルーし選択が意図せず解除される。既存チェックリスト項目3「記入中ドラフトの矢印キー分岐」は対象タイミングが異なり捕捉できず、7項目目「選択状態自体の矢印キー平行移動」を新設）。**（2026-07-13隠密2棚卸しで制度化前の遡及実例10件確認）**T-019・T-033増分1・T-041増分1/増分2/増分7・T-043→T-045増分B（同一弱点が2タスクにまたがり未修正のまま連鎖）・T-051・T-055増分1・T-080・T-082初回。詳細は`docs-notes/pattern-recurrence-log-survey-20260713.md`参照 | **制度化済み**（07b0890、`task-implementation`スキル+`samurai.md`「新規選択可能状態の横展開チェックリスト」5項目） |
| PR-02 | 未確定編集の確定漏れ——`UpdateSourceTrigger=Explicit`な入力（機器名編集等）を、選択状態を変更する前に確定処理（`CommitDeviceNameEdit()`等）で確定し忘れ、編集内容がサイレント消失する。確定処理の呼び出しが呼び出し元の自己申告に依存し一元化されていない | T-049（詳細はtodo-archive.md参照、`MainWindow.xaml.cs`コメントに経緯記載） | T-066（機器表セル編集、詳細同上）・T-069往復3周目（2026-07-13、要素/縦コネクタ/行操作の3分岐へ個別パッチ）。**（2026-07-13隠密2棚卸しで制度化前の遡及実例3件確認）**T-051（Ctrl+Z/Yガード漏れ）・T-063（`DeleteMenuItem_Click`）・T-065/T-066（型式セル編集、文書自身が「T-049/P-013の再発」と明記）。詳細は`docs-notes/pattern-recurrence-log-survey-20260713.md`参照 | **制度化済み（軽量案、2026-07-13殿裁定）**——PR-01のチェックリストへ6項目目「未確定編集の確定処理」として追加（`samurai.md`・`task-implementation`スキル）。**殿の明示条件：4回目が発生したら構造的解決（setter側への集約）へ格上げ**、P-071参照 |
| PR-03 | 早期return（SetPropertyの値不変判定によるクリア処理スキップ）——参照先（実体）は変わったがSetPropertyが比較する値（index等の数値キー）が偶然一致し、本来発火すべき`OnPropertyChanged`ひいては連鎖するクリア・再描画処理が丸ごとスキップされる。既存メモリ`ecad2_setproperty_early_return_trap.md`が指す罠と同型だが台帳には未記帳だった | T-019（b7844d2、起動直後Sheets=0→最初のシート追加でCurrentSheetIndex=0のまま） | T-041増分5（シート削除でCurrentSheetIndex数値不変、往復2周目で再発も確認）・T-082（MoveSheetCommandで添字変化のみ判定、初回・往復2周目とも再確認） | **制度化済み**（2026-07-13家老、`samurai.md`「SetProperty早期return罠の確認」+`task-implementation`スキル） |
| PR-04 | 境界値/クランプ検証漏れ（新規プリミティブ・座標境界チェックの個別実装分散）——新しい図形要素・操作を追加する際、既存要素で確立済みの境界クランプ・範囲検証（Grid行数、画面外周、min>maxガード等）がその新規実装には移植されず、クラッシュや視覚異常を招く | T-033（増分2、クランプの非重なり非保証・範囲外セルでの視覚異常） | T-041増分7（VerticalConnector Top/Bottom未検証・FreeLineクランプ欠如・ConnectionDot未適用の3件）・T-051（round3、SelectedCell復元のGrid.Rows範囲クランプ漏れ）・T-055増分1（DeleteRowCommandのクランプ漏れ） | **制度化済み**（2026-07-13家老、`samurai.md`「新規プリミティブ・座標処理の境界検証チェックリスト」+`task-implementation`スキル。台帳中最多の再発件数） |
| PR-05 | 状態リセット処理の横展開漏れ（Document/Sheet構成変更時）——PR-01の親戚筋。文書・シート構成を差し替える処理を新設する際、既存の同種処理（`ReplaceDocument`/`SetCurrentSheetIndexCore`等）が担う状態クリア責務（UndoManager・OutputPanel・SelectedSheet通知・SelectedCellクランプ）への追従が漏れる | T-051（`ReplaceDocument`のUndoManagerクリア責務欠如ほか3件） | T-055増分1（DeleteRowCommandの状態クリア漏れ）・T-070往復2周目（2026-07-14、D-4＝逆方向の型。既存処理`ReplaceDocument`へ新規責務`Find.ClearResults()`を追加する際、`ApplyUndoRedoSnapshot`のみに適用し`ReplaceDocument`本体への横展開が漏れた。隠密は「型は同一・方向が逆」と申告、家老確認のうえ記帳） | **制度化済み**（2026-07-13家老、PR-01とは対象が異なる（選択状態 vs 文書/シート構成変更時の責務）ため独立記帳・独立チェックリスト化。`samurai.md`「文書/シート構成変更処理の状態リセットチェックリスト」+`task-implementation`スキル） |
| PR-06 | 型強制不足（規約レベルの誘導止まり）——「正しい呼び出し順序・対の処理」が型システム（コンパイラ）で強制されず、コメント・テスト・レビューという人的手段のみに依存。将来の拡張（第5のドラッグ型追加等）で同型欠陥が再発しうる | T-045増分D（`ForceCancelIfAny`のcancel引数がAction型のまま） | T-050（fix2、`SetCurrentSheetIndexCore`のペアリング不変条件が型で強制されない） | **制度化済み（レビュー観点として軽量対応）**（2026-07-13家老、`onmitsu.md`「型強制不足の観点確認」——実装レベルの型強制は個別設計判断のため見送り、コードレビュー時の確認観点として追加） |
| PR-07 | コピペ重複/共通ロジックの複数箇所分散（rule of three到達）——同一ロジック（SelectedCellクランプ式・StatusMessage/MarkDirty/Notifyトリプレット・機器表クリーンアップ・ページ構成ロジック等）が3箇所目の複製に到達、過去に実際の書き漏れバグ実績あり | T-055（増分2・増分3差し込み分、SelectedCellクランプ式等が3箇所目に到達） | T-060（プレビュー/実出力PDFのページ構成ロジック二重実装）・T-064矢印キー（2026-07-13、View側`MoveSelectedXxxByKey`が`MoveSelectedImageByKey`新設で4箇所目の複製に到達、3箇所目<ConnectionDot>時点で制度化手続きが踏まれた形跡なし。本コミット単体の欠陥ではなく蓄積債務として指摘） | **制度化済み**（2026-07-13家老、`samurai.md`「共通ロジックの複製検知」+`task-implementation`スキル） |
| PR-08候補 | SetPropertyバイパスによる旧値ロスト——SetProperty非経由の直接代入＋1引数版`OnPropertyChanged`呼び出しで旧値が常にnullになる。侍が個別箇所を直すたび兄弟コードに同型が新たに見つかる「もぐら叩き」型 | T-039（`SelectedElementDeviceName`/`Tool`/`ReplaceDocument`→修正の都度`SaveToFile`/`DeviceTableViewModel`/`SheetNavigationViewModel`で再発、同一タスク内で3回） | （2件目未確認、新規パターン候補のまま） | 新規パターン候補（2026-07-13隠密2発見、1タスクのみ） |
| PR-09候補 | 文字列正規化漏れ——真偽値等の文字列判定が半角"0"→全角０→全角ラテン文字と、直すたび対象範囲が拡大する横展開漏れ | T-039（同一タスク内で3回連続再発） | （2件目未確認、新規パターン候補のまま） | 新規パターン候補（2026-07-13隠密2発見、1タスクのみ） |
| PR-10候補 | 旧値捕捉タイミング誤り——`oldValue`をコレクション/インデックス変更後に読んでしまい、old==newや無関係な値をPropertyChangedへ通知する。PR-08候補（SetPropertyバイパス）とは別メカニズムだが「oldValueが不正確」という症状面で関連しうる | T-050（`CurrentSheetIndex`セッタ・`ResetSheets()`・`SelectedSheet`セッタの3箇所で同根） | （2件目未確認、新規パターン候補のまま） | 新規パターン候補（2026-07-13隠密2発見、1タスクのみ） |
| PR-11候補 | 座標系（絶対/相対）取り違え——`Margin`計算等が絶対座標系と相対座標系を混同する | T-033（増分2、`PositionPlacementBar`の初回バグと修正後に残った同型の脆弱性） | （2件目未確認、新規パターン候補のまま） | 新規パターン候補（2026-07-13隠密2発見、1タスクのみ） |
| PR-12 | 新規上位モード導入時のマウス/キーボード経路対応漏れ——アプリ全体を横断する新モード（AppMode等）を導入する際、マウスイベントハンドラには個別に`if (Mode==X) {...; return;}`という特殊分岐を追加するが、グローバルキーボードショートカット・メニュー・ツールバーボタン・Undo/Redo・テキスト編集欄等の他の入力経路には同種のガードを追加し忘れる。加えてモード遷移時に選択状態（SelectedCell等）自体をクリアしないため、キーボード側のDelete/矢印キー/Undo等が無条件でその選択状態に作用してしまう。PR-01（新規選択可能状態の横展開漏れ）と性質は近いが対象が異なる（PR-01は選択可能状態、本件は上位モード）ため独立記帳 | T-061（2026-07-13、テストモードUI結線。マウス側2箇所のみガードしキーボード全域・ツールバー配置ボタン・削除メニュー・Undo/Redoが素通し、`docs/archive/ecad2-t061-review-onmitsu.md`） | T-061修正1周目（2026-07-13、同一タスク2周目で再発。`CanEditDiagram`統合という横展開自体は実施したが、対象の洗い出しが不完全でDeviceNameBox（機器名編集欄）・画像挿入メニューの2箇所が漏れた。`docs/archive/ecad2-t061-fix-review-onmitsu.md`） | **正式パターン確定**（2026-07-13、2件目発生。新規ゲート導入時は全入力経路（ツールバー・キーボード・メニュー・テキストボックス編集欄）を機械的に列挙してから適用する手順が必要、家老へ制度化検討を促す） |
| PR-13 | 上位モードゲート確立後に追加された新機能のゲート接続漏れ——`CanEditDiagram`/`HasProject`等の統一ゲートが既に存在する状態で、後続の新機能（検索置換・新規キーボードショートカット等）を実装する際、その新機能のCanExecute/IsEnabled/実行条件にゲートへの参照を追加し忘れる。PR-12（モード新設時に既存経路の洗い出しが漏れる）とは時系列の方向が逆（PR-12=モード新設時、本件=モード確立後の新機能実装時）のため独立記帳 | T-070（2026-07-14、検索・置換機能のReplaceOne/ReplaceAllCommandがCanEditDiagramガードを持たず、テストモード中でも機器名変更が実行できてしまう。`docs/ecad2-t070-review-onmitsu.md` A-1） | T-087（2026-07-14、Ctrl+Tショートカット新設時に`HasProject`ガード（メニュー/ツールバーは`IsEnabled="{Binding HasProject}"`で保護済み）を迂回。プロジェクト未作成時にCtrl+Tを押すとMode=Testへ遷移し、`ReplaceDocument`がMode/IsTestModeを一切リセットしないため後続の新規作成が編集不能になる二次被害まで確認） | **正式パターン確定**（2026-07-14、2件目発生。新規キーボードショートカット追加時は、既存の統一ゲート（CanEditDiagram/HasProject等）への参照有無を機械的に確認する手順が必要、家老へ制度化検討を促す） |
| PR-14候補 | バグ修正の適用範囲が同一機能内の別呼び出し経路まで及ばない——ある入力経路（自動処理・単発操作等）で発見したバグを直す際、追加した保護条件（ガード）が、同じ機能を提供する別の呼び出し経路（明示的なユーザー操作・全件処理版等）には横展開されない。PR-01/PR-05/PR-12/PR-13候補（いずれも「新規状態・新規モード導入時」の横展開漏れ）とは異なり、本件は「1件のバグ修正がその場しのぎに終わり、同機能の兄弟経路まで意識が及ばない」という型 | T-070往復2周目（2026-07-14、D-1＝A-3のin-place移行修正が「複数要素同名」ケースでは効かず重複再発。`docs/ecad2-t070-review-onmitsu.md` D-1） | D-5（同一タスク内2026-07-14、B-2のドラフト保護が`RunSearch`の自動JumpToにしか適用されず、明示的な`Next`/`Prev`/`JumpToMatch`経路には未適用。同上D-5） | 新規パターン候補（2026-07-14隠密発見、同一タスク内で2件。家老判断で記帳、他タスクで3件目が出れば正式パターン化検討） |
| PR-15候補 | 横断的なリセット/初期化処理が、後から横展開された状態同期ロジックへの再適用を持たない——AvalonDockのオフツリー罠（§3、`docs-notes/handover-next-session.md`）への対処として確立した「コードビハインドでPropertyChanged購読→Title直接更新」方式を複数パネルへ横展開する際、既存の共通リセット処理（`ResetDockingLayoutToDefault()`）からの再呼び出しが個別パネル追加のたびに漏れる。PR-13（既存ゲートへの新機能接続漏れ）と構造は近い（横断的な既存処理に新要素の接続を追加し忘れる）が、対象がCanExecuteゲートではなくリセット処理からの状態再同期である点で独立記帳 | T-058増分2（2026-07-14、出力パネルTitle同期`UpdateOutputPanelTitle`がリセット処理から未接続。当初の増分2静的レビューでは未検出のまま経過） | T-058増分3（2026-07-15、右パネルTitle同期`UpdateRightPanelBottomTitle`でも同型再現。増分3レビュー時に増分2側の未検出欠陥も遡って発覚、`docs/ecad2-t058-increment3-review-onmitsu.md`） | 新規パターン候補（2026-07-15隠密発見、同一タスクT-058内で2件。侍へ両者同時修正を采配、他タスクで3件目が出れば正式パターン化検討） |
| PR-16 | 1パラメータのみの書き込み経路欠落——機能全体（Core層のシミュレーション・描画ロジック等）は完備しているが、そのうち特定の1パラメータだけを書き込むUI（プロパティパネル等）が丸ごと存在せず、常に既定値0/未設定のまま固定される。従来の「Core完備・App未結線」（機能全体が丸ごと未結線、T-055/T-060/T-061等多数）より発見しづらい——単体テスト・シミュレーション自体は正常に動作し、パラメータが既定値のままでも一見動いているように見えるため看過されやすい | T-086（2026-07-14、セレクトSWのノッチ番号`ParamKeys.Position`を設定するUIがApp層に皆無。`CycleSelectSwitch`/`NetlistBuilder`は複数要素の`Params[Position]`を巡回する設計だが書き込み経路が無く常に既定値0のまま、殿のA-1実機確認で発覚） | タイマー接点のテストモード時挙動（2026-07-15、殿直接指摘・隠密調査`docs/ecad2-timer-setpoint-investigation-onmitsu.md`。`grep "Setpoint" src/Ecad2.App`が0件、プロパティパネルにタイマ設定時間欄が存在せず`Evaluator.cs`の`TimerSetpoints.TryGetValue`が常に失敗しsp=0.0固定→限時接点が通電状態に関係なく固定表示。T-061のDoDはタイマ接点の限時動作を元々含んでおらず検証範囲外に置かれ続けていた）。**殿指示によるCore層`ParamKeys`全6定数の網羅再調査（2026-07-15、隠密、`docs/ecad2-paramkeys-unwired-survey-onmitsu.md`）でさらに3件追加発見**＝Breaker3P種別`Type`（NFB/MCCB/ELB選択UI皆無、既定"NFB"固定、P-100）・主回路記号向き`Orient`（V/H設定UI皆無、実機未確認、P-101）・ラベル高さオフセット`LabelDy`（微調整UI皆無、致命度は他2件より低いと隠密所見、P-102）。全6定数中、結線済みはPositionのみと判明（5/6が未結線） | **正式パターン確定・6例判明**（2026-07-15、2件目発生時点で確定・その後の網羅調査でさらに3件追加。単発の見落としではなく構造的傾向と判明——新規パラメータ追加時のプロパティパネル設計チェックリストへ「Core層`ParamKeys`定義の全パラメータに対応する入力欄が揃っているか」の確認観点追加を強く推奨、家老へ制度化検討を促す） |

| PR-18 | 色対応調査の網羅性不足——ダークモード等の配色対応表を作成する際、「明示的な色指定のgrep」に基づいて対応箇所を洗い出すため、既定値のまま(Foreground/Background等を明示指定していない)のコントロールが対応表から漏れる。既定値のコントロールはWPF標準色（白背景・黒文字等）のまま残り、暗背景と衝突して可読性が低下する | T-083増分5（2026-07-16、殿直接指摘・隠密2調査。シート一覧ListBox・機器表/出力DataGrid中身が対応表から漏れていたと判明） | 新規発見1（2026-07-17、殿直接指摘。プロパティパネルのTextBlock/CheckBox 8箇所のラベル文字色が黒固定のまま残存）・新規発見1・3レビュー時（2026-07-17、隠密。プロパティパネルのTextBox 5箇所(DeviceNameBox等)がInputBackgroundBrush/ForegroundBrush対象外のまま残存、増分3で確立したはずの入力欄対応表からも漏れていた） | **正式パターン確定（3例判明）**（2026-07-17家老、2件目時点で確定基準を満たす。着手前調査（色対応表作成）では明示的色指定のgrepに加え、対象領域内の全コントロール型（TextBox/TextBlock/CheckBox/ListBox/DataGrid等）をXAML要素列挙で機械的に洗い出し、いずれのブラシ系列にも属さない要素の有無を確認する手順を追加すべき。次回同種調査（着手前調査）の際、隠密は本行を参照し明示的指定grepと既定値コントロール列挙の両方を行うこと） |
| PR-19候補 | 新規実装直後の不具合調査が、直近に追加した自分の差分の副作用検討を飛ばし、フレームワーク・環境側の謎機構解明へ向かってしまう——「差分が小さい・既定テンプレートに忠実」という点から「無害」と暗黙に前提し、調査の出発点が外部要因側へ固定される | T-099（2026-07-17、殿直接指示「根本的な実装も疑って調査せよ」を受けた隠密2の棚卸しで発覚。PoC自身が追加したDataTrigger（HeaderPanelのCollapsed化）が原因だった可能性が濃厚と判明するまで、3周にわたりIsVirtualizingAnchorable・WM_SIZE・UIAutomationCore等フレームワーク側の機構を追い続けていた。隠密2所見＝T-100の標的取り違え(8650a66、Grid列0Auto化が的外れだった件)にも共通の匂いがある） | （2件目未確認、新規パターン候補のまま） | 新規パターン候補（2026-07-17家老記帳、隠密2発見。今後「新規実装直後の不具合」は調査開始時に直近差分自体の副作用確認を最優先観点に含めることを推奨、2件目が出れば正式パターン化検討） |
| PR-17 | PartId配置要素の種別解決漏れ——`ElementInstance.Kind`を種別判定に直接使用すると、部品テンプレート(PartId)経由で配置された要素は常に既定値（ContactNO等）のままで実際の種別を反映せず誤判定する。実際の種別解決には`PartResolver.ComponentKind()`を経由する必要があるが、新規機能実装時にこの経由を忘れ生のKindを直接参照してしまう | T-071（2026-07-11、隠密静的レビュー指摘②。`DiagramRenderer`のタイマ限時/瞬時ラベルが生の`ElementInstance.Kind`参照で描画されない。修正は`PartResolver.ComponentKind`経由へ差し替え。当時は台帳未記帳） | T-097（2026-07-15、侍自己発見・回帰テストで検出。`DefaultLabelDy`へ渡す種別に`el.Kind`を直接使うと誤動作、`IsSelectedElementTimerRelated`と同型に`PartResolver.ComponentKind`経由で解決するよう修正） | **制度化済み**（2026-07-15家老、`samurai.md`「種別解決の直接Kind参照回避チェックリスト」） |

## 運用ノート

- 台帳が肥大化してきたら、決着済み（制度化済みで再発なし）のパターンは要約のみ残し詳細を
  `docs/archive/`等へ移すことを検討する（現時点では2件のみのため未実施）。
- 「初出」の日付が特定できない場合（旧タスクでコード内コメント等からしか経緯を追えない場合）は
  無理に日付を推測せず、タスクIDと参照先のみ記す（不明は不明と明示する原則）。
