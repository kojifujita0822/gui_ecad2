# 不具合パターン再発 台帳

「パターン再発検知」制度（`docs-notes/roles/onmitsu.md`・`docs-notes/roles/karo.md`参照）を、
記憶やその場のgrepという偶然に頼らず確実に運用するための台帳。2026-07-13、殿のご指摘で新設
（T-069往復3周目レビューで`CommitDeviceNameEdit()`呼び忘れ3回目を検出できたのは、たまたま
コード内コメントに過去の経緯が残っていたからに過ぎず、再現性が無いとの所感による）。

## 使い方

- **レビュー・調査で不具合を見つけたら、まず本台帳の「パターンの型」列と照合する**（記憶・grepは
  補助手段に留める）。一致する型があれば「パターン再発」として報告し、本台帳の該当行へ再発履歴
  （タスクID・日付）を追記する。
- 一致する型が無いが、過去の別タスクと根本原因が同じと判断できる場合は、**新規パターンとして
  本台帳へ追加**する（初出時点では「再発」ではなく「新規パターン候補」として記録し、2件目が
  出た時点で正式に「パターン」と確定してよい）。
- 追記は**検知した役が自著してよい**（隠密が主だが、忍者・侍が気づいた場合も可）。追記後は
  家老へ一報し、家老が制度化の要否を検討する（`karo.md`「パターン再発の制度化検討」参照）。
- 「制度化状況」列は、対処が完了したら更新する（pending→制度化済み等）。制度化済みでも再発する
  ようなら、その制度化が不十分だった証拠として扱い、再検討する。

## 記録様式

| ID | パターンの型（根本原因） | 初出 | 再発履歴（タスクID） | 制度化状況 |
|---|---|---|---|---|

## 記録

| ID | パターンの型（根本原因） | 初出 | 再発履歴（タスクID） | 制度化状況 |
|---|---|---|---|---|
| PR-01 | 新規選択可能状態の横展開漏れ——状態管理の入口（選択排他setter・Escキー条件・矢印キードラフト分岐・削除コマンドOR連鎖・右クリックヒットテストチェーン）が複数箇所に分散し、新しい`Selected*`状態を追加する際にそのいずれかへの反映が漏れる | T-082（2026-07-12、`CurrentSheetIndex`のクロスカット処理漏れ） | T-069（2026-07-13、`SelectedElement`/`HitTestElement`不整合）・T-064（2026-07-13、`SelectedImage`の横展開漏れ5件） | **制度化済み**（07b0890、`task-implementation`スキル+`samurai.md`「新規選択可能状態の横展開チェックリスト」5項目） |
| PR-02 | 未確定編集の確定漏れ——`UpdateSourceTrigger=Explicit`な入力（機器名編集等）を、選択状態を変更する前に確定処理（`CommitDeviceNameEdit()`等）で確定し忘れ、編集内容がサイレント消失する。確定処理の呼び出しが呼び出し元の自己申告に依存し一元化されていない | T-049（詳細はtodo-archive.md参照、`MainWindow.xaml.cs`コメントに経緯記載） | T-066（機器表セル編集、詳細同上）・T-069往復3周目（2026-07-13、要素/縦コネクタ/行操作の3分岐へ個別パッチ） | **制度化済み（軽量案、2026-07-13殿裁定）**——PR-01のチェックリストへ6項目目「未確定編集の確定処理」として追加（`samurai.md`・`task-implementation`スキル）。**殿の明示条件：4回目が発生したら構造的解決（setter側への集約）へ格上げ**、P-071参照 |

## 運用ノート

- 台帳が肥大化してきたら、決着済み（制度化済みで再発なし）のパターンは要約のみ残し詳細を
  `docs/archive/`等へ移すことを検討する（現時点では2件のみのため未実施）。
- 「初出」の日付が特定できない場合（旧タスクでコード内コメント等からしか経緯を追えない場合）は
  無理に日付を推測せず、タスクIDと参照先のみ記す（不明は不明と明示する原則）。
