# T-083増分2（UIクローム基盤構築、層C基盤）実機検証記録

検証者: 忍者（key=1784187128338） / 検証日: 2026-07-16
対象コミット: 1235898（`src/Ecad2.App/`配下）

## 結論（先出し）

観点1・2・4・5はOK。**観点3（メニューバー本体の暗色化）はNG**——侍報告「効果なし・保留」が
実機でも裏付けられた。コード内コメント「オーバーライドすることで対応する」は実態と食い違って
おり、記述の見直しが必要。

## 観点別 OK/NG

### 観点1：ツールバー本体（ToolBarArea）のLight/Dark連動 — OK

起動直後（Light）は白/薄水色系背景、ダークモードON後は黒背景へ即時切替。
（`t083-2-initial-light.png` → `t083-2-darkon-nosheet.png`）

### 観点2：シート0件時のキャンバス色固定バグ解消 — OK

シート0件のままダークモードON/OFFを切替えたところ、ON時は黒、OFF時は濃紺と、シートが無い
状態でも正しく連動して切り替わることを確認。増分1 PoC時点の既知バグ（シート0件時のみキャン
バスが固定の濃紺色になる）は解消されている。
（`t083-2-darkon-nosheet.png` / `t083-2-darkoff-nosheet.png`）

シート追加後（シート1）でも黒背景・グレーグリッド線で正しく表示されることを確認済み
（`t083-2-darkon-sheet1.png`）。

### 観点3：メニューバー本体の暗色化【隠密指摘、重要】 — **NG**

ダークモードON状態で確認したところ、メニューバー（「ファイル(F)」「編集(E)」「図面(D)」
「表示(V)」「ツール(T)」「ヘルプ(H)」が並ぶ行）の背景は白いままで、暗色化していない
（ツールバー本体は黒くなるのに対しメニューバー行のみ変化なし）。侍の実装報告「効果なし・保留」
が実機の見た目と一致し、コード内コメント「オーバーライドすることで対応する」という成功を
明言する記述は実態と食い違っている。（`t083-2-darkon-nosheet.png`, `t083-2-darkon-sheet1.png`
のいずれもメニューバー行が白いまま）

### 観点4：右クリックContextMenu（ラダーキャンバス上）の意図せぬ変化有無 — OK（波及なし）

シート追加後、キャンバス上で右クリックしContextMenu（「行2の前に行を挿入」「末尾に行を追加」
「行2を削除」）を表示。ダークモードON状態でも白背景・黒文字のままで、SystemColors上書きの
影響による意図せぬ変化は見られなかった。（`t083-2-contextmenu-darkon.png`）

### 観点5：既存機能への回帰なし — OK

- 層A（作図キャンバス色）：観点2参照、正しく連動・回帰なし
- 層B（AvalonDockドッキングクローム、増分1で保留中）：シートパネルのタイトルバー等は今回も
  変化なし（青いまま）。保留中の想定通りで、増分2による新たな影響は見られない

## 範囲外検出

なし。

## スクリーンショット一覧

`C:\Users\kojif\AppData\Local\Temp\claude\C--ECAD2\94da924d-f90e-4959-a6f5-2c5199b52c6c\scratchpad\`
配下：`t083-2-initial-light.png`, `t083-2-darkon-nosheet.png`, `t083-2-darkoff-nosheet.png`,
`t083-2-darkon-sheet1.png`, `t083-2-contextmenu-darkon.png`（一時ディレクトリのため恒久保存が
要る場合は家老の指示で`docs-notes/`等へ移す）

## 検証中の経緯（参考）

検証開始時、侍殿が増分3実機確認用にEcad2.Appを起動したままにしていたため、事前にpeerメッセージで
確認のうえ終了いただいてから、こちらでビルドし直して起動した（コミット1235898反映済みを確認）。
