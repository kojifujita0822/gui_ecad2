# `PR-17`複製統合 測定点②（段1後）実測記録（忍者、2026-08-06）

**検体**＝コミット`adc8d64`（段1＝複製3箇所を基準呼び出しへ統合、45行→12行）。
**主張**＝振る舞い不変。①（`docs/ecad2-pr17-baseline1-verification-ninja.md`）と同一手順で再測し突き合わせた。

---

## 結果一覧（①→②）

| # | 観点 | ①の実測 | ②の実測 | 判定 |
|---|---|---|---|---|
| 5-4 | 配置経路、Escのみで即確認 | 二重型が出る（穴あり） | **二重型が出る（不変）** | **OK（段1では塞がらぬ、設計どおり）** |
| 5-4対照B | 別セル経由で復帰 | プレースホルダ正しく消える | **同左（不変）** | **OK** |
| 5-1 | `ReplaceDocument`（開く） | プレースホルダ正しく表示 | **同左（不変）** | **OK** |
| 5-2 | `ReplaceDocument`（新規作成） | プレースホルダ正しく表示 | **同左（不変）** | **OK** |

**4項目すべて①と一致。段1の主張（振る舞い不変）を実機で裏付けた。**

---

## 家老の申し送り（`SelectedCell` setterの無条件通知1件）について

**`SelectedImage=null`／`SelectedFrame=null`の無条件通知setterが、統合前から`HasNoPropertySelection`を
1件飛ばしていた由**（侍実測、段1で生じたものではない）。**本記録の5-4対照Bはこの経路を通っており、
①②とも「正しく消える」という結果はこの1件に支えられている**——**これは段1の影響ではなく元々の
仕様**ゆえ、①→②で不変という結論に変わりはない。

---

## 段2への申し送り

**5-4（配置経路）は段2でこそ「塞がる」ことを見る観点**（観点書5節、5-3・5-4行）。
**5-1・5-2は段2でも「変わらぬこと」を確認する観点**（暗黙の救いが段2で明示に変わるのみ、
挙動としては不変が期待値）。**③の実測でこの2種の期待値を混同せぬこと。**

## 後始末

- `main-layout.xml`退避＝`main-layout.xml.ninja-pr17-stage2-backup-20260806`。
  **今回も`graceful close`で上書きされたため、退避コピーから復元し無傷に戻した**
- `src`・`tests`への書き込みは一切行っておらぬ
