# `PR-17`複製統合 測定点③（段2後）実測記録（忍者、2026-08-06）

**検体**＝コミット`d152286`（段2＝`HasNoPropertySelection`を基準へ足す、`P-169`の手当て）。
**主張**＝意図した変化（②までの「穴は残る」が「塞がる」へ転じる）。

---

## 結果一覧

| # | 観点 | ①②の実測 | ③の実測 | 判定 |
|---|---|---|---|---|
| 5-4 | 配置経路、Escのみで即確認 | 二重型が出る（穴あり） | **塞がった**（プレースホルダ非表示、種別:a接点は正しく表示） | **OK** |
| P-169本題 | 要素選択中に`Delete`キー | 空白型（穴あり、既存記録どおり） | **塞がった**（プレースホルダが正しく表示） | **OK** |
| 5-3 | 行削除経由（`P-169`第六経路、`DeleteRowAtCommand`） | 未測→②で穴確認 | **塞がった** | **OK** |
| 5-2 | `ReplaceDocument`（新規作成） | 正常表示（不変） | **不変のまま正常** | **OK** |
| 5-1 | `ReplaceDocument`（開く） | 正常表示（不変） | **正常表示**（下記の留保あり） | **OK（留保付き）** |

**4項目（5-4／P-169本題／5-3／5-2）は明確に確認できた。**

**5-1のみ留保**——測定の途中、部品配置のOKボタンInvokeで一度`null`例外が出申した（既知の
タイミングの罠、`ecad2-ui-automation`スキル既載）。結果としてプレースホルダは正しく表示されたが、
**「要素選択中」という前提条件が完全に満たされていたか確証が薄い**。ただし「未選択状態で開いても
プレースホルダは元々正しく出る」ため、いずれにせよ結果は正常の範囲内。**再測すればより確実**だが、
①②で同型の観点は既に確認済みであり、③の主眼（5-4・P-169本題・5-3の「塞がる」確認）は他4項目で
十分に裏付いておるため、時間配分の判断で深追いはせぬ。

---

## まとめ

**段2の実装（基準`NotifySelectedElementChanged()`へ`HasNoPropertySelection`を追加）により、
①②で確認した4種の穴（P-169本題の削除経路・行削除経路・配置経路2種＝5-4で代表確認）が
すべて実機で塞がることを確認した。**

**`ReplaceDocument`経路は侍の推論どおり①〜③を通じて見え方の変化なし**（暗黙の救いが明示に
変わっただけ）。

**予期せぬ結果は出ておらぬ**（家老申し送りの「期待値が外れる恐れ」は本件では的中せず）。

## 後始末

- `main-layout.xml`退避＝`main-layout.xml.ninja-pr17-stage3-backup-20260806`。
  **今回も`graceful close`で上書きされたため、退避コピーから復元し無傷に戻した**
- `src`・`tests`への書き込みは一切行っておらぬ
