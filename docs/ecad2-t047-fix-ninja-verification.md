# T-047修正 実機再確認記録（忍者、key=1783553255032）

検証日: 2026-07-09
対象コミット: `afeb068`（フォーカス修正）+ `cffef19`（並び替え）、main上・push保留中
参照: 隠密テスト設計書 `docs/ecad2-t047-fix-test-design-onmitsu.md`

## 総合判定: **全観点OK（GREEN）**

家老指定観点(1)〜(4)いずれも良好。前回検証（`docs/ecad2-t047-ninja-verification.md`）で発見した
所見1・新規重大所見とも解消を確認した。

---

## 観点(2) 並び順の目視（先に実施）

新規プロジェクト作成直後のツールバーをUI Automation要素列挙＋スクショで確認：

選択ツール→a接点(F5)→OR a接点(sF5)→b接点(F6)→OR b接点(sF6)→コイル(F7)→端子台(F8)→
自由線(横線)記入(F9)→自由線(縦線)記入(sF9)→縦分岐線記入(sF9)→接続点記入(F10)→
配線分断記入(F10)→区切り線→自作パーツ

家老指定の並び順と完全一致。精細スクショ採取済み（`docs-notes/images/t047-fix-toolbar-order-ninja.png`）。OK。

## 観点(1)【本丸】フォーカス修正の確認 — 対称性表（設計書2-3節）に基づく検証

### 縦分岐線記入（制御回路シート、上下キー、前回4-aで再現確認済み）

| 項目 | 修正前(前回RED) | 修正後(今回) |
|---|---|---|
| Tab+Enter起動直後のフォーカス | ボタン上に残留 | **`LadderCanvasHost`へ即座に移動** |
| 矢印キー押下 | 無反応 | **プレビュー範囲が正しく変化**（スクショ確認） |
| 矢印キー後のフォーカス | ボタン上のまま | **キャンバスに維持**（隣接ボタンへの逃げなし） |
| Enter確定 | 無反応 | **正常に確定**（ツール状態がSelectへ復帰） |

GREEN。スクショ: `t047-fix-connector-arrowkey-works-ninja.png`

### 自由線(横線)記入（主回路シート、左右キー、前回4-cで新規重大所見を確認した対象）

| 項目 | 修正前(前回RED) | 修正後(今回) |
|---|---|---|
| Tab+Enter起動直後のフォーカス | ボタン上に残留 | **キャンバスへ即座に移動** |
| 左右矢印キー押下 | 無反応、かつ隣接ボタン「接続点記入」へフォーカス移動（新規重大所見） | **プレビューが正しく伸長、フォーカスはキャンバスに維持（隣接ボタンへの移動なし）** |
| Enter確定 | 誤って隣接ボタンが起動され接続点が意図せず確定配置 | **正常に自由線が確定**（誤配置なし） |

**前回発見した新規重大所見（ツールバーナビゲーション奪取→隣接ボタン誤起動→データ混入）は完全に解消。**
スクショ: `t047-fix-freeline-h-no-hijack-ninja.png`

### 自由線(縦線)記入（主回路シート、上下キー、**前回未検証・今回初確認**）

| 項目 | 結果 |
|---|---|
| Tab+Enter起動直後のフォーカス | キャンバスへ即座に移動 |
| 上下矢印キー押下 | プレビューが正しく伸長、フォーカスはキャンバスに維持 |
| Enter確定 | 正常に確定 |

GREEN（新規確認）。スクショ: `t047-fix-freeline-v-works-ninja.png`

### 接続点記入・配線分断記入（即時確定型、設計書＝変更不要の対象）

接続点記入をTab+Enterで起動 → **設計書どおりフォーカスはボタン上に残留したまま**（現状維持、
変更なしを確認）。即時確定型のため記入モードに入らず、その場で確定（黒丸が正しく配置される
ことをスクショで確認）。回帰なし。スクショ: `t047-fix-connectiondot-unchanged-ninja.png`

### 既存ボタンの非回帰確認（懸念4）

代表として「a接点配置」ボタンをTab+Enterで起動 → 修正前と同じくフォーカスがボタン上に
残留（懸念4対応の既存挙動を維持）。ツール状態も`PlaceElement`へ正しく遷移。回帰なし。

### マウス起動経路の非回帰確認

「縦分岐線記入」ボタンをUI Automation Invoke（マウス相当）で起動 → 矢印キー・Enterとも
従来通り正常動作。回帰なし。

## 観点(3) 軽い回帰

- Shift+F9（縦分岐線、直接キー押下・Tab経由せず）→ `PlaceConnector`へ正常遷移、Escキャンセルも正常
- F10（配線分断、制御回路シート）→ 即時確定、従来通り
- シート種別によるボタン活性/非活性の連動は、シート追加・切替の過程で終始正しく維持されていた
  （前回検証済みの内容を含め、本検証中も崩れは見られず）

いずれも回帰なし。OK。

## 観点(4) 余力があれば（必須ではない、副次確認）

主回路シートで自由線(横線)記入を起動（記入中＝`PlaceLine`状態）した後、**Tabキーを押すと
フォーカスがメニューバー「ファイル(F)」まで抜けてしまう**ことを確認した。Shift+Tabで
ツールバーへ戻しても記入中の状態（`Tool.Mode=PlaceLine`）自体は破壊されずに維持されており、
データ混入や無反応の再発には至らなかった。

**気づき（断定なし・深掘りは行っていない）**: 記入中状態でもTabキー自体はツールバーの外
（メニューバー）まで通り抜けてしまう経路が残っている。今回はメニューバーに着地しただけで
実害は確認されなかったが、家老采配になかった観点(4)は「必須ではない」項目のため、これ以上の
追跡（メニューバー到達後にさらに操作を続けた場合の挙動等）は行っていない。修正の要否は
家老・隠密の判断に委ねる。

---

## 総括

観点(1)〜(3)は全てGREEN。前回発見した所見1・新規重大所見（データ混入）とも解消を確認した。
観点(4)（任意）で軽微な気づき（記入中のTabキーがメニューバーまで抜ける）を1件観察したが、
実害なし・深掘り対象外として記録するに留める。

**全観点OKにつき、まとめてpush→殿の意匠目視→T-047クローズの運びで支障なしと判断する。**

---

## 追記: 半透明化（`12f85b8`）の対比スクショ採取

対象コミット`12f85b8`（無効時ボタンの半透明化、隠密確認クリーン済み）を反映した新バイナリで
再ビルド・再起動の上、家老采配どおり3枚を採取した。

- **制御回路シート表示時**: 自由線(横線)・自由線(縦線)・接続点の3ボタンが半透明(0.35)、
  縦分岐線・配線分断の2ボタンが通常表示（活性）。対比明瞭。
  `docs-notes/images/t047-opacity-control-circuit-ninja.png`
- **主回路シート表示時**: 上記が完全に逆転（自由線2種・接続点=活性、縦分岐線・配線分断=半透明）。
  `docs-notes/images/t047-opacity-main-circuit-ninja.png`
- **プロジェクト未作成時**（余力ありのため撮影）: ツールバー全ボタン（新設5ボタン含む）が
  半透明。`docs-notes/images/t047-opacity-no-project-ninja.png`

**観察所見（0.35で判別できるかの実感）**: 実機の等倍表示（100%）で目視した限り、半透明
(0.35)ボタンと通常表示ボタンの区別は十分判別可能だった。アイコンの線が薄いグレーがかった
色合いになり、通常表示のはっきりした黒系アイコンとの濃淡差が一目で分かる。既存の配置系
ボタン（a接点等、`InverseBool`等既存の無効化スタイル）と並べても違和感なく、新設5ボタンだけ
浮いて見えることはなかった。
