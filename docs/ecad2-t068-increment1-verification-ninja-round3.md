# T-068増分1 再検証記録（忍者・往復3周目）

対象コミット: `9855a36`（2件目バグ修正、`CustomPartsMenu.UpdateLayout()`明示呼び出し）
前回記録: `docs/ecad2-t068-increment1-verification-ninja-round2.md`

## 結論：観点3、依然として未解消

前回試した4パターン（複数ステップホバーを除く）を全て再試行したが、**いずれも前回と全く同一の
症状が再現し、修正の効果は確認できなかった**。

### 手法1: `ExpandCollapsePattern.Expand()`
- 対象（「忍者テストパーツ01」）の`SupportedPatterns`は前回同様`ExpandCollapsePatternIdentifiers.
  Pattern`を含む
- `Expand()`実行後、メニュー項目一覧はトップレベル（ファイル/編集/図面/表示/ツール/パーツ/ヘルプ）
  のみに戻り、全階層が閉じた

### 手法2: 物理クリック単発（`SetCursorPos`+`mouse_event`合成）
- 対象の座標中心をクリック → 手法1と同一結果（全階層が閉じる）

### 手法4: キーボードナビゲーション（`Alt+P`→`↓`→`→`→`↓`→`↓`→`→`）
- `→`押下直前、フォーカスは正しく「忍者テストパーツ01」に位置していることを確認
  （`HasKeyboardFocus=True`）
- `→`押下後、手法1・2と同一結果（全階層が閉じる）

### 手法5: タイミングサンプリング（Right送信直後を0ms/100ms/500ms間隔で計測）
- Right送信前：ポップアップウィンドウ数4件（メインウィンドウ＋「パーツ」「自作パーツ」の2階層分＋α）
- Right送信直後（0ms）：**既に2件に減少**（前回と全く同じ数値）
- 100ms後・500ms後：変化なし（2件のまま）
- 3階層目のポップアップウィンドウが一瞬でも出現した形跡は今回も確認できず

## 総合判定

`CustomPartsMenu.UpdateLayout()`の追加は、忍者が確認できる範囲では症状に変化を与えなかった。
4パターンとも往復2周目と完全に同一の失敗モード（メニュー全体が閉じる、3階層目ポップアップが
一切出現しない）を示しており、再現性は高い（変化なし＝安定して再現）。

## 参考：忍者からの気づき（範囲外、判断は家老・侍・隠密に委ねる）

`CustomPartsMenu.UpdateLayout()`は`CustomPartsMenu`（2階層目、`自作パーツ`メニュー自体）に対して
呼ばれている。動的生成される個別パーツ項目（`sub`、3階層目の親）自体、あるいはその中の
`editItem`/`deleteItem`（3階層目本体）に対しては`UpdateLayout()`が呼ばれていない。もし今回の
仮説（Measure/ApplyTemplate未実行によるPopup未確立）が正しいなら、`UpdateLayout()`の呼び出し対象が
一段浅い（`CustomPartsMenu`止まり）ため、`sub`自身のPopup確立までは効果が及んでいない可能性を
疑う余地がある（一次ソース未確認、忍者の推測に留まる）。

家老依頼どおり、これ以上忍者単独での試行は行わず、詳細な症状（上記）を記録して報告する。設計見直し
（動的メニュー構築を別方式へ変更）の検討に入る場合、判断材料として本記録を活用されたし。
