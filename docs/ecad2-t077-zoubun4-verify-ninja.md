# T-077增分4 実機検証（忍者）

**対象コミット**: `f961c06`（使い方ウィンドウの参照先を`docs/usage`平易版へ切替）
**検証日**: 2026-07-21
**手法**: `ecad2-ui-automation`スキル。使い方ウィンドウは`Ecad2Native`経由のPrintWindow撮影。
**参照**: 隠密静的レビュー`docs/ecad2-t077-zoubun4-review-onmitsu.md`（軽微な指摘2点、いずれも実害なし・修正不要）

## DoD別結果

### (1) F1キー/「ヘルプ」メニュー「使い方(_G)」から「使い方」ウィンドウが開くこと — OK

両方とも正しく開くことを確認。なお`Send-Ecad2Keys "{F1}"`はアプリ起動直後の1回目は反応しないことがあった（増分2検証時と同様の現象、1秒待って再送信すると正常に開いた）。実装側の問題ではなく起動直後のフォーカス確立タイミングの問題と見られる（T-105以前から時折見られる既知の傾向）。

### (2) 左目次の全11領域で`docs/usage`平易版の内容が表示されること — OK

全11領域を順にクリックし、いずれも開発者向け原文（クラス名・コード行番号・出典明記等）ではなく、ユーザー向けの平易な操作説明に置き換わっていることを確認：

- 「結線操作」（`ecad2-usage-wiring.md`）：クラス名・行番号等の記述は一切なく、「自由線・接続点・縦コネクタ・配線分断」の使い方が平易に説明されている（`ecad2-t077z4-topic3.png`）
- 他10領域も同様、`MainWindowViewModel.cs:xxx`のような実装詳細への言及は皆無

**DisplayName更新3件（隠密指摘(c)で確認済み）も実機で確認**：
- 「元に戻す・やり直し（Undo/Redo）」（旧「Undo/Redo」）
- 「設計チェック(DRC)・出力パネル・検索」（旧「設計チェック(DRC)・出力パネル」）
- 「ステータスバー・モード表示」（旧「ステータスバー・モード可視化」、隠密が申告漏れとして発見した1件）

いずれも目次表示・見出し文言とも正しく反映されている。

### (3) 各領域のコンテンツがMarkdownFlowDocumentConverterで意図通り変換されていること — OK

全11領域とも見出し・段落・箇条書き・番号付きリストの書式崩れなし。表構文（メニュー・ツールバー全体構成／結線操作／ステータスバー・モード表示等、複数領域に存在）も増分1・2で確認済みの既知制約どおりプレーンテキストとして視認可能、実害なし。

**文章内容（誤字等）の確認**：11領域を目視した限り、明らかな誤字・脱字・表現の誤りは見当たらなかった。

### (4) ライト/ダークモード両方で表示に異常がないこと — OK

ダークモードへ切替え、目次パネル・右コンテンツともに正しく暗色配色へ追従することを確認（`ecad2-t077z4-darkmode.png`）。表示崩れなし。背景色・文字色の理論値一致は増分1・2で画素採取済みのため、今回は目視確認に留めた。

### (5) 非モーダル動作・多重起動防止に回帰がないこと — OK

- 非モーダル：使い方ウィンドウを開いたまま、メインウィンドウのシート追加操作（ダイアログ表示→OK確定）が正常に機能することを確認。
- 多重起動防止：使い方ウィンドウを開いたままF1キーを再送信し、可視ウィンドウ数が増えず（2のまま）、`GetForegroundWindow()`が既存の使い方ウィンドウを指す（前面化）ことを確認。

## 総合結論

DoD(1)〜(5)全項目OK。範囲外の不具合検出なし、誤字等の指摘事項もなし。

## 現在のアプリ状態

検証完了、忍者にて`Stop-Ecad2App`で終了済み。
