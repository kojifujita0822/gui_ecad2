# T-077增分5 実機検証（忍者）

**対象コミット**: `725dbd8`（MarkdownFlowDocumentConverterに表構文対応を追加、無限ループバグ修正含む）
**検証日**: 2026-07-21
**手法**: `ecad2-ui-automation`スキル。`Ecad2Native`経由のPrintWindow撮影＋画素採取（背景色・文字色は色出現頻度集計方式、罫線は列間余白でのy方向走査）。
**参照**: 隠密静的レビュー`docs/ecad2-t077-zoubun5-review-onmitsu.md`（指摘なし、無限ループバグ修正の論理的妥当性も確認済み）

**殿指示によりアプリは実機確認後も起動したまま維持（`Stop-Ecad2App`未実行、殿ご自身で確認予定）。**

## DoD別結果

### (1) 殿指摘6領域で表が罫線付きテーブル形式で表示されること（列整列確認） — OK

対象6領域（menu-toolbar・pdf-testmode・placement・sheet-document・statusbar・wiring）を全て開き、いずれも生のMarkdown表記法（増分1〜4では`| 列1 | 列2 | ... |`のプレーンテキストのまま）ではなく、罫線付きのテーブルとして正しく描画されていることを確認した：

- 「メニュー・ツールバー全体構成」：4列（メニュー/項目/ショートカット/できること）、複数行にわたり列位置が揃っている（`ecad2-t077z5-topic1.png`）
- 「結線操作」：3列（操作/ショートカット/使えるシート）、5行のデータが罫線で綺麗に区切られている（`ecad2-t077z5-topic3.png`）
- 「配置操作」：2列（キー/配置される要素）、「ステータスバー・モード表示」：3列、「PDF出力・テストモード」：2列、いずれも同様に正しく整列（`ecad2-t077z5-topic2.png`・`topic8.png`・`topic10.png`）

ヘッダー行はグレー系背景＋区別された文字色で強調表示され、データ行との視覚的な区別も明瞭。列の整列に崩れは一切なし。

### (2) 表を含まない残り5領域に回帰がないこと — OK

undo-redo・canvas-display・part-management・device-table・drc-outputの5領域を確認、いずれも増分4検証時と同様に表示崩れなし（`ecad2-t077z5-topic4.png`・`topic5.png`・`topic6.png`・`topic7.png`・`topic9.png`）。

### (3) ライト/ダークモード両方で表の罫線・ヘッダー背景色が正しく配色されること — OK

画素採取（列間余白でのy方向走査により罫線位置を特定、ヘッダー文字は色出現頻度集計）で確認：

| モード | ヘッダー背景 実測/理論値 | ヘッダー文字 実測/理論値 | 罫線 実測/理論値 |
|---|---|---|---|
| ライト | `#F0F0F0` / `#F0F0F0`（`PanelHeaderBackgroundBrush`） | `#000000` / `#000000`（`PanelHeaderForegroundBrush`） | `#D2D2D2` / `#D2D2D2`（`PanelGridLineBrush`） |
| ダーク | `#3E3E42` / `#FF3E3E42` | `#F0F0F0` / `#FFF0F0F0` | `#565656` / `#FF565656` |

両モードとも全項目が理論値と完全一致。メインウィンドウの「ダークモード(作図色)」切替に使い方ウィンドウの表も即座に追従することを確認した。

罫線探索では、文字が描画されているy座標（ヘッダー行の中心等）で走査すると文字グリフのアンチエイリアシングに埋もれて罫線を検出できなかったため、列間の余白座標でy方向に走査する方式に切り替えて解消した（次回同種の検証における参考知見）。

### (4) 11領域を一通り開いてもフリーズ・無応答が発生しないこと（無限ループバグ再発なし） — OK

全11領域を順にクリックし、各選択操作から画面更新完了までの時間を計測。いずれも406〜436ms（`Start-Sleep 400ms`とほぼ同水準）で応答し、ハング・タイムアウトは一切発生しなかった。隠密が指摘した無限ループの根本原因（表構文の複合判定条件をすり抜けた行が段落結合ループで1文字も前進しない）に対する修正が実機でも有効に機能していることを確認した。

## 総合結論

DoD(1)〜(4)全項目OK。範囲外の不具合検出なし。

## 現在のアプリ状態

**殿の「stay」指示により、Ecad2.Appは起動したまま維持（未終了）。** 使い方ウィンドウはダークモード・「PDF出力・テストモード」領域選択中の状態で残置。
