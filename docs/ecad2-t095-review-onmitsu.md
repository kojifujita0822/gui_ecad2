# T-095（ツールバー1段目ラベル表示：ショートカット→機能名）静的レビュー（隠密）

レビュー日: 2026-07-15
対象コミット: d4d10e0
対象diff: `src/Ecad2.App/MainWindow.xaml`（1ファイル、19行追加/8行削除）
レビュー深度: 軽量既定（1周目、karo.md方針）
併用: code-reviewスキル（low effort）

## 総合判定

**設計・実装とも殿裁定どおり。指摘なし。**

## (a) 台帳DoDとの整合（`docs/todo.md` T-095節）

- 対象8ボタン（新規/開く/保存/戻す/やり直す/PDF出力/行を追加/行を削除）：実装で全て確認、一致。
- 「テスト」「一時停止」ボタンは既に機能名表示のため対象外：確認済み、変更なし（`MainWindow.xaml:249,260`）。
- 2段目（F5〜F10等）は対象外、現状維持：確認済み、`Band="1"`配下（264行以降）は全てショートカット表示のまま変更なし。
- 機能名表記（殿裁定=案2、簡潔化案）：コミットメッセージと実装一致。PDF出力のみ簡潔化せず据え置き（侍提案・家老裁量承認）も確認。
- ToolTip流用（既存"機能名 (ショートカット)"形式）：変更不要、確認済み（下記(c)参照）。

## (b) code-reviewスキル併用（low effort）

diffは8箇所いずれも`TextBlock.Text`属性値の変更のみ（XAML表示文言、ロジック無し）。正しさのバグ・重複・デッドコードは無し（(none)）。

## (c) 狙い撃ち観点

### 対象範囲の限定性（2段目・テスト/一時停止ボタンへの誤爆なきこと）
`MainWindow.xaml`全体を確認。1段目（`Band="0"`）の対象8ボタンのみが変更され、以下は変更なしを確認：
- 「テスト」（249行）・「一時停止」（260行）：`TestModeToolBarButtonStyle`使用、元々機能名表示のため対象外どおり無変更。
- 2段目（`Band="1"`、264行以降）：`PlacementToolBarKeyLabelStyle`使用、Esc/F5/sF5/F6/sF6/F7/F8/F9/sF9/F10/部品、いずれもショートカット表示のまま変更なし。

誤爆なし。

### AutomationProperties.Name等アクセシビリティ関連プロパティの整合
8ボタン全てにおいて、`ToolTip`・`AutomationProperties.Name`属性は変更されておらず、既存の"機能名 (ショートカット)"形式（例：`"新規作成 (Ctrl+N)"`、`"PDF出力 (Ctrl+P)"`）を維持していることを直読で確認した。変更されているのは表示用`TextBlock.Text`のみ（例：`"Ctrl+N"` → `"新規"`）。`ecad2-ui-automation`スキルはボタン操作時に`AutomationProperties.Name`（またはToolTip由来のName）を参照するため、既存の実機確認手順・自動化スクリプトへの影響はない。

## スコープ確認
diff対象は`MainWindow.xaml`1ファイルのみ、対象8ボタンのXAML変更に閉じている。侍自己申告と一致、範囲外変更なし。

## 総括
致命的指摘・cleanup指摘とも無し。1周目レビューとして完了扱いで問題ないと判断する。
