# T-100新規発見6 修正実機確認（忍者）

**対象コミット**: `9f26852`（ListBoxの配置バー表示中パネル白化を修正、方式1=ControlTemplate差し替え、殿裁定）
**検証日**: 2026-07-21
**手法**: `ecad2-ui-automation`スキル、PrintWindow方式スクリーンショット＋画素採取
**参照**: 隠密静的レビュー`docs/ecad2-t100-shinki6-fix-review-onmitsu.md`（指摘なし）、忍者前回再現確認`docs/ecad2-t100-shinki6-verify2-ninja.md`

## 結論：修正確認、DoD(1)〜(3)全OK。隠密申し送り事項も実害なし

## DoD別結果

### (1) ダークモードでインラインバー表示中、シートパネル・部品選択パネルが正しく`#2D2D30`に保たれること — OK

前回再現確認と同一シナリオ（a接点配置ツール選択→セルクリックでインラインバー表示）で画素採取：

| 座標 | 内容 | 修正前（前回） | 修正後（今回） | 期待値 |
|---|---|---|---|---|
| (100, 500) | シートパネル余白 | `#FFFFFF` | `#2D2D30` | `#2D2D30` |
| (1100, 595) | 部品選択パネル余白 | `#FFFFFF` | `#2D2D30` | `#2D2D30` |
| (1100, 300) | 機器表パネル余白（対照） | `#2D2D30` | `#2D2D30` | `#2D2D30` |

500ms間隔で3回連続撮影・画素採取を行い、いずれも一貫して`#2D2D30`を維持（`ecad2-t100z6fix-inlinebar-rep1.png`〜`rep3.png`）。前回確認した持続的な白化現象が解消されたことを実機で確認した。

### (2) OK確定後も正常動作に回帰がないこと — OK

デバイス名M1を入力しOKボタンで確定、要素が正しく配置され機器表にも「M1 リレー」と反映（`ecad2-t100z6fix-afterok.png`）。パネル色も引き続き正常。配置操作自体への回帰なし。

### (3) ライトモードでも配置バー表示中の見た目に異常がないこと — OK

ライトモードへ切替え、同一手順（a接点配置ツール選択→セルクリック）でインラインバー表示中を確認。目視で異常なし（`ecad2-t100z6fix-light-inlinebar.png`）、画素採取でもシートパネル・部品選択パネルとも期待値`#FFFFFF`と一致。

## 隠密申し送り事項の確認：新規BorderThickness/BorderBrushによる見た目変化 — 実害なし

通常表示時（配置バー非表示）のSheetNavList・PartSelectionListについて、境界付近を画素採取で確認：

- SheetNavList（ライトモード）：左端境界に1px幅の`#CCCEDB`（境界線色）を検出、内側は正常な白背景。過度な太さ・異質な色ではない。
- PartSelectionList（ダークモード、ツール選択直後）：左端境界に1px幅の`#3F3F46`（`PanelGridLineBrush`相当）を検出、内側は正常な`#2D2D30`背景。

いずれも1px程度の細い境界線のみで、隠密の想定どおり「意図的なテーマ色を使った既存の暗黙的な値の明示化」であり視覚的な実害は認められない。

## 現在のアプリ状態

検証完了、忍者にて`Stop-Ecad2App`で終了済み。
