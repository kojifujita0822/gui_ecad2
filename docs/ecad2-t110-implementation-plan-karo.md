# T-110 4分割DockingManagerの単一統合 実装プラン（家老）

作成日: 2026-07-21　作成者: 家老　委任元: 殿裁定2026-07-21「隠密プランが出たら実装プランを作成せよ、
但し家老プランは実装前に隠密へ再検証させてから実装に進めよ」

**土台**: `docs/ecad2-t110-single-dockingmanager-unification-plan-onmitsu.md`（隠密プラン、依頼内容
1〜6全対応、殿裁定事項=裁1〜裁6を提示済み）。本書は隠密プランの技術的結論をそのまま採用し、
**着手順序・殿裁定ゲートの置き所・各増分の采配文面**を家老の責務として具体化するもの。
隠密プラン本文の技術的検討（統合方式・影響範囲・高リスク評価）を再論しない。

本書自体、実装着手前に**隠密へ再検証を依頼**する（§5）。

---

## 1. 着手ゲート方針 → 【裁1〜裁6、殿裁可済み（2026-07-21）】

**殿裁可（2026-07-21）**: 裁1〜裁5=隠密推奨どおり採用、裁6=許容（アクティブ色表示喪失を許容し、
代替の視覚表現は設けない）。確定内容:

| # | 論点 | 確定内容 |
|---|---|---|
| 裁1 | 統合トポロジ | 案1（キャンバス=LayoutDocumentPane） |
| 裁2 | ドキュメントタブ表示 | 非表示化 |
| 裁3 | 旧保存レイアウト | 既定へフォールバック（移行ロジックは作らない） |
| 裁4 | フロート化可否 | `CanFloat="False"`で封じる（配置ツールバーのみフロート可を維持） |
| 裁5 | タイトルバー非表示方式 | 案A（完全非表示） |
| 裁6 | アクティブ表示喪失の許容 | 許容（代替表現は設けない） |

これにより「増分1着手前に殿裁定を仰ぐ」ゲートは**充足済み**。以後は追加の殿裁定を待たず、
増分0（PoC）→増分1（骨格統合）→増分2（回帰総点検）→増分3（タイトルバー非表示）の順で
検証パイプラインを回しつつ進めてよい。

**留保1点（2026-07-22解消）**: 裁5の付帯裁定「AutoHide（ピン留め）機能を残すか」は隠密プランでも
明確な推奨が無く（`docs/ecad2-t110-single-dockingmanager-unification-plan-onmitsu.md`§6.4）、
当初の裁可には含まれていなかった。増分0・増分1完了後、家老から殿へ確認し、**「残す（代替UIを
新設）」と裁定済み**（`docs/todo.md` T-110節参照）。増分3ではタイトルバー自体は案A（完全非表示）
のまま、AutoHide機能はメニュー等への代替UI新設で維持する方向で設計する。

- **増分0（PoC）は`poc/`配下の技術検証**。「案1トポロジ・標準Dock()の挙動・ContentIdによる
  スタイル分岐」等の**技術的成立可否**を確かめる。裁1〜裁6は既に確定したため、PoC(f)
  （ドキュメントタブの見え方）・PoC(h)（タイトルバー非表示デモ）はいずれも「判断材料集め」
  ではなく「確定方式（裁2=非表示化／裁5=案A）の実装可否の技術検証」という位置づけに変わる
  （(f)は「非表示化スタイル＝DocumentPaneControlStyleコピー+タブ領域Collapse、隠密プラン§2.4
  の実装可否確認」として実施する）。
- 増分3（タイトルバー非表示、依頼内容6）は増分1・2完了後の独立増分。統合自体の検証と混ぜない
  （隠密プラン§5方針を踏襲）。裁5・裁6が確定済みのため増分1・2は待たず設計を進めてよいが、
  **AutoHide要否（§1留保1点）は増分3の実装内容そのもの（代替UI設置か`CanAutoHide="False"`
  封止か、隠密プラン§6.4付帯裁定）を左右するため、増分3の着手前には殿確認の完了が必要**
  （増分1のブロッカーではないが、増分3のブロッカーではある）。

**殿ご帰還後、家老が提示すべき事後報告材料一式**（裁定は既に得たため「材料」ではなく「PoC完了
報告」の位置づけ）：PoC(a)〜(h)＋AutoHide挙動＋PaneControlスタイル全ペイン適用の結果一式
（特に(c)候補a再現有無・(d)アクティブ色一元化実証・(f)タブ見え方／(h)タイトルバー非表示の見た目
スクリーンショット）。増分1着手可否の最終確認と、留保中のAutoHide要否（§1留保1点）の判断材料
を兼ねる。

---

## 2. 増分計画（隠密プランの技術内容を踏襲、采配単位に整理）

### 増分0: PoC（`poc/t110-single-dockingmanager-poc/`、今すぐ着手可）

- **内容**: 隠密プラン§5 (a)〜(g)全項目＋隠密再検証（`docs/ecad2-t110-karo-plan-review-onmitsu.md`）
  で追加された以下3件:
  - (h) タイトルバー非表示の見た目デモ（LayoutAnchorableControlスタイルコピー+Header Collapse、
    隠密プラン§6.2の手法。裁5・裁6の判断材料をスクリーンショット付きで揃える目的、提案1採用）
  - AutoHide（ピン留め）操作の挙動確認（統合Managerのサイド領域がウィンドウ全域になる挙動の
    観察、裁4・裁5付帯裁定の判断材料、漏れ1）
  - `PlacementToolBarPaneControlStyle`全ペイン適用時の副作用確認（増分1の設計前提の成立可否を
    前倒しで確認、漏れ2）

  既存`poc/t058-avalondock-poc/`（T058AvalonDockPoc、元の4分割導入時のPoC）を構成の参考に、
  単一DockingManager+5領域（ダミー中身）の最小WPFアプリを新規作成する。
- **DoD**:
  1. (a)〜(h)全項目＋AutoHide挙動＋PaneControlスタイル全ペイン適用の実証結果（成立/不成立、
     実測値、スクリーンショット）を隠密がまとめる
  2. 特に(c)「標準`Dock()`でタブ自己複製バグが再現するか」は増分1の設計分岐点のため、
     結論（再現する/しない）を明記する。**再現検証は前提条件を本実装と忠実に一致させること**
     （2タブ同居・CanFloat/CanClose設定・ContentDocking系ハンドラ無し、詳細は§4采配文参照）
  3. (d)アクティブ色一元化は**ペインの中身をクリックして青帯が移ること**で確認する
     （タイトルバークリックのみでは§7-4「中身クリックでのアクティブ化」が検証されない）
  4. (e)タイトルスタイルのContentId分岐はVs2013Light/Dark**両テーマ**で確認する
  5. 裁1〜裁6の判断材料（該当するもの）を家老が殿へ提示できる形に整理する
- **検証観点**: 本実装非接触（`poc/`配下に閉じる）ことをまず確認。PoCゆえ隠密の静的レビューは
  軽量（コード品質より「実証すべき論点を正しく検証できているか」の観点を主とする）。
- **スコープ境界**: PoCは実証のみ、本実装（`src/Ecad2.App/`）には一切触れない。
- **担当采配**: 侍（PoC実装）→隠密（軽量レビュー＋結果とりまとめ）→忍者（表示・操作感の実機確認、
  セカンドモニタ徹底）。

### 増分1: 骨格統合（本実装、裁1〜裁6は§1のとおり裁可済み・着手可）

- **内容**: 隠密プラン§2.2案1トポロジへの統合（4Manager撤去→単一Manager+LayoutDocumentPane）、
  永続化単一ファイル化、`ApplyDockingManagerThemes`単一化+統合タイトルスタイル、T-103再配線、
  T-104タブナビ再確認、既存テスト追随修正。**增分0のPoC・忍者実機確認で以下が確定済み**:
  1. **T-099(c)案Y代替＝候補a確定**（標準`Dock()`をそのまま使用）。增分0のPoC(c)で
     Float→Dock往復をボタン経由3周＋MenuDropDownButton経由2周、計5周実施しタブ自己複製・
     縦長化・空白化いずれも再現しなかった（`docs/ecad2-t110-poc-verification-ninja.md`(c)）。
     統合トポロジではPreviousContainer解決の文脈が変わりバグが再現しないという隠密プラン§3.3の
     見立てどおり。**`ContentDocking`ハンドラの`e.Cancel`自体を撤去してよい**（候補b/cは不要）。
  2. **裁2（ドキュメントタブ非表示）は`ShowHeader="False"`属性方式を採用**（增分0静的レビューで
     判明、`docs/ecad2-t110-poc-review-onmitsu.md`§3——テンプレートコピー約60行が不要、ライブラリ
     正規機構ゆえ将来のAvalonDock更新にも強い）。**残課題**＝`DockAsDocument`経路で生成される
     新規ペインの`ShowHeader`封止要否（未検証、一次ソース確認要）。
  3. **統合PaneControlスタイルに`Items.Count==1`時のTabItem Collapseトリガーを追加必須**
     （增分0修2で判明した設計ギャップ、本実装の`PlacementToolBarPaneControlStyle`自体にも
     元々無い。一次ソースGeneric.xaml:536-540どおりStyle.Triggersへ追加）。
  4. ダークモード切替時は`Application.Current.Resources.MergedDictionaries`側のTheme.Light/Dark
     差し替えも本実装の`ApplyUiChromeTheme`（既存、`MainWindow.xaml.cs:796-807`）でカバー済みの
     はずだが、統合後のManager一元化と整合しているか回帰確認に含める（增分0修1と同型の見落とし
     防止）。
- **申し送り事項（增分1設計・レビュー時に確認）**: (a) `DockAsDocument`経路の新規ペイン
  `ShowHeader`封止要否 (b) `SelectedContentIndex="1"`が実機で反映されない事象の要否確認
  （軽微） (c) DataGridColumnHeader単体クリックで非アクティブなまま（軽微、実害なしと見られる）
  (d) 配置ツールバーが単独ドッキングペインになる場面が本実装で実在するか（ContentId分岐の
  ラベル非表示トリガーが実際に発火する条件の特定、`docs/ecad2-t110-poc-review-onmitsu.md`
  実機確認後の追補§追2参照）。
- **DoD**: 隠密プラン§5増分1の忍者確認項目全て（起動・全ペイン表示・リサイズ・保存/復元・
  Ctrl+Alt+R・ダークモード切替・Float/Dock往復・Tab巡回・ElementPlacementBar位置）が正常。
  加えて增分0(f)相当（ドキュメントタブ非表示時にキャンバス内容が正しく表示されること）を
  ShowHeader方式で回帰確認。
- **検証観点**: 高リスク領域（`AnchorablePaneControlStyle`系）ゆえ、侍実装→隠密静的レビュー
  （フル観点、code-reviewスキルは恒久無効のため手動レビュー代替。PR-21パターン=Style本体の
  全Setter確認を必ず含める）→忍者実機確認、**省略厳禁**。モグラ叩き検知（2周）を適用。
- **スコープ境界**: 依頼内容(6)（タイトルバー非表示）は含まない（増分3で独立実施）。

### 増分2: 回帰総点検

- **内容**: T-100/T-101/T-103/T-104/T-106/T-107/T-108の回帰を忍者が一括実機確認。
- **検証観点**: 色・配色系は目視でなく画素採取で判定【MUST、既存原則】。
- **スコープ境界**: 新規不具合が見つかった場合は「範囲内欠陥」として侍へ差し戻し、往復2周まで
  は家老裁量、超えたら殿へ。

### 増分3: 単一ペインタイトルバー非表示（依頼内容6、裁5・裁6確定を踏まえ実装）

- **内容**: 裁5確定=案A（完全非表示）＋代替UI設計（AutoHide要否の殿確認結果を反映、上記§1参照）
  を実装。裁6=許容のため、アクティブ表示の代替視覚表現は設けない。
- **検証観点**: T-099の罠（`wpf_collapse_visibility_hides_interaction`）と同型の事故を防ぐため、
  隠密プラン§6.2の「テーマ公認パターン（Header Border層でのCollapse）」を踏襲しているか、
  レビューで必ず確認する。
- **スコープ境界**: 統合自体の検証（増分1・2）と混ぜない独立増分。

---

## 3. リスク管理（全増分共通）

- **モグラ叩き検知【MUST】**: 同一介入箇所への対症療法的追加修正が2周続いたら、3周目に入る前に
  介入方式自体の妥当性を俯瞰評価する（`karo.md`既存原則、T-099(c)の教訓）。
- **検証パイプライン省略厳禁【MUST】**: 「変更が小さいから」を理由に隠密レビューを飛ばさない
  （2026-07-19殿明示訂正済み）。規模の大きいT-110こそ徹底する。
- **rm禁止・削除不要**: 裁3（旧保存レイアウト4ファイル）はフォールバックで無害、削除しない。

---

## 4. 侍への増分0采配文面（下書き、隠密検証OK後に正式送付）

```
【家老より侍へ】T-110 増分0（PoC）実装依頼
- 対象: poc/t110-single-dockingmanager-poc/ 新規作成（本実装非接触、src/Ecad2.App/には触れぬこと）
- 参照: docs/ecad2-t110-single-dockingmanager-unification-plan-onmitsu.md §5（増分0の(a)〜(g)）
  既存poc/t058-avalondock-poc/（T058AvalonDockPoc）を構成の参考にされたし
- 【必須】パッケージ構成: t058 PoCは`Dirkster.AvalonDock.Themes.VS2013`パッケージを含まぬ。
  本PoCの(d)アクティブ色・(e)スタイル分岐はVS2013テーマ必須のため、`Dirkster.AvalonDock`・
  `Dirkster.AvalonDock.Themes.VS2013`を両パッケージとも4.74.1で揃え、`manager.Theme`へ
  `Vs2013LightTheme`/`Vs2013DarkTheme`を設定すること（隠密検証済み、t058 PoCとの差分点）
- 【必須】(c)再現条件: タブ自己複製バグの再現検証は前提条件を本実装と忠実に一致させること。
  配置ツールバー相当ペインは2タブ同居（MainToolBar相当=CanFloat="False"・PlacementToolBar
  相当=フロート可）・両タブCanClose="False"・SelectedContentIndex="1"・**ContentDocking系
  ハンドラは付けない**（標準動作のままでバグ再現有無を観ること、簡略化すると偽陰性の恐れあり）
- 追加検証: (h)タイトルバー非表示の見た目デモ（LayoutAnchorableControlスタイルコピー+Header
  Collapse、隠密プラン§6.2手法）／AutoHide操作の挙動／PlacementToolBarPaneControlStyle
  全ペイン適用時の副作用、の3件も併せて実証すること
- DoD: (a)〜(h)全項目＋AutoHide挙動＋PaneControlスタイル全ペイン適用の実証結果を隠密へ引き継げる
  状態にすること。(d)はペイン中身クリックでの色移動を確認、(e)はLight/Dark両テーマで確認すること
- 検証観点: 本実装に一切影響を与えないこと（poc/配下に閉じる）
- スコープ境界: 案1トポロジ以外（案2）の実装は不要、PoCは案1のみ検証すればよい
```

---

## 5. 隠密への再検証依頼事項（本プランの妥当性チェック）

1. 「増分0はUI/UX最終確定を含まないため殿裁定不要」という家老の切り分けに無理がないか
   （PoCの内容自体が実質的にUI/UX判断を先取りしてしまっていないか）
2. 増分1着手前ゲートに裁1〜裁6全てを含めた判断（裁5・裁6を増分3でなく増分1前に前倒し）の
   整合性（隠密プラン§6.5根拠との一致確認）
3. 各増分のDoD・検証観点に、隠密プラン§4.4（高リスク領域検証必須項目5件）・§7（不明点6件）
   からの漏れがないか
4. §4の侍采配文面（下書き）に技術的な誤り・スコープ逸脱がないか
