# T-118 実機確認（忍者、2026-07-22）

## 対象
`docs/todo.md` T-118（シート改名時の選択色消失を根本対処、P-109対処・案A）。隠密静的レビュー
完了（問題なし）を受け、家老采配。以前P-109追加検証で特定した「改名対象がリスト末尾でない場合に
選択色消失」という条件の裏取り。

## 操作手法
UI Automation経由の合成操作（`Invoke-Ecad2Button`/`InvokePattern.Invoke()`・
`SelectionItemPattern.Select()`）。殿ご自身の実操作は未実施。

## 検証内容・結果

シートを3枚作成し、末尾/非末尾を含む全位置で改名を実施：

| 改名対象 | 位置 | 改名後IsSelected（全項目） | 判定 |
|---|---|---|---|
| 1番目 | 非末尾 | `True,False,False` | OK（対象のみ選択維持） |
| 2番目 | 非末尾 | `False,True,False` | OK（対象のみ選択維持） |
| 3番目 | 末尾 | `False,False,True` | OK（対象のみ選択維持） |

いずれも改名対象の項目が正しく`IsSelected=True`のまま維持され、選択色の消失は発生しなかった。
3番目（末尾）改名後のスクリーンショットでも「Third-T118」が青くハイライトされたままであることを
目視確認。

修正前（`RemoveAt`+`Insert`+`BeginInvoke(ContextIdle)`方式）ではP-109追加検証（
`docs/ecad2-p109-additional-verify-ninja.md`）にて非末尾（1番目・2番目相当）で選択消失が
複数回・複数構成で再現していたが、今回の根本対処後は同条件で消失せず——**判定OK**、案A
（軽量ViewModelラッパー方式への置換）が有効に機能していることを確認。

## 総括

| 観点 | 結果 |
|---|---|
| 非末尾シートの改名 | OK（2件とも選択色維持） |
| 末尾シートの改名 | OK（選択色維持） |

回帰の有無：新規回帰なし。DoD項目3（AddCommand/DeleteCommand等の既存コマンドへの影響）は
本検証の観点外だが、シート追加（3枚作成）は正常に動作しており、少なくとも操作の続行に支障は
確認されなかった。

## 付記
殿より「stay」の御下命があり、本検証完了時点でEcad2.Appは終了せず起動したまま維持している
（殿ご自身の確認のため）。
