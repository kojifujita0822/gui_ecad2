# T-119 実機確認（忍者、2026-07-22）

## 対象
`docs/todo.md` T-119（配置ツールバーのタブ形状をAeroテーマ風に変更、往復3周目対処後）。隠密静的
レビュー完了（問題なし）を受け、家老依頼の3観点——(1)上下反転 (2)文字消失解消 (3)強調色——を
実機確認。

## 操作手法
UI Automation経由の合成マウスクリック（`Invoke-Ecad2ScreenClick`）。タブ要素は`SelectionItemPattern`
非対応（`SynchronizedInputPatternIdentifiers`のみ）のため合成クリックで操作。画素採取は範囲サンプ
リング（支配色判定）を基本とし、単発ピクセルは境界・文字にかかりやすいため避けた。殿ご自身の
実操作は未実施。

## (1) 上下反転：OK

タブ部分を4〜5倍拡大したスクリーンショットで視認。両タブとも「曲線がタブ上辺（コンテンツ＝
ツールバー本体との接続部）側で水平に近く、外周（左下）側に丸みが集中する」形状——DoDの期待
形状と一致。ライト/ダーク両テーマで確認。

## (2) 文字消失解消：OK

「配置ツール」タブ選択時も文字「配置ツール」は明瞭に表示。ダーク/ライト、初期状態/タブ切替後の
いずれの条件でも文字消失は再現せず。

## (3) 強調色：【重要な新規所見】初期選択状態でのみ強調表示が反映されない

### 発見の経緯
実装コード（`Theme.Dark.xaml`）を確認したところ、選択中背景は`#FF232326`→`#FF17171A`
（非選択`#FF454549`→`#FF343438`より明確に暗い）と定義されている。しかし、**シート追加直後
（配置ツールタブが起動時からの初期選択状態のまま、タブ操作を一切行っていない状態）**で画素採取
したところ、選択中タブの背景色が非選択の値（`#41-42`系）とほぼ同じで、想定される暗い色
（`#23`系）になっていなかった。

### 対照実験で確定
「基本機能」タブへ一旦クリックし、その後改めて「配置ツール」タブへクリックし直したところ、
**背景色が明確に暗く変化し、外周に青いアクセント枠線（`PlacementTabSelectedBorderBrush`）も
表示された**。拡大スクリーンショットで視覚的に明確な違いを確認（初期状態と再クリック後で
別物のように見える）。

| 状態 | ダークモード選択中タブ背景 |
|---|---|
| 初期状態（タブ操作なし） | 非選択とほぼ同じ薄い色（想定`#23`系に対し実測`#41-42`系） |
| 明示的にクリックし直した後 | 想定通り暗い色＋青枠線を確認 |

同じ現象をライトモードでも確認（初期状態では強調表示なし）。ライトモードのクリック後の反映
有無は、非選択との色差自体が小さい設計（`#D8D8D8`系 vs `#FAFAFA`系）のため画素採取だけでは
明確に判定しきれず、追加確認が必要（枠線色`#0050A6`系のピクセルは検出したが確度は中程度）。

### 所見（推測、断定しない）
T-119往復3周目コメント（`MainWindow.xaml`200-207行）に記載された「Template切り替え直後に
既にIsSelected=Trueな場合のVisualTree名前解決タイミング問題」——文字消失の原因として特定・
対処済みの症状——と類似の根本原因を、**背景色・枠線のControlTemplate.Triggerが持っている
可能性がある**。文字消失自体はTemplateBinding方式への統一で解消されたが、同種のタイミング
問題が背景色/枠線トリガーには残っているのではないか、というのが忍者の所見（一次ソース
未確認、侍・隠密の判断を仰ぐ）。

## 総括

| 観点 | 判定 | 備考 |
|---|---|---|
| (1) 上下反転 | OK | 拡大画像で視認確認、両テーマ |
| (2) 文字消失解消 | OK | 初期状態・クリック後とも文字表示正常 |
| (3) 強調色 | **NG（条件付き）** | 初期選択状態では強調表示が反映されない。タブ切替操作を経由すると正常化 |

**再現手順**：シート追加直後（配置ツールタブが自動選択された状態のまま）でスクリーンショットを
撮ると、選択中タブの強調表示（背景色・枠線）が反映されていないことを確認できる。基本機能タブ→
配置ツールタブの順にクリックし直すと直る。

殿がこれまで実機確認された際、既にタブ操作（基本機能↔配置ツール切替）を経由していた可能性が
高く、その場合は正しい強調表示が見えていたと考えられる——今回の所見は「アプリ起動直後の
初見状態」という特定条件でのみ顕在化する可能性がある。

---

## 往復4周目（2026-07-24、殿指摘＝見本比較用スクリーンショット撮影）

### 依頼内容
殿より「タブのデザインが見本（AvalonDock Aeroテーマ画面例）と違いすぎる、特に曲線の形・大きさが
気になる」とのご指摘。家老依頼により、配置ツールバータブ（`PlacementToolBarTabItemTemplate`）を
ライト/ダーク×選択中/非選択の4パターンで拡大撮影。

### 操作手法
- テーマ切替：メニュー「表示(V)」→「ダークモード(作図色)(D)」を`ExpandCollapsePattern.Expand()`→
  `InvokePattern.Invoke()`（MenuItem）で操作。フォーカス非占有。
- タブ選択：**当初`SelectionItemPattern.Select()`を試みたところ、タブの見た目・ツールバー内容が
  一切変化しないまま、配置ツールバーペイン上部に無関係な青い帯（幅一杯の`AnchorablePaneTitle`
  相当）が出現するという想定外の副作用が発生**。ninja.md既知の罠「UI Automation経由のSelect/
  Invokeが内部状態を不安定化させる」に該当する疑いが濃厚と判断し、深追いせず即座に合成マウス
  クリック（`Invoke-Ecad2CanvasClick`、Win32合成イベント、殿の実操作とは別物）に切替。切替後は
  ツールバー内容・タイトルバー表示（後述）とも正しく連動し、正常に撮影できた。
- スクリーンショット：`Save-Ecad2Screenshot`（PrintWindow方式）。撮影後に該当領域を
  `System.Drawing`でクロップ→8倍拡大（NearestNeighbor、ピクセルそのまま拡大）。

### 撮影結果（画像パス）
| パターン | パス |
|---|---|
| ライト×選択中（基本機能） | `%TEMP%\claude\C--ECAD2\8fdee7e8-a0c1-4867-b90f-864c7faeaa5c\scratchpad\t119\light_selected_kihon.png` |
| ライト×非選択（配置ツール） | `...\t119\light_unselected_haichi.png` |
| ダーク×選択中（配置ツール、物理相当クリックで選択確定済み） | `...\t119\dark_selected_haichi.png` |
| ダーク×非選択（基本機能） | `...\t119\dark_unselected_kihon.png` |
| 参考：両タブ並び（ライト） | `...\t119\tabs_light_both.png` |
| 参考：両タブ並び（ダーク） | `...\t119\tabs_dark_both.png` |

### 所見1：曲線が左肩のみで非対称（範囲内・依頼の核心）
4パターンいずれも、**丸い切込み状の曲線装飾はタブ左肩にのみ存在し、右肩は直角のまま**という
非対称な形状だった（「基本機能」タブの右側に見える曲線は、隣接する「配置ツール」タブ自身の
左肩装飾であり、「配置ツール」タブ単体を切り出すと右肩に装飾が無いことを確認）。一般的な
AvalonDock Aeroテーマの見本（山型・台形状に左右対称で傾斜/湾曲する形状）とは構造的に異なる
可能性が高い——殿ご指摘の「曲線の形・大きさが見本と違いすぎる」という所感と符合する所見。
実装コードの構造的な原因（`SplineBorder`が左肩のみを描画する設計か等）は忍者の調査範囲外の
ため、侍・隠密の判断を仰ぐ。

### 所見2：ダークモードは選択中/非選択の背景色差が視認困難（既存所見(3)との関連）
ダーク×選択中（配置ツール、タブ切替を経由して選択確定済み）を撮影したところ、隣接する
非選択（基本機能）との背景色差が拡大画像でもほとんど視認できなかった。今回はタブ切替操作を
経由済み（往復3周目所見(3)の「初期選択状態限定」条件には該当しない）にもかかわらず差が
薄いため、往復3周目で確認した強調色条件付きNGとは別に、**タブ切替後であっても選択中/非選択の
視覚的コントラスト自体が小さい**可能性がある。範囲外の気づきとして家老へ報告のみ行う。

### 総括
撮影完了、家老へ画像パスと所見を報告済み。判定（曲線形状が見本と乖離しているか、対処要否）は
殿・家老の判断に委ねる。

---

## 往復5周目（2026-07-24、`SplineBorder.cs`対称S字曲線修正後の再確認）

### 依頼内容
侍が`SplineBorder.cs`のみ変更（対称なS字曲線、両端水平接線）。隠密静的レビュー完了（指摘なし）。
確認観点＝(1)左肩の曲線が「外側へ丸く膨らむ」自然な丸みに見えるか(内側へえぐれる欠けは解消
されたか) (2)前回と同じ構図で4パターン再撮影し見本(`ss_20260722_172901.png`)と比較。

### ビルド確認
DLLタイムスタンプ(10:27:42)がソース更新(10:21:46)より新しいこと、起動プロセスの`StartTime`
(10:27:57)がDLL更新より新しいことを確認——最新コードが実行されていることを確定させた上で検証。

### 撮影結果（画像パス、前回と同一構図・同一クロップ座標）
| パターン | パス |
|---|---|
| ライト×選択中（基本機能） | `...\t119\v2_light_selected_kihon.png` |
| ライト×非選択（配置ツール） | `...\t119\v2_light_unselected_haichi.png` |
| ダーク×選択中（配置ツール、物理相当クリックで選択確定済み） | `...\t119\v2_dark_selected_haichi.png` |
| ダーク×非選択（基本機能） | `...\t119\v2_dark_unselected_kihon.png` |
| 前回(往復4周目)分（比較用） | `...\t119\tabs_light_both.png` / `tabs_dark_both.png` 等 |

### 所見1：内側へえぐれる非対称は解消。ただし「丸み」は肉眼でほぼ識別不能【重要】
目視・8倍拡大では、曲線が完全な直線（斜めカット）にしか見えず、丸みが全く感じられなかった。
実装ミス（意図した曲線が反映されていない）を疑い、以下の手順で定量検証した：

1. **ビルド反映確認**：上記の通りDLL/プロセス時刻を突合、最新コードでの実行を確認。
2. **ピクセル境界トレース**：`v2_full_light.png`から基本機能タブのSplineBorder範囲
   （ローカル座標`w=20,h=22`相当）の境界線ピクセル位置を1行ずつ実測。
3. **理論値との突合**：`SplineBorder.cs`の3次ベジェ式（`P0=(w,h)`, `P1=(0.6w,h)`,
   `P2=(0.4w,0)`, `P3=(0,0)`）から同一y座標における理論x値を算出。

   | y(ローカル) | 実測x | 理論x（3次ベジェ補間） | 差 |
   |---|---|---|---|
   | 1 | 3 | 約2.77 | 0.23px |
   | 18 | 14 | 約14.26 | 0.26px |

   実測は理論値と誤差0.3px未満で一致——**実装は`SplineBorder.cs`のコード通り正しく描画されている**
   （ビルド反映漏れでも計算式バグでもない）。
4. **直線との比較**：同じ実測点を対角線（単純な直線）の理論値と比較すると、中間点付近で
   最大2.5px前後（コントロール高さ22pxの約11%）の乖離があり、幾何学的には確かに「曲線」だが、
   **コントロールサイズが20×22pxと小さいため、この程度の曲率は肉眼では直線とほぼ区別がつかない**
   という結論に至った。

### 所見2：見本とのサイズ感の違い（範囲外だが関連の深い気づき）
見本画像（`ss_20260722_172901.png`）のタブは、コントロール自体が今回よりも大きく描画されており
（画面全体に対するタブの相対サイズが大きい）、傾斜角度も緩やかで、曲線であることが一目で分かる
「山型」に見える。今回の実装は20px幅という小さい領域に同型の数式を適用しているため、**曲率の
絶対値（ピクセル単位のズレ量）が理論上正しくても、視覚的な「丸み」の印象は再現できていない**
可能性が高い。対処するなら曲線の数式パラメータ自体よりも、曲率の強さ（制御点比率や中間膨らみ量）
を今回のタブサイズ向けに調整する方向の検討が要るのではないか、というのが忍者の所見（一次ソース
未確認、侍・隠密の判断を仰ぐ）。

### 所見3：ダークモードの強調色、前回同様に視認困難（継続）
往復4周目所見2と同一の状況が今回も継続。選択中(配置ツール)と非選択(基本機能)の背景色差が
拡大画像でもほとんど見えない。

### 総括
| 観点 | 判定 | 備考 |
|---|---|---|
| (1) 内側へえぐれる非対称の解消 | OK | 実装は理論通り正しく対称曲線を描画 |
| (1) 「外側へ丸く膨らむ」丸みの視認性 | **NG（定量的に確認）** | 曲率は存在するが小さすぎ肉眼では直線に見える |
| (2) 見本との比較 | 未解消 | 見本のような明確な山型の丸みには程遠い |

判定・対処要否は殿・家老の判断に委ねる。

---

## 往復7周目（2026-07-24、縁線消失の修正確認）

### 依頼内容
侍が縁線消失の修正完了（`SplineBd`のThicknessバインド先を`BorderThickness.Left`→`Bottom`へ
変更）。隠密静的レビュー完了・問題なし。確認観点＝(1)縁線が視認できるか(前回殿ご指摘「輪郭判別
不能」の解消確認)、ライト/ダーク両テーマ (2)継ぎ目の自然さ(前回保留分) (3)丸みの視認性に回帰が
ないこと。

### 検証手順
アプリ再起動（新ビルド反映）後、前回同様の構図・座標で4パターン（ライト/ダーク×選択中/非選択）
を再撮影。

### 所見1：縁線の視認性＝OK、殿ご指摘は解消
ライト/ダーク・選択中/非選択いずれも、曲線に沿った明確な縁取り線を視認できた。前回(往復6周目)は
ダークモードで輪郭がほぼ判別不能だったが、今回は基本機能・配置ツールタブとも縁線がはっきり
見える。

### 所見2：継ぎ目の自然さ＝OK
曲線の下端(右下)から直線Border(四角本体)への接続部に段差・ギャップは見当たらず、自然に
つながって見える。

### 所見3：丸みの視認性の回帰＝なし
往復6周目で確認した丸み（一目で丸みとわかる水準）と同等の見た目を維持しており、劣化は無い。

### 総括
| 観点 | 判定 | 備考 |
|---|---|---|
| (1) 縁線の視認性 | OK | ライト/ダーク両テーマで解消確認 |
| (2) 継ぎ目の自然さ | OK | 段差・ギャップなし |
| (3) 丸みの視認性 | OK（回帰なし） | 往復6周目と同水準を維持 |

3観点とも解消・改善を確認。画像パス：`v4_light_selected_kihon.png` / `v4_light_unselected_haichi.png`
/ `v4_dark_selected_haichi.png` / `v4_dark_unselected_kihon.png`（いずれも
`%TEMP%\claude\C--ECAD2\8fdee7e8-a0c1-4867-b90f-864c7faeaa5c\scratchpad\t119\`配下）。

---

## 往復6周目（2026-07-24、曲率強化・BorderThickness辺別指定後の再確認）

### 依頼内容
侍が`SplineBorder.cs`の制御点比率を0.3w/0.7wへ拡大（曲率強化）、`MainWindow.xaml`で
`BorderThickness`を辺別指定（左辺・上辺を0、継ぎ目/コンテンツ接続部のため）。隠密静的レビュー
完了（指摘なし）、いずれも未コミット。確認観点＝(1)丸みが見本同様「一目でわかる」程度に見えるか
(2)右側四角枠と曲線の継ぎ目が自然につながって見えるか。

### 所見1：丸みの視認性は大幅改善＝OK
前回(往復5周目、制御点比率0.6w/0.4w)は肉眼でほぼ直線だったが、今回(0.3w/0.7w)は
ライト/ダーク・選択中/非選択いずれも明確に弧を描く丸みとして視認できた。見本画像と比べても
「一目で丸みとわかる」水準に到達したと判断する。

### 所見2：【新規発見・殿ご指摘】曲線の縁取り線(Stroke)が消失、ダークモードで特に視認困難
撮影・報告作業中に**殿より「曲線にアウトラインがないので視認が難しい」とのご指摘**があり、
直ちにコード確認へ切り替えた。

**原因特定**：`SplineBorder`のStroke太さは`Thickness="{Binding BorderThickness.Left,
RelativeSource={RelativeSource TemplatedParent}}"`（`MainWindow.xaml`212行）で
`TabItem.BorderThickness`の**左辺**の値をそのまま使う設計。今回の辺別指定変更で、非選択
`BorderThickness="0,0,1,1"`・選択中`BorderThickness="0,0,2,2"`と、**両方とも左辺(Left)が0**に
なったため、`SplineBorder`のThicknessが常に0になり、曲線の縁線(`borderGeometry`のStroke)が
完全に描画されなくなっていた。ライトモードでは背景が明るいためFill色のグラデーション
（影のような表現）だけでもうっすら輪郭が判別できるが、**ダークモードは背景もFill色も両方暗く、
境界がほとんど判別不能**——殿のご指摘と完全に一致する。

「継ぎ目解消」（左辺・上辺のBorder枠線を消し、`SplineBd`との段差を無くす）を狙った変更だが、
`SplineBorder`自身のStroke太さも同じ`BorderThickness.Left`に依存する設計だったため、**意図しない
副作用として曲線自体の縁取りごと消えてしまった**、というのが技術的な原因と推測する（一次ソース
未確認、侍・家老の判断を仰ぐ）。対処するなら、`SplineBorder.Thickness`のバインド元を左辺以外
（例えば下辺・右辺、または固定値）へ変更する必要があるのではないか。

### 所見3：継ぎ目（四角枠との接続）
曲線から本体四角部分への接続自体は段差なく自然に見えるが、所見2の通り曲線側の縁線が
そもそも見えていないため、「継ぎ目が自然かどうか」を輪郭線ベースで厳密に判定できる状態ではない。
縁線が復元された後に再確認が必要。

### 総括
| 観点 | 判定 | 備考 |
|---|---|---|
| (1) 丸みの視認性 | OK | 前回の課題(曲率不足)は解消、見本同様一目で丸みとわかる |
| (2) 継ぎ目の自然さ | 判定保留 | 縁線消失(所見2)のため厳密な評価不可 |
| （新規）曲線の縁取り線消失 | **NG（殿ご指摘・原因特定済み）** | `BorderThickness.Left`依存の設計により辺別指定変更の副作用で消失 |

判定・対処要否は殿・家老の判断に委ねる。
