# T-120 実機確認（忍者、2026-07-22）

## 対象
`docs/todo.md` T-120（配置ツールタブ末尾にテストモードボタンを増設）。隠密静的レビュー完了
（問題なし）を受け、家老采配。「配置ツール」タブでのテストモード切替が「基本機能」タブ側の
表示にも連動することを確認。

## 操作手法
UI Automation経由の合成マウスクリック（`Invoke-Ecad2ScreenClick`）。タブ・テストボタンとも
`SelectionItemPattern`/`TogglePattern`をサポートせず（`SynchronizedInputPatternIdentifiers`
のみ）、UIA標準パターンでは操作不可のため合成クリックで統一。殿ご自身の実操作は未実施。

## 検証内容・結果

1. **両タブへのボタン存在確認**：シート作成後、「配置ツール」タブ末尾に新規「テスト」ボタンを
   確認。「基本機能」タブに切替え、既存の「テスト」ボタンも末尾に確認——**DoD項目1、OK**
2. **基本機能→配置ツールへの連動**：基本機能タブの「テスト」ボタンをクリックしONへ
   （ステータスバー「モード: テスト」）。配置ツールタブへ切替えたところ、末尾の「テスト」
   ボタンも同様にオレンジ色（ON状態）で表示、他の配置系ボタンもグレーアウト（T-114/P-082との
   整合も確認）——**OK**
3. **配置ツール→基本機能への逆方向連動**：配置ツールタブの「テスト」ボタンをクリックしOFFへ
   （ステータスバー「モード: 作画」）。基本機能タブへ戻ったところ、「テスト」ボタンも通常色に
   復帰、新規/開く/保存等の他ボタンも有効状態に復帰——**OK**

双方向とも`IsTestMode`への連動を確認——**DoD項目2、OK**。

## 総括

| 観点 | 結果 |
|---|---|
| 両タブへのボタン増設 | OK |
| 基本機能→配置ツール連動 | OK |
| 配置ツール→基本機能連動 | OK |

回帰の有無：新規回帰なし。既存のP-082（テストモード中の配置系ボタン無効化）との整合も
併せて確認できた。
