# T-123 `.gcad` 関連付けの実機確認——忍者

対象＝`b483ab5`（`.iss` の書き込み先を HKCR から HKA へ改める）。検証日 2026-08-16。
段取りは `docs/ecad2-t123-verify-plan-ninja.md`。

## 1. ここまでに判っておること（2026-08-16 13:10〜13:18）

| 軸 | 答え |
|---|---|
| `HKLM\SOFTWARE\Classes\.gcad` 既定値 | `Ecad2.Document` |
| 同 `Ecad2.Document\shell\open\command` | `"C:\Program Files\ecad2\Ecad2.App.exe" "%1"` |
| `HKCR\.gcad`（統合ビュー） | `Ecad2.Document` |
| `cmd` の `assoc .gcad` | `.gcad=Ecad2.Document` |
| `cmd` の `ftype Ecad2.Document` | `"C:\Program Files\ecad2\Ecad2.App.exe" "%1"` |
| `FindExecutable` | **`err=31`（NOASSOC）**。対照 `.txt`→Notepad／`.cbz`→NeeView は正常 |
| **殿のダブルクリック** | **GuiEcad が開いた** |

**着地そのものは成功しており申す。**HKCU 側は触られておらず、予測（`.eml` と同じ形ゆえ
統合ビューに HKLM が出る）も的中した。**にもかかわらず実操作では GuiEcad が開く。**

## 2. 【測る前に書き置く】何が見えれば何が言えるか

**家老の下命により、以下を測る前に書いたものにござる。**
本日二度、先に予測を置く作法が効いたゆえ、ここでも同じくいたす。

### 分岐(A) `UserChoice` キーが生まれ、`ProgId = GuiEcad.Document` を持つ

→ **Windows がユーザー選択を作り直した。** エクスプローラーは `UserChoice` を最優先ゆえ、
これが GuiEcad を開いた直接の原因と言える。HKLM の `.gcad` 既定値は**上位に負けておる**。

**帰属**＝`HKA` 改修は正しく効いており、`.iss` に瑕疵は無い。
**届かぬのは Windows の既定アプリ機構が上位に在るゆえ**にござる。
手当ての筋は `.iss` ではなく、既定アプリの設定側へ移る。

### 分岐(B) `UserChoice` は無いが `UserChoiceLatest\ProgId` が再生成されておる

→ Win11 の新しい機構が働いた形。上位で GuiEcad を指す点は (A) と同じ。**帰属も (A) と同じ。**

### 分岐(C) `UserChoice` も `UserChoiceLatest\ProgId` も無い（インストール前と同じ `Hash` のみ）

→ **ユーザー選択は関与しておらぬ。** ならば GuiEcad が開いた原因を別に求めねばならぬ。

**この場合、`FindExecutable` の `err=31` は実態を正しく写しておったことになる**
——すなわち**「測定手段の側の問題」と見た忍者の仮説（`.gcad` の HKCU 空キーが
`FindExecutable` を誤らせておる）が誤り**であったことになり申す。

**二つの独立した観測（`FindExecutable` と実操作）が同じ方向を指す**以上、
疑うべきは**レジストリ二軸の方**——`assoc`/`ftype`/統合ビューが「書かれておる」ことは示せても
「シェルがそれを使う」ことまでは示せておらぬ、という筋にござる。

**忍者は (C) を最も重く見る。** `FindExecutable` の err=31 と実操作の GuiEcad が符合するゆえ。

### 分岐(D) GuiEcad プロセスの `Path`

- `C:\Program Files\GuiEcad\`（生きた登録）＝`HKLM\...\GuiEcad.Document` が使われた筋
- `C:\Program Files\Gui_cad\`（死んだパス）＝**起動し得ぬ**ゆえ、起動しておるならこちらではない

**予測＝`GuiEcad\`（生きた方）。** これが出れば、
「`.gcad` → `GuiEcad.Document`（HKLM）」という経路が今も生きて使われておることになる。

**そしてそれは奇妙にござる**——`HKLM\...\.gcad` の既定値は今 `Ecad2.Document` に書き換わっており、
`GuiEcad.Document` を指す `.gcad` の登録は**どこにも残っておらぬ筈**。
ゆえに (D) が予測どおりなら、**シェルは `.gcad` の既定値以外の何かを見て GuiEcad へ至っておる**。
その何かの候補＝`OpenWithProgids`／`OpenWithList`／`Applications\GuiEcad.App.exe`。

### 併せて観る＝`OpenWithProgids`／`OpenWithList` の差分

インストール前＝`OpenWithProgids` に三件（`gcad_auto_file`／`GuiEcad.Document`／`Ecad2.Document`）、
`OpenWithList` に `PickerHost.exe`(a)／`GuiEcad.App.exe`(b)／`Ecad2.App.exe`(c)、`MRUList=cba`。

**`MRUList` の順が変われば、殿のダブルクリックが記録された証**になり申す。

---

## 3. 測定結果（13:21）——(C) と (D) が的中

| 項 | 実測 | 予測との対応 |
|---|---|---|
| `UserChoice` | **不在** | **(C)** |
| `UserChoiceLatest` | `Hash = /UYkoly0L7A=` のみ。**`ProgId` は再生成されておらぬ** | **(C)** |
| `OpenWithProgids` | `gcad_auto_file` / `GuiEcad.Document` / `Ecad2.Document`（前と同一） | — |
| `OpenWithList` | a=`PickerHost.exe` b=`GuiEcad.App.exe` c=`Ecad2.App.exe`、**`MRUList` が `cba` → `bca`** | — |
| GuiEcad の `Path` | **`C:\Program Files\GuiEcad\GuiEcad.App.exe`**（生きた方）、PID=19708、13:17:52 | **(D)** |

**`MRUList` の変化が、殿のダブルクリックが `b`（GuiEcad）を選んだ記録**にござる。
ただし**開いた後の記録ゆえ、原因ではなく結果**。

## 4. 【最も重い一事】忍者の仮説は誤りであった

分岐(C) に書いたとおり——**`FindExecutable` の `err=31` は実態を正しく写しておった。**

忍者は「測定手段の側の問題」と見て、`.gcad` の HKCU 空キーが `FindExecutable` を
**誤らせておる**という仮説を立てた。**実際は誤らせておったのではなく、正しく
「関連付けなし」を報じておった。**

**疑うべきはレジストリ二軸の方であった**——`assoc`／`ftype`／統合ビューは
**「書かれておる」ことを示せても「シェルがそれを使う」ことまでは示せておらぬ。**

**朝に「読み取り時の解決を測ったにすぎぬ」と断りを置いておきながら、
その断りの外側を測らぬまま「関連付けは完全に成立しており申す」と報じた**——
**断りを置くことと、断りの外側へ踏み込まぬことは別**にござった。
`ninja.md`「射程を断っても、断った先の記述精度が伴わねば意味を成さぬ」の、
射程の外側版と言える。

## 5. 新たな仮説（未検証と明記する）

**シェルの ProgId 解決は、HKCU の `.gcad` 空キーで打ち切られておるのではないか。**

1. HKCU に `.gcad` キーが在り、既定値が無い
2. シェルは HKCU を先に見る → 既定値が無い → **そこで打ち切り、HKLM へ落ちぬ**
3. ゆえに `OpenWithProgids`／`OpenWithList` の側で解決し、GuiEcad へ至る

**`.eml` とも整合する**——`.eml` も HKCU に空既定値のキーを持ち、
`FindExecutable` は ProgId（`Outlook.File.eml.15`）ではなく **thunderbird** を返しておった。
**すなわち `.eml` も ProgId 経由では解決しておらぬ。**
本日「`.ext` の既定値さえ在れば解決は通る」と結論したのは、
**レジストリの読み取りについては正しく、シェルの解決については誤り**であった。

**侍の懸念は正しかった**——ただし「キー単位で覆い隠す」という機序ではなく、
**「シェルの解決が HKCU の空キーで打ち切られる」**という形で。
**レジストリの読み取りは値単位でマージされるが、シェルの解決は別の機構にござる。**

## 6. 手当ての候補（いずれも裁可を要す）

| 案 | 中身 | 見立て |
|---|---|---|
| (i) | `HKCU\Software\Classes\.gcad` の空キーを取り除く | **仮説を検めるにも最も安い。**一手で決する |
| (ii) | `.iss` で HKCU 側にも書く | `HKA` の意義が薄れる |
| (iii) | `.iss` で当該 HKCU キーを消す指定を加える | 他環境にも効くが、他アプリの登録を消す恐れを検める要あり |

**(i) を試せば仮説は一手で決し申す**——空キーを除いて `FindExecutable` を測り直すのみ。
**されど書き換えゆえ、殿の裁可を待つ。**

## 6-2. 【測る前に書き置く・三度目】空キーを除いた後、何が見えれば何が言えるか

殿ご裁可により `HKCU\Software\Classes\.gcad` の空キーのみ除いて測り直す。
**本節は削除の前に書いたものにござる。**

### 予測1（忍者の本命）＝`FindExecutable` が `C:\Program Files\ecad2\Ecad2.App.exe` を返す

→ **新仮説が確定する。** 「シェルの ProgId 解決は HKCU の空キーで打ち切られる」。

**帰属**＝`.iss` の `HKA` 改修は正しく、瑕疵は無い。
**遮っておったのは既存環境に残った空の殻**にござる。
手当ては環境側の掃除、あるいは `.iss` で当該キーを除く指定を加える筋へ移る。

**併せて言えること**＝新規環境（空キーが無い）では**元より通っておった**ことになり申す。
すなわち T-123 の残件「新規環境でダブルクリックしてEcad2が開くか」は、
**殿の環境が新規環境でなかったがゆえに再現できなんだ**という形。

### 予測2＝なお `err=31` を返す

→ **新仮説も外れ。** 原因は別の層に在る。候補は三つ。

- **(a) シェルのキャッシュ**——`ChangesAssociations=yes` の通知が届いておらぬ、
  あるいはプロセスを跨いで古い解決が保たれておる
- **(b) `FindExecutable` は元より ProgId 経由を見ぬ API である**——
  本日測った `.rar`／`.eml`／`.cbz` は**いずれも ProgId の既定値とは違う答え**を返しており、
  三つとも `OpenWith` 系で解決しておった疑いがござる。
  **もしそうなら `FindExecutable` は本件の判定に元より使えぬ**
- **(c) `.gcad` 固有の何か**——`NoOpen` 値等

**とりわけ (b) が重い**——**その場合、忍者が主軸に据えた三軸のうち二軸目までが落ちる**
（朝に `AssocQueryString` が落ち、次に `FindExecutable` が落ちる形）。
**残るのはレジストリ直読のみとなるが、それは「書かれておる」しか示せぬ。**
**すなわち実態を決するのは殿のダブルクリックただ一つ**という所へ戻り申す。

### 予測3＝`HKCR\.gcad` の統合ビューは変わらず `Ecad2.Document`

HKCU にキーが無くなれば HKLM がそのまま見える。自明に近いが、
**変わらぬことを確かめておかねば「除いたせいで別の何かが壊れた」を排除できぬ。**

### 対照を併せて採る

`.txt`／`.cbz` も同時に測る。**手段自体が働いておることを毎回確かめる**
——本日、測定手段が二度まで違う答えを返しておるゆえ。

## 6-3. 【訂正】`UserChoiceLatest\ProgId` は在った——忍者の三度の報告が誤り

13:30、家老の下命「`FileExts\.gcad` 配下を丸ごと採られたい」に従って再帰で採ったところ、

```
HKCU\...\FileExts\.gcad\UserChoiceLatest\ProgId   ← 【サブキー】
    'ProgId' = GuiEcad.Document                    ← その中の値
```

**`ProgId` は値ではなくサブキーとして在り、その中に値として `GuiEcad.Document` を抱えておった。**

忍者は 13:10／13:21／13:30 の三度にわたり「`ProgId` は無く `Hash` のみ」と報じたが、
**`GetValueNames()` のみを見て `GetSubKeyNames()` を見ておらなんだ。**
**「`ProgId` という名の値は無い」を「`ProgId` は無い」と報じた**形にござる。

**これで機序が通る**——エクスプローラーはこれを見て GuiEcad を開き、
`.gcad` の既定値（`Ecad2.Document`）は上位に負けておる。
**空キーを除いても変わらなんだのは当然にて、原因はこちらであった。**

**8/14 に隠密が突き止めた構図は今も生きており、忍者が「消えておる」と報じたのが誤り。**

### 帰属の訂正

**分岐(C)（ユーザー選択は関与しておらぬ）という忍者の判定は誤り。**
正しくは **`HKA` 改修は効いており `.iss` に瑕疵は無い。届かぬのは Windows の
ユーザー選択機構が上位に在るゆえ**——家老が最初に立てた見立てに近い。

### 露見した契機

**家老の「丸ごと採られたい」という指図。** 観点を絞って測り続けておれば見落としたままであった。
**`ninja.md`「疑うべきは観測対象だけでなく、観測手段そのものでもある」の、
最も素朴な形**——**採る範囲を絞ったこと自体が、見えるべきものを見落とす形になっておった。**

## 6-4. 【測る前に書き置く・四度目】既定のアプリを Ecad2 へ変えた後

殿ご裁可により、設定画面で `.gcad` の既定を Ecad2 へ変えて測る。**本節は殿の操作の前に書いた。**

### 予測1（本命）＝Ecad2 が開く

`UserChoice` が作られ `ProgId = Ecad2.Document` を持つ。エクスプローラーはこれを最優先ゆえ開く。

**この一手で確定すること**
- `Ecad2.Document\shell\open\command` の記述が正しい
- Ecad2 が引数でファイルを受けて開く（**これは本日 T-151 の実機確認で既に実証済み**——
  `Ecad2.App.exe <パス>` の引数付き起動で `t151-repro2.gcad` が開いておる）

**この一手で確定【せぬ】こと**——**ここを分けて記すのが肝要にござる**
- **`.gcad` の既定値（`.gcad` → `Ecad2.Document` のマッピング）が使われるか。**
  `UserChoice` は ProgId を直に指すゆえ、**マッピングを経由せぬ。**
  すなわち `.iss` が書いた四行のうち、**`.gcad` の既定値の一行だけは検証されぬまま残る**
- **新規環境で初期状態のまま効くか。** `FileExts\.gcad` の履歴こそ新規環境に無いものにて、
  **殿が明示的に選んで作った `UserChoice` は、新規環境には無い**

### 予測2＝Ecad2 が開かぬ（GuiEcad が開く、あるいは何も開かぬ）

→ **殿が明示的に選んでなお届かぬなら、この端末の履歴では説明が付かぬ。**
配布物の側を疑う筋へ入る。

**されど忍者はこれを低く見る**——**引数処理は本日既に実証されており**、
`command` の記述も実測で確かめてござる。**予測2 が出れば、極めて奇妙な事態**にて、
その時は「設定画面の操作が `UserChoice` を作れておらぬ」（＝Windows 側の保護機構）を先に疑う。

### 予測3＝`UserChoice` キーが生まれる

設定画面経由なら `Hash` 付きの `UserChoice` が作られる筈。`UserChoiceLatest\ProgId` も更新される筈。
**生まれなければ、Windows の機構についての我らの理解が誤っておることになる。**

### 予測4＝`FindExecutable` はどう答えるか

忍者の仮説「HKLM の ProgId を見ておらぬ」が正しければ、`UserChoice` に入っても
`Ecad2.Document` は HKLM ゆえ**なお `err=31` の目**がござる。
**もし Ecad2 を返せば、仮説は「HKLM を見ぬ」ではなく「`UserChoice` を見る」であったことになる。**
**どちらに転んでも一つ分かる**ゆえ、必ず測る。

## 6-5. 【測る前に書き置く・五度目】`UserChoiceLatest\ProgId` を除いた後

殿ご裁可により当該サブキーのみ除く。**本節は削除の前に書いた。**

### 予測A＝Windows が `UserChoiceLatest` を作り直すか

**忍者の見立て＝削除の直後には作り直されぬ。** 選択操作を経ずに再生成される理由が無いゆえ。

**ただし `Hash` が残る点に注意**——`Hash` は `UserChoiceLatest` 直下の値にて、
`ProgId` サブキーだけを除けば**署名と中身の整合が崩れる**。
**望ましい筋＝Windows がこれを無効と判じて無視する。**
**危うい筋＝整合が崩れたと見て作り直す**——その時は**測る前に条件が変わる**ゆえ、
**ダブルクリックの前と後の両方で `FileExts` を採る。**

### 予測B（本命）＝Ecad2 が開く

`UserChoiceLatest` が効かなくなり、`.gcad` の既定値（`Ecad2.Document`）へ落ちるゆえ。

**これが出れば `.iss` の四行すべてが検証され、この端末で測れる限りの確定に至る。**

### 予測C＝GuiEcad が開く／予測D＝何も開かぬ

**ここで家老と見立てが分かれる。家老は「`.iss` の当該一行に瑕疵が在る筋へ入る」と申されたが、
忍者はそう見ぬ。**

**理由**＝`.gcad` の既定値が `Ecad2.Document` であることは、
**`assoc`／`HKCR` 統合ビュー／`HKLM` 直読の三軸で確定済み**にござる。
**書かれておることは既に測れておる。** ゆえに開かぬのであれば、
**「書かれておるのに使われぬ」**——それは `.iss` の瑕疵ではなく、
**Windows がユーザー選択の無い拡張子をどう扱うかの問題**にござる。

**そしてこの端末には `OpenWithProgids`（三件）・`OpenWithList`（三件）という履歴が残っており、
新規環境には無い。** すなわち**この端末で GuiEcad が開いても、新規環境の結論にはならぬ。**

### 【射程・(甲) をもってしても測れぬこと】

**(甲) が測るのは「`UserChoiceLatest` が無い状態」までにて、「履歴が一切無い状態」ではない。**
`OpenWithProgids`／`OpenWithList` は残る。**新規環境へ一段近づくが、届きはせぬ。**

## 6-6. 【測る前に書き置く・六度目】アンインストールの前後

殿ご裁可＝アンインストールのみ行い、関連付けは 8/18 まで空のままとする。
**本節はアンインストールの前に書いた。事前状態も採取済み**（下記）。

### 採った事前状態（13:46）

| 項 | 値 |
|---|---|
| `C:\Program Files\ecad2\` | **438ファイル・153,981,360バイト**（一覧＝`t123-installdir-before.txt`） |
| アンインストーラ | `unins000.exe`（4,470,396B）／`unins000.dat`（70,476B） |
| ショートカット | `ProgramData\...\Start Menu\Programs\ecad2` 在り／`Public\Desktop\ecad2.lnk` 在り |
| Uninstall レジストリ | `ecad2` / `0.6.0` / `"C:\Program Files\ecad2\unins000.exe"` |
| ユーザーデータ | `…\ドキュメント\Ecad2\` **24ファイル**（MD5 一覧＝`t123-userdata-before.txt`） |
| `CloseApplications` | **`yes`**（`.iss:53`）＝**起動中でも自動終了されるゆえ、殿が開いたままでも進められる** |

### 予測

| # | 予測 | 根拠 |
|---|---|---|
| 1 | `HKLM\...\.gcad` の既定値が**消える** | `Flags: uninsdeletevalue` |
| 2 | `HKLM\...\Ecad2.Document` が**キーごと消える** | `Flags: uninsdeletekey` |
| 3 | `C:\Program Files\ecad2\` が消える | ただし**残骸の目あり**——インストール後に生成された物（ログ・設定）は `[Files]` の管轄外 |
| 4 | ショートカット二つが消える | `[Icons]` |
| 5 | **ユーザーデータ24件は無傷** | インストーラーは `MyDocuments` に触れておらぬ |
| 6 | **`FileExts\.gcad` はそのまま残る** | インストーラーの管轄外。**`Hash` だけの不整合も残る** |

### 【予測に添える一事】我らの検証が、新たな残骸を一つ作り申す

**`OpenWithProgids` の `Ecad2.Document` は残る一方、ProgId の実体は消える。**
すなわち**「名前だけ残って実体の無い登録」が Ecad2 側にも生まれる**——
**本日朝に見つけた `gcad_auto_file`（実在せぬ `Gui_cad\` を指す残骸）と、同じ形**にござる。

**朝は「この残骸は誰がいつ作ったか追えぬ」と申したが、こちらは我らが作ったと判っておる。**
**記録に残しておけば、次に誰かが `.gcad` を調べた時、由来を辿れ申す。**

## 6-7. 【測る前に書き置く・七度目】v0.7.0 の正式インストール——T-123 残件の答えが出る場

殿ご下命により 8/18 を待たず本日リリース。**本節は生成物が揃う前に書いた。**

### なぜこれが「一度きりの機会」か

**今の殿の環境は、我らの検証の副産物として次の形に在る**——

| 項 | 現状 |
|---|---|
| `HKLM\...\.gcad` | **空キー**（値はアンインストールで消え、キーだけ残った） |
| `Ecad2.Document` | 不在 |
| `UserChoiceLatest\ProgId` | **不在**（忍者が (甲) で除いた） |
| `UserChoiceLatest\Hash` | `iWwXOO9lEBQ=` が残る（片割れ） |
| `OpenWithProgids`／`OpenWithList` | 三件ずつ残る |
| `.gcad` の解決 | **どこにも向かぬ** |

**インストールすれば `.gcad` の既定値が HKLM へ書かれ、
本日 (甲) の後（＝Ecad2 が開いた状態）と同じ形になる筈**にござる。

**ただし真の新規環境とは、なお二点違う**——**`Hash` の片割れが残ること、
`OpenWith` 系が残ること。** **家老の申された「履歴が無い状態」は、正確には
「`UserChoice` 系の `ProgId` が無い状態」**にて、**履歴が皆無なわけではない。**
**この区別を結論の文言へ通すこと**（本日、家老の「構造が同じ」を忍者が検分したのと同じ筋）。

### 予測

| # | 予測 | 根拠 |
|---|---|---|
| 1 | インストールだけでは `UserChoice`／`UserChoiceLatest\ProgId` は**生まれぬ** | 本日の実測——インストール直後は `Hash` のみであった。インストーラーは `FileExts` を書かぬ |
| 2 | **ダブルクリックで Ecad2 が開く** | `.gcad` 既定値が HKLM に在り、`UserChoiceLatest\ProgId` が無い——**本日 (甲) で実証済みの形と同一** |
| 3 | 引数のファイルが読み込まれる | 本日二度実証済み（機器表で判定） |
| 4 | `FindExecutable` は**なお `err=31`** | 本日五度そうであった。この API は本件に使えぬ |
| 5 | 版数は `0.7.0`（`csproj`／`.iss`／exe の三点が揃う） | `memory: ecad2_release_procedure` の突合 |

**予測2 が当たれば、それが T-123 残件の答えにござる**——
**「`UserChoice` 系の履歴が無い状態で、`.iss` が書いた登録だけで開く」の実証。**

**予測2 が外れた場合**＝`.gcad` 既定値が在ってなお開かぬということにて、
**本日 (甲) で開いた事実と矛盾する。** その時は**インストーラーが書いた内容が (甲) の時と違う**
（版が上がって `.iss` が変わった等）を先に疑い、**配布物の側へ短絡せぬこと。**

### 測る観点（本日二度通した道＋リリース固有）

1. レジストリ四箇所（HKLM 着地）——本日と同じ
2. `FileExts\.gcad` 配下を**再帰で**（`GetSubKeyNames()` まで。本日三度誤った所にござる）
3. **ダブルクリック**（殿のお手）→ プロセス差分（名前で絞らず）→ 読み込み確認（機器表）
4. `FindExecutable`＋対照（`.txt`／`.cbz`）
5. 版数三点の突合／インストール先のファイル数・サイズ／ショートカット二つ
6. **ユーザーデータ（`ドキュメント\Ecad2\`）が無傷**——本日24件で差分ゼロを確認済み、同じ形で
7. 起動・終了の実測時刻

## 6-8. 【答え】v0.7.0 正式インストールとダブルクリック（14:34〜14:46）

**8/14 から追うてまいった T-123 残件に、この一度で答えが出た。**

### 非侵襲の測定——予測どおり

| # | 観点 | 実測 | 予測との対応 |
|---|---|---|---|
| 1 | インストール先 | **438ファイル**・153,983,536バイト | 一致 |
| 2 | 版数三点 | exe `0.7.0+8a4aa46…`／csproj `0.7.0`／`.iss` `0.7.0` | **予測5 的中** |
| 3 | レジストリ四箇所 | `.gcad`＝`Ecad2.Document`／ProgId／`DefaultIcon`／`command` すべて正 | 一致 |
| — | `UserChoice` 系が生まれるか | **生まれず**（`Hash` のみ） | **予測1 的中** |
| 5 | ショートカット | 二つとも在り | 一致 |
| 6 | ユーザーデータ | **24件、差分ゼロ** | 一致 |
| 7 | 実装が載って動くか | 下記 | 一致 |

**【特筆】`HKCU\SOFTWARE\Classes\.gcad` はキーごと不在のまま**——忍者が本日除いた空キーが
**復活しておらぬ。** すなわち **`HKA` が HKLM のみへ書き、HKCU 側へ一切触れておらぬ証**にて、
**朝に侍が懸念した「HKCU へ吸い寄せられる」は完全に断たれてござる。**

### 観点7＝版数に拠らず、中身と挙動の二段で

**中身**（`Assembly.LoadFrom`）＝`BuiltinPartIds`／`PartResolver.DefaultLabelDy`（T-151）、
`IsExcludedFromCrossReference`（T-152前半）、`IsExcludedFromDeviceTypeConsistency`（T-152追補）
**四点とも在り。**

**挙動**＝インストール版で `t152-thermal.gcad` を開き DRC を走らせ、
**`DRC-TYPE-001` は出ず `DRC-XREF-002/CR4` の1件のみ**——`bin` 版で測った修正後の値と完全に一致。

### 殿のダブルクリック——答え

| 項 | 実測 |
|---|---|
| プロセス | **`Ecad2.App` PID=5504、14:44:59、`C:\Program Files\ecad2\`**＝新規に立った |
| GuiEcad | **立たず**（直近5分の全プロセスを名前で絞らず走査） |
| 機器表 | **`Y8 \| リレー` 1行** |
| 母線名 | **`N24` / `P24`**（既定にあらず、検体固有） |
| 絵 | a接点 `Y8` が行1に一つ、シート名 `シート1` |
| `UserChoice` 系 | **作られず**（`Hash` のみのまま） |
| `HKCU\...\.gcad` | **不在のまま** |

**読み込みは三軸（機器表・母線名・絵）で証した**——**開いたことと読み込んだことは別**ゆえ。

### 【確定すること】

> **`UserChoice` 系の履歴が無く、`HKCU\Software\Classes\.gcad` のキーすら不在の状態で、
> `.iss`（`HKA`）が書いた登録だけによって、エクスプローラーのダブルクリックが
> Ecad2 を起動し、引数のファイルを正しく読み込む。**

### 【確定せぬこと——忍者が自ら区切る】

**真の新規環境と同一ではない。** なお残る残骸＝`OpenWithProgids` 三件／`OpenWithList` 三件／
`UserChoiceLatest\Hash` の片割れ。

**ただし `OpenWith` 系が支配的でないことは、本日朝の実測が間接に示す**——
**朝は `OpenWith` 系が今と同じ三件ずつ、`MRUList=cba`（`c`＝`Ecad2.App.exe` が先頭）で
ありながら GuiEcad が開いた**（`UserChoiceLatest\ProgId=GuiEcad.Document` ゆえ）。
**`OpenWith` 系が支配的なら朝も Ecad2 が開いた筈にて、開かなんだ。**

**されど「`.gcad` 既定値」と「`OpenWithProgids` の `Ecad2.Document`」のどちらが効いたかは、
今回**直には**分けておらぬ**（両方とも Ecad2 を指すゆえ）。
**分けるには `OpenWithProgids` から当該項を除いて測る要があるが、忍者は朝の対照で足りると見る。**

### 本日昼の (甲) との差

**今回の方が一段強い**——(甲) は人の手で `ProgId` を除いた後にて `Hash` が片割れで残り、
`HKCU\...\.gcad` の空キーも在った。**今回はインストール前から `UserChoice` 系が無く、空キーも不在。**

## 7. 操作手法・現況

- 測定はすべて読み取りのみ。書き換えは一切行うており申さぬ
- `FindExecutable`／`cmd` の `assoc`・`ftype`／レジストリ直読の三軸。
  `AssocQueryString` は本日の実測で使えぬと判じ、用いており申さぬ（段取り書1節）
- Ecad2 のプロセスは見当たらず（家老が起こされた PID=16624 は既に不在）。
  GuiEcad が PID=19708 で走っておる。いずれも忍者は手を触れておらぬ
