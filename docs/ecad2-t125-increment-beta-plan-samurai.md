# T-125 増分β 実装計画（侍・起草／2026-07-28 更新）

> 2026-07-27 侍起草。家老の下知（DoD 5件）に応じ、増分β（ヒットテスト優先順位ロジックの共通化）の
> 計画を起草する。**起草のみ。実装には一切入っていない**（殿裁可を待つ）。
> ブランチ運用＝**main直**（殿裁定2026-07-27、αと同じ）。
>
> **【2026-07-28 更新】** 家老の下知（案C裁可を織り込んだβ-2/β-3の計画更新）に応じて改訂した。
> **本更新も計画のみ。`src/`・`tests/` へは一切書き込んでおらぬ**（家老のスコープ境界指示）。
> **一次ソースの行番号はすべて `b0645c3` 時点へ改めた**（旧版は `ffbec7d` 時点）。

---

## 【2026-07-28 更新】この改訂で変わったこと——4点

### 1. 行番号が全面的にずれた（-17〜-21行）

`ffbec7d` 以降に **P-141（T-130一時計装の除去、`f0d6d5e`、`MainWindow.xaml.cs` から打点13箇所を削除）**と
**β-1（`2df5ab6`、同ファイルへ +10行）**が入ったため。**差し引き `MainWindow.xaml.cs` は -27行**。

| 対象 | 旧（`ffbec7d`） | 新（`b0645c3`） |
|---|---|---|
| 系統A（左Down） | `MainWindow.xaml.cs:1501` | **`:1480`** |
| 系統B（左Up） | `:1850` | **`:1833`** |
| 系統C（右Down） | `:2072` | **`:2055`** |
| `OpenFrameLabelEditor` | `:3903-3916` | **`:3886-`** |
| `SelectedElement` | `MainWindowViewModel.cs:2101` | **`:2101`（変わらず）** |
| `HitTestElement` | `:2779` | **`:2779`（変わらず）** |

**教訓の再確認**＝`karo.md`非同期委譲10項「数を書き残すときは、その数を再現する手段も併記する【MUST】」。
**本計画書の行番号もまた腐る。** ゆえに以下すべての行番号には**メソッド名・シンボル名を併記**した——
**行番号が腐っても、名前で引き直せる**ようにするためにござる。

### 2. 【最重要】β-2の前提が崩れた——**挙動不変を証明するテストが原理的に書けぬ**

旧版β-2は検証観点に「共通化の前後で、同一座標に対する判定結果が3系統とも一致することをテストで固定する」
と記した。**これは現状の設計では書けぬ。**

**確認済み事実**（`tests/` 配下の棚卸しによる。件数の前に個々を列挙して数えた）——
- **3系統のマウスハンドラを呼ぶテストは、App.Tests に1件も存在しない**
- ゆえに**チェーンの判定順序を固定しているテストも1件も無い**。とりわけ
  **系統BのConnectionDot→FreeLine順（T-116＝P-107対処）を守る網は無い**
- 理由は既存テストのクラスコメントに明記されておる（`tests/Ecad2.App.Tests/FreeLineConnectionDotHitTestOverlapTests.cs:8-14`）
  ＝**`MainWindow` のインスタンス化が AvalonDock 初期化等の重い副作用を伴うため単体テスト不可**。
  同テストは「両者が同一座標で同時にヒットしうる」という**前提条件だけ**を固定し、
  **順序の確認は忍者の実機確認へ委ねておる**

**すなわち、旧版β-2の設計（既存のチェーンをそのまま1箇所へ寄せる）を採る限り、
「壊していないこと」を機械的に確かめる手立てが無い。** これは`samurai.md`
**「『テストしにくい』は設計の匂い」**がそのまま当てはまる場面にござる（→ β-2節で設計を改めた）。

### 3. 案C（`SelectedCell`のアンカー正規化）が殿裁可済みとなった

旧版は三案を並べて案Cを推奨する形であった。**殿裁可により案Cで確定**（P-077）。
三案の比較は**決定の経緯として残す**が、β-3は案C前提で具体化した。

### 4. β-1は完了した（`2df5ab6`）

実装・隠密レビューまで完了。**忍者の実機確認のみ未了**。

---

## 総括——着手前指標に誤りがあった。ただし今度は着手前に見つけた

**「要素の当たり判定は3実装」という数え方は誤りである。実際は5箇所。** UI Automation 経路の2件が
どの調査にも含まれていなかった（家老の台帳・隠密のβ前段調査・侍自身のα計画書、いずれも3としていた）。

αでは同種の数え上げ誤りが**2度**起きた（弁別ロジック「4箇所」→実は2箇所、境界ガード「9/9」→実は7/9）。
いずれも**実装の後**に判明した。**今回は着手前に見つかった**——家老の「βの3系統も改めて裏を取れ」
という指示がそのまま効いた形にござる。

| | 従来の数え方 | 実際 |
|---|---|---|
| 座標→種別の優先順位チェーン | 3系統 | **3系統**（変動なし。裏取り済み） |
| 要素の当たり判定 | 3実装 | **5箇所**（完全一致3・区間交差2） |

---

## DoD(5)：着手前の指標を確定する

### 指標1＝座標→要素種別の優先順位チェーン：**3系統**

| 系統 | file:line（`b0645c3`） | メソッド | 判定順序 |
|---|---|---|---|
| A（掴む） | `MainWindow.xaml.cs:1480` | `LadderCanvasHost_PreviewMouseLeftButtonDown` | Connector → WireBreak → FreeLine → ConnectionDot → ImageHandle → Image → **Element** → Frame |
| B（選ぶ） | `MainWindow.xaml.cs:1833` | `LadderCanvasHost_PreviewMouseLeftButtonUp` | Connector → Frame → WireBreak → ConnectionDot → FreeLine → Image → **セル（＝Element暗黙）** |
| C（メニュー） | `MainWindow.xaml.cs:2055` | `LadderCanvasHost_PreviewMouseRightButtonDown` | **Element** → Connector → Frame → Image → 行操作 |

**【2026-07-28 追記＝旧版の表に載っていなかった前段ガード】** 上表は「**要素種別**の優先順位」に絞った
数え方ゆえ、**チェーン先頭に置かれた前段ガードが落ちておった**。共通化の設計では**これらも一緒に
動かすことになる**ゆえ、ここに補う（系統Bを一次ソースで読み直して確認、`MainWindow.xaml.cs:1968-2031`）——

| 系統 | 種別判定の前に置かれたガード |
|---|---|
| A | 行コメントのダブルクリック → 枠ラベルのダブルクリック → 画像配置中 → 枠配置中 |
| B | **行コメント記入領域のクリック**（`ShouldSkipSelectionForRungCommentAreaClick`、`:1968-1972`）、テストモード |
| C | 行範囲チェック（`rowInRange`） |

**これらのガードのうち3つは既に純粋関数として切り出され、テストが在る**——
`ShouldSkipSelectionForRungCommentAreaClick`（`RungCommentSelectionGuardTests`、3ケース）／
`ShouldOpenRungCommentEditor`（`RungCommentDoubleClickTests`、5ケース）／
`ShouldSkipSelectionInTestMode`（`TestModeSelectionGuardTests`、2ケース）。
**すなわち「チェーンの判断部分だけを純粋関数へ切り出してテストする」という手法は、
本プロジェクトに既に前例がある**（→ β-2の設計はこれを踏襲する）。

**3系統は末端の `HitTestXxx` のみ共有し、チェーン自体は3回手書きされている。** 判定順序も互いに一致
しない。数え方の定義は**α計画書で定めたものを踏襲**する——「マウス座標を受け取り、要素種別を
優先順位付きで順に判定していくチェーンを、ラダー編集画面（`MainWindow.xaml.cs`）に限って数える」。
`PartEditorCanvas.cs:418`（部品エディタ、2種のみ）・状態ディスパッチ連鎖6系統・単一種別経路2件は
対象外とする（理由はα計画書に既述）。

### 指標2＝要素（`ElementInstance`）を解決する式：**5箇所**（完全一致3・区間交差2）

| # | file:line（`b0645c3`） | 式 | 尺度 | 役割 |
|---|---|---|---|---|
| 1 | `MainWindowViewModel.cs:2101` | `el.Pos == pos` | **完全一致** | `SelectedElement`（プロパティパネル・削除・移動・ドラッグの起点） |
| 2 | `MainWindowViewModel.cs:2779` | `el.Pos.Row == pos.Row && el.Pos.Column <= pos.Column && pos.Column <= el.Pos.Column + el.CellWidth - 1` | **区間交差** | `HitTestElement`（右クリック・テストモード） |
| 3 | `MainWindow.xaml.cs:1668` | 同上を手書き（`elemHitPos` / `dragElem`） | **区間交差** | 左Downのドラッグ開始（選択済み1件への内包判定） |
| 4 | `LadderCanvasAutomationPeer.cs:65` | `e.Pos == cell` | **完全一致** | UIA `ISelectionProvider.GetSelection()` |
| 5 | `SymbolAutomationPeer.cs:80` | `SelectedCellForAutomation == _element.Pos` | **完全一致** | UIA `ISelectionItemProvider.IsSelected` |

**【2026-07-28 追記】#2 には既にテストが4件在る**——`MainWindowViewModelTests` の
`HitTestElement_HitsNonAnchorCell_WhenCellWidthGreaterThanOne`（`:583`）・`HitTestElement_HitsAnchorCell`（`:603`）・
`HitTestElement_ReturnsNull_OutsideOccupiedRange`（`:614`）・`HitTestElement_ReturnsNull_DifferentRow`（`:626`）。
**うち第1のテスト名がそのまま案Cの土台になる**——**区間交差は既に正しく動いており、
β-3で足すのは「その結果を`SelectedCell`へどう反映するか」だけ**にござる。

**#4・#5 が従来の数え方から漏れていた。** これらは「クリック位置から要素を決める」当たり判定ではなく
「選択セルから要素を決める」**選択解決**だが、**同じ完全一致式の複製であり、#1 の定義を変えれば
必ず影響を受ける**。ゆえに指標に含めるのが筋と考える。

**β完了時の目標＝チェーン 3系統→1系統／要素の解決式 5箇所→1箇所**（ただし後者は下記の案により変わる）。

---

## DoD(2)【最重要】：P-077の機能的非対称をどう扱うか

### 症状（隠密が確定、侍が裏取り済み）

`CellWidth > 1` の要素（`Motor`=3セル、`Breaker3P`/`ContactorMain3P`/`ThermalOverload3P`=各2セル、
および自作パーツ）について——

- **左クリック**：非アンカーセル（左端以外）では**選択できない**。プロパティパネルが開かない
- **右クリック**：非アンカーセルでも**メニューは開く**（`HitTestElement`＝区間交差を使うため）
- **ドラッグ**：既に区間交差（`MainWindow.xaml.cs:1684`）ゆえ、**選択さえできていれば**掴める

### 三案

#### 案A：`SelectedElement` を区間交差へ揃える

`MainWindowViewModel.cs:2101` の式を `HitTestElement` と同じ区間交差にする。

- **利**：一箇所の変更で非対称が解消する。最も直截
- **害**：**`SelectedCell` と `SelectedElement.Pos` がずれる状態が生まれる**。非アンカーセルを選択した
  まま `SelectedElementDeviceName` の setter（`:2147`、機器表 `Document.Devices` を書き換える）や
  `SelectedElementSetpoint`・`SelectedElementLabelDy` 等の setter が走る経路が生じ、Undo・機器表の
  整合を全て検証し直す必要がある（派生プロパティは**13個**）
- **さらに**：**UIA の #4・#5（完全一致）と食い違う**。画面では選択されているのに UIA は
  「選択されていない」と報告する状態になり、**忍者の実機確認スキルが誤った判定を下しうる**
  （`memory: ecad2_comparison_target_identity_pitfall` と同型の罠を我らが作ることになる）

#### 案B：現状維持（チェーンの共通化のみ行い、非対称は温存）

- **利**：挙動が一切変わらない。最も安全
- **害**：P-077 は未解決のまま。**家老が「βの共通化がこの非対称を温存したまま固めてしまう恐れ」と
  懸念したとおりの結末**になる

#### 案C【推奨】：`SelectedCell` を要素のアンカー位置へ正規化する

左Up チェーン（系統B）に Element の区間交差判定を追加し、**非アンカーセルがクリックされたら
`SelectedCell` へ要素のアンカー位置を代入する**。`SelectedElement` の定義（完全一致）は変えない。

- **利1**：**不変条件「`SelectedCell` が要素上を指すなら、それは必ずアンカー位置」が保たれる**。
  ゆえに派生プロパティ13個・削除2経路・移動2経路・ドラッグ1経路は**一切無影響**
- **利2**：**UIA の #4・#5 も無影響**——`SelectedCell` が常にアンカーを指すため、完全一致のままで
  正しく解決される。案Aで生じる「画面とUIAの食い違い」が起きない
- **利3**：**既存の対処と同型**。右クリック経路は既に「選択の正規化を各メニュー項目の Click 内へ
  遅延する」方式を採っており（`MainWindow.xaml.cs:2250/2258/2269`、T-069往復3〜4周目）、
  **「アンカーへ正規化する」という考え方はプロジェクトに既にある**
- **利4**：要素の解決式を**5箇所→1箇所**（区間交差の共通ヘルパー）へ寄せつつ、**完全一致側3箇所は
  そのまま残せる**——正規化により完全一致でも正しく解くため
- **害**：`SelectedCell` がユーザーのクリック位置と厳密には一致しなくなる（非アンカーをクリックすると
  左端へ寄る）。**セル選択の見た目（カーソル枠）が左端へ移動する**ため、使用感の変化は生じる

### 侍の推奨＝**案C** → **【2026-07-27 殿裁可により案Cで確定】**

理由は上記の利2が決定的にござる。**案Aは、我らが忍者の検証手段そのものを壊す**——画面とUIAが
食い違えば、実機確認の判定が信用できなくなる。案Cは影響範囲を左Upチェーンの1分岐に閉じ込めつつ、
症状を解消する。

**殿へ諮った形**：「Motor等の幅広部品を左端以外でクリックしたとき、(1)今のまま選択できないでよいか、
(2)選択できるようにしてカーソル枠は左端へ寄せるか、(3)選択できるようにしてカーソル枠はクリック位置に
留めるか（＝案A、ただし内部整合の検証が大きい）」。

**殿裁定＝(2)＝案C**（**非アンカー位置でも選択でき、カーソル枠はアンカーへ寄る**）。
**以後、案A・案Bは採らぬ。** 上記の三案比較は**決定の経緯として残す**（次に同種の分岐を判ずる者が
「なぜ案Aを退けたか」を辿れるようにするため）。**β-3は案C前提で具体化した**（後述）。

---

## DoD(3)：留保5（左Down系統の `return` 欠落）の扱い

### 事実（侍が一次ソースで確認）

`MainWindow.xaml.cs:1684-1696` の Element 分岐だけが、**最後の分岐でないのに `return` を持たない**。
他6分岐（Connector/WireBreak/FreeLine/ConnectionDot/ImageHandle/Image）はすべて成功時に `return` する。

### α計画書での我が留保と、その後

α計画書に「`SelectedElement` は `SelectedCell` 由来、`SelectedFrame` 選択時は `SelectedCell` が
null になるため**両立せず実害は無い見込み**。**推測ゆえ断じない**」と記した。

**この留保が正しかった。** 隠密のβ前段調査が、両立する具体経路を特定した——
**`OpenFrameLabelEditor`（`MainWindow.xaml.cs:3903-3916`）のみ、`SelectedFrame` の設定前に
`SelectedCell = null` を呼んでいない**（侍が一次ソースで確認済み。他3箇所＝`MainWindow.xaml.cs:2010`・
`:2150`・`MainWindowViewModel.cs:1623` はいずれも直前に `SelectedCell = null` を伴う）。

同メソッドのコメントは「編集ボックス表示中は `IsMainContentEnabled` 経由でキャンバス操作が
ブロックされるため、編集中に `SelectedFrame` が他へ変化する心配はない」と述べるが、
**編集終了後については何も述べていない**。

**もし断じていれば、この経路を塞がぬままβを固めていた。**

### 扱い＝**β-1 に含めて塞ぐ**

`return` を補うだけでは片手落ちにござる。**`OpenFrameLabelEditor` 側の非対称も併せて正す**
（`SelectedFrame` 設定前に `SelectedCell = null` を置く、または編集終了処理でクリアする）。
どちらが筋かは着手時に一次ソース（`RenameSelectedFrame` 等の確定処理）を読んで決める。

**実機での実害の再現は忍者の領分**であり、実機は殿がT-130の観測に使われている。**再現を待たずに
構造として塞ぐ**——`return` の欠落はパターンからの明確な逸脱であり、実害の有無に関わらず正すべき。

### 【2026-07-28 追記】β-1は完了した（`2df5ab6`）

| | 実績 |
|---|---|
| 差分 | `MainWindow.xaml.cs` **+10行**（**実コード2行＋コメント8行**）／`tests/Ecad2.App.Tests/T125BetaSelectionExclusivityTests.cs` **+160行（新規）** |
| テスト | **新規5件**。Core 280・App 879 **全合格** |
| RED証明 | **原理的に不可**（View層テスト基盤の不在）。ゆえに**ViewModel層で「規約違反の順」「規約どおりの順」を対照**し、**壊す実測で検出力を確かめた** |
| レビュー | **隠密の静的レビュー完了** |
| 残 | **忍者の実機確認のみ未了**（観点は引き継ぎ書§2） |

**この段で得た副産物が、β-2/β-3の設計を左右する**——新設した
`T125BetaSelectionExclusivityTests` の2件（`二重掴みで要素と枠を続けて掴める`・
`二重掴みで要素を先に確定すると枠のドラッグは巻き戻される`）は、
**左Down/左Upの「分岐の評価順序」に依存した現状の挙動を固定しておる**。
**β-2でチェーンを「ヒットしたら単一種別を返して打ち切る」形に変えると、
このテストが前提とする両立状態そのものが（チェーン経由では）作れなくなる**——
すなわち**アサートは通り続けるが、守っておる不変条件は消える**。
**これは「偽GREEN」であり、テストが壊れるより質が悪い**（→ β-2の検証観点へ明記した）。

---

## DoD(4)：γ側へ及ぶ範囲の線引き

### βに含める（構造が閉じており、γの責務分割を待たずに直せるもの）

1. 3系統のチェーンを共通化する（末端 `HitTestXxx` は既に共有。**チェーンの順序と分岐を1箇所へ**）
2. 左Down の `return` 欠落と `OpenFrameLabelEditor` の非対称（上記）
3. 要素の当たり判定を区間交差の共通ヘルパーへ寄せる（#2・#3 の重複解消）
4. 案C を採るなら、左Up チェーンでの `SelectedCell` 正規化

### γに残す（`MainWindowViewModel` の責務分割と不可分なもの）

1. **`SelectedElement` 派生プロパティ13個**（`SelectedElementDeviceName`/`Comment`/`NotchPosition`/
   `LampColor`/`Setpoint`/`LabelDy` 等）の整理——プロパティパネルの表示ロジックそのものであり、
   責務分割の対象。**βでは触れない**
2. **変更通知ブロック4箇所の重複**（`MainWindowViewModel.cs:467-480`・`2434-2446`・`2569-2581`・
   `3222-3234`、13〜14行の同一列挙が4回複製）——`memory: ecad2_setproperty_early_return_trap` の
   親戚筋。γの責務分割で自然に解消される見込み
3. **UIA の完全一致2箇所**（#4・#5）——案Cを採れば無影響ゆえ、βでは触れない。案Aを採るなら
   βに含めざるを得ない（これも案Cを推す理由の一つ）

### 線引きの原則

**「`SelectedCell` → `SelectedElement` の解決経路」まではβ、「`SelectedElement` から先の表示・
編集・通知」はγ**——この一線で切る。案Cはまさにこの線の内側で完結する。

---

## DoD(1)：増分βの分割案

**3段**に分ける。段ごとに検証パイプライン（隠密の静的レビュー→忍者の実機確認）を回す。

### β-1：`return` 欠落と `OpenFrameLabelEditor` の非対称を塞ぐ【バグ修正・挙動改善のみ】

- **対象**：`MainWindow.xaml.cs:1684-1696`（`return` 追加）、`:3903-3916`（`SelectedCell` の扱い）
- **検証観点**：
  - RED先行証明——`SelectedElement != null && SelectedFrame != null` を作り、`return` が無いと
    両方の `BeginDrag*` が走ることを実測する。**ViewModel 側で状態を組めるためテスト可能**
    （`_draggingElement`/`_draggingFrame` の同時非nullを検証）
  - 回帰：通常のドラッグ（要素のみ・枠のみ）が従来どおり働くこと
  - **`OpenFrameLabelEditor` の変更は枠ラベル編集の既存挙動を壊さないこと**（編集→確定→再編集）
- **規模見込み**：実装5行程度、テスト6件程度
- **この段だけは殿裁可を待たずに進められる**（挙動改善のみ、使用感は変わらない）と考えるが、
  判断は家老に委ねる

### β-2：3系統のチェーンを共通化する【挙動不変のリファクタリング】

> **【2026-07-28 全面改訂】** 旧版の設計（既存チェーンをそのまま1箇所へ寄せる）は
> **「壊していないこと」を確かめる手立てを持たぬ**と判明したため、設計を改めた。
> **併せて、旧版の目標「チェーン 3系統→1系統」は達成すべきでないと判ずるに至った**（下記）。

#### 【最重要・目標の訂正】「3系統→1系統」は誤った目標であった

旧版は「β完了時の目標＝チェーン 3系統→1系統」と書いた。**これは退けるべきにござる。**

**理由1＝3系統は判定順序が異なり、その違いには各々理由がある。**
系統Bの ConnectionDot→FreeLine 順には `MainWindow.xaml.cs:2005-2007` に
**「T-116（P-107対処、GuiEcad原本 `MainPage.Pointer.cs` 354行踏襲）。接続点は線の交点上に置かれる
ため自由線より先に判定する（逆順だと交点上の接続点が自由線の当たり判定に隠れ実質選択不能になる）」**
と理由が明記されておる。**1系統へ揃えれば、この理由を持つ順序が壊れる。**

**理由2＝3系統は「ヒット後にすることが」全く違う。**

| 系統 | ヒット後の処理 | 均質か |
|---|---|---|
| A（左Down） | `BeginDragConnector` / `BeginResizeImage` / `BeginDragElement` …**種別ごとに別のメソッド** | **不均質** |
| B（左Up） | `SelectedCell = null; SelectedXxx = hit;` **全種別で同型** | **均質** |
| C（右Down） | 種別ごとに**別のContextMenuを構築** | **不均質** |

**処理を括り出せるのは系統Bだけ**にござる。系統A・Cで括れるのは**順序の宣言**のみ。

**ゆえに改めた目標＝「重複の除去」ではなく「散在の集約」**——
**3つの順序表を1つのファイルへ集め、テストで固定する。チェーンは3系統のまま残す。**
利は「3つの順序を並べて見比べられる」ことと、**「順序が今どうなっておるかをテストが証言する」**ことにござる。

#### 設計＝**判断部分だけを純粋関数へ切り出す**（既存の前例を踏襲）

`samurai.md`「**『テストしにくい』は設計の匂い**」に従い、**`MainWindow` を触らずに済む形へ寄せる**。
**本プロジェクトには既に前例が3つある**——`ShouldSkipSelectionForRungCommentAreaClick`・
`ShouldOpenRungCommentEditor`・`ShouldSkipSelectionInTestMode` は、いずれも
**`MainWindow` の `static` 純粋関数として切り出され、テストが在る**。同じ形を採る。

**3層に分ける**——

| 層 | 中身 | テスト |
|---|---|---|
| 1 | **順序表**（系統ごとの `HitTargetKind[]` 定数） | **可**（配列の並びを直接検証） |
| 2 | **解決器**（順序表と「その種別が当たったか」を問う述語を受け、最初に当たった種別を返す純粋関数） | **可** |
| 3 | 実際の `HitTestXxx` 呼び出しと選択状態への代入（`MainWindow` 内） | **不可**（従来どおり実機確認へ委ねる） |

層2の署名案（実装時に詰める）：

```csharp
internal static HitTargetKind? ResolveFirstHit(
    IReadOnlyList<HitTargetKind> order,
    Func<HitTargetKind, bool> isHit)   // 遅延評価。当たった時点で打ち切り、後続は問わない
```

**遅延評価は必須**にござる——`HitTestXxx` を全種別ぶん先に走らせる形にすると、
**当たり判定の実行回数が変わる**（性能のみならず、将来ヒットテストに副作用が入った時に挙動が変わる）。
**「後続を問い合わせぬこと」自体をテストで固定する。**

#### この設計が回収するもの——**旧版で「書けぬ」とした検証観点が書けるようになる**

| 旧版の検証観点 | 旧設計 | 新設計 |
|---|---|---|
| 系統BのConnectionDot→FreeLine順（T-116）が保たれる | **書けぬ**（`MainWindow`をインスタンス化できぬ） | **書ける**——順序表の添字を検証。**入れ替えればRED** |
| 複数種別が同時にヒットした時、順序表の先頭が勝つ | **書けぬ** | **書ける** |
| 当たった時点で後続を問い合わせぬ | **書けぬ** | **書ける**（述語の呼ばれた回数を数える） |

**すなわちβ-2は「テストを増やすためのリファクタリング」でもある**——
**現状、3系統の順序を守る網は1件も無い。**

- **設計の要**：**3系統は判定順序が異なる**（下表）。**この違いは保つ。**
  **順序表を系統ごとの定数として持ち、置き場所だけを1箇所へ集める**

| 種別 | 系統A | 系統B | 系統C |
|---|---|---|---|
| Element | 7 | 暗黙 | **1** |
| Connector | 1 | 1 | 2 |
| Frame | 8 | **2** | 3 |
| WireBreak | 2 | 3 | **無** |
| ConnectionDot | 4 | 4 | **無** |
| FreeLine | 3 | **5** | **無** |
| Image | 5,6 | 6 | 4 |

  - **系統AとBでFreeLine/ConnectionDotの順序が逆**。系統Bの順序には
    **`MainWindow.xaml.cs:2005-2007`**（`b0645c3`時点）に「T-116(P-107対処)、逆順だと交点上の接続点が
    選択不能」と理由が明記されている。**この順序は壊してはならぬ**
  - 系統Cに WireBreak/ConnectionDot/FreeLine が無いのは非対称だが、**βで揃えるかは別途判断**
    （右クリックメニューの項目を新設することになり、範囲が広がる）。**本増分では順序のみ共通化し、
    「どの種別を対象にするか」は各系統の現状を保つ**ことを提案する

#### 検証観点【2026-07-28 改訂】

1. **順序表そのものを固定する**——系統Bで **ConnectionDot の添字 < FreeLine の添字**（T-116）。
   **RED証明＝順序表を入れ替えて実測する**（**旧設計では書けなかった観点。これが新設計の主目的**）
2. **解決器の決着**——複数種別が同時にヒットしたとき、**順序表の先頭にある方が勝つ**
3. **打ち切り**——当たった時点で**後続の種別を問い合わせぬ**（述語の呼出回数を数える）
4. **3系統それぞれの順序表が、現状のチェーンと同じ並びであること**——
   **これは「移し替えの正しさ」であり、目視でなく表で突き合わせる**
5. 決着ルールの混在（先頭一致／最短距離／最前面／面積最小の4種）は**共通化しない**——
   各 `HitTestXxx` の内部に閉じており、それぞれ理由がある（`LadderCanvas.cs:464-466`・`486-488`）
6. **【新設・偽GREENの警戒】既存2件の意味が失われぬか確かめる**——
   `T125BetaSelectionExclusivityTests` の `二重掴みで…` 2件は
   **左Down/左Upの分岐評価順序に依存した現状挙動を固定しておる**。
   **チェーンを「単一種別を返して打ち切る」形にすると、アサートは通り続けるが守る対象が消える**。
   **通ったことをもって「無傷」と報じてはならぬ**——この2件は**個別に意図を確かめ、
   必要なら層2のテストへ移し替える**
7. **【新設】`MainWindowViewModelTests` の `RightClickElementSelection_*`（`:648`・`:673`、10ケース）は
   系統Cの正規化をテスト側でコピー模擬しておる**——**共通化で本番の担当が変わっても、
   模擬したままのテストは通り続ける**。**β-2で本番の関数を直接呼ぶ形へ改める**

#### 触るファイルとテスト方針【2026-07-28 新設・DoD(1)】

| 区分 | ファイル | 見込み |
|---|---|---|
| 新規 | `src/Ecad2.App/Views/SelectionChain.cs`（仮称。`HitTargetKind` enum ＋ 3系統の順序表 ＋ `ResolveFirstHit`） | **60〜90行** |
| 変更 | `src/Ecad2.App/MainWindow.xaml.cs`（3系統を順序表経由へ差し替え） | **±80行前後** |
| 新規テスト | `tests/Ecad2.App.Tests/SelectionChainOrderTests.cs` | **18〜24件** |
| 変更テスト | `tests/Ecad2.App.Tests/MainWindowViewModelTests.cs`（観点7の付け替え） | **2メソッド（10ケース）** |

- **触るファイル＝実装2・テスト2の計4**。**`MainWindowViewModel.cs` には触れぬ**（β-2の範囲外）
- **既存の `HitTestXxx`（`LadderCanvas.cs` の `internal` 群）のシグネチャは変えぬ**——
  **`FreeLineConnectionDotHitTestOverlapTests` と `RungCommentHitTestTests` の計5件が
  これらを `internal` で直接呼んでおり、シグネチャを変えると即コンパイル不能**になるゆえ。
  **併せて `LadderCanvas.cs:457` の `RightBusXMm`（「テストのため存置」と明記済み）も消さぬ**
- **規模見込み**：**中**（旧版の見立て「実装60〜100行、テスト20件」から大きくは動かぬが、
  **内訳が『既存チェーンの再編』から『純粋関数の新設＋薄い差し替え』へ変わった**）

### β-3：要素の当たり判定を一本化する【**案C確定**（殿裁可2026-07-27）＝挙動変化】

> **【2026-07-28 改訂】** 案Cが裁可されたため具体化した。**併せて、正規化の置き場所に
> 落とし穴があると判明した**（下記「設計の要」）。

- **対象**：`MainWindowViewModel.cs:2779`（`HitTestElement`）を正とし、**`MainWindow.xaml.cs:1668`**
  （`b0645c3`時点。旧版 `:1684`）の手書き判定をこれへ寄せる。加えて左Upチェーンに Element 分岐を追加し、
  **ヒットしたら `SelectedCell` を要素のアンカー位置へ正規化**する

#### 【設計の要1】正規化をView層に置くと、またテスト不能になる

**β-2で学んだ轍をここでも踏まぬこと。** 左Upチェーンは `MainWindow` の中にあり、
**そこへ正規化を書けば `MainWindow` のインスタンス化なしには検証できぬ**——
**β-3の主眼（非アンカーセルで選択できる）そのものがテストできなくなる。**

**ゆえに正規化は `MainWindowViewModel` 側の純粋メソッドとして置く**。案＝

```csharp
// MainWindowViewModel（テスト可能）
public GridPos NormalizeToElementAnchor(GridPos pos)
    => HitTestElement(pos) is ElementInstance hit ? hit.Pos : pos;
```

**View層に残るのは1行だけ**にござる——`SelectedCell = _viewModel.NormalizeToElementAnchor(ToGridPos(position));`。
**これならRED証明が立つ**：`NormalizeToElementAnchor` を素通し（`=> pos`）に壊せば、
非アンカーセルのテストがREDになる。

#### 【設計の要2】配置モード中は正規化してはならぬ

系統Bの末尾は現状こうなっておる（`MainWindow.xaml.cs:2029-2030`、一次ソース確認済み）——

```csharp
_viewModel.SelectedCell = LadderCanvasHost.ToGridPos(position);
TryPlaceActiveTool();
```

**`TryPlaceActiveTool` は `Tool.Mode == PlaceElement` のときのみ働く**
（`:2381-2384`、`if (_viewModel.Tool.Mode != ToolMode.PlaceElement …) return;` を一次ソースで確認）。

**ここで無条件に正規化すると、配置モード中に幅広要素の非アンカーセルをクリックした際、
選択セルがアンカーへ吸い寄せられる。** 配置自体は占有チェック（`ValidatePlacement`）で弾かれるが、
**カーソル枠だけが勝手に左へ飛ぶ**——**殿が裁可された案Cは「選択の話」であって
「配置の話」ではない。** ゆえに——

- **正規化は `Tool.Mode == Select` のときに限る**
- **既存のプリミティブ選択チェーン（Connector/Frame/WireBreak/…）も同じ
  `Tool.Mode == Select` ガードの内側にある**（`:1980`）。**案Cの分岐も同じガードの内側に置けば、
  形が既存と揃う**

**実装の形は2案**（実装時に決める。**いずれも挙動は同じ**）——

| 案 | 形 | 評 |
|---|---|---|
| **(A) 推奨** | `Tool.Mode == Select` ブロックの**内側**に Element 分岐を追加し、ヒットしたら `SelectedCell = hit.Pos; return;` | **既存6分岐と形が揃う**。ただし `SelectedCell = null` を先に呼ぶ既存パターンとは異なる（Elementは`SelectedCell`経由で解決されるゆえ null 化は不要かつ有害） |
| (B) | 末尾の `ToGridPos` の結果を、Selectモード時のみ `NormalizeToElementAnchor` に通す | 差分は最小（1〜2行）だが、**チェーンの一部であることが読み取りにくい** |

#### 検証観点

1. **RED先行証明**——`CellWidth=3` の要素を置き、非アンカーセル（+1、+2）を指したとき
   **`NormalizeToElementAnchor` がアンカーを返すこと**。**素通しに壊せばRED**
2. **`SelectedElement` が非nullになること**——正規化後の位置で完全一致が解けること
   （**`SelectedElement` の定義は変えぬ**のが案Cの肝）
3. **UIA 経路（#4 `LadderCanvasAutomationPeer.cs:65`・#5 `SymbolAutomationPeer.cs:80`）が
   従来どおり選択を報告すること**——正規化により**完全一致のままで正しく解ける**ことの確認。
   **案Aを退けた最大の理由がここゆえ、必ず観点として残す**
4. **配置モード中は正規化されぬこと**（設計の要2）——**`Tool.Mode` を変えて対照する**
5. 回帰：`CellWidth=1` の要素、要素の無いセル、境界（要素の右端+1セル＝**当たらぬこと**）
6. **入力の対称性・退化性**（`samurai.md`）——`CellWidth=1` は「アンカー＝全域」ゆえ正規化の
   有無が結果に現れない**退化ケース**。**必ず `CellWidth>=2` を主検体とする**。
   **加えて、要素を列0に置かぬこと**——**アンカー列が0だと「正規化されて0になった」のか
   「もともと0だった」のか弁別できぬ**（同節「対称・軸上・退化した値を避ける」の適用）
7. **手書き判定（#3 `MainWindow.xaml.cs:1668`）を `HitTestElement` へ寄せた後、
   ドラッグ開始が従来どおり働くこと**——**`T088ElementMoveTests`（11件）が回帰網**

#### 触るファイルとテスト方針【2026-07-28 新設・DoD(1)】

| 区分 | ファイル | 見込み |
|---|---|---|
| 変更 | `src/Ecad2.App/ViewModels/MainWindowViewModel.cs`（`NormalizeToElementAnchor` 新設） | **+5行程度** |
| 変更 | `src/Ecad2.App/MainWindow.xaml.cs`（系統Bへ分岐追加、系統Aの手書き判定を `HitTestElement` へ寄せる） | **±20行** |
| 新規テスト | `tests/Ecad2.App.Tests/T125BetaAnchorNormalizationTests.cs`（仮称） | **12〜16件** |

- **触るファイル＝実装2・テスト1の計3**
- **`SelectedElement` の定義（`:2101` の完全一致）は変えぬ**——**案Cの中核**にござる
- **UIA 2ファイルには触れぬ**（案Cを採る限り無影響。**触らぬこと自体が案Cの利**）
- **規模見込み**：**小〜中**。旧版の「実装20行程度、テスト12件程度」から大きくは動かぬ

### 段の順序と理由

**（旧版）β-1 → β-2 → β-3**。β-1は挙動改善のみで独立、β-2は挙動不変、β-3のみ使用感が変わる。
**判断を要するものを最後に置く**ことで、殿の裁可を待つ間もβ-1・β-2を進められる。

#### 【2026-07-28 改訂】**β-1完了・案C裁可により前提が変わった。順序の入れ替えを具申する**

旧版が β-3 を最後に置いた理由は**「殿の裁可を待つ間も他を進められるから」**であった。
**その裁可は既に下りた。** ゆえに理由が失効しておる。

**具申＝β-3 → β-2 の順に入れ替える**（**β-1は完了済み**）。

| | β-3を先にする（**具申**） | β-2を先にする（旧版） |
|---|---|---|
| 価値の出方 | **早い**。P-077の実害（幅広部品を左端以外で選べぬ）が先に解消する | 遅い。β-2は内部改善ゆえ殿の目には何も変わらぬ |
| 切り分け | **易しい**。小さな挙動変更（β-3）を先に済ませ、**その後に大きな構造変更（β-2）を単独で行う** | **難しい**。大きな構造変更の直後に挙動変更を重ねると、不具合が出た時にどちらが原因か判じにくい |
| 手戻り | **小さい**。β-2は「その時点で在る分岐を順序表へ移す」作業ゆえ、**分岐が1つ増えても手間はほぼ変わらぬ**（順序表に1項目足すだけ） | 小さい。β-3の分岐が最初から順序表の枠組みに収まる |

**決め手は2行目**にござる——**構造変更と挙動変更は分けるのが筋**。
**「構造を整えてから足すほうが綺麗」という利（3行目）よりも、
「壊れた時にどちらが原因か即座に判る」という利を採る。**

**ただしこれは侍の具申であり、家老・殿の裁定に従う。**

---

## 【2026-07-28 新設】P-142（両立状態の是非）を併せて判ずべきか——侍の所見

> 家老の下知 DoD(2)＝「**P-142をβ-2/β-3で併せて判ずるべきか否かの所見**」への回答にござる。
> P-142起票時の家老の見立ては「**β-2/β-3で選択解決を一本化する際に併せて判ずるのが安い**」であった。

### 結論＝**「併せて判ずる」ではなく「β-2の設計で構造的に狭め、根絶の是非はγで判ずる」を具申する**

**ただしβ-2の設計判断に1点だけ織り込むことを条件とする**（下記「条件」）。

### 理由1＝β-2・β-3のいずれも、両立状態を解消せぬ

**P-142の中身**＝`SelectedFrame`≠null かつ `SelectedElement`≠null が両立しうること。
**`SelectedElement` は `SelectedCell` の派生プロパティ**（`MainWindowViewModel.cs:2101-2102`）ゆえ、
これは実質**「`SelectedCell`≠null かつ `SelectedFrame`≠null」**と同義にござる。

- **β-3（案C）は `SelectedCell` の値を正規化するだけ**で、`SelectedFrame` との排他には**一切触れぬ**。
  **両立状態は寸分も減らぬ**
- **β-2は「順序の集約」であって「選択状態の設計変更」ではない**（上記の目標訂正のとおり、
  処理を括り出せるのは系統Bのみ）

**すなわち「一本化する際に併せて」という前提が、そもそも成り立たぬ**——
**β-2/β-3で一本化されるのは「解決の順序」と「当たり判定の式」であって、「選択状態そのもの」ではない。**

### 理由2＝根絶には別軸の設計変更が要り、それはγの領分

両立状態を**構造的に不可能**にするには、**`Selected*` 群（Cell/Connector/Frame/WireBreak/
FreeLine/ConnectionDot/Image の7種）を単一の選択状態へ束ねる**——
すなわち**「今どれか一つだけが選ばれている」ことを型で表す**設計へ移す要がある。

これは**`MainWindowViewModel` の責務分割そのもの**であり、**旧版DoD(4)で引いた線
「`SelectedCell`→`SelectedElement` の解決経路まではβ、そこから先はγ」の、明確にγ側**にござる。
**現状の排他は `SelectedCell` の setter が7種を手で潰す形**（`MainWindowViewModel.cs:415-460`）で
保たれており、**「手で潰す」限り潰し漏れは必ず起きうる**——β-1で塞いだ
`OpenFrameLabelEditor` の一件がまさにそれであった。

### 理由3＝現に、両立を作る経路はもう「チェーン外」しか残っておらぬ

- **β-1で `OpenFrameLabelEditor` の排他規約違反は塞いだ**
- 残る経路は**隠密の6段手順＝「Select以外のモードで両立を作り、Selectへ戻す」**——
  **チェーンの外側**にござる
- **ゆえにβ-2でチェーンをいくら綺麗にしても、この経路には届かぬ**

### 条件＝**β-2の設計で「単一種別を返して打ち切る」形を必ず採ること**

これがγでの根絶を安くする**唯一の織り込み**にござる。
上記β-2の層2（`ResolveFirstHit` が `HitTargetKind?` を返す）は、
**「チェーンの結論は常に高々一つ」**という形を型で表しておる。
**γで選択状態を束ねる際、この戻り値がそのまま「選ばれた一つ」の入力になる。**

**逆に、β-2で「複数種別を返しうる」形や「各分岐が直接 `Selected*` へ代入する」形を残すと、
γでの束ね直しが高くつく。**

### 併せて＝**β-2の検証観点6が、そのままP-142の見張り番になる**

β-1で置いた `二重掴みで要素を先に確定すると枠のドラッグは巻き戻される` は、
**P-142の「実害が吸収されておる」という前提が失われた時にREDになる網**として置かれたもの。
**β-2でチェーンの形を変える際、この網が偽GREEN化せぬかを必ず確かめる**（観点6に明記済み）。
**網を失えば、P-142は「pending」のまま牙だけ研がれることになる。**

### まとめ（家老への回答）

| 問い | 侍の答 |
|---|---|
| β-2/β-3で併せて判ずべきか | **否**。両段とも両立状態には触れぬゆえ、「併せて」の前提が成り立たぬ |
| ではいつ判ずるか | **γ**（`MainWindowViewModel` の責務分割）。**`Selected*` 7種を束ねる設計判断と不可分** |
| βで何をすべきか | **β-2で「単一種別を返して打ち切る」形を採ること**（γを安くする織り込み）＋**既存の網を偽GREEN化させぬこと** |
| P-142の記載を改めるべきか | **「β-2/β-3で併せて判ずるのが安い」→「γで判ずる。βでは構造的に狭めるに留める」へ改める**ことを具申する（**家老の起票文の更新ゆえ、家老の裁量にて**） |

---

## 副次所見（本増分の範囲外。家老の判断を仰ぐ）

1. **`HasNoPropertySelection` の変更通知が3箇所で欠落している疑い**——`SelectedElement` の変更通知
   ブロック4箇所のうち、`SelectedCell` setter（`:467-480`）だけが `HasNoPropertySelection` を
   発火し、`DeleteSelectedElement`（`:2434-2446`）・`NotifySelectedElementChanged`（`:2569-2581`）・
   `ReplaceDocument`（`:3222-3234`）の3箇所は発火しない。**要素削除後にプロパティパネルの
   「選択なし」表示が更新されない可能性**がある。
   **【未確認】侍は行番号と欠落の事実のみ確認しており、実機での表示崩れは未検証。**
   `memory: feedback_checklist_layer_mismatch_blindspot`（T-107で同型が起きた）と同じ匂いがする。
   **γで扱うか、別建てで起票するかを家老に諮りたい**
2. **右クリックチェーンに WireBreak/ConnectionDot/FreeLine が無い非対称**——過去に Image（T-064）・
   Frame（T-067(5)）が同じ形で後から追加された記録がある（`MainWindow.xaml.cs:2153-2156` のコメント）。
   **同型の横展開漏れが3種残っている**と読めるが、**メニュー項目の新設を伴うため使用感の設計判断**。
   βには含めず、**別途起票が筋**と考える

---

## 【2026-07-28 新設】案Cの妥当性が実測で裏づけられた——**右クリック経路には既に在る**

**旧版の「利3＝既存の対処と同型」は、当時は状況証拠であった。今回、一次ソースで確証を得た。**

`tests/Ecad2.App.Tests/MainWindowViewModelTests.cs:642-665` の
`RightClickElementSelection_OnAnyOccupiedCell_ResolvesDeviceNameToSameElement` は、
**まさに案Cと同じことを行っておる**——

```csharp
// 右クリックハンドラの正規化ロジック(MainWindow.xaml.cs)を模擬: HitTestElement→アンカー位置。
var hit = vm.HitTestElement(clickedPos);
vm.SelectedCell = hit!.Pos;          // ← これが案Cの正規化そのもの
```

**すなわちβ-3は「新しい考え方を持ち込む」のではなく、
「右クリックで既にやっておることを、左クリックにもさせる」だけ**にござる。
**非対称を作るのではなく、非対称を消す変更**である——**案Cを推す理由がもう一つ増えた。**

**併せて、テストの入力値の選び方も既存に倣える**——同テストは
`CellWidth=2,3` × アンカー/中間/上限（`[InlineData(2,0)(2,1)(3,0)(3,1)(3,2)]`）を取り、
**要素を `GridPos(0, 1)`＝列1に置いておる**（**列0でない**）。
**上記β-3検証観点6で「列0に置くな」と書いたが、既存テストは既にその配慮を持っておった。**

---

## 未確認・留保（断じておらぬ事柄）

> **【2026-07-28】旧留保2・3は回収した。取り消し線でなく、回収の経緯を残す。**

1. **実機での再現は依然として未確認**——**P-077の症状は実機で確かめておらぬ**。
   β-1の実害（要素と枠の二重掴み）も**忍者の実機確認が未了**。本計画は静的読解のみに立脚する
2. ~~`OpenFrameLabelEditor` の編集終了処理が `SelectedFrame` をクリアするか否かは未読~~
   → **【回収済み】β-1着手時に一次ソースを読み、対処を確定して実装した**（`2df5ab6`）
3. ~~`tests/` 配下に既存のヒットテスト優先順位テストがどこまであるかは未調査~~
   → **【回収済み・2026-07-28】棚卸しを行い、結果が本改訂の根幹になった**——
   **チェーン順序を固定するテストは1件も無い**。**核心の2点は侍自身が一次ソースで裏を取った**＝
   (a)`FreeLineConnectionDotHitTestOverlapTests.cs:8-14` のクラスコメントが
   **「MainWindow自体のインスタンス化（AvalonDock初期化等の重い副作用を伴う）を要するため単体テスト不可
   （RED証明不可・View層依存、samurai.md該当節）」「実際の優先順位確認は忍者実機確認に委ねる」**と
   明記しておること (b)`MainWindowViewModelTests.cs:656` が
   **「右クリックハンドラの正規化ロジック(MainWindow.xaml.cs)を模擬」**と明記しておること
4. **副次所見1の実害**（プロパティパネルの表示崩れ）は未検証
5. **【新設】テスト棚卸しの網羅性**——観点1〜3の列挙はサブエージェントへ委譲した結果であり、
   **侍が全テストの本文を読んだわけではない**。**上記(a)(b)の核心2点のみ自分で裏を取った**。
   **「危険度」の判定はβ-2の設計を仮定した上での見立てであり、実際の設計次第で変わる**
6. **【新設】β-2の層2（`ResolveFirstHit`）の署名は案であり、確定ではない**——
   **遅延評価をどう表すか（`Func<HitTargetKind,bool>` か、種別ごとの `Func<object?>` か）は
   着手時に決める**。**「後続を問い合わせぬこと」をテストで固定する**という要件のみ確定
7. **【新設】β-3の実装形（案A／案B）は未確定**——**挙動は同じゆえ、着手時に一次ソースを見て決める**

---

## 出典

> **行番号は `b0645c3` 時点。腐るゆえシンボル名を併記する**（`karo.md`非同期委譲10項）。

### 実装（一次ソース、侍が直読）

- `src/Ecad2.App/MainWindow.xaml.cs`
  — 系統A `:1480`（`LadderCanvasHost_PreviewMouseLeftButtonDown`）／
  系統B `:1833`（`…MouseLeftButtonUp`、**選択チェーン本体は `:1968-2031`**）／
  系統C `:2055`（`…MouseRightButtonDown`）／
  手書き区間交差 `:1668`（`elemHitPos` / `dragElem`）／
  `OpenFrameLabelEditor` `:3886`／`TryPlaceActiveTool` `:2381`（**`PlaceElement`時のみ働くことを確認**）／
  T-116の順序理由コメント `:2005-2007`／`Tool.Mode == Select` ガード `:1980`
- `src/Ecad2.App/ViewModels/MainWindowViewModel.cs`
  — `SelectedElement` `:2101-2102`（完全一致）／`HitTestElement` `:2779-2783`（区間交差）／
  `SelectedCell` setter `:415-460`（7種の `Selected*` を手で潰す排他）
- `src/Ecad2.App/Views/LadderCanvas.cs`（`HitTestXxx` 群の定義と決着ルール、`RightBusXMm` `:457`）
- `src/Ecad2.App/Views/LadderCanvasAutomationPeer.cs:65`・`Views/SymbolAutomationPeer.cs:80`
  （**従来の数え方から漏れていた完全一致2箇所**。**案Cを採る限り無影響**）

### テスト（棚卸し。**核心2点は侍が直読、他は委譲調査**）

- `tests/Ecad2.App.Tests/FreeLineConnectionDotHitTestOverlapTests.cs:8-14`
  （**クラスコメントが「単体テスト不可・実際の優先順位確認は忍者実機確認に委ねる」と明記**。**侍が直読**）
- `tests/Ecad2.App.Tests/MainWindowViewModelTests.cs:583-626`（`HitTestElement_*` 4件）・
  `:642-684`（`RightClickElementSelection_*` 2件×5ケース。**`:656` の「模擬」コメントを侍が直読**）
- `tests/Ecad2.App.Tests/T125BetaSelectionExclusivityTests.cs`（**β-1で侍が新設**、5件）
- `tests/Ecad2.App.Tests/RungCommentHitTestTests.cs`・`RungCommentSelectionGuardTests.cs`・
  `RungCommentDoubleClickTests.cs`・`TestModeSelectionGuardTests.cs`
  （**純粋関数として切り出しテストを置いた既存の前例3件**）

### 文書

- `docs/ecad2-t125-beta-p077-investigation-onmitsu.md`（隠密のβ前段調査）
- `docs/ecad2-t125-increment-alpha-plan-samurai.md`（α計画書、数え方の定義と留保5の初出）
- `docs/proposed.md:154`（**P-142の起票文**。家老起票2026-07-27）
- `docs-notes/handover-next-session.md`（β-1の完了状況・案C裁可の記録）
