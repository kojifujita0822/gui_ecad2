# T-133 増分2 実機確認（忍者、2026-07-28）

対象＝`ee4d0d9`「増分2 - `CellHeight`の器を新設（殿裁定11の土台）」。
**性質上「限定的」で足りる**（家老采配）——**既存ファイル・自作パーツが今までどおり開けるか**の確認。

**操作手法**：UI Automation＋合成キー送出。**判定はJSONの直読による往復突合**（撮影に依らぬ）。
**検証バイナリ**＝DLL 16:48:18／プロセス起動 16:48:20、`ee4d0d9`はHEADに含まれる。

---

## 0. 結論——**回帰なし。読み書きとも情報の欠落なし**

| 観点 | 判定 |
|---|---|
| 既存`.gcad`が開けるか | **OK**（`big_sample.gcad`、35要素・埋め込みパーツ2件） |
| 描画が崩れておらぬか | **OK**（母線・接点・コイル・行コメント・グループ枠・接続点とも正常） |
| 往復（開く→保存）で情報が落ちぬか | **OK＝12項目すべて一致** |
| 自作パーツ（`.gcadpart`）が読めるか | **OK＝ディスク18件すべて** |

---

## 1. 往復突合（本検証の要）

**「開ける」だけでは弱い**ゆえ、**開いた後そのまま別名保存し、元のJSONと突き合わせた。**

| 項目 | 元 `big_sample.gcad` | 往復後 `t133inc2.gcad` |
|---|---|---|
| schemaVersion | 1 | **1** |
| シート | 1 | **1** |
| 要素 | 35 | **35** |
| コネクタ | 5 | **5** |
| グループ枠 | 2 | **2** |
| 行コメント | 11 | **11** |
| 接続点・自由線・配線分断 | 0 / 0 / 0 | **0 / 0 / 0** |
| 埋め込みパーツ | 2 | **2** |
| devices | 0 | **0** |
| グリッド | 24行 x 11列 | **24行 x 11列** |

**12項目すべて一致。**

### 1-1. `cellHeight` の振る舞い（増分2の的）

| | `cellHeight` キーを持つ要素 | 値 |
|---|---|---|
| **元（旧形式）** | **0 / 35** | — |
| **往復後** | **35 / 35** | **すべて `1`** |

**すなわち「旧形式（キー無し）を読むと既定1が入り、書き出すと明示的に`1`が付く」**——
**侍がテストで固定した旧ファイル互換が、実機の往復でも成り立っておる。**

---

## 2. 自作パーツ側

**ディスク上18件**（`図形`直下15＋`自作`3）が**すべて部品リストに載った**
（リスト表示は20件＝18＋`ORa接点`/`ORb接点`の派生2件）。

---

## 3. 【自己訂正】期待値を一度誤った——入れ子の数え違い

**「devices = 1件」と見積もったが誤り。実体は `devices.byName` が空＝0件**にござった
（`$o.devices`直下のプロパティ＝`byName`のみを数えたゆえ）。
**機器表が0行であったのは正常**であり、**危うく「読み込み不全」と誤報するところであった。**

**同じ誤りを本日2度犯しておる**——`library`でも`byId`直下を数えず「1件」と出し、
構造を確かめて2件と訂正した。**JSONの入れ子は、キーを1つ下りねば数が合わぬ。**
`memory: feedback_disclose_counting_unit`（数は「何を1と数えたか」とセットで出す）の実地版にござる。

---

## 4. 【重要】検証中に殿の`main-layout.xml`が上書きされた——退避が効いた初の実例

**`Stop-Ecad2App` が `Ecad2.App stopped (graceful close)` を返し、`main-layout.xml` が
2387→2445バイトへ書き換わった**（`2073FD82…`→`88C377F3…`）。

**機序**＝**本検証では途中で「名前を付けて保存」を行ったゆえ未保存でなくなり、確認ダイアログが出ず、
正規終了がすんなり通った**。その結果 `Window_Closing`→`SaveDockingLayoutAsDefault` が走った。

**同日4回の起動のうち、保存を挟まなんだ3回はWARNING（強制終了）で無傷**であった——
**「WARNINGゆえ無傷」は状態依存の幸運にすぎなんだ。**

**対処**＝検証前に退避してあったバックアップから**復元し、ハッシュ一致まで確認済み**
（`2073FD825E887960`、2387バイト、更新時刻 2026/07/27 15:41:48）。
**上書き後の版も証跡として`scratchpad\main-layout-overwritten-by-ninja-20260728.xml`へ保存した。**

**スキルへ落とし済み**（3節`Stop-Ecad2App`の項＋「検証を始める前に退避する」【MUST】節）。

---

## 5. 範囲外として検出（自らタスク化はせぬ）

1. **`cellHeight` が書き出されるようになっても `schemaVersion` は `1` のまま**。
   **旧版のecad2が新しいファイルを開けるか（前方互換）は未確認**——
   未知キーを無視する実装なら開ける道理なれど、**実測しておらぬゆえ断じ申さぬ。**
2. **残置物**＝`C:\ECAD2\sample\t133inc2.gcad`（18,154バイト、往復突合の検体）。
   **数値は本書に記録済みゆえ不要**（`.gitignore`済みでgitは汚れぬ）。`rm`禁止ゆえ忍者は消さず
