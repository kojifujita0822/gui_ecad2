# T-133増分5 実機確認記録（忍者）

**検体**＝コミット `68e24c6`（隠密の静的レビュー指摘2件を侍が直した版）
**日時**＝2026-08-05
**環境**＝ウィンドウ1400x800・ズーム100%・セカンダリモニタ（`Left=1960`）・主回路シート2枚
**操作手法**＝UIA（`InvokePattern`／`ExpandCollapsePattern`／`SelectionPattern`）＋
合成マウスイベント（`Invoke-Ecad2CanvasClick`／`Invoke-Ecad2ScreenClick`＝Win32 `mouse_event`）
＋合成キー送出（`Send-Ecad2Keys`）。**殿の実操作は用いておらぬ。**

**【同日の別件】`P-169`（要素削除時にプレースホルダが出ぬ）の実機確認は
`docs/ecad2-p169-placeholder-verification-ninja.md` に別立てとした**
——**本増分とは無縁の既存の穴ゆえ**（隠密の起票、家老の采配で記録を分離）。

---

## 1. 結論

**【初報から訂正あり】観点2は未達。** 他の観点はPASS・回帰なし。

| # | 観点 | 結果 |
|---|---|---|
| 1 | ComboBoxの出現条件 | **PASS**（4経路すべて） |
| 2 | 即時反映 | **【訂正】未達**——値は即座に変わるが**絵はキャンバスのクリックまで更新されぬ**（4-1） |
| 3 | 形状の描き分け（三分割） | **PASS**（PDF直読で裏取り） |
| 4 | Undo（三通りの非対称） | **PASS** |
| + | 隠密の追加観点（再選択時の再出現） | **PASS** |

**初報では観点2を「PASS」と記したが、それは`ComboBox`の表示値のみを見たものにて誤りであった**
（家老の下問で露見。経緯は4-1節）。**家老の裁定＝範囲内の欠陥、侍へ修正を采配。**

**【侍の静的読解との突合】**（家老経由、某の実測の後に伝えられたもの）——
**TextBox系6件（`DeviceNameBox`・`CommentBox`・`NotchPositionBox`・`LampColorBox`・`SetpointBox`・
`LabelDyBox`）は`LostKeyboardFocus`／`Enter`で`CommitDeviceNameEdit()`を呼び、そこで
`RedrawCanvas()`を明示的に叩く。ComboBoxはこの経路に届かず、かつ固定リストにも載らぬ＝両経路から漏れた。**
**某が代わりの対照に選んだ`LabelDyBox`は、その6件の一つであった。**
**某の実測と侍の静的読解は独立に一致しており申す。**

---

## 2. 着手前に一次ソースで固めた期待値

**家老の采配文は観点3を「NFB/MCCBで絵が実際に描き分けられておるか」と記していたが、
一次ソースを当たると形状の描き分けは `ELB` のみであった。**

- `SymbolGlyphs.cs:288-309`＝`Breaker3P` は `variant == "ELB"` の時のみテストボタン（小四角）を足す。
  **NFBとMCCBは同じ基本形状（288-306行の弧・端子○）を通る**
- 両者の区別は記号脇のテキストラベルのみ（`DiagramRenderer.cs:1044-1058`）

**ゆえに「NFB→MCCBで絵が変わらぬ」は不発ではなく期待値**にござる
（`ninja.md`「対照で差が出ぬことが期待値である場合がある」）。
**これを知らずに実機へ入っておれば、誤報しておった。**
**侍も同じ箇所を独立に読み返し、見立ての一致を確認済み。**

---

## 3. 観点別の実測

### 観点1：ComboBoxの出現条件

`BreakerTypeCombo`（Name＝「ブレーカ種別」）。**パネル初のComboBox**。

| 選択対象 | ComboBox | 備考 |
|---|---|---|
| Breaker3P | **PRESENT**（既定`NFB`） | 選択肢3件＝NFB／MCCB／ELB |
| 空セル | **absent** | |
| a接点（X1） | **absent** | `DeviceNameBox`は`X1`を表示 |
| a接点 → Breaker3P | **PRESENT** | **要素間の遷移でも再出現** |

**既定表示`NFB`は`BreakerTypeOptions.Default`（`Element.cs:21`）と一致。**

**隠密の追加観点**（別セル経由での再選択時に出現するか＝`SelectedCell` setterの通知漏れ修正）は、
**空セル経由・他要素経由の双方でPASS。** 修正の裏づけとなる。

### 観点2：即時反映

**ComboBoxの値は即座に変わる。** 一方**キャンバスの絵は、選択を外すまで更新されぬ**（4-1参照）。

### 観点3：形状の描き分け（三分割）

| 遷移 | 期待値 | 実測 |
|---|---|---|
| NFB → MCCB | ラベルのみ変化・**形状不変** | **一致** |
| MCCB → ELB | **テストボタン出現** | **一致** |
| ELB → NFB | **テストボタン消失** | **一致** |

**PDF直読による裏取り**（`sample/t133i5elb.pdf`、`re` オペレータ24件を全数走査）——

- **正方形はPDF全体で1件のみ**＝`x=61.739 y=342.510 w=6.633 h=6.633`。
  **ELBを1つだけ置いた検体と整合し、NFB側にテストボタンが無いことも同時に示す**
- **理論値との突合**：

  | 項目 | 根拠 | 値 |
  |---|---|---|
  | `DefaultCellWidth(Breaker3P)` | `ElementCatalog.cs:12` | 2セル |
  | `CellMm` | `DiagramRenderer.cs:9` | 9.0 |
  | 記号幅 | 2 × 9.0 | 18.0mm ＝ 51.024pt |
  | `k` | 幅 ÷ 2 | 25.512pt |
  | **テストボタン一辺（理論）** | `SymbolGlyphs.cs:309` の `±0.13` ＝ 0.26k | **6.6331pt** |
  | **同（実測）** | PDFコンテンツストリーム | **6.633pt** |

  **差は0.001pt未満。**

**目視の断定は避けた**（家老の【MUST】）。一辺6.633pt＝画面上ごく小さく、
**4倍拡大画像での確認とPDF数値の二軸で判じた。**

### 観点4：Undo（三通りの非対称）

**測り方**＝Undoを1回だけ送り、**何が取り消されたか**を直に見る
（「回数を数える」より強い指標。`IsEnabled`では測れぬ罠を回避＝スキル6.2節末尾）。

| # | 操作 | 期待値 | 実測 |
|---|---|---|---|
| (a) | 未編集要素で**NFB**（同値）を選ぶ | 履歴を積まぬ | **Undoで「配置」そのものが取り消された**＝積んでおらぬ |
| (b) | 未編集要素で**MCCB**を選ぶ | 履歴+1 | **Undoで種別のみNFBへ戻り、要素は残った** |
| (c) | **MCCB → NFB** | 履歴+1 | **Undoで種別がMCCBへ戻った** |

**(a)が本観点の核心**にござる——**「何も起きぬことが正しい」という侍の設計意図は、
ラベルにもVM単体テストにも現れず、Undoでしか測れぬ。**

---

## 4. 範囲外の所見（2件）

**いずれも「範囲外として検出」。起票の要否は家老の判断に委ねる。**

### 4-1. 種別を変えても、キャンバスのクリックまで絵が更新されぬ

**【重要・観点2の判定を訂正した】** 初報では観点2を「即時反映＝PASS」と記したが、
**それは`ComboBox`の表示値のみを見たものにて、絵は含めておらなんだ**（家老の下問により露見）。
**己で選んだ値が己で選んだ欄に映るのを見ただけにて、即時反映の証としては弱い。**
**絵については「即時反映は満たされておらぬ」が正しい。**

**再検証（4経路の剥がし＋対照）＝下記。**

| # | 経路 | 絵が戻るか |
|---|---|---|
| 1 | **選択したまま9秒待つ** | **戻らぬ**（時間の線を否定） |
| 2 | **ウィンドウを最小化して復元**（描き直し） | **戻らぬ**（WPFの再描画では不足） |
| 3 | **`Enter`を押す** | **戻らぬ** |
| 4 | **同じセルを再クリック** | **戻る**（**選択解除は要らぬ。赤枠は残ったまま**） |
| 5 | 別要素を選ぶ | 戻る |

**引き金は「キャンバス上のクリック」にござる**——選択解除でも、時間でも、確定操作でもない。

**【家老の追加観点＝ラベル文字とテストボタンは分かれるか】**
**分かれぬ。同じ引き金で同時に戻る**（経路4で両者が一度に現れた）。**描画の層は分かれておらぬ。**

### 4-2. 【対照】機器名は即時反映される ── 侍の静的読解と食い違う

**素朴なベースライン**（`memory: feedback_control_experiment_needs_naive_baseline`）として、
**既存プロパティ（機器名）で同じ症状が出るかを測った。**

- a接点の`DeviceNameBox`を物理クリック→`Ctrl+A`→`X99`→`Enter`
- **選択を外さぬまま**撮影 → **絵のラベルが`X1`から`X99`へ変わっておった**

**すなわち機器名は即時反映される。**

**侍の静的読解**＝`MainWindow.xaml.cs:823-841`の`ViewModel_PropertyChanged`が持つ再描画リストに
`SelectedElementXxx`系は一つも載っておらぬ（grep0件）ゆえ「既存の一群が共有する性質」——
**この結論とは食い違う。**

**某が言えるのは実測のみ**——**機器名は即時反映され、種別は反映されぬ。**
**機器名の反映が`PropertyChanged`経由か`Enter`確定のハンドラ経由かは、某には分かり申さぬ。**
**侍の「リストに載っておらぬ」という静的事実は正しくとも、別経路で再描画されておる公算がある。**

**【断り】某はこの再検証を、侍の読解を見た後に行うており申す**（独立ではない）。

### 4-3. 【追加検証】どの項目が即時反映されるか ── 家老の二択の外に答えがあった

**家老の追加下命**＝「`SelectedElementNotchPosition`（セレクトSWのノッチ位置）は即時反映されるか。
**反映されれば種別だけが穴＝範囲内／反映されねば既存にも同じ穴＝範囲外**」。

**結果＝どちらでもない。ノッチ位置は元より絵に出ぬ項目にござった。**

- `NotchPositionBox`は**TextBox**（ComboBoxではない）。値は`2`へ確かに入る
- **選択したままでも、キャンバスをクリックしても、絵は一切変わらぬ**
- **一次ソース＝`Ecad2.Core`（描画層）に`NotchPosition`の参照は0件。** App層（VM・XAML）のみ
- **実測とソースの二軸が一致**——**この観点では即時反映を測れ申さぬ**

**代わりの対照＝`LabelDyBox`（ラベル高さ調整）。** `DiagramRenderer.cs:1142`で描画に使われており、
`0`→`8`と変えたところ**選択を外さぬままラベルが上へ動いた＝即時反映される。**

| 項目 | 形 | 絵に出るか | 即時反映 |
|---|---|---|---|
| 機器名 | TextBox＋Enter | ○ | **される** |
| ラベル高さ | TextBox＋Enter | ○（`DiagramRenderer.cs:1142`） | **される** |
| ノッチ位置 | TextBox＋Enter | **×**（`Core`で参照0件） | **測れぬ** |
| **ブレーカ種別** | **ComboBox** | ○ | **されぬ** |

**絵に出る既存プロパティは二件とも即時反映され、反映されぬのは種別のみ。**

**【射程・某が自ら区切る】**
- **機器名・ラベル高さは`TextBox`＋`Enter`確定、種別は`ComboBox`選択にて操作の形が違う。**
  **「ComboBoxという新しい形が既存の再描画経路に繋がっておらぬ」という見方もできる**
- **パネル初のComboBoxゆえ、比較できる既存のComboBox項目は存在せぬ**——**対照の片側が無い**
- ゆえに某が申せるのは**「絵に出る既存項目は二件とも反映され、種別は反映されぬ」**まで。
  **「ComboBoxゆえか、増分5ゆえか」は分けておらぬ。**

### 4-4. `P-036`との切り分け

**引き金が異なる。**
- `P-036`＝**配置ツール使用中**に絵が見えず、**選択ツールへの切替**で表示される
- 本件＝**選択ツール使用中**に、**キャンバスのクリック**で戻る

**ただし本セッションで`P-036`自体は再現しておらぬゆえ、「同一機構でない」とまでは申せぬ。**
**言えるのは「引き金が違う」までにござる。**

### 4-2. 機器表（`DeviceTableGrid`）は3極記号を捉えぬ

3極記号を配置しても**機器表の行数は増えぬ**（a接点は増える＝1行）。

**某はこれを知らず「機器表0行＝配置されておらぬ」と誤判定し、数手を空費した。**
**スキル6.8節【MUST】は「結果側の指標（機器表の行数／キャンバスの絵／保存ファイル）で判ずる」と
説くが、機器表は要素の種類によっては指標にならぬ。**
**落とし先＝`ecad2-ui-automation`スキル6.8節への射程の限定**（家老の裁可を仰ぐ）。

---

## 5. 某の誤りと訂正（2件、いずれも自ら気づいて訂正）

**記録として残す。いずれも報告前に訂正しており、家老・侍への誤報には至っておらぬ。**

1. **「機器表0行ゆえ配置されておらぬ」と判じた**——真は4-2のとおり。
   `Enter`経路で「選択したセルには既に要素があります」と出て初めて割れた。
   **切り分けの決め手は経路を替えたこと**（クリック→Enter）。
2. **「a接点→ブレーカでComboBoxが消える」と観測しかけた**——真は
   **a接点配置ツールが残ったままで、そもそも要素選択が起きておらなんだ**
   （スキル6.0節の既知の罠＝配置後もツールは残る＝殿裁定の仕様）。
   **危うく「要素間遷移で通知漏れ」と誤報するところであった。**
   画を撮って初めて割れた——**UIAの数値だけを見ておれば気づけなんだ。**

**併せて**：3極記号の「3つの弧」を**3つのブレーカと誤読**し、対象の同定が付かなくなった。
**新しいシートに1つだけ置き直して解消**（`memory: ecad2_comparison_target_identity_pitfall`）。

---

## 6. 再確認（修正後、検体`4b2d7b1`、2026-08-05）

**侍の修正**＝`ViewModel_PropertyChanged`の固定リストへ`SelectedElementBreakerType`を追加。
**併せて`LadderCanvas.cs`の静的Pen 5件を`.Freeze()`**（新設テストが全体スイートで落ち、
未凍結Penが別STAスレッドから触れられる既存の穴が露見したため）。

### 観点A（修正そのもの）＝PASS

**キャンバスをクリックせず、ラベル文字とテストボタンが揃って即時に出た。**
**三遷移すべて**（NFB→ELB／ELB→MCCB／MCCB→NFB）。**片方だけということはなかった**
——**初報で「ラベルとテストボタンは同じ引き金で同時に戻る」と測ったことが、そのまま期待値になった。**

### 観点B（回帰）＝PASS

- **観点1**＝四経路（Breaker3P選択／空セル／a接点／要素間遷移）いずれも初報どおり
- **観点3**＝形状の三分割は観点Aで同時に確認
- **観点4**＝Undo三通り。**(a)同値選択で配置ごと取消＝履歴を積まず／(b)(c)は種別のみ戻り要素は残る。**
  **固定リストへの追加は`RecordSnapshot`の積まれ方に障っておらぬ**

### 観点C（凍結の副作用）＝五つすべてPASS（打ち切り不要）

**`.Freeze()`した5件はいずれも選択ハイライトの`Pen`。落ちずに描かれれば凍結は害を成しておらぬ証。**

| # | 対象 | 記入手段 | シート | ハイライト |
|---|---|---|---|---|
| 1 | セル | 要素配置 | 主回路 | **赤枠** |
| 2 | コネクタ | 縦分岐線記入（上下キー＋Enter） | **制御回路** | **赤い縦線** |
| 3 | 自由線 | 自由線記入（左右キー＋Enter） | **主回路** | **赤い横線** |
| 4 | 挿入画像 | 図面→画像挿入 | 制御回路 | **赤枠＋リサイズハンドル** |
| 5 | 基準枠 | グループ枠記入（ドラッグ） | 主回路 | **赤い点線枠** |

**侍・隠密が独立に「実行時の書き換えは0件」を静的に確かめており、本実測が三つ目の軸となる。**

### 【取り下げ】自由線ツールのボタンが効かぬ ── 忍者の誤報であった（`P-171`はclosed）

**当初、下記のように報じた**——
> `Invoke-Ecad2Button`でも物理クリックでも切り替わらぬ。`F9`ボタンは青くハイライトされるのに
> ステータスバーは「ツール: 選択」のまま。キーボード`F9`でのみ切り替わる。**自由線のみ。**

**隠密の静的読解が内部矛盾を突き**（**自由線は`CanPlaceOnMainCircuit`、縦分岐線は`CanPlaceOnControlCircuit`に
束縛されており、同一シートで両立せぬ。ゆえに「自由線はボタンで効かぬが縦分岐線は効いた」は
別シートでの観測を束ねたことになる**）、**忍者が測り直して誤報と確定した。**

**【真相】自由線ボタンは正しく動く。**

| 条件 | 結果 |
|---|---|
| セル**未選択**＋ボタン | 「**配置するセルを先に選択してください**」（切り替わらず） |
| セル**選択済み**＋`Invoke` | **「ツール: 自由線記入」＝正常** |
| セル**選択済み**＋物理クリック | **同じく正常** |

**一次ソース**＝`MainWindow.xaml.cs:3592-3596`。`TryBeginFreeLineDraft`は`SelectedCell`が
未選択なら即`return`する。**ボタン（`FreeLineHorizontalButton_Click`）もキーボード`F9`も
同じメソッドを同じ引数で呼んでおり、両者に差はない。**

**【忍者が犯した三つ】**
1. **ステータスの全文を読まなかった**——`-Prefix "ツール:"`で絞っており、**最終行のメッセージ欄を
   見ておらぬ。値は既に画面に出ていた**（`memory: ecad2_symptom_to_number_translation`の対句）
2. **「青くハイライトされる」を「押されたのに効かぬ」の証と読んだ**——**押下の視覚効果であって
   活性状態とは別物**（隠密の指摘）。実際は処理が走り早期`return`していた
3. **キーボードとの差を「手法の差」と帰属したが、実は「前提条件の差」**——
   **`F9`で成功した時は直前にセルを選択していた**（記録に「cell: 行10/列4」と残る）。
   **手法を変えたつもりで、セル選択状態まで一緒に変えていた**——`ninja.md:283`の
   **「1つずつ剥がして測れ」に背いた形。**

**【最も応えた一点】縦分岐線では同じメッセージを読み、セルを選んでから押し直して成功している。**
**同じ文言を、一度は読み、一度は読まなかった。手法の問題ですらなく、注意の問題であった。**

**【家老の総括＝Wチェックの新しい形】**——**従来は「侍の診断と隠密の独立調査」であったが、
本件は「忍者の実測と隠密の静的読解」にて、しかも静的が実測を覆した。**
**「実測が一次情報」は正しいが、測り方が誤っていれば実測は静的読解に劣る。**
**隠密は実機を一度も見ずに、`IsEnabled`のバインド先という実装の事実だけで内部矛盾を突いた。**

**【落とし込み】`ecad2-ui-automation`スキル`:715`直下へ三点を追記した**——
`Tool_Checked`がパーツエディタ専用である旨の射程明示／メインウィンドウで「ボタンが効かぬ」と
見えたらまず前提条件を疑う旨／「青くハイライト」は活性状態ではない旨。
**スキルの当該記述が「ツール選択RadioButton」と一般化されており、メインウィンドウとも読めたことが
誤読の一因であった**——**記述そのものに射程が無かった。**

### 【某の誤りと訂正】Undo(b)で一度「戻らぬ」と観測した

**回帰を疑ったが、`Ctrl+Z`のフォーカス要件を忘れた測り方の穴であった**
（直前に`ComboBox`を操作しており、キャンバスにフォーカスが無かった。
`MainWindow.xaml.cs:2942`の`IsCanvasFocused()`ガード＝仕様。スキル6.2節末尾に既載）。
**空セルを叩いてフォーカスを戻すと正しく戻った。**
**初報の測定では空セルクリックを挟んでおり踏まなかったが、
「修正で即時反映されるゆえ選択解除は不要」と手順を縮めた際に落とした。**
**手順を縮めるとき、その一手が何を担っていたかを問うべきであった。**

### 三点突合（侍の途中編集は無害）

| 項目 | 時刻 |
|---|---|
| 某のビルド（`Ecad2.App.dll`） | **21:09:30** |
| 実機起動 | 21:09:45 |
| 侍の`LadderCanvas.cs`編集 | 21:12:02（**コメント4行の追加のみ・コードは不変**） |
| 測定時に実機が掴んでいたDLL | **21:09:30のまま＝再ビルドされておらぬ** |

**ゆえに測定は`4b2d7b1`の版で通った。**

## 7. 後始末

- **`main-layout.xml`は無傷**（退避と**ハッシュ一致**＝`18CE2288...`、2448バイト）。
  `Stop-Ecad2App`は`WARNING`（未保存ゆえ強制終了）＝スキル記載の常態にて、
  **終了時保存が走らなんだゆえ殿のレイアウトは守られた**
- **`src`・`tests`への書き込みは一切行っておらぬ**
- **残置物**＝`sample/t133i5elb.pdf`（`.gitignore`済み）／
  `%APPDATA%\Ecad2\docking-layout\main-layout.xml.ninja-t133i5-backup-20260805`（退避）。
  **削除は`CLAUDE.md`【MUST】ゆえ殿の裁可を待つ**
- スクリーンショット・PDF展開テキストはスクラッチパッド（セッション専用）に置いた
