# T-133増分9（掲出）＋並べ替え 静的レビュー（隠密）

対象コミット: `6455cc0`（掲出、案A）／`c06692e`（3極記号6項目「横→縦」並べ替え）。
深度＝1周目・軽量既定。観点(a)台帳DoDとの整合／(c)既知トラップの狙い撃ち。`code-review`スキルは`gh pr`前提のため不使用（既知事象）。

## 結論

**要修正なし。** 両コミットとも殿裁定・台帳DoDと整合し、既知トラップ（Bubble再発火・添字ずれ・Click多重配線）はいずれも塞がれている。侍が自ら区切った2件の限界・1件の自己訂正も、いずれも実相に即しており過大でも過小でもない。

## 検分の詳細

### 1. 台帳DoDとの整合（観点a）

`docs/todo.md:2764-2771`（殿裁定2026-08-07）と実装を突き合わせ：

| 裁定 | 実装 |
|---|---|
| 掲出先＝案A（`OtherSymbolsMenu`末尾＋セパレータ） | `MainWindow.xaml.cs`の`RebuildPinnedSection`が末尾へ追加。一致 |
| 並び順＝「横→縦」（3極記号6項目のみ、モータは据え置き） | `MainWindow.xaml`で3ペア（Breaker3P/ContactorMain3P/ThermalOverload3P）をH→Vへ入替。モータ2項目は無変更。一致 |
| 検体`sample/t133i7-ab-verify.gcaddoc.gcad`削除可 | 実体不在・`.gitignore:7`（`sample/`）該当を確認 |

台帳`:2707`「並べ替えは既存の添字を動かす……侍に触れた件数と理由を報告させる」の求めに対し、コミットメッセージが「添字は5〜10のまま保たれる（手前へ挿れたわけではない）」と正確に応えている。範囲外の変更（行0拒否の案内・`P-174`等、同節に残る別の申し送り）には触れていない。

### 2. 既知トラップの狙い撃ち（観点c）

- **Bubble再発火**（T-068増分1の再発型）：`OtherSymbolsMenu_SubmenuOpened`冒頭の`if (!ReferenceEquals(e.OriginalSource, OtherSymbolsMenu)) return;`で塞がれている。現状の配下は葉の`MenuItem`のみゆえ実際には発火せぬが、将来への備えとして先に打たれている。
- **ダミー子項目不要**：`OtherSymbolsMenu`は静的11項目を持つゆえ`HasItems=false`の罠は最初から当たらぬ。コメントで明記済み。
- **Click多重配線**（開くたびに再構築するメニューの典型的な穴）：`RebuildPinnedSection`は毎回`tracked`の旧項目を`Items.Remove`してから**新しい`MenuItem`インスタンス**を生成しClickを1回だけ購読する。ハンドラの二重登録は起きぬ構造。テスト「作り直しても掲出分は重ならぬ」で実測済み。
- **添字ベーステストの脆さ**（`FirstSymbolIndex=5`／`MotorPartIndex=3`、隠密2026-08-06検分）：末尾追加ゆえ両定数とも無傷。並べ替え側もペア内入替のみで添字を動かさぬ。

### 3. 最重点観点：既存テスト改変（`MenuPlacementToolTests.ExpectedEntries`）の検出力

`ExpectedEntries`（6行）の改変は、`[Theory][MemberData(nameof(ExpectedEntries))]`を使う2メソッド（`各項目のタグが種別と向きへ解ける`・`項目を押せば配置ツールが切り替わる`）×6行＝**12ケース**に及ぶ——実ファイルで数え直し、コミットメッセージの「12ケース」と一致。

各行は`{index, tag, kind, orient}`の4項組を丸ごとH/V入替しており、XAML側の実変更（各ペアをH先頭へ）と1対1で対応する。索引・タグ・種別・向きが揃って動くため、検出力の欠落（例：タグだけ変えて向きを変え忘れる等）は見当たらない。

**壊す実測の記述も検分**：「期待値を直す前にXAMLだけ並べ替えて12ケースRED」は、実装（テーブル駆動でindexごとにタグを直接比較）の構造から見て整合する主張であり、過大な踏み込みは無い。

### 4. 侍が自ら覆した箇所（(C)層2件の限界表記）の検分

クラス冒頭のdocコメントで「殿の環境のピン留めに左右されぬ」から「改変A・B・Gではピン留め0件の環境で(C)の2件がGREENのまま通る」への訂正を検分。

- `RebuildPinnedSection`の実装を読むと、`pinned.Count == 0`で即returnし何も追加しない。ゆえに0件環境では「先頭を壊す」「作り直しで前回分を残す」の両方とも観測結果（項目数・先頭11件のタグ）に差が出ず、(C)の2テストは原理的に検出できない——**訂正は実装と整合**。
- 訂正後の記述は「改変A・B・Gを当てて実測」と対象を明示し、それ以外の改変（C〜Fは生成側の観点であり(C)層と無関係）には言及していない。実測を超えた一般化は見られない。

### 5. 侍が「測っておらぬ」と区切った2件の妥当性

- **ピン留めが在る状態で末尾に正しく生える箇所**：`OtherSymbolsMenu_SubmenuOpened`は`_viewModel.PartPalette.PinnedEntries`を直接`RebuildPinnedSection`へ渡す一本道。ここを誤って別の一覧（例：`Entries`全件）に差し替えても、(C)の2テストはいずれも「先頭11件が不変か」「二度開いて件数が同じか」のみを見るため、0件環境では検出できない——**侍が挙げた素通し形は実装の構造どおりで妥当**。
- **Bubble再発火ガード**：現在の配下が葉の`MenuItem`のみで`SubmenuOpened`を発火する子孫が無い以上、ガードの有無で観測結果は変わらぬ——**記録のみで正しい**。埋めるべき穴ではなく、将来サブメニューを持つ項目が入った時に効く備え。

## 追加検証（再現手段）

- **ビルド**：`dotnet build src/Ecad2.sln --no-incremental` → 0エラー（既存警告4件のみ、対象外）
- **テスト**：`dotnet test src/Ecad2.sln --no-build` → Core 502／App 1351／**合計1853件合格**。侍の申告と一致
- **検体削除**：`ls sample/t133i7-ab-verify.gcaddoc.gcad` → 実体不在。`.gitignore:7`＝`sample/`で該当確認
- **簡体字混入チェック**：両コミットのdiffを`增|实|检|殷|侪`で照合 → 0件

## 出力破損の付記

Grep contentモード使用時に型A破損1件（記録簿`#33`）。行頭`//`が`\ `に化けたのみでRead直読により即裏取り、作業への支障なし（殿ご裁定2026-08-01の基準）。以後Read直読中心へ切替済み。
