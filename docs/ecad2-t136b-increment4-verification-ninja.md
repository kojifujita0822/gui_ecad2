# T-136(B)增分4 実機確認（忍者、2026-08-02）

task_id＝T-136／対象コミット＝`4ea2a7f`→`ae54bd4`→`c13bd95`（最終）／
題目＝接続点に種類を持たせ色で表す（赤=電源に接続される点／青=制御配線でDRC無効な点、既定値=赤）

## 判定（4観点すべてOK・異常なし）

### 観点1：パーツエディタの接続点が赤で描かれるか

新規パーツエディタで接続点を配置し画素採取。**実測`#DC1414`＝一次ソース`DrawingTheme.cs:43`
`PortPower = new(255, 220, 20, 20)`と完全一致。** ライト・ダーク両テーマで確認。

### 観点2：選択リング（増分3）との共存

接続点を選択前後で画素差分を採取。**中心色（接続点自体）は選択前後とも`#DC1414`で不変**
（選択リングは中心を上書きしない）。**リングは中心から半径方向に約60画素の灰色系差分として出現**
——増分3の選択リング機構がそのまま機能しており、種類色と共存できている。

### 観点3：マゼンタ等の異常色

一次ソース確認＝`PortUnknown = new(255, 255, 0, 255)`（マゼンタ）がフォールバック値と判明
（`DrawingTheme.cs:48,120`）。**本検証では新規配置・既存パーツ読込のいずれでもマゼンタは一切
出現せず**、赤（`#DC1414`）とリングの灰色系のみを確認。**異常なし。**

### 観点4：既存部品の健全性

- **既存の保存済みカスタムパーツ（T136AFFANY、増分3以前に作成）を「編集」で開き、2つの接続点が
  正常に赤で表示されることを確認。** マゼンタ（未知種別のフォールバック）は出ず、既定値Power
  への正しいフォールバックが機能している。
- **組込み部品（a接点）を主キャンバスへ配置**——正常に配置・描画され、母線・記号とも異常なし。
- 侍の報告「既存27箇所の接続点定義は無改修」については一次ソース照合まではしておらぬが、
  上記2件の実機健全性で裏づけは取れたと判断する。

## 【殿へ差し上げる絵】

- ライトモード：`scratchpad\t136b4-forprince-light.png`（左右2点とも赤`#DC1414`）
- ダークモード：`scratchpad\t136b4-forprince-dark.png`（同、ダーク背景で視認性良好）

**青（`PortDrcExempt`）を出すUIは本増分に存在せず**——接続点の右クリック・選択後のプロパティ欄
（役割・配置先の既存2コンボボックスのみ）のいずれにも種類変更の手段は無い。既定値が常にPower
（赤）になる作りであり、種類を切り替える経路自体が増分5待ちと見受ける。家老の申し送りどおり
「能わずば赤のみで構わぬ」に従い、赤のみの絵をお届けする。

## 回帰・範囲外

回帰なし。論点5（組込み15件の個別指定）は範囲外につき検分せず（現状全て赤で正しい）。

## 操作手法

UIA `SelectionItemPattern`は接続点ツールのRadioButtonで機能したが、念のため物理クリック
（`Invoke-Ecad2ScreenClick`/`[Ecad2Native]::Click`）で確実化。パーツエディタはメイン
ウィンドウ座標系と別であるため、`Get-Ecad2WindowRect`基準の相対座標ヘルパーは使えず、
絶対スクリーン座標（`[Ecad2Native]::Click`直呼び）で操作した。色判定は画素の直接採取
（支配色・特定座標値）。殿代行操作は不要。

## 総合判定

**観点1〜4すべてOK。異常なし。殿へお見せする絵（ライト・ダーク）を用意した。青との対比は
UI未実装のため赤のみ。**
