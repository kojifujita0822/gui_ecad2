# T-141 実機確認（忍者、2026-08-02）

task_id＝T-141／対象コミット＝`598706d`（実装）＋`8af8836`（コメント訂正のみ）／
題目＝配置ツールバーの枠ツールにラベル「枠」を付す（殿ご指示2026-08-02）

## 判定（4観点すべてOK）

### 観点1：「枠」ラベルの表示

- UIA＝グループ枠記入ボタン配下に`ControlType.Text Name='枠'`を確認（Bounds=8x11、ボタン内に収まる）
- 画素＝ライト/ダーク両テーマでスクリーンショット上に明瞭に視認可能（後述、色も理論値と一致）

**OK。**

### 観点2：レイアウトへの波及（ボタン幅・高さ・折り返し）

ツールバー2段目14ボタン全件をUIAで一括取得（Left・Width・Height）：

| ボタン | ラベル | Left | Width | Height |
|---|---|---|---|---|
| 選択ツール | Esc | 1989 | 36 | 36 |
| a接点配置 | F5 | 2027 | 36 | 36 |
| OR a接点配置 | sF5 | 2065 | 36 | 36 |
| b接点配置 | F6 | 2103 | 36 | 36 |
| OR b接点配置 | sF6 | 2141 | 36 | 36 |
| コイル配置 | F7 | 2179 | 36 | 36 |
| 端子台配置 | F8 | 2217 | 36 | 36 |
| 自由線(横線) | F9 | 2255 | 36 | 36 |
| 自由線(縦線) | sF9 | 2293 | 36 | 36 |
| 縦分岐線記入 | sF9 | 2331 | 36 | 36 |
| 接続点記入 | F10 | 2369 | 36 | 36 |
| 配線分断記入 | F10 | 2407 | 36 | 36 |
| **グループ枠記入** | **枠** | 2445 | **36** | **36** |
| 自作パーツ | 部品 | 2488 | 36 | 36 |
| テストモード（別系統トグル） | テスト | 2531 | 46 | 42 |

**枠ボタンは他12件の固定ショートカット系ボタンと寸分違わずWidth=36・Height=36。** 隣接間隔も
一律38px（枠→自作パーツのみ43pxだが、これはセパレータを挟む既存のグループ区切りで枠追加とは無関係）。
**折り返し・拡大・縮小は一切発生せず。**

隠密の静的見立て「Width=46固定」は実測（36）と数値が異なったが、**全ボタン共通の値であり枠ボタン
固有の変化ではない**ため、「波及なし」という結論そのものは実測でも成立する。数値の食い違いのみ
申し送る。

**OK（波及なし）。**

### 観点3：他ラベルの不変確認

上表の14件＝家老が伝えた隠密の列挙「Esc/F5/sF5/F6/sF6/F7/F8/F9/sF9/sF9/F10/F10/(空→枠)/部品」と
**完全一致**。sF9とF10が各2件（縦分岐線記入・配線分断記入、シート種別排他）である点も含め相違なし。
「テスト」は別スタイルのトグルボタンで対象外（家老申し送りどおり）。

**OK（他13件変化なし）。**

### 観点4：「枠」ラベルの色のテーマ追従

理論値（一次ソース）＝`Theme.Light.xaml:19` `ToolBarForegroundBrush="#000000"`／
`Theme.Dark.xaml`同キー`#FFF0F0F0`。

| テーマ | 枠ラベル領域の実測 | 理論値 | 判定 |
|---|---|---|---|
| Light | 最暗画素 `#000000` | `#000000` | **完全一致** |
| Dark | 最明画素 `#F0F0F0` | `#F0F0F0` | **完全一致** |

（アンチエイリアスで縁は中間色になるため、支配色でなく「最暗/最明画素」で理論値との一致を確認する
手法を用いた＝6.3節「文字はアンチエイリアスで中間色が混ざる」を踏まえた採り方）

**OK（両テーマとも既存ラベルと同じスタイルで正しく追従）。**

## 新規気づき（実装バグではなく操作手法の罠、報告に留める）

検証序盤、シート追加ボタンを2回連続でInvokeしたところ、**「シート追加」ダイアログが2枚重なって
開いた**（スキル6.6節既載「モーダルダイアログの多重化」に合致する既知の罠。忍者自身の連続invoke
操作が原因で、実装側の不具合ではない）。1枚をキャンセル・もう1枚をOKで確定し解消、以後の検証には
影響なし。**落とし先＝既にスキル6.6節に記載済みにつき追記不要**（今回は罠を踏んだ実例が増えた
のみで、対処法は既知のままで足りた）。

## 回帰・範囲外

回帰なし。他ツールへのラベル追加は殿の御指示範囲外ゆえ検分せず。

## 操作手法

UIA `InvokePattern`／`ExpandCollapsePattern`（メニュー操作・テーマ切替）、UIA `BoundingRectangle`
一括取得（ボタン寸法比較）、`Save-Ecad2Screenshot`（PrintWindow方式）、画素の最暗/最明値抽出
（アンチエイリアス対策）。殿代行操作は不要だった。

## 総合判定

**観点1〜4すべてOK。回帰なし。範囲外の実害なし（多重化の罠は自己解消済み）。**
