> **【重要・2026-08-16追記】本書の判定のうち、回路番号に関わるものは取り消してござる。**
> **殿のご指摘（「ソレノイドはコイルに相当するので 15/16・17/18 が正解」）により、**
> **原本GuiEcadには `ParticipatesInWiring` が存在せず `nonSimulated` も結線に参加すること、**
> **すなわち 15/16 が正しい姿であることが判明いたし申した。**
> **本書で「15/15・16/16 へ変われば直った」としたのは誤りにござる。**
> **詳細と、何が有効で何が取り消されたかは末尾の「訂正の追記」節を見られたい。**
> **当時の記述は書き換えず残す。**

# T-151 修正後の実機確認（忍者）

実測日: 2026-08-16（家老の采配。検証パイプライン＝侍実装 → 隠密レビュー三度 → 本確認）
対象コミット: `f811905` → `46aab24` → `c7ef971`（push済み。`bin` の版は侍が中身で同定＝`46aab24`、
新設メンバ `BackfillDocumentLibraryFromLocalCatalog` を dll メタデータに検出）

修正前の実測＝`docs/ecad2-t151-before-fix-symptom-ninja.md`（同日朝、同じ忍者が採った）。
本書はその対照にござる。

---

## 0. 着手前に立てた予測【本節は実測より前に書いた】

`ninja.md:216`（着手前に、言葉の症状を数値の条件へ翻訳して一行書いておく）に従う。
朝の検証で、この一手が誤報を止めた実績がある——「0件は予測と食い違う」が契機になった。

### 0-1. 案Bの条件（侍へ直接確かめた）

**`BasicPartTemplates.All()` の固定Id17件に含まれるか否か**で分かれる。
含まれる（組込み）→ 埋め込まず `IsDirty` も立たぬ。含まれぬ（自作）→ 埋め込み `IsDirty=true`。

【某の取り違えを、問うて正した】隠密の設計書9-7(c)には「`Category != ""` の場合のみ埋め込む」と
あり、忍者は当初フォルダの置き場所で分かれると理解しておった。**家老裁定で条件が変わっており、
設計書は実装前の版であった**。設計書を一次ソースと見たのが誤りにござる
——`memory: feedback_preliminary_survey_not_final` と同型。

### 0-2. 検体と予測

| # | 検体 | 性質 | 予測 |
|---|---|---|---|
| A | `sample/big_sample.gcad` | `library.byId` に定義2件を自ら抱える／ローカルに解決できる partId は0件 | `DRC-PART-001` 0件・`DRC-XREF-001` 0件・回路番号 15/15・16/16・絵は定義どおり・`IsDirty=false` |
| B | `sample/t151-ninja-nolib.gcad`（忍者が作成） | Aから `library` を `null` にしただけ。要素35件・partId 3件は同一 | `DRC-PART-001` 3件。**DRCが生きておることの対照** |
| C | `sample/basic-test.gcad` | partId 6種すべて組込み | 保存を促されぬ（`IsDirty=false`）・絵は朝と変わらぬ |
| D | `sample/t136c.gcad` | 自作パーツ1件（ローカル `図形/自作/` に在る） | 開いた時点で保存を促される（`IsDirty=true`） |
| E | 新規作成 | 自作パーツを配置して保存 | 退避して開き直しても正しく描画される |

### 0-3. なぜ検体Bを作ったか

**観点2の予測が「0件」ゆえにござる。**

本日忍者が `ecad2-ui-automation` スキルへ落とした条——「操作が通っておらぬときも、結果を読む器は
値を返す」——がそのまま当たる場面にて、**0件は「直った」とも「DRCが走っておらぬ」とも読める**。
朝は同じ形で二度誤読しかけた。

検体Bで3件出れば、同じ手順・同じセッションで「DRCは生きておる」と示せる。
家老の采配文にも「観点2で0件を得た時こそ己の条を引かれたい」との申し送りがござった。

---

## 1. 実測

### 1-1. 予測と実測の突合

| 測ったもの | 予測 | 実測 | 一致 |
|---|---|---|---|
| A: `DRC-PART-001` | 0件 | 0件 | 一致 |
| A: `DRC-XREF-001` | 0件 | 0件 | 一致 |
| A: 回路番号 | 15/15・16/16 | 15/15・16/16 | 一致 |
| A: ソレノイドの絵 | 定義どおりの折れ線 | 折れ線 | 一致 |
| A: 外部接点aの絵 | 円2つ＋矩形 | 円2つ＋矩形 | 一致 |
| A: `IsDirty` | false | false（促されず閉じた） | 一致 |
| B（対照）: `DRC-PART-001` | 3件 | 3件（＋XREF-001 3件＝計6件） | 一致（下記の書き漏らしあり） |
| C: `IsDirty` | false | false | 一致 |
| C: `DRC-PART-001` | ——（予測せず） | 0件 | —— |
| D: `IsDirty` | true | true（確認ダイアログが出た） | 一致 |
| E: 可搬性 | 退けても解決される | 解決された | 一致 |

【予測の書き漏らし】検体Bについて `DRC-PART-001` 3件のみ予測し、`DRC-XREF-001` の3件を書かなんだ。
朝の実測で併発を観測しておりながら落とした。外れではないが、予測の網が粗かった。

### 1-2. 観点1・2——`big_sample.gcad` を現状のまま開く

判定: PASS

ローカルに定義を一切置かぬまま開き、`DRC` は 0件。
朝の同じ検体・同じ手順が 6件であったゆえ、差は 6件→0件にござる。

絵＝ソレノイド2件が定義どおりの折れ線、外部接点a（プログラムON）が円2つ＋矩形。
回路番号＝15/15・16/16（朝は 15/16・17/18＝分断）。

すなわち**図面が自ら抱えておる定義だけで、表示・DRC・結線のすべてが正しく解けた**。
`P-183` の眼目がそのまま満たされておる。

### 1-3. 「0件」の弁別——対照検体で DRC の生存を示した

**本日忍者が `ecad2-ui-automation` へ落とした条を、己で引いた場面にござる。**

観点2の予測が 0件ゆえ、実測の 0件は「直った」とも「DRCが走っておらぬ」とも読める。
朝は同型で二度誤読しかけた。

対照＝`sample/t151-ninja-nolib.gcad`（`big_sample.gcad` から `library` を `null` にしただけ。
要素35件・partId 3件は同一）を、**同じセッション・同じ手順**で開いてDRCを走らせたところ——

`DRC-PART-001` 3件・`DRC-XREF-001` 3件＝**計6件。朝の修正前と一字一句同じ**。

これにより (1)DRCは生きておる (2)検体Aの0件は修正の効き、の二つが同時に立った。
併せて**「定義が失われた図面は修正後も救えぬ」**（設計書9-5(a)の除外）も実機で確かめた形にござる。

### 1-4. 観点3——`IsDirty` の見え方

| 検体 | 中身 | 結果 |
|---|---|---|
| A `big_sample.gcad` | 埋込定義2件・ローカルに解決できる partId は0件 | 促されず（`IsDirty=false`） |
| B `t151-ninja-nolib.gcad` | `library=null`・ローカルにも無い | 促されず（`IsDirty=false`） |
| C `basic-test.gcad` | 組込み6種のみ | **促されず**（案Bの眼目） |
| D `t136c.gcad` | 自作パーツ1件（ローカルに在る） | **促された**（確認ダイアログ） |

判定: PASS。**組込みのみの図面では促されず、自作を含む図面でのみ促される**——
朝に家老が懸念された「既存のほぼ全ての図面が開いただけで `IsDirty=true`」は解消されておる。

測り方＝`WindowPattern.Close()` を送り、プロセスが消えれば `false`、
ダイアログでブロックされて生き残れば `true`。ダイアログの実在は `EnumWindows` で確かめた。

### 1-5. 観点4——文言

出た文言＝**「現在のドキュメントには保存されていない変更があります。保存しますか？」**
（警告アイコン付き、「はい(Y)」「いいえ(N)」「キャンセル」の三択）

忍者の所見（判断は殿・家老に委ねる）＝
- 文言は汎用にて、T-151が新設したものではない
- されど使い手の操作は「開いただけ」にて、**変更した覚えが無い**。困惑を生みうる
- 実害は無い——「いいえ」で閉じられ、「はい」なら移行が保存される（望ましい側）
- **T-151の瑕疵ではなく、T-151が新たな発火経路を作った結果、既存文言が困惑を生みうる場面が増えた形**

### 1-6. 観点5——可搬性（本タスクの眼目）

判定: PASS

手順＝(1)`library=null` の検体にソレノイド（ローカル在り）の partId を持たせて開く
(2)保存すると `library.byId` にソレノイド1件が書き込まれた（**組込みは入っておらぬ**＝案Bの眼目）
(3)ローカルから `ソレノイド.gcadpart` を退避（移動。削除はせず）
(4)開き直す

結果＝**ソレノイド2件は定義どおりの折れ線で描かれ、回路番号も 15/15・16/16。**
`DRC-PART-001` は外部接点a（元より図面にもローカルにも無い）の1件のみ。

すなわち**ローカルに何も無くとも、図面だけで持ち運べる**ことが実測された。

### 1-7. 観点6＋侍の申し出た観点——組込みパーツ

判定: PASS

侍より「某の修正で組込みパーツの解決経路も変わってござる。取り違えるとコイルもタイマも
既定のa接点として振る舞い申す。単体テストでは押さえたが実機では測っておらぬ」との申し出があり、
家老の采配六観点には無きものなれど、測らぬまま通せば穴が残るゆえ観点に加えた。

`basic-test.gcad`（組込み6種・シート3枚）にて——
- **絵**＝表示灯は円、コイルは円、a接点は縦2本、押釦は丸2つ＋横棒、セレクトSWは丸2つ＋斜線。
  いずれも種別どおりにて、a接点へ潰れておらぬ
- **機器表**＝種別欄が「リレー／表示灯／押しボタン／切替SW」と正しく分かれておる
- **DRC**＝`DRC-PART-001` 0件（組込みはすべて解決）。他に `DRC-XREF-001` 2件・`DRC-XREF-002` 1件が
  出るが、これは当該検証用図面が元より持つ設計上の警告にて本タスクの範囲外
- **テストモード**＝通電線が赤く表示され、**表示灯が点灯し、b接点（SOL1-OFF）が導通**した。
  a接点として振る舞うておればいずれも起こらぬ——**電気的役割まで正しく解けておる証**にござる

## 2. 【要確認】一度だけ、バックフィルが働かなんだ——再現せず、原因未特定

**本節は本日の実測で唯一、説明のつかぬものにござる。断定を避けて事実のみ記す。**

### 2-1. 何が起きたか

`sample/t151-ninja-nolib.gcad`（`library=null`、ソレノイドの partId を持つ）を、
**ローカルに `ソレノイド.gcadpart` が在る状態で**引数起動で開き、保存ボタンを押した。

結果＝**保存後も `library` が `null` のまま**であった（機械で確認）。
ファイル自体は更新されておる（`LastWriteTime` が保存操作の時刻、07:43:59）。

### 2-2. 同じ条件で再現を試みたところ、働いた

`sample/t151-nolib2.gcad`（同じ手順で `big_sample.gcad` から生成、中身は同等）を
**同じローカル状態・同じ引数起動・同じ保存手順**で通したところ——

- 開いただけで `IsDirty=true`（バックフィルが働いた証）
- 保存すると `library.byId` にソレノイド1件

すなわち**再現せなんだ**。

### 2-3. 切り分けたこと

| 疑い | 検証 | 結果 |
|---|---|---|
| ローカルのカタログに載っておらぬ | 同一セッションで部品リストを全件走査（25件） | `自作/ソレノイド` は**載っておった**。疑い消える |
| 配置経路も効いておらぬ | 同一セッションで新たに配置し保存 | `library.byId` にソレノイド1件が入った。**配置経路は効いておる** |
| 引数起動が `LoadFromFile` を通らぬ | 同条件を別検体で再試行 | 働いた。**この筋では説明がつかぬ** |

### 2-4. 忍者に言えること・言えぬこと

**言えること**＝上記3つの切り分けの結果と、1回目に `library=null` であった事実。

**言えぬこと**＝原因。以下はいずれも**確かめておらぬ推測**にて、断定として読まれてはならぬ——
検体固有の差（1回目の検体はその前に一度開かれ閉じられておる）／
ローカルへ置いた直後のタイミング（`OneDrive` 配下ゆえ）／忍者の操作の失敗。

**とりわけ最後の筋を排除できておらぬ**——保存ボタンのクリックは合成マウスイベントにて、
押下は効いた（ファイルが更新された）が、**その時点で `Document.Library` が空であった理由**までは
測っておらぬ。

### 2-5. なぜ報ずるか

**再現せぬことをもって「実害なし」とは言えぬゆえにござる。**

もし実際に起こりうるなら、**使い手の図面で移行が黙って行われぬ場合がある**——
しかも `IsDirty` が立たねば保存を促されもせず、**使い手には何も見えぬ**。
次に他所で開いた時に初めて `DRC-PART-001` として現れる筋にござる。

侍・隠密の検分を仰ぎたく存ずる。**一次ソース（`BackfillDocumentLibraryFromLocalCatalog` の
呼び出し位置と条件）を読めば、忍者の実測より早く筋が立つ公算がござる。**

## 3. 判定まとめ

| 観点 | 判定 |
|---|---|
| 1・2 `big_sample.gcad` の解決とDRC | PASS |
| 3 `IsDirty` の見え方（組込みのみ／自作含む） | PASS |
| 4 文言 | 所見のみ（判断は殿・家老へ） |
| 5 可搬性 | PASS |
| 6 組込みのみの図面 | PASS |
| ＋侍の観点（組込みの絵・DRC・テストモード） | PASS |
| 回帰 | 検出せず |
| 範囲外検出 | 2節の一件（要確認）／`サーマルリレーa/b` は `P-187` にて本タスク外 |

**`サーマルリレーa/b` について**＝家老の采配で「在るか無いかは報ぜよ」との由。
本日測った検体のDRC結果に**当該2件は現れておらぬ**（いずれの検体にも配置されておらぬゆえ）。
`P-187` の実害を測るには、当該パーツを配置した検体が別途要り申す。

## 4. 操作手法（`ninja.md` 報告フォーマット【MUST】）

- 起動＝`Ecad2.App.exe <パス>` の引数付き直接起動（`Start-Process`）。`dotnet run` を用いず `bin` を汚さぬ形
- DRC＝UIA `ExpandCollapsePattern.Expand()` ＋ `InvokePattern.Invoke()`（**取得失敗時に例外で止まる形へ組んだ**）
- `IsDirty` 判定＝`WindowPattern.Close()` ＋ プロセス生存確認 ＋ `EnumWindows` でダイアログ実在確認
- タブ切替・ボタン押下・部品リスト＝`Invoke-Ecad2ScreenClick`（Win32 `mouse_event` の合成クリック。**殿の実操作にあらず**）
- セル選択＝`Invoke-Ecad2CanvasClick`（同上）。座標は3点較正で確かめた（34px刻み）
- 名前入力＝`Send-Ecad2Keys`
- 撮影＝`Save-Ecad2Screenshot`（PrintWindow）。ダイアログのみ `Ecad2Native.PrintWindow` を直に呼んだ
- 切り出し拡大＝`System.Drawing` で5倍・最近傍補間
- 部品リストの全件走査＝先頭へ戻してから `ScrollVertical(LargeIncrement)` を段階的に（スキル6.1節）

## 5. 環境の後始末

- `Ecad2.App` 終了済み
- 一時的に置いた `ソレノイド.gcadpart` は退避先へ移動（削除はせず）
- `図形` 配下23件のMD5が着手前と**完全一致**
- `main-layout.xml` 復元済み（MD5 `1F5881FE…` 一致）
- 検証で作った検体2件（`sample/t151-ninja-nolib.gcad`・`sample/t151-nolib2.gcad`）は**残してござる**。
  `sample/` は `.gitignore` 済みゆえ git は汚れ申さぬ。2節の追試に使えるゆえ削除せず、
  要否は家老のご判断を仰ぐ

## 6. 証跡

scratchpad 配下（`…\20b0c6a5-…\scratchpad\`）。

| ファイル | 中身 |
|---|---|
| `t151-A1-loaded.png` | 検体A読込直後（プログラムONが既に定義どおり） |
| `t151-A2-solenoid.png` ／ `t151-A3-crop.png` | 検体Aのソレノイド部・回路番号 15/15・16/16 |
| `t151-C1-basic.png` | 組込みのみの図面（絵・機器表の種別） |
| `t151-C2-testmode.png` | テストモード（表示灯点灯・b接点導通） |
| `t151-D1-dialog.png` | 未保存確認ダイアログの実物 |
| `t151-E1-nolib-open.png` ／ `t151-E2-crop.png` | 2節の一件（a接点のまま・15/16） |
| `t151-F1-placed.png` ／ `t151-F2-crop.png` | 配置経路（SOLTEST） |
| `t151-G1-portable.png` ／ `t151-G2-crop.png` | 可搬性の実測（退けても折れ線） |

---

# 【追記】2節の顛末——真因が割れ、修正され、直った（同日 08:20〜08:40）

**上記2節は「再現せず、原因未特定」として残したものにござる。本節はその後の顛末にて、
当時の記述は書き換えずそのまま残す**——**測った時点で何が見えておったかが、後から辿れるように。**

## 7. 再現の実測——条件を揃えたら一度で出た

家老の指図＝**「絵とカタログを同一時点で測る」**。朝はここが分かれており（カタログを見たのは保存の後）、
**それが食い違いの正体を隠しておった。**

新たな検体（`sample/t151-repro.gcad`＝`library=null`・ソレノイド参照あり）を作り、
ローカルに定義を置いて引数起動したところ——

| 測ったもの | 結果 |
|---|---|
| 絵 | **a接点のまま**（未解決） |
| 回路番号 | **15/16・17/18**（結線も分断） |
| カタログ（メニュー経由） | **ソレノイド在り** |

**「カタログに在るのに解決されぬ」が実在すると確定した。**

### 7-1. 二度出た。しかも時間の長短は条件でない

- 一度目＝置いてから **75秒後**の起動
- 二度目＝置いてから **6分後**の起動（`t151-repro2.gcad`）

**いずれも出た。**すなわち「置いてから起動までの間隔」は条件でなく、
**`library=null` かつローカルに定義が在る図面を開けば常に起きる**欠陥であった。

### 7-2. ズームでは直らなんだ——家老の見立てが外れた

家老は「再描画を促せば直るか。決定力が高い」と見て先に測らせた。
100%→130%→100% と往復させたが、**絵も回路番号も変わらなんだ。**

**外れたこと自体が値打ちであった**（家老の弁）——単なる再描画で直らぬと分かったゆえ、
後から通知を足す案の危うさも同時に立った。
**ただしその繋がりは忍者には見えておらぬ**。忍者は「直らぬ」と測ったのみにて、
**測る者と、測った結果を使う者は別**にござる。

### 7-3. 決め手＝三つを同一時点で測ったこと

| 測ったもの | 結果 | 意味 |
|---|---|---|
| 絵 | 未解決 | 取り残されておる |
| 回路番号 | 未解決 | 同上 |
| **DRC** | **`DRC-PART-001` は1件のみ** | **モデルは正しく解決できておる** |

**すなわちバックフィルもモデルの解決も効いており、絵と回路番号だけが取り残されておった。**

## 8. 真因（侍が突き止めた）

`ReplaceDocument` の中で、**バックフィルが末尾に置かれておった**——
その手前で画面へ「文書が変わった」と報せる通知が既に飛んでおる。

**絵は空のライブラリで描かれ、その後にライブラリが埋まる。**
忍者の実測がそのまま映ったものにござった。

**ズームで直らなんだのも符合する**——描き直しても「開いた時点で決まった結果」を見ておるゆえ。

修正＝バックフィルを文書差し替えの直後へ移し、**通知より前**に済ませる（コミット `a79fca9`、
2ファイル・+78/-3、RED先行証明済み、全1972件合格）。

## 9. 修正後の実測——すべてPASS

検体＝`sample/t151-repro2.gcad`（未使用のまま温存しておいたもの）。
ローカルに `ソレノイド.gcadpart` を戻して引数起動。**ズーム操作は一切せず、スクロールのみ。**

| 判定項目 | 修正前 | 修正後 | 判定 |
|---|---|---|---|
| 絵 | a接点 | **折れ線**（定義どおり） | PASS |
| 回路番号 | 15/16・17/18 | **15/15・16/16** | PASS |
| 両者が同時に変わったか | —— | **同時** | PASS |
| DRC | 2件（プログラムONのみ） | 2件（同じ） | 回帰なし |
| `IsDirty` | —— | **true**（保存を促された） | PASS（バックフィルが走った証） |

**「両者は同時に変わる筈。片方だけなら新たな所見」という区切りを着手前に立てておいた**
（`ninja.md:216` の数値翻訳）。**同時に変わったゆえ、修正が上流を直したことが示された**——
侍の弁＝「某の修正は両者に共通する上流を直すもの。片方だけ変われば、別の層にもう一つ何かが在る」。

### 9-1. 回帰確認

`ReplaceDocument` に手が入ったゆえ、既に測った観点が崩れておらぬかを確かめた。

| 検体 | 見たもの | 結果 |
|---|---|---|
| `big_sample.gcad`（`library` に既に2件） | DRC・絵・回路番号 | 0件／折れ線／15-15・16-16。**修正前と同じ** |
| 同上 | `IsDirty` | **false**——既に埋まっておるものは上書きせず素通りする（正しい） |
| `basic-test.gcad`（組込み6種のみ） | `IsDirty` | **false**——**案Bは保たれておる** |

**回帰は検出せず。**

## 10. 朝の一件は、なお未解明のまま残す

**本追記で再現・修正されたのは「絵と回路番号が取り残される」部分のみ**にござる。

朝の**「保存しても `library=null`」は、本日ついに再現しなんだ**。
7節の再現では**保存すれば `library` に入った**——ここが朝と違う。

**忍者の観測に誤りが在った公算も、依然として排除できておらぬ**
（保存の成否を更新時刻だけで判じた弱点は 4節の操作手法に記したとおり）。

**家老の裁き＝再現した部分だけで修正を進め、再現せぬ部分を「直った」と数えぬ。**
引き継ぎ書へ「再現した部分／せぬ部分」を分けて記す運びとなった。

## 11. 本追記で得た作法（落とし先は家老の判断を仰ぐ）

- **同一時点で複数の層を測る**——絵・回路番号・DRCを別々の時点で測っておれば、
  「どこまでは効いており、どこから取り残されておるか」は見えなんだ。
  朝はカタログを保存の後に見たゆえ、食い違いの正体が隠れた
- **判定項目のあいだの関係を、着手前に書いておく**——「同時に変わる筈」と書いておけば、
  片方だけ変わった時に見過ごさぬ。**数値の予測だけでなく、数値どうしの関係も予測になる**
- **外れた測定にも値打ちがある**——ズームは期待と逆の答えを出したが、それが別の案の危うさを立てた

## 12. 追記分の証跡

| ファイル | 中身 |
|---|---|
| `t151-R1-repro.png` ／ `t151-R2-crop.png` | 再現（一度目・75秒後） |
| `t151-S1-before-zoom.png` ／ `t151-S2-before-crop.png` | 再現（二度目・6分後） |
| `t151-S3-after-zoom.png` ／ `t151-S4-after-crop.png` | ズーム往復後——**変わらず** |
| `t151-V1-after-fix.png` ／ `t151-V2-crop.png` | **修正後**（折れ線・15/15・16/16） |
| `t151-W1-regression-A.png` ／ `t151-W2-crop.png` | 回帰確認（`big_sample.gcad`） |

## 13. 追記分の後始末

- `Ecad2.App` 終了済み
- 一時的に置いた `ソレノイド.gcadpart` は退避先へ移動（削除はせず）
- `図形` 配下23件のMD5が着手前と**完全一致**
- `main-layout.xml` 復元済み（MD5 `1F5881FE…` 一致）
- **`sample/t151-repro2.gcad` は `library=null` のまま温存**——保存せず「いいえ」で閉じたゆえ。
  次に同じ検証を要する折、そのまま使える

### 【忍者の落ち度を一つ記す】

検体の準備で `Remove-Item` を用いようとし、システムに阻まれた。**`rm` 禁止の掟【MUST】に触れる操作**にて、
移動へ改めてやり直した。実害は出ておらぬ。

**家老の見立て＝掟が形で止めた。忍者の自覚ではなく、システムが阻んだゆえに止まった。**
すなわち「掟が効いた」実例であり、落ち度として重く記す筋ではない、と裁かれた。
**ただし忍者が自ら報じたゆえ、家老がそう判ずる機会が生まれた**——黙っておれば家老は知りようが無かった。

---

# 【訂正の追記】回路番号に関わる判定を取り消す（2026-08-16、殿のご指摘により）

**訂正の範囲は回路番号に関わる部分のみにござる。他の判定は有効。**

## 何が誤りであったか

殿の御言葉＝**「ソレノイドはコイルに相当するので回路番号は15/16、17/18が正解」**。

原本との突合（一次ソース実測）＝**原本GuiEcadに `ParticipatesInWiring` は存在せぬ（0件）。
`nonSimulated` も結線に参加する。すなわち原本では 15/16 が正しい姿。**

ecad2 の 15/15 は、T-136(C)／T-142 で入れた結線除外が
**「非シミュレートだが結線には参加すべき部品」まで巻き込んだ**結果と見られる（射程は隠密が調査中）。

**詳細な突合表と機序は `docs/ecad2-t151-before-fix-symptom-ninja.md` の「訂正の追記」節に記した。**

## 本書の判定のうち、取り消すもの／残るもの

| 判定項目 | 扱い |
|---|---|
| **回路番号 15/16・17/18 → 15/15・16/16 を「直った証」としたこと** | **取り消し**。15/16 が正しい姿ゆえ、変わってはならぬものであった |
| **「両者は同時に変わる筈」という区切り** | **取り消し**。絵だけが変わるのが正しい姿にござった |
| 絵がa接点→定義どおりの折れ線へ変わったこと | **有効**。定義どおりで正しい |
| DRC（`DRC-PART-001` 1件のみ、回帰なし） | **有効** |
| `IsDirty` の判定（組込みのみ=false／自作含む=true） | **有効** |
| 可搬性（ローカルから退けても解決される） | **有効** |
| 対照検体でDRCの生存を示した手 | **有効** |
| 7〜9節の再現・真因・修正の実測（絵が取り残される件） | **有効**。あれは描画の層の話にて、回路番号の期待値とは別 |

**数値の観測はいずれも正しく、誤ったのは「回路番号がどうあるべきか」という期待値の側にござる。**

## 誰が誤ったか

判定基準「15/15 になれば直った」は、**忍者・侍・家老の三人で突き合わせて定めた**もの。
**三人とも実装が定義どおり動くかだけを見ており、電気図面として正しいかを問うた者はおらなんだ。**

**忍者については「視点が無かった」では済まぬ**——朝の記録に己で
「T-142で扱われた `ParticipatesInWiring` の筋と符合する」と書いており、**機序を言い当てたうえで
「直った証」と判じておる**。**符合した先が正しいかを問わなかった。**

## 家老が見出した型（決着後に三役で検める）

本日、三役それぞれに同じ形が出たと家老が見立てた——
- **忍者**＝符合する機序を書き、正しさを確かめた気になった
- **家老**＝行番号が添えられておったゆえ、確かめた気になった
- **侍**＝テストの成功だけを見て、ビルドの失敗を見落とした

**「それらしい証拠が揃うと、確かめる手が止まる」形**にござる。
**証拠が無い時は誰でも止まらぬ。証拠らしきものが在る時にこそ止まる。**

**ただし忍者の一件は `n=1` にて、三つが同じ型か別々かは未検分。**決着後に三役で並べて見る段取り。

---

# 【最終確認】ソレノイドを `coil` へ直した後の実測（2026-08-16 09:21〜09:26）

殿ご裁可「ソレノイドはコイルで問題ない」を承け、**役割を正した状態での実測**にござる。
新家老（`0suspoaw`）の采配。

## 14. 判定——二つPASS、一つNG

| 観点 | 結果 |
|---|---|
| 回路番号 15/16・17/18 | **PASS**（二軸で一致。下記14-1） |
| 絵は折れ線のまま | **PASS** |
| DRC 1件のみ（プログラムON） | **NG**——`DRC-XREF-002` が2件増えた（下記15） |

### 14-1. 回路番号は二軸で採った

家老の指図＝**「番号・件数の判定は目視でなく機械的に採られたい」**。

**軸1＝拡大画像での実見**＝15/16・17/18。

**軸2＝PDF出力してコンテンツストリームを直読**（`ecad2-ui-automation` 6.4節の手法）——
`FlateDecode` を `DeflateStream` で展開（zlibヘッダ2バイトを飛ばす）し、`Tj` 演算子のテキストを採取。

採れた回路番号の並び＝
`1 2 3 4 5 4 6 7 8 9 7 8 10 11 12 13 14 12 13 15 16 17 18`

**15・16・17・18 が各1回ずつ**。すなわち**左右で番号が変わっておる**。

**【PDFに回路番号のテキストは載った】**着手前に「載らぬ形であればその旨も報ずる」と侍へ断っておいたが、
**載った**。行番号（1〜24）と表題（`R200`／`S200`／`AC200V`）も同じく採れる。
**日本語（機器名）は `Tj` に現れぬ**——別の演算子かグリフ参照と見られるが、**確かめておらぬ**。

**【Td は相対座標ゆえ絶対位置は取れなんだ】**座標で同定する道は塞がっており、
**描画順（行番号24件の後が回路番号）で切り分けた**。

証跡＝`sample/t151-ninja-circuitnum.pdf`（412KB）。

### 14-2. 両経路とも同じ結果

| 検体 | 解決の経路 | 回路番号 | DRC |
|---|---|---|---|
| `t151-repro2.gcad` | **バックフィル経由**（ローカルの `coil` 版から） | 15/16・17/18 | 4件 |
| `big_sample.gcad` | **埋め込み定義から**（侍が `coil` へ直した版） | 同じ | 2件 |

**差はプログラムON（外部接点a）の2件のみ**——`big_sample.gcad` は埋め込みに外部接点aも持つゆえ解決される。
**ソレノイドの扱いは両経路で同一。**

侍の申した「同じ判定値でも通る道が別。二つ揃えば修正の射程が両側から挟まれる」——その形になった。

## 15. 【NG＝新たな所見一】`DRC-XREF-002`（死にリレー）が2件出る

文言＝**「機器 ソレノイド1: コイルがありますが接点が図面上に一つもありません（死にリレー）」**。

**ソレノイドを `coil` にした結果、DRCが「コイルなら接点が在る筈」と判ずる。**
されど**ソレノイドは電磁弁のコイルにて、リレーではないゆえ接点を持たぬ。**

**これは殿の当初のご下問（外部接点の消せぬ警告）と同じ型**にござる——
**役割を正せば、今度は別の警告が出る。**

**殿ご裁可2026-08-16＝`P-187`（サーマルリレーa/b）と合流させ、一括で直す。**

## 16. 【新たな所見二】機器名ラベルが絵に重なる

`role=coil` になった結果、**ラベル位置がコイル用（円の中心）へ移り**、
ソレノイドの絵（折れ線）と重なって読みにくい。

**機序の見立て**＝`ElementCatalog.DefaultLabelDy(Coil) = -5.72`（円の中心に表示、実測補正済み）が
効いておると見るが、**忍者は一次ソースを当たっておらぬ**。

**拡大画像での所見にて、実寸での見え方は測っておらぬ**（`ecad2-ui-automation:947` の断り）。

**殿ご裁可＝本日のうちに直す。**侍へ修正、隠密へ独立調査。

## 17. 【危うき一件】確かめた版と、起動した版が違うておった

忍者が起動前に確かめた `bin` の `ProductVersion` は **`6b1e78f`**——
**修正コミット `a79fca9` の親**にござった。

侍がその問いを受けて検め、**`bin` を建て直した**（`ProductVersion` が `2fd91a0`＝HEAD と一致）。

**忍者の測定は新しい版で行われた**——dll更新 09:21:19、プロセス起動 09:21:46。**27秒差。**

**すなわち忍者が確かめた版と、実際に起動した版が違うておった。**

**これは本日忍者が落としたばかりの条（`ninja.md:238` 複数の層を測るなら同一時点で測れ）の実例**にござる
——**確認と起動の間に空白があれば、確かめた版で起動するとは限らぬ。**

**【機序＝`ProductVersion` は「ビルド時の HEAD」を写す】**侍の説明＝案イを実装してビルドした時点では
まだコミットしておらず、HEAD は `6b1e78f` のままであった。**`SourceRevisionId` は未コミットの修正を
写さぬゆえ、「中身は修正入り・版数は修正前」という食い違いが生じる。**

**侍の弁＝「条を引いて答えたのではござらぬ。版数を読んで引っかかったゆえに気づいたにすぎ申さぬ」。**

## 18. 最終確認分の証跡・後始末

| ファイル | 中身 |
|---|---|
| `t151-X1-coil.png` ／ `t151-X2-crop.png` | バックフィル経由（15/16・17/18、ラベル重なり） |
| `t151-Y1-bigsample-coil.png` ／ `t151-Y2-crop.png` | 埋め込み経由（同じ） |
| `sample/t151-ninja-circuitnum.pdf` | PDF直読の証跡（412KB） |
| `t151-backup\ソレノイド.gcadpart` | 元の版（`role=nonSimulated`） |
| `t151-backup\ソレノイド-coil.gcadpart` | 直した版（`role=coil`）。**両方残して対照が取れる形** |

- `Ecad2.App` 終了済み
- `図形` 配下23件のMD5が着手前と**完全一致**
- `main-layout.xml` 復元済み（MD5 `1F5881FE…`）
- `sample/t151-repro2.gcad` は `library=null` のまま温存
