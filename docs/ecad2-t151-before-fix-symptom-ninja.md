# T-151 修正前症状の実測記録（忍者）

実測日: 2026-08-16（家老の采配による下ごしらえ。修正後との対照に用いる）
task_id: T-151（自作パーツ定義を図面へ埋め込む。`P-183` の原本回帰）
版: `Ecad2.App.exe` 2026-08-16 06:30:46 ビルド。作業ツリーの `src`/`tests` は清浄
（侍の三点回答＝一時改変マーカー0件・未コミット変更0件・`bin` は `f407baf` 以降 `src` に動き無し）

---

## 0. 采配の前提と現物の食い違い（実機に入る前に判明）

家老の采配文は「ローカルの `図形/自作` からソレノイドを退けた状態で開く」であったが、
退ける対象が元より存在せなんだ。

- ローカル `…\Ecad2\図形\自作\` の中身は5件のみ
  ——T136AFFANY／T136AFFCTRL／T136AFFMAIN／T136UIMAIN／T142検体NS
- ソレノイド・外部接点a とも無し。すなわち現状が既に「退けた後」に当たる

`ninja.md:69`（検体を指定されたら実機に入る前に中身を読む）が働き、空振りの一周を省けた。
家老は粗を認め、対照の組み直しを承認なされた。

なお `T142検体NS.gcadpart` は `primitives: []` にて絵を持たぬ（`ninja.md:69` の T136AFFANY と同型）。
当初これを対照に使う案であったが、描画の対照が取れぬゆえ採らなんだ。

## 1. 検体

`sample/big_sample.gcad`（17,454バイト、2026-07-14最終更新。GuiEcadで作られた図面）

`library.byId` に定義を2件持つ——これが本件の眼目にござる。

| Id | 名 | role | primitives |
|---|---|---|---|
| `0b62e3697b194251a78d88e7601e99e4` | ソレノイド | nonSimulated | polyline＋line（稲妻様の折れ線） |
| `f60e2a88967e4e2ea7e4f77cfcfd770c` | 外部接点a | inputNO | circle×2＋rect |

`partId` を持つ要素は3件。

| 要素Id | deviceName | 位置（内部座標） | partId | JSON上の kind |
|---|---|---|---|---|
| `d2ffd5bc…` | ソレノイド1 | row22, col5 | `0b62e369…` | contactNO |
| `a9f516a3…` | ソレノイド2 | row23, col5 | `0b62e369…` | contactNO |
| `95b9687c…` | プログラムON | row5, col1 | `f60e2a88…` | contactNO |

すなわち検体は定義を自ら抱えておりながら、ecad2はそれを見ておらぬ。
これが `P-183`／T-151 の症状そのものにござる。

## 2. 症状の数値への翻訳（着手前に書いたもの。`ninja.md:216`）

予測＝`DRC-PART-001` が3件（上記3要素）。絵はいずれもa接点で描かれる。

`PartResolver.IsUnresolvedPartId`（`PartResolver.cs:98`）が
「PartId が設定されているのに lib で解決できぬ」を判じ、
`DesignRuleCheck.cs:44-45` の `UnresolvedPartId` が拾う筋。

## 3. 観点A——現状（ローカルに定義なし）

判定: NG（症状が出る＝修正前として期待どおり）

| 測ったもの | 予測 | 実測 | 一致 |
|---|---|---|---|
| `DRC-PART-001` の件数 | 3件 | 3件（ソレノイド1／ソレノイド2／プログラムON） | 一致 |
| `DRC-XREF-001` の件数 | 予測せず | 3件（同じ3要素） | ——（下記4節） |
| 絵 | a接点 | 3件ともa接点（縦2本の平行線） | 一致 |

回路番号（キャンバス上の青数字）＝ソレノイド1の左右が 15／16、ソレノイド2の左右が 17／18。
すなわち左右が別ネットに分かれておる。

出力パネルの全件性は二軸で確かめた。
- 軸1＝`OutputGrid` の `ScrollPattern` が `VerticallyScrollable=False`・`VerticalViewSize=100`
  （DataGrid仮想化による取りこぼしが無いことの機械的な裏づけ。スキル6.1節の罠への対処）
- 軸2＝検体JSONの `partId` 保有要素が3件＝`DRC-PART-001` の3件と一致

証跡: `t151-02-drc-before.png`／`t151-03-solenoid-before.png`／`t151-07-crop-before.png`

## 4. `DRC-XREF-001` は予測しておらなんだ副次症状

「機器 ◯◯: リレー接点がありますが駆動コイルが図面上に見つかりません（駆動元不明）」が
同じ3要素に対して出ておる。

観測はここまでにて、帰属は下記5節の対照が示す——定義を置くと同じ3件のうち2件が消えた。
すなわち未解決の要素が `kind=contactNO`（既定値）へ落ちた結果、
本来リレー接点ではないものがクロスリファレンス検査の対象に入っておる、と読める。

ただし `CheckCrossReference` の除外条件までは当たっておらぬゆえ、
機序の断定は実装側（隠密・侍）に委ねる。忍者が申せるのは
「定義の有無で XREF-001 の件数も連動して変わる」という実測のみにござる。

## 5. 観点B——対照（同じ検体に、定義をローカルへ置いた場合）

家老の意図なされた「退ける前・退けた後」の対照を、より強い形で組み直した。
別の図面を作るのではなく、検体1の `library.byId` にあるソレノイド定義を
そのまま `.gcadpart` として `図形\自作\` へ一時的に置き、同じファイルを開き直した。

同一ファイル・同一要素で、ローカル定義の有無だけが変わる形にござる。

判定: OK（症状が消える＝原因の帰属が固まった）

| 測ったもの | 予測 | 実測 | 一致 |
|---|---|---|---|
| `DRC-PART-001` | 1件（プログラムONのみ） | 1件（プログラムON） | 一致 |
| `DRC-XREF-001` | 1件（同上） | 1件（プログラムON） | 一致 |
| ソレノイドの絵 | 定義どおりの折れ線 | 稲妻様の折れ線＋左の線＝定義の polyline と一致 | 一致 |
| プログラムONの絵 | a接点のまま | a接点のまま | 一致 |

外部接点a は置いておらぬゆえ「プログラムON」の2件だけが残った。
これが内部対照として効いておる——同一の実行の中に、解決できる要素とできぬ要素が併存し、
後者だけが警告を出す。ゆえに「症状はローカル定義の不在に起因する」が、
別々の図面を比べるより強く言える。

証跡: `t151-05-argload.png`／`t151-06-solenoid-after.png`／`t151-08-crop-after.png`

> **【訂正・2026-08-16追記】本節の回路番号に関する解釈は誤りにござる。**
> **殿のご指摘（「ソレノイドはコイルに相当するので 15/16・17/18 が正解」）により発覚いたし申した。**
> **原本GuiEcadには `ParticipatesInWiring` が存在せず（実測0件）、`nonSimulated` も結線に参加する**
> **——すなわち原本では 15/16 が正しい姿にござる。**
> **下記で「結線の分断が解消」「正しく解決された」と書いたのは、ecad2固有の挙動を正解と取り違えたもの。**
> **当時の記述は書き換えず残す。詳細は末尾の「訂正の追記」節を見られたい。**

### 予期せぬ所見——回路番号が変わる（結線の分断が解消）

| | ソレノイド1の左右 | ソレノイド2の左右 |
|---|---|---|
| 定義なし（症状あり） | 15 ／ 16 | 17 ／ 18 |
| 定義あり（症状なし） | 15 ／ 15 | 16 ／ 16 |

すなわち症状は見た目とDRC文言に留まらず、電気的な結線の解釈まで変えておる。
未解決の要素は `kind=contactNO` として左右を分断するが、
正しく解決されると `role=nonSimulated` ゆえ結線判定から外れ、左右が同じネットになる
（T-142で扱われた `ParticipatesInWiring` の筋と符合する）。

これは修正後の再検証で最も判りやすい指標になる——番号が 15/15・16/16 になれば、
埋め込み定義が読まれておる証にござる。

## 6. 回帰・範囲外

- 回帰＝無し（本件は実装前の実測ゆえ、比較対象そのものが無い）
- 範囲外検出＝下記7節の2件（いずれも実装の不具合ではなく、検証手法・記録に属する）

## 7. 測っておらぬこと（射程を区切る）

- 外部接点a をローカルへ置いての確認はしておらぬ。「置けば消える」は機序からの類推にすぎぬ
- `DRC-XREF-001` が出る機序（`CheckCrossReference` の除外条件）は一次ソースに当たっておらぬ
- 回路番号の変化の機序は `ParticipatesInWiring` との符合を見たまでにて、コードで辿ってはおらぬ
- 絵の同定は画像の拡大による目視。定義の polyline 座標との数値照合まではしておらぬ
  （形が明確に別物ゆえ足りると判じたが、`ecad2-ui-automation:947` の
  「拡大して判じたものは実寸の見え方ではない」の射程外——本件は判読性ではなく
  同一性の判別ゆえ拡大が正しい手であるが、断りとして記す）

## 8. 操作手法（`ninja.md` 報告フォーマット【MUST】）

- アプリ起動＝1回目は `Start-Ecad2App`、2回目以降は `Ecad2.App.exe <パス>` の引数付き直接起動
  （`Start-Process`）。いずれもセカンダリモニタへ移動して操作
- メニュー操作＝UIA `ExpandCollapsePattern.Expand()` ＋ `InvokePattern.Invoke()`
- タブ切替＝`Invoke-Ecad2ScreenClick`（Win32 `mouse_event` による合成クリック。殿の実操作にあらず）
- ファイルダイアログ＝`Send-Ecad2Keys`（合成キー入力）。ただし下記のとおり2度失敗した
- スクロール＝`RangeValuePattern.SetValue`
- 撮影＝`Save-Ecad2Screenshot`（PrintWindow方式）。切り出し拡大は `System.Drawing` で5倍・最近傍補間
- DRC件数＝UIA `FindAll` による `DataItem` の数え上げ＋`ScrollPattern` での全件性確認

## 9. 検証中に踏んだ罠（落とし先＝`ecad2-ui-automation` スキル）

### (a) 「開く」ダイアログへの `Send-Ecad2Keys` が黙って効かず、DRC 0件を「症状が消えた」と誤読しかけた

2度目のファイル読込で `Send-Ecad2Keys` が届かず、図面が開かぬままDRCを実行した。
結果は0件。ローカルへ定義を置いた直後であったゆえ、
そのまま「対照で症状がすべて消えた」と報ずる一歩手前であった。

救うたのはスクリーンショットの実見にござる——シートが空、キャンバスが無地、
保存・PDF出力のボタンがDisabled。すなわち図面が無かった。

これは `ecad2-ui-automation` 6.8節「入口が開いたことは通れたことではござらぬ」
（結果側の指標で判ずる）がそのまま効いた形にて、条が既に在ったものを引けた例にござる。
ただし引く契機は「0件という数が予測と食い違うた」ことであった
——外部接点a を置いておらぬゆえ最低1件は残る筈、という予測を先に持っておったのが効いた。
`ninja.md:216`（着手前に症状を数値へ翻訳せよ）が、思わぬ形で誤報を止めた。

### (b) ダイアログが閉じずに残り、二重に開いた

(a) の失敗で1枚目が閉じておらず、そこへ2枚目を開いてしまった
（`EnumWindows` で「開く」が2件検出）。スキル6.6節のダイアログ多重化そのもの。

回避策＝ファイルダイアログを使わず、`Ecad2.App.exe <パス>` の引数付き起動で開く。
T-123で追加された起動引数がそのまま検証の道具になった。
以後、検体を開くのはこの手が確実にござる。

---

## 付録・証跡ファイル

いずれも scratchpad 配下（`…\20b0c6a5-…\scratchpad\`）。

| ファイル | 中身 |
|---|---|
| `t151-01-loaded.png` | 検体1の初回読込直後 |
| `t151-02-drc-before.png` | 症状あり・DRC 6件 |
| `t151-03-solenoid-before.png` | 症状あり・ソレノイド部（行23/24）とDRC全件 |
| `t151-07-crop-before.png` | 同・5倍拡大（a接点の絵） |
| `t151-05-argload.png` | 対照・引数起動での読込 |
| `t151-06-solenoid-after.png` | 対照・ソレノイド部とDRC 2件 |
| `t151-08-crop-after.png` | 同・5倍拡大（定義どおりの折れ線） |
| `t151-backup\ソレノイド.gcadpart` | 対照に用いた一時パーツ（退避済み。修正後の再検証で再利用可） |
| `t151-backup\parts-md5-{before,after}.txt` | パーツフォルダ23件のMD5。差分なしを確認済み |
| `t151-backup\main-layout.xml.bak` | 退避したレイアウト。復元してMD5一致を確認済み |

---

# 【訂正の追記】回路番号の解釈が誤っておった（2026-08-16、殿のご指摘により）

**本書 5節および 3節で、回路番号 15/16・17/18 → 15/15・16/16 の変化を
「結線の分断が解消」「修正後の最も判りやすい指標」と書いたが、これは誤りにござる。**

## 何が誤りであったか

殿の御言葉＝**「ソレノイドはコイルに相当するので回路番号は15/16、17/18が正解」**。

忍者が原本と突合した結果（一次ソース実測）——

| | 原本GuiEcad | ecad2 |
|---|---|---|
| `ParticipatesInWiring` | **存在せぬ（`grep` 0件）** | `PartResolver` に在り、`NetlistBuilder.cs:145` で結線から外す |
| `nonSimulated` の扱い | `CreatesComponent` で評価対象外にするのみ。**結線には参加する** | **結線からも外れる** |
| ソレノイドの回路番号 | **15/16**（左右で変わる＝負荷として正しい） | **15/15**（左右が繋がる） |

**すなわち 15/16 が正しい姿にて、15/15 は ecad2 固有の挙動にござる。**

ecad2 側は **T-136(C)（殿裁定2026-08-02、組込みモータのため）で `ParticipatesInWiring` を新設し、
T-142 で自作パーツの `NonSimulated` にも広げた**もの。その射程が
**「非シミュレートだが結線には参加すべき部品」**（ソレノイド＝電磁弁のコイル＝負荷）を
巻き込んでおる公算にござる。

## 修正前が「正しく見えておった」理由

- **修正前**＝定義が解決できず `kind=contactNO`（既定値）扱い → 結線に参加 → **偶然 15/16**
- **修正後**＝定義どおり `nonSimulated` → 結線から外れる → **15/15**

**T-151の実装は定義どおりに正しく動いておる。されどこの図面については見た目が悪化する。**
すなわち **T-151 が既存の穴を露わにした**形にござる。

## 忍者の落ち度——材料は手元に在った

**「電気図面の視点が無かった」では済まぬ。**本書を書いた時点で忍者は——

- 検体の定義を直読し `role: nonSimulated` を**己の目で見て、1節に表で書いておる**
- 引き継ぎ書の「T-142＝非シミュレート部品が結線を断つ穴」も**読んでおる**
- そのうえで5節にこう書いておる——「正しく解決されると `role=nonSimulated` ゆえ結線判定から外れ、
  左右が同じネットになる（**T-142で扱われた `ParticipatesInWiring` の筋と符合する**）」

**すなわち機序を正しく言い当てたうえで、「直った証」と判じておる。**

**視点が無かったのではなく、持っておる材料を繋げなかった。**
**「符合する」と書いた時、符合した先が正しいかを問わなかった**——
**機序が説明できたことで、正しさを確かめた気になっておった。**

**`output-corruption-log.md` `#37` で隠密が退けた「機序の説明が付いたように見えたゆえ」という形が、
本件では実際に当たる**（己のことゆえ忍者が申せる）。

## 何が訂正され、何が残るか

| | 判定 |
|---|---|
| 回路番号を「直った証」とした解釈 | **取り消し** |
| `DRC-PART-001` 3件→0件の実測 | **有効**（数値そのものは正しい） |
| 絵がa接点から定義どおりへ変わった実測 | **有効** |
| 可搬性の実測（退けても解決される） | **有効** |
| 対照検体でDRCの生存を示した手 | **有効** |

**数値の観測はいずれも正しく、誤ったのは「回路番号がどうあるべきか」という期待値の側にござる。**

## 三人とも実装の側からしか見ておらなんだ

判定基準「15/15 になれば直った」は、**忍者・侍・家老の三人で突き合わせて定めたもの**にござる。
**三人とも実装が定義どおり動くかだけを見ており、電気図面として正しいかを問うた者はおらなんだ。**

**殿が実物としてご覧になり、一言で覆された。**

`ninja.md:238`【MUST】＝**「探す対象の定義が原本の一次ソースで確かめられておるか。
証明は正しく、問いが誤っていた」**——本件はその型そのものにござる。

## 【決着】殿ご裁可 2026-08-16＝「ソレノイドはコイルで問題ない」

家老が殿へ直に伺い、御裁可を得た。**対処は部品の役割設定を直す形**にござる——

- **T-151の実装は正しい。**回路番号の件は実装の瑕疵ではなく、**部品の `Role` 設定の問題**であった
- ソレノイドが `Role=Coil` として設定されておれば、**結線に参加し 15/16 が自然に得られる**
- **本節で取り消した判定は、取り消したままで正しい**

**【諮り方が結果を分けた一点】**忍者は家老へ
「殿の御言葉は**判定基準の訂正**であって**今後の運用**についてではない。混ぜて諮られませぬよう」と
区切って申し送った。家老はそれを容れ、**まっさらな形で殿へ伺うた**。

家老の弁＝**混ぜて諮っておれば、殿は「既にそう申した」と受け取られるか、
違う形でお答えになっておったやもしれぬ。**

**【併せて・配置の実測が選択肢を並べさせた】**忍者がJSON直読で
「ソレノイド2件は行の終端にあり、後ろに負荷0件」と確かめておいたゆえ、
**案A（機序を原本へ戻す）を採っても実害が及ばぬと分かり、家老は両案を並べて諮れた**
（結果として殿は案Bをお選びになった）。

**選ばれなかった案の影響を測っておいたことが、諮る形を作った**——
`ninja.md` の「対照で差が出ぬことが期待値である場合がある」と対をなす形にござる。

---

## 環境の後始末

- 一時的に置いた `ソレノイド.gcadpart` は退避先へ移動（削除はしておらぬ）
- `図形` 配下23件のMD5が着手前と完全一致——無傷
- `main-layout.xml` は復元済み（MD5 `1F5881FE…` 一致）。
  検証中に `graceful close` が2度あり上書きされておったが、退避が効いた
- `Ecad2.App` は終了済み
