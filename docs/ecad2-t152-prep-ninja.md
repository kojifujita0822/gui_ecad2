# T-152 実機確認の備え——検体三つと、明日の観点（忍者）

2026-08-16、家老の下命による。**実機そのものは明日**（`DRC-TYPE-001` の手当てが明日入るゆえ、
手当ての前後で二度測らせぬための采配）。本書は検体作りと段取り。

**本日の実測で、明日の期待値はすべて数値・文言まで固まってござる。**

## 0. 測った版

`src\Ecad2.App\bin\Debug\net10.0-windows\Ecad2.Core.dll`(13:10:24) を `Assembly.LoadFrom` し、
**中身で同定した**（`ninja.md:128`）——

- `Ecad2.Model.PartDefinition.IsExcludedFromCrossReference` **在り**＝T-152 実装済み
- `Ecad2.Model.BuiltinPartIds` **在り**＝T-151 も載っておる

侍の申告（納め済み）と一致。

**【番号は訂正した。経緯を残す】** 侍は当初「`8ca7a13`（前半）／`0e3ab7f`（追補）」と申告したが、
**いずれも実在せぬハッシュ**であった（忍者が `git log` で `unknown revision` を確認、
侍も `git cat-file -t` で `Not a valid object name` を確認）。
**正しくは `cb7a1c0`（前半）／`544056f`（追補）。**

**機序（侍の自己申告）**＝コミット後に `git show --stat HEAD | Select-Object -Last 12` を打っており、
**`-Last` は末尾しか取らぬゆえ commit ハッシュ（先頭行）が一度も目に映っておらなんだ。**
それを記憶で書いた形。**T-151・T-123 のハッシュが正しかったのは、
あちらでは別途 `git log --oneline` を打っていたゆえ**——**作法を守った時だけ正しく、省いた時に誤った。**

**この誤りが無害に終わったのは、版の同定を番号でなく型の在否で行うたゆえ**にござる。
**番号が誤っておっても、掴んだ版は正しかった。**

## 1. 検体三つ——いずれも対照を検体に内蔵してござる

**同じ図面の中に「鳴る側」と「鳴らぬ側」を並べる**形にした。
**一度の測定で両方が出るゆえ、除外の実装が余計なものまで巻き込んでおれば即座に露見する。**
（T-151 で効いた作法。侍のテストも同型を採っており、机上と実機で同じ形の網になってござる）

### 検体A＝`sample/t152-thermal.gcad`（`DRC-TYPE-001`）

ローカルの組込みパーツのみを使う（`library: null`）。**`図形` 配下には触れ申さぬ。**

| 機器 | 構成 | 本日の実測（手当て前） |
|---|---|---|
| `THR1` | 本体（`basic-thermal-overload`）＋補助接点a（`basic-thermal-relay-no`）を**同一名** | **エラー `DRC-TYPE-001` が鳴った** |
| `THR2` | 本体のみ | 鳴らず |
| `THR3` | 補助bのみ | 鳴らず |

**文言**＝「機器 THR1: 励磁系接点（ContactNO/NC）と入力系接点（押釦・タイマ等）が混在しています。
シミュレーションが正しく動作しません。」／**severity＝エラー**／該当箇所＝`P1 行1, P1 行1`

**すなわち隠密の検算（同一機器名でのみ発火する）が実機で裏づいた**——
**これまで誰も実機では測っておらなんだ分にござる。**

（併せて `DRC-XREF-002 / CR4` が1件出る。接点を置かなんだゆえで、本件とは無関係）

### 検体B＝`sample/t152-marker.gcad`（マーカー・コイル側）

**自作パーツ定義を `library` へ埋め込む形**にした。**T-151 で入った自己完結の仕組みがそのまま使え、
`図形` 配下を一切汚さずに済む。**

| 機器 | パーツの `isExcludedFromCrossReference` | 本日の実測 |
|---|---|---|
| `MK1` | `true` | **警告が出ぬ**＝除外が効いておる |
| `MK2` | `false` | `DRC-XREF-002` が出る |
| `MK3` | **フィールドそのものを書かず** | `DRC-XREF-002` が出る＝**既定 false として読まれた（後方互換）** |
| `PB1` | 組込みコイル＋接点3つ | 警告なし（正常の対照） |

### 検体C＝`sample/t152-marker-contact.gcad`（接点側と、侍が足した網）

| 機器 | 構成 | 本日の実測 |
|---|---|---|
| `EX1` | マーカー立ちの自作接点のみ | **`DRC-XREF-001` が出ぬ**＝マーカーは接点側にも効く |
| `EX2` | マーカー `false` の自作接点のみ | `DRC-XREF-001` が出る |
| `EX3` | **除外コイル＋同名の通常接点** | **接点側の `DRC-XREF-001` が出る** |
| `OK1`〜`OK3` | 組込みコイル＋同名接点 | 警告なし（正常の対照） |

**`EX3` が侍の足した網そのもの**——「除外されたコイルが、対になる接点側の警告を消さぬこと」。
**実機で効いておることを確かめた。**

## 2. 【侍が区切った射程を、この検体が埋めてござる】

侍の申し送り（本日、忍者の問いに答えて）——

> 某が実測したのは `.gcadpart` 単体の経路（`PartLibrarySerializer.DeserializeOne`）にござる。
> 貴殿の検体は `.gcad` の `library` へ埋め込む経路にて、そちらは某が直に測っており申さぬ。
> 機序は同じと見てござるが、直の実測は無い。

**本日、検体B の `MK3`（フィールド欠落）が `.gcad` の `library` 経路で既定 `false` として読まれ、
従来どおり警告が出ることを実機で確かめた。**

**【二軸で一致した】** 侍はその後、自ら網を1件足して机上でも測り、同じ結論に至っておる——
`library` 配下から当該キーを落とした `.gcad` を `GcadSerializer.Deserialize` に通し、
**(1) `IsExcludedFromCrossReference == false` で読まれること (2) `CheckCrossReference` が
`MK3` に `CoilWithoutContact` を出すこと**の二段で。
**侍の弁＝「値だけ見て『読めた』と判ずれば、検査へ繋がっておらぬ形を素通しする」。**

**射程を自ら区切って申し送られたゆえ、埋めるべき穴が明らかであった。**
**そして忍者が実機で測る間に、侍が机上で同じ穴を埋めた**——**独立の二軸が一致した形にござる。**

### 【境界】侍の網と、この検体が届く先は違う

**侍が測るのは `Diagnostic` が返るところまで。**
**この検体が測るのは、それが実際に画面（`OutputGrid`）へ出るところまで**——侍が自ら区切った分。
**両者が繋がって初めて「使い手に見える」ことの証になり申す。**

## 3. 明日測る観点

| # | 観点 | 検体 | 本日の値（手当て前） | 明日の期待 |
|---|---|---|---|---|
| 1 | `DRC-TYPE-001` の誤発火が消える | A | `THR1` でエラー | **消える** |
| 2 | 対照が巻き込まれておらぬ | A | `THR2`／`THR3` は鳴らず | **変わらず鳴らぬ** |
| 3 | マーカーがコイル側に効く | B | `MK1` 出ず／`MK2` 出る | **変わらず** |
| 4 | 後方互換（フィールド欠落） | B | `MK3` 出る | **変わらず** |
| 5 | マーカーが接点側に効く | C | `EX1` 出ず／`EX2` 出る | **変わらず** |
| 6 | 対になる警告を消さぬ | C | `EX3` 接点側が出る | **変わらず** |
| 7 | **チェックボックスの見え方** | パーツエディタ | **未測** | 配置先コンボの隣に在る |
| 8 | **マーカーの Undo 実挙動** | パーツエディタ | **未測** | `Ctrl+Z` で戻る |

**7・8 は侍が「忍者の領分」と区切った分**。パーツエディタを開く要があり、
`図形` 配下へ触れる恐れがあるゆえ、退避を先に済ませてから測る。

**【観点7・8の的を先に押さえた】**（`PartEditorDialog.xaml` を直読）——

| 項 | 値 |
|---|---|
| チェックボックスの `AutomationId` | **`ExcludeFromCrossReferenceCheck`**（`x:Name` 由来） |
| UIA から見た `Name` | **`クロスリファレンス検査から除外する`**（`Content` 由来） |
| 位置 | **`AffinityCombo`（配置先コンボ、`:64`）の直後 `:73`**＝家老の申された「配置先コンボの隣」と一致 |
| Undo ボタン | `UndoButton`（`Ctrl+Z`）／`RedoButton`（`Ctrl+Y`） |

**測り方の要注意**＝**UIA の `TogglePattern.Toggle()` は Click ハンドラを迂回しうる**
（`ninja.md` 既知の罠）。**まず UIA で測り、疑わしき結果が出たら物理クリックで測り直す。**
**とりわけ Undo の検証は、値が変わったこと自体が Undo 記録の契機ゆえ、
迂回されておれば「Undo できぬ」という偽の結果が出申す。**

**1〜6 は本日値を採ってあるゆえ、明日は同じ手順を一度なぞるだけで差分が出る。**

## 4. 手順（画面占有と環境改変を最小に）

**【本日中に実施と決した】**（2026-08-16 14:20、殿「時間があるゆえ進めてよい」との仰せ、家老の采配）。
隠密のレビューが通り、侍の追補（`544056f`）まで揃うたゆえ、明日へ回さず本日締める。

**手順一番目は 14:21 に実施済み**——`docking-layout` 五件を scratchpad の `t152-backup\` へ退避し、
`図形` 配下22件の MD5 も記録した。**本日落とした条を、その日のうちに引いた形にござる。**

1. **`main-layout.xml` を先に退避する**（下記5の反省）
2. 侍へ三点の問い（実機を握る前の【MUST】）
3. 版を中身で同定（`IsExcludedFromCrossReference` の在否＋手当ての新しい印）
4. 検体A→B→C の順に引数付き起動し、各々 DRC を走らせて `OutputGrid` を読む
   - **`OutputGrid` は必ず `AutomationId` で名指しすること**（下記5）
   - メニューは `Expand()` と `Invoke()` を**同じ呼び出しの中で**行う（別呼び出しではメニューが閉じる）
5. 観点7・8＝パーツエディタ。`図形` 配下の退避を先に

## 5. 【本日の己の誤りと怠り】

### (あ) `OutputGrid` の取り違え（本日午前、T-151 で踏んだもの）

`FindFirst` で `ControlType` だけを条件に `DataGrid` を掴み、**機器表を DRC の器と取り違えて
「0行」を得、回帰を疑いかけた。** 実際の DRC 結果は `OutputGrid` の側にあった。

**同じ `ControlType` の器が複数在るのは常にござる。`AutomationId` で名指しすること。**
本日午後の測定はすべて名指しに改めてござる。

### (い) `main-layout.xml` の事前退避を二度怠った

**一度目（T-151）は実害なし**——`Stop-Ecad2App` が強制終了に落ち、終了時の保存が走らなんだゆえ。
**二度目（本件）は実害が出た**——`Stop-Ecad2App` が正規終了に成功し、保存が走った。

**変わったのは `LastActivationTimeStamp` 三箇所のみで、レイアウト構造は不変**
（`Compare-Object` で34行中6行、いずれも時刻属性）。**ゆえに実害は軽微。**

**されど学びは重い**——**`Stop-Ecad2App` は強制終了に落ちることも正規終了に成功することもあり、
どちらに転ぶかは事前に判じられぬ。** スキルには「`Window_Closing` を経由せぬ罠」が既載なれど、
**経由する場合もあるのが実相**にござる。**ゆえに事前退避が要る**——条の理由を身をもって知った。

## 5-2. 【実施結果】本日中に締めた——八観点すべて PASS（14:21〜14:35）

殿「時間があるゆえ進めてよい」との仰せを受け、明日へ回さず本日実施した。

### 測った版——中身で同定

`Ecad2.Core.dll`（14:20:09、侍が通常形で建て直した分）を `Assembly.LoadFrom` し、

- **`Ecad2.Simulation.DesignRuleCheck.IsExcludedFromDeviceTypeConsistency` 在り**
  ＝**本日の追補（`DRC-TYPE-001` の手当て）を含む版**
- `IsExcludedFromCrossReference` 在り＝T-152 前半も載る

**弁別点を忍者が独立に見つけ、侍の答えと一致した**——
`IsExcludedFromCrossReference` は前半（`cb7a1c0`）で入ったものゆえ**追補の弁別には使えぬ**。
`git show <追補> -- DesignRuleCheck.cs` で新設メンバーを拾い、それを印とした。

### 結果——変わったのは `THR1` ただ一つ

| # | 観点 | 修正前 | 修正後 | 判定 |
|---|---|---|---|---|
| 1 | `DRC-TYPE-001` の誤発火 | `THR1` で **エラー** | **消えた** | **PASS** |
| 2 | 対照（本体のみ／補助のみ） | 鳴らず | 鳴らず | PASS（不変） |
| 3 | マーカー・コイル側 | `MK1` 出ず／`MK2` 出る | 同じ | PASS（不変） |
| 4 | 後方互換（フィールド欠落） | `MK3` 出る | 出る | PASS（不変） |
| 5 | マーカー・接点側 | `EX1` 出ず／`EX2` 出る | 同じ | PASS（不変） |
| 6 | 対になる警告を消さぬ | `EX3` 出る | 出る | PASS（不変） |
| 7 | チェックボックスの見え方 | — | 下記 | **PASS** |
| 8 | マーカーの Undo | — | 下記 | **PASS** |

**家老の立てた「変わる筈は `THR1` ただ一つ、他が動けばそれが回帰」に照らし、回帰なし。**

### 観点7＝数値で示す

| 項 | 実測 |
|---|---|
| `ControlType` | `ControlType.CheckBox` |
| `Name` | `クロスリファレンス検査から除外する`（殿ご裁可の文言と一致） |
| 既定の `ToggleState` | **`Off`**（殿ご裁可「既定オフ」と一致） |
| 位置 | `AffinityCombo`（配置先コンボ）の**右隣・同じ行**——**Y差 3px／間隔 16px** |

「配置先コンボの隣」を**目視の印象でなく座標の差で示した**。

### 観点8＝値だけでなく「履歴が伸びたこと」まで測った

| 段 | `ToggleState` | `UndoButton.IsEnabled` |
|---|---|---|
| 変更前 | `Off` | **`False`**（履歴なし） |
| `Toggle()` 後 | `On` | **`True`**——**Undo 記録が走った証** |
| Undo 後 | **`Off`（戻った）** | — |

**値が戻ったことだけを見れば「戻った」で終わるが、`IsEnabled` の `False→True` が
『記録が積まれた』ことを別軸で示してござる。**

**併せて、UIA の `Toggle()` が迂回されておらぬことも、この `IsEnabled` の変化が証している**
——迂回されておれば履歴は伸びず `False` のままであった筈。
**すなわち `IsEnabled` の変化は、UIA 迂回の有無を判定する指標としても使える。**

**この観点は侍が実装者判断で範囲を広げた分**（`SheetAffinity` と同じ扱い、T-144 の作法に倣う）にて、
**家老の裁可は得ておったが実機では誰も測っておらなんだ。**

### 環境を汚さぬための工夫

**パーツエディタは「新規作成」から開いた**——既存の自作パーツを開けば誤って保存する恐れがあるが、
**新規なら保存せぬ限りファイルは生まれぬ。** 測り終えて**キャンセルで閉じた。**

**測り方で一度つまずいた**——ダイアログを「デスクトップの子ウィンドウ」として探したが見つからず、
**操作が通っておらぬと疑った。** スクリーンショット・`EnumWindows`・UIA の三軸で確かめたところ、
**ダイアログは開いており（`自作パーツ新規作成`）、UIA ではメインウィンドウの子孫として現れておった。**
**探し方が的外れであっただけ**にござる。

## 6. 環境の後始末

- `Ecad2.App` 終了済み
- **`図形` 配下22件が本日の着手前と完全一致**（`Compare-Object` で差分ゼロ）。
  **検体三つとも `library` 埋め込みかローカル組込みパーツのみを使うたゆえ、一つも触れておらぬ**
- `main-layout.xml` は上記5-(い)のとおり時刻属性のみ更新。家老の判断＝**復元は無用**
  （時刻属性を戻す意味は薄く、戻す作業自体が新たな書き換えになるゆえ）

### 【本実施ぶんの後始末＝条を引いた効果が出た】（14:35）

**手順一番目として `docking-layout` 五件を退避しておいたゆえ、今回は戻せた。**

- 終了後、`main-layout.xml` は **`B847A325…` → `6A2DCAB9…`** へまた上書きされておった
  （`Stop-Ecad2App` が二度とも `graceful close` で通ったゆえ）
- **退避から無条件に復元し、五件すべて MD5 一致を確認**（`SKILL.md :408` の作法どおり）
- **`図形` 配下22件は検証前と完全一致**（`Compare-Object` で差分ゼロ）。
  パーツエディタを「新規作成」から開き、キャンセルで閉じたゆえ一つも触れておらぬ

**本日、同じ怠りを二度繰り返し、一度は実害を出した。**
**その条を落とした直後の三度目で、初めて退避してあった。**
**——条は落とすだけでは効かず、次の一度で引いて初めて効くものにござる。**
- 検体三つは `sample/` 配下（`.gitignore` 済みゆえ git は汚れ申さぬ）
- 証跡＝scratchpad の `t152-01-thermal-drc.png`／`t152-02-marker-drc.png`／`t152-03-contact-drc.png`
