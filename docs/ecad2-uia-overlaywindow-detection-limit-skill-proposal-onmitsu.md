# ecad2-ui-automationスキルへの反映検討: OverlayWindow(十字型ドロップターゲットUI)検出限界

調査日: 2026-07-20　調査者: 隠密　委任元: 家老
対象: `docs-notes/handover-next-session.md` §5-8に残る懸案（T-099(c)十字型UI位置ズレ調査での忍者実測）

---

## 1. 結論

**反映すべきと判断する。** 理由は以下2点。

1. 今後の別タスク（T-103「枠ドロップ方式」PoC含む）でも、AvalonDockのドラッグ中一時UI検証で
   同じ壁に再度ぶつかる可能性が高く、次回以降のセッションが同じ試行錯誤（今回は3回）を
   繰り返す損失を防げる。
2. T-103自体が「AvalonDock標準OverlayWindow/DropTargetに依存しない独自方式」という設計思想
   であり、これは図らずも今回の限界（標準UIはUIA検証しにくい）を踏まえた設計といえる。
   T-103のPoC実機確認で「独自の枠UIならUIA/EnumWindowsで検出できるか」を検証する際、
   本教訓が対比の基準として役立つ。

---

## 2. 反映先: 6節「トラブルシュート」への新規項目追加を推奨

理由: 6節は既存項目（例: 386-395行目「PlacementToolBarDockingManagerがPrintWindow・UIA両方で
捕捉できない」等）が「日付・実証タスク・現象・対策」の形式で時系列に積まれる構成であり、
今回の事例もこの形式に合致する。0節「前提・既知の制約」の末尾（59-67行目、AvalonDockペイン
ドラッグの既存教訓）と直接関連するため、そちらへの短いクロスリファレンス追記も併せて提案する。

---

## 3. 追記文言案（6節末尾、438行目の後に追加）

```markdown
- **【新規のUIA限界】AvalonDock標準のOverlayWindow/DropTarget機構（ドラッグ中に一時的に現れる
  十字型ドロップターゲットUI）は、`SetCursorPos`+`mouse_event`合成によるマウスドラッグでは
  検出できないことがある**（2026-07-19、T-099(c)十字型UI位置ズレ検証で実証）。忍者が3回試行
  したが、`EnumWindows`・`CopyFromScreen`いずれの手法でも十字型UI自体を一度も検出できず、
  副次的にフロートウィンドウのBoundsが不自然な動き（Y座標固定・X座標のみ変化・Width縮小）を
  観測するに留まった。0節「AvalonDockペインのドラッグ操作」で述べた`Invoke-Ecad2Drag`（多段階
  マウス合成）はタブ切り離し・境界リサイズの成立には有効だが、**ドラッグ中にのみ一時的に生成
  されるOverlayWindow自体の検出には別の壁がある**とみられる（原因未特定。合成マウス操作が
  AvalonDock独自の`DragService`にドラッグ開始と認識されず`CreateOverlayWindow()`自体に到達
  していない可能性、OverlayWindowの特殊なウィンドウ属性がEnumWindows等の標準列挙で拾いにくい
  可能性のいずれも排除できない）。本家AvalonDockも実ドラッグの自動テストを行わず内部API直接
  呼び出しで代替している（0節既存記述参照）ことから、構造的な自動化限界の可能性が高い。
  **この種の検証（ドロップターゲットUIの表示位置・タイミング等）は、UIA/EnumWindows合成操作
  で粘らず、早期に殿ご自身の実機目視・操作確認へ切り替えることを検討する**。
```

## 4. 0節への短いクロスリファレンス案（67行目の直後、既存教訓の末尾に一文追加）

```markdown
  （※ただし、ドラッグ中に一時的に現れるOverlayWindow=十字型ドロップターゲットUI自体の検出には
  別の限界がある。6節「トラブルシュート」の同名項目参照）
```

---

## 5. 不明点

- OverlayWindow検出失敗の技術的原因（DragService未認識か、ウィンドウ属性起因か）は未特定の
  まま。今回は推測の域を出ないため、上記案文言でも「可能性のいずれも排除できない」と明記した。
- 次回T-103 PoCで独自枠UIの検出可否を試すことで、間接的にこの原因究明の手がかりが得られる
  可能性はあるが、これは範囲外の派生提案につき `docs/proposed.md` 経由が筋と考える（自ら着手せず）。
