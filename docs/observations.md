# 経過観察一覧

修正しない合意済みの経過観察をまとめる一覧。台帳(`docs/todo.md`)の各タスク備考に埋もれると
再発時に見つけにくいため、1件1行で切り出す。新規の経過観察が発生した場合はここへ追記する。

| # | 症状 | 由来タスク | 再発条件 | 合意日 |
|---|------|-----------|---------|--------|
| 1 | Space押下中の強制フォーカス移動で記録が残留し、直後に同一ボタンをUIA InvokeするとFocusCanvas判定が1回ズレうる | T-021 増分(vi) | 検証専用級の極端条件（Space押下中の強制フォーカス移動の直後に、同一ボタンへ連続でUIA Invokeする）。通常操作では自己修復する。 | 2026-07-04 |
| 2 | Alt+Tab等でウィンドウが非アクティブ化した場合も、#1と同種の記録残留が起こりうる | T-021 増分(vi) | ウィンドウ非アクティブ化（Alt+Tab等）の直後の操作 | 2026-07-04 |
| 3 | ToolMode未配線モード（PlaceConnector/PlaceFrame/PlaceLine/PlaceDot/PlaceWireBreak）が将来配線されると、Esc層2の解除対象（`Tool.Mode == PlaceElement` 限定）から漏れる | T-021 増分(iv) | 上記モードが将来実装され、かつ配置中にEscを押した場合。現状はモード自体が到達不能のため実害なし。**当該モードを配線する増分で要注意。** | 2026-07-04 |
| 4 | 未選択状態から最初にUp/Leftキーを押した場合のみ、追従スクロールがスキップされる | T-021 増分(v) | 未選択状態（`SelectedCell` 既定値 `(0,0)`）から Up/Left キーを最初に押下した場合、`current` と `newCell` が共に `(0,0)` で一致するため発火しない。`(0,0)` は通常最初から可視のため実害は低い。 | 2026-07-04 |

## 参照

- 各行の詳細な経緯・議論は `docs/todo.md` の該当タスク備考（T-021行）を参照。
- 役割定義: `docs-notes/roles/{karo,samurai,ninja,onmitsu}.md`
