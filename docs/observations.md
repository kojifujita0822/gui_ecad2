# 経過観察一覧

修正しない合意済みの経過観察をまとめる一覧。台帳(`docs/todo.md`)の各タスク備考に埋もれると
再発時に見つけにくいため、1件1行で切り出す。新規の経過観察が発生した場合はここへ追記する。

| # | 症状 | 由来タスク | 再発条件 | 合意日 |
|---|------|-----------|---------|--------|
| 1 | Space押下中の強制フォーカス移動で記録が残留し、直後に同一ボタンをUIA InvokeするとFocusCanvas判定が1回ズレうる | T-021 増分(vi) | 検証専用級の極端条件（Space押下中の強制フォーカス移動の直後に、同一ボタンへ連続でUIA Invokeする）。通常操作では自己修復する。 | 2026-07-04 |
| 2 | Alt+Tab等でウィンドウが非アクティブ化した場合も、#1と同種の記録残留が起こりうる | T-021 増分(vi) | ウィンドウ非アクティブ化（Alt+Tab等）の直後の操作 | 2026-07-04 |
| 3 | ToolMode未配線モード（PlaceConnector/PlaceFrame/PlaceLine/PlaceDot/PlaceWireBreak）が将来配線されると、Esc層2の解除対象（`Tool.Mode == PlaceElement` 限定）から漏れる | T-021 増分(iv) | 上記モードが将来実装され、かつ配置中にEscを押した場合。現状はモード自体が到達不能のため実害なし。**当該モードを配線する増分で要注意。** | 2026-07-04 |
| 4 | 未選択状態から最初にUp/Leftキーを押した場合のみ、追従スクロールがスキップされる | T-021 増分(v) | 未選択状態（`SelectedCell` 既定値 `(0,0)`）から Up/Left キーを最初に押下した場合、`current` と `newCell` が共に `(0,0)` で一致するため発火しない。`(0,0)` は通常最初から可視のため実害は低い。 | 2026-07-04 |
| 5 | 母線可視スクロールのwiden判定で、`grid.Columns<=0` の場合にviewColumnクランプが-1となり判定が誤って真になる | T-021 増分(v)追加修正 | `.gcad`ファイルの手編集/破損でColumns<=0になった場合のみ。UI操作ではColumnsは常に20固定で、正規経路では到達不能。 | 2026-07-05 |
| 6 | 極端な高ズームかつキャンバス領域を大幅に狭めた状態では、母線側へ拡張したRect幅がビューポート幅を超え、選択セル自体が視野外に押し出される可能性が理論上ある | T-021 増分(v)追加修正 | 極端な高ズーム（400%程度）＋GridSplitterで中央列を大幅縮小の複合条件。既定ウィンドウ幅・実機検証済みの150%ズームでは到達しない。 | 2026-07-05 |
| 7 | シート改名ボタンが起動直後（シート0枚・SelectedSheet=null）でも押下可能に見えるが、押しても無反応（コードビハインド側でガード済み、クラッシュなし） | T-019 追加実装 | 起動直後（濃紺・シート0枚）で改名ボタンを押下。Clickイベント直結のためCanExecute連動によるグレーアウトが無い。 | 2026-07-05 |
| 8 | LadderCanvasHost_PreviewMouseLeftButtonUpがCurrentSheetのnullチェックをしていない | T-019 追加実装 | 現状はClear()後Width/Height=0でヒットテスト自体が発生せず実害なし。将来キャンバスの空状態表示方法が変わった場合のみ顕在化しうる。 | 2026-07-05 |
| 9 | システムシャットダウン/ログオフ時、Window_Closing内のMessageBox/SaveFileDialogがOSに応答遅延と判断され強制終了→未保存確認の保護が効かない可能性（低確度・実測未検証） | T-019 追加実装 | シャットダウン/ログオフ時に未保存変更（IsDirty=true）がある場合。 | 2026-07-05 |
| 10 | .gcadpart重複IDの先勝ち判定（CreationTime最古優先）が、robocopy /COPY:DAT等タイムスタンプ保持型の複製ではCreationTime一致→パス辞書順タイブレークとなり、辞書順逆転（コピー側勝ち）が再発しうる（隠密実機確認済み） | T-035 往復1周目 | robocopy等タイムスタンプ保持ツールでの.gcadpart複製時のみ。Explorer標準コピーでは発生しない。 | 2026-07-06 |
| 11 | マイドキュメントがOneDriveリダイレクトされる環境で、再同期/再ハイドレート時にCreationTimeが書き換わり、#10と同様に先勝ち判定が狂いうる（Microsoft公式Q&Aに事象報告あり、ecad2実運用環境では未検証） | T-035 往復1周目 | 部品フォルダがOneDrive管理下にあり、再同期/再ハイドレートが発生した場合。優先度中〜低。 | 2026-07-06 |
| 12 | ORa/ORb項目クリック後、ダイアログで非接点系（端子台等）へ切替えてOKするとisOrが無言で無効化される（安全側の正しい挙動だが、メッセージ等がなくOR意図が静かに握り潰される点はUX上の軽微な余地） | T-037 往復1周目 | ORa/ORb項目クリック→ダイアログで非接点系へ種別切替→OK確定した場合のみ。 | 2026-07-06 |
| 13 | 配置バー表示直後、初期フォーカスのBeginInvoke(Loaded)実行前のごく短い窓でTab等を押すと想定外のフォーカス順序になりうる（バー自身は無効化対象外のため操作可能。DispatcherPriority.Loaded(6)>Input(5)ゆえ実害小、隠密PLAUSIBLE） | T-033 増分1修正 | バー表示直後の遅延窓内でTab等のフォーカス移動キーを押した場合のみ。 | 2026-07-07 |
| 14 | プロパティパネルのDeviceNameBox編集中にF5等で新規配置をトリガーすると、IsPlacementBarVisible=trueの作業域無効化で編集中Boxが強制フォーカス喪失し、LostKeyboardFocusがTryPlaceElement実行中に再入発火する（無害な再描画1回程度、隠密PLAUSIBLE） | T-033 増分1修正 | 既存要素のプロパティパネル編集中に、別セルへF5等で新規配置をトリガーした場合のみ。 | 2026-07-07 |
| 15 | 配置バーのMargin計算がRootLayoutGrid絶対座標とバー親セル相対座標を区別せず、現状「ルートRow0+Row1=実質0」に依存して成立している（隠密CONFIRMED・現時点で実害なし）。将来ルートGridのRow0/Row1に単独専有の子要素が追加されると座標ズレが再発しうる技術的負債 | T-033 増分2修正(bfa8994) | ルートGridのRow0/Row1へ単独専有の子要素を追加する構造変更をした場合のみ。P-025（App層リファクタ）検討時の候補。 | 2026-07-07 |
| 16 | 配置バーがMarginを「位置決め」と「測定リセット値」の二重役割で使う設計は、将来リサイズ追従等の新経路が追加されると同種のMargin汚染バグが再発するリスクを残す（隠密PLAUSIBLE・現状バグではない）。Canvas.Left/Top方式への置換が検討候補 | T-033 増分2修正(5c73b66) | 配置バーのMargin/Measure周りに新経路（リサイズ追従等）を追加する改修をした場合のみ。P-025等でMargin/Measureに再度手を入れる機会に検討。 | 2026-07-07 |
| 17 | GX様式グリフのPath DataがPartEntryToGlyphGeometryConverter（増分4）・MainWindow.xamlツールバー（T-040）・PartThumbnailRenderer（T-043、参照方向の制約で共有不可）の**3箇所**にハードコードされており、一部だけ直すと見た目が食い違う（隠密PLAUSIBLE。T-040グリフが1日に3回作り直された実績あり、リスクは実績ベース） | T-033 増分4(025e8d6)・T-043(c56b13c) | グリフ（F5〜F8・sF5/sF6等）のPath Dataを一部の箇所だけ変更した場合。次にグリフへ手を入れる機会に共通リソース化を検討（App→Rendering.Wpf参照方向の制約に留意）。 | 2026-07-07 |
| 18 | 配置バー初期選択の第2段階フォールバック（Id一致のみ）は理論上OR意図が静かに失われる余地がある。現状はBasicPartTemplatesハードコード＋Enumerate()自己修復＋SaveCustomカテゴリ分離の三重保証で到達不能だが、保護テストが無い（隠密PLAUSIBLE） | T-033 増分5(d089f88) | 将来、新規OR対象パーツ追加時にIsOrEligible設定を忘れる等で顕在化しうる。OR対象パーツを増やす改修時に保護テスト追加を検討。 | 2026-07-07 |

## 参照

- 各行の詳細な経緯・議論は `docs/todo.md` の該当タスク備考（T-021行）を参照。
- 役割定義: `docs-notes/roles/{karo,samurai,ninja,onmitsu}.md`
