# 提案・気づきリスト（未承認・着手禁止）

家老の裁量では着手しない置き場。忍者・隠密の調査中に得た「気づき」や、`docs/todo.md` の
範囲外にある改善提案はここに記録し、殿の承認が付いたものだけ `docs/todo.md` へ移す
（詳細は `docs-notes/roles/karo.md` の「采配の権限線引き」）。

| ID | 発見元 | 内容 | 元タスクとの関係 | 規模見積 | 殿の判断 |
|----|--------|------|-----------------|---------|---------|
| P-001 | 忍者（T-002検証中） | PdfExporterが1ページで打ち切られる仕様。PoCとしては範囲内だが、本実装で複数ページ図面を扱う場合は要対応 | T-002範囲外（本実装時の課題） | 小（未見積） | 保留継続（殿裁定2026-07-10、将来実装拡張時に再検討） |
| P-002 | 侍（T-026実機検証対応中、忍者のデバッグログが端緒） | `DiagramRenderer.PageSize()`(src/Ecad2.Core/Rendering/DiagramRenderer.cs 121-126行)が`Sheet.Grid.Rows`を無視し配置済み要素の最大行のみでキャンバス高さを計算するバグ。既に正しい`TotalRows(sheet)`(62-67行)が同ファイルに存在するのに未使用（車輪の再発明バグ）。要素が少ないシートでキャンバスが極端に小さくなり、空きセルへのヒットテストが届かない。T-026のOR入力検証（観点3・6）がこのバグでブロックされている | Ecad2.Core共有レンダリング層の既存バグ、T-026範囲外だがT-026完了の前提条件 | 小（PageSize()内1箇所をTotalRows()呼び出しに統一、影響範囲はPageSize呼び出し元＝PDF出力等） | approved → T-026の一環として修正（2026-07-03） |
| P-003 | 侍（T-026実機検証対応中、忍者の追加バグ報告が端緒） | `DiagramRenderer.cs`の`LeftTerminator`/`RightTerminator`(367-387行)が、縦コネクタの`TopRow`(基準行、本来は母線に繋がるべき)と`BottomRow`(新規行、縦コネクタ経由でのみ接続)を区別せず`(c.TopRow == row \|\| c.BottomRow == row)`という条件でどちらの行も母線への直接接続線を省略してしまうバグ。OR入力で生成した並列回路ブロック全体が母線から浮いた状態になる。GuiEcadから移植されたコアロジックの既存バグ | Ecad2.Core共有レンダリング層の既存バグ（GuiEcad由来）、T-026範囲外だがT-026完了の前提条件。手動配置の縦コネクタ機能（既存）にも影響する変更 | 小（条件を`c.BottomRow == row`のみに変更）。GcadCompatibilityTests等で回帰確認予定 | approved → T-026の一環として修正済み（コミット282f3eb、2026-07-03）。列位置の確認点：既存DrawRungWiresは元々「各行の要素をColumn順ソートし先頭要素(k==0)からのみ母線へ線を引く」設計で、今回の修正(TopRow/BottomRow区別)とは独立した別軸と判明、基本ケースでは正しく組み合わさることを確認。基準行に複数要素があり新要素が末尾寄りの複雑ケースは今回のOR実装スコープ外として扱う。GcadCompatibilityTests回帰なし |
| P-004 | 家老（殿の指示） | 忍者・隠密のSOP化。侍SOP採用の効果（属人性排除・報告定型化・品質下限保証）を実機検証/調査の2役へ拡大 | 運用体系の拡張。殿裁定で方式変更：別SOPファイル+スキルではなく `docs-notes/roles/{ninja,onmitsu}.md` に手順・制約・統一報告フォーマットを直接統合 | 小（role.md 2本の追記のみ） | approved（2026-07-04殿裁定：1.家老起草 2.兼務記述は最小 3.role.mdに統合 4.報告統一OK）→ **実装済み**（ninja.md/onmitsu.md 更新完了、要コミット） |
| P-005 | 忍者（T-020検証中） | `DeleteSheetButton` が最後の1枚のみの状態でもUI AutomationからIsEnabled=Trueと報告され続ける。実害なし（CanExecuteガード `Sheets.Count > 1` は正しく機能し、クリックしてもコマンドは実行されず0枚にはならない）。ボタンの見た目上のIsEnabled反映に遅延/不整合がある可能性 | T-020範囲外の気づき。シート管理＝T-019と関連 | 小（未見積） | 保留継続（殿裁定2026-07-10、実害なしのため記録のみ） |
| P-006 | 隠密（T-021増分1コードレビュー、忍者と並行Wチェック） | 新規`Keyboard.Focus(LadderCanvasHost)`（TryPlaceElement末尾）は、F5等のツール選択キー経由で「既存選択セルへの即時配置」でも一律実行される。機器表など他パネルで作業中にF5を押すと、配置後に強制的にキャンバスへフォーカスが奪われる副作用が生じる | T-021増分1の範囲内の副作用。UI/UX分岐（他パネルからの強制フォーカスの是非） | 小〜中（対処方法次第） | **resolved（2026-07-04、実装→検証→確認サイクルで家老裁量）**：F5経路は現状維持（常にキャンバスへ強制フォーカス、一貫した挙動として採用）。コード変更なし。※3ツールバーボタン(選択/配置/自作パーツ)経由のクリック起因フォーカス問題は別途「懸念4」として設計集約プラン(下記関連ファイル参照)で対応中 |
| P-007 | 忍者（T-021増分(v)追加修正の実機検証中） | ズーム操作直後に選択が未選択に戻る現象を観測→**忍者訂正（2026-07-05）：自作UI Automation合成ヘルパー（Ctrl+ホイール合成）固有の副作用と判明、アプリの実挙動ではない**。詳細: `docs-notes/ecad2-t021-increment-v-additional-fix-verification-ninja.md` | T-021範囲外の気づき | - | withdrawn（対応不要、2026-07-05忍者訂正） |
| P-008 | 隠密（T-019追加実装d9aa49bレビュー） | IsDirty/MarkDirty方式は「新規変更操作追加時の呼び忘れ」の構造的リスクを抱える（GuiEcadの同型問題の前例あり、本コミット自体もNotifyHasProjectChanged漏れの前例を含む）が、対象がEcad2.App層のためテストが0件。最低限の回帰テスト追加（App層ViewModelテストの新設=tests体系の追加を伴う）を提案 | T-019範囲外の品質改善提案 | 中（テストプロジェクト新設+ViewModelのテスト容易性確認） | approved → T-034として起票（2026-07-05殿裁定） |
| P-009 | 隠密（T-015レビュー） | `.gcadpart`ファイルをエクスプローラでコピーすると`PartDefinition.Id`（生成時Guid固定）が重複したまま残り、PartLibrary構築が後勝ち上書きのため、コピー後に片方だけ編集すると誤ったサムネイル・定義参照が生じうる。対処は読込時のID重複検出・再採番等、データ形式・読込仕様に触れる設計判断が必要 | T-015範囲外（既存のID管理構造の問題がサムネイル表示で顕在化） | 小〜中（重複検出+再採番の設計次第） | approved → T-035として起票（2026-07-05殿裁定：読込時に重複検出+再採番） |
| P-010 | 忍者（T-015実機検証） | 部品選択リストの固定種（`BasicPartTemplates.All()`）とツールバー固定ボタンのラインナップが不一致——ツールバーにはORa/ORb配置ボタン（Shift+F5/F6）があるが、リストにはORa/ORbが無く代わりに「セレクトSW」が含まれる。T-007移植（88ea0fd）由来の既存構成。どちらのラインナップに揃えるかはUI/UX判断 | T-015範囲外（既存構成の不一致がサムネイル追加で可視化） | 小（テンプレート定義の追加/整理） | approved → T-037として起票（2026-07-05殿裁定：案1=リストへORa/ORb追加。隠密調査`docs/archive/ecad2-p010-or-fixed-parts-investigation-onmitsu.md`参照） |
| P-011 | 忍者（T-015実機検証） | 要素配置時に機器表（DeviceTableGrid）へデバイス名が反映されない——`PlaceElementAtSelectedCell`（T-026由来）が`sheet.Elements.Add`と`MarkDirty`のみで`Document.Devices`登録・機器表Refreshを呼ばず、プロパティパネルでのデバイス名編集時のみ反映される。部品選択リスト・ツールバー直接配置とも同一。配置直後から機器表に載るべきか否かの仕様確認含む | T-015範囲外（T-026由来の既存欠陥） | 小（配置経路への登録処理追加） | approved → T-036として起票（2026-07-05殿裁定：修正する） |
| P-012 | 忍者（T-036実機検証） | 行0（最上段の行）へ要素を配置すると母線（水平線）が描画されない。要素データ自体は正常（プロパティパネル・機器表に反映済み）で描画のみの問題と思われる。未調査。**追加観測（2026-07-06、T-037検証中・忍者）**：行0で基準要素を配置するとシンボル自体が描画されず別要素のラベルが誤表示される描画異常も検出（同種の根本原因の可能性、行0以外の通常位置では正常）。**隠密の静的調査（2026-07-06、`docs/ecad2-p012-investigation-onmitsu.md`）＝決定打なし（否定的結果）**：DiagramRenderer/GridGeometry/NetlistBuilder/WpfRenderer等を一通り調査したが「行0の特別扱い分岐」「row=0のみ破綻するオフバイワン」は不在、座標計算は線形・母線番兵（Row=-1）非衝突・行数不足なし。ラベル重なり説も座標計算で10mm離れておりREFUTED寄り。次善策=実機の座標実測（診断ログ注入=src変更ゆえ殿承認待ち）or 忍者の再現詳細記録（コード変更なし、2026-07-06委譲済み）。**忍者の再現試行（2026-07-07、`docs-notes/ecad2-p012-reproduction-attempt-ninja.md`）＝6条件すべて不再現**（a接点/端子台×キーボード/部品リスト物理クリック×保存再読込往復・OR基準行0の1連鎖/3連鎖、母線・シンボル・ラベルとも正常描画）。静的（隠密）・実測（忍者）の二系統とも決定打なし。原観測が一時的/環境固有事象・再現条件未到達・別コミットで意図せず解消のいずれかの可能性があるが裏付けなし。診断ログ注入は再現条件不明ゆえ効果未知数、保留継続が妥当（忍者・家老同意見） | T-036範囲外の気づき（描画層） | 不明（静的・実測とも決定打なし） | 保留継続（殿裁定2026-07-10、既に忍者・家老同意見の保留を追認） |
| P-013 | 隠密（T-036デバイス名修正レビュー） | デバイス名欄の編集中にCtrl+S/N/O・ウィンドウクローズを行うと、フォーカスが動かずUpdateSource契機が無いため、未確定編集がサイレントに保存漏れ／無確認破棄されうる。旧LostFocus実装時代からの独立課題（新規回帰にあらず、隠密検証済み）。対処には「未確定編集を確定してから保存すべきか・破棄か・確認を挟むか」の仕様判断を含む | T-036範囲外（T-019のIsDirty機構導入時点からの既存課題） | 小〜中（仕様判断次第） | approved → T-049として起票（2026-07-10殿裁定：確定してから保存する方式を採用） |
| P-014 | 隠密（T-039再々レビュー、code-review併用の独立探索） | トレースログ有効化の環境変数判定で全角ラテン文字（ｆａｌｓｅ/ｏｆｆ/ｎｏ等）が正規化されず、修正済みの全角「０」と同型の事故が兄弟パターンとして残存（実機検証済み）。隠密所見：`string.Normalize(NormalizationForm.FormKC)`なら全角数字・ラテン文字を1回で解決でき、自前実装より広くカバー（実機確認済み）。既定OFFのオプトイン診断機能内でありアプリ本体機能への影響なし | T-039範囲外（finding4修正の方式置き換えを含むため裁量対象外） | 小（TraceLog.cs 1ファイル） | approved → T-050として起票（2026-07-10殿裁定：兄弟課題P-015と統合） |
| P-015 | 隠密（T-039再々レビュー、code-review併用の独立探索） | 兄弟2ファイル（`DeviceTableViewModel.cs` の `Refresh()`、`SheetNavigationViewModel.cs` の `SelectedSheet` setter）にfinding3と同型の旧値null化が現存。T-039初回実装時から未対応だった箇所（退行ではなくスコープ外）。既定OFFのオプトイン診断機能内でありアプリ本体機能への影響なし | T-039範囲外（初回実装時からのスコープ外箇所） | 小（2ファイル） | approved → T-050として起票（2026-07-10殿裁定：P-014と統合） |
| P-017 | 隠密（T-035レビュー、実装追跡で発見） | `PartResolver`が未解決のPartId（参照切れ）を警告なく`ElementKind.ContactNO`へフォールバックし、ランプ/コイル等が「a接点」として静かに描画・シミュレートされる既存挙動。ID重複・ファイル削除等で部品参照が切れた際に異常が可視化されず、誤図面・誤シミュレーション結果に気づけない | T-035範囲外（既存挙動。T-035欠陥の被害連鎖の調査で顕在化） | 小〜中（検出は容易、DRC警告・プレースホルダ表示等の見せ方はUI/UX仕様判断を含む） | approved → T-052として起票（2026-07-10殿裁定：案A=DRC新規警告項目を採用、`docs/archive/ecad2-uiux-proposals-p017-p020-p023-onmitsu.md`） |
| P-018 | 忍者（T-035実機検証）＋侍（診断ログ実測）＋隠密（独立理論、Wチェック完全一致） | 自作パーツ・部品選択リスト経由の配置フローの既存設計欠陥：リスト項目クリックの瞬間に`TryPlaceElement`が即実行され、SelectedCell未選択なら「配置するセルを先に選択してください」表示のみでabort。**その後セルを選択しても再試行されず**、リスト経路は`Tool.PartId`を設定しないためEnter/キャンバスクリック配置も設計上効かない（基本図形のツールバー経路との非対称）。オリジナル（basic-*固定Id）でもクリーン状態から同一再現＝Id無関係の経路差。「リスト選択でTool.PartIdを設定しEnter配置を可能にする（キーボードファースト整合）」等の対処はUI/UX仕様判断を含む→**忍者の再検証（2026-07-06）で取り下げ**：正しい手順（セル選択→リストクリック）ならオリジナル・再採番とも機器表反映まで完全動作（診断ログ実測で確認）。当初の現象は操作手順の逆順による誤観測で、実装バグは存在しない。残存事実として「リスト経路はTool.PartId不設定ゆえEnter/キャンバスクリック配置が効かぬ非対称（都度リスト項目クリックが必要）」はあるが、ガードとメッセージ誘導で成立している設計と整理 | T-035範囲外（誤観測と確定） | - | withdrawn（対応不要、2026-07-06忍者訂正・Wチェック確認済み） |
| P-019 | 隠密（T-037補正主体の診断調査、CONFIRMED） | App層テスト13件（`MainWindowViewModelTests` 8件＋`SheetNavigationViewModelTests` 5件）が`new MainWindowViewModel()`（MainWindowViewModel.cs:444-447が無条件に`PartPalette = new PartPaletteViewModel()`）→`PartFolderStore.CreateDefault()`（PartPaletteViewModel.cs:33、実MyDocuments/OneDrive実体）→EnsureFolders→SeedBasics→Enumerate（T-037の補正＋書き戻し込み）の連鎖を`dotnet test`のたびに実環境で発火させる。今回は「正しい方向への補正」だったため実害なしだが、**テストが実ユーザーデータへ副作用を及ぼす構造的欠陥**（T-034時点から潜在、T-037のEnumerate書き戻し追加で顕在化。実例=a接点.gcadpartの更新時刻18:16がコミット時刻と一致、殿・侍とも起動しておらぬことを確認済み）。対処案=PartPaletteViewModel/MainWindowViewModelにPartFolderStoreを注入できる口を設け、テストは一時フォルダを叩く構成へ | T-037範囲外（T-034時点からの潜在構造） | 小〜中（コンストラクタ注入＋テスト側変更、P-016のDispatcher注入と同系の分離設計） | approved → T-042として起票（2026-07-06殿裁定：T-040の前に差し込み実施） |
| P-020 | 忍者（T-037差分確認）＋隠密（原因調査2026-07-06、CONFIRMED） | 機器表（DeviceTableGrid）の「種別」列が要素種別によらず一律「Other」表示になる（ORa/ORb配置時に観測）。**隠密の原因特定（CONFIRMED）**：`Device.Class`を設定する箇所は`MainWindowViewModel.cs:224`と`:339`の2箇所のみ（grep網羅）で、いずれも新規デバイス登録時に`Class = DeviceClass.Other`を無条件固定。ElementKind/PartDefinitionからDeviceClass（Relay/PushButton/SelectSwitch/Lamp/Timer等）を推定するマッピングロジック自体が不存在（既存挙動というより**未実装機能**）。P-017（PartResolverフォールバック）とは無関係（別プロパティ・伝播経路なし、CONFIRMED）。副次所見：`MainWindow.xaml:334`の種別列はenum直バインドで日本語変換なし——修正時はOther以外も英語表示になる点を併せ確認要（見せ方はUI/UX判断を含む） | T-037範囲外の気づき（未実装機能の顕在化） | 小〜中（マッピング表の新設＋表示の日本語化。種別の見せ方はUI/UX判断含む） | approved → T-053として起票（2026-07-10殿裁定：案A=Converter新設+PDF表記統一を採用。**前提修正=マッピング自体はT-045増分Bで実装済みと隠密調査で判明、残るは表示のみ**） |
| P-016 | 侍（T-034実装、テスト実証で発見） | `SheetNavigationViewModel`の`AddCommand`/`RenameCommand`が`System.Windows.Application.Current.Dispatcher`へ直接依存しており、WPF Application非起動のテストプロセスで実行するとNullReferenceExceptionになる（テストで再現確認済み）。このためT-034では両コマンドをテスト対象外とした（Dispatcher非依存のDeleteCommandのみテスト済み）。改修するならDispatcher注入等の分離設計が必要。MainWindowViewModel本体・DeviceTable/OutputPanel/PartPaletteの各子ViewModelはWindow非依存でテスト可能と確認済み | T-034範囲外（テスト容易性確認の副産物、本体コード改修を伴うため裁量対象外） | 小〜中（Dispatcher抽象化の設計次第） | approved → **T-045として起票**（2026-07-08殿裁定：P-025と統合、T-041完了後着手） |
| P-021 | 隠密（T-033増分1レビュー、code-review併用CONFIRMED） | `Sheet.Elements`（`List<ElementInstance>`）が同一Posの重複要素を防ぐ機構を持たず、`PlaceElementAtSelectedCell`も配置直前の占有再チェックを行わない——深層防御の欠如。今回はT-033増分1のバー表示中ガード漏れ（DRC出力パネル行クリックでSelectedCellが占有セルへ移動→OKで同一Posに二重生成）として顕在化し、当該経路自体は増分1修正（作業域全体無効化）で塞ぐが、防御機構の不在は将来の別経路（新機能・自動化処理等）で再発しうる | T-033範囲外（Core層/VM層の構造的な防御欠如、T-033修正では到達経路のみ塞ぐ） | 小〜中（PlaceElementAtSelectedCellへの占有再チェック追加が最小案、Sheet.Elements側の不変条件保証はデータ構造設計判断を含む） | 最小案（占有再チェック）はT-045増分Bで実装済み（2026-07-09、ValidatePlacement統合）。Sheet.Elements側の不変条件保証は将来課題としてpending継続（増分Bレビュー所見F同旨、`docs/archive/ecad2-t045-increment-b-review-onmitsu.md`） |
| P-022 | 忍者（T-033増分1実機確認） | 行番号ラベル付近をクリックすると「選択セル 行0/列-1」という範囲外セルが選択状態になり、そのままF5で配置バーが開く。列-1という不正座標が選択可能な点はヒットテストの境界処理の問題の可能性。配置ガード（範囲外セルでの配置起動可否）の要否は殿判断。P-012（行0の描画異常）との関連は未確認→**忍者の関連確認（2026-07-07）：行0/列-1へ実際に配置すると第三の描画異常**（シンボルが左母線より左＝行番号ラベル域へはみ出す、`docs-notes/images/p022-p012-relation-negcol-row0-ninja.png`）**を確認。P-012の症状とは症状・発生条件（列の正負）が異なり別種と判断**（同一原因なら通常列でも再現するはずだが6試行とも不再現）。境界値座標処理という共通テーマはあり、いずれかを修正する際は他方への影響確認を推奨。**殿教示（2026-07-07）：行-1・列-2までの選択範囲は意図的仕様であり「選択できること」自体はバグではない。バグはその範囲が描画範囲に含まれてしまっている点（修正予定）**——本Pの論点は「範囲外選択の禁止」ではなく「仕様範囲の描画の扱い」へ再解釈が必要。**殿裁定（2026-07-09）：配置下限=0（負マージン行-1・列-2への配置は不可、選択仕様は不変）＝配置側の対処はT-045増分Bの境界ガードで実施** | T-033範囲外（既存の選択・ヒットテスト境界処理の問題が顕在化） | 小〜中 | 配置側approved → T-045増分B（殿裁定2026-07-09=下限0）／描画範囲の扱いは殿修正予定待ち |
| P-023 | 忍者（T-033増分1実機確認） | 部品選択リストの項目をクリックしても選択ハイライトが残らない（クリック即配置バー表示の設計の副産物）。どの部品を選んだかの視覚フィードバックが乏しい。実害は未確認 | T-033範囲外（T-026/T-037由来の既存リスト挙動の視覚フィードバック） | 小（見せ方はUI/UX判断含む） | approved → T-054として起票（2026-07-10殿裁定：案A=配置バー内に選択中部品情報を表示を採用） |
| P-024 | 殿（T-033増分2の直接確認中、2026-07-07） | 選択セルが描画グリッドの範囲外（負の行）へ移動でき、そのまま配置すると**キャンバスには描画されないがデータ上は設置される**。機器表には機器（X10等）が出現し、DRC出力の該当箇所に「P1 行-1」「P1 行0」等の負行・範囲外表記が出る（選択枠がグリッド上方の空白に浮くスクリーンショットあり）。証跡=`docs-notes/images/p024-outofrange-{canvas,devicetable,drc-negativerow}-tono.png` 3枚。P-022（行0/列-1選択・はみ出し描画）と同じ**選択・配置の境界ガード欠如**のテーマで、こちらは行方向・データ設置まで到達する点が新しい。内部行インデックスと表示行ラベルのずれ（DRC行0=表示行2付近？）も要調査。**殿教示（2026-07-07）：行-1・列-2までの選択範囲は意図的仕様。バグは当該範囲が描画範囲に含まれている点（修正予定）**——「負行に設置できること」の是非は殿教示を踏まえた再整理が必要（設置可否と描画範囲は別論点）。**殿裁定（2026-07-09）：設置不可＝配置下限0。T-045増分Bの境界ガードで対処** | T-033範囲外（既存の選択移動・配置経路の境界ガード欠如が顕在化） | 小〜中（P-021・P-022と合わせ「配置前検証」として一括対処も検討可） | 配置側approved → T-045増分B（殿裁定2026-07-09=下限0）／描画範囲の扱いは殿修正予定待ち |
| P-026 | 殿（2026-07-07、T-033増分2確認後の直接要望） | プロパティ欄（右パネル）の部品選択リストに表示されるORa/ORbのサムネイルシンボルを、上パネル（ツールバー2段目）のsF5/sF6アイコンと同じシンボル（T-040 GX様式=└┤├┘構図の並列接点グリフ）に統一したい。現状はT-037実装の「通常接点シンボル＋右下ORバッジ合成」（PartThumbnailRenderer拡張）でツールバーと見た目が不一致 | T-037（ORバッジ方式の置き換え）・T-040（GX様式グリフ）の後追い統一。todo.md範囲外の新規要望 | 小（PartThumbnailRendererのORa/ORb描画をツールバーsF5/sF6と同構図へ描き起こし。ツールバー=XAML Path・サムネイル=レンダラ描画のため実装形式は異なる） | approved → T-043として起票（2026-07-07殿裁定：T-033完了直後・T-041前に差し込み） |
| P-028 | 隠密（T-044 GuiEcad比較調査、2026-07-07）＋再レビューonmitsu-2で実証 | 母線到達判定ロジックがNetlistBuilder（`TopRow\|\|BottomRow`）・DiagramRenderer（T-026で意図的にBottomRow限定へ変更）・MainWindowViewModel（T-044で`TopRow\|\|BottomRow`採用）の3箇所に散在。判定条件の微妙な非対称（observations #21）も既にあり、将来同種の見落としバグが再発しやすい構造。Core層への共有関数集約を提案。**再レビューonmitsu-2（2026-07-07）で追認**：T-044修正がViewModel側に`TopRow\|\|BottomRow`基準をさらに1箇所定着させ、DiagramRendererのBottomRow限定との非対称が3モジュール間で確定。今回の反例（A→B→Cの単純連鎖）では両基準が一致するため表面化しないが、将来「基準行を再利用する複雑な分岐パターン」で電気的判定と描画判定の乖離が顕在化しうる。**P-028の設計時にはViewModel側（3箇所目）も集約対象に含めるべき**（隠密明示）。**さらに実証（2026-07-07）**：殿がT-044実機確認中に連鎖ORの右合流断線描画を発見（`ss_20260707_134207.png`）→Wチェック調査で、この非対称が「配置列が基準要素より右へ広がる連鎖OR」で実際に描画上の隙間として可視化することを確認（`docs/ecad2-t044-disconnect-investigation-onmitsu.md`）。理論上の負債から具体的描画欠陥の実証へ格上げ。ただし電気的には正常（NetlistBuilderはTopRowも見るためネット分断なし・忍者WireNumber実測で確定）で見た目のみ。**殿裁定でT-044は一旦保留し、手動配線基盤T-041（線の記入・消去）を本命化して対処する方針**（自動ロジック精緻化＝本Pの集約含む、は袋小路との殿判断） | T-044範囲外の設計改善提案（P-025のApp層リファクタと同時期の検討が好適） | 小〜中（共有関数抽出＋3箇所置換） | 見送り（殿裁定2026-07-10、今回は着手せず。別途時期を見て判断） |
| P-027 | 忍者（T-043差分確認中、2026-07-07。断定なし・原因未検証） | ウィンドウを縦リサイズした際、キャンバス内の行1描画位置が約56pxオフセットする現象に遭遇。リサイズ後の座標系変化に気づかず旧座標でクリックすると「行0」という異常セルが選択され機器表にのみ残存するテストデータが発生（新規作成でクリーン化して回復）。T-033原点ズレ系・P-024（範囲外セル設置）と類似の挙動に見えるが、忍者自身の誤操作起因の可能性が高く未検証→**殿教示（2026-07-07、忍者へ直接）：行-1・列-2までの選択範囲は意図的仕様。ただしその範囲が描画範囲に含まれてしまっているのはバグで修正予定**——本件の「行0選択」自体は仕様内の挙動と判明（忍者の誤操作説は撤回）。残る論点はリサイズ時の描画オフセットと描画範囲バグの関係 | T-043範囲外の気づき（リサイズ時の座標系テーマ。P-022/P-024の再解釈は下記殿教示参照） | 不明（再現条件未特定） | 保留継続（殿裁定2026-07-10） |
| P-029 | 隠密（T-044再レビューonmitsu-2、2026-07-07） | App層テスト（配置ロジック→Connector生成の検証）とCore層テスト（Connector構成→Netlist正しさの検証）を実際に接続する「橋渡しテスト」（`NetlistBuilder.Build(vm.CurrentSheet!, vm.PartLibrary)`形式）が存在しない、テストカバレッジの隙間。今回のT-044修正はApp層・Core層それぞれのテストで正しく機能確認できたが、両層を跨ぐ実配線の正しさは直接検証されていない。加えてverifyエージェント発見の周辺隙間（App層テストの`"contact-no"`というPartId文字列が実カタログID`"basic-contact-no"`と不一致で、たまたま`ElementKind`既定値に頼っている等）も`OrWiringTests.cs`のT-044以前からの既存前提。P-028（判定ロジック重複）とは別軸（テスト網羅の隙間であり判定ロジックの重複とは無関係、隠密明示） | T-044範囲外（テストカバレッジの品質改善提案、既存の設計上の隙間） | 小〜中（橋渡しテスト新設＋PartId文字列の整合修正） | 見送り（殿裁定2026-07-10、今回は着手せず。別途時期を見て判断） |
| P-025 | 殿（2026-07-07、リファクタリング検討の発意）＋家老所見 | App層の的絞りリファクタリング検討。プロジェクト構成の層分離（Core=WPF非依存等）は既に健全でモジュール分割の増設は時期尚早、一方で負債は`MainWindow.xaml.cs`／`MainWindowViewModel`に集中——P-020（種別マッピング未実装）・P-021（占有再チェックなし）・P-022/P-024（境界ガード欠如）は全て「配置経路の検証散在」という同根、隠密レビューでもcode-behind直操作→ViewModel単一情報源化の指摘が反復。核=**「配置前検証サービス」抽出**（セル選択→検証（境界・占有）→配置→機器登録（種別割当）→再描画の一本化）で、P-020/021/022/024の修正が構造改善を兼ねる。適期=T-033完了後・T-041着手前（T-041は配置経路にコードを足すため先に土台を整えると安い）。進め方=**専用ブランチで作業（mainを直接汚さずリスク回避、殿指示2026-07-08）**→隠密の構造調査（ホットスポット計測）→侍の計画起草→殿裁可→増分実施→完了後mainへマージ（App/Coreテスト24件＋T-034/T-042の注入口が回帰網） | 新規（複数P束ねの構造改善、todo.md範囲外） | 中（設計次第。P4件の一括解消を含む） | approved → **T-045として起票**（2026-07-08殿裁定：適期がT-041本命化で後ろ倒しに、T-041完了後着手。P-016と統合、専用ブランチで作業） |
| P-034 | 隠密（T-041増分7最終レビュー往復3周目、CONFIRMED実測クラッシュ再現）＋忍者（静的レビューで独立一致） | 所見AA（境界クランプ）対応自体が新たな重大バグを作り込んでいた（往復4周目）。所見AB＝FreeLineの境界クランプにmin>maxガードが無く、通常操作（F9記入+矢印キー連打）だけで到達可能な`ArgumentException`クラッシュを実測再現（独立2エージェント、うち1件はビルド済みDLLロードで実例外確認）。所見AC＝キーボード端点リサイズ(`ResizeSelectedFreeLineEndpoint`)に境界クランプが無くドラッグ版と非対称。所見AD＝ConnectionDotにも境界クランプが適用漏れ（忍者の静的レビューと3者独立発見、拡大を推奨） | T-041増分7範囲内（往復2周超過につきゲート、`docs-notes/roles/karo.md`規定により殿上申） | 小（`MainWindowViewModel.cs`中心） | approved → 4周目修正を一括実施（2026-07-08殿裁定：所見AB/AC/AD一括対応）→ **4周目修正完了（コミット767325b）、隠密最終レビューでクリーン確定**（`docs/archive/ecad2-t041-increment7-review-onmitsu-4.md`）。忍者の実機確認を残すのみ |
| P-039 | 忍者（T-041増分7実機確認、範囲外検出1）＋侍（コード確認） | VerticalConnectorのみドラッグで列位置（水平方向）を動かせない（`UpdateDragConnector`がTopRow/BottomRowのみ扱いColumnに触れず、`HitTestConnectorDragMode`も列ズレを許容誤差内に制限、`BeginDragConnector`もColumnをスナップショット保存せず）。キーボード側は`MoveSelectedConnectorColumn`で列移動対応済み、他3種はドラッグで両軸移動可能で、VerticalConnectorのみ非対称。殿裁定案X「ドラッグとキーボードのどちらでも同じ結果に到達できる」との不整合＝実装漏れの可能性が高い（侍判断） | T-041増分7範囲内の欠陥（案X裁定との不整合） | 小（`MainWindowViewModel.cs`＋`LadderCanvas.cs`のドラッグ経路拡張） | approved → **ドラッグでも列移動可に修正**（2026-07-08殿裁定：案Xとの整合・他3種との一貫性を優先）→ **完了（コミット86bf96e）**：新制度（テスト設計分離+RED証明）初適用、隠密レビュークリーン（設計書突合完全一致）・忍者実機確認全観点OK。T-041クローズ |
| P-036 | 忍者（T-041増分7実機確認） | WireBreak配置直後、要素が一時的に描画されないケースを複数回観測（T-041増分5検証時と同型の気づき、P-031関連）。再描画のタイミング問題と推測されるが、別操作を挟むと正しく表示されたため実害は確認できていない | T-041範囲外の気づき（P-031と同根の可能性） | 不明（再現条件未特定） | 保留継続（殿裁定2026-07-10） |
| P-037 | 忍者（T-041増分7実機確認） | WireBreak配置により、電気的には正常なはずの右側配線が視覚上完全に消える現象を観測。T-044の既知描画欠陥（連鎖ORで右合流線が断線して見える）と類似する可能性があるが、検証の本筋から外れるため深追いしていない | T-041範囲外の気づき（T-044描画欠陥と関連の可能性） | 不明（未調査） | 保留継続（殿裁定2026-07-10） |
| P-038 | 忍者（T-041増分7実機確認） | シート追加操作が1回だけ、新規シートが追加されずシート1のみのまま反映されなかった（再現性未確認、その後は毎回正常に追加できた） | T-041範囲外の気づき | 不明（再現条件未特定） | 保留継続（殿裁定2026-07-10） |
| P-035 | 隠密（T-041増分7最終レビュー往復4周目） | `GcadSerializer`（永続化層）が`GridSpec.Rows`/`Columns`の下限検証を持たない。現在のUI操作からはこれらが0以下になる経路は無いため実害なしだが、将来ファイル読込の堅牢化（手編集・破損ファイル対策）を検討する際の対象になりうる | T-041範囲外の気づき（永続化層の頑健性） | 不明（将来検討） | 保留継続（殿裁定2026-07-10、将来課題のまま） |
| P-033 | 侍（T-041増分7実装中） | 線分系（VerticalConnector/FreeLine）の端点伸縮（Shift+矢印キー）のキー割当。単一選択のみ（端点単位選択はスコープ外）のため、Shift+矢印がどちらの端点を動かすか一意に決める手段が必要。3案提示：案1(推奨、上下2キー固定対応、実装最小だが画面向きと不一致の場面あり)/案2(Tabトグル選択後4方向操作、実装量大だが直感的)/案3(クリック位置記憶、マウスと一致するが本体クリック時の対象未定義問題あり)。平行移動（Shift無し矢印、割当表`docs/archive/ecad2-t041-increment7-key-operation-proposal-samurai.md`1節）は異論なく先行実装可 | T-041増分7範囲内（UI/UX使用感に関わる分岐のためゲート、`docs-notes/roles/karo.md`規定） | 中（新規状態保持＋トグルUI＋ステータスバー表示が必要） | approved → **案2（Tabトグル選択後4方向操作）を採用**（2026-07-08殿裁定）。平行移動は割当表のまま先行実装可 |
| P-031 | 忍者（T-041増分5実機確認） | F10（WireBreak）配置直後、キャンバス再描画が一時的に反映されずその後の別操作を挟んで初めて表示される事象を1回観測。Undoで取り消せたことから配置自体は成立していたと判断。制御回路シートの空セル（配線なし）では正しく無反応を確認済み。再現条件は完全には切り分けられておらず、UI Automation操作特有のタイミング問題（ninja.md既知の罠）の可能性もあり断定なし | T-041増分5範囲外の気づき（1回のみ観測、DoDには影響しないと忍者判断） | 不明（再現条件未特定） | 保留継続（殿裁定2026-07-10） |
| P-032 | 忍者（T-041増分5実機確認） | シート追加・削除操作がCtrl+Z（Undo）の対象外に見える（要素の配置・削除はUndo可能なことを他場面で確認済みとの対比）。意図した設計か未確認 | T-041範囲外の気づき（既存のUndo機構の対象範囲） | 不明（仕様確認が先） | approved → T-051として起票（2026-07-10殿裁定）→ **着手前の侍調査で前提崩れ＝Undo機構自体が未実装と判明**（UndoStack・Ctrl+Z配線とも不在、家老独立grepでも確認）→ **忍者実機再検証で確定（2026-07-10、4試行すべてUndo不作動、`docs/archive/ecad2-t051-precheck-undo-verification-ninja.md`）。原観測「配置・削除はUndo可能」は誤認と忍者自身が訂正**。T-051は殿裁定で一旦保留（Blocked） |
| P-030 | 隠密（T-041増分5再レビュー往復2周目、CONFIRMED） | `CurrentSheetIndex`のsetter設計が往復2周を超えてモグラ叩きの様相（1周目=早期return除去→2周目=改名操作でドラフト黙示破棄の副作用発生→2周目修正が削除時の再描画漏れ(症状1)を別条件下で再発）。根本原因＝`SelectedCell`（値そのものが表示対象）と`CurrentSheetIndex`（参照先が入れ替わりうるキー）の性質が非対称なのに、同一の「setter粒度パターン」を機械的適用したこと。隠密提案の3周目修正方針：①`OnPropertyChanged(nameof(CurrentSheet))`は1周目同様に無条件配置へ戻す ②所見Lの真因（改名操作が`SelectedCell=null`という強い操作を誘発すること）を呼び出し元（`RenameCommand`）側で対処し、改名時は`CurrentSheetIndex`への代入を経由せず`RefreshSelectedSheet()`のみ呼ぶ形に変更 | T-041増分5範囲内（往復2周超過につきゲート、`docs-notes/roles/karo.md`規定により殿上申） | 小（1-2ファイル：`MainWindowViewModel.cs`の`CurrentSheetIndex`setter＋`SheetNavigationViewModel.cs`の`RenameCommand`） | approved → T-041増分5の一環として3周目修正実施（2026-07-08殿裁定：隠密提案の修正方針を採用）→ **3周目修正完了（コミット914a2c7）、隠密最終レビューでクリーン確定**（`docs/archive/ecad2-t041-increment5-review-onmitsu-4.md`）。忍者の実機確認を残すのみ |

| P-041 | 忍者（T-045増分C実機検証、範囲外検出） | 部品選択リストで種別切替直後（約300ms以内）にキャンバスをクリックすると、UIA上は選択済み(True)でも直前の種別（a接点）のまま配置される現象に2/3回遭遇（間隔を空ければ正常=SelectSwitch配置）。UI Automation高速連続操作特有の擬似事象の可能性が高い（忍者判断・両論併記）が、リスト選択→配置種別反映の実レースの可能性も排除できず。実操作では表面化しにくいと推測。詳細=`docs/archive/ecad2-t045-increment-c-ninja-verification.md` | T-045範囲外の気づき（既存の選択反映タイミング。増分B/Cは当該経路に非接触） | 不明（原因切り分けが先。T-038診断ログ連携の適用候補） | 保留継続（殿裁定2026-07-10、A群と同性質のため仕分け漏れを事後補正） |
| P-040 | 隠密（T-045増分Aレビュー、code-review併用・所見10） | 直接Dispatcher依存（`System.Windows.Application.Current.Dispatcher`等への直接参照）の再発防止機構が未整備。P-016で`SheetNavigationViewModel`の既知依存は`IDispatcherService`経由へ分離したが、将来の新規コードが再び直接依存を書くことを機械的に検出する仕組み（アーキテクチャテスト等）が無い。詳細は`docs/archive/ecad2-t045-increment-a-review-onmitsu.md`所見10 | T-045増分Aレビューの範囲外の気づき（将来課題、家老仕分け2026-07-09でproposed.md送り） | 小（アーキテクチャテスト1本の新設想定） | approved（殿裁定2026-07-09）→ **完了（2026-07-09、コミット`511254f`）**：ViewModels配下走査のアーキテクチャテスト新設（`Application.Current.Dispatcher`/`Dispatcher.CurrentDispatcher`の2変種検出・正規アダプタ`WpfDispatcherService`のみ除外）。RED証明=両パターン一時注入で失敗実測→復元GREEN・229件全合格。隠密レビュークリーン（経過観察3件=コメント起因false positive／検出パターン2文字列限定のすり抜け／同名ファイルのパス無視除外、`docs/archive/ecad2-p040-review-onmitsu.md`）。実機不要=家老判断。push済み`874758d` |
| P-043 | 忍者（T-047修正実機再確認、余力観点・実害なし） | 線記入モード中（自由線・縦分岐のドラフト中）にTabキーを押すとフォーカスがメニューバーまで抜ける。記入モードの操作系（矢印・Enter/Esc）からの離脱経路として意図した設計かは未確認。実害なし・深掘りせず記録のみ | T-047範囲外の気づき（既存のフォーカス巡回設計のテーマ、T-021 CyclePanelFocus関連） | 不明（仕様確認が先） | 保留継続（殿裁定2026-07-10、実害なし記録のみ） |
| P-042 | 忍者（T-045増分D最終実機検証、範囲外検出・断定なし両論併記） | FreeLine検証中の非定型操作（UIA操作失敗を無視して続行）で選択済み自由線をキャンバス外までドラッグ→グリッド範囲外（大きく負の行）へ移動し、Ctrl+Zを押しても見た目上復元されなかった（ズームアウトで境界外に実在は確認=データ消失ではない。通常のキャンバス内ドラッグではCtrl+Z正常復元を別途確認）。再現試行2回目は境界外移動自体が不再現で条件切り分け未完。両論併記：(a)境界外ドラッグ時のUndo不整合が実在 (b)非定型操作起因の見かけ上の現象。増分Dの新規コード（ForceCancelIfAny等の外的要因キャンセル機構）とは無関係で、既存のドラッグ座標クランプ処理（UpdateDragXxx、増分Dでは不可侵=変更なし）側の話と推測。詳細=`docs/archive/ecad2-t045-increment-d-ninja-verification.md` §5 | T-045範囲外の気づき（既存ドラッグ境界クランプ+Undoのテーマ、P-022/P-024の境界系・P-032のUndo対象範囲と関連） | 不明（再現条件未特定） | 保留継続（殿裁定2026-07-10）。**注記：本欄の「通常のキャンバス内ドラッグではCtrl+Z正常復元を別途確認」はT-051前提検証（2026-07-10）で誤認と確定**（Undo機構自体が未実装、`docs/archive/ecad2-t051-precheck-undo-verification-ninja.md`）——「Undo不整合」という論点自体が消滅し、残る論点は境界外ドラッグ挙動のみ |
| P-044 | 隠密（T-050静的レビュー、範囲外気づき） | `SheetNavigationViewModel.cs`に旧値を捕捉しない1引数版`OnPropertyChanged`が同型で3箇所残存（`RefreshSelectedSheet`=高頻度・`ResetSheets`・`DeleteCommand`=対応コスト実質ゼロ）。P-015（旧値null化漏れ）の兄弟課題だがP-015原文の対象範囲外のためT-050の不備ではない。詳細=`docs/archive/ecad2-t050-review-onmitsu.md` | T-050範囲外（P-015兄弟課題、既定OFFのオプトイン診断機能内） | 小（1ファイル3箇所） | approved（2026-07-10殿裁定：T-050修正と同時に侍が対応） |
| P-045 | 忍者（T-049実機確認、範囲外の気づき） | あらかじめ選択済みの空セルへF5配置すると、プロパティパネルが「要素を選択してください」のまま更新されない（SelectedCellの値自体が配置前後で不変のため通知がスキップされる設計と忍者が静的確認。一度別セルへ選択を移し戻せば正常表示）。SetProperty早期returnトラップと同型テーマの新事例。詳細=`docs/archive/ecad2-t049-verification-ninja.md` | T-049範囲外（既存挙動、T-049差分とは無関係） | 小〜中（通知経路の設計次第） | pending |
| P-046 | 隠密（T-055調査、範囲外の気づき） | PDF出力機能がUIに未結線の疑い（`RenderPageCount`等の呼び出し元不在）。T-007でGuiEcadから移植されたPdf層が、App層のメニュー・コマンドから到達できない可能性。詳細=`docs/archive/ecad2-t055-guiecad-row-busnumber-survey-onmitsu.md`派生の気づき節 | T-055範囲外（PDF出力の結線状況、要確認） | 不明（結線調査が先） | approved → T-060として起票（殿裁定2026-07-11） |
| P-047 | 侍（T-050往復2周目の設計中） | `SelectedSheet`セッタ（シートタブ直接クリック経路、`MainWindow.xaml:368`のSelectedItemバインド実体）にも、Add/DeleteCommandと同根の二重発火が現存。ただしSheets構成を変えない「選択変更」経路であり、T-050往復2周目のスコープ（構成変更操作=Add/Delete/ResetSheets）外と家老仕分け（便乗拡大防止、2026-07-10）。実害はTraceLog診断ログ限定 | T-050範囲外（同根バグの別経路、構成変更でなく選択変更） | 小（Add/Deleteと同型の対処を横展開） | pending |
| P-048 | 隠密（T-055増分1往復1周目の再レビュー、範囲外の気づき） | 行削除時のSelectedCellクランプ（`c28ec40`）は公開セッタ経由のため、既存副作用（SelectedConnector等の排他クリア・記入中ドラフトのクリア）も連動する。「削除対象の最終行を選択中かつ記入中（ConnectorDraft等）」という限定的な組合せでのみ、行削除操作が記入中状態を意図せずクリアしうる。実害の顕在化条件は非常に狭い | T-055増分1範囲外（既存セッタ副作用との相互作用、修正自体は殿裁定どおりで正） | 小（要否の仕様判断が先） | pending |
| P-049 | 隠密（T-055増分3コードレビュー、code-review併用・PLAUSIBLE） | `InsertRowBeforeCommand`/`DeleteRowAtCommand`のCanExecuteが`param is int`とGrid.Rows上限/下限のみ判定し、`0<=row<Grid.Rows`は検証しない。現状唯一の呼び出し元（`LadderCanvasHost_PreviewMouseRightButtonDown`）が事前ガード済みのため実害なし。安全性が「呼び出し元が1箇所だけ」という運用上の偶然に依存しており、将来別の呼び出し元（新規XAMLバインド等）が追加されると即座に顕在化する構造的な脆さ。詳細=`docs/archive/ecad2-t055-increment3-round1-review-onmitsu.md`所見d | T-055増分3範囲外（現状無害） | 小 | pending |
| P-050 | 隠密（T-055増分3コードレビュー、code-review併用・PLAUSIBLE） | `RowOps.InsertRow`/`DeleteRow`はGroupFrameの`TopLeft.Row`/`Height`をシフトするが、同じGroupFrameが持つmm絶対座標オーバーライド（`VisualXMm`/`VisualYMm`/`VisualWidthMm`/`VisualHeightMm`、T-007のGuiEcad移植由来）は未追随。`DiagramRenderer`はこの値が非nullなら優先して描画するため論理座標とずれるが、枠ドラッグ機能・GuiEcadインポート機能とも現状未実装で到達経路なし。将来ドラッグ移動機能実装時に顕在化する潜在バグとして記録。詳細=同上所見e | T-055増分3範囲外（現状無害、将来課題） | 不明（将来のドラッグ機能設計次第） | pending |
| P-051 | 家老（殿の質問を受けた検分、2026-07-11） | テストモード機能がUIに未結線の疑い。Core層Simulation（`TestSession.cs`・`Netlist.cs`、T-007でGuiEcadから移植済み）は存在するが、App層（`src/Ecad2.App`）に一切参照なし（grep確認、`TestSession`/`IsTestMode`等ゼロ件）。メニュー「テストモード(_M)」項目もCommand未バインド（`InputGestureText`のみでClick/Command無し）。P-046（PDF出力未結線）と同型の未結線疑い | 該当タスクなし（UI全体の未結線調査、殿の直接質問で発覚） | 不明（結線調査が先、P-046と同様） | approved → T-061として起票（殿裁定2026-07-11） |
| P-052 | 隠密2（T-056案起草、範囲外の気づき2026-07-11） | アプリ全体のユーザー設定永続化機構（ウィンドウサイズ・パネル幅・表示設定等をアプリ再起動間で記憶する仕組み）がecad2に皆無。T-056グリッド表示切替の永続化論点で発覚したが、T-059出力パネル高さ・将来のT-058ドッキングレイアウト永続化とも横断する基盤論点。個別機能ごとに場当たりで持つか、共通の設定ファイル機構を一度設けるかの設計判断を要する | T-056範囲外（横断的な基盤の不在） | 中（設定ファイル機構の新設） | pending |
| P-053 | 隠密2（T-061調査、範囲外の気づき2026-07-11） | メニュー「テストモード(_M)」の`InputGestureText="F5"`表示が、F5の実利用（a接点配置、T-026キー体系の殿裁定）と矛盾——メニューは「F5でテストモード」と偽って見せるが、実際にF5を押すとa接点配置が動く。GuiEcad自体はテストモード切替にキー割当なし（マウス専用）。今すぐ誤解を招きうる表示のみの軽微バグ | T-061範囲外の気づき（ただしT-061実装前に解消必須と調査書に明記、T-061着手時に吸収するのが自然） | 小（InputGestureText削除または正しい割当への変更、割当自体はT-061のUI/UX論点） | **approved → T-061裁定（2026-07-12）で解決＝メニュー表示削除（キー割当なし）** |
| P-054 | 殿（忍者の実機確認を見ての直接指摘2026-07-11）＋家老検分 | GroupFrame（グループ枠）を新規作成するUI機能が未実装。家老grep検分=`new GroupFrame`・`Frames.Add`ともsrc全体で0件、App層のGroupFrame参照はコメント1箇所のみ——**生成経路がアプリに一切存在しない**。Core層は完備（モデル・描画・RowOpsシフト・T-055増分3のHeight--詰め処理まで実装済み）で、GuiEcadファイル読込時の表示は可能だが、ecad2内で枠を新規作成・編集・削除する手段が皆無。GuiEcad側には枠追加/削除/リネーム/移動/枠線スタイルのコマンド群が存在（隠密T-051調査`docs/archive/ecad2-t051-undo-redo-design-survey-onmitsu.md`で確認済み）。関連=P-050（Visual*Mm座標未追随、枠ドラッグ実装時の潜在バグ） | 該当タスクなし（殿の直接指摘による新規機能候補） | 中〜大（新規UI操作系一式。描画・データモデルは既存） | approved → T-067として起票（殿裁定2026-07-11、B区分一括タスク化） |
| P-055 | 隠密（T-051 round2レビューPLAUSIBLE）＋忍者（実機再現2026-07-11） | デバイス名編集を確定した直後にUndoを実行すると、編集内容が見た目上消える（Redoで復元可能、恒久喪失ではない）。デバイス名編集はUndo管理対象外（T-051 MVPはシート追加/削除のみ）だが、スナップショット復元がDeviceTable表示を巻き戻すことによる表示上の不整合。Undo対象範囲の拡張（候補2以降）時に自然解消する見込み | T-051範囲外（MVP対象範囲の設計上の制約が表示に露出） | 小〜中（Undo対象拡張時に再評価） | pending |
| P-057 | 忍者（T-071実機確認、範囲外の気づき2026-07-11） | 表示灯(Lamp)のLampColor(色ラベル)がApp層に一切結線されておらず、UIから設定する手段が現状存在しない。ユニットテストでレンダラーロジック自体はGREEN確認済みだが、実機の通常操作では色ラベルを設定できず表示確認も不可。詳細=`docs-notes/ecad2-t071-realmachine-verification-ninja.md` | T-071範囲外（表示灯自体は既存パーツ、新規10種追加とは無関係） | 小 | approved → T-085として起票（殿裁定2026-07-13：GuiEcad実物提示、フリーテキスト記号入力「ランプ色（中央表示）」欄を新設） |
| P-058 | 忍者（T-065/T-066実機確認2026-07-12）＋侍（実測診断で真因確定） | 機器配置直後の保存操作等で機器表エントリが消失する既存バグ（T-049以降存在）。真因=`PlaceElementAtSelectedCell`がSelectedElement関連の変更通知を発火せず、古い表示値を既存`CommitDeviceNameEdit()`が誤コミット | T-065/T-066範囲外（既存バグの顕在化） | 小 | approved → T-079として起票・即時修正（殿裁定2026-07-12）→ **完全Done（2026-07-12、コミット2442882）** |
| P-059 | 隠密（T-060静的レビュー2026-07-12、要判断F） | GuiEcadにあった「プレビューなし直接エクスポート」経路がecad2に無い（GuiEcad原本は「PDFプレビュー...」「PDF出力...」の2メニュー、ecad2は「PDF出力(_P)」1エントリで常にプレビュー経由）。殿裁定②「プレビュー機能=今回実装」はプレビュー追加の可否のみで、直接出力経路の廃止は明示裁定されていない（事前調査書でも「両方併存」の第三選択肢は未提示）。実害=再エクスポート時に毎回プレビュー確認の1手間が挟まるUXコストのみ（バグではない）。意図的簡素化として追認するか直接出力も追加するかはUI/UX判断 | T-060範囲内のスコープ判断事項（バグではない） | 小（追加する場合はメニュー1項目+既存Export呼び出し） | **resolved（殿裁定2026-07-12）＝プレビュー経由の1本のみで問題ない（直接出力経路は追加しない、現行実装を追認）** |
| P-060 | 侍（T-080実装中の計算検分2026-07-12）＋**殿（目視確認2026-07-12）** | 枠ありPDF出力でグリッドが用紙に収まらない。GridSpec.Columns=40固定・CellMm=9.0からRightBusX≈384.5mmが用紙幅を超過し、グリッド右側がページ外にクリップされる。**殿御自身の目視でも「そもそも収まっていなかった」と確認済み＝実在確定**。T-060実機検証では左寄り配置のテストデータゆえ顕在化せず。T-080 DoD(6)（行コメント用スペース事前確保）の前提が崩れるため同DoDは保留中。対処方針（縮小フィット/水平ページ分割/用紙設定/列数見直し等）は隠密のGuiEcad原本調査（同問題をどう解決していたか）を待って殿へ選択肢提示。**調査進展（2026-07-12）**：(1)隠密2の実物照合=GuiEcad原本に収める仕組みは一切存在しない（横置きなし・列方向分割なし・縮小は死んだ引数）＝踏襲選択肢は不成立 (2)隠密=殿のGuiEcad体験（行コメント印刷反映）との整合判明——GuiEcadはEnableBorderトグル持ちで、枠なし時は可変ページサイズゆえクリップなし。**ecad2は同ロジック移植済み（DiagramRenderer.cs:120-143）だがT-060裁定「常に枠あり」で到達経路が死んでいるのが直接原因** (3)**侍前提の訂正**=実際のシート作成はColumns=20明示指定（40ではない）、RightBusX(20)≈204.5mmはA4縦(210mm)にほぼ収まる計算——殿目視のはみ出しが実際どの経路か（枠の内側余白との重なり/行コメント文字域/その他）は忍者実測待ち (4)対処候補5案のコスト概算済み（`docs/ecad2-p060-grid-overflow-investigation-onmitsu.md`） (5)忍者実測（`docs-notes/ecad2-t080-grid-overflow-verify-ninja.md`）=現行20列運用ではクリップなし・プレビュー/実出力一致・縮小なし・右母線右側の余白約10mmのみ（行コメント20文字≈60mmは収まらない）。副次発見=シート作成コードがColumns=20明示指定でSheet.cs既定値40と食い違い | T-080範囲外の既存欠陥（T-060実装で顕在化可能になった） | 中（対処方式の設計次第） | **approved（殿裁定2026-07-12）＝縮小フィット採用**（常に枠あり維持、グリッド+コメント域が用紙幅を超える場合のみ自動縮小。T-080 DoD(6)の一環として侍が実装） |

| P-061 | 隠密（T-080往復1周目再レビュー2026-07-12、CONFIRMED） | 主回路のmm実座標を考慮する幅計算ロジック`MainCircuitContentMaxX`が`MainCircuitVirtualRows`等と3箇所に重複散在。前回レビュー指摘K/Jと同根、T-033で同型の実害前例あり。動作は正しく保守性リスクのみ。隠密は次増分での対応を推奨 | T-080範囲外（構造の重複、動作正常） | 小（共有ヘルパーへの集約） | pending |
| P-062 | 隠密（T-080往復1周目再レビュー2026-07-12、CONFIRMED） | F2キー経路と`HitTestRungCommentRow`の適格条件（MainCircuit除外・行範囲判定）が2箇所に手書き重複、共有ヘルパー化なし。将来の条件変更時に片側更新漏れのリスク | T-080範囲外（構造の重複、動作正常） | 小 | pending |
| P-063 | 隠密（T-080往復1周目再レビュー2026-07-12、PLAUSIBLE・新規論点） | `RequiredContentWidthForScale`が主回路シートでも描画されない右母線座標(RightBusX)を必要幅の下限に含め続けている（既定シートで204.5mm、縮小閾値205mmと僅か0.5mm差）。列数変更機能が現状無いため実害未達だが、前回指摘D（主回路の幅計算）の完全性に関わる。隠密より要殿確認の申し出 | T-080範囲外（実害未達の潜在論点、Dの完全性） | 小 | pending |
| P-064 | 隠密（T-080往復1周目再レビュー2026-07-12、CONFIRMED軽微） | `InverseBool`未使用リソースの残存、`RecordingRenderer`テストダブルの重複定義。軽微な清掃課題2件 | T-080範囲外（軽微清掃） | 小 | pending |
| P-065 | 隠密（T-080追加往復・課題3調査2026-07-12） | `PositionPlacementBar`(T-033配置バー)も位置クランプ基準が`MainWorkAreaGrid`（可視ビューポートより広い）という課題3と同型の設計（課題3の修正元パターンはここからの踏襲と判明）。現状は常に可視セルからのみ呼ばれるため顕在化せず実害未確認だが、将来スクロール外セルから呼ぶ経路が増えると課題3と同じ画面外描画が起きうる | T-080範囲外（同型の潜在問題、実害未確認） | 小（クランプ基準のCanvasArea化、課題3修正の横展開） | pending |
| P-066 | 侍（T-082往復1周目の修正2規約調査、2026-07-12） | シート削除時にPageNumberが再採番されない既存挙動（`DeleteCommand`は再採番処理を持たず、`AddCommand`も新規シートのみ設定）——削除後は飛び番のまま残る。T-082並び替えは再採番する仕様としたため、操作間で採番規約が非対称に。意図した仕様（GuiEcad踏襲等）か否かの確認が先 | T-082範囲外（既存挙動、修正2の既存規約調査で発覚） | 小（DeleteCommandへ再採番追加、ただし仕様確認が先） | approved → T-084として起票（殿裁定2026-07-13：実再採番は行わず警告文表示で対応、P-067と統合） |
| P-067 | 隠密（T-082再レビュー2026-07-12、派生提案） | シート削除（`DeleteCommand`）にDRC結果破棄フックが無い——T-082修正2で並び替え時はDRC破棄+案内表示を実装したが、削除でも診断が古いシート構成を指したまま残る同型問題が既存挙動として残置。P-066（削除時PageNumber非再採番）と同根テーマ（削除操作の後始末の欠落）、併せて判断が好適 | T-082範囲外（既存挙動、再レビューの派生） | 小（修正2のClearResults+表示の横展開） | approved → T-084として起票（殿裁定2026-07-13：T-082修正2と同一パターンを横展開、P-066と統合） |
| P-068 | 隠密（T-082往復3周目事後レビュー2026-07-12、6角度独立検出） | `SetCurrentSheetIndexWithoutCrossCut`（`MainWindowViewModel.cs:208`）がSetPropertyへpropertyNameを明示せず、`[CallerMemberName]`により発火するPropertyChangedの名前が"CurrentSheetIndex"でなく"SetCurrentSheetIndexWithoutCrossCut"になる。既存の`SetCurrentSheetIndexCore`も同型の欠陥を本コミット以前から抱えており（`ReplaceDocument`だけがnameof明示で回避、非対称）、今回で欠陥箇所が2つに増えた。現状XAML直接バインド・PropertyChanged購読が無く実害ゼロだが、診断ログ運用や将来のバインディング追加時の温床になりうる | T-082範囲外（既存欠陥の同型再発、実害なし） | 小（nameof明示2箇所） | pending |
| P-069 | 隠密（T-082往復3周目事後レビュー2026-07-12、PLAUSIBLE） | `BeginInvoke(ContextIdle)`によるSelectedSheet通知の遅延と、`SetCurrentSheetIndexWithoutCrossCut`の同期更新のタイミング差で、連続Alt+下操作時に選択ハイライトが一瞬無選択表示になる理論的懸念。テストは`ImmediateDispatcherService`のため検出不可、実機での確認が必要 | T-082範囲外（理論的懸念、実機観察推奨） | 不明（実機確認が先） | **withdrawn（2026-07-13、忍者実機確認で発生せず・殿目視確認済み、`docs-notes/ecad2-t082-ninja-verification.md`）** |
| P-070 | 家老（棚卸し再点検、2026-07-13、殿へ確認済み） | シート設定ダイアログ（`SheetSettingsDialog.xaml`）の項目追加。Core層に器がある未結線項目3点のうち、**列数(2〜20)・電源ラベル(`BusConfig.PowerLabel`)の編集項目追加**——GuiEcadは列数2〜20/電源ラベル設定可、ecad2ダイアログは現状「行数・左右母線名」のみ。既存ダイアログへの項目追加のみで対応可能 | 該当タスクなし（棚卸し#15の残存ギャップ、既存項目の隣に並べるだけの小規模拡張） | 小 | pending（**主回路チェック追加は不要と殿裁定2026-07-13で除外**——T-041で新規シート作成時の種別選択ダイアログが既にあり、既存シートを事後的に切替える手段は不要と判断） |
| P-071 | 隠密（T-069往復3周目事後レビュー2026-07-13、パターン再発検知3回目・確定） | `CommitDeviceNameEdit()`（未確定編集を選択状態変更前に確定する処理）の呼び忘れが、T-049・T-066に続き**3回目**の同型再発。根本原因（選択状態を動かす入口が複数箇所に分散し、確定処理の呼び出しが呼び出し元の自己申告に依存）は一度も解決されておらず、局所パッチの積み重ねのみ。次に新しい選択状態変更経路（T-067のGroupFrame選択等）が追加されれば4件目が起きうる | 該当タスクなし（横断的な構造課題、T-069のパッチとは別枠） | 中（構造的解決＝確定処理をsetter側へ集約する等の設計判断を要する。既存の新規選択可能状態チェックリストへ6項目目として追加する軽量案もあり） | pending（**家老所見**: 3回目確定ゆえ制度化の要否を殿に上申） |
| P-072 | 隠密（T-069往復3周目事後レビュー2026-07-13、派生提案） | 「表示と実行の整合原則」（往復2〜3周目でT-069が右クリック経路のみ達成）が、左クリック・矢印キー等の既存経路には未適用のまま残存。左クリックでCellWidth>1要素の非アンカーセルを選ぶと空選択になる・矢印キー移動後のDeleteが無反応、等、右クリックとの左右非対称が生じる。`SelectedElement`ゲッター自体（`el.Pos == pos`単純一致）の見直しを要する | T-069範囲外（右クリックメニューのスコープ外の既存経路） | 不明（`SelectedElement`ゲッターの設計変更を伴う可能性） | pending |

<!-- 判断: pending（承認待ち） / approved → 移送先 T-ID / rejected -->
