# タスク台帳アーカイブ（完了・取り止めタスクの詳細経緯）

本ファイルは 2026-07-09 の todo.md 軽量化（殿指示）で分離した、分離時点の台帳**全文スナップショット**。
完了・取り止めタスクの詳細経緯（実装コミット・レビュー往復・裁定の記録）はここを参照する。

- **生きているタスクの最新状態は必ず `docs/todo.md` を見ること**——本ファイル内に残る
  未完了タスクの記述は分離時点の古いスナップショットであり、更新されない。
- 以後の運用: タスクが完了（Done/Rejected）したら、todo.md の詳細をそのまま本ファイル末尾の
  「## 追記（軽量化以降の完了分）」節へ移し、todo.md 側は1行索引に縮める（家老の仕事）。

---

以下、2026-07-09 分離時点のスナップショット原文。

- 状態: Proposed → Approved → In-progress → Done / Rejected（+ Blocked＝外部要因待ち）
- 種別: auto-OK（家老の裁量で采配可） / gated（殿の承認を経たもの）

## 現在の要望スコープ

- REQ-01: 技術スタック選定（`docs/ecad2-stack-decision-brief.md` 参照）

## 優先順位チェックリスト

進行順・完了状況を一目で確認するためのチェックリスト。詳細な備考は下の「タスク」表を参照。
チェックは`状態`列がDoneのものに付与。並び順はおおむね着手順＋今後の優先順位（未定のものは末尾にまとめる）。

- [x] T-001 技術スタック裁定
- [x] T-002 フォーカス保持PoC
- [x] T-003 最終スタック確定（WPF）
- [x] T-005 WPF非技術面検証
- [x] T-006 タブ切替フォーカス喪失対策
- [x] T-004 4セッション体制・雛形/アーキ設計
- [x] T-007 GuiEcad実ソース移植
- [x] T-011 保存先フォルダ名変更（GuiEcad→Ecad2）
- [x] T-008 UI/UX全体像設計
- [x] T-010 GX Works3 UI/UX調査（Web一次情報）
- [x] T-012 GX Works3実機追加調査
- [x] T-014 GX Works3技術スタック調査
- [x] T-009 Ecad2.App UI実装（骨格、全8段階）
- [x] T-024 gui_ecad 新規/開く/保存フロー調査
- [x] T-025 gui_ecad 要素選択・ヒットテスト調査
- [x] T-016 要素配置ロジック本体
- [x] T-026 左パネル→ナビツリー化・ツールバー移行（観点1〜9すべて実機検証済み、副産物バグ3件も解消）
- [x] T-030 グリッド線表示の有効化（実機テスト補助、殿直接依頼）
- [x] T-017 要素選択・編集フォーカス制御の本実装（矢印キー・Deleteキー含む、実機検証済み）
- [x] T-027 選択中セルの視覚的ハイライト表示（T-017に統合、実機検証済み）
- [x] T-018 DesignRuleCheckと下部出力パネルの接続（優先1・完了、忍者再検証OK 2026-07-04）
- [x] T-020 空状態⇔作業領域の動的切替（優先2・完了、実装OK・回帰なし。濃紺の最終目視・最終判断はT-019後。2026-07-04）
- [x] T-023 LadderCanvasアクセシビリティ強化（優先3・完了、忍者検証OK 2026-07-04。R9をここに一本化）
- [x] T-021 キーボード規約の残り（全増分＋母線可視スクロール追加修正まで実装・検証完了、mainマージ済み 2026-07-05。**殿使用感確認済み（2026-07-05）：問題なし、完全確定**。spec: `docs/archive/ecad2-t021-keyboard-spec.md`）
- [x] T-019 ドキュメント管理（新規/開く/保存）（**完全Done 2026-07-05**：基本実装＋殿裁定反映（未保存確認3択・新規=1シート・起動濃紺#24325A・保存ガード）＋案B（濃紺状態のツール無効化・案内表示）まで、殿実機採決・忍者検証とも全OK）
- [x] T-015 図形ビジュアルプレビュー（**完了2026-07-05**：案1=部品選択リストへサムネイル追加。忍者実機7観点全OK・回帰なし。サイズ24px等の見た目微調整は殿帰宅後確認）
- [ ] T-028 浮動インライン入力ダイアログの「拡張表示」ボタン（殿確認済み・今回実装不要、T-026段階4から分離。**位置・レイアウトの殿注文3点は2026-07-05にT-033へ統合**）
- [x] T-029 ツールバーボタン配置時のゴースト表示（**取り止め=殿裁定2026-07-09**：ゴースト表示はキーボードファースト理念と相反する仕様のため不要と裁定（隠密の先行調査でUI/UX選択肢を提示した際の御下命）。隠密の先行調査書`docs/archive/ecad2-t029-presurvey-onmitsu.md`は収蔵維持——DrawPreview既存資産・GuiEcad前例・実装方式比較の知見は将来の参考資料。実装なし・簡易版=ステータスバー表示が引き続き現行仕様）
- [ ] T-022 ステータスバー高情報密度化（他機能への依存あり）
- [ ] T-013 ツールバー/メニューアイコン本格意匠（素材制作寄り）
- [x] T-033 配置後入力の真の非モーダル浮動インライン化（**完全Done 2026-07-07**：全5増分＝非モーダル骨格・殿注文3点＋位置バグ根治（Margin残留の自己参照ループ）・拡張表示ボタン配置・種別シンボル化・7種統一（表示どおりの動作）。殿最終目視OK）
- [x] T-036 配置時の機器表即時反映＋デバイス名編集の一連修正（**完全Done 2026-07-05**：配置時反映・FocusScope構造バグ・Esc層消費・空文字整合まで、忍者検証・殿実操作確認とも全OK）
- [x] T-034 App層（ViewModel）テストプロジェクト新設＋Dirty/HasProject等の回帰テスト（**完全Done 2026-07-06**：新規12件・計15件合格、往復1周→隠密再レビュークリーン→忍者軽量スモークOK。Dispatcher依存の発見はP-016へ）
- [x] T-035 .gcadpart読込時のID重複検出＋再採番（**完全Done 2026-07-06**：CreationTime最古優先の先勝ち再採番＋書き戻し。往復1周＋Wチェックで誤観測2件を訂正、忍者実機5観点全OK・回帰なし）
- [x] T-037 部品選択リストへORa/ORb追加（固定7種化）（**完全Done 2026-07-06**：往復3周（上限超過は殿裁可・侍C案）→隠密最終レビュークリーン→忍者差分確認全OK。P-010由来、殿裁定2026-07-05=案1）
- [x] T-038 診断ログ連携運用の整備（**完了2026-07-05**：忍者起草→殿承認→ninja.mdへ正式統合＋karo.md/samurai.mdへ参照節追加）
- [x] T-039 操作トレースログ基盤（**完全Done 2026-07-06**：侍実装＋修正往復2周→隠密再々レビュークリーン→忍者実機確認4観点全OK・回帰なし。旧CRITICAL解消を排他ロック実験で直接実証）
- [x] T-040 ツールバー配置系ボタンのGX様式化（**完全Done 2026-07-07**：実装3回（8d10684/52e064d/8a3ad4a）→隠密再レビュークリーン→忍者スクショ・動作確認全OK→殿目視確定「OKよくできてる」）
- [x] T-043 ORa/ORbサムネイルのシンボル統一（**完全Done 2026-07-07**：往復2周=IsOrEligible/Roleベース化+Categoryゲート、忍者差分確認全OK、殿目視OK）
- [ ] T-044 OR自動配線の冗長縦分岐抑止（**一旦保留＝殿裁定2026-07-07**：実装d04c9a3＋隠密再レビュー＋忍者実機とも全OK・電気的に正常だが、連鎖ORで低ズーム時に右合流線が断線して見える描画欠陥が判明。自動ロジック精緻化は袋小路[真因はP-003/P-028の既存欠陥]ゆえ手動配線基盤T-041で対処する方針へ転換。断線は描画のみ・電気的正常を忍者実測[WireNumber一致]で確定済み）
- [x] T-041 主回路用の横配線・縦分岐線の手動記入**＋消去＋修正**（**完全Done 2026-07-08**：全7増分完了。増分1〜5=4種（VerticalConnector/WireBreak/FreeLine/ConnectionDot）の設置・消去・選択＋主回路シート作成ダイアログ（増分5は`CurrentSheetIndex`setter設計で往復3周、P-030）。増分6=ドラッグUI基盤PoC 4項目PASS。増分7=4種の移動・リサイズ本実装（案X=ドラッグ+キーボード等価、端点伸縮は案2=Tabトグル。往復4周：所見A/B/C→X（ドラッグ確定がLostMouseCaptureと競合し全種機能不全、隠密が実測実証）→Y/Z/AA→AB（境界クランプ起因の実測クラッシュ）/AC/AD）＋P-039（列ドラッグ、新制度=テスト設計分離+RED証明の初適用）。最終=隠密レビュークリーン→忍者実機確認全観点OK・回帰なし（`docs/archive/ecad2-p039-ninja-verification.md`）。テスト179件全合格。範囲外検出はP-031/P-032/P-035〜P-038へ記録済み）
- [x] T-042 App層テストの実環境副作用解消（**完全Done 2026-07-06**：往復ゼロ・隠密実測CONFIRMED・忍者スモーク全OK。P-019由来、殿裁定2026-07-06=T-040の前に差し込み）
- [x] T-045 App層リファクタリング（配置前検証サービス抽出＋Dispatcher依存分離）（**完全Done 2026-07-09**：全4増分＋補遺2クローズ、家老裁量`--no-ff`マージ`5f2ee6e`でmainへpush済み。**実装計画殿裁可2026-07-08**：`docs/archive/ecad2-t045-implementation-plan-samurai.md`（増分A=P-016 Dispatcher分離→B=P-025 VM層→C=View層+死にコード掃除→D=ドラッグ外枠共通化、専用ブランチ`feature/t045-app-layer-refactor`・中断基準つき）を計画どおり承認、着手指示済み。以下は起票時の経緯：**T-041完了後着手＝殿裁定2026-07-08**：P-025（配置前検証サービス抽出、P-020/021/022/024由来）とP-016（`SheetNavigationViewModel`のDispatcher直接依存分離）を統合。MainWindowViewModelの状態管理散在——選択/ドラフト/ドラッグのクロスカットクリア漏れがT-021・T-041増分2/5/7で反復——が全体を通して最も発生頻度・手戻りコストが高いと家老所見、テスト容易性向上（P-016）も合わせて解消する狙い。**追加根拠（2026-07-08、隠密所見AC）**：T-041増分7で4種（VerticalConnector/WireBreak/FreeLine/ConnectionDot）のドラッグ状態機械が同一外枠（nullableフィールド+`IsDragging*`+`ForceCancelDrag*IfAny`+Begin/Update/Confirm/Cancel）のままコピペで4並列し、同一の設計ミス（所見Y）が独立に4回混入するリスクを実際に体現。共通型（DragSession等）への集約を将来のT-045設計対象に含める。進め方=**専用ブランチ（mainを直接汚さずリスク回避、殿指示2026-07-08）を作成して作業**→隠密の構造調査（ホットスポット計測）→侍の計画起草→殿裁可→増分実施→完了後mainへマージ。**増分B対応表=殿裁可2026-07-09（案A=計画第一案+家老補完）**：EmergencyStop→PushButton・ContactorMain3P→Relay（MCコイルと同一機器の種別揺れ防止）・ThermalOverload/ThermalOverload3P/Motor/Breaker3P→Other・Timer接点系（限時/瞬時）→Timer、他は計画第一案どおり。種別列は英語表示のまま＝日本語化はP-020副次所見に据え置き（殿確認済み）。**増分Aクローズ2026-07-09**＝実装`eb3e9b0`→隠密レビュークリーン（docs収蔵`docs/archive/ecad2-t045-increment-a-review-onmitsu.md`、code-review新知見はテスト盲点・簡潔性のみ→家老仕分け：所見1・2＋軽微指摘＋所見4・9=補遺采配、所見10=P-040送り）→忍者実機回帰全OK（`docs/archive/ecad2-t045-increment-a-ninja-verification.md`）。**増分A補遺もクローズ2026-07-09**（`aba8c51`=テスト盲点2件補強・新規4テスト・RED2種実測、183件全合格、隠密レビュークリーン・実機不要=家老判断）。増分B往復1周目＝侍実装`e45c2d3`→隠密レビュー要修正（セレクトSW再採番誤分類CONFIRMED）→制度適用（隠密テスト設計→侍修正`3c9dd5a`・RED証明・199件全合格）→隠密再レビュークリーン（所見G=経過観察の家老裁定）。忍者実機4観点全OK→**増分Bクローズ2026-07-09**（`docs/archive/ecad2-t045-increment-b-ninja-verification.md`）。増分C実装完了`bf0e2c2`（境界チェック=殿裁定の既存様式・RowAtDip除去・208件全合格）→隠密レビュークリーン（指摘ゼロ）→忍者実機必須4観点全OK＝**増分Cクローズ2026-07-09**（範囲外検出はP-041記録）。増分D＝ForceCancelIfAny共通化+ConfirmDrag<T>/CancelDrag<T>4種統一（`10b350c`+`9225c3a`、隠密2度クリーン・208件+ドラッグ96件全合格）。補遺2＝Stryker棚卸し起点のテスト補強（`dc81b74`、全20値Theory・notify発火12箇所・228件全合格、隠密クリーンでクローズ）。**増分D最終実機＝全観点OK 2026-07-09**（PC再起動で白紙描画復旧を確認、4種ドラッグ回帰一巡＋全体スモーク、`docs/archive/ecad2-t045-increment-d-ninja-verification.md`。範囲外の気づき=P-042記録）→マージ3条件（全増分パイプライン通過・228件全合格の侍再実測・忍者最終回帰OK）成立→**殿裁定2026-07-09（離席前）どおり家老裁量で`--no-ff`マージ`5f2ee6e`→main push・事後報告（2026-07-09）**。正直な限界（総括）＝将来第5型ドラッグ追加時に`CancelDrag<T>`経由を強制する型制約は無く人的レビュー依存（隠密CONFIRMED・実装コメントにも明記）
- [ ] T-046 「必ず通過するテスト」防止の仕組み化（**殿裁定2026-07-08、T-045リファクタリング前に実施**：①RED先行証明の必須化＝バグ修正の回帰テストは修正前コードでREDになることをgit stash実測で証明し報告に含める（`samurai.md`・`karo.md`へ【MUST】追記済み、即日運用開始）②Stryker.NET＝**手動棚卸しから段階導入で確定（殿裁定2026-07-08）**：隠密調査（`docs/ecad2-t046-stryker-survey-onmitsu.md`、WPF相性OK・全体3分弱・現状score Core 3.76%/App 19.79%）を受け、往復修正案件のクローズ時に隠密が手動実行してテストの穴を棚卸しする運用から開始（`karo.md`・`onmitsu.md`へ運用追記済み）。score改善後にCI化を再検討③**テスト設計と実装の分離**＝バグ修正・往復案件では隠密が仕様側からテスト設計（同値分割・境界値分析・状態遷移・対称性点検・`[Theory]`活用の技法適用必須）を先に起草し、侍はコード化に徹する（実装者バイアス対策＋テスト設計技法の欠如対策、殿指摘2件を制度化。`onmitsu.md`「テスト設計の起草」・`samurai.md`「テスト設計と実装の分離」・`karo.md`同名節へ【MUST】追記済み、即日運用開始）。背景=T-041増分7で「旧実装でも新実装でも通る回帰テスト」が複数残存していた実例（忍者静的レビューで発覚）＋既存テストが場当たり的なケース列挙で体系的技法を欠く点への殿指摘）
- [ ] T-047 手動配線系（F9/F10系）のツールバーボタン作成（**起票=殿指示2026-07-09**：T-041実装の手動配線4種——FreeLine横線=F9・縦線=Shift+F9／VerticalConnector=Shift+F9／WireBreak=F10／ConnectionDot=F10——は現在キーボード専用で、ツールバーに対応ボタンが無い（T-045増分D忍者検証記録§1でも「専用ボタン無し」と明記）。既存の配置系ボタン（T-026の7ボタン化・T-040のGX様式意匠・F5〜F8系）と整合するボタン追加を行う。考慮点=シート種別（主回路/制御回路）でツール割当が変わる仕様に対するボタンの活性制御・表示の扱い／ボタン数とグルーピング／意匠。**ボタン構成・見た目はUI/UX分岐ゆえ着手時に殿へ選択肢提示必須**。**着手順=T-029取り止め（殿裁定2026-07-09）につき繰り上げで着手（元裁定=T-029→T-047の順）**。隠密の先行調査→殿へ選択肢提示→裁定→侍実装の順）
- [ ] T-032 CSV取り込みによる図面自動生成（将来構想、詳細仕様未定）

## 侍への実装プラン（隠密の調査を踏まえて家老が逐次更新）

1. **T-026・T-030・T-017・T-027**（すべてDone）：実機検証まで完了。
2. **調査完了グループ（実装順を殿承認、2026-07-04）**：隠密の先行調査完了済み（`docs/archive/ecad2-preimplementation-survey-onmitsu.md`）。
   以下の順で家老が逐次アサインする。うち優先4・5は着手前に別途殿の判断を仰ぐ。
   1. **T-018**（DesignRuleCheckと下部出力パネルの接続）：UI/UX殿裁定済み（DataGrid+色分け）、着手障壁が最も低い。**最初にアサインする。**
   2. **T-020**（空状態⇔作業領域の動的切替）：UI/UX殿裁定済み（空状態=濃紺⇔作業領域=白の状態依存配色を新規導入）、独立性高い。
   3. **T-023**（LadderCanvasアクセシビリティ強化）：純技術・UI/UXゲート不要。骨格1〜2日＋記号増減毎の同期コスト継続。
   4. **T-021**（キーボード規約の残り）：着手前にdesign-brief R6/R7（Enter=確定/Esc）の訂正可否を殿へ諮る（GX Works3由来として不正確と隠密が指摘）。
   5. **T-015**（図形ビジュアルプレビュー）：**要スコープ再定義**。T-026で左パレット→ツールバーへ図形選択が移行済のため「左パレットの図形」という対象が消滅。プレビュー対象（ツールバー記号／右パネル自作パーツ選択等）をどこにするか殿へ選択肢提示してから着手（UI/UX分岐）。
3. **T-021マージ後の順序（殿裁定2026-07-05）**：mainマージ → T-019（ドキュメント管理）→ T-015（スコープ再定義の選択肢提示から）——**すべて完了（2026-07-05）**。
4. **以降の着手順（殿裁定2026-07-05打ち合わせ、同日更新）**：T-019追加増分（案B）の検証完了 → **T-036（機器表反映+デバイス名編集修正）→ T-039（操作トレースログ基盤、殿裁定でT-036の次へ）→ T-034（App層テスト）→ T-035（ID重複）→ T-037（リスト固定7種化）→ T-033（非モーダル化＋殿注文3点、次期本命）**。
4. **調査不要／調査済みグループ**（優先順位は上記グループの結果を見てから改めて検討）：
   - T-019（先行調査T-024済み、未着手）
   - T-022（機種名/局番/ステップ数など他機能への依存があり、依存元の実装状況次第）
   - T-013（意匠制作、実装というより素材作成寄り）
5. **T-027**：T-026段階4のSelectedCell実装と同時に切り出し済み。単体タスクとして別途着手時期を検討。

## タスク

| ID | タイトル | 状態 | 担当 | 根拠 | 種別 | 備考 |
|----|---------|------|------|------|------|------|
| T-001 | 技術スタック裁定（路線A=WPF本命 ／ 路線B=Qt） | Done | 殿 | REQ-01 | gated | 殿裁定：路線A（WPF本命）で仮確定。Qt不採用・Avaloniaはクロス保険で保留。最終確定はT-003（PoC結果次第） |
| T-002 | フォーカス保持PoC（PointerReleased後のフォーカス保持・大量記号描画・PDF出力の最小検証） | Done | 侍(実装)／忍者(実機検証) | REQ-01 | gated | 実機検証完了。フォーカス保持=良好、記号描画=5万個113ms、PDF出力=良好。タブ切替時のフォーカス挙動のみ考慮事項あり |
| T-003 | PoC結果を踏まえた最終スタック確定 | Done | 殿 | REQ-01 | gated | 殿裁定：WPFに正式確定。T-002/T-005/T-006の検証結果を踏まえた最終決定 |
| T-004 | 4セッション体制の立て直し・選定スタックで雛形/アーキ設計へ着手 | Done | 侍 | REQ-01 | gated | 雛形完成（コミットfb85746）。src/Ecad2.sln、Core(Model/Simulation/Rendering/Persistence)/Rendering.Wpf/Pdf/App、tests/。dotnet build成功。CLAUDE.md担当パス欄に反映済み |
| T-007 | GuiEcad実ソース（gui_ecad）からのModel/Rendering/Simulation/Persistenceの移植 | Done | 侍 | REQ-01 | gated | 全段階完了(コミット一覧: Model 6eab0ba／Rendering a762b20,d1198cc,3c67ab5,771fb08,b825ef4,ac0c703／Persistence 88ea0fd)。.GCAD互換性はgui_ecad実サンプルでのテスト3件で実証済み。気づき：PartFolderStore等の保存先フォルダ名が"GuiEcad"のまま→殿へ確認中 |
| T-008 | ecad2 UI/UX全体像設計（画面構成・操作フロー、GuiEcadの画面構成にこだわらず新規検討） | Done | 殿 | REQ-01 | gated | 殿裁定：区画分け(左パレット/中央キャンバス/右機器表・プロパティ/下部出力)は維持、中身は視覚シンプル化＋キーボード主体・ボタン補助に刷新。詳細`docs/ecad2-ui-ux-design-brief.md` |
| T-009 | Ecad2.App UI実装（視覚シンプル化・キーボード主体設計・GX Works3踏襲の反映） | Done | 侍 | REQ-01 | gated | 全8段階完了。コミット一覧：86898fb/1d682ea/211a945/94a9742/351d908/f20ae34/4fba36a/a15def0/12b6736/7c7d3a1/fa0a5ef/6d4d458。骨格実装完了、残課題はT-015〜T-024参照 |
| T-015 | 図形ビジュアルプレビュー（**案1**：右パネル「部品選択」リストの各項目にサムネイル追加） | Done | 侍 | REQ-01 | gated | T-009残課題2。隠密の先行調査完了（`docs/archive/ecad2-preimplementation-survey-onmitsu.md`）：GuiEcadに汎用サムネイル生成ロジックはないが、DiagramRenderer.DrawPreview／IRenderer抽象／SymbolGlyphs等は流用可能。ecad2新規実装はDrawingVisual→RenderTargetBitmap方式を推奨（SVG経由よりコスト低）。**スコープ再定義の殿裁定（2026-07-05）：案1採用**——`PartSelectionList`のItemTemplateへサムネイル（DrawPreview→RenderTargetBitmap）をテキスト左に追加、固定6種+自作パーツ両方が対象（選択肢の詳細は`docs/archive/ecad2-t015-scope-redefinition-options-onmitsu.md`）。T-019完了後に着手（殿不在中の着手も委任済み、新たなUI/UX分岐が出たら保留し帰宅待ち）。**実装・検証完了（2026-07-05）**：段階1〜4（f881005=DrawPreview拡張／a842089=サムネイル生成ラッパー／6963a62=SelectionEntries+ItemTemplate）→隠密レビュー要修正2件（PartLibrary構築の重複コード・XAMLコメント追随）→2d5f3dc（一本化リファクタ、往復1周）→隠密再レビュー合格→忍者実機7観点全OK（サムネイル表示・図形区別・自作パーツ実形状反映・配置フロー回帰なし・リスト応答実測187ms・スクリーンショット採取済み・回帰スモークOK）。ID重複時の誤サムネイルはP-009。**殿帰宅後確認**：サムネイルサイズ24px・余白の見た目（スクリーンショットは忍者scratchpad、実機目視推奨）。範囲外の気づき2件はP-010（固定種ラインナップ不一致）・P-011（配置時の機器表未反映）へ |
| T-016 | 要素配置ロジック本体（キャンバスクリックでSheetへ要素追加、ToolState接続） | Done | 侍 | REQ-01 | gated | 全バグ解消(94a4e65)、忍者が観点1〜6すべて解消・正常動作を再検証確認済み。LadderCanvasのAutomation Peer未実装分はT-023へ送り |
| T-026 | 左パネルをシート/ページナビゲーション(ナビツリー)に置き換え、図形選択はツールバーアイコンへ移行 | Done | 侍 | REQ-01 | gated | 全段階(1〜4)完了、忍者が観点1〜9すべて正常動作を確認済み（2026-07-03、再検証込み）。左パレット→ナビツリー化、ツールバー7ボタン化（選択/a接点/b接点/コイル/端子台/ORa接点/ORb接点/自作パーツ）、自作パーツ選択UIは案B（右パネル下段Tool.Mode連動切替）、浮動インライン入力ダイアログ（T-021前倒し）、F5-F8+Shift+F5/F6キーバインド、パネルフォーカスはShift+Tabに変更。副産物として3件の既存共有バグを解消：DiagramRenderer.PageSize()のTotalRows未使用バグ（P-002、コミット28b6195）、LadderCanvasのヒットテスト真因＝透明領域未描画（コミット7fa3e6f）、LeftTerminator/RightTerminatorのTopRow/BottomRow混同バグ（P-003、コミット282f3eb、3重並列でも動作確認済み）。詳細な設計経緯はgit log（コミット9dcc32d〜282f3eb）参照。派生タスク：T-027(セルハイライト)/T-028(拡張表示ボタン)/T-029(ゴースト表示) |
| T-017 | キャンバス上の要素選択・編集フォーカス制御の本実装（右パネル連動含む） | Done | 侍 | REQ-01 | gated | 全機能の実装・実機検証完了（2026-07-03）。SelectedCell再利用+SelectedElement連動でプロパティパネルに種別・デバイス名編集表示（種別自体の変更は削除→再配置が必要なため別タスク）、デバイス名変更はDeviceRenamerへ委譲。矢印キーでセル単位移動（design-brief原則1どおりキャンバスフォーカス時のみ有効、ScrollViewer既定スクロールを上書き）。選択セルの視覚的ハイライト（T-027を統合）実装。Deleteキーで選択要素を削除（Escは従来通り選択解除/キャンセルのまま変更なし、他要素が同じデバイス名を参照中なら機器表エントリは保持）。忍者検証で検出された3件（種別表示・機器表未反映・ステータスメッセージ残留）も修正済み。コミット1b4301e, a58ed86, bb03055 |
| T-018 | DesignRuleCheckと下部出力パネルの接続 | Done | 侍 | REQ-01 | gated | 完了（2026-07-04、忍者再検証で全OK）。実装bf6791d＋不具合修正6cc84a5（クロスシートジャンプ周り3件A/B/Cを修正）。DataGrid＋重大度色分け（Error=赤/Warning=橙/Info=グレー、殿裁定どおり）、行選択でシート・セルへジャンプ。忍者検証で観点1/4・同一/別シートジャンプ全OK、UI Automation偽結果の罠にも留意して物理クリックで再検証済み。家老が侍へ采配（優先1）。T-009残課題5。隠密の先行調査完了（`docs/archive/ecad2-preimplementation-survey-onmitsu.md`）：DesignRuleCheckはGuiEcadと完全同一移植済み、GuiEcad側出力はListView+文字列整形（色分けなし）。**殿裁定（2026-07-03）：構造化DataGrid＋重大度色分けを採用**（ObservableCollection<Diagnostic>+DataGridバインディング） |
| T-019 | ドキュメント管理（GcadSerializer Load/Save、新規/開く/保存のメニュー・ツールバー接続） | Done | 侍 | REQ-01 | gated | **殿裁定（2026-07-05）：T-021のmainマージ後に着手する次タスク。着手（2026-07-05、殿就寝中の続行指示による。UI/UX分岐は保留し標準規約部分から進める方針）。実装・検証完了（2026-07-05、暫定仕様）**：段階1〜5（715e0e6/4b34585/7cb7aa0/ece2525、メニュー/ツールバー/Ctrl+N,O,S接続は各段階に統合）→隠密静的レビュー要修正3件（DRC残留・StatusMessage残留・Tool未リセット、いずれもReplaceDocument内のクリア漏れ→4ba546aで往復1周解消）→忍者実機8観点全OK・回帰なし（濃紺空状態のクラッシュなし表示含む）→忍者検出の範囲内欠陥1件（不正ファイル読込時に生JSON例外文面表示。Wチェック並行方式：侍診断と隠密独立調査が完全一致＝双方catch節のex.Message直埋め込み・変換層未実装）→42f8036で修正（往復2周、一般向け日本語文面+対象パスのみへ、保存側同型も同時解消）→隠密再レビュー合格→忍者当該2経路再確認OK。プランdocs/archive/ecad2-t019-implementation-plan-samurai.md／レビューdocs/archive/ecad2-t019-review-onmitsu.md／検証docs-notes/ecad2-t019-verification-ninja.md。**UI/UX分岐の殿裁定（2026-07-05）**：(1)未保存確認フロー=**入れる**（様式は標準3択=保存/破棄/キャンセル、Dirty判定基準は侍裁量の技術選択） (2)「新規」の初期状態=**即1シート生成**（Sheets=0暫定から変更） (3)エラー文面=殿実機確認中 (4)最近使ったファイル等は今回範囲外と判断済み。(1)(2)の追加実装を侍へ采配（2026-07-05）→計4件（+シート追加/削除時のHasProject通知発火修正=殿実機検出の範囲内欠陥、Wチェック診断一致）実装済みd9aa49b・隠密レビュー中。**追加裁定（2026-07-05）：起動直後=濃紺スタート**（ダミー文書生成を廃止、新規/開くで白へ）、**濃紺色は#24325Aへ調整**（殿裁定「ほんの少しだけ明るく」、色見本3案から選択）——次増分として侍が準備中。**殿の追加裁定4件（2026-07-05、外出前）**：T-021使用感=問題なし完全確定／T-015=案1採用／不在中の進行=T-019・T-020検証完走→Done化→T-015着手まで委任（新たなUI/UX分岐が出たら保留）。**追加実装2周目完了（2026-07-05夜）**：d9aa49b→隠密レビューで閉じる時確認漏れ（最重要）+Rename無条件MarkDirty→b7844d2（起動濃紺#24325A・Window_Closing確認・同値ガード同梱）→隠密レビューで新規回帰2件（初回シート追加の空白画面=CurrentSheetIndex 0→0通知不発／Sheets=0保存可能）→fdc994d（0→1遷移時明示発火+保存3入口ガード、往復2周=上限で仕留め）→隠密再レビュー合格→忍者一括実機10観点全OK（キャプチャ白化は一時的環境要因と切り分け済み、42f8036比較で3コミット起因を否定。濃紺#24325Aピクセル実測一致・初回シート追加の空白回帰解消を実測実証）。テスト0件の構造リスクはP-008、経過観察3件はobservations.md #7〜#9へ。**殿帰宅後の確認項目**：(a)濃紺#24325Aの色味目視（スクリーンショット有） (b)Sheets=0時の保存無効化=家老既定案の実挙動確認 (c)未保存確認3択・閉じる時確認の使用感 (d)エラーMessageBox文面。**殿採決（2026-07-05帰宅後）**：1-1濃紺色味=OK／1-2サムネイル見た目=OK／1-4エラー文面=OK／1-3で新注文=濃紺状態の配置操作が無警告で無視される件、**案B採用**（ツール系ボタン一式をHasProject連動でグレーアウト＋配置キーはステータスバー案内「シートがありません。新規作成から」）→**実装・検証完了（ae2db4b、2026-07-05）**：隠密レビュー合格（全キー経路網羅・リスト経由の穴なし・Escクリア整合を理論確認）→忍者実機5観点全OK（8ボタングレーアウト・案内表示・Ctrl+N後復帰・Sheets=0ファイルを開いた際のガード再現・回帰なし）。**これにてT-019完全Done**。T-009残課題6。**申し送り（消化済み）：T-020濃紺は殿実機確認済み（2026-07-05、色#24325Aへ調整済み・忍者ピクセル実測一致）。殿の色味最終目視のみ帰宅後** |
| T-020 | 空状態(濃紺)⇔作業領域(白)の動的切替 | Done | 侍 | REQ-01 | gated | 実装完了（2026-07-04、コミット6dc2fae）。ViewModel.HasProject＋XAML DataTriggerで濃紺#1E2A47⇔白を切替（GX Works3踏襲・殿裁定どおり）。忍者検証：結線・回帰OK。ただし濃紺の空状態はDeleteガード（Sheets.Count>1）でシート0枚に到達不可のため**T-019未実装ゆえ目視検証不能**。**殿指示（2026-07-04）：T-019完了後に濃紺を最終目視して最終判断する**→**殿実機確認済み（2026-07-05）：濃紺採用、色は#24325Aへ調整**（殿裁定「ほんの少しだけ明るく」、現行#1E2A47から。T-019の起動濃紺化増分と同時実施）。T-009残課題7。現状は作業領域色で暫定固定。隠密の先行調査完了：GuiEcadには「未作成の空状態」概念自体が存在しない（起動時に即座に空ドキュメント生成）。**殿裁定（2026-07-03）：ecad2で新規導入する。GX Works3風の濃紺⇔白切替も踏襲**。ViewModelにHasProject等の状態プロパティ＋XAML側DataTriggerでの実装を推奨（隠密案） |
| T-021 | キーボード規約の残り（Enter=配置確定＝案X、Esc多段階4層、モーダル非ネスト規約、パン＝矢印追従+ホイール） | Done | 侍 | REQ-01 | gated | **作業は`feature/t021-enter-placement`ブランチ上（mainではない、要注意）。** 仕様確定（詳細は `docs/archive/ecad2-t021-keyboard-spec.md`）。案X採用、殿裁定：分岐A=ツール保持連続配置／分岐B=命名中Escは配置ごと取消／案C=配置後入力は当面現行モーダルのまま。PoC成立（poc/隔離、フォーカス復帰は暗黙委譲でも成立を実証）。**増分1（Enter配置+フォーカス復帰、コミット457cddb）実装・機能面は忍者検証済み。** その後、フォーカス関連の後追い修正が3往復（cf54a3e/c83d9e3/1315fe4）発生し、隠密の2回目コードレビューで「後追いパッチのモグラ叩き」構造問題が判明（懸念4＝ツールバーをキーボードナビ中に選択ツールでEnter→キャンバスへ強制フォーカス→Tabで戻れずGridSplitterに迷い込む、キーボードファースト理念に反する）。**殿裁定（2026-07-04）で設計集約へ切替：案(a)+(c)ハイブリッド採用**（隠密作成 `docs/archive/ecad2-t021-focus-design-consolidation-plan-onmitsu.md`、コミット43b962d済）。増分1の既存4コミットは破棄不要、増分(vi)として上乗せ実装。**増分(vi)完了（2026-07-04、忍者最終検証で全項目OK・push済み）**。経過：初回実装39c419d→隠密レビュー1周目=要修正4件（Click全廃によるUIA Invoke完全無反応ほか。当初プランのClick→Preview分離方式自体の欠陥と判明）→家老裁定で方針転換（**Clickイベント復活＋PreviewKeyDownは「キーボード由来」記録のみ＋Clickハンドラ内で記録==sender照合によりFocusCanvas要否分岐**）→bcb8aab→2周目=フラグ堅牢化（由来ボタン参照化等）→8b3d922→3周目レビュー通過→忍者実機でUIA Invoke・マウス・回帰OKだが**Space非対称NG**（Enterはツールバーに留まるがSpaceはキャンバスへ）→殿承認の3周目修正=案A（LostKeyboardFocusのNewFocus null判定＋Focus()復帰、5ec9e55）→実機で同症状再現→**Wチェック並行切り分け**（侍の診断ログ実測＋隠密の理論再調査）で真因確定：**Space完了時、ButtonBase内部のFocus(null)は主フォーカススコープの復元先（直前の具体的要素）まで遷移するため、NewFocus値で「一時喪失」と「真キャンセル」を区別する発想自体が原理的に不成立**→案B切替（殿承認済み条項の発動。LostKeyboardFocus完全撤去＋isKeyboardOrigin時のsender.Focus()復帰維持、bb6acfb）→隠密5周目通過→忍者最終検証で**Space/Enter対称性（3回試行）・UIA Invoke・マウス経路・GridSplitter 3箇所Tab除外とも全OK**。**経過観察（隠密・家老合意、要修正としない）**：(1)Space押下中の強制フォーカス移動で記録残留→直後に同一ボタンをUIA InvokeするとFocusCanvas判定が1回ズレうる（検証専用級の極端条件・通常操作で自己修復） (2)Alt+Tab等ウィンドウ非アクティブ化でも同種の残留可能性。**増分(iv)実装完了（2026-07-04、コミット0ab81d8）**：Escapeケースを一括クリアから4層段階判定へ分解（層1=編集キャンセルはElementPlacementDialogのIsCancel既存でWPF標準Esc規約が有効ゆえ追加実装なし・分岐B原子的取消も構造上成立／層2=PlaceElement→SelectDefault・SelectedCell保持／層3=選択解除／層4=無視。FocusCanvas()は全層無条件維持）。隠密静的レビュー1周目：**要修正1件（中〜高）＝層4のStatusMessage残留退行**（セル未選択でF5〜F8→TryPlaceElementガードのエラーメッセージ表示→Tool.Mode不変のままEscが層2・層3の条件を満たさず層4へ落ちクリアされない。旧一括クリアからの退行。finder2系統＋手動が独立に同一結論）→侍修正完了（往復1周目、コミットf531b76）：StatusMessageクリアを条件分岐全体の前へ1回だけ移設（層4含め一律クリア・重複2箇所も解消）＋不正確化したコメントも同梱修正→隠密再レビュー合格（旧「Esc常時クリア」不変条件の復元を確認）。**経過観察（新規）**：ToolMode未配線モード（PlaceConnector/PlaceFrame/PlaceLine/PlaceDot/PlaceWireBreak）が将来配線されると層2のEsc解除対象（Tool.Mode==PlaceElement限定）から漏れる——現状到達不能で実害なし、配線する増分で要注意。**増分(v)実装完了（2026-07-04、コミット7ad82ce）**：MoveSelectedCellでSelectedCell更新後にBringIntoView(CellRectDip(newCell))を呼ぶ1箇所差分（分岐C=BringIntoView標準挙動、プラン158行の家老承認どおり）。隠密レビューで要修正2件（右母線位置=Column==Columnsで実体の無い余白矩形へ過剰スクロール／グリッド端クランプの無変化時も発火し手動スクロール位置を引き戻す。後者は家老が経過観察から要修正へ格上げ）→侍修正（往復1周目、コミット8776795）：viewColumn=Min(Column, Columns-1)の呼び出し側クランプ（CellRectDip本体・SelectedCell・ハイライト・Automation Peerは不変）＋newCell!=currentガード＋断定コメント緩和→隠密再レビュー合格（ズーム時のMakeVisible変換考慮はTransformToAncestorの仕組み上妥当との理論裏付けも完了）。**経過観察（新規）**：未選択状態から初回Up/Left押下時のみ追従スキップ——current既定値(0,0)とnewCell(0,0)が一致するため。(0,0)は通常最初から可視ゆえ実害低（隠密・家老合意）。**全増分の実機検証完了（2026-07-04、忍者が(iv)(v)全10観点OK・回帰なしを確認：Esc4層2段階解除／セル未選択F5→Escメッセージクリア／ボタン即時全解除との対比／Esc後矢印移動／矢印4方向追従／ズーム150%位置ずれなし／右母線余白送りなし／端クランプ引き戻しなし／ホイール・ズーム回帰なし／Esc後FocusCanvas→矢印→追従連携）。残作業：殿の使用感最終確認（Esc2段階解除・ズーム時追従の実挙動）→mainへのマージを殿確認のうえ実施（`docs/archive/ecad2-t021-implementation-plan-samurai.md`参照）。** design-brief R6/R7のGX Works3由来記述は不正確との隠密指摘は現行の訂正注記(L53-58)で十分と殿確認済み(論点1=A)。T-009残課題8。**母線可視スクロール追加修正完了（殿直接指示2026-07-05）**：左端/右端到達時にBringIntoView矩形を母線側へ1セル拡張（340f53d）→隠密レビューCONFIRMED1件（列不変の行移動でwiden毎回発火＝手動横スクロール引き戻し）→侍が列変化ガード追加（7e70c3d、往復1周）→隠密再レビュー合格→忍者実機5観点OK・回帰なし。理論上の縁ケース2件はobservations.md #5/#6へ、範囲外の気づき（ズーム直後の選択解除、ヘルパー由来の可能性）はP-007へ記録。**mainマージ実施（2026-07-05、殿の委任「マージも任せます」による。使用感の感想は後日）** |
| T-027 | 選択中セルの視覚的ハイライト表示（枠線等、GX Works3のカーソル相当） | Done | 侍 | REQ-01 | gated | T-017に統合して同時実装・検証完了（コミット1b4301e）。詳細はT-017参照 |
| T-028 | 浮動インライン入力ダイアログの「拡張表示」ボタン（詳細な命令選択画面） | Proposed | - | REQ-01 | gated | T-026段階4の浮動インライン入力ダイアログ実装時、仕様未確定のため侍が未実装のまま起票（2026-07-03）。殿確認済み：今回は実装不要、別タスク化。**経緯（2026-07-05）**：位置・レイアウトの殿注文3点を一旦本タスクへ載せたが、殿裁定によりT-033（非モーダル化）へ統合して移管。本タスクは元の「拡張表示ボタン」のみに戻す |
| T-030 | キャンバスへのグリッド線表示を有効化（実機テストでの行合わせ補助） | Done | 侍 | REQ-01 | auto-OK | 殿直接依頼（2026-07-03）。ShowGrid=true化完了（コミット9fc7990）、build/test成功 |
| T-029 | ツールバーボタン配置時のゴースト表示（マウス位置追従の半透明プレビュー） | Rejected | - | REQ-01 | gated | T-026段階4観点4で必要性が判明（2026-07-03）し分離起票、簡易版（ステータスバー表示）で進行していた。着手承認（殿裁定2026-07-09）→隠密先行調査完了（`docs/archive/ecad2-t029-presurvey-onmitsu.md`：DrawPreview既存資産・GuiEcad前例あり・方式比較・UI/UX分岐4点）→**取り止め（殿裁定2026-07-09）：キーボードファースト理念と相反する仕様のため不要**。調査書は将来の参考資料として収蔵維持 |
| T-022 | ステータスバーの高情報密度化（機種名/局番/ステップ数等） | Proposed | - | REQ-01 | gated | T-009残課題9。本実装機能への依存あり |
| T-031 | ニーモニック（接点表記）の扱い方基準の調査 | Done | 隠密 | REQ-01 | auto-OK | 完了（`docs/archive/ecad2-mnemonic-naming-survey-onmitsu.md`）。結論：GX Works3・GuiEcadとも「内部識別子は英語、UI表示は日本語ラダー用語(a接点/b接点等、JIS C 0617由来の正式呼称)」で一致。GX Works3では「AND/OR」は接点命令(LD/AND/ANI/OR/ORI=直列/並列接続)と論理演算命令(ビット演算)の2カテゴリで意味が異なり衝突リスクがあるため、回路記号の表示名として単独採用するのは避けるべき。**方針確定：T-026のAND/NAND/OR/NOR等の内部識別子は維持してよいが、UI表示は「a接点」「b接点」「ORa接点」「ORb接点」等の日本語ラダー用語で統一する（現状のままが適切）** |
| T-032 | CSV取り込みによる図面自動生成（将来構想） | Proposed | - | REQ-01 | gated | 殿の将来構想（2026-07-03）。**GX Works3が出力するCSV（SHIFT-JISエンコード、ニーモニック命令列：LD/AND/ANI/OR/ORI/OUT等）を読み込んでラダー図を表示・自動生成したい**。実装時はニーモニックと回路記号のマッピングが必要（T-031の調査結果が基礎資料になる見込み）。GX Works3の実際のCSV出力フォーマット（列構成、命令表記の実例）は未調査。詳細仕様・着手時期は未定 |
| T-023 | LadderCanvasへのAutomation Peer付与等アクセシビリティ強化 | Done | 侍 | REQ-01 | gated | 完了（2026-07-04、忍者検証で全観点OK・回帰なし）。コミット63cc3a9。LadderCanvasAutomationPeer(ISelectionProvider)＋SymbolAutomationPeer(ISelectionItemProvider)で、キャンバス内各記号を個別ピアとして公開（Name=「種別[ 機器名] (行N列M)」、IsSelected・BoundingRectangle・追加削除追随、ズーム/スクロール考慮の画面座標）。副次効果：忍者のUI Automation検証効率が大幅向上。T-021のR9(アクセシビリティツリー明示管理)も本タスクに一本化。家老裁量で采配（優先3）。T-009残課題10。隠密の先行調査完了：WPFの一般パターン（OnCreateAutomationPeerオーバーライド＋子ピア手動生成）を確認、GuiEcadに前例なし。実装コストは中程度（骨格1〜2日＋記号増減毎の同期コストが継続発生） |
| T-024 | gui_ecadの新規/開く/保存フロー調査（T-019先行調査） | Done | 隠密 | REQ-01 | gated | 完了。`docs/archive/ecad2-guiecad-code-survey-onmitsu.md`参照。**重要**：gui_ecad現行コードにOnMenuRestartがDirty判定なしで即Exit()する実バグを発見（GuiEcad本体の欠陥、ecad2の範囲外・殿へ別途報告） |
| T-025 | gui_ecadの要素選択・ヒットテストロジック調査（T-017先行調査） | Done | 隠密 | REQ-01 | gated | 完了。結論：座標→グリッドセル変換+要素リスト線形走査、種別ごとHitTest+if-else優先順位。ポート個別ヒットテストは前例なし |
| T-010 | GX Works3のUI/UX調査（アイコン様式・配色・リボン/ツールバー構成・パネル配置の具体像） | Done | 隠密 | REQ-01 | gated | 完了（Web一次情報ベース）。`docs/ecad2-gxworks3-uiux-survey-onmitsu.md`参照 |
| T-012 | GX Works3実機によるUI/UX追加調査（T-010のWeb調査を実機で裏取り・精緻化） | Done | 隠密 | REQ-01 | gated | 完了。`docs/ecad2-gxworks3-uiux-survey-onmitsu-part2.md`参照。新発見：F-key併記アイコン・配置直後の浮動インライン入力・状態依存配色(空=濃紺/作業領域=白)。家老裁量で採用決定、T-009段階3以降着手可 |
| T-014 | GX Works3の技術スタック調査（殿直接依頼） | Done | 隠密 | REQ-01 | gated | 完了。`docs/ecad2-gxworks3-uiux-survey-onmitsu-part2.md`付録参照。結論：ネイティブC++(MFC/ATL)+.NET Framework(WinForms)+商用UIライブラリ3社(Syncfusion/DevExpress/Infragistics)のハイブリッド構成。参考情報として記録、直接のタスク影響なし |
| T-013 | ツールバー/メニューアイコンの本格的な意匠制作（記号・単色ベースのグラフィックデザイン） | Proposed | - | REQ-01 | gated | T-009段階3では簡易プレースホルダ(Path Geometry/Unicode記号)で仕組みのみ実装。本格的な意匠は別タスクとして切り出し(家老裁量) |
| T-011 | PartFolderStore/PinnedPartStoreの保存先フォルダ名をGuiEcad→Ecad2に変更 | Done | 侍 | REQ-01 | gated | 完了（コミット49b4554）。build/test成功 |
| T-006 | タブ切替時のフォーカス喪失対策の実装・再検証（同一テスト項目の再実行） | Done | 侍(実装)／忍者(再検証) | REQ-01 | gated | 対策：編集モード中はタブ切替操作自体を無効化（e.Handled=true）。再検証で問題なし・回帰なしを確認 |
| T-005 | WPF非技術面の多角検証（ライセンス・Microsoft保守体制/将来性・.NET対応ロードマップ・業界採用実績等） | Done | 隠密 | REQ-01 | gated | 完了。`docs/archive/ecad2-wpf-nontechnical-survey-onmitsu.md` 参照。結論：非技術面リスクは低〜許容範囲、WPF本命判断を補強 |

| T-033 | 配置後デバイス名入力の真の非モーダル浮動インライン化（現行ElementPlacementDialogはモーダルWindow） | Done | 侍 | REQ-01 | gated | T-021の隠密調査(2026-07-04)で判明した齟齬から分離。現行の配置後入力は別Windowのモーダルダイアログ(ShowDialog())で、design-brief/GX Works3が意図する「非モーダル浮動インライン」と食い違う。殿裁定=案C（2026-07-04）：T-021ではモーダルのまま案X実装、非モーダル化は本タスクに切り出し後日殿と相談。UI/UX分岐のため要殿確認。**殿注文3点を統合（2026-07-05、殿裁定）**：(1)表示位置を選択セル（カーソル位置）の真下にする (2)全体をもう少し小さく横長にし、接点選択と機器名入力を横並びに配置 (3)はい/いいえボタンが枠に収まらず下が切れているレイアウト不具合を修正——非モーダル化の際にまとめて作り込む。**殿の構想図提示（2026-07-05、GX Works3実画面。`docs/images/t033-gxworks3-inline-input-reference.png`に保全）**：選択セルの真下に横長入力バーを浮動（非モーダル）表示。構成は左から[小アイコン類][種別選択ドロップダウン=ASCII風の記号テキストで簡素表示・幅小さめ][機器名入力欄（長め）][OK][キャンセル][拡張表示]。拡張表示ボタンはバー上に配置のみ（押下時の詳細画面はT-028のまま別タスク）。**着手順裁定済み（2026-07-05）：T-036→T-034→T-035の後の次期本命**。**先後裁定（2026-07-07、殿）：T-041より先行で着手（In-progress、侍）**——侍はまず実装プランを起草（増分分割・フォーカス/キーボード制御はGuiEcad失敗領域＋T-021モグラ叩きの前歴ゆえリスク洗い出し優先、必要ならPoC先行）→UI/UX分岐は実装前に殿へ諮る。**侍プラン起草→家老裁可（2026-07-07、50dbcfa、`docs/archive/ecad2-t033-implementation-plan-samurai.md`）**：増分1=非モーダル化の骨格のみ（最高リスク、PoC先行承認。確認3項目=TextBoxフォーカス中のEnter/Esc・フォーカス復帰・バー表示中のグローバルショートカット影響）→増分2=殿注文3点＋画面端クランプ→増分3=拡張表示ボタン配置のみ。フォーカス遷移点はライフサイクル内の一箇所に集約する設計方針（T-021教訓）。仮既定=バー表示中の配置系ショートカットは現行モーダル同等（効かない）、異なる見せ方の分岐が出たら保留し殿へ。不明点の割り振り：非モーダル技術選定のUIA影響=隠密の並行調査／現行OK・キャンセル見切れの再現確認=忍者スポット（いずれも2026-07-07采配済み）。**隠密UIA影響調査完了（2026-07-07、`docs/archive/ecad2-t033-ui-automation-impact-survey-onmitsu.md`）＝同一Window内オーバーレイ推奨**——Popupは別HWND化に伴う落とし穴複数（WS_EX_NOACTIVATEでWin32フォーカスが渡りにくい→IsDefault/IsCancel・SendKeys到達に懸念、AccessKeyManagerはPresentationSource単位）。UIAツリー到達性自体は両方式とも理論上可。副産物：ButtonAutomationPeer.Invoke()はDispatcher.BeginInvoke経由でClick相当を発火と確認→OK/キャンセルはClickハンドラ一本維持を推奨（T-021増分(vi)不明点2への回答材料）。残る不明点3点は実機PoCで確認（文書内に明記）。**増分1 PoC成立（2026-07-07、8feb4b2、`docs/archive/ecad2-t033-poc-result-samurai.md`）**：技術選定を隠密所見どおり同一Window内オーバーレイへ切替。(1)Enter/Esc確定（IsDefault/IsCancel）成立 (2)フォーカス復帰は明示Keyboard.Focus(Canvas)必須（暗黙委譲では戻らない、T-021同様） (3)バー表示中のグローバルショートカット素通しが判明→**殿裁定（2026-07-07）＝無効化**（現行モーダル同等の使用感、安全側）。UIA/SendKeys到達性の最終確認は増分1本実装後の忍者実機で。OK/キャンセルはClickハンドラ一本維持（隠密推奨どおり）。**増分1本実装を家老裁可（2026-07-07）、侍着手**。**増分1本実装完了（2026-07-07、de9543c）**：モーダル廃止→同一Window内オーバーレイ・TryPlaceElementをOK/キャンセルハンドラへ分離・ClosePlacementBarでフォーカス復帰一箇所集約・バー表示中はWindow_PreviewKeyDown冒頭でF5-F8等全無効化（殿裁定どおり）。位置・レイアウトは現行据え置き（増分2の受け持ち）。build/test成功（App 12/Core 12）。→隠密静的レビューへ（2026-07-07采配、観点5点=無効化網羅性・マウス経路整合・原子的取消・フォーカス集約徹底・Esc4層整合・UIA到達性）。**隠密レビュー完了（2026-07-07、`docs/archive/ecad2-t033-review-onmitsu.md`）＝要修正（重大）**：バー表示中ガードがキーボード経路のみで、マウス経路6系統（キャンバス/部品リスト/DRC出力行/シートナビ/選択ツール/新規・開く）が素通し（code-review high併用、10件全CONFIRMED）。最重要=DRC行クリック→占有セルへジャンプ→OKで同一Posに要素二重生成の実データ不整合。フォーカス集約（観点3）自体は良好だが初期表示のFocus()が同期呼び出し（Loaded/BeginInvoke推奨、Microsoft Learn一次情報）。UIA到達性（観点5）は問題なし。恒久対応=個別ガード後追い（T-021の轍）でなくメイン作業域全体の無効化を推奨→**殿裁定（2026-07-07）＝グレーアウト**（IsEnabled標準無効化、T-019濃紺時のボタングレーアウトと同系の見せ方）。**侍へ修正采配（往復1周目、2026-07-07）**：全体無効化＋初期フォーカスタイミング修正を同梱、バー表示状態のViewModel単一情報源化は推奨（侍裁量）。範囲外の気づき（Sheet.Elements重複Pos防止機構なし=深層防御欠如）はP-021へ記録。**往復1周目修正完了（2026-07-07、4d73864）**：IsPlacementBarVisible（単一情報源）新設＋Menu/ToolBarTray/メイン作業域Grid/OutputPanelAreaのIsEnabled一括バインドで6系統を恒久遮断（個別ガードなし）・初期フォーカスをBeginInvoke(DispatcherPriority.Loaded)化・_placementIsOr nullable化＋ClosePlacementBarリセット。build/test成功（App 12/Core 12）。→隠密再レビューへ（2026-07-07采配。追加観点=バー自身のIsEnabled継承巻き込み・T-019案B HasProject連動グレーアウトとの共存・キー経路早期リターンとの併存整合）。**隠密再レビュー=クリーン（2026-07-07、`docs/archive/ecad2-t033-review-onmitsu-2.md`）**：6系統すべて無効化ツリー配下・バーは兄弟要素ゆえ巻き込みなし・HasProject連動とはIsEnabled AND合成で競合なく共存（「HasProject=false時もバーが開ける」候補はREFUTEDでT-019既存ガードが防止済み）・Visibility直操作の残骸なし・BeginInvoke化妥当。軽微PLAUSIBLE2件（フォーカス遅延中Tab競合・編集中再入）はobservations #13/#14へ、4箇所同一IsEnabledバインドのラッパーGrid一元化（CONFIRMED）は**増分2以降の改善事項として侍が拾う**。→忍者実機確認へ（2026-07-07采配）。**殿目視の指摘2点（2026-07-07、忍者検証中の画面を直接目視）**：(1)バーが横長レイアウトになっていない (2)表示位置が選択セルの右下で真下ではない——いずれも増分2（小型横長化・カーソル直下表示）のスコープだが、侍の増分1報告「位置は現行中央のまま据え置き」と(2)が食い違う疑いがあるため、位置の実挙動の検分を忍者観点へ追加（据え置きから外れていれば増分1の範囲内欠陥）。**忍者実機確認完了（2026-07-07、`docs-notes/ecad2-t033-inc1-verification-ninja.md`＋スクショ6枚）＝全観点OK・NGなし、増分1クローズ**：グレーアウト画素差分確認・マウス経路6系統すべて物理クリックで遮断実証（旧二重生成経路は完全封殺）・SendKeys/UIA Invoke到達性OK（PoC積み残し解消）・三経路対称/原子的取消/フォーカス復帰OK・T-019濃紺回帰OK・モーダル非ネストOK・隠密申し送り2点は実害なし。**位置の白黒＝作業域中央に完全固定**（セル2箇所+ズーム150%で実座標不変）＝侍の「中央据え置き」宣言と一致、増分1範囲内欠陥にあらず（殿目視の「セル右下」は初回セルとの偶然の相対位置）。範囲外検出2件はP-022（行0/列-1選択でバーが開く）・P-023（リスト選択ハイライト残らず）へ。忍者の気づき=グレーアウトの作業域自体の見た目変化は乏しい→増分2のUX検討材料。**増分2を侍へ采配（2026-07-07）**：カーソル直下表示・小型横長化＋横並び（構想図）・見切れ修正・画面端クランプ・IsEnabledバインドのラッパーGrid一元化。UI/UX分岐（グレーアウト見た目強化の要否等）が出たら殿へ。**[小アイコン類]の殿裁定（2026-07-07）＝見た目だけ置く**（押下無反応のプレースホルダ、増分3の拡張表示ボタンと同扱い。侍の省略案は差し戻し、構想図と見た目完全一致の御意向）。**増分2実装完了（2026-07-07、8fd60f6・侍がpush済み・家老が混入なし確認）**：(1)セル直下表示（PositionPlacementBar、TranslatePointでズーム/スクロール吸収） (2)小型横長化＋種別/機器名横並び＋小アイコンプレースホルダ (3)画面端クランプ (4)IsEnabledバインドをMainContentAreaラッパーGridへ一元化。build 0/test 24件合格。補：旧レイアウトのOK/キャンセル見切れは忍者の軽再現でも不再現（現行バーはAutoサイズ、旧モーダルの固定Width320由来と推定・増分2の横長化で構造的に解消見込み）。→隠密静的レビューへ（2026-07-07采配、`docs/archive/ecad2-t033-review-onmitsu-3.md`予定）。**殿目視の指摘（2026-07-07、増分2ビルド直接確認）＝配置バーが選択セルの上側に表示される事象**（要件=真下）。**殿スクショ2枚で条件判明**（`docs-notes/images/t033-inc2-bar-above-{row1-toolbar-overlap,row6-midgrid}-tono.png`）：行6=盤面中ほどでも約2行上に表示→下端クランプ回り込みではなく**Y計算誤りの筋が濃厚**（セル上端−バー高さ相当の位置）、行1では作業域を突き抜けツールバー直下に重なる→**上方向クランプ未実装の疑い**。隠密レビュー観点へ追加済み、CONFIRMEDなら増分2の範囲内欠陥（要修正）。**第三の症状（殿目視）：同一セルでバーを開き直すと毎回位置が変わる（非決定的）**→位置計算がバー寸法（ActualWidth/Height）をレイアウト確定前に読む疑い濃厚（初回0/以降は前回の古い寸法。増分1の「Visible直後Measure未完了」指摘と同族）。症状は「上側表示=系統誤差」＋「開き直しブレ=非決定性」の2層構造として隠密が切り分け中。**第四の証拠（殿スクショ）：行10=最下段セルでバーはキャンバス最上部に表示**（`docs-notes/images/t033-inc2-bar-top-when-cell-row10-tono.png`）。**隠密正式レビュー完了（2026-07-07、`docs/archive/ecad2-t033-review-onmitsu-3.md`）＝要修正・重大**：原点ズレCONFIRMED——MainContentAreaラッパー導入でルートGridのAuto行（メニュー/ツールバー/出力パネル）が実質0に潰れ（WPF Grid既知仕様：RowSpanでStar行を跨ぐ要素はAuto行の寸法計算から除外）、バー親セル原点とMainWorkAreaGrid原点が食い違うのに、PositionPlacementBarがMainWorkAreaGrid基準座標をMarginへ直流用（殿の症状1〜3と整合）。症状4（開き直し非決定性）は原点ズレでは説明不可・追加要因不明→実機診断ログ推奨（T-038運用、往復に数えない）。副次=プレースホルダ2ボタンのTab迷子（Focusable/IsTabStop未設定・修正容易）・クランプがセル自体を覆い隠す可能性ほかcode-review(high)計10件。P-024絡みの例外なし（クラッシュ説REFUTED・視覚異常のみ）。**次セッション冒頭：侍へ修正采配（往復1周目）→隠密再レビュー→忍者実機**。あわせて殿発見のP-024（負行への範囲外データ設置、詳細はproposed.md）も同時刻に検出——範囲外セル選択時のバー位置計算への波及も隠密が一瞥。**所見3（可視ラベル「種別:」「デバイス名:」削除）は殿裁定（2026-07-07）＝現状維持で容認**。**往復1周目修正完了（2026-07-07、bfa8994、家老push済み）**：(a)PositionPlacementBarのTranslatePoint変換先をMainWorkAreaGrid→ルートGrid（RootLayoutGrid新設）へ揃え、クランプ基準もworkAreaOrigin経由へ修正 (f)プレースホルダアイコン2個へIsTabStop=False付与（GridSplitter既存パターン踏襲）。build 0エラー・test 24件合格。**隠密再レビュー=クリーン（2026-07-07、`docs/archive/ecad2-t033-review-onmitsu-4.md`）**：(a)(f)とも解消確認・退行なし、Simplification代案は数学的同値でREFUTED（現状コード維持で良し）。新規所見2件=Margin計算のRow0/Row1=0依存（潜在負債、observations #15・P-025候補）／右母線列の余白矩形問題（範囲外・残存、忍者確認へ申し送り）。→忍者実機確認へ（2026-07-07采配：位置再検証=行1/6/10・開き直し複数回（症状4再発なら報告のみで診断ログ路線）・ズーム150%・クランプ、殿注文3点スクショ、グレーアウト+Tab迷子解消、回帰スモーク）。**忍者実機確認完了（2026-07-07、`docs-notes/ecad2-t033-inc2-verification-ninja.md`＋証跡13枚）＝位置NG（重大）・他は全OK**：行1=セル直下OKだが行6=セル真上・行10=画面最上部（旧症状3再現）・行10開き直し3回で2パターンのブレ（症状4再現）・**ズーム150%では行6/7ですら最上部＝100%より悪化**。隠密onmitsu-4「解消済み」と実機の食い違い発生。殿注文3点の見た目・グレーアウト+Tab迷子解消・回帰スモークはOK。右母線列は未検証（キーボード列移動が機能せず見送り、不明として申告）。**家老采配（2026-07-07）＝診断ログ路線（T-038運用、往復に数えず）＋Wチェック並行**：侍へPositionPlacementBar実行時の実測ログ注入（TranslatePoint入出力/workAreaOrigin/ActualWidth・Height/バーDesiredSize/スクロールオフセット/クランプ前後xy）、隠密へ独立理論調査（着眼=ズーム依存悪化・DesiredSizeレイアウト前読み・自レビュー前提の検分）を同時采配。ログ実測→理論と突合で原因確定後に修正へ。**真因確定（2026-07-07、三者完全一致）**：診断ログ注入0abd0fe+IsArrangeValid等追加3fdc185→忍者一次パス実測（`docs-notes/ecad2-t033-diag-pass1-{ninja.log,analysis-ninja.md}`）＝translateOut完全安定・ブレの発生源はbarDesiredSize単独・クランプはActualSizeでなくDesiredSize使用→侍診断（`docs-notes/ecad2-t033-diag-pass1-diagnosis-samurai.md`）と隠密理論（8/8数値検算）と家老検算が**完全一致＝ElementPlacementBar.Marginが前回値のまま残留し、WPF仕様（DesiredSize=content+Margin）で前回位置が今回の測定を汚染する自己参照フィードバックループ**（行10の257.57⇔359.47往復はlimit cycle、非決定に見えたブレは決定論的振動。隠密の当初仮説TranslatePoint過渡値説は実測で否定→隠密が率直に自己訂正）。**往復2周目修正（2026-07-07、5c73b66）**：Measure直前のMargin=Thickness(0)リセット1行のみ→**隠密再レビュー=クリーン（`docs/archive/ecad2-t033-review-onmitsu-5.md`、実機実構造前提の実測で間接汚染説REFUTED、Margin二重役割の将来リスクはobservations #16へ）**→**忍者再検証=全観点OK（2026-07-07、`docs-notes/ecad2-t033-inc2-verify2-ninja.md`+診断ログ数値証明）**：行1/6/10全てbarDesiredSize=barActualSize=(540,38)完全一致・行10開き直し3回全数値一致（症状4解消）・150%行6=セル直下/行7=正当クランプ・目視全ケースセル直下・軽回帰OK。**位置バグ・非決定性とも解消、殿御目視OK（2026-07-07）**→診断ログ除去41177dd（削除のみ-30行、本修正維持、build/test合格）→**忍者クリーン軽スモーク全観点OK（2026-07-07、証跡images/t033-final-*-ninja.png 3枚）**：起動・行1/行10セル直下・配置確定→機器表反映・diag.log新規生成なし（除去実証）。**増分2クローズ（2026-07-07）**。→増分3（拡張表示ボタン配置のみ、押下時の詳細画面はT-028のまま別タスク）を侍へ采配（2026-07-07）。**増分3実装完了（2026-07-07、c8930f2）**：拡張表示ボタンをキャンセル右へ配置（Clickハンドラ未配線=押下無反応・UIA名正式名称・Tab順は侍裁量で維持=T-028で実機能を持つ予定の本物のボタンゆえプレースホルダと別扱い、理由コミット/XAMLコメント明記）→隠密レビューへ（2026-07-07采配、onmitsu-6予定）。**殿の新注文（2026-07-07）＝増分4**：配置バーの種別選択ComboBoxを「a接点」等の文字表記からシンボルのみ（1グリフ、T-040 GX様式と同系）表示へ変更・省スペース化（閉状態・ドロップダウン項目とも。ToolTip/UIA名は正式名称維持。新規注文ゆえ往復に数えず）→侍へ采配（2026-07-07）。**増分3隠密レビュー完了（2026-07-07、`docs/archive/ecad2-t033-review-onmitsu-6.md`）＝要修正なし・Tab順のみUI/UX分岐で殿確認推奨**：並び順一致・増分1設計堅持・便乗変更なしOK。Tab維持（侍裁量）は実機検証で「フォーカス中Enter→OK確定不発火」の実害CONFIRMED（軽微・Shift+Tab回避可）→**殿裁定（2026-07-07）＝当面Tab除外**（IsTabStop=False、T-028で機能実装時にTab復帰。殿裁定ゆえ往復に数えず、増分4作業に同梱可）。軽微PLAUSIBLE（UIA名が「未実装」と伝わらない誤診断リスク）は忍者へ申し送り。**Tab除外77c2f81＋増分4実装025e8d6完了（2026-07-07）**：PartEntryToGlyphGeometryConverter新設（Id定数参照・既知5種グリフ＋汎用フォールバック、Core層はId定数化のみ値不変）・セレクトSWグリフ新規起草・ComboBox幅90→40（見切れ懸念も構造解消）→**隠密レビュー=クリーン（`docs/archive/ecad2-t033-review-onmitsu-7.md`、ConvertBack例外説は実測REFUTED、グリフ二重ハードコードはobservations #17へ）**→**忍者最終実機確認=全観点OK（2026-07-07、`docs-notes/ecad2-t033-final-verification-ninja.md`＋証跡7枚）**：拡張表示ボタン配置/押下無反応/Tab循環スキップ・シンボルのみ表示（閉/展開）・セレクトSW切替→配置→機器表反映・行1/2/10バー位置回帰なし。**実装・検証すべて完了→殿目視（2026-07-07）で新注文**：ドロップダウンにORa/ORbが無い（現行はOR属性が入口=sF5/sF6・リストORa/ORb項目でのみ決まる別フラグ設計ゆえ、ORa経由でもドロップダウンは「a接点」表示＝表示と実態の食い違い）→**殿裁定（2026-07-07）＝増分5：7種に統一し「表示どおりの動作」へ**——ドロップダウンを部品選択リストと同じ7種（ORa/ORbグリフ=sF5/sF6流用）にし、選択項目がOR属性も決める（ORa/ORb選択=isOr有効、a/b接点選択=isOr無効、入口がsF5/ORaなら初期値もORa表示）。**T-037裁定の暗黙保持ルール（接点系同士の切替でisOr保持）は本裁定で廃止・改定**。侍へ采配（2026-07-07、新規注文ゆえ往復に数えず）。**増分5実装完了（2026-07-07、d089f88）**：ItemsSource=SelectionEntries7種（リストと同一並び）・ORa/ORbグリフ=sF5/sF6流用・初期選択=Id+IsOr一致優先・effectiveIsOr=選択項目のIsOr直参照・_placementIsOr削除→**隠密レビュー=クリーン（`docs/archive/ecad2-t033-review-onmitsu-8.md`、暗黙保持廃止の完全性を入念検証・新設計の方が構造的に堅牢と評価。初期選択フォールバックの理論リスクはobservations #18へ）**→**忍者実機確認=全観点OK（2026-07-07、`docs-notes/ecad2-t033-inc5-verification-ninja.md`＋証跡9枚）**：7種表示・sF5→ORa/F5→a接点の初期値・ORa配置=OR縦コネクタ+機器表・ORa→b接点=ORなし（旧ルール廃止実証）・ORa→ORb=ORのb接点・コイル切替=OR自然無効・回帰なし。リスト単体クリック新規起動のみ今回再現できず不明申告（バー表示中切替は正常、実害低の見立て）。**殿最終目視OK（2026-07-07）＝セレクトSWグリフ構図・7種ドロップダウンとも承認、T-033完全Done** |

| T-034 | App層（ViewModel）テストプロジェクト新設＋回帰テスト追加 | Done | 侍 | REQ-01 | gated | P-008由来（殿承認2026-07-05）。tests/にEcad2.App層のテストを新設し、Dirty追跡（MarkDirty呼び忘れ検出）・HasProject切替等の最低限の回帰テストを追加。背景：明示MarkDirty方式の呼び忘れリスク（実装中に通知漏れを2度踏んだ実績）。ViewModelのテスト容易性（Window依存の分離）確認込み。**侍実装完了（ef787b5、2026-07-06）**：tests/Ecad2.App.Tests新設（net8.0-windows+UseWPF）、MainWindowViewModelのDirty追跡（配置/削除/デバイス名変更/保存/読込）・HasProject切替の回帰テスト計8件。build 0警告0エラー・test 11件合格（既存3+新規8）。Window依存分離の確認結果：MainWindowViewModel本体・DeviceTable/OutputPanel/PartPalette各子VMはWindow非依存でテスト可、SheetNavigationViewModelのAdd/RenameCommandのみApplication.Current.Dispatcher直接依存でテスト不能→P-016へ起票（改修は範囲外）。**隠密静的レビュー完了（2026-07-06、`docs/archive/ecad2-t034-review-onmitsu.md`）**：要修正級なし・土台健全。往復1周目采配（テストファイルのみ）：(1)DeleteCommand_MarksDirtyのコメント/実装食い違い (2)最後の1枚削除不可CanExecuteテスト追加 (3)Add/RenameCommandのMarkDirty検証（try/catchによるNRE隔離で検証可と隠密実証、P-016根本解消までの暫定）。PLAUSIBLE2件（ResetDirtyViaSaveのファイルI/O・LoadFromFile内容未検証）は経過観察。CLAUDE.md担当パス記載漏れは家老是正済み。**往復1周目（5b3cfcd、テストファイルのみ）→隠密再レビュークリーン（追跡3点とも意図通り解消、App.Tests単体12件・計15件合格）→忍者軽量スモーク（build 0/0・起動・シート追加/削除OK。要素配置確認は物理クリック無反応の環境異常で一旦打ち切り、テストのみの変更ゆえ検証十分と家老裁定→その後、異常の原因は殿PC上のマウスツール競合と判明・殿がツール終了で完全復旧し、要素配置→機器表反映も正常完了を追認＝**全項目OK**）→完全Done（2026-07-06）**。教訓：4役共有作業ツリーでは忍者の検証buildが侍の未コミット編集を巻き込みうる→以後、検証build前に`git status`でsrc/未コミット変更を検める申し合わせ |
| T-035 | .gcadpart読込時のID重複検出＋再採番 | Done | 侍 | REQ-01 | gated | P-009由来（殿承認2026-07-05：読込時に重複検出+再採番の方式）。PartDefinition.Idがファイルコピーで重複したまま残り後勝ち上書きされる問題への対処。データ形式・読込仕様（PartLibrarySerializer.LoadOne周辺）に触れるため設計注意。**侍実装完了（fd032bb、2026-07-06）**：PartFolderStore.Enumerate＝パス順ソート（列挙順のOS依存を決定論化）+HashSet重複検出+先勝ち再採番（コピーは「- コピー」等で名前が延びるためオリジナル先行の経験則）+SaveOne書き戻し（1ファイル単位try/catch例外隔離、失敗時はメモリ内のみ適用・次回起動で再検出され得る旨コメント明記）、戻り値をPartEnumerationResult(Entries, ReassignedCount)へ変更。PartPaletteViewModelでReassignedCount>0時にTraceLog.LogPartIdReassigned（新設）で記録（Core→App逆参照を回避した経路）。tests新規3件（重複検出/非重複/読取専用の例外隔離）。build 0/0・test 18件合格（Core 3→6・App 12）。**隠密静的レビュー完了（2026-07-06、`docs/archive/ecad2-t035-review-onmitsu.md`）＝要修正級CONFIRMED**：先勝ち基準（パス辞書順）がWindowsコピー命名「元 - コピー.gcadpart」と衝突——空白(U+0020)<ピリオド(U+002E)ゆえコピーが辞書順で先に来て**オリジナル側が誤って再採番される**（隠密実機確認済み、basic-*固定IDも喪失しうる）。軽微2件=TraceLogが件数のみで事後調査困難・Id null/空文字列の処理漏れ（ArgumentNullException可能性）。**往復1周目采配**：先勝ち基準をCreationTime最古優先（同時刻タイはパス辞書順。コピーはCreationTime新規・LastWriteTime引き継ぎのWindows挙動に基づく、サフィックス照合はロケール依存ゆえ不採用）へ差し替え＋null/空Id即再採番＋TraceLog詳細化（ファイル名・旧新Id・書き戻し成否）、「元 - コピー」実命名テスト必須。範囲外の気づき（PartResolver無警告フォールバック）はP-017へ起票。**往復1周目修正完了（893a7f9、2026-07-06）**：3点対応＋「部品 - コピー.gcadpart」実命名でのオリジナルId維持実証・CreationTime/パス順逆転・null/空Id再採番のテスト追加、build 0/0・test 20件合格（Core 6→8・App 12）。**隠密再レビュー=クリーン（2026-07-06、`docs/archive/ecad2-t035-review-2-onmitsu.md`）**：3点とも正しく解消・退行なし、「元 - コピー」実命名でのCreationTime逆転解消を実機確認、往復1周で仕留め。残存リスクPLAUSIBLE2件（robocopy等タイムスタンプ保持複製・OneDriveのCreationTime書き換え）は経過観察=observations.md #10/#11。REFUTED2件（GetCreationTimeUtc例外説は.NET仕様=1601年既定値返却で否定等）。**忍者実機確認（2026-07-06）：観点1（Id維持・再採番・書き戻し）観点2（TraceLog詳細記録）OK、観点3で要注意挙動を検出**——再採番パーツ（新Guid）のみ部品選択リスト経由の配置が「配置するセルを先に選択してください」のまま完了しない（オリジナルは毎回正常、再現不完全、1回のみダイアログが開くも機器表未反映）。観点4・5は中断。**Wチェック並行方式を発動→突合完了（2026-07-06）：T-035の欠陥にあらずと確定**——侍の診断ログ実測（クリック瞬間にTryPlaceElement即実行→SelectedCell無しabort・再試行なし。オリジナルbasic-*でもクリーン状態から同一再現）と隠密の独立理論（リスト経路はTool.PartId不設定の経路差説）が完全一致。既存設計欠陥としてP-018起票（往復2周目は不要）→**最終決着（2026-07-06）：忍者の再検証で「正しい手順（セル選択→リストクリック）ならオリジナル・再採番とも機器表反映まで完全動作」を診断ログ実測で確認、当初の現象は操作手順の逆順による誤観測と確定（侍のログ精査が決め手）。P-018はwithdrawn**。実装バグなし・修正往復も1周のみで確定。**忍者実機確認完遂・完全Done（2026-07-06）**：観点4=非重複時の無変更（ハッシュ・更新時刻とも不変・書き戻し不発生）OK／観点5=既存フロー回帰スモーク（配置・機器表・シート追加/削除）OK。診断ログ撤去後のクリーン状態（893a7f9相当）で検証締め |
| T-036 | 配置時の機器表即時反映＋デバイス名編集の一連修正 | Done | 侍 | REQ-01 | gated | P-011由来（殿承認2026-07-05：修正する）。**完全Done（2026-07-05、殿実操作確認済み）**。(1)配置時反映（26ab231）：全配置経路でdeviceName非空なら機器表へ即時登録（既存経路と同一ポリシー、非上書き）、忍者6観点中5観点OK。(2)検証で発覚したデバイス名編集不能バグの一連修正：UpdateSourceTrigger=LostFocusがFocusScope跨ぎで不発火する構造問題（T-017由来の潜在バグ、診断ログ+殿実操作+隠密理論のWチェックで確定）→Explicit化+LostKeyboardFocus/EnterのUpdateSource明示実行（ab7cf7e）→Esc=入力破棄の殿裁定（529092b）→隠密指摘2件=Esc独立層消費・Enter時再描画（2a06a1a、往復1周）→空文字確定時の機器表孤立残存をDelete同一ポリシーで解消（f985490、往復2周=上限で仕留め）。忍者再確認全OK（修正後はUIA操作でも3経路一発検証可=LostKeyboardFocus契機の副次効果）・殿実操作確認OK。教訓：T-038運用の初適用成功。残課題はP-013（編集中Ctrl+S等の未確定入力消失、独立課題）として起票済み。レビューdocs/archive/ecad2-t036-fix-review-onmitsu.md／検証記録docs-notes/ecad2-t036-*-ninja.md参照 |
| T-038 | 診断ログ連携運用の整備（UIA操作の限界時に診断ログ注入へ早期切替する連携フロー） | Done | 忍者(起草) | REQ-01 | auto-OK | 殿直接指示（2026-07-05）。背景：T-036観点6のデバイス名反映バグでUIA操作特有か実バグかの判別に難航、侍の診断ログ+殿実操作で確定した実例を受け、忍者が無理せず家老・侍と連携してログを仕込む運用を忍者自身が起草する。項目：切替判断基準／連携手順（申告→ログ注入采配→操作者使い分け→回収解析分担）／ログ標準形（%TEMP%\ecad2-diag.log方式）。起草→殿承認→ninja.md等へ反映の順 |
| T-039 | 操作トレースログ基盤（検証・調査時に「ログモードで事前起動」して操作全体を記録） | Done | 侍(実装)／隠密(レビュー)／忍者(実機確認) | REQ-01 | gated | 殿指示（2026-07-05）。常時ONではなく起動時スイッチ（コマンドライン引数/環境変数）でONにする方式。隠密が実装方式2案を比較検討済み（`docs/archive/ecad2-t039-design-comparison-onmitsu.md`。比較軸=忍者現場意見`docs-notes/ecad2-t039-trace-log-field-feedback-ninja.md`への適合度）。**殿裁定（2026-07-05）：案B採用**——隠密推奨形＝(a)ViewModelBase.OnPropertyChangedへの一括フック（setter5パターン混在でも全プロパティ変更を捕捉）＋(b)EventManager.RegisterClassHandlerでGotKeyboardFocus/LostKeyboardFocus横断捕捉＋(c)ButtonBase.ClickEventクラスハンドラ。**旧値記録（old=/new=形式）は侍実装時判断に委任**（SetProperty側での旧値捕捉等、安く実現できるなら入れる）。非機能要件：既定OFF+フラグ起動・セッション区切り必須・高頻度イベント除外・key=value形式・%TEMP%置き場。T-038（深掘り用の追加ログ注入）とは補完共存。**侍実装完了（562a0ad、2026-07-05）**：TraceLog.cs新設（%TEMP%\ecad2-trace.log、key=value、既定OFF、--trace-log/ECAD2_TRACE_LOG、セッション区切り、CanvasScale除外）+ViewModelBase一括フック（SetProperty経由の旧値捕捉）+App.xaml.csクラスハンドラ3種。ビルド0警告0エラー・既存テスト3件合格・侍スモークOK（有効時記録良好／OFF時ファイル無生成）。**隠密静的レビュー完了（同日、要修正）**：全12件は`docs/archive/ecad2-t039-implementation-review-onmitsu.md`参照。[CRITICAL]TraceLog.Write/リフレクション取得の例外処理皆無×クラスハンドラは本来処理より先に実行されるWPF仕様→ログ書込失敗がPropertyChanged・T-036修正・Click実行を丸ごとスキップさせる／[HIGH]初期化がDispatcherUnhandledException購読前／[MEDIUM-HIGH]カスタムsetter系（SelectedElementDeviceName/Tool等）の旧値が常にnull／[MEDIUM]環境変数判定`!= "0"`。隠密推奨=TraceLog内部try/catch隔離でCRITICAL/HIGH大半解消見込み。ログ名ecad2-trace.logは妥当（隠密所見）。**セッション終了（Usage都合・殿裁定2026-07-05）により侍修正→隠密再レビュー→忍者実機確認は次回へ持ち越し**。**修正往復2周完了（2026-07-06）**：1周目=12件中8件修正（ad6b1fd、例外安全性・旧値捕捉ほか）→隠密再レビューCONFIRMED4件（`docs/archive/ecad2-t039-re-review-onmitsu.md`）→2周目=4件修正（beeabc2）→**隠密再々レビュー=クリーン**（4件全解消・退行なし・3周目不要、`docs/archive/ecad2-t039-re-review-2-onmitsu.md`）。範囲外の気づき2件（全角ラテン文字未正規化・兄弟VM2ファイルの旧値null化）はP-014/P-015へ記録済み。**忍者実機確認完了・完全Done（2026-07-06）**：観点1=起動スイッチ2系統OK／観点2=フォーカス遷移・Binding旧値・Click・ツール切替の記録OK（カスタムsetter旧値も"Y002"→"Y099"と正確記録を実機確認）／観点3=既定OFF時の無生成・無副作用OK／観点4=複数インスタンス相当（ログファイル完全排他ロック下）でもClick・PropertyChanged・UI更新は正常完了し該当ログ行のみ静かに欠落＝旧CRITICAL（ログ書込失敗が本来処理を巻き込む）の解消を直接実証。回帰なし。検証記録`docs-notes/ecad2-t039-final-verification-ninja.md` |
| T-040 | ツールバー配置系ボタン群のGX様式化（各ボタン=ラダー記号グリフ＋キー凡例「F5/sF5」表記のコンパクトなボタン列へ統一） | Done | 侍 | REQ-01 | gated | 殿裁定（2026-07-06）：殿提示のUI/UXサンプル（GX系ラダーツールバー、`docs/images/t040-gx-ladder-toolbar-reference.png`に保全）の様式へ、OR系に限らず配置系ボタン全体（F5〜F8・Shift+F5/F6・自作パーツ等）を統一する。「Shift+F5」表記は凡例「sF5」の短縮形も本様式に含む。家老の段取り：T-037（案A、ツールバー無変更）を先に小さく仕留め、本タスクをT-037直後・T-033の前に実施（スコープ分離で往復レビューを軽く保つ）。本格的な意匠制作（T-013）とは別——本タスクは様式・レイアウト・凡例表記の統一、意匠の作り込みはT-013で継続。**殿裁定3点（2026-07-06、侍の下調べ3問への回答）**：(a)対象=**2段目（配置系）限定**（1段目ファイル操作系は現状維持） (b)並び順=**GX画像踏襲**（F5→sF5→F6→sF6→F7→F8、基本キーの直後にそのShift版） (c)サイズ=**中間のややコンパクト**（36x36px前後、細部は侍の調整に委ねる）。表記は「ShF5」→「sF5」へ。侍の下調べ所見：2段目は既にアイコン＋キー凡例の骨格あり（T-012由来ToolBarButtonStyle）。**表記の追加裁定（2026-07-06、侍案採用）**：ツールチップ・UIA名（AutomationProperties.Name）は正式名称「Shift+F5」を維持、ボタン上の凡例のみ「sF5」へ短縮（忍者のUIA検証互換への配慮）。**着手順の更新（殿裁定2026-07-06）**：T-042（テスト実環境副作用解消）を本タスクの前に差し込む→T-037完了→T-042→T-040→T-033。**侍実装1回目（8d10684、2026-07-06）**：並び順・36x36/Icon14/Label8・sF5表記・ToolTip/UIA名正式維持、共有スタイル直接変更は1段目へ波及するため2段目専用スタイル（PlacementToolBarButtonStyle等、BasedOn継承）新設で1段目無変更を担保。build 0/0・test 24件。**隠密レビュー=クリーン（2026-07-06）**：3観点CONFIRMED（裁定4点一致・1段目非波及・ボタン移動の取り違えなし）、視認性懸念（低確信）は経過観察。**殿の追加注文（2026-07-06、見た目直接確認）**：サイズOK・**アイコン（グリフ）はサンプル（`docs/images/t040-gx-ladder-toolbar-reference.png`）を参照して作り直し**——F5=開接点・sF5=並列開接点等の記号様式をGXへ寄せる。サンプルに対応物のないボタン（Esc選択・自作パーツ等）は様式を揃えて侍起草→忍者スクリーンショット→殿目視で確定。新規注文ゆえ修正往復に数えず。**忍者動作確認（2026-07-06）全OK**：ボタン7種クリック・実キー送信（F5〜F8/Shift+F5/F6）とも取り違えなし・UIA名正式名称維持・F5配置→機器表反映の回帰スモークOK（見た目観点は殿直接目視に代替）。**グリフ範囲の殿裁定（2026-07-06）＝全ボタン見直し**：侍精査（sF5/sF6=左端分岐線の新デザイン・F7=真円→括弧型コイル、F5/F6はサンプル概ね一致）に対し、F5/F6/F8/Esc/部品も含め線の太さ・密度からサンプルの風合いへ全面統一（隠密の視認性懸念への手当て兼務）。サンプル対応物なしのボタンは新様式に揃えて侍起草。**侍実装2回目（52e064d、2026-07-06）**：sF5/sF6=左独立分岐線・F7=括弧型・StrokeThickness=1.0統一・他は線幅調整のみ、自己レンダリング確認済み。**殿の意匠直接指示（2026-07-06、逐次）**：(1)sF5/sF6は「└┤├┘」構図へ＝接点の左右リード線両端が上へ立ち上がる対称形（左独立分岐線案は破棄） (2)F7コイルは括弧型を取りやめ**ただの丸（真円）へ戻す** (3)F8端子台は「-Φ-」構図（リード線＋円＋縦貫通線）で代用。いずれも殿の新規意匠注文ゆえ修正往復に数えず。構図裁定は殿直轄となったため、隠密レビューは技術面（線幅統一・Path Data整合・1段目非波及・回帰）に絞る整理。**殿不在中の進行方針（2026-07-06）**：差し替え実装→隠密→忍者スクリーンショット採取まで進め、最終の殿目視確定のみ帰還待ちで保留。**侍実装3回目（8a3ad4a、2026-07-06）**：4箇所一括差し替え（sF5/sF6=└┤├┘・F7=真円半径5へ拡大・F8=-Φ-）＋境界ボックス検算で全ボタン統一（隠密申し送りのStretch=Uniformスケール差へ対応）。build 0/0・test 24件。**隠密再レビュー=クリーン（2026-07-06）**：4観点CONFIRMED（Path Data構文・境界ボックス手計算検算一致・1段目非波及・ロジック無変更＋独立build 0/0）。**忍者スクリーンショット採取済み（2026-07-06）**：`docs-notes/images/t040-design-fullwindow-ninja.png`（全体）・`t040-design-toolbar-zoom-ninja.png`（2段目5倍拡大）、起動時描画異常なし・家老一次目視でも並び順/新意匠の反映・崩れなしを確認。**殿目視確定（2026-07-07、実機起動にて直接確認）：「OKよくできてる」＝新意匠（sF5/sF6=└┤├┘・F7=真円・F8=-Φ-）と全体統一を承認。T-040クローズ** |
| T-042 | App層テストの実環境副作用解消（PartFolderStoreの注入口追加＋テストの一時フォルダ化） | Approved | - | REQ-01 | gated | P-019由来（殿裁定2026-07-06：起票しT-040の前に差し込み実施）。根拠=隠密の診断調査（CONFIRMED）：App層テスト13件が`new MainWindowViewModel()`→`PartPaletteViewModel`→`PartFolderStore.CreateDefault()`の連鎖で`dotnet test`のたびに実MyDocuments（OneDrive実体）の図形フォルダへSeedBasics/Enumerate（補正＋書き戻し）を発火。今回は正しい方向への補正ゆえ実害なしだったが、テストが実ユーザーデータへ副作用を及ぼす構造欠陥（T-034時点から潜在、T-037の書き戻し追加で顕在化）。対処=PartPaletteViewModel/MainWindowViewModelにPartFolderStoreを注入できる口を設け、テストは一時フォルダを叩く構成へ（P-016のDispatcher注入と同系の分離設計だが、P-016自体は本タスクに含めない）。規模小〜中・触るファイル3〜4。**侍実装完了（075b4bc、2026-07-06）**：2本立てコンストラクタ（無引数=本番用CreateDefault委譲・引数版=テスト注入用）＋ViewModelTestBase新設（一時フォルダ+CreateViewModel()+IDisposable後片付け）＋13箇所置換。一時フォルダ仕組みはCore.Testsと意図的に独立（共有プロジェクト追加は過大の判断、コメント明記）。build 0/0・test 24件合格・テスト実行前後で実フォルダのタイムスタンプ不変を実測確認。**隠密レビュー=クリーン（往復ゼロ、2026-07-06）**：3観点とも実測CONFIRMED（本番経路不変・置換漏れゼロ＋独立にdotnet test前後で図形フォルダ全ファイルのタイムスタンプbit単位一致を再現・一時フォルダecad2-apptest-\*残存0件）。code-review併用の軽微3件（Dispose時try/catch無し・Core.Tests側とのパターン重複=意図的トレードオフ明記済み・store引数nullチェック無し）は実害なし、記録のみ。**忍者起動スモーク全OK（2026-07-06）**：部品リスト7種+サムネイル従来どおり・a接点配置→機器表即時反映・回帰なし。**T-042クローズ** |
| T-043 | ORa/ORbサムネイルのシンボル統一（部品選択リストのサムネイルをツールバーsF5/sF6と同じGX様式グリフへ） | Done | 侍 | REQ-01 | gated | P-026由来（殿直接要望+着手時期裁定2026-07-07：**T-033完了直後・T-041前に差し込み**）。現状はT-037実装の「通常接点シンボル＋右下ORバッジ合成」（PartThumbnailRenderer）でツールバー（T-040 GX様式=└┤├┘構図）と見た目不一致。規模小。T-033増分4（バー種別選択のシンボル化）とグリフ資産を共用できる見込み。**着手（2026-07-07、T-033完全Done直後に侍へ采配）**：グリフ資産はPartEntryToGlyphGeometryConverterのPath Data流用推奨・ORa/ORb以外の5種は現状維持・実装方式は技術裁量。**実装c56b13c→隠密レビューで新規退行CONFIRMED**（Id完全一致判定→コピー部品でORグリフ欠落）→**往復1周目de93462**（IsOrEligible/Roleベース化+テスト2件）→**再レビューで同族弱点がConverter側に残存とCONFIRMED**（コピー部品でコンボが汎用アイコン化、実害範囲広）→**往復2周目=上限e7e03c2**（Category/Role/IsOrEligibleベース+Category==""ゲート+テスト4件、計30件合格）→**隠密再々レビュー=クリーン**（軽微副作用2件はobservations #19/#20へ）→**忍者差分確認=全観点OK（2026-07-07、`docs-notes/ecad2-t043-verification-ninja.md`+スクショ2枚）**：ORa/ORb新グリフ・旧バッジ消滅・他5種維持・ComboBox回帰なし・OR配置動作OK。範囲外の気づき（縦リサイズ時の行1描画56pxオフセット）はP-027へ。**殿目視OK（2026-07-07）＝T-043完全Done** |
| T-044 | OR自動配線の冗長縦分岐抑止（左母線直結時は母線接続のみ） | 保留（T-041本命化に伴う。実装/レビュー/実機は全OK・電気的正常） | 侍(実装)／隠密(事前調査) | REQ-01 | gated | 殿直接要望（2026-07-07、T-033完了後の実機確認より）。**仕様（殿確認済み2026-07-07）**：(1)ORa/ORb回路追加時、**配置行で自ノードと左母線の間に何も無い場合**は左側の縦分岐（VerticalConnector）を生成せず母線接続のみとする（母線自体が縦導体ゆえ冗長） (2)2番目のノード（合流側）は左向きは前ノードへ・右側は上行の配線へ接続（従来どおり縦分岐維持） (3)既存保存データは補正しない（新規生成のみ変更、殿裁定）。留意=P-003（LeftTerminatorのTopRow/BottomRow区別：OR新規行は縦コネクタ経由でのみ母線接続の前提）との整合、NetlistBuilder接続性、シミュレーション影響。隠密の事前調査（OR自動生成の縦コネクタ作成箇所・左分岐省略時の描画/ネットリスト整合）→侍実装の順。着手順=T-043完了後・T-041前（家老提案）。**隠密事前調査完了（2026-07-07、`docs/ecad2-t044-presurvey-onmitsu.md`）**：P-003前提を壊すリスク低。重要発見=現状コードは列0の縦コネクタをLeftTerminator/LeftRailReached判定対象外（`c.Column>0`）としており、列0のOR新規行では「冗長縦コネクタ＋母線直結横線」の**二重接続**が発生——これが殿の見た冗長の正体。左コネクタ省略時は既存フォールバック（コネクタ無ければ直結線）に自然に乗り、母線から浮かず回路分断もなし。**適用範囲の殿最終裁定（2026-07-07）＝上下両行とも空きなら列途中も適用**——配置行と基準行（上の行）の両方でOR左接続点と左母線の間に要素が無い場合に左縦分岐を省略（トポロジー等価が保証されるケースのみ。上の行に接点がある場合は従来どおり縦分岐維持=バイパス誤結線の防止）。列0は自明なケースとして包含。**侍実装完了（2026-07-07）**：MainWindowViewModel.cs+14行＋OrWiringTests.cs新設83行（テスト計34件合格・build 0エラー、侍が現行mainで独立検証済み）。注=本実装はコミット0d3425a（家老のdocsコミット）に混入して載った——git index共有による巻き込み事故だが、侍が「差分ゼロ・意図した最新版と完全一致・ビルド可」を確認し、そのまま正とする裁定（改めての実装コミット不要）。教訓は家老メモリへ収蔵済み（コミットはパス限定形が鉄則）。→隠密レビューへ（2026-07-07采配）。**隠密再レビュー=クリーン（`docs/ecad2-t044-review-onmitsu-2.md`：Connectors条件`<=`必須の裏取り・WireBreak REFUTED）→忍者実機確認=全観点OK（`docs/ecad2-t044-ninja-verification.md`：左母線際二重接続解消・連鎖OR3階層・回帰）**。**殿がT-044実機確認中に接続不良を発見（右合流の縦線が下端接点に届かず断線して見える、`ss_20260707_134207.png`）→バグ対応Wチェック発動（忍者実機再現＋隠密独立調査を並行、`docs/ecad2-t044-disconnect-{repro-ninja,investigation-onmitsu}.md`）**：両系統一致で**d04c9a3は無罪、真因はT-026/P-003で意図的にスコープ外とした既存描画欠陥（DiagramRendererがBottomRowのみ見TopRowを見ない仕様）が連鎖ORで初めて可視化**。**忍者実測で電気的には正常＝描画のみと確定（WireNumber一致で同一ネット、隙間はズーム100%限定の見た目。忍者は当初「配置列<基準列で断線」と誤判定→高倍率で自己訂正）**。P-028を理論→実証へ格上げ。**殿裁定（2026-07-07）＝T-044は電気的正常につき一旦保留し、手動配線基盤T-041を本命化**（自動ロジック精緻化は袋小路、断線描画は手動基盤で対処。修正3択＝P-028リファクタ/即精密修正/独立タスク化は保留） |
| T-041 | 主回路用の横配線・縦分岐線の手動記入（GXツールバーF9=横線/sF9=縦線相当） | Approved | - | REQ-01 | gated | 殿要望＋裁定（2026-07-06）：(1)「横配線」は**主回路用の自由線**（制御回路グリッド内は自動配線があるため対象外）＝既存`FreeLine`モデル流用、縦分岐線は既存`VerticalConnector`流用 (2)T-040（様式統一）とは**スコープ分離**で別起票 (3)キー割当は**GX踏襲でF9/sF9を新設**（GuiEcadはパレット選択＋ドラッグのみでキー割当なし）——キー操作の詳細方式（カーソル位置からの記入等）は実装時に侍が案を起草し殿へ諮る（UI/UXゲート）。隠密の事前調査＝`docs/archive/ecad2-t040-wire-survey-onmitsu.md`：Core層データモデル・描画は移植済みで流用可能（OR自動生成が現用中）、欠けるはApp層のUI配線（ドラッグ→コマンド→キーバインド）のみで規模中程度。GuiEcadの`MainPage.Pointer.cs`ドラッグ確定ロジック（`AddConnectorCommand`/`PlaceFreeLineCommand`）が実装パターンの直接参考。着手順はT-040完了後→**殿裁定（2026-07-07）：T-033の後**。**殿裁定更新（2026-07-07）＝本命・前倒し＋「消去」スコープ追加**：殿がT-044自動配線の実機確認中「縦棒が消せない、ロジックだけでは処理できない、縦線・横線を記入・消去する方法を確立する必要がある」と発意（`ss_20260707_135241.png`）→手動配線基盤を本路線とし、当初の「記入」に加え**線の消去**を明確にスコープへ含める。T-044（OR自動配線）は電気的正常につき一旦保留し本タスクへ移行。段取り=隠密が現状調査（既存の線削除手段の有無・GuiEcad移植知見・Coreデータモデル適性）＋キー操作方式の設計案（複数併記）を起草→家老が殿へUI/UX裁可を諮る→侍実装。着手＝実質即時（T-033完了済み・T-044保留、`docs/archive/ecad2-t041-manual-wiring-survey-onmitsu.md`予定）。**操作方式=案A確定＋侍実装プラン起草完了（2026-07-07、`docs/archive/ecad2-t041-implementation-plan-samurai.md`）**：増分1(選択モデル+Delete統合、VerticalConnector限定)→増分2(VerticalConnector記入)→増分3(FreeLine幾何ヒットテストPoC)→増分4(FreeLine記入・消去)→増分5(修正機能)。不明点5点を殿へ諮った結果**（2026-07-07裁定）**：(1)F9/sF9対象はシート種別で自動切替 (2)選択は増分1では単一選択のみ (3)**「修正」はドラッグ等の移動・リサイズを新設**（侍仮案の「消去→再記入」代替ではなく本格実装、スコープ増） (4)**ConnectionDot・WireBreakも今回まとめて基盤化**（侍プランの「スコープ外」仮定を修正、対象は4種全て）。記入時の具体キー操作フロー（始点/終点指定方法）は侍が原案起草後に改めて殿へ諮る。→侍へプラン改訂を依頼（2026-07-07采配）。**改訂プラン提出完了（2026-07-07、`docs/archive/ecad2-t041-implementation-plan-samurai.md`）＝7増分**：増分1(選択モデル+Delete統合、VerticalConnector限定)→増分2(VerticalConnector記入)→増分3(WireBreak横展開)→増分4(FreeLine/ConnectionDot幾何ヒットテストPoC)→増分5(FreeLine/ConnectionDot記入・消去)→増分6(ドラッグUI基盤PoC、ecad2初のマウスキャプチャ実装ゆえ最大リスク)→増分7(移動・リサイズ本実装)。残る不明点2点のうち、**「修正」操作のドラッグ/キーボード併用方式は殿裁定（2026-07-07）＝案X（ハイブリッド：ドラッグ主手段＋矢印キー等のキーボード等価操作を併設、keyboard-first方針維持。実装量最大・侍推奨案）**。記入時のキー操作フロー具体形（1点）は侍が原案起草後に改めて殿確認。**増分1は殿裁定済み範囲内のため家老裁量で着手を許可（2026-07-07）**。**増分1完了（2026-07-07、592131c）**：縦コネクタ選択モデル新設(mm距離ヒットテスト、許容誤差2.0mm)+Delete統合(部品優先→配線プリミティブ)+選択ハイライト+Escape層3連動、build/test37件合格。→隠密レビューへ（2026-07-07采配）。**キー操作フロー原案提出（2026-07-07、`docs/archive/ecad2-t041-key-flow-proposal-samurai.md`）＝殿裁定確定**：(1)キー割当=**案A（線系F9/sF9＋点系専用キーF10に統一）**（当初の案B選択を殿が「F10統一でいいよ」と訂正、最終確定は案A） (2)FreeLineは**水平・垂直のみに限定**（斜め線はスコープ外）。線系フロー＝SelectedCellから開始→矢印キーでリアルタイムプレビュー調整→Enter確定/Esc取消。点系フロー＝F10押下と同時に即時記入（確認フェーズなし）。→侍は増分2(VerticalConnector記入)へ着手可。**増分1〜3・F10バグ修正まで完了（2026-07-07）**：増分1(選択モデル+Delete統合、592131c→修正1edf36c→隠密再レビュークリーン)・増分2(sF9手動記入、be9c15f→シート切替時の状態リーク修正f5cbde8→隠密再レビュークリーン。所見=増分1所見Aの状態分散懸念が別形で再発、setter集約パターンの延長で解消)・増分3(F10 WireBreak記入、9cc5b32→隠密レビュークリーン)。**忍者実機確認でF10重大バグ発見**（WPF標準仕様でF10単体キーがWM_SYSKEYDOWN扱い・メインメニューへフォーカスが奪われ記入不能→侍修正abddba3・隠密独立調査で原因一致確認→再検証OK）。**忍者最終確認（`docs/archive/ecad2-t041-increment123-ninja-verification.md`）＝全観点OK・回帰なし（T-044/T-017とも異常なし）**。**範囲外検出**：UIから主回路シート(`MainCircuit=true`)を作成する手段が現状無い（シート追加は常に制御回路シートのみ生成）。増分5(FreeLine/ConnectionDot、主回路対象)の実機検証・実用に影響するため、増分4/5着手前に対処要否を検討する必要あり。**増分4完了（2026-07-07、f11d478）**：FreeLine/ConnectionDot幾何ヒットテストPoC(poc/t041-freeline-hittest-poc/、点と線分・点と点の距離計算、9/9 PASS)。ズーム安全性はLayoutTransform適用前ローカル座標を返すWPF仕様により構造的に保証(増分1と同前提)と判明、UI付きPoCは不要と判断。HitTestFreeLineは「nearest-wins」方式を推奨（HitTestConnectorの「先頭一致」は据え置き）。**主回路シート作成UI不在の調査完了（`docs/archive/ecad2-t041-mainCircuit-sheet-creation-gap-samurai.md`）＝殿裁定（2026-07-07）＝案1（シート追加時に種別選択ダイアログ、既存RenameDialogと同型）**。**主回路シート作成ダイアログ完了（2026-07-07、fa66efd）**：AddSheetDialog新設(RenameDialogと同型、名前+種別ラジオボタン)、空名時は自動採番名にフォールバック(既存挙動保持)、`AddCommand`呼び出し規約変更(名前+主回路フラグのタプル引数)→隠密レビュークリーン(dotnet test 7/7、AddCommand呼び出し元1箇所のみで波及漏れなし)→忍者実機確認全観点OK(生成・空名フォールバック・キャンセル・ダイアログ見切れなし・生成シートが正しくMainCircuit=trueとしてsF9制限される、`docs/archive/ecad2-t041-addsheetdialog-ninja-verification.md`)。**主回路シート作成UI不在の課題は解消**。**増分5実装完了（2026-07-07、d6f1747）**：F9=横線/sF9=縦線(主回路シート限定、シート種別自動切替)・F10=接続点(制御回路WireBreakとキー共有)、ヒットテストは増分4PoCのnearest-wins方式採用、選択・記入中状態ともsetter集約パターンを踏襲。**隠密レビューでCONFIRMED重大所見**：`SheetNavigationViewModel.DeleteCommand`でシート削除後にindex数値がたまたま維持されるケースで`CurrentSheetIndex`のsetterの`SetProperty`早期returnにより全状態クリア・再描画が発火せず、記入中状態が「シート削除」経路からクロスリークしうる（増分1由来の構造的穴、`docs/archive/ecad2-t041-increment5-review-onmitsu.md`）。**修正往復1周目完了（2026-07-07、4264220）**：setterの早期returnの前にクリア処理を無条件実行する設計へ改修、regression proof実施(クリア処理を一時的に外してFAIL確認→復元)、68件全合格。**次回セッションへ引き継ぎ**：隠密の再レビュー未実施（本日終了）。クリーンなら忍者実機確認（増分5全体：F9/sF9/F10記入・選択・削除・シート削除時のクロスリーク再現確認）→増分6（ドラッグUI基盤PoC）・増分7（移動・リサイズ本実装、案X）へ |
| T-037 | 部品選択リストへORa/ORb追加（固定7種化）＋OR入力専用UI | In-progress | 侍 | REQ-01 | gated | P-010由来（殿裁定2026-07-05：案1採用）。ツールバーのOR系と同じ操作フラグ方式（PartId+IsOrの直交設計）を流用しCore層無変更で実装可能（隠密調査`docs/archive/ecad2-p010-or-fixed-parts-investigation-onmitsu.md`）。サムネイルがOR/非ORで同形になる課題は実装時に殿へ相談（名前表示で妥協 or 区別表示の追加実装）→**殿裁定更新（2026-07-06）：「OR入力は専用UIを作成する」——案1（リストへORa/ORb追加）は実施のうえ、加えてOR入力の専用UI（右パネル・ツールバーでの専用表記）を新設する。インラインバー（T-033浮動入力）でのOR表記は別途相談（今回スコープ外）**。段取り：侍が画面案3案を起草（`docs/archive/ecad2-t037-or-ui-proposals.md`）→**殿裁定（2026-07-06）：案A採用**——リストへORa/ORb追加（旧案1どおり）＋サムネイル右下に「OR」バッジを合成描画（PartThumbnailRenderer拡張）、ツールバーは変更なし。実装着手はT-035完了後。補：ツールバーの見せ方は殿のUI/UXサンプル提示によりT-040（配置系ボタン全体のGX様式化）として分離起票（2026-07-06）、本タスクではツールバーに触れない。**侍実装完了（8ebc05d、2026-07-06）**：PartSelectionEntryViewModel（IsOr/DisplayName）＋PartThumbnailRenderer（ORバッジ合成）＋PartPaletteViewModel（IsOr=trueの論理エントリ追加、Core層無変更）＋MainWindow.xaml/xaml.cs（isOr連動・DisplayNameバインド）。build 0/0・test 20件合格・回帰なし。**隠密静的レビュー完了（2026-07-06、`docs/archive/ecad2-t037-review-onmitsu.md`）＝要修正2件**：(1)ダイアログ内種別切替時にisOrがクリック時初期値のまま渡り不整合（ORa→端子台切替で縦コネクタ誤生成、手動+code-review独立で同一結論） (2)OR対象判定がName文字列一致で脆い（T-035のNameそのまま挙動と組むと重複表示リスク）→根本策=PartDefinition.Role（ContactNO/NC）判定へ置換（1行・Core層無変更）。PLAUSIBLE軽微1件（サムネイル生成重複）は任意。**往復1周目修正完了（159e2f0、2026-07-06）**：(1)ダイアログ確定時の最終パーツRole判定——接点系（ContactNO/NC）同士の切替はisOr保持・非接点系へはisOr無効化（理由=ORは接点同士の並列接続というトポロジ概念ゆえ接点間切替は保持が自然、非接点はOR無意味かつ誤動作源で安全側へ） (2)OR対象判定をRole判定へ置換（1行・Core層無変更）。PLAUSIBLE軽微は見送り。build 0/0・test 20件合格。**隠密再レビュー=クリーン（2026-07-06）**：2件とも意図通り解消（effectiveIsOr=isOr&&isContact、selectedEntry null時も安全側）・侍の設計判断も妥当判定・code-review(low)新規バグ候補なし、往復1周で仕留め。非接点切替時の無言isOr無効化はUX軽微=observations #12へ。**忍者実機確認は観点1のみで中断（2026-07-06、忍者使用量逼迫）**：観点1=ORa/ORb項目表示・ORバッジ右下表示OK。ただし**「ORセレクトSW」も出現**——セレクトSWはRole=ContactNO（`BasicPartTemplates.cs:112`、シミュレーション上「接点として扱う」設計）のためRole判定置換が正当に巻き込んだ形。**殿裁定（2026-07-06）：ORa/ORbのみに絞る、除外修正は次回冒頭**（修正すれば往復2周目=上限。コピー耐性を保つ除外方式は侍の技術裁量→隠密再レビューで妥当性確認）。観点2〜6（OR配置フロー・種別切替整合の実証・回帰）も次回へ→**忍者が使用量回復後に復帰し観点2〜6を完遂（2026-07-06夜、対象159e2f0）：全OK**——(2)OR縦コネクタ描画+機器表反映 (3)ORa→端子台切替でOR不生成 (4)ORa→b接点切替でOR保持 (5)非OR回帰 (6)回帰スモーク。物理クリック操作も完遂＝MouseAssistant競合は回復済み。**残作業はORセレクトSW除外修正（殿裁定、往復2周目）→隠密再レビュー→忍者の軽い差分確認（OR項目数=2・バッジ・OR配置スポット）のみ**。範囲外の気づき（行0の基準要素でシンボル未描画+ラベル誤表示）はP-012へ追記。**往復3周目（殿裁可2026-07-06=上限超過を承認、侍C案）**：2周目8341062は隠密再レビューで致命的懸念CONFIRMED（`docs/archive/ecad2-t037-review-onmitsu-2.md`）——SeedBasics非上書き仕様×旧版JSON（isOrEligibleキー無し）の組み合わせで実機のORa/ORbが0件化、テスト20件はインメモリ生成ゆえ検出不能。3周目8a6077a=Enumerate内でId重複チェックより前に固定Id（ContactNO/NC）基準の後方互換補正＋書き戻し（失敗時は例外隔離・次回起動時再補正）＋後方互換テスト4件追加（旧版JSON補正/セレクトSW非該当/読取専用時の安全継続/コピー耐性、test 24件合格）→隠密最終レビュークリーン（`docs/archive/ecad2-t037-review-onmitsu-3.md`、5観点）。補正主体の謎（実ファイル更新18:16）は隠密の診断調査で**テスト実行の実環境副作用**と特定（殿・侍・忍者の非起動証言と整合）→P-019→T-042起票。**忍者差分確認（2026-07-06）全観点OK**：OR項目=ORa/ORbの2件のみ（セレクトSW混入なし・0件でもなし）・ORバッジ右下表示・OR配置で黒丸接続ノード付き縦コネクタ自動生成＋機器表反映・回帰なし（`docs-notes/ecad2-t037-final-verification-ninja.md`）。範囲外の気づき（機器表「種別」列が一律Other表示）はP-020へ。**T-037クローズ** |

<!-- 完了したタスクも消さず Done にして残す（履歴として活用） -->

---

## 追記（軽量化以降の完了分）

（タスク完了時、todo.mdの詳細をここへ移す）

### T-048 手動配線ボタン（sF9・F10系）のグリフ変更 — 完全Done 2026-07-10

**起票=殿指示2026-07-09（T-047最終目視の所見）、意匠=2026-07-10殿提示**（`sample/F10_sample/4856.png`
+.aiベクター=はさみ線画、`sample/sF9_sample/icon_sample2.png`=下向き矢印、後に高解像度版
icon_sample2_127px.png追加提示）。

経緯（パイプライン全段）:
- 殿裁定4点（2026-07-10）: はさみ=F10系ベース／矢印=sF9系ベース／形状のみ採用・色は現行モノクロ統一／
  ベース+識別要素で描き分け+実装確定前プレビュー殿提示【MUST】
- 侍試作→**殿プレビュー承認制**（侍ウィンドウで直接提示、往復2回調整=サンプル忠実→分岐枝追加→
  枝を丸中心貫通・左右対称化）→「見た目はOK」で承認→コミット`0986c81`（MainWindow.xamlのみ、
  7行追加5行削除）
- 最終意匠: 制御シート=縦分岐線(sF9)が矢印形・配線分断(F10)がはさみ形／主回路シート=自由線縦線(sF9)の
  棒・接続点(F10)の●は既存意匠を殿指示で維持（diffなし）
- 隠密静的レビュー（軽量）: 問題なし（T-047半透明化Trigger・シート種別連動とも無傷、便乗変更なし、
  observations #17への影響は中立=手動配線グリフは3箇所ハードコードの共有対象外）
- 忍者実機回帰（`docs/archive/ecad2-t048-verification-ninja.md`+証跡8枚）: 4観点すべてOK（シート種別での
  活性/グレーアウト逆転・並び順不変・機能起動=矢印形でPlaceConnector起動+はさみ形で配線分断実配置）
- 範囲外の気づき: Esc直後の1回目クリックが不発になり2回目で反応する事象を観測（UIA合成クリック特有の
  タイミング事象と忍者判断、実害なし・記録のみ。ninja.md既知の罠と同系）
- グリフ意匠変更ゆえRED証明は不要（バグ修正にあらず、家老判断）

### T-049 デバイス名編集中の未確定編集を確定してから保存 — 完全Done 2026-07-10

**起票=P-013（隠密T-036デバイス名修正レビュー、2026-07-05）を殿裁定2026-07-10で承認**
（確定してから保存する方式を採用、確認ダイアログは挟まない）。デバイス名欄の編集中に
Ctrl+S/N/O・ウィンドウクローズを行うと、フォーカスが動かずUpdateSource契機が無いため
未確定編集がサイレントに保存漏れ／無確認破棄されうる既存課題（T-019のIsDirty機構導入時点から）。

経緯（パイプライン全段）:
- 実装`bfa37cc`（侍、1ファイル=`MainWindow.xaml.cs`のみ。DeviceNameBox_LostKeyboardFocus/
  PreviewKeyDownをCommitDeviceNameEditへ統合し、SaveDocument()・ConfirmDiscardIfDirty()の入口で
  確定するよう追加）。ビルド0警告0エラー・テスト241件全合格
- RED先行証明=不可と侍申告（View層code-behind=TextBoxバインディング挙動そのものが対象、既存
  テスト基盤はViewModelのみ計装可・STA/Window未対応）——隠密レビューで妥当と確認（T-047型の
  純粋関数切り出しも本件には適用不能と判定）
- 隠密静的レビュー（`docs/archive/ecad2-t049-review-onmitsu.md`）: 要修正なし。家老特記3観点（RED不可の
  妥当性・CommitDeviceNameEdit呼び出し網羅・SetProperty早期returnトラップ）いずれも妥当、
  code-reviewスキル（medium・8角度）の候補2件もVerify段階で両方REFUTED
- 忍者実機（`docs/archive/ecad2-t049-verification-ninja.md`）: 観点1〜4+Ctrl+O補足の全てOK。判定は
  見た目でなく保存ファイル（.gcad）のデータで実施（フォーカス保持のままCtrl+S→"deviceName":"Y5"
  反映、Ctrl+N/クローズ→未確定編集込みで破棄確認ダイアログ、回帰=通常のフォーカス外し経路も正常）
- 範囲外の気づき: P-045（選択済み空セルへのF5配置でプロパティパネル未更新=SelectedCell不変で
  通知スキップ、既存挙動）

### T-047 手動配線系（F9/F10系）のツールバーボタン作成 — 完全Done 2026-07-09

**起票=殿指示2026-07-09**。T-041実装の手動配線4種（FreeLine横線=F9・縦線=Shift+F9／
VerticalConnector=Shift+F9／WireBreak=F10／ConnectionDot=F10）がキーボード専用でツールバーに
ボタンが無かったものを、既存配置系ボタン（T-026・T-040様式）と整合する形で追加した。

経緯（パイプライン全段）:
- 隠密先行調査（`docs/archive/ecad2-t047-presurvey-onmitsu.md`）: キー経路はActivateBuiltinToolと別系統・
  シート種別連動の活性制御プロパティ新設要・GX実画像で「既存配置ボタン群と同一行に区切り線で隣接」の前例確認
- GX意匠資源調査（`docs/ecad2-t047-gx-icon-survey-onmitsu.md`）: GPPW3配下887画像+DLL135件の全数調査で
  記号系アイコン画像は不在=ベクター描画と推測、画像抽出の道なし
- UI/UX殿裁定3点（2026-07-09）: (1)5ボタン固定+非対応シートはグレーアウト (2)意匠=T-040様式で侍起草→殿目視
  (3)既存7ボタン末尾に区切り線で隣接。キー凡例は既存様式踏襲=家老判断
- 実装`4ecae77`: 5ボタン新設+IsMainCircuitSheet/IsControlCircuitSheet新設（CurrentSheet変更3経路の通知を
  NotifyCurrentSheetDependentPropertiesChangedへ一元化=SetProperty早期returnトラップ対策、RED証明実測）・
  234件全合格（新規5）
- 隠密2（臨時5人目、起動プロンプト改訂`ba7e7b5`で制度化）静的レビュー（`docs/archive/ecad2-t047-review-onmitsu2.md`）:
  機能クリーン。要修正候補=所見1（キーボード起点起動でフォーカスがボタン残留→矢印/Enter無反応、
  線記入系はマウス確定手段が無く手詰まりに見える）。経過観察5件
- 忍者実機（`docs/archive/ecad2-t047-ninja-verification.md`+証跡5枚）: 所見1再現確定+**新規重大発見**=残留状態の
  矢印キーがツールバー既定ナビへ流れ隣の接続点記入を誤起動→意図しないデータが黙って確定配置されEsc不能
- 修正=制度適用（テスト設計と実装の分離、往復1周）: 隠密設計書（`docs/archive/ecad2-t047-fix-test-design-onmitsu.md`、
  Tool.Modeベース分岐推奨・フォーカス実挙動はヘッドレス検証不能ゆえ忍者実機観点と切り分け）→
  侍修正`afeb068`（RequiresCanvasFocusContinuation純粋関数化+[Theory]全7値・RED証明=常時false化で2件失敗実測・
  241件全合格）→隠密再レビュークリーン（`docs/archive/ecad2-t047-fix-review-onmitsu.md`、所見1=派生経路の副次救済と
  判明し記録のみ・所見2=observations.md #22へ）
- 殿新注文1=並び替え`cffef19`（選択→F5〜F10連続→区切り→部品ボタン最後尾）、隠密軽量確認クリーン
  （byte単位突合・便乗変更なし）
- 殿新注文2=無効時半透明化`12f85b8`（単純図形はグレー化だけで判別困難との殿目視所見→基底ToolBarButtonStyleへ
  IsEnabled=false→Opacity0.35 Trigger、全ボタン統一）、隠密軽量確認クリーン（Style継承の落とし穴なし）
- 忍者実機再確認（`docs/archive/ecad2-t047-fix-ninja-verification.md`+証跡8枚）: 全観点GREEN（フォーカス修正・
  データ混入再発なし・自由線縦線の上下キー[前回未検証]・並び順一致・半透明0.35で判別明瞭）
- 殿最終目視（2026-07-09）: 現行意匠で当面良し。**sF9・F10のグリフ変更は次回課題=T-048起票**（意匠は別途殿提示）
- 範囲外の気づき: P-043（記入中Tabがメニューバーへ抜ける、実害なし）。副次事象=T-047初回コミットが
  レビュー前にpushされた件から**push全面保留の新掟**確立（未レビュー実装コミットがmain上にある間はdocs含め保留）


## T-050 TraceLogの全角ラテン文字正規化統一 — 完全Done（2026-07-10、往復3周+Stryker棚卸し）

**起票**=P-014/P-015統合（殿裁定2026-07-10）：TraceLog環境変数判定の全角ラテン文字（ｆａｌｓｅ等）
未正規化＋兄弟2ファイル（DeviceTableViewModel.Refresh/SheetNavigationViewModel.SelectedSheet setter）の
同型旧値null化。いずれも既定OFFのオプトイン診断機能内。

経緯（パイプライン全段）:
- 初回実装`3190226`: NFKC正規化（string.Normalize）+兄弟2ファイル旧値null化修正、RED証明
- 隠密レビュー: 指摘2件（不対サロゲートでArgumentException・SelectedSheetセッタold==new実バグ）→
  1周目修正`e2f44d7`（経路X採用、RED証明2系統。xUnit InlineDataの不対サロゲートU+FFFD変換の罠を
  コードポイントint渡しで回避=メモリ収蔵済み）
- 隠密事後レビューで**新規バグ発見=2周目へ**（AddCommand/DeleteCommandがCurrentSheetIndexセッタ経由で
  二重発火+old値不整合、ResetSheetsも呼び出し元の先行代入で誤old値。3独立エージェント一致CONFIRMED、
  実害はTraceLog診断ログ限定。`docs/archive/ecad2-t050-fix-review-onmitsu.md`）
- 制度適用（テスト設計と実装の分離）: 隠密テスト設計→侍修正`1d6db37`（SetCurrentSheetIndexCore抽出+
  通知移動方式+ViewModelBaseテストフック層3、RED証明8件、268件全合格）。教訓=旧値はセッタ内の
  無条件捕捉でなく**変更前に呼び出し元で捕捉**が原則
- 2周目隠密再レビュー=クリーン（5観点妥当、`docs/archive/ecad2-t050-fix2-review-onmitsu.md`）。軽微2件
  （reuse・wasEmpty重複判定）は残置=経過観察。テストコード静的レビューで新規指摘1件=
  DeleteCommand境界値テスト4件が削除後のSelectedSheet実値を未検証
- 殿裁定=**往復3周目で補強**→`7838956`（実値アサートx4、InlineData第3引数で期待値を手導出明示。
  RED証明=Math.Min一時退行で補強前4/4 GREEN（検知不可の実測）→補強後3c末尾/3d下限がRED→復元後
  268件全合格）。※前任侍の§5離脱（出力破損#3#4）を挟み、臨時引き継ぎ書
  `docs-notes/handover-samurai-t050-round3.md`経由で新侍が完遂
- 3周目隠密レビュー=クリーン（期待値を実装ロジックから独立に手導出し全4ケース一致・RED整合・
  src残置なし・code-reviewスキル(none)）
- 忍者実機=全観点OK（シート追加0→1含む・削除境界4種・改名・DRCジャンプ同期、回帰なし。
  `docs-notes/ecad2-t050-realmachine-verification-ninja.md`。範囲外の気づきは既知P-005の再観測のみ）
- Stryker手動棚卸し（往復2周超えクローズ時の掟、`docs/archive/ecad2-t050-stryker-review-onmitsu.md`）:
  App score 23.88%、T-050変更行に直接該当する生存ミュータントは3件のみ（常に真の防御ガード・
  診断機能内catch変異・テストフック自身の未検証）→**殿裁定=経過観察でクローズ**（2026-07-10）
- 特記: 期間中に出力破損#3〜#7が侍系列で頻発（Grep結果行改変型が主、Grep使用誘因疑い）、
  §5離脱2回+フリーズ1回を挟みつつ引き継ぎ書2本で作業損失ゼロで完遂。侍ターミナルは
  殿裁定で新規ウィンドウへ系列一新

### T-075 主要機能の仕様書整備 — 完全Done（2026-07-11）

**起票=殿直接指示2026-07-11**（忍者からの上申を受け、家老が選択肢を提示して殿裁定）：「仕様かバグかの
判断が難しいなら仕様書を作成してはどうか。後でヘルプ画面に使用方法を記載する予定ゆえ無駄にならない」。

**背景**：T-062実機検証だけでも3度、仕様/バグの切り分けに難航（起動直後シート0件・横線記入ボタン
無効・要素配置Undo不可、いずれも過去記録の発掘やコード読解で「仕様」と事後確認）。一次資料が無く
検証のたび難航している実態があり、仕様書があれば検証は「仕様書と実機の突合」となり速度・精度向上、
隠密の静的レビューとも共通基準にでき、将来のヘルプ画面へも流用可（T-077で転用予定）。

**殿裁定（スコープ）**：**主要機能を体系的に網羅**——隠密がメニュー/ツールバー単位で一括調査・起草。
`docs/spec/ecad2-spec-{領域}.md`を新規体系として作成（殿指示により`docs/spec/`へ保存）。対象=配置・
結線・Undo/Redo・メニュー構成等主要機能一式。規模=中〜大（調査工数大）。
**全11領域完了（2026-07-11、隠密）**：第1弾=シート/ドキュメント管理・結線操作・Undo/Redo（3件）、
第2弾=メニュー・ツールバー全体構成（1件）、第3弾=配置操作（1件）、第4弾=機器表・BOM／設計チェック
(DRC)・出力パネル／キャンバス表示／部品選択・自作パーツ管理／PDF出力・テストモード／ステータスバー・
モード可視化（6件）。各仕様書は事実・伝聞・推測を峻別し出典（コード・殿裁定記録・実機検証記録）付き
で整理、複数の重要な設計上の発見（配置操作と結線操作のシート種別依存の非対称性、P-050とGroupFrame
配置単位の設計分岐の関連、PDF出力Core層の到達不能コード等）も収録。詳細は`docs/spec/`配下各ファイル
参照。次＝T-077（ヘルプ「使い方」画面への転用）着手時の素材とする。

隠密へ委譲。

### T-074 「バージョン情報」ダイアログへのバージョン番号表示 — 完全Done（2026-07-11）

**起票=殿直接指示2026-07-11**「次回からアプリの『ヘルプ』→『バージョン情報』にもバージョンいれて」。
現状の実態（家老の事前検分）：`MainWindow.xaml:137`に「バージョン情報(_A)」メニュー項目は存在するが、
Click/Command結線が無く器のみ（押下しても何も起きない）。
**殿裁定（2026-07-11、プレビュー提示で選択）**：表示内容=**アプリ名+バージョン番号のみ**
（シンプル、例「Ecad2 v0.3.0」）。著作権表示・ビルド日時等は含めない。
**実装完了（2026-07-11）**：コミット`70d1bb3`。メニューへClickハンドラ結線＋新設`AboutDialog`
（RenameDialog同型の単一階層モーダル）で「Ecad2 vX.Y.Z」のみ表示。バージョンは
`Assembly.GetExecutingAssembly().GetName().Version`経由で`csproj`の`<Version>`と自動連動
（ハードコードなし）。Core64件・App419件全合格、差分は予定範囲内のみ。
**隠密静的レビュー（2026-07-11、`docs/archive/ecad2-t074-about-dialog-review-onmitsu.md`）＝3観点全クリーン**：
表示文言の適合・Version取得方式の妥当性・既存ダイアログとの構造一貫性、いずれも問題なし
（Revision常時0でMajor.Minor.Build表示となる点は現運用と整合する留意点として記録のみ）。
**忍者実機確認＝3点全OK**：AboutDialog表示（スクショ+UIAテキスト直接読取で裏取り）・OKクローズ・
クローズ後の画面遷移影響なし。多重化・例外なし。

### T-073 ecad2-ui-automationスキルのSendKeysフォーカス誤爆対策 — 完全Done（2026-07-11）

**起票=P-056（T-062実機回帰中の殿PC完全フリーズ、T-062節参照）を受けた殿直接指示の一連の経緯**：
「実機確認でクラッシュの原因の調査から初めて」→忍者・隠密のWチェック調査（並行）→「enter試してみて」
→忍者の実機試行でフォーカス誤爆によるモーダルダイアログ多重化を確認（フリーズ再現には至らず、安全に
収拾済み）→「確認はできた、対策プランを検討して」→隠密が対策プラン検討→「3点対応して」で実装許可。

**原因**：`Set-Ecad2Foreground`(helpers.ps1)がモーダルダイアログ表示中でも無条件に`SetForegroundWindow`を
呼び、`ShowDialog`内部の`EnableWindow(FALSE)`と矛盾する状態を生む。SendKeysと組み合わさりフォーカス
誤爆→意図しないコマンド誘発（実機でCtrl+O相当の再トリガーによるダイアログ多重化を確認）。
技術裏付け=`docs/archive/ecad2-p056-sendkeys-freeze-onmitsu.md`（隠密）、実機記録=
`docs-notes/ecad2-p056-sendkeys-freeze-investigation-ninja.md`（忍者）。

**内容（対策プラン推奨案3点、隠密検討・殿裁可済み）**：
1. `Set-Ecad2Foreground`へ`IsWindowEnabled`判定追加、モーダル無効化中はSetForegroundWindow自体をスキップ
2. `Send-Ecad2Keys`へ長文字列ガード（20文字超は例外、`-Force`で明示解除）
3. `SKILL.md`のトラブルシュート節へ危険パターン反映

対象=`.claude/skills/ecad2-ui-automation/helpers.ps1`・`SKILL.md`の2ファイルのみ、src/への変更は伴わない
（検証ツール側のみの改修）。
**実装完了（2026-07-11）**：コミット`a8be0bd`。3点とも対策プランどおり実装。
**隠密静的レビュー（2026-07-11、`docs/archive/ecad2-t073-p056-fix-review-onmitsu.md`）**：5節対策プランどおり
正確な実装・致命的問題なしと確認（code-reviewスキル併用、境界値・既存呼び出し元6箇所への影響・
リポジトリ全体の回帰、いずれも問題なし）。要修正1件（`SKILL.md:35-36`の参照先が実在しない見出し名を
指す不整合、実害=読者が参照先を探せない）を往復1周目として侍へ差し戻し。経過観察4件（スタイル不統一等）
は対応不要と仕分け。
**往復1周目修正完了（2026-07-11）**：コミット`348be4d`（差分1行のみ）。隠密再レビューでクリーン確定。
**忍者実機再検証（2026-07-11）＝3観点全OK**：(1)モーダル表示中`Set-Ecad2Foreground`呼び出しで
`SetForegroundWindow`がスキップされフォアグラウンドはダイアログのまま変化なし (2)`Send-Ecad2Keys`
21文字送信で`-Force`なし例外・`-Force`指定時は正常反映（対策前は1文字すら届かなかったのと対照）
(3)Enter単独送信の再試行でダイアログ多重化が再発せず1枚のまま維持（対策前は2枚に増えていたのと対照）。
フリーズ・異常兆候なし。詳細=`docs-notes/ecad2-p056-sendkeys-freeze-investigation-ninja.md`7〜8節。

### T-072 v0.3仮リリースビルドの作成 — 完全Done（2026-07-11）

**起票=殿直接指示2026-07-11**「T-062完了しマージまでできたらV0.3とする」。**T-062（.NET 10移行）の
mainマージ完了を着手条件**とする。内容=T-057（v0.2）で確立したリリース手順の踏襲：
`Ecad2.App.csproj`のVersionを0.3.0へ→フレームワーク依存publish（bin配下＋`C:\ECAD2\version\v0.3`の
2箇所）→生成物の起動・終了を実機実測（FileVersion/ProductVersion=0.3.0確認）→gitタグ`v0.3`付与
（家老）→csprojのみコミット。
**留意**：.NET 10移行後の初リリースゆえ、配布物は**.NET 10ランタイム前提**となる（v0.2までの
.NET 8前提から変わる。殿PCはSDK 10.0.301導入済みで問題なし。他環境へ配布する場合はランタイム要）。
**実装完了（2026-07-11）**：コミット`b46dd36`（csproj Version 0.2.0→0.3.0の1行差分のみ）。
フレームワーク依存publishでbin配下＋`C:\ECAD2\version\v0.3`の2箇所へ配置、実機実測で
FileVersion=0.3.0.0/ProductVersion=0.3.0・起動→終了とも正常確認。gitタグ`v0.3`付与・push完了（家老）。
仮リリース第二弾として殿の実機確認へ引き渡し。

### T-071 経路B部品（押釦・タイマ接点等）の部品選択リスト追加 — 完全Done（2026-07-11）

**起票=部品差分調査（`docs/ecad2-guiecad-hardcoded-parts-diff-survey-onmitsu2.md`）を受けた殿裁定**。
**殿裁定3点**：(1)GuiEcadの専用ツールパレット（経路B）のUIは再現不要——既存UIのまま (2)経路Bの
パーツは**「部品」（右パネルの部品選択リスト）から呼び出せればよい** (3)コイル・端子・a/b接点は
既存7種と重複するため追加不要。
追加対象（重複除外後）：**押釦NO/NC・タイマ接点NO/NC・瞬時接点NO/NC・表示灯・サーマル・非常停止・
モータの約10種**（セレクトSWも既存リストに含まれるため同様に重複除外——P-010記録より家老検分。
正確な対象一覧は調査書と突合の上、着手時に侍が確定・報告）。
実装方針：Core層は完備（ElementKind・SymbolGlyphs・DeviceClassとも移植済み）ゆえ
`BasicPartTemplates`へのPartDefinition追加＋サムネイル対応が主作業。規模小〜中。
ロードマップ位置=T-062後の小粒群（T-063/065/066）と同グループ。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t071-part-addition-design-onmitsu2.md`）**：
対象10種は齟齬なく確定。PartDefinition追加方針は3グループ——①押釦NO/NC・表示灯（3種）は既存
`PartRole`(InputNO/InputNC/Lamp)で対応可・規模小 ②モータ（1種）は`PartRole.NonSimulated`流用可、
3端子(U/V/W)ポート定義が別途必要・規模小 ③タイマNO/NC・瞬時NO/NC・サーマル・非常停止（6種）は
対応する`PartRole`が存在せず**`PartRole` enum拡張＋`PartResolver.ComponentKind()`のswitch拡張が
必要**（既存Roleへの丸めは電気的挙動・機器分類双方が劣化するため非推奨）・規模小〜中。
`DeviceClass`分類側は既に対応済みにつき追加実装不要。
サムネイルは2経路：**①配置バーの選択中部品の大サムネイル(`PartThumbnailRenderer`)は新規10種とも
既に自動対応済み（訂正前の報告は不正確だった）** ②コンボ内の小アイコン
(`PartEntryToGlyphGeometryConverter`)のみ既存5種限定でCustomフォールバックとなる。
**GuiEcadの専用グリフの正体＝実体の記号描画ロジック(`SymbolGlyphs.Draw`)をSVG化してツールボタンへ
流用する仕組み**（`SvgRenderer.GenerateSymbolSvg`）、ecad2にも同型`SymbolGlyphs.Draw`が完備（隠密2、
殿の実例指摘を受けた追加調査で判明）。
実現方式2案：A=既存5種と同じGX様式簡略化アイコンで手作り（意匠統一、規模小〜中）／
B=GuiEcad同様に実体記号描画をPath→Imageへ差し替えて流用（規模小、ただし既存5種とタッチが異なる）。
**殿裁定（2026-07-11、プレビュー提示で選択）＝方式B（実体記号描画を流用）**。
**実装完了（2026-07-11）**：コミット`c779210`。10種PartDefinition追加、配置バー小アイコンは方式B
（`PartThumbnailRenderer`直接バインド、新規部品追加時に自動対応）。Core71件・App415件全合格。
**隠密静的レビュー（2026-07-11、`docs/ecad2-t071-part-addition-review-onmitsu.md`）**：要修正2件
CONFIRMED——①Motor(WidthCells=3)が「全パーツ1セル幅」前提を破り配置時の境界/重複チェックをすり抜ける
（データ実害あり） ②タイマ限時/瞬時ラベルが生の`ElementInstance.Kind`参照で描画されない。
DoD4（`PartEntryToGlyphGeometryConverterTests`削除の妥当性）は`PartThumbnailRenderer.Render`側の
既存カバーで裏付け確認済み。
**往復1周目修正完了（2026-07-11）**：コミット`e1fd101`。隠密テスト設計（境界値10ケース+ラベル7ケース）
どおり、`IsWithinGridBounds`等をcellWidth対応へ拡張・`DiagramRenderer`のラベル解決を
`PartResolver.ComponentKind`経由へ差し替え。★ケースRED→GREEN実測、Core71件・App425件全合格。
隠密再レビューでクリーン確定（View層プレチェックとCore側最終判定の二重防御構造がT-033増分5の
既存仕様と整合）。
**忍者実機確認＝4観点中3点OK**：新規10種配置・Motor境界外/重複拒否・限/瞬ラベル表示（機器名設定後）・
配置バー意匠混在、いずれも正常。範囲外の気づき（表示灯LampColorのUI結線が皆無）はP-057として
`proposed.md`へ記録、T-071スコープ外として完全クローズ。

### T-063 「名前を付けて保存」「削除」のメニュー露出 — 完全Done（2026-07-11）

**起票=棚卸し「ほぼA区分」2件を1タスクに統合**。いずれも内部機能・キー操作は既存で、メニュー
項目の露出のみ欠落。規模小（メニュー項目追加＋既存処理への結線のみ）。T-062後の肩慣らし筆頭。
**実装完了（2026-07-11）**：コミット`4f8ea7f`。「名前を付けて保存」＝ファイルメニュー追加、
SaveButtonと同じ`IsEnabled`バインディング＋既存`SaveDocumentAs()`へ結線。「削除」＝編集メニュー
追加、Key.Delete caseと同一の5種`DeleteSelected*`呼び出しを流用（選択なし時は各メソッドがfalseを
返し安全にno-op、既存のDeleteキー自体に可視的な無効化パターンが無いこととの整合を優先）。
Core64件・App419件全合格、差分は予定範囲内のみ。
**隠密静的レビュー（2026-07-11、`docs/archive/ecad2-t063-menu-review-onmitsu.md`）**：「名前を付けて保存」・
他メニューとの一貫性はクリーン。**「削除」メニューで要修正1件CONFIRMED**——`DeleteMenuItem_Click`が
`CommitDeviceNameEdit()`未呼び出し。Key.Delete caseは`IsCanvasFocused()`判定でDeviceNameBox編集中は
到達し得ないが、メニュークリックはフォーカス非依存で発火するため「機器名未確定編集中に選択要素
（＝編集対象そのもの）をメニューから削除する」新経路が生まれ、未確定入力が黙って破棄される
（コードレベルで実害確認済み、クラッシュではないがUX実害）。
**往復1周目修正完了（2026-07-11）**：コミット`0910658`（差分4行、`SaveAsMenuItem_Click`同型で
`CommitDeviceNameEdit()`追加）。隠密再レビューでクリーン確定（SelectedElementの不変性まで確認）。
**忍者実機確認＝全観点OK**：両メニューとも既存処理への結線が正常動作、Deleteキーとの対照実験も一致。
補足の気づき（線要素は中心クリックでなく実体上クリックで選択、実害なし）は経過観察のみ。

### T-062 .NET 10（net10.0-windows）への移行 — 完全Done（2026-07-11、mainマージ済み）

**起票=殿直接指示2026-07-11**「.NET 10移行をタスク起票して。デバッグ範囲が広がることを懸念して
移行は早いほうがいいかもな」。背景：(1)現行.NET 8（LTS）のMicrosoftサポートが**2026-11-10終了**（残り4ヶ月）。
.NET 9（STS）も**同日2026-11-10終了**——当初の家老知識「9は2026年5月終了済み」は誤りで、STSの
サポート期間が18ヶ月→24ヶ月へ変更されていたことが原因（隠密の一次情報裏取り2026-07-11で訂正、
出典=learn.microsoft.com Lifecycle）。いずれにせよ9は4ヶ月後に切れるため移行先は
.NET 10（LTS、**2028-11-14まで**）一択で結論不変 (2)AvalonDock（T-058採用候補）がnet8明示
対象外・net10明示対応（`docs/ecad2-t058-avalondock-net8-precheck-onmitsu2.md`）——移行後にT-058
PoCを行えばnet5フォールバックの不安も消える (3)殿の理=機能が増えるほど移行時の検証範囲が広がる
ゆえ早期が得。
内容：4プロジェクト（App/Rendering.Wpf=net8.0-windows→net10.0-windows、Core/Pdf=net8.0→net10.0）
のTargetFramework更新＋依存NuGetパッケージ（PDFsharp等）の互換確認＋テスト全実行＋忍者実機確認
（起動・主要操作の回帰）＋リリース手順（publish先2箇所）への影響確認。
**前提確認事項（着手時）**：殿PCへの.NET 10 SDK導入有無（未導入なら移行前に殿へ依頼）、
global.json等のSDK固定設定の有無、サポート期限の一次情報裏取り。
**前提整備完了（2026-07-11）**：殿がwingetで.NET 10 SDK導入→家老検分で**10.0.301の導入を確認**
（`dotnet --list-sdks`実測、8.0.422と併存）。global.jsonは無し（SDK固定なし、そのまま10が使われる
環境ではなくTargetFramework側で制御——csproj更新で移行）。**残る待ちはT-051クローズのみ**、
クローズ次第、侍へ着手采配。
**ブランチ運用（殿裁定2026-07-11）**：**作業前に専用ブランチを作成し、テスト合格後にmainへマージ**
（T-045と同じ運用=mainを直接汚さずリスク回避）。ブランチ名=`t062-net10-migration`（家老指定）。
マージ条件=検証パイプライン完走後（ビルド・テスト全合格→隠密レビュー→忍者実機回帰の起動・主要操作
確認まで通してから家老がマージ・push）。ブランチ作業中も他役の調査・レビューはmain基準で並行可。
**移行作業完了（2026-07-11）**：コミット143e1c0（ブランチ上）。6ファイルのTargetFramework更新
（本体4+テスト2——テスト2はProjectReference互換の構造的必然として侍上申→家老裁可の追加分）。
PDFsharp 6.2.4は最新・net10互換で更新不要。`dotnet build --no-incremental`0警告0エラー
（.NET 10固有の非推奨警告も無し）、テストCore64件・App419件全合格・退行なし。コードのモダナイズは
指示どおり不着手。
**隠密静的レビュー完了（2026-07-11、`docs/archive/ecad2-t062-migration-review-onmitsu.md`）＝家老指定4観点
（TFM値・混入有無・PDFsharp互換・ビルドテスト実測）いずれもクリーン**。
**忍者実機回帰は起動確認のみ完了・退行なし**（dotnet build 0警告0エラー→起動正常、ステータスバー
初期値"ツール: Select"/"ズーム: 100%"も正常、クラッシュ・例外なし）。主要操作フロー（配置・結線・
保存/読込・Undo/Redo）は殿のPC環境不調（P-056参照）により中断・未検証のまま持ち越し。
**P-056（家老所見）→T-073で対策完了・解消**：本日の中断契機＝殿PCでキーボード入力が完全に不通となる
フリーズが発生し、殿がスタートメニュー経由で手動再起動（マウスは生存）。原因調査（隠密の技術裏付け＋
忍者の実機切り分け、Wチェック）の結果、**検証ヘルパー（`ecad2-ui-automation`スキル）側の実装課題**
（`Set-Ecad2Foreground`がモーダルダイアログ表示中でも無条件`SetForegroundWindow`を呼び`EnableWindow(FALSE)`
と矛盾する状態を生む）と判明、アプリ自体は健全と確認。対策は**T-073として正式分離・完全Done**
（詳細はT-073節参照）。
**主要操作フロー実機回帰完了（2026-07-11、`docs-notes/ecad2-t062-main-operations-regression-ninja.md`）
＝4観点全OK、退行なし**：(1)配置操作=a接点/b接点/コイル/端子台の4種、正常配置・自動配線
(2)結線操作=横線/縦線/縦分岐線/接続点、正常動作（シート種別依存のIsEnabled仕様[主回路/制御回路限定]
は既存仕様と確認、退行でない） (3)保存/読込=新規作成→保存→読込のラウンドトリップ完全一致
(4)Undo/Redo=シート追加/削除は正常動作、要素配置はUndo対象外（T-051のMVP仕様どおり、退行でない）。
**mainマージ・push完了（2026-07-11）**。次＝T-072（v0.3仮リリース）着手判断。

### T-059 出力パネルの高さをドラッグで調整可能にする — 完全Done（2026-07-11）

**起票=殿直接指示2026-07-11**「出力パネルの高さをスライダーで調整できるようにしたい」。
現状の実態（家老の事前検分）：`MainWindow.xaml:503`の`OutputPanelArea`（DockPanel）が
`Height="160"`固定、所属行`Grid.Row="3"`もAutoで可変機構が無い。既存の右パネル
（機器表⇔プロパティ、`MainWindow.xaml:435-456`）・3カラム分割（`MainWindow.xaml:431`）は
いずれも`GridSplitter`によるドラッグリサイズが既に実装済みであり、本件も同一パターンの踏襲で足りる
と判断。「スライダー」は文言上の表現であり、実装は既存踏襲のドラッグ式`GridSplitter`とする
（機能的にはドラッグでサイズ調整＝要望と同じ、新規コントロール種別ではない）。侍へ着手を采配。
**実装完了（2026-07-11）**：コミットc01b0fe。右パネル分割と同一パターンでAuto行(GridSplitter,
Height=4)を新設、OutputPanelAreaはGrid.Row 3→4・固定初期値160+MinHeight=80へ、DockPanel側の
明示Height撤去、メイン作業域に MinHeight=200。StatusBarAreaは別階層Gridのため対象外と判明（誤配線
ではない）。Core53件・App346件変化なし全合格、差分1ファイルのみ。
**忍者実機全観点OK（2026-07-11、6件一括検証）＝完全Done**（実機記録は T-052節参照）。

### T-057 v0.2仮リリースビルドの作成 — 完全Done（2026-07-10）

**起票=殿直接指示2026-07-10**「どこかで仮リリースをしたい。実機で確認して今後の機能を検討したい。
v0.2でリリースできないか」。**殿裁定2点**：(1)タイミング=現時点のmainで今すぐ（進行中タスクの
完了を待たない） (2)配布形態=フレームワーク依存型（軽量、.NET 8ランタイム前提でexe+dll一式）。
現状の実態（家老の事前検分）：`Ecad2.App.csproj`にVersionタグ無し（暗黙1.0.0.0扱い）、gitタグも
皆無、publish設定も未整備——ゼロからの新設。
**着手順序（家老判断）**：ちょうどT-055増分2往復2周目（文言修正のみ、軽微）が進行中のため、
コンテキスト断片化を避けるべくその完了を待ってから連続して着手する（数分程度の遅延見込み）。
内容: `Ecad2.App.csproj`へ`<Version>0.2.0</Version>`追加、`dotnet publish -c Release
--self-contained false`でフレームワーク依存の配布物一式を生成、動作確認後にgitタグ`v0.2`を付与。
配布先・具体的な確認観点は着手時に必要なら都度殿確認。
**T-057=完全Done（2026-07-10）**：`722bf92`（csproj Version=0.2.0のみ、+1行）。フレームワーク依存
publishで生成物の起動→終了を侍が実機実測（FileVersion/ProductVersion=0.2.0確認）。gitタグ`v0.2`
付与済み（家老）。仮リリース第一弾として殿の実機確認へ引き渡し。
**リリース方針の殿裁定（2026-07-10）**：**本リリース（正式版）はインストーラー形式とする**。
v0.2等、それまでの実機確認・機能検討目的のリリースは**仮リリース**と位置付け、フレームワーク
依存型の簡易配布物で足りる（インストーラー整備は本リリース着手時の別タスク、具体的な技術選定
＝MSI/WiX等は未定・将来検討）。

### T-056 キャンバスのグリッド線表示切替機能 — 完全Done（2026-07-11）

**起票=殿直接指示2026-07-10**「キャンバス画面のグリッド/非グリッド機能がない」。現状の実態
（家老の事前検分）：`LadderCanvas.cs:29-32`で`ShowGrid=true`が**固定値としてハードコード**
されており（T-030「グリッド線表示の有効化」殿直接依頼2026-07-03の経緯＝実機テストの行合わせ
補助として常時表示化）、表示⇔非表示を切り替えるトグルUI（ボタン・メニュー・ショートカット等）
は一切存在せぬ。**殿確認済み**：グリッド線の表示/非表示を切り替える新規機能の追加。
**優先順位=T-052〜054より後**（殿裁定2026-07-10、現行キューT-055→T-052〜054の最後尾へ）。
UI/UX分岐（トグルの配置場所・既定値・アイコン等）は着手時に都度殿確認【MUST】。
**事前案起草完了（2026-07-11、隠密2、`docs/archive/ecad2-t056-grid-toggle-proposals-onmitsu2.md`）**：
「表示(_V)」メニューに「グリッド表示(_G)」項目が既に存在するが未結線（器のみ）と判明。
案A=同項目へIsCheckable+Command結線（コスト小、隠密2推奨）／案B=ツールバーへトグルボタン新設
（トグル型UI先例皆無で新規スタイル要、コスト中）／案C=併用。既定値（現状ShowGrid=true維持か）・
ショートカット（Ctrl+G候補、現行未使用）・永続化（ユーザー設定永続化機構がecad2に皆無と判明、
新設ならコスト中〜大）の3論点も整理済み。
**殿裁定4点確定（2026-07-11）**：(1)トグルUI=**案A（メニュー項目へIsCheckable+Command結線）**
(2)既定値=**表示で起動（現状維持）** (3)ショートカット=**Ctrl+G割当**（現行未使用・衝突なし）
(4)永続化=**今回は非永続**（起動毎に既定値、共通設定機構はP-052として別途）。
**実装完了（2026-07-11）**：コミットfeb8d9c。裁定4点どおり——メニュー「グリッド表示」へ
IsCheckable+IsChecked(TwoWay)結線・既定true・Ctrl+G追加・非永続。VMへ`IsGridVisible`新設
（既存CanvasScale同型）、LadderCanvasの`_renderer`readonly解除+ShowGrid経由再構築（Core層無改修）、
PropertyChanged検知→ShowGrid同期→RedrawCanvas()の即時反映。Core64件・App395件（+3）全合格、
差分は裁定範囲と完全一致。
**隠密レビュークリーン（2026-07-11）**：4観点（裁定4点一致・renderer再構築方式の妥当性
[リーク/競合なし・PDF非波及のT-040原則維持]・Ctrl+G衝突なし・SetProperty早期returnトラップ
非該当[単純boolトグル、同値時不発火もテスト済み]）とも問題なし、隠密実測で全合格確認。
**忍者実機全観点OK（2026-07-11、6件一括検証）＝完全Done**。検証中に忍者がCtrl+G「描画反映されず」
とNG誤診断→殿の実機操作で正常確認・訂正（原因=UIA経由キー送信の検証手順側、副次知見3として
スキル追記候補。実機記録はT-052節参照）。

### T-055 行数拡張のGuiEcad方式踏襲＋母線番号入力の同仕様化 — 完全Done（全増分1〜3、2026-07-11）

**起票=殿直接指示2026-07-10**「10行ではまったく足りないのでgui_ecadの方式を参考にして欲しい。
なお母線番号入力も同じ仕様にしたい」。
現状の実態（家老・探索役の事前検分2026-07-10）：行追加機能は不在、新規シートは`Rows=10`固定
（`SheetNavigationViewModel.cs:106`・`MainWindowViewModel.cs:1472`）、配置・選択とも上限
`Grid.Rows-1`でクランプ（T-045境界ガード）、描画側`TotalRows`の拡張分岐はUI操作では到達不能。
**第一段=隠密のGuiEcad方式調査**（`C:\Users\kojif\Desktop\生産物\gui_ecad`）：
(1)行の追加・管理方式（UI操作・データモデル・上限・既定行数・描画/ページとの関係）
(2)母線番号入力の仕様（UI・入力契機・データ保持・描画・保存形式）
**隠密調査完了（2026-07-10、`docs/archive/ecad2-t055-guiecad-row-busnumber-survey-onmitsu.md`）＋
殿裁定3点（2026-07-10）**：
1. 「母線番号入力」の正体=**母線名（LeftName/RightName、文字列の手動編集）**——GuiEcadに数値の
   母線番号は不在と判明したための概念確定（回路番号CircuitNumberは対象外）
2. 行追加UI=**GuiEcad全系統踏襲**（ツールバー行±・Ctrl+Shift+Up/Down・右クリック任意位置挿入・
   シート設定ダイアログ数値入力）
3. 既定行数・上限=**既定10・上限60で統一**（GuiEcad内部の不統一[既定8/10/22・上限バラバラ]は
   持ち込まない。下限1）
次の段取り=侍がT-050往復2周目の後に**増分計画を起草**→殿裁可→増分実装。設計中の新たな
UI/UX分岐は都度殿確認【MUST】。
**計画起草完了＋殿裁可（2026-07-10）**：計画書=`docs/archive/ecad2-t055-implementation-plan-samurai.md`
（3増分リスク低→高、`d47b700`収蔵）。**開かれた論点6点の殿裁定**=(1)行±ボタンはツールバー大型
（`ToolBarButtonStyle`同型）(2)要素の存在する行の削除は**拒否（警告）**（Undo未実装ゆえの安全策、
増分1・3共通）(3)コンテキストメニュー文言はGuiEcad踏襲 (4)シート設定は新規ダイアログ
（RenameDialog拡張は不採用）(5)増分順序は提案どおり裁可 (6)`Sheet.Lines`（`CircuitLine.Row`）は
シフト対象外＋明示コメント（家老配下探索で実質未使用と確定=書込経路`CircuitNumberer.Number`・
読取経路`CircuitByRow`とも呼び出し元なしのデッドコード、JSONへ空配列直列化のみ）。
増分1（末尾行加減算）を侍へ采配（2026-07-10）。
**補足裁定（2026-07-10、侍の着手前確認への回答）**：裁定(2)「要素の存在する行の削除は拒否」の
「要素」は**広義=5種すべて**（ElementInstance・VerticalConnector・WireBreak・GroupFrame・
RungCommentのいずれかが最終行にあれば削除拒否。Undo未実装ゆえ配線・コメント等の暗黙消失も防ぐ
安全側の解釈、増分1・3共通）。
**増分1=完全クローズ（2026-07-10）**：実装`6a6eaf7`→隠密レビューで要修正1件（SelectedCell範囲外
残置、CONFIRMED）→殿裁定=新末尾行へクランプ・列維持→修正`c28ec40`→隠密再レビュークリーン→
忍者実機全6観点OK（`docs-notes/ecad2-t055-increment1-realmachine-verification-ninja.md`）だが
新規指摘1件（DeleteRowCommand成功時に拒否警告StatusMessage残留）→往復2周目・Wチェック（隠密が
根本原因=成功パスのクリア処理書き漏れと確定、AddRowCommand側の対称欠落も家老裁定で本往復へ吸収）
→テスト設計→修正`0d2ed4d`（両コマンド成功パスへクリア追加、RED証明・304件全合格）→隠密再レビュー
クリーン→忍者再確認OK（証跡2件）。**次は増分2（シート設定ダイアログ）へ**。
****増分2=完全クローズ（2026-07-10）**：実装`4a91c38`→隠密レビューで要修正1件（占有チェック欠落、
PLAUSIBLE→殿裁定=拒否）+cleanup1件（トリプレット重複→殿裁定=共有ヘルパー化）→修正`accaf6a`
（往復1周目）→隠密再レビューで新規指摘1件（拒否文言が「最終行」固定でDeleteRowCommand流用、
縮小範囲内の任意行での拒否に不一致、CONFIRMED）→修正`b1054a9`（往復2周目、実行番号を含む文言へ）
→隠密再レビュークリーン確定（4観点妥当・要修正なし）→忍者実機全6観点OK
（`docs-notes/ecad2-t055-increment2-realmachine-verification-ninja.md`、往復1・2周目の両修正とも
実機で機能を実証）。副次知見=ダイアログ（別トップレベルウィンドウ）はSave-Ecad2Screenshotで
写らずスキル恒久対応は別途検討（実害なし、忍者証跡は個別対応済み）。**次は増分3（任意位置挿入・
削除、最高リスク）へ**——PoC推奨、着手前に段取り検討。

**増分3進行中（2026-07-10）**：隠密事前調査（`docs/archive/ecad2-t055-increment3-precheck-onmitsu.md`）＝
ContextMenu基盤・行シフト処理ともGuiEcad実装（`RowOps.ShiftRows`等、実物照合済み）を同型踏襲すれば
見通し良好、**独立PoCフェーズは不要**の所見。実装`33a59f9`（ContextMenu基盤・RowOps・
InsertRowBeforeCommand/DeleteRowAtCommand、削除は「対象行に要素なし」契約のみ）→隠密レビューで
要修正2件（a: SelectedCellがRowシフトに追随せず誤操作リスク、CONFIRMED／b: テスト名が未実装挙動を
確定仕様のごとく誤読させる、CONFIRMED）+cleanup1件（c: 占有拒否ブロックがrule of three到達、
過去裁定の「3箇所到達で再検討」条件成立）→修正`e9d062a`（往復1周目、TryRejectOccupiedRowへ共通化
含む）→隠密再レビュークリーン確定（3件とも往復2周目の指摘なし）→忍者実機確認**6観点中5観点OK**
（右クリックメニュー・任意位置挿入5種シフト・要素なし削除・SelectedCell追随・境界値クランプ）。
**新規検出→殿裁定確定（2026-07-11）**：GroupFrame内部削除のHeight--ロジック（計画書明記、Core層は
単体テスト済み）が、コマンド層の`IsRowOccupied`判定（GroupFrame範囲全体を占有とみなす）により
実機で到達不能なデッドコード疑いと判明していた件。既存の未確定論点「削除対象行そのものに要素が
ある場合、拒否か要素ごと削除か」（計画書109行目）と表裏一体と判明し、殿裁定を仰いだ結果：
**要素ごと削除（GuiEcad同型）を採用**。**適用範囲は増分3（任意位置削除＝`DeleteRowAtCommand`）
のみ**——既にクローズ済みの増分1（`DeleteRowCommand`・末尾行削除）・増分2
（`UpdateSheetSettingsCommand`・シート設定でのRows縮小）は現状の「拒否（警告）」のまま維持し、
遡及修正は行わない（殿裁定2026-07-11）。
**設計調査＋テスト設計完了（2026-07-11、`docs/archive/ecad2-t055-increment3-delete-occupied-design-onmitsu.md`）**：
GuiEcad5種の削除条件・実行順序を実物照合済み（ElementInstance/WireBreak/RungComment=Row一致で削除、
VerticalConnector=端点一致のみ削除、GroupFrame=開始行一致で枠ごと削除／内部にかかればHeight--）。
改修方針＝`IsRowOccupied`自体は不変・`DeleteRowAtCommand`のみ占有拒否撤廃・`RowOps.DeleteRow`を
GuiEcad同型へ拡張（戻り値に削除ElementInstance群を追加、呼び出し元で機器表クリーンアップ）。
テスト設計＝境界値9パターン含め起草済み。新規論点（SelectedCellが削除対象行を指す場合の据え置き
挙動の要否）は隠密よりUI/UX分岐の可能性ありと申し送りありしも、クラッシュ等の実害なし・隠密自身も
「家老裁量で据え置きのまま実装→忍者実機確認で違和感あれば再検討」の代替案を提示済みにつき、
**家老裁定＝据え置きのまま実装、忍者実機確認で違和感の有無を確認**（往復発生時のみ改めて殿へ）。
**実装完了（2026-07-11）**：コミット424130e。`RowOps.DeleteRow`をGuiEcad同型へ拡張（実行順序厳守・
戻り値`IReadOnlyList<ElementInstance>`追加）、`DeleteRowAtCommand`の占有拒否撤廃＋機器表
クリーンアップ（`CleanupRemovedDeviceNames`新設）、境界値9パターン（B1〜B9）全実装。実装中に
既存Theory1件がVerticalConnector共有ヘルパーと新設端点削除の組合せで意図せず失敗→設計書§3.3の
「VerticalConnectorは個別Factで扱う」指示どおり専用Factへ分離し解消（範囲内修正）。
Core64件（+11）・App349件（+3）全合格、差分4ファイル、増分1・2は不可侵維持。
**隠密レビュー（2026-07-11、`docs/archive/ecad2-t055-increment3-round2-review-onmitsu.md`）**：家老指定
5観点は全てOK（実行順序・境界値9パターン・機器表クリーンアップ・Theory分離・増分1/2不可侵）だが、
code-review併用で**重大データ破損バグ2件を検出（いずれもCONFIRMED・再現手順明示）**：
a=削除対象行そのものを選択中に削除→PropertyChanged未発火→UIが削除前要素の情報を表示したまま→
後の編集Tab確定でシフト後の別要素を`DeviceRenamer.Rename`が**ドキュメント全体誤改名**。
b=削除行より後ろを選択中→SelectedCellの-1シフト代入が`RowOps.DeleteRow`より先に実行され
シフト前座標系でSelectedElement評価→誤った要素情報が永続表示（機構自体はe9d062a由来の既存コード、
占有行削除の解禁で到達頻度が実質上昇）。根本原因共通=行削除がSelectedCellの実データ変化を通知せぬ
設計。隠密の恒久対応推奨=削除後に無条件でSelectedElement系PropertyChangedを発火する方式への転換。
c=`CleanupRemovedDeviceNames`が機器表クリーンアップ3箇所目の重複→共通ヘルパー抽出推奨
（増分2の同型裁定「同一往復で相乗り」前例に倣い家老裁量で本往復へ吸収）。d〜gは経過観察。
**家老仕分け＝a・b・cとも範囲内欠陥として往復1周目（要素ごと削除対応の初回往復）**。データ破損
ゆえ忍者実機確認より修正を先行。制度【MUST】適用：隠密テスト設計起草→侍修正＋RED証明の順。
**往復1周目修正完了（2026-07-11）**：コミット19b4574。隠密テスト設計
（`docs/archive/ecad2-t055-increment3-selectedcell-bugfix-test-design-onmitsu.md`、T-a1〜a3・T-b1〜b2）を
全実装。a=`NotifySelectedElementChanged()`新設（削除完了後に無条件でSelectedElement系4プロパティ
通知）。b=設計書(ii)方式採用——`RowOps.DeleteRow`実行後にSelectedCellシフト代入・通知を行う順序
変更で「未シフトElementsでの一時誤評価」自体を排除（単発発火で正しい値のみ）。c=
`RemoveDeviceIfUnreferenced`共通ヘルパーへ3箇所一本化。**RED証明実測**=T-a1「Expected:True
Actual:False」・T-b1「Expected:"A001" Actual:"B001"」とも設計書予測どおり失敗→修正後GREEN。
Core64件・App392件（+5）全合格、差分2ファイル、RowOps.cs不変。
**隠密再レビュークリーン確定（2026-07-11）**：4観点（設計書突合・RED証明整合・(ii)方式の4懸念
[クランプ二重通知・通知タイミング・クリーンアップ順序・SetProperty早期returnトラップ再発]検証
=いずれもREFUTED・共通ヘルパー参照透過）とも問題なし、往復2周目の指摘なし。ビルド・テストも
隠密実測で全合格確認。
**忍者実機全観点OK（2026-07-11、6件一括検証）＝増分3完全クローズ、T-055全増分Done**
（SelectedCell据え置きの使用感も違和感なし。実機記録はT-052節参照）。
d（CanExecute境界チェック不足、P-049）・e（GroupFrame Visual*Mm座標未追随、P-050）は実害なしに
つきproposed.mdへpending記録済み、対応不要。実機確認詳細=
`docs-notes/ecad2-t055-increment3-realmachine-verification-ninja.md`。

**増分2着手前の殿裁定（2026-07-10）**：Bus名（LeftName/RightName）の空文字入力は**許容**
（GuiEcad踏襲、自由記述で空文字も有効な値として保存可）。
**増分2トリガーUIの殿裁定（2026-07-10、侍の着手前確認への回答）**：左パレット（シート
ナビゲーション）下部の既存ボタン列（＋/－/名前変更、`MainWindow.xaml:376-383`）に**「設定」
ボタンを追加**（同型の小型ボタン、既存UIの文脈に沿う最小差分。ダブルクリック案は不採用）。
**増分2隠密レビュー指摘の裁定（2026-07-10）**：(3)`UpdateSheetSettingsCommand`にDeleteRowCommand
同型の`IsRowOccupied`チェックが無く、Rows一気縮小でキーボードのみ到達不能・マウス経由のみ到達可
という非対称が生じる問題（キーボードファースト方針に抵触）→**殿裁定=同じ位置で拒否（警告）**、
DeleteRowCommandと同じ占有チェックを追加し往復1周目として侍へ采配。(4)StatusMessage/MarkDirty/
NotifyCurrentSheetChangedのトリプレット重複（rule of three超え、CONFIRMED・実害なし）→**殿裁定=
今回の往復で共有ヘルパー化**（同一往復に相乗り、便乗拡大ではなく指摘対応の一環）。

### T-054 部品選択リストの選択中部品を配置バー内に表示 — 完全Done（2026-07-11）

**起票=P-023を殿裁定で承認（案A採用）**。部品選択リストはSelectedItemバインドが元々皆無で選択
状態の保持先が存在しない設計（隠密調査）。案A=配置バー内に選択中部品情報を表示する方式
（既存disabled化と非競合、実装コスト小）。案の詳細=`docs/archive/ecad2-uiux-proposals-p017-p020-p023-onmitsu.md`
**実装完了（2026-07-11）**：コミット762d9c8。ElementPlacementBarへサムネイル+名前表示エリア追加
（ComboBoxとデバイス名入力欄の間）。`PlacementPartComboBox.SelectedItem`をElementNameバインドで
直接参照（初期選択・選び直しとも自動追従、同期コード不要）。`TryPlaceElement`内の照合ロジック
重複を`PartPaletteViewModel.ResolveEntry`へ抽出共有。配置場所は既存パターン踏襲の明白な選択として
侍判断（殿確認省略、異論あれば差し戻し可と申告——実機確認証跡で殿の目に触れる前提）。
Core64件・App387件（+4）全合格、差分4ファイル。
**隠密レビュークリーン（2026-07-11）**：3観点（SelectedItem直接バインドのnull安全・ResolveEntry
移設の挙動等価・案B不採用のスコープ遵守）とも問題なし、境界値テストのカバーも適切、隠密実測で
全合格確認。
**忍者実機全観点OK（2026-07-11、6件一括検証）＝完全Done**（配置場所の見た目はスクショ証跡あり、
実機記録はT-052節参照）。

### T-053 機器表「種別」列の日本語表示化 — 完全Done（2026-07-11）

**起票=P-020を殿裁定で承認（案A採用+PDF表記統一）**。ElementKind→DeviceClassマッピング自体は
T-045増分Bで実装済みと判明（隠密調査による前提修正）、残るは表示のみ。案A=「重大度」列の既存
`DiagnosticSeverityToTextConverter`と同型のConverter新設。**日本語ラベルはPDF出力の既存表記と
統一する（殿裁定）**。案の詳細=`docs/archive/ecad2-uiux-proposals-p017-p020-p023-onmitsu.md`
**実装完了（2026-07-11）**：コミット9f54aac。`DeviceClassToTextConverter`新設・種別列へ適用。
文言一致の実現手段=侍の技術判断で**Core層`DeviceClassLabel`のpublic化＋App層から直接参照**を採用
（コピー方式は二箇所保守の追従漏れリスク、DiagramRendererは既にApp層から広く参照される公開クラス
ゆえ層構造と整合、との理由付き）。Core64件・App383件（+18）全合格、差分4ファイル。
**隠密レビュークリーン（2026-07-11）**：3観点（同型性・public化の依存方向正当・PDF側不変+文言一致
テスト済み）とも問題なし、隠密実測で全合格確認。
**忍者実機全観点OK（2026-07-11、6件一括検証）＝完全Done**（実機記録はT-052節参照）。

### T-052 未解決PartIdフォールバックのDRC警告追加 — 完全Done（2026-07-11）

**起票=P-017を殿裁定で承認（案A採用）**。`PartResolver`が未解決PartId（参照切れ）を警告なく
`ElementKind.ContactNO`へフォールバックする既存挙動に対し、DRCへ新規警告項目を追加（既存の
出力パネル・ジャンプ機構を流用、実装コスト小）。案B（プレースホルダ描画）は不採用。
案の詳細=`docs/archive/ecad2-uiux-proposals-p017-p020-p023-onmitsu.md`
**実装完了（2026-07-11）**：コミット9393b8f、Core48件（基準45+3）・App344件全合格。隠密レビュー
（code-reviewスキルmedium併用）で手動観点3点は適合と確認の上、要修正候補3件・経過観察4件を検出
（`docs/archive/ecad2-t052-review-onmitsu.md`）。家老仕分け＝3件とも範囲内欠陥として往復1周目で侍へ采配：
(1) JumpTo誤ジャンプ（`OutputPanelViewModel.cs:96-98`、DeviceName未入力要素で診断クリック時に
別要素へ誤って選択移動、通常UI操作で到達可能） (2) 文言不自然（`DesignRuleCheck.cs:278-282`、
DeviceName空だと「機器 : 〜」と表示、`CheckSeriesCoils`の"(無名)"フォールバック先例に倣う）
(3) 判定ロジック重複（`DesignRuleCheck.cs:276`、`PartResolver.cs`内3箇所と同一判定を独自複製、
共通ヘルパー抽出）。経過観察4件（大文字小文字比較・DRC走査重複・OrderBy重複・e.Kind前提依存）は
実害軽微/経路無しにつき対応不要、proposed.mdへ記録不要（軽微な観察記録のみ）。
**往復1周目修正完了（2026-07-11）**：コミットd4bad3d。#1は`PartResolver.IsUnresolvedPartId`経由で
JumpTo判定を修正、#2は`CheckSeriesCoils`先例の"(無名)"表記へ統一、#3は`PartResolver.IsUnresolvedPartId`
へ一本化しDesignRuleCheck/JumpTo双方から参照。Core53件（+5）・App346件（+2）全合格、範囲外検出なし。
**隠密再レビュークリーン確定（2026-07-11）**：3件とも往復2周目の指摘なし（`docs/archive/ecad2-t052-review-onmitsu.md`4節）。
**忍者実機全観点OK（2026-07-11、6件一括検証）＝完全Done**。検証中にDataGrid UI仮想化がUIA探索の
全行取得を妨げる罠を検知（誤診断未遂、殿の指摘で解消、副次知見としてスキル追記候補）。
実機記録=`docs-notes/ecad2-t052-t059-t055inc3-t053-t054-t056-realmachine-verification-ninja.md`。

### T-051 シート追加・削除操作をUndo対象に含める — 完全Done（2026-07-11）

**起票=P-032（忍者T-041増分5実機確認）を殿裁定で承認**したが、着手前の侍調査（2026-07-10）で
**前提が崩れた**：ecad2にはUndo機構自体が未実装（UndoStack・Undoコマンド・Ctrl+Z配線とも不在、
`MainWindowViewModel.cs:82-84`の注釈にも「Undo機能自体が未実装」と明記。メニュー「元に戻す」は
表示のみ未配線）。家老の独立grepでも確認。**忍者の実機再検証（2026-07-10、4試行すべてUndo不作動、
`docs/archive/ecad2-t051-precheck-undo-verification-ninja.md`証跡9枚）で確定**——P-032/P-042の原観測は
誤認と忍者自身が訂正済み。副次所見=「元に戻す」「やり直し」ボタンが操作履歴皆無でも常時
IsEnabled=True（履歴と非連動）——Undo基盤新設時に併せて対処すべき事項として記録。
「シートのみ追加の小修正」ではなくUndo/Redo基盤のゼロからの新設＝構造的大仕事となるため、
一旦保留（殿裁定2026-07-10）としていたが、**殿直接指示（2026-07-11）「先行でUndo機能を検討して
ほしい」により再開**。まずはスコープ再定義＝基盤新設の設計調査（隠密へ委譲、2026-07-11）から着手。
**設計調査完了（2026-07-11、`docs/archive/ecad2-t051-undo-redo-design-survey-onmitsu.md`）**：
(1) GuiEcadに前例あり（`IUndoCommand`+`CommandHistory`、コマンド逆操作記録型、App層約700行28種類、
要素配置〜配線〜グループ枠〜シート構造まで広く網羅、ただしテスト0件） (2) ecad2現行はModel層
mutable POCO・DeepCloneは葉ノードのみ・Command層は`RelayCommand`直書きで統一規律なし、
MarkDirty同型の「記録漏れ」構造リスクあり (3) 設計3案（A=コマンドパターン/コスト大、
B=Memento/Sheet単位/コスト中、C=GcadSerializer流用の全体スナップショット/コスト小〜中）、
隠密所感=C推奨 (4) 段階導入MVP3候補（候補1=シート追加削除のみ[起票背景P-032に直結、案Cと相性良]、
候補2=要素配置削除、候補3=行挿入削除[T-055直後ゆえ仕様確定待ちが妥当]）、隠密所感=候補1推奨。
不明点：GroupFrameのApp層コマンド有無未発見、Params/Comment専用セッター有無未確認、
案B/Cの実性能未計測。
**殿裁定確定（2026-07-11）**：設計方式=**案C（GcadSerializer流用の全体スナップショット）**、
MVP対象範囲=**候補1（シート追加/削除のみ）**。次段取り=侍が実装計画を起草（T-055方式踏襲：
計画書起草→殿裁可→実装）。あわせて副次所見（「元に戻す」「やり直し」ボタンが履歴皆無でも常時
IsEnabled=True）も本基盤新設時に対処する。GroupFrame/Params/Comment関連の不明点はMVP範囲外
（シート単位操作のみ）につき今回は追跡不要。
**実装計画起草完了＋開かれた論点4点の裁定（2026-07-11）**：計画書=
`docs/archive/ecad2-t051-implementation-plan-samurai.md`（UndoManager新設・RecordSnapshotは操作直前・
Redo履歴クリアは新規操作時のみ・ApplyUndoRedoSnapshot新設[ReplaceDocument不流用]・UIは既存
プレースホルダへのCommand結線のみ・IsEnabled連動はCommandManager.RequerySuggested自動）。
論点裁定：(1)巻き戻し範囲=**シート構成のみ復元**（SelectedCell/Tool状態/StatusMessageは現状維持、
殿裁定） (2)IsDirty=**Undo後も未保存扱いのまま**（保存時点追跡はMVP過剰、殿裁定） (3)DoD(2)の
「Insert」=家老のDoD文言の綾であり、RenameCommand内部のInsert（シート数を変えぬ実装詳細）は
対象外という侍解釈が正（家老裁定） (4)ボタン配置=既存プレースホルダ結線のみで新規UI無し、
UI/UX分岐なしと確認（家老承認）。**実装GO（2026-07-11）**。
**実装完了（2026-07-11）**：コミット0693755。計画書1.1〜1.4節どおり（UndoManager新設・
UndoCommand/RedoCommand・ApplyUndoRedoSnapshot[シート構成のみ復元+MarkDirty維持=論点1・2裁定
どおり]・Add/DeleteCommandへRecordSnapshot・既存プレースホルダ結線+Ctrl+Z/Y・IsEnabled自動連動）。
テスト=UndoManagerTests単体10件+UndoRedoCommandsTests結合6件。計画書2節の1項目（Undo後IsDirty=true
専用テスト）のみ、IsDirty setterがprivate＋直前操作が既にMarkDirty済みでRED証明可能な形にできず
見送り（技術的縮小、テストファイル内コメント明記）——隠密レビューで妥当性確認要。
Core64件・App365件（+16）全合格、差分7ファイル、計画書スコープと完全一致。
**隠密レビュー（2026-07-11、`docs/archive/ecad2-t051-review-onmitsu.md`）＝重大4件、全CONFIRMED**：
#1（データ破損・最重要）=`ReplaceDocument`（新規/開く）がUndoManagerをクリアせず（Clear手段自体も
未実装）、文書A編集→文書Bを開く→Ctrl+Z→Aの内容が復元→Ctrl+SでBのファイルへA内容を上書き保存＝
**データ消失経路**。#3（データ破損）=DeviceNameBox編集中のCtrl+Z/Yに`CommitDeviceNameEdit()`ガードが
無く（既存Ctrl+S/O/N=T-049にはあり）、未確定入力の消失または無関係要素への誤書き込み。
#2（表示不整合）=Undo/Redoの度に左パレットのシート選択ハイライトが崩れる（ResetSheetsのClear()が
Reset通知→WPF Selectorが選択破棄、SelectedSheet通知不発火）。#4（表示不整合）=DRC実行後の
Undo/Redoで存在しないシートを指す診断が出力パネルに残留（`OutputPanel.ClearResults()`未呼び出し、
T-019で対処済みの「沈黙」不整合の再発）。根本＝ReplaceDocumentの「文書差し替え時の状態リセット
責務」がApplyUndoRedoSnapshot新設時に横展開されず4箇所に分散して漏れた。IsDirtyテスト省略は機能上
妥当と確認（軽微指摘のみ）。
**家老仕分け＝4件とも範囲内欠陥として往復1周目**。データ破損2件を含むゆえ忍者実機確認より修正先行
（増分3と同じ流れ）。制度【MUST】適用：隠密テスト設計起草→侍修正＋RED証明。
**テスト設計起草完了（2026-07-11、`docs/archive/ecad2-t051-bugfix-test-design-onmitsu.md`）**：
#1=UndoManager.Clear新設前提、RED中核=NewDocument後CanUndo=false（単体4+結合2件、LoadFromFileは
同一ReplaceDocument経由ゆえ追加不要と整理）。#2=RefreshSelectedSheet活用前提、既存
SelectedSheetNotificationTests（T-050のPropertyChangedForTestフック）へケース6統合、意味論整理=
Undo/Redoは「クランプされた選択位置」を維持するのみ（元のシートへは戻らない）、境界値4件。
#3=**既存Ctrl+S/O/Nにも専用テストが皆無**（MainWindow.xaml.cs直接操作テストが技術的に不可）と
判明、テスト設計対象外としコードレビュー静的確認+忍者実機確認で担保する整理（観点明記済み）。
#4=RED中核=DRC実行後Undo→Diagnostics.Count==0、境界値4件。侍へ往復1周目采配（T-056完了後着手）。
**往復1周目修正完了（2026-07-11）**：コミット8b1b734。#1=UndoManager.Clear()新設+ReplaceDocument
入口で呼び出し、#2=ApplyUndoRedoSnapshotへRefreshSelectedSheet追加、#3=Ctrl+Z/Yケースへ
CommitDeviceNameEdit()追加（Ctrl+S/O/N同型）、#4=OutputPanel.ClearResults()追加。RED証明3件実測。
Core64件・App410件（+15）全合格。往復中に侍がテスト設計側の誤り（S-B2/S-B4、AddCommandの
BeginInvoke自動選択移動=T-050既存仕様の考慮漏れ）を発見・訂正。
**隠密再レビュー（2026-07-11、`docs/archive/ecad2-t051-round2-review-onmitsu.md`）**：4観点妥当（設計訂正も
コード実物裏取りでバグ隠蔽に非ずと確認）。**ただし新発見1件CONFIRMED**——`ApplyUndoRedoSnapshot`が
殿裁定（SelectedCell現状維持）に反しUndo/Redoの都度SelectedCellを無条件nullリセット（初回実装
0693755由来の既存バグ、隠密自身の初回レビュー見落としと率直に自己申告）。PLAUSIBLE1件（デバイス名
編集確定直後のUndoで見た目上消える）は忍者実機観点へ追記。code-review10角度中9角度完了
（1角度はAPI上限で中断、往復2周目の再々レビュー時に補完予定）。
**家老仕分け＝殿裁定への明確な違反ゆえ範囲内欠陥、往復2周目（上限内）で侍へ差し戻し**。
制度どおり隠密テスト設計起草→侍修正＋RED証明。
**往復2周目修正完了（2026-07-11）**：コミットf2aaaad。隠密設計
（`docs/archive/ecad2-t051-selectedcell-bugfix-test-design-onmitsu.md`）どおり、ApplyUndoRedoSnapshot内で
SelectedCellを退避→SetCurrentSheetIndexCore→復元の局所対応（本体無変更）。T-selcell-1〜4全実装、
RED証明実測=1/2/3がFAIL・4はPASS（設計書想定どおり）。Core64件・App414件（+4）全合格。
設計書§4のスコープ外事項（Tool状態/記入中ドラフト）は指示どおり不触。
**隠密再々レビュー（2026-07-11、`docs/archive/ecad2-t051-round3-review-onmitsu.md`）**：主目的
（nullリセット解消）は達成、T-selcell-1〜4設計書どおり・RED証明想定どおり・全合格。code-review
残り1角度（language-pitfall）補完で**新規PLAUSIBLE1件**——SelectedCell復元がGrid.Rows範囲クランプ
（FinishRowCountChange）を経由せず、シート追加Undo+その後の行数拡張の組合せでSelectedCellが
範囲外座標を指しうる（クラッシュ無し・表示不整合止まり、verify確認済み。**往復2周目自体が持ち込んだ
新規回帰**=8b1b734時点では経路自体が不存在）。既知の残課題（記入中ドラフト無警告消失、設計書§4で
当初からDoD範囲外）も現状のまま。
**殿裁定（2026-07-11）＝往復3周目で修正してからクローズ**（新規回帰を知りながら残さない方針）。
制度どおり隠密テスト設計→侍修正＋RED証明→再レビュー→忍者実機。T-062着手はその後へ。
**往復3周目修正完了（2026-07-11）**：コミット8a6eb13。隠密設計
（`docs/archive/ecad2-t051-selectedcell-clamp-test-design-onmitsu.md`）どおり
`ClampSelectedCellToSheetRowsヘルパー`新設・復元代入をRowクランプでラップ（基準=復元後
CurrentSheet、Column対象外、FinishRowCountChange本体不触）。T-selclamp-1〜5全実装、RED証明実測=
1/2/4/5がFAIL・3はPASS（設計書想定どおり）。Core64件・App419件（+5）全合格。
**隠密最終レビュー（2026-07-11、`docs/archive/ecad2-t051-round4-final-review-onmitsu.md`）＝通常観点(a)〜(d)
全て妥当・クリーン確定**（ヘルパーはRowのみ純粋関数・RED整合・クランプ基準の実行順序正・PLAUSIBLE
経路の封鎖を実測確認）。
**忍者実機8観点全OK（2026-07-11、`docs-notes/ecad2-t051-realmachine-verification-ninja.md`）＝
完全Done**。基本Undo/Redo・IsEnabled連動（積年の副次所見解消を実機確認）・#1〜#4修正・
SelectedCell維持+クランプ（T-selclamp-1相当の実機裏付け）まで回帰なし。(h)のPLAUSIBLE事項
（デバイス名編集確定直後のUndoで見た目上消える、Redoで復元可・恒久喪失でない）は実機再現→
P-055としてpending記録。副次発見（UIA InvokePatternがモーダル制約を無視して背後ボタン連打可→
同一ダイアログ3枚重なり）はスキル追記候補として忍者へ采配。
**Stryker手動棚卸しは技術的障害で延期（家老裁定2026-07-11）**：Ecad2.App（WPF）側が「重複する
Compileアイテム」（NETSDK1022系）でDesignTimeBuild解析に失敗しmutation実行不能（obj/bin完全削除・
_wpftmp系2717個削除・2回再試行とも解消せず。Core側は正常=score7.43%/49秒。Stryker内部Buildalyzer
とWPF DesignTimeBuildの既知の相性問題と隠密Web調査で判明）。T-046/T-050では同一手順で正常動作
実績→**家老の原因仮説=本日導入の.NET 10 SDK（10.0.301）**（global.json無しゆえBuildalyzerが
SDK 10でDesignTimeBuildを走らせるようになった＝時期が完全に符合）。**T-062（net10移行）完了後に
再試行し、T-051変更領域の棚卸しをそこで実施**（移行でSDK経路が正式化され解消する見込み。なお失敗
するならglobal.json/csproj対処を侍へ采配）。テストコード静的レビューは最終レビュー内で実施済み。
次＝忍者実機確認→クローズ。

### T-078 todo.mdの軽量化（完了済みタスクの詳細をアーカイブへ移送） — 完全Done（2026-07-12）

**起票=殿直接指示2026-07-12**「todo.mdも肥大化してきている。要約して縮小して」。現状の実態
（家老の事前検分）：`docs/todo.md`が958行まで肥大化。既存の軽量化運用（2026-07-09殿指示）どおり、
「生きているタスク」節に完全Done/Rejected済みタスクの詳細な経緯（往復記録・コミットハッシュ・
隠密レビュー詳細等）が残ったまま蓄積されている。
内容：(1) 完全Done（またはRejected）となっているタスクの詳細記述を洗い出す（T-051/T-052〜056/
T-057/T-059/T-062/T-063/T-071/T-072/T-073/T-074/T-075等） (2) それぞれの詳細経緯を
`docs/todo-archive.md`末尾へ移送（内容は変更せず移動のみ） (3) `docs/todo.md`側は該当タスクを
1行索引へ置き換える (4) 実装順ロードマップ表は触らない (5) 進行中タスクは対象外。
侍へ委譲、スコープ境界：内容の要約・改変はしない、移動のみ。
**完了確認（2026-07-12、侍報告）**：todo.md実測435行・archive側822行、958→435行の軽量化を
実施済み（15タスク分の詳細をarchiveへ移送、コミット2b49829）。

### T-076 docs/配下の整理（完了済み調査書のアーカイブ化） — 完全Done（2026-07-12）

**起票=殿直接指示2026-07-11**「docs/が肥大化してきたので整理してほしい」。現状の実態（家老の事前
検分）：`docs/`直下に173個のMarkdownファイルがフラットに並び、大半が過去タスク（T-XXX）ごとの
調査書・レビュー書・実装計画書。**殿裁定（プレビュー提示で選択）**：完了済みタスクの調査書を
`docs/archive/`へアーカイブ、進行中タスクの調査書のみ`docs/`直下に残す方針。
内容（2段階）：(1) 完了・取り止めタスク索引＋本文中「完全Done」記載タスクに対応するファイルを
洗い出し、アーカイブ対象一覧・残留対象一覧を作成し家老へ提示 (2) 家老確認後、`docs/archive/`を
新設し`git mv`で移動（履歴保持必須）、`docs/todo.md`・`docs/todo-archive.md`・`docs/README.md`・
`docs-notes/`配下の参照リンクを全て新パスへ更新。侍へ委譲（2026-07-11）。
**完了確認（2026-07-12、侍報告）**：段階1・2とも完了済み（コミット9d8e648起票→0ce03ef
「段階2-136件をarchiveへ移動・リンク全更新」）。173→37件、136件を`docs/archive/`へ移動、
`docs/todo.md`・`todo-archive.md`・`README.md`・`docs-notes/`配下のリンク327箇所を新パスへ更新済み。

### T-065 ドキュメント情報の編集UI — 完全Done（2026-07-12）

**起票=棚卸しB区分**。`DocumentInfo`（タイトル等）はCore完備・保存形式にも含まれるが、編集UIが
無い。規模小〜中（プロパティダイアログまたはパネル）。UI形式は着手時に殿確認。
**着手前調査完了（2026-07-11、隠密、`docs/ecad2-t065-t066-pre-investigation-onmitsu.md`）**：
`DocumentInfo`は8フィールド+改定履歴(`Revisions`)、既存編集UIなし。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①UI形式＝**案A（`RenameDialog`等同型のプロパティ
ダイアログ新設）** ②改定履歴（`Revisions`）＝**編集対象に含めない**（8フィールドのみ、規模「中」の
まま） ③メニュー配置＝**新規「図面(_D)」メニューを新設し配下に「ドキュメント情報」項目**
（「ファイル」流用は不採用）。新規トップレベルメニュー追加につき規模がやや増える点は実装時に留意。
**実装完了（2026-07-12、侍、コミットfe11e30）**：新規「図面(_D)」メニュー→「ドキュメント情報」、
`DocumentInfoDialog`新設（`RenameDialog`同型）、8フィールド編集・Revisions除外・保存でMarkDirty連動。
**隠密静的レビュー（2026-07-12）**：D＝nullガードがDate欄のみで他7欄が無防備（中）を指摘、殿裁定
内容との整合は問題なし。**侍が修正（コミットfd67ed9、往復1周目）**：8欄全て`?? ""`で統一。
**隠密再レビュー＝クリーン確定**。
**忍者実機確認＝OK**（8フィールド編集・保存・キャンセル・Revisions非含有を確認）。
なお実装完了後、忍者実機検証で[[T-079]]（機器表エントリ消失バグ、T-065/T-066とは無関係の既存バグ）
が発覚しクローズ保留となったが、T-079修正完了・再検証OKによりT-065として完全Done確定。

### T-066 機器表のBOM編集（型式/メーカー/数量） — 完全Done（2026-07-12）

**起票=棚卸しB区分**。`Device.Model/Maker/Quantity`はCore完備・PDF出力のBOM表にも使用中だが、
機器表DataGridが`IsReadOnly="True"`で画面から編集不能。規模小〜中（編集可能化＋MarkDirty連動＋
検証）。編集可能にする列の範囲はUI/UX分岐ゆえ着手時に殿確認。
**着手前調査完了（2026-07-11、隠密、`docs/ecad2-t065-t066-pre-investigation-onmitsu.md`）**：
現状は機器名/種別/型式の3列表示のみ、`Maker`/`Quantity`列は画面に存在せず（PDF出力は5列）。
`Device`クラスはsetter有りだがセット元コード無し、`INotifyPropertyChanged`未実装の構造的制約あり。
**殿裁定（2026-07-12、プレビュー提示で選択）＝案A（型式のみ編集可能）**。既存`Model`列の
`IsReadOnly`のみ解除＋`CellEditEnding`での`MarkDirty()`呼び出し追加。規模小・リスク低。
Maker/Quantity列の追加・編集は今回スコープ外。
**実装完了（2026-07-12、侍、コミットfe11e30）**：`Model`列のみ`IsReadOnly`解除、`CellEditEnding`で
`MarkDirty()`連動。機器名/種別列は読み取り専用のまま。
**隠密静的レビュー（2026-07-12）**：要修正・重大2件＝A(型式列編集可能化の副作用でグリッド全体の
行追加・削除が意図せず解放、`CanUserAddRows`/`CanUserDeleteRows`未設定)、B(型式セル編集中の
Ctrl+S/新規/クローズで無警告入力破棄、T-049/P-013の同種再発)。中2件＝C(値未変更でも無条件
MarkDirty)、D(→T-065側)。経過観察3件・REFUTED2件は対応不要。詳細=`docs/ecad2-t065-t066-review-onmitsu.md`。
**侍が修正（コミットfd67ed9、往復1周目）**：A=`CanUserAddRows="False" CanUserDeleteRows="False"`追加、
B=`CommitDeviceTableEdit()`新設し`CommitDeviceNameEdit`と並べて保存/新規/クローズ3箇所へ追加、
C=旧値比較後のみMarkDirty。**隠密再レビュー＝クリーン確定**。
**忍者実機確認（1回目）**：①②はOK、③（型式編集中Ctrl+S等での値保持）に**重大NG**——機器配置直後
にCtrl+S等を押すと直近配置機器が機器表から消失（キャンバス図形は残存、幽霊図形化）を発見。詳細=
`docs-notes/ecad2-t065-t066-verify-ninja.md`。
**Wチェック（並行方式）実施**：侍実測により真因は`PlaceElementAtSelectedCell`が要素追加後に
SelectedElement関連プロパティの変更通知を発火しないことと判明——**型式列編集機能（本タスク）とは
無関係の既存バグ（T-049以降存在、今回初めて顕在化）**。隠密の独立調査（`CommitEdit(Row,true)`の
DataGrid内部メカニズム説）とは結論が食い違ったが、実測を優先し確定（隠密自身も「確証には至らず」と
自認、詳細=`docs/ecad2-t066-p-savebug-investigation-onmitsu.md`）。殿裁定によりT-079として緊急
タスク化・別ID追跡、本タスクはT-079修正完了までクローズ保留とした。
**T-079完了（コミット2442882、詳細は[[T-079]]参照）後、忍者が③④の再検証を実施し全観点OK
（詳細=`docs-notes/ecad2-t079-verify-ninja.md`）＝T-066として完全Done確定**。

### T-079 機器配置直後の保存操作等で機器表エントリが消失するバグ修正（P-058） — 完全Done（2026-07-12）

**起票=P-058。T-065/T-066実機確認（忍者）で発覚**。機器（要素）配置直後、View未更新のプロパティ
パネルDeviceNameBox表示値（前回選択時の古い値、大抵は空文字列）が残ったまま、Ctrl+S等の既存
`CommitDeviceNameEdit()`（T-049対策）がこれを誤ってコミットし、機器名クリア→機器表エントリ削除を
引き起こす（キャンバス図形は残存＝データ乖離、幽霊図形化）。型式列編集機能（T-066）の有無を問わず
発生する既存バグ（T-049以降存在、今回のT-065/T-066実機検証で初めて顕在化）。
**原因診断（侍実測、2026-07-12）**：真因は`PlaceElementAtSelectedCell`が要素追加後に
`SelectedElementDeviceName`等SelectedElement関連プロパティの変更通知を一切発火しないこと。
隠密の独立調査（`CommitEdit(Row,true)`のDataGrid内部メカニズム説）とは結論が食い違ったが、
侍は実測（2実験で再現・特定）、隠密は「確証には至らず」と自認する推測ゆえ、実測を優先し確定。
**殿裁定（2026-07-12）**：緊急タスク化、侍の対処案で即時修正。T-065/T-066はバグ修正・再検証完了
までクローズ保留。
**修正完了（コミット2442882）**：`PlaceElementAtSelectedCell`の機器表登録処理後に
`NotifySelectedElementChanged()`（既存の同種ヘルパー、SelectedElement/HasSelectedElement/
SelectedElementKindDisplay/SelectedElementDeviceNameの4プロパティ通知）を追加、isOr分岐より前に
配置しOR接続の有無を問わず必ず発火。他配置経路（縦コネクタ・配線分断・自由線・接続ドット）は
機器表を操作しないため影響なしとgrepで確認済み。
**RED証明**：git stashで本体修正のみ一時的に戻し、新規回帰テスト
`PlaceElementAtSelectedCell_RaisesSelectedElementDeviceNameChanged`がFAIL
（Collection:["IsDirty"]のみ）することを実測確認、stash popで復元後は全件PASS。
**隠密再レビュー＝クリーン確定**（配置位置の経路網羅性・横展開確認・RED証明の妥当性いずれも確認）。
**忍者実機確認＝全観点OK**（詳細=`docs-notes/ecad2-t079-verify-ninja.md`）。build exit0・
test exit0（Core.Tests 71件・App.Tests 428件、全合格）。

### T-060 PDF出力機能のUI結線 — 完全Done（2026-07-12）

**起票=P-046（隠密、T-055調査時の範囲外気づき）を殿裁定でタスク化**。`Ecad2.Pdf`層
（T-007でGuiEcadから移植済み）は存在するが、ツールバー・メニューの「PDF出力」ボタンに
Click/Commandハンドラが結線されておらず、押下しても何も起きない。
**結線調査を隠密2へ采配（2026-07-11）→出力破損2回検知により§5離脱・調査中断**。引き継ぎ=
`docs-notes/handover-onmitsu2-t060.md`。**調査途中の重要判明**：P-001「PdfExporterが1ページで
打ち切られる」はT-002 PoC限定の簡易実装の話で、本実装Core層`DiagramRenderer`は最初から複数ページ
設計＝**P-001の懸念は現行アーキテクチャに当てはまらず実質解消済み**。
**調査完了（2026-07-11、新・隠密2が引き継ぎ再開し完遂、`docs/ecad2-t060-pdf-ui-wiring-survey-onmitsu2.md`）**：
Core層（CrossReferenceBuilder/DeviceTable/EnableBorder/PaperSize）はGuiEcadと全文一致の移植済みで
追加実装不要。App層に新規実装4点=(1)PDFエクスポートのオーケストレーション (2)保存ダイアログ
(3)メニュー/ツールバーCommand結線 (4)プレビュー機能（WPF新規設計要）。
**殿裁定（2026-07-12、プレビュー提示で選択）**：(1)出力範囲＝全シート常に（GuiEcad踏襲）
(2)プレビュー機能＝今回実装（2段階UI、規模大承知の裁定） (3)枠切替＝UIなし・常に枠あり。
**実装完了（2026-07-12、侍、増分1〜3、コミット364cb0f・923ee29）**：PdfExporter.Export新設
（全シート走査+枠ページ分割+CrossRef+BOM、単体テスト3件）、PdfPreviewCanvas/PdfPreviewDialog新設
（GuiEcadのBuildPageList・ページ送り・ズーム移植）、メニュー/ツールバー結線（2段階UI）。
**隠密静的レビュー（2026-07-12、11経路調査+9件verify、`docs/ecad2-t060-pdf-export-review-onmitsu.md`）**：
要修正・重大5件（全CONFIRMED）＝A(プレビューと実出力の総ページ数不一致=WYSIWYG違反)、
B(Ctrl+P未結線)、C(Enter既定動作が原本の「安全な閉じる」から「出力実行」へ反転)、
D(ズームが絶対サイズ化しA3がはみ出す)、E(生のex.Message表示の再発)。要判断F(直接出力経路の不在)
→P-059起票→**殿裁定=プレビュー経由1本のみで問題ない（追認）**。経過観察G〜L・REFUTED1件。
**侍が修正（コミット44d1250、往復1周目）**：A=PdfPageLayout.Build新設でページ構成を1箇所に集約
（経過観察Hのロジック重複も解消、xref二重計算は経過観察のまま残存）、B=Key.P case追加、
C=既定ボタンを「閉じる」へ、D=ダイアログ幅相対フィット+SizeChanged追従+「幅に合わせる」改称、
E=固定日本語文面へ統一。回帰テスト追加（totalPages=実PDF物理ページ数一致）。
**隠密再レビュー＝クリーン確定**（build/test再実行含む、Core75/App428全合格）。
**忍者実機確認＝DoD8点全てOK（2026-07-12、`docs-notes/ecad2-t060-verify-ninja.md`）**：
重点のプレビュー総ページ数とPyMuPDF実測の実PDF物理ページ数・表題欄印字が完全一致、A3収まり・
リサイズ追従、ロック中ファイル上書きで日本語固定文面のみ、Enter/Esc安全クローズ、いずれも確認。
初回表示ちらつき（隠密申し送り）は実機で明確な違和感観測されず（瞬間事象ゆえ断定不可、記録のみ）。

### T-081 GuiEcad仕様書の作成（ecad2仕様書との比較用） — 完全Done（2026-07-12）

**起票=殿直接指示2026-07-12**「前回、ecad2の仕様書を作成したが、gui_ecadの仕様書も比較するために
隠密2に調査させて」。担当=**隠密2（殿指名）**。
内容：T-075のecad2仕様書（`docs/spec/`、全11領域）と同じ領域区分でGuiEcad原本の仕様を調査・
文書化し、比較可能な形（GuiEcadのみ/ecad2のみ/挙動差の3表）で纏める。増分方式（構成叩き台+
1領域試作→家老確認→残り展開）。
**完遂（2026-07-12、全11領域）**：`docs/spec/guiecad-spec-{placement,menu-toolbar,sheet-document,
undo-redo,wiring,device-table,drc-output,canvas-display,part-management,pdf-testmode,statusbar}.md`
収蔵（コミット6bfd757/db366fd/3731b3f/99abfda）。
**主な発見**：(1)旧T-051調査の「約28種」を実物列挙で36クラスへ訂正 (2)キーバインドカスタマイズは
GuiEcadのみ(JSON永続化) (3)コピペはGuiEcad実働・ecad2未結線の逆非対称(棚卸しC区分の裏付け)
(4)Undo対象範囲がほぼ逆転 (5)ecad2のキーボード配線記入・端点リサイズ・OR配置・配置バーは完全新規
機能と確認 (6)GuiEcadパーツエディタ実在(T-068の参考資料) (7)GuiEcadに用紙収め機構なし(P-060裁定の
根拠) (8)シートツリー並び替え=WinUI3既定挙動のみで動くがモデル同期なし(T-082起票の契機)。
**副次成果**：T-081のPDF領域先出し調査がP-060(グリッド用紙はみ出し)のWチェック突合材料となり、
「GuiEcad踏襲不成立」の確定に寄与。device-table仕様書の鮮度補正(T-066完了反映)も実施。

### T-080 行コメント機能（右母線の右側、GuiEcad踏襲） — 完全Done（2026-07-12、往復3周+殿実機確認）

**起票=殿直接指示2026-07-12**「gui_ecadには右母線のさらに右側に行コメントを記入できたがecad2は
記入できない。印刷にも反映されない。右母線の右側クリックで行コメント記入できるようにして」。
**印刷（PDF出力）への反映も含む**。
**着手前調査完了（2026-07-12、隠密、`docs/ecad2-t080-rung-comment-design-onmitsu.md`）**：
GuiEcad原本=右母線右側**ダブルクリック**→インラインTextBoxオーバーレイ（キャンバス上）、
Enter/Tab確定・Esc取消・フォーカスロスト確定、表示=右母線+2mm/FontSizeMm3.0/折返しなし、
PDF反映済み（画面と同一Renderer）。ecad2側=Core（モデル・RowOpsシフト）・描画（DrawRungComments）・
永続化（GcadSerializer）は**既に完備**、App層のみ皆無——「印刷に出ない」の実体は「入力手段がなく
データが常に空」。実装叩き台=ヒットテスト新規（DiagramRenderer.RightBusXのinternal化等）、
WPFネイティブClickCount==2でダブルクリック検出、入力UIはT-033配置バーの同一Windowオーバーレイ
設計・位置計算(TranslatePoint)流用（規模は単一TextBoxでかなり小）。
**殿裁定（2026-07-12、4点）**：
(1) 記入トリガー＝**ダブルクリック（GuiEcad踏襲）**
(2) 空文字列＝**削除扱い**（GuiEcadの空エントリ残留癖は踏襲しない）
(3) 印刷はみ出し対策＝**文字長20文字以内に制限し、印刷時のスペースを事前確保する**
（GuiEcadの「枠あり時未対応」は踏襲しない、殿指定）
(4) Undo＝**対象外を追認**（T-051 MVP現行方針どおり、他編集操作と同じ扱い）
**実装中間報告（2026-07-12、侍、コミット3b9080f）**：DoD(6)（スペース事前確保）を除く全DoD実装完了
（ダブルクリック→インラインTextBox・確定/取消・空は削除・20文字制限・SetRungCommentテスト6件、
build/test全合格）。DoD(6)は実装検討中に**P-060**（枠ありPDFでグリッド+コメント域が用紙に収まらない
既存欠陥、殿目視+忍者実測+隠密理論+隠密2のGuiEcad照合で確定）が発覚し一時保留→
**殿裁定（2026-07-12）＝縮小フィット採用**（常に枠あり維持、グリッド+行コメント域が用紙幅を超える
場合のみ自動縮小して収める。GuiEcad原本に収める仕組みは無く踏襲不成立、完全新規設計。
調査書=`docs/ecad2-p060-grid-overflow-investigation-onmitsu.md`ほか）。DoD(6)を縮小フィット方式で
実装続行→全DoD実装完了（コミット27d3ba9）。
**隠密静的レビュー（2026-07-12、`docs/ecad2-t080-rung-comment-review-onmitsu.md`）**：要修正・重大
8件CONFIRMED（A=CalcPageScaleが印刷枠5mm内側を未考慮、B=原点基準縮小で余白バランス崩れ、
C=複数ページ分割時に無関係ページも一律縮小、D=主回路シートのmm実座標コンテンツ未考慮で縮小フィット
実質不動、E=編集ボックス表示位置が実描画位置と約4mmずれ、F=編集中IsEnabled非連動、G=ヒットテストの
MainCircuit判定なし、H=クローズ時の無条件FocusCanvasでフォーカス強奪）。経過観察4件・REFUTED3件。
**殿裁定（2026-07-12、追加）＝キーボード経路も追加**：隠密の要判断指摘（起動経路がダブルクリック
一本のみ、キーボードファースト原則との不整合）を受け、選択セルの行に対し**F2キー**でコメント編集
ボックスを開く経路を修正往復と一括で実装（GuiEcad原本にも「コメント編集」キー割当が存在した）。
侍へ修正9件（A〜H+F2経路）を采配（往復1周目）。
**往復2周目〜クローズ（2026-07-12）**：往復1周目実装（`d7ed029`+`c4dd2b1`）・隠密再レビュー
全通過後、忍者実機で「UIA限界」と見えたマウス経路2件を殿の物理確認でNG確定→**診断ログ実測（T-038方式）で
原因確定**。(a)ダブルクリック不成立＝WPF仕様（MouseUp側の`ClickCount`は常に1、Down側でのみ2到達）の
見落としで、判定をDown側へ移設し解消（侍・隠密の独立診断が完全一致、`74d2783`）。(b)窓内クリックで
閉じない＝**現状を仕様化**（殿裁定。クローズ経路はEnter/Tab/Escのみ、`docs/spec/ecad2-spec-canvas-display.md`
へ明記）——round2で一度「閉じる」と誤実測したのは検証報告のためのチャット復帰クリック自体が
非アクティブ化を起こす「観測汚染」で、教訓はスキル`ecad2-ui-automation`とメモリへ収蔵済み。
追加往復（殿裁可）で課題2（ダブルクリック1発目の素通りによる選択巻き添えクリア＝(a)解消で表面化した
既存設計漏れ）・課題3（エディタ位置クランプ基準誤りで左端表示時に画面外描画）も修正（`516a81d`）。
殿直命でステータスバーへ操作説明「行コメント編集中: Enterで確定・Escで取消」表示を追加（`547fe59`）、
診断ログは除去済み（`f8b45d7`）。忍者実機3巡全観点OK+殿仕上げ確認済み。回帰テスト537→546件
（RED先行証明つき、配線回帰は単体で突けぬ限界を隠密が明示し実機確認を最終防波堤としてDoD化）。
範囲外の気づきはP-061〜P-065へ起票済み。

### T-082 シート並び替え機能（殿指摘起票） — 完全Done（2026-07-13、往復3周+忍者実機全観点OK）

**起票=殿指摘2026-07-12**「gui_ecadにはシート並び替えがある」（家老の初期検分「両方に無い」は誤りで、
殿のスクリーンショット提示により訂正——教訓は`framework-default-features-invisible-to-grep`メモリ参照）。
**GuiEcad側の実体**：WinUI 3 `TreeView`の標準機能（`CanDragItems`/`CanReorderItems`既定true）で
ドラッグ並び替えが動く（明示コードは一行も無い）。ただし`DragItemsCompleted`等のモデル同期処理が
見当たらず、**保存順序（`Document.Sheets`）へは反映されない「見た目のみ」の可能性**（静的読解による
推測、実装時に照合。`RebuildNavTree`はモデル順で作り直すため次の再構築で見た目も戻る疑い）。
**ecad2側**：WPFのTreeViewに標準並び替え機能は無く、明示実装が必要——ドラッグ&ドロップUI+
`Document.Sheets`モデル同期+永続化+`PageNumber`採番（`SheetNavigationViewModel`）。GuiEcadと違い
保存順序へ正しく反映させる。
**着手=T-080完了後（殿裁定）**。
**殿裁定（2026-07-12）＝操作方式はドラッグ+キーボード**：ドラッグ&ドロップ（GuiEcad同様の直感操作）
に加え、キーボード操作も実装（キーボードファースト方針と整合）。**キー割当=Alt+上下で確定
（殿裁定2026-07-12、T-080クローズ後の着手前確認）**。いずれの操作でも並び替え結果は保存順序
（`Document.Sheets`）へ正しく反映させる。Undo対象はT-051 MVP現行方針どおり対象外が既定
（シート追加/削除と同種ゆえ実装中に疑義が出たら殿へ諮る）。
**采配時のDoD雛形（家老用、2026-07-12起草）**：(1)ドラッグ&ドロップ並び替え（WPF TreeViewに
標準機能無し、明示実装） (2)Alt+上下のキーボード並び替え (3)両操作ともDocument.Sheetsへ反映
(4)保存/読込で順序保持 (5)PageNumber再採番（SheetNavigationViewModel） (6)単体テスト+build/test
全合格。検証観点=端（先頭/末尾）での移動・シート1枚時・ドラッグ視覚フィードバック・保存読込往復。
ドラッグの視覚表現等のUI/UX分岐は実装前に殿へ【MUST】。参考仕様書=`docs/spec/guiecad-spec-sheet-document.md`。
**殿裁定（2026-07-12、実装着手後の分岐確認）＝ドラッグ視覚表現は案A標準フィードバック採用**：
ドラッグ中カーソル変化+ドロップ位置に挿入線表示+ドラッグ元アイテム半透明化（AdornerLayer活用。
簡易フィードバック案Bは不採用）。なお**GX Works3にはシートの概念自体が無い**（殿直接教示
2026-07-12、侍経由）——本件に「GX Works3踏襲」の参照先は存在しない。
**実装完了（2026-07-12、侍、9c147ce）**：DoD(1)〜(6)全実装、build/test全合格。
**往復1周目（2026-07-12）**：隠密静的レビュー（`docs/ecad2-t082-sheet-reorder-review-onmitsu.md`）で
要修正3件（1=無関係シートの並び替えでも選択再設定が無条件発火し記入中ドラフト含む選択消失〔所見L型〕、
2=DRC診断のPageNumber凍結で誤ジャンプ、3=SelectedSheetのPropertyChanged通知欠落）+経過観察7件+
テスト網羅の穴3点。侍が修正1・3+テスト補強を完遂（1b42fd3、RED証明=修正前FAIL実測、567件全合格）。
修正2は既存規約なし（シート追加/削除では頁番号不変ゆえ並び替えで初顕在化する空白地帯）として
掟どおり差し戻し→**殿裁定（2026-07-12）＝案A:並び替え時にDRC結果を破棄し「DRC結果が削除されました。
DRC再実行してください。」と表示**（クリアすべき診断が存在した場合のみ、表示先はStatusMessage既定）。
派生=P-066（シート削除時のPageNumber非再採番、採番規約の非対称）をproposed.mdへ起票。
**往復2周目再レビュー（2026-07-12、`docs/ecad2-t082-sheet-reorder-review2-onmitsu.md`）**：
修正2・3は正しく機能。**修正1が本質未解決（CONFIRMED、code-review2系統独立検出+隠密裏取り）**——
ガード`newSelectedIndex != oldSelectedIndex`が「添字変化」基準のため、**移動対象自体が選択中シート
（=自分が開いているシートを動かす、最も基本的な使い方）の場合に常時発火**しSelectedCell・記入中
ドラフト消失が再発。既存テストは「移動対象≠選択中シート」の1パターンのみで検出漏れ（569件全合格の陰）。
隠密の修正案方向性=「indexは変わるがCurrentSheetの実体は不変」ケース用に、クロスカット処理
（SelectedCellクリア）を伴わない値更新経路を設ける（RenameCommandの精神の拡張）。
**往復2周超過ゆえ3周目は殿裁可待ち（2026-07-12セッション終了時点の上申事項）**。裁可後の采配は
バグ修正往復案件の制度どおり「隠密テスト設計（「移動対象=選択中シート」ケース必須）→侍実装」の
分離+RED証明で。派生=P-067（DeleteCommandのDRC破棄フック欠如）起票。経過観察の新規数点
（Alt+上下キーリピートのBeginInvoke蓄積・StatusMessage無条件上書き等）はレビュー書参照。
**往復3周目（2026-07-13、殿裁可）＝完全決着**：隠密テスト設計書（`docs/ecad2-t082-fix1-test-design-onmitsu.md`、
核=「実体不変の原則」）→侍実装（043bc1f）＝`SetCurrentSheetIndexWithoutCrossCut`新設、
MoveSheetCommandは常時この経路使用。RED証明=修正前9件FAIL実測（設計書予告どおりP3のみ現行PASS）→
修正後578件全合格。**隠密事後レビュー（`docs/ecad2-t082-fix1-review3-onmitsu.md`）＝指摘なし、
所見L型根治確認、忍者実機へ進めてよいと判定**。派生=P-068（PropertyChanged通知名の[CallerMemberName]
誤爆、既存欠陥の同型再発）・P-069（Alt+下連続操作時の選択ハイライト理論的懸念、忍者実機観察項目）を起票。
**忍者実機確認＝殿帰還まで保留（2026-07-13）**：T-082の発火経路（ドラッグ&ドロップ・Alt+上下キー）は
いずれもninja.mdの掟で殿の実機代行操作を要する種別と忍者が判定、殿ご不在では着手不可。検証用シート
4枚は準備済み、殿帰還後すぐ着手可能な態勢。実装・レビューは全周完了済みで、**残工程はこの実機確認のみ**。
**忍者実機確認完了（2026-07-13、殿帰還後、`docs-notes/ecad2-t082-ninja-verification.md`）＝全観点OK**：
DoD(1)〜(6)全OK（(6)のシート1枚時のみ実機直接確認せずコードガード+端no-op実証からの類推、忍者申告）、
侍未検証申告2点（Alt+上下反応・ドラッグ視覚表現）とも殿操作+目視でOK、往復3周目の核心（移動対象=
選択中シート自身での選択セル保持）を4往復のAlt+上下で確認・修正の実機効果を確認、P-069（連続Alt+下の
瞬間無選択懸念）は発生せず、DRC連動（CR1配置→DRC実行→並び替え→案内表示+出力パネルクリア）も実機確認、
保存/読込でDocument.Sheets反映・PageNumber再採番(1,2,3,4)・順序完全一致を確認。回帰なし・範囲外検出なし。
**T-082完全クローズ**。
**侍の着手時調査所見（2026-07-12）**：シート一覧の実体はListBox（SheetNavList、フラット表示）、
WPFネイティブDragDrop APIは使用実績皆無で新規導入、キーバインドはWindow_PreviewKeyDownのswitch文
（Alt+上下はKey.System/e.SystemKey分岐の同型対応が要る可能性）、GcadSerializerはリスト順そのまま
JSON化ゆえ順序変更のみで保存/読込に自動追従、PageNumber再採番ロジックは新規実装要。

### T-064 画像挿入機能のUI結線 — 完全Done（2026-07-13、往復5周相当+忍者実機全観点OK）

**起票=棚卸しB区分**。`ImageInsert`モデル・`DrawImages`描画ともCore完備（GuiEcadでは挿入/移動/
リサイズ/トレース設定まであった機能）だが、App層に挿入・操作UIが無かった。
**着手前調査（2026-07-11、隠密2、`docs/ecad2-t064-image-insert-design-onmitsu2.md`）**：GuiEcad実物
操作体系＝挿入(専用ツールボタン→FileOpenPicker→ホバー位置配置、長辺120mm上限自動縮小)・選択(クリック
ヒットテスト)・移動(ドラッグ、サブグリッドスナップ)・リサイズ(プロパティパネル数値入力のみ)・削除・
Undo(個別コマンドクラス4種)。ecad2のUndo基盤(T-051、JSONスナップショット方式)は既存`RecordSnapshot`
パターン流用のみで済み、GuiEcadより実装コスト低と判明。
**殿裁定（2026-07-12、プレビュー提示）**：挿入トリガー＝メニューのみ／配置位置＝マウスホバー基準
（GuiEcad踏襲）／リサイズ＝ドラッグハンドル新設（GuiEcadの数値入力のみは不採用）／対応ファイル形式＝
jpg等追加（bmp/pngのみのGuiEcad踏襲は不採用）。
**着手時分岐2件・殿裁定（2026-07-13）**：(1)Undo対象＝画像操作(挿入/移動/リサイズ/削除)もUndo対象に
する（他要素との非対称を許容、既存`RecordSnapshot`パターン流用） (2)配置位置決定方式＝案A・2段階操作
（メニュー選択→ファイル選択→キャンバス上配置待機モード→ホバー追従→クリック確定、既存の記入中ドラフト
と同型の操作感）。

**実装完了・往復1周目（2026-07-13、侍、5a2568a）**：DoD全実装、build/test全合格。
**隠密静的レビュー（`docs/ecad2-t064-image-insert-review-onmitsu.md`、dc0d737）**＝要修正5件CONFIRMED
（最重要=リサイズ最小サイズ制約と境界クランプの適用順序、ほか矢印キーPlaceImage分岐・Escキー条件・
ConfirmImageInsertDraftのSelectedCellクリア・右クリックHitTestImage対応）。侍が5件全修正（5a2568a、
RED先行証明済み、624件全合格）。
**隠密再レビュー往復1周目（`docs/ecad2-t064-round1-review-onmitsu.md`）**＝元指摘5件は正しく解消。
新規CONFIRMED1件（画像がグリッド外上部余白帯にあると右クリックメニュー不出現、修正5のHitTestImage
分岐が既存行範囲外ガードより後段）+PLAUSIBLE1件（リサイズでハンドルをアンカー反対側へ超えると
旧実装の軸反転追従挙動がClampResizeTarget新設で消失、UI/UX判断のため殿確認）。**殿裁定＝軸反転は
退行として復活**。
**往復2周目（侍、965e5d9）**：右クリック範囲外ガード順序修正（画像ヒットテスト優先）+リサイズ軸反転
追従復活（ドラッグ位置基準判定→最小サイズ確保不可の異常時のみハンドル本来方向へフォールバック、
既存の境界外はみ出し解消・最小サイズ確保とも両立）。**隠密フル観点再レビュー（`docs/ecad2-t064-round2-review-onmitsu.md`）**
＝元指摘2件とも正しく解消・DoD達成、5系統独立検証で退行なし確認。新規指摘4件はいずれもseverity低の
経過観察（P-073〜076）、派生提案1件（左右クリックのヒットテスト優先順位ロジック重複、パターン疑いあり、
P-077）としてproposed.mdへ記帳。

**追加往復（T-069往復4周目レビューで範囲外発見、殿裁定で即修正）**：`ReplaceDocument`(新規/開く)が
`CancelImageInsertDraft()`を呼んでおらず、画像挿入ドラフト保持中に新規/開くを実行すると`HasAnyDraft`
残留で右クリック無反応+幽霊プレビューの恐れ（侍調査でCONFIRMED）。侍修正（0f9977e）。**隠密フル観点
レビュー（`docs/ecad2-t064-additional-round-review-onmitsu.md`）**＝修正自体はDoD達成だが、同じ
`ReplaceDocument`内、修正箇所の6行上に**新規発見（CONFIRMED・重大）**＝`SelectedImage`だけクリア漏れ
（台帳PR-01横展開漏れの典型例、「1件を塞ぐモグラ叩き」の実例）。加えて一元化ヘルパー未再利用の構造的
指摘（Altitude、P-080）。**殿裁定＝即修正**。
**再追加往復（侍、dc4cbc1）**：`SelectedImage`クリアを`ReplaceDocument`へ追加。**隠密フル観点レビュー**
＝DoD全項目確認OK・新規指摘なし、この時点でT-064完全決着と一次判定。

**忍者実機一括確認（T-069と合同、545168c、`docs-notes/ecad2-t069-t064-verification-ninja.md`）**：
境界外はみ出し解消・軸反転追従・余白帯右クリック・Esc/Delete/右クリック対称性・新規/開く時のドラフト/
選択クリア、いずれもOK・回帰なし。ただし**新規所見**＝画像選択中に矢印キー(右)を押すと選択解除+セル
(行1/列1)選択という意図不明の挙動（バグか仕様か忍者では判断できず要確認）。
**隠密静的調査（`docs/ecad2-t064-arrow-key-investigation-onmitsu.md`、原因完全特定）**：矢印キー
ハンドラに`SelectedImage`を見る分岐が無く既定の`MoveSelectedCell`へフォールスルー、`SelectedCell`の
setterが無条件で`SelectedImage`をクリアするため発生（台帳PR-01近縁の新しい切り口）。**殿裁定＝他要素
（コネクタ等）と同様に矢印キーで平行移動を実装**。
**矢印キー画像平行移動実装（侍、ba09bed）**：`MoveSelectedImage`/`MoveSelectedImageByKey`新設、他の
`MoveSelected*`と同型パターン。**隠密フル観点レビュー（`docs/ecad2-t064-arrow-key-move-review-onmitsu.md`）**
＝境界クランプ整合性・横展開完全性はOKだが**新規発見（CONFIRMED・最重要）**＝`MoveSelectedImage`が
殿裁定「画像操作は全てUndo対象」に違反し`UndoManager.RecordSnapshot`未呼び出し（参照元の`MoveSelected
ConnectionDot`等がUndo対象外という既存仕様につられた取り違え）。Ctrl+Zで移動1回分でなく挿入前まで
一気に巻き戻る実害あり。制度化提案2件（チェックリスト7項目目「矢印キーによる選択状態自体の平行移動」
・View側`MoveSelectedXxxByKey`が4箇所目複製=PR-07再発）はいずれも家老裁量で即制度化（398a700、
`samurai.md`・`task-implementation`スキル、台帳PR-01/PR-07追記）。
**Undo対応追加（侍、85b8ca3）**：`MoveSelectedImage`へ`RecordSnapshot`追加、他の画像操作系メソッドと
同型。**隠密フル観点レビュー**＝DoD全項目確認OK・新規指摘なし。

**T-064完全決着**。往復1周目〜Undo対応追加まで計7コミット、隠密指摘への迅速な対応と丁寧な検証を確認。
一連の往復で発見・制度化したパターン＝PR-01（横展開漏れ、T-064だけで3実例=SelectedImage初期実装漏れ
5件・ReplaceDocumentクリア漏れ・矢印キー分岐漏れ）・PR-03/PR-04/PR-05/PR-06/PR-07（隠密2棚卸しに伴う
制度化）・P-071（CommitDeviceNameEdit呼び忘れ、軽量案採用）。経過観察・派生提案はP-073〜P-080として
proposed.mdに記帳済み（いずれも実害僅少または範囲外、修正不要）。忍者実機確認は`docs-notes/ecad2-t069-t064-verification-ninja.md`
参照（T-069と合同実施、観点1のリサイズ境界クランプ保留は隠密が「既存テスト4件で担保済み・UIA精度
問題」と確認済み）。

### T-069 右クリックメニュー残り4系統（コピペ/要素/縦コネクタ/枠） — 完全Done（2026-07-13、往復4周+忍者実機確認+殿実機裏取り）

**起票=棚卸しB区分**。右クリックメニュー基盤（T-055増分3のContextMenu）は行操作系のみ実装済み、
GuiEcadにあった残り4系統（コピー・ペースト系／要素操作系／縦コネクタ系／GroupFrame系）が未実装だった。
**着手前調査（2026-07-11、隠密2、`docs/ecad2-t069-context-menu-design-onmitsu2.md`）**：4系統は依存
関係が均質でないと判明——コピペ系はクリップボード機能（C区分・後日判断）に依存し単独対応不可、要素
操作系（削除・コメント編集・機器名変更）とGroupFrame系（T-067前提）は依存先が異なる。**隠密2の分割
提案（家老承認）**：「即着手可能な部分」（縦コネクタ削除＋要素の削除/機器名変更＋右クリックヒット
テスト基盤新設）と「後続待ちの部分」（コメント編集UI新規実装・GroupFrame・コピペ）に分割。**殿裁定
（2026-07-12）**：機器名変更のフォーカス誘導＝`DeviceNameBox`へ自動フォーカス（新規インライン入力
ボックスは不採用）。**家老の裏取り訂正（2026-07-13）**：コメント編集はT-080完了（`RungComment`編集UI
実装済み）により依存解消、即着手可能へ格上げ。

**即着手可能部分の実装完了（侍、`4e049c6`）**：縦コネクタ削除・要素の削除/機器名変更・コメント編集・
右クリックヒットテスト基盤の4系統全実装、578件全合格。
**往復1周目**：隠密静的レビューで要修正3件（CellWidth判定漏れ・DeviceNameBox未確定編集の消失・部品
配置モード中の機器名変更無反応）+要確認1件（右クリックでメニューを開くだけで記入中ドラフトが消える）。
**殿裁定＝ドラフトは保護する方針**（Tool.Mode!=Selectなら右クリック処理自体をスキップするガード）。
**往復2周目（侍、`95f5e36`）**：修正1〜4全て完遂、RED証明、582件全合格。
**往復3周目実装完了（殿裁可済み、侍、`a0ef691`）**：修正1=案B採用（右クリック側でSelectedCellを
`hitElement.Pos`（アンカー位置）へ正規化）、修正2=`HasAnyDraft`新設でガードを実際にドラフト保持中
のみへ限定、修正3=行操作コマンド分岐へ`CommitDeviceNameEdit()`追加。RED証明済み、621件全合格。
**往復3周目事後レビュー**：修正1〜3は正しく解消・設計書とも忠実に一致。**新規要修正2件+パターン
再発2件を発見、往復4周目相当が必要**——(1)ツールバーボタンがドラフトをクリアせずTool.Modeを直接
上書きするため`HasAnyDraft`ガードが真のまま右クリック無反応が別経路で再発 (2)連続配置中に無関係な
要素を確認するだけの右クリックでSelectedCellがアンカー位置へ正規化されEscapeでも作業起点へ戻らない
（実害大）。**パターン再発（3回目・確定、P-071起票）**＝`CommitDeviceNameEdit()`呼び忘れがT-049・
T-066に続き3回目（根本原因は未解決のまま局所パッチ継続）。**パターン再発（疑いあり、P-072起票）**＝
「表示と実行の整合原則」が右クリックのみ達成、左クリック・矢印キー等の既存経路には未適用のまま。
**往復2周超過（2回目）ゆえ往復4周目は殿裁可待ち**。

**往復4周目（殿裁可）**：隠密テスト設計書（`docs/ecad2-t069-fix4-test-design-onmitsu.md`、テスト設計と
実装の分離原則）→侍実装（`fdd101c`）＝共通ヘルパー`CancelResidualDraftForToolSwitch`新設（ツールバー
ボタンのドラフトクリア漏れ対応）・右クリック時点のSelectedCell正規化をメニュー項目実行直前へ遅延
（連続配置中の作業起点保護）。**隠密フル観点レビュー**＝修正1・2とも設計書どおり正しく実装。新規
CONFIRMED1件（メニュー表示中のプロパティパネル/選択ハイライト不整合、severity中、修正2の意図的
トレードオフの副作用）+PLAUSIBLE1件（テスト未実装3件中1件は実装可能だった疑い）は**殿裁定＝現状の
ままクローズ**（P-078/P-079としてproposed.md記帳）。範囲外の新規CONFIRMED発見（`ReplaceDocument`の
画像ドラフトクリア漏れ）は**殿裁定でT-064の追加往復へ切り出し**（T-064節参照）。**これにて隠密レビュー
往復は決着**。

**忍者実機一括確認（T-064と合同、`545168c`、`docs-notes/ecad2-t069-t064-verification-ninja.md`）**：
記入中ドラフト保持中のツールバー切替→キャンセル+右クリック・連続配置中の作業起点保持・削除の対象
整合性、いずれもOK・回帰なし。ただし**新規所見（NG疑い、要確認）**＝`Send-Ecad2Keys`(SendKeys経由)
でEscを送り右クリックメニューをキャンセルするとTool.ModeがPlaceElement→Selectへ変化する（メニュー
外クリックでは維持される）、対照実験で経路により結果が分岐。
**診断ログ調査（侍、忍者）**：`ninja.md`「診断ログ連携」運用に従い侍がログ注入、忍者がUIA操作で
再現しログ採取。生ログにより**決定的な手がかり**＝Escケースでは「ContextMenu.Closed」がEscape到達
より189ms先に発火しており、ContextMenuはEscapeキーではなく`Send-Ecad2Keys`内部の`Set-Ecad2Foreground`
(`SetForegroundWindow`)呼び出しで既に閉じていたと判明。その後独立して届いたEscapeキーが、本来
ContextMenu閉鎖時には不要なはずのメインウィンドウの層2ロジック（配置モード中Escでキャンセル）を
誤って発火させた、という順序が確定。UIA操作固有のアーティファクトである疑いが強く、殿の実機裏取りを
依頼（`ninja.md`「殿代行操作の運用」該当）。
**殿実機確認完了（2026-07-13）**：家老がアプリを起動、ステータスバー表示（「ツール: 」）を基準に
物理操作で3回試行、いずれもEsc後もTool.Mode=PlaceElementを維持（変化なし）。診断ログにも3回とも
「Window_PreviewKeyDown Escape到達」「層2発火」の記録が一切残らず（UIA経由の1回のみに出現していた
ことと対照的）——**UIA操作（SendKeys経由のSetForegroundWindow呼び出し）固有のアーティファクトと確定、
実装バグではないと判明**。修正不要、診断ログは侍が除去。

**T-069完全クローズ**。一連の往復で確立・強化した制度＝新規選択可能状態の横展開チェックリスト
（PR-01、6・7項目目追加）・パターン再発台帳運用（P-071/P-072起票）・診断ログ連携運用（UIA限界を
実機裏取りで確定した実例）。

### T-061 テストモード機能のUI結線 — 完全Done 2026-07-14

**起票=P-051（家老、殿の質問を受けた検分2026-07-11）を殿裁定でタスク化**。Core層Simulation
（`TestSession.cs`・`Netlist.cs`、T-007でGuiEcadから移植済み）は存在するが、App層に一切結線
されていない（メニュー「テストモード(_M)」もCommand未バインド）。design-brief等で「作画/テスト
モードの明確な視覚的フィードバック（色変化・右クリックメニュー分岐・タイマ実時間計時パネル）」が
既に構想されている（`docs/ecad2-ui-ux-design-brief.md`、`docs/ecad2-ladder-reference-systems-survey-onmitsu.md`）
ため、単純な結線に留まらずモード切替UI自体の新規設計を伴う可能性が高い。UI/UX分岐（モード切替の
見せ方・対象操作範囲等）は着手時に都度殿確認【MUST】。
**設計調査完了（2026-07-11、隠密2、`docs/ecad2-t061-testmode-ui-wiring-survey-onmitsu2.md`）**：
Core層（TestSession/Netlist）はGuiEcadと全文一致移植済み・`DiagramRenderer.Render`もsim引数対応済み
で追加実装不要。**App層新規実装7点**（モード状態管理・切替UI結線・通電色分け設計・入力操作結線・
右クリック強制ON/OFFメニュー・実時間タイマパネル・ステータス連携）、段階導入5段階の叩き台あり。
**重要発見**：メニュー「テストモード」の`InputGestureText="F5"`表示がa接点配置（F5実利用中）と
矛盾——実装前に解消必須（P-053起票）。設計指針の隠密2所感=GuiEcadの`_testMode`単純boolを踏襲せず
上位enum（AppMode等）一元管理がToolState.csの既存教訓と整合的。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①F5表示矛盾＝**メニュー表示を削除**（`InputGestureText`
除去、新規キー割当はしない） ②モード管理方式＝**上位enum新設（例：`AppMode`）**（design-briefの
状態一元化方針と整合） ③通電表示配色＝**LDmicro式（通電=赤/非通電=グレー）** ④モード切替の
見せ方＝**ツールバーToggleButton+背景色変化（GuiEcad踏襲）** ⑤シートまたぎ状態保持＝**OFF時に
全シート分クリア（GuiEcad踏襲）** ⑥タイマパネル＝**今回実装**（段階導入5段階すべてを今回スコープに
含める）。P-053（F5矛盾）はこの裁定で解決済み。

**往復2周のレビューで重大な追加発見（2026-07-12〜13）**：1周目隠密レビュー
（`docs/archive/ecad2-t061-review-onmitsu.md`）でA/B/C群の重大指摘（キーボード操作全域で
Mode==Testガード皆無・通電表示配色が接点等で機能不全・セレクトSWノッチ順送りが型不整合等）
→殿裁定（全指摘正当）→隠密が修正方針設計書起草（`docs/ecad2-t061-fix-design-onmitsu.md`、
UI/UX確認事項4点殿裁定済み）→侍が8段階増分で実装（`a9008d3`、RED先行証明の精査を経て
`d671d2c`で根本原因修正）→**隠密2周目フル観点レビュー完了**
（`docs/archive/ecad2-t061-fix-review-onmitsu.md`）で致命的な新発見2系統：
- **A-1（セレクトSWの電気的導通判定が構造的に機能しない、T-046由来の既存構造的制約）**：
  実配置経路がセレクトSWを`PartRole.ContactNO`として生成するため`Evaluator.IsConducting`の
  SelectSwitch専用ノッチ判定分岐が到達不可能なデッドコード。**殿裁定（2026-07-14）＝構造対処
  （規模大、`PartResolver`/`NetlistBuilder`の設計変更を伴う）を採用**。**設計調査完了
  （2026-07-14、`docs/ecad2-t061-a1-select-switch-design-onmitsu.md`）**：根本原因確定
  （`PartRole`enumに`SelectSwitch`値が存在せず`PartResolver.ComponentKind`のマッピングパス
  が構造的に無い）、対処は想定より軽量（コア3点+横展開1点）。**要殿確認事項が判明**：
  `PartFolderStore.SeedBasics()`の冪等設計により既存展開済み環境（開発機含む）には自動反映
  されない（T-037既知パターンの再発）。**殿裁定（2026-07-14）＝恒久対応（マイグレーション処理
  新設）をA-1スコープに含める**。**マイグレーション設計完了（2026-07-14）**：隠密が前版の
  事実誤認（T-037恒久対応は実は既に実装済み）を一次情報で自己訂正、既存の`PartFolderStore.
  Enumerate()`内マイグレーションパターンを再利用する形で決着、規模はさらに軽量化（コア3点+
  横展開1点+マイグレーション1点）。設計完結、侍へ実装采配済み。**実装完了・隠密静的レビュー完了
  （2026-07-14、コミット268f6cc）＝設計書どおり正しく実装、correctness bug無し**。新規発見G-1
  （中程度）：テストモード突入直後、Positions未初期化のためセレクトSWの全ノッチが一時的に非導通
  表示になる（クリックした瞬間から正しくなる、A-1が新たに持ち込んだバグではなく既存の設計
  ギャップ）。**殿ご自身の目視確認で「確認OK」（2026-07-14）＝対処不要と判断、決着**。
  詳細は`docs/ecad2-t061-a1-review-onmitsu.md`G-1節参照。**T-061観点5（マイグレーション処理、
  開発機の既存展開ファイル自動補正）も忍者確認OK（出力パネルにエラー・警告なし、機器表正常表示）
  ——T-061は全観点確認完了、完全決着**。
- **A-2/A-3/A-5（機器名編集欄・画像挿入メニューのCanEditDiagramガード漏れ、PR-12確定の端緒）**：
  A-5はテストモード中に画像挿入メニューをクリックすると`HasAnyDraft`が恒久true化しテスト用
  右クリックメニューが二度と出せなくなる重大バグ。**殿承認済み（2026-07-13）、3周目修正完了
  （コミット3a3abc2）→隠密静的レビュー承認可（correctness bug 0件）→忍者実機確認DoD5点全OK
  （2026-07-14）で完全決着**。範囲外の気づき3件はP-081〜083として`docs/proposed.md`へ記録済み。

### T-070 検索・置換機能 — 完全Done 2026-07-14

**起票=棚卸しB区分**。Core/App両層に検索・置換機能は皆無、機器名（デバイス名）変更・置換の
循環ジャンプ導線を想定。GuiEcadではApp層実装のためecad2に論理ごと無し。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t070-find-replace-design-onmitsu2.md`）**：
**最重要発見**——`DeviceRenamer.Rename(doc,from,to)`が既に全シート横断のDeviceName完全一致要素
一括置換＋機器表キー移行まで完結済み（**全置換はこれを呼ぶだけで完結**）。`Find(doc,name)`は
コメントに「検索バーのハイライト用」と明記されながら**App層のどこからも呼ばれていない未使用
メソッド**（grep0件確認）——T-064・T-071と同じ「Core完備・App結線待ち」パターンがここでも再現。
GuiEcad実物＝Ctrl+Fで作図エリア上部にFindBarオーバーレイ、機器名完全一致検索・循環Next/Prev、
ジャンプはビューパン中心化、置換1件は現在要素のみ（機器名整合性は保証しない設計）・全置換は
ループで`RenameDeviceCommand`、検索結果は下部出力パネルの別タブでDRCと同居。新規実装が要るのは
検索状態管理(Index/循環)・トリガーUI・置換1件時の機器表整合処理・検索結果パネル(DRCタブ同居)のみ。
Undo対応は新規コマンドクラス不要（既存`RecordSnapshot`パターン、T-064/T-067と同結論）。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①UI形式・配置場所＝**上部バー（GuiEcad踏襲、
Ctrl+FでFindBarオーバーレイ）** ②検索対象範囲＝**機器名完全一致のみ（GuiEcad踏襲）**（部分一致・
コメント等への拡大は不採用） ③置換1件の挙動＝**GuiEcad同様、現在ハイライト中の要素1件のみ置換**
（常に全置換扱いへの統一は不採用、機器名整合性は保証しない設計をそのまま踏襲）。

**実装完了・往復4周を経て決着（2026-07-14）**：初回実装（コミット`fab6d51`）→隠密1周目レビュー
（要修正9件+sweep3件）→侍修正（`218a769`）→隠密2周目レビュー（新規D-1〜D-5、5件CONFIRMED、
往復2周超過につき殿確認・続行承認）→侍修正（`af9d1fa`）→隠密3周目レビューで新規重大バグE-1発見
（誤爆置換、Undo後のCurrentIndex/SelectedCell不整合でデータ整合性に直結、往復4周目につき再度
殿確認・続行承認）→侍修正（`6184e29`）→**隠密4周目レビューでcorrectness bug無し、決着確認**。
気づき1件（IndexOfSelectedCellOrZeroのシート比較条件の専用テスト不足、緊急性なし）はP-085として
`docs/proposed.md`へ記録。**実機確認**：観点1・2・7 OK。観点3(A-7)はNG疑い（検索バー+配置バー
同時表示でEscape1回無反応）→隠密が根本原因特定（WPF AccessKeyManagerのHandled=true仕様）→侍が
修正（`ClosePlacementBar()`呼び出し追加）→隠密静的レビュー承認可→**忍者実機再確認でA-7決着**。
**観点4（D-1）・5（D-2、殿判断でE-2挙動は修正不要と確定）・6（E-1、誤爆置換なしを確認）も全てOK
——T-070は全観点（1・2・4・5・6・7 OK、3=A-7別途決着）完了、完全決着**。

### T-086 セレクトSWのノッチ番号(Position)設定UI新設 — 完全Done 2026-07-14

**起票=P-086（T-061 A-1実機確認中の殿直接指摘2026-07-14）を殿裁定でタスク化**。セレクトSWの
ノッチ番号（`ParamKeys.Position`）を設定するUIがApp層に一切存在しない（家老検分、grep確認）。
`CycleSelectSwitch`/`NetlistBuilder`は同一デバイス名の複数要素が持つ`Params[Position]`値を巡回
する設計だが、これを書き込む経路が無く、盤面に何個接点シンボルを配置しても全て既定値0のまま
——GuiEcad踏襲のはずの「ノッチ3まで」仕様が実質機能しない。「Core完備・App未結線」の既知パターン
（T-055/T-060/T-061と同型）。
**調査完了（2026-07-14、`docs/ecad2-t086-select-switch-position-ui-survey-onmitsu.md`）**：
GuiEcadはプロパティパネル内Kind別条件分岐の専用数値入力（WinUI NumberBox、0-99）で実装、ecad2は
同種のKind別プロパティUIが皆無につき本件が最初の実装例。侍の副産物調査（サンプル図面のSS1要素は
Params空、Position書き込み経路が皆無と実測込みで裏付け）とも整合。**殿裁定（2026-07-14）＝入力
形式はTextBox+数値バリデーション（DeviceNameBox同型）、範囲制約はGuiEcad踏襲(0〜99)**。副次効果：
本UI完了で`CycleSelectSwitch`のpositions常に空集合問題（T-061観点1判定不能の一因）も解消見込み。
**実装完了・忍者実機確認で完全決着（2026-07-14）**：DoD5点全OK。特に複数ノッチ切替（自動側ノッチ0・
手動側ノッチ1を設定しテストモード検証）で排他的な切り替えが正しく機能することを確認——**T-061
観点1（ノッチ切替での導通/非導通判定不能）もこれにより併せて解消**。SS1のMouseUpガード修正
（Mode==Testガード漏れ、構造バグ）も正常動作を確認済み。

### T-087 ショートカットキー追加（部品パネルF11・テストモードCtrl+T） — 完全Done 2026-07-14

**起票=殿直接指示2026-07-14**。部品選択パネル（`PartSelectionList`）へフォーカスを移す
ショートカットとしてF11、テストモード切替のショートカットとしてCtrl+Tを新規追加する。
- F11：既存のパネル循環（Shift+Tab、対象は`SheetNavList`/`LadderCanvasHost`/`DeviceTableGrid`の
  3つのみ）に部品パネルが含まれていなかった欠落を埋める形
- Ctrl+T：T-061裁定時（2026-07-12）は「メニュー表示を削除・新規キー割当はしない」だったが、
  今回殿より新たな指示。テストモードのON/OFFトグルとして機能させる想定

**実装往復5周を経て完全決着（2026-07-14）**：初回実装（`5595182`）→隠密1周目レビューでbug3件
発見（Ctrl+T HasProjectガード漏れ=**PR-13の2件目・正式パターン確定**、F11パネル非表示中無反応、
F11後Shift+Tab循環漏れ）→侍修正（`f7a8479`）→隠密2周目レビューでさらに2件発見（F11同期呼び出し
のタイミング問題・Shift+Tab循環の呼び出し漏れ、加えてF11自体のガード漏れも新規発見）→往復3周目
につき殿確認・続行承認→侍修正（`bdb7dbb`）→隠密3周目レビューで同型のガード漏れが兄弟経路
（Shift+Tab循環）に残存＋副作用2件発見→往復4周目につき殿確認・続行承認（共有ヘルパー化で対応）
→侍修正（`60fd2c1`、`ActivateAndFocusPartSelection()`新設）→**隠密4周目レビューで「3副作用が
まとめて解消」という侍の主張は誤りと判明**（副作用1のみ解消、副作用2・3=記入中ドラフト破棄・
武装済みToolリセットは未解消のまま）。制度化：PR-13（既存統一ゲートへの新機能接続漏れ）を
`samurai.md`・`task-implementation`スキルへ反映済み。**往復4周連続で同一アプローチ（ガード
追加）が不完全な修正に終わったため、殿裁定（2026-07-14）＝一旦立ち止まり設計自体を見直す**。

**設計見直し完了（2026-07-14、`docs/ecad2-t087-part-panel-focus-design-onmitsu.md`）**：
根本原因＝`ActivateOpenPartSelection()`の2つの副作用（記入中ドラフト無条件破棄・Tool無条件
上書き）を、フォーカス移動という軽量操作から無条件に呼んでいたこと。対症療法（ガード追加）
ではなく構造的解決策を提案——`HasAnyDraft`による操作自体の抑制（記入中は無条件で抑制）＋
`Tool.Mode`が既に`PlaceElement`なら再代入自体をスキップ（武装状態を自然に保持）で3副作用が
一体解消される設計。UX選好2点は**殿裁定＝①記入中の抑制時は無反応（F5〜F10と同じ一貫性）
②Shift+Tab循環で抑制時は次のパネルへ自動スキップ**。

**往復5周目実装完了（2026-07-14、`efbb342`）**：判定ロジックを`ShouldSuppressPartSelectionActivation`/
`ShouldReactivateToolForPartSelection`としてinternal static抽出、既存の`ShouldSkipSelectionInTestMode`
と同型パターンでRED先行証明を実現。**隠密最終レビューでcorrectness bug無し確認**——3副作用とも
「`Tool.Mode=PlaceElement`の代入箇所は直前に必ず`CancelResidualDraftForToolSwitch()`を呼ぶ」
という不変条件のもと構造的に解消されていることを確認。**忍者実機確認DoD1〜5全良好で完全決着**：
プロジェクト未作成時の無反応・記入中ドラフト保持・武装済みTool保持（実配置操作で実効性も実証）・
テストモード中の既存ガード回帰なし、いずれも証跡つきで確認。

副次発見：F5〜F10のグローバルショートカットが`HasAnyDraft`を見ておらず記入中でも配置バーが
開いてしまう既存バグ（T-087実装の方が正しい逆転状況）はP-093として`docs/proposed.md`へ記録。

### T-088 基本図形（Element）の配置後移動機能新設 — 完全Done 2026-07-14

**起票=P-088（実機確認雑談中の殿直接指摘2026-07-14）を殿裁定でタスク化**。基本図形（接点・
コイル等）の配置後移動機能がecad2に無い。**隠密調査完了（2026-07-14、
`docs/ecad2-element-move-feature-survey-onmitsu.md`）**：GuiEcad原本には確かにマウスドラッグ
移動機能が存在（`MoveElementCommand`、キーボード操作なし）するが、「Core完備・App未結線」
パターンには該当せず、GuiEcad自身もApp層のみの軽量実装（`ElementInstance.Pos`は元々単純
setter、Core層に移動専用ロジックは無い）。ecad2は既に画像・縦コネクタ・自由線・接続点の
ドラッグ移動をApp層に確立済みで、基本図形だけがこの機構の対象外になっている状態。
**殿裁定（2026-07-14）＝矢印キー移動もecad2独自拡張として対応**（GuiEcad踏襲のマウスドラッグ
のみではなく、ecad2の「キーボードファースト」方針・他要素との一貫性を優先）。
**スコープ境界**：ecad2に範囲選択機能自体が無いため、GuiEcadの複数要素一括移動は対象外
（単一要素移動のみ）。実装方針は既存の画像ドラッグパターン（`IsDraggingImage`/`BeginDragImage`
等）の横展開が自然、CanEditDiagramガード【MUST】・Escapeキャンセル層追加も既存パターン踏襲。

**実装完了（2026-07-14、コミット`b35b42e`）**：`IsOccupied`/`ValidatePlacement`へexclude
パラメータ追加（移動対象自身の占有除外）で解決。マウスドラッグ（既存6系統との競合順序整理済み）
・**矢印キー移動はCtrl+矢印キーに決定**（通常の矢印キー処理がSelectedElement特有の算出方式と
衝突するため、侍が殿へ直接3択提示し決定）・Escapeキャンセル層・CanEditDiagramガードとも実装、
build/test全合格（Core112・App610、新規7件）。**隠密静的レビューでfindings1-5発見・侍修正
（`d88eda3`）→隠密再レビュークリーン→忍者実機確認DoD1〜4全OK——完全決着**。狭トリガー条件の
副次発見2件（P-092、ドラッグ保持中のキーボード操作割込み）は実害軽微につき修正見送り・経過観察。

### T-090 Ctrl+Shift+Up/DownのCanExecute素通り修正 — 完全Done 2026-07-14

**起票=P-090（隠密、T-087再レビュー2026-07-14、PR-13の他箇所への波及）を殿裁可でタスク化**。
`Ctrl+Shift+Up/Down`（`AddRowCommand`/`DeleteRowCommand`）がCanExecuteを経由せず直接Execute
しており、`CanEditDiagram`を素通りする。PR-13（既存統一ゲートへの新機能接続漏れ）と同型だが、
T-087コミット範囲外の既存バグ。
**実装完了（コミット9b49617）**：T-061 A-3のUndo/Redo同型パターンを踏襲しCanExecute経由化。
RED先行証明実測（CanEditDiagramガード一時削除→新規テスト2件REDを実測確認→復元しGREEN）。
build/test全合格（740件）。**隠密静的レビュー通過**（Undo/Redoパターンとの完全同型を確認、
AddRow/DeleteRowCommandのCanExecute自体もCanEditDiagram含むこと確認済み、重大指摘なし）。
**忍者実機確認完了**：通常時（Ctrl+Shift+Up、10→11行に増加、正常動作）・テストモード中
（無反応、行数変化なし）とも確認OK——完全決着。
範囲外の気づきとしてP-096（Ctrl+Shift+Up/DownのIsCanvasFocused非対称性、忍者実機確認中に殿の
実機操作で発覚）を記録（pending、バグか意図的仕様か不明→2026-07-15にT-094として正式起票）。

### T-091 F5〜F10グローバルショートカットのHasAnyDraft見落とし修正 — 完全Done 2026-07-14

**起票=P-093（隠密、T-087往復5周目最終レビュー2026-07-14、副次発見）を殿裁可でタスク化**。
F5〜F10のグローバルキーボードショートカット（ツールバー配置系）のcase guardが
`IsCanvasFocused && CanEditDiagram`のみで`HasAnyDraft`を一切見ていない。記入中（縦コネクタ/
自由線ドラフト中）でも普通に動作し配置バーが開いてしまい、記入中ドラフトはキャンセルされず
宙に浮く。T-087でF11/Shift+Tabに追加した`HasAnyDraft`抑制の方が正しい実装で、F5〜F10側が
既存バグとして取り残されている逆転状況。
**実装完了（コミット9b49617、T-090と同一コミット）**：T-087のF11実装
（`ShouldSuppressPartSelectionActivation`）と同型に判定ロジックを`ShouldAllowShortcutPlacement`
へinternal static抽出、F5〜F10該当箇所全9箇所へ適用（侍自己申告「8箇所」だが隠密実測で9箇所、
カウント相違のみで実害なし）。RED先行証明実測（`!hasAnyDraft`条件一時削除→新規Theoryテストの
該当ケースがREDを実測確認→復元しGREEN）。build/test全合格（740件、既存比+6件）。
**隠密静的レビュー通過**：F5〜F10全9箇所（F5×2/F6×2/F7/F8/F9×2/F10）漏れなく置換済み、
`HasAnyDraft`も縦コネクタ/自由線/画像挿入の3種全て網羅と確認。経過観察2件（P-094：ドラフト中
のRow操作でインデックスクランプ、T-090/T-091範囲外の既存非対称／P-095：新設
`ShouldAllowShortcutPlacement`がF11用ロジックとド・モルガン否定で完全等価、重複実装の軽微
cleanup）はいずれもpending・今回対応不要（→2026-07-15にそれぞれT-092・T-093として正式起票）。
**忍者実機確認完了**：F5〜F10配置バーが縦コネクタ/自由線/画像挿入いずれのドラフト中でも無反応、
ドラフト無し時は従来通り動作——完全決着。

### T-092 ドラフト中の行操作/Undo/Redoによる無警告ズレ確定の防止 — 完全Done 2026-07-15

**起票=P-094（隠密、T-090/T-091静的レビュー2026-07-14の経過観察）を殿裁定でタスク化**。
ドラフト中（縦コネクタ/自由線ドラフト等）にAddRow/DeleteRow/Undo/Redoを実行すると、ドラフトの
行インデックスが無警告でクランプされ誤った行・別シートへ確定されうる不具合。T-090/T-091コミット
由来ではなくT-041/T-055/T-051由来の既存非対称。
**殿裁定（2026-07-15）＝対処方針はブロック方式**：AddRow/DeleteRow/Undo/RedoのCanExecuteへ
`!HasAnyDraft`を追加、T-091と同型の横展開。
**実装完了（コミットe057bde）**：ドラフト種別3種（Connector/FreeLine/Image）×4コマンド=12ケース
全て個別にRED先行証明を実測確認（git stash --keep-index限定退避）、対照ケース2件（ドラフト無し）
は修正前後ともGREEN維持。build/test全合格（754件、既存比+14件）。
**隠密静的レビュー通過**：ドラフト種別×コマンドの網羅漏れなし、RED証明12ケースの妥当性も追加
確認（Execute呼び出し経路をExplore委譲で全11箇所洗い出し、CanExecuteをバイパスする無条件直接
実行の経路は皆無と確認）。
**忍者実機確認完了**：(1)ドラフト中グレーアウト＝縦コネクタ/自由線/画像挿入の3種全てでOK
(2)キーボード経由ブロック＝Ctrl+Shift+Up/Down・Ctrl+Z/Yとも状態不変を確認 (3)回帰確認＝ドラフト
無し時は従来通り正常動作——完全決着。

### T-093 ShouldAllowShortcutPlacement/ShouldSuppressPartSelectionActivationの重複実装統合 — 完全Done 2026-07-15

**起票=P-095（隠密、T-090/T-091静的レビュー2026-07-14の軽微cleanup）を家老裁量でタスク化
（auto-OK、UI/UX影響なし・実害ゼロ）**。T-091新設の`ShouldAllowShortcutPlacement`はT-087の
`ShouldSuppressPartSelectionActivation`のド・モルガン否定と完全等価、重複実装。共通判定への
統合（規模小）。

### T-094 Ctrl+Shift+Up/Down・Ctrl+Z/YへのIsCanvasFocused判定追加 — 完全Done 2026-07-15

**起票=P-096（忍者、T-090実機確認中2026-07-14の殿実機操作で発覚）を殿裁定でタスク化**。
`Ctrl+Shift+Up/Down`（`AddRowCommand`/`DeleteRowCommand`）がF5-F10等の他ショートカットが持つ
`IsCanvasFocused()`判定を持たず、シートパネル等にフォーカスがあっても効いてしまう。隠密の追加
調査（`docs/ecad2-proposed-p094-p095-p096-p098-review-onmitsu.md`）で、同じ並びのCtrl+Z（Undo）・
Ctrl+Y（Redo）も同様に`IsCanvasFocused()`を持たないと判明——Ctrl系複合キーショートカット群に
共通する設計上の特徴の疑い。
**殿裁定（2026-07-15）＝統一する（バグ扱い）**：フォーカス範囲をIsCanvasFocused必須で統一する。
対象＝Ctrl+Shift+Up/Down・Ctrl+Z・Ctrl+Yの4ハンドラへ`IsCanvasFocused()`判定を追加。

### T-093 ShouldAllowShortcutPlacement/ShouldSuppressPartSelectionActivationの重複実装統合 — 完全Done 2026-07-15

**起票=P-095（隠密、T-090/T-091静的レビュー2026-07-14の軽微cleanup）を家老裁量でタスク化
（auto-OK、UI/UX影響なし・実害ゼロ）**。T-091新設の`ShouldAllowShortcutPlacement`はT-087の
`ShouldSuppressPartSelectionActivation`のド・モルガン否定と完全等価、重複実装。
**実装完了（コミット25b1e9b）**：`ShouldSuppressPartSelectionActivation`を
`!ShouldAllowShortcutPlacement(...)`へ委譲する形に統合、呼び出し元9+1箇所・シグネチャとも
無改変。build/test全合格（754件、回帰なし）。
**隠密静的レビュー通過**：ド・モルガンの法則による数学的検証（`!(c && !h)` = `!c || h`）で
旧実装と完全一致、真理値表4パターン全て検算確認。既存`T087FixTests.cs`のTheoryテストが
(canEditDiagram, hasAnyDraft)全4組み合わせを網羅し新実装でも期待値一致することを確認、新規
テスト不要との侍判断は妥当と裏付け。呼び出し元13箇所（定義2+呼び出し元）とも無改変を確認。
**純粋リファクタリング（シグネチャ・呼び出し元とも不変・全真理値表テスト済み）につき実機確認は
省略、家老が妥当性を追認——完全決着**。

### T-094 Ctrl+Shift+Up/Down・Ctrl+Z/YへのIsCanvasFocused判定追加 — 完全Done 2026-07-15

**起票=P-096（忍者、T-090実機確認中2026-07-14の殿実機操作で発覚）を殿裁定でタスク化**。
`Ctrl+Shift+Up/Down`（`AddRowCommand`/`DeleteRowCommand`）がF5-F10等の他ショートカットが持つ
`IsCanvasFocused()`判定を持たず、シートパネル等にフォーカスがあっても効いてしまう。隠密の追加
調査で、同じ並びのCtrl+Z（Undo）・Ctrl+Y（Redo）も同様に`IsCanvasFocused()`を持たないと判明
——Ctrl系複合キーショートカット群に共通する設計上の特徴の疑い。
**殿裁定（2026-07-15）＝統一する（バグ扱い）**：フォーカス範囲をIsCanvasFocused必須で統一する。
**実装完了（コミット245a4ba）**：4ハンドラ（Ctrl+Shift+Up/Down・Ctrl+Z・Ctrl+Y）へ
`&& IsCanvasFocused()`を追加。**副作用発覚**＝Ctrl+Z/Yのcase冒頭にあった`CommitDeviceNameEdit()`
（T-051由来、DeviceNameBox編集中の未確定入力を確定してからUndo/Redoする安全弁）が、
DeviceNameBox編集中はキャンバス非フォーカスとなりcase自体に到達しなくなるため実質デッドコード
化する点を侍が発見・確認要請→**殿裁定＝統一のまま採用**（安全弁自体はUndo/Redoが実行されない
ため実害なし、コードは将来のフォーカス範囲変更に備え残す）。RED証明不可（View層
`Keyboard.FocusedElement`依存、T-090と同型理由）は妥当と判断。build/test全合格（754件、回帰
なし）。
**隠密静的レビュー通過**：4ハンドラ全て正しく適用、副作用の設計判断も殿裁定を正しく反映と確認。
**忍者実機確認完了**：(1)キャンバスフォーカス時4つとも従来どおり動作 (2)シートパネル等
フォーカス時4つとも無反応 (3)DeviceNameBox編集中のCtrl+Z/Y無反応（補足：WPF標準TextBoxの
ネイティブUndo/Redo＝文字入力の取消/復元は独立して働くが、アプリレベルのUndo=シート追加取消等
は発動せず、殿裁定どおりの仕様と確認）——完全決着。

### T-095 ツールバー1段目のラベル表示方式変更（ショートカット表示→機能名表示） — 完全Done 2026-07-15

**起票=殿直接指摘2026-07-15**。ツールバー1段目「行を追加」「行を削除」ボタンのラベル文字列
「Ctrl+Shift+↑」「Ctrl+Shift+↓」（13文字）が共通ボタン幅46pxに収まらず表示崩れ。
侍が短縮表記3案（"CS+↑"等）の比較プレビューを提示→**殿がプレビューを見て方針転換を裁定**：
短縮表記ではなくラベル表示そのものをショートカットから機能名表示へ変更（ショートカットは
マウスオーバー時のToolTipへ、既存"機能名 (ショートカット)"形式をそのまま流用）。**対象範囲＝
ツールバー1段目全体8ボタン**（新規・開く・保存・元に戻す・やり直す・PDF出力・行を追加・
行を削除）へ拡大、殿裁定。
侍が機能名表記2案（案1=正式名称そのまま／案2=簡潔化）を実機検証つきで提示→**殿裁定＝案2
（簡潔化）採用**：新規/開く/保存/戻す/再実行/PDF出力（据え置き）/行追加/行削除。PDF出力の
据え置き判断は簡潔化方針の枠内の技術判断として家老裁量で承認。
**実装完了（コミットd4d10e0）**：ToolTip/AutomationProperties.Nameは無変更（マウスオーバー
要件は既存流用）、TextBlock.Textのみ変更。build/test全合格（754件、回帰なし）。殿プレビュー
確認済み（このまま確定）。
**隠密静的レビュー通過**：対象範囲の限定性確認（1段目8ボタンのみ、テスト/一時停止・2段目とも
誤爆なし）、AutomationProperties.Name/ToolTip無変更でecad2-ui-automationスキルへの影響なしと
確認。
**忍者実機確認完了**：(1)8ボタン全て機能名表示・幅崩れなし (2)ToolTipショートカット併記残存
(3)2段目・テスト/一時停止の回帰なし——完全決着。

### ToolTip表記漏れ修正（自作パーツ→F11・テストモード→Ctrl+T） — 完全Done 2026-07-15

**起票=殿直接指摘2026-07-15**（「テストモードと部品のホバーでショートカット表記がない」）。
家老調査でF11（自作パーツ）・Ctrl+T（テストモード）とも実装済みのショートカットがToolTipに
表記されていないと判明。**調査途中、家老がCtrl+Tを自作パーツのショートカットと誤診断する一幕
あり**（`Key.T`のcase節を中身確認せず前後の文脈だけで早合点）——殿の実機操作「Ctrl+Tでテスト
モード作動できた」という指摘により発覚・訂正。侍の独自確認とも一致し、正しい対応
（F11=自作パーツ、Ctrl+T=テストモード）を確定。正式タスク番号は振らず、殿直接指摘への家老
裁量auto-OK修正として処理。
**実装完了（コミットab99421）**：自作パーツボタン→"自作パーツ (F11)"、テストモードボタン→
"テストモード (Ctrl+T)"へToolTip/AutomationProperties.Name修正、L241の古いコメント（T-061時点
「ショートカット新規割当なし」、T-087以降は実情と乖離）も更新。build/test全合格（754件、
回帰なし）。
**隠密静的レビュー通過**：F11/Ctrl+Tの対応関係・TextBlock.Text不変・他ボタンとの表記形式整合、
いずれも確認。**忍者実機確認完了**：ToolTip表示・ラベルテキスト不変・F11/Ctrl+T機能とも回帰
なし——完全決着。

### T-058 パネル（ツールバー含む）のドック化・フロート配置機能の新規検討 — 完全Done 2026-07-15（全5増分）

**起票=殿直接指示2026-07-11**。当初「ツールバー右端の展開ボタンはフロート機能向けか」との問いから、
家老検分の結果、当該ボタンはWPF `ToolBar`標準のoverflow表示ボタン（ツールバー幅不足時の自動あふれ
格納UI）であり、フロート機能とは無関係と判明（`ToolBarTray`・`ToolBar`とも`IsLocked="True"`で
ドラッグ切り離しは明示的に無効化されている）。殿より「本当にツールバーフロート機能を新規検討したい」
との指示に続き、**「新規作成などのパネルもドック化してフロート配置できるようにして」と範囲拡大の
指示（2026-07-11）**——ツールバー（1段目=新規作成/開く/保存等・2段目=F5〜F10/部品）に加え、
他パネル（左パレット=シートナビ、右パネル=機器表/プロパティ、下部出力パネル）も対象候補となる、
GX Works3的な本格ドッキングペイン系の新規検討へ拡大。
現状：ゼロからの新規検討（前例・仕様とも未確定）。**WPF標準の`ToolBarTray`/`ToolBar`には真の
フロート化（別ウィンドウとして切り離す）能力が無い**（バンド間の並べ替えのみ標準対応）ため、実現には
(a)自前でWindow動的生成・再アタッチを実装する、(b)外部ドッキングライブラリ（AvalonDock等）を
導入する、のいずれかが必要と見込まれる——(b)は新規依存関係の追加でありCLAUDE.md「不要な外部依存を
追加しない」原則・karo.mdの依存関係変更ゲートに抵触するため、**採否は改めて殿へゲート**する。
第一段=隠密による調査（WPF標準機能の限界確認、外部ライブラリ候補の要否・コスト・ライセンス、
GX Works3等参考UIでの類似事例、対象パネル候補の棚卸しと優先度叩き台）。実装はまだ行わない。
**調査完了（2026-07-11、`docs/ecad2-t058-docking-float-survey-onmitsu.md`）**：
(1) WPF標準（ToolBarTray/DockPanel/GridSplitter）に真のフロート化能力なし、自前実装はドロップ先
プレビュー・タブ化・レイアウト永続化等でコスト大 (2) 外部ライブラリ5件比較で**AvalonDock
（Dirkster99フォーク、MS-PLで商用配布可・保守活発・NuGet導入容易）を最有力**と隠密推奨。
Xceed無償版=商用不可、DevExpress/Telerik/Infragistics=年$1000超の商用ライセンス。.NET8明示対応
未確認につき導入前PoC推奨 (3) GX Works3自体もWinForms+商用ドッキングコンポーネント併用と判明
（自前実装せず既製ライブラリに寄る業界先行判断） (4) 対象パネル難易度=左パレット・出力パネルが
低難易度で先行候補、ツールバー中難易度、右パネル（上下分割+状況依存切替）が最難関。
**殿裁定（2026-07-11）＝外部ライブラリ導入で進める**（「自作のコストが大きいので外部ライブラリー
導入で進めて」、依存関係ゲート通過）。採用候補=AvalonDock（Dirkster99フォーク、MS-PL）。
次段取り（実装段が来た際）：.NET8明示対応が未確認のため**導入前PoCから着手**（隠密調査の推奨
どおり。poc/配下で最小ドッキング・フロート化・レイアウト永続化を検証→OKなら本実装の増分計画へ）。
対象パネルの優先順位（左パレット・出力パネル先行が隠密叩き台）は実装段で改めて殿確認。
**PoC完了（2026-07-14、侍、`poc/t058-avalondock-poc/`、コミット26b58e6）**：
(1) AvalonDock 4.74.1は`net10.0-windows`を明示サポート（`ComponentTargetFrameworks`に含有、
隠密の事前確認時点の懸念＝net8非対応は、T-062完了によりnet10化済みのため非該当と確定）
(2) フロート化・再ドッキング（`Float()`/`Dock()`）は左パレット・ツールパレット（F5-F10相当）
とも独立ウィンドウとして正常動作、殿ご下問「ツールパレットもフロート可能か」も実証済み
(3) レイアウト永続化（保存/復元、コンテンツ再バインド込み）を実機確認、構造・コンテンツとも
完全復元。ただし`Deserialize()`単体ではView（コンテンツ）が失われ、`LayoutSerializationCallback`
でのContentIdキー再バインドが必要（実装済み・要件化）
(4) **隠密調査との相違点＝朗報**：`Dirkster.AvalonDock.Serializer.Xml`パッケージはv5.0.0-preview系
のみの新設で4.74.1には存在せず、4.74.1のコアパッケージに`XmlLayoutSerializer`が同梱済みのため
追加パッケージ不要（依存を1つ減らせる）
(5) **T-083向け重要発見**：`ThemeMode=Dark`はウィンドウ全体・標準コントロールはダーク化するが、
**AvalonDockのドッキングクローム（タブ・境界）と中央ドキュメント領域は明るいまま**——隠密の
懸念（UIクローム全体を丸ごとThemeModeに任せるのは危険）を実測で裏付け。AvalonDock独自テーマ
（VS2013 Dark/Light）との組み合わせ検証が別途必要
**VS2013テーマパッケージ採用決定（2026-07-14、殿裁可）**：`Dirkster.AvalonDock.Themes.VS2013`は
**nuget.org公式版を採用**（本家Dirkster99本人所有・最新安定版4.74.1・侍導入のAvalonDock本体
4.74.1と完全一致・依存`Dirkster.AvalonDock >= 4.74.1`・`net10.0-windows7.0`対応済み・匿名取得可）。
殿ご提示のGitHub Packages版（Clear-Bible/AvalonDock経由、第三者フォーク・4.70.5で約4年更新停止・
GitHub Packages配信のためPAT認証必須）は**不採用**（隠密調査、`docs/`配下に調査記録要確認）。
T-083の追加PoC検証（VS2013 Darkテーマ×ThemeMode×DrawingThemeの3層連動）で本パッケージを使用する。

**増分1完了・隠密静的レビュー（2026-07-14、コミットc9b9a9b）**：左パレット(シートナビ)をDockingManager
配下のLayoutAnchorableへ移行。**往復1周目（実機で発覚）**＝`LayoutRoot.RootPanel`は`LayoutPanel`型
必須のところ`LayoutAnchorablePane`を直接子にして`XamlParseException`（ビルドは0エラーで通過、
実機起動で発覚）→単一ペインでも`LayoutPanel`で包むよう修正し解消。build/test全合格(740件)。
隠密静的レビュー結果：
- **CONFIRMED（重要・対処確定）**：`LayoutAnchorable`の`CanFloat`/`CanAutoHide`が既定`true`のまま
未指定。誤操作でシートパレットがフロート化/オートハイド化し画面から消えると、キーボードのみでは
復旧手段がない（View menuにリセット項目もなし）。**忍者実機確認（2026-07-14）で致命的UXと確定**
（左パレットを誤ってドラッグでフロート化させる再現に成功、殿の実機試行でもキーボードのみでは
復旧不可能・マウスでも今回の試行では未成功）。キーボードファースト方針と真っ向から相反する。
**殿裁定（2026-07-14）＝対処方針確定**：全パネル共通の**レイアウトリセット機能を新設**（例：
Ctrl+Alt+R等で「デフォルトレイアウトに復帰」、キーバインドは既存ショートカット規約との衝突確認の
上で侍が確定案を提示）。T-058のDoDに追加、**増分2着手前に実装**（増分1の重大欠陥への対処優先）。
**実装完了（2026-07-14、侍、コミットd86224b）**：`EventManager.RegisterClassHandler(typeof(Window), ...)`
方式でCtrl+Alt+Rを全Windowクラス(メインウィンドウ・AvalonDockフロートウィンドウ)へ捕捉。殿実機確認
でフロート化パネル自身にフォーカスがある状態からの復旧もOKと確定。
**隠密静的レビュー完了（2026-07-14）**：RegisterClassHandler方式は設計妥当と裏付け確認（WPF公式
ドキュメントでクラスハンドラの発火順・既存switch文との二重発火が構造的に起きないことを確認、
AvalonDockの`LayoutFloatingWindowControl`が`System.Windows.Window`を直接継承することも本家ソースで
確認済み）。指摘2件：
- **CONFIRMED（修正要、侍へ采配）**：`typeof(Window)`クラスハンドラは対象を絞っておらず**全モーダル
ダイアログ**（AddSheetDialog等）も捕捉してしまう。ダイアログ表示中にCtrl+Alt+Rを押すと、裏で
メインウィンドウのレイアウトが無言でリセットされる（データ喪失はないが予期しない副作用）。
→**モーダルダイアログ表示中はCtrl+Alt+Rを無効化するガードを追加**（既存の「配置バー表示中」等の
ガード群と同様のパターン）
- PLAUSIBLE（増分2/3着手時のチェック項目）：`RegisterDockingContents`は`ContentId`が`null`の
`LayoutAnchorable`/`LayoutDocument`があるとDictionary代入で`ArgumentNullException`（起動時クラッシュ）。
現状は唯一のLayoutAnchorableに`ContentId`設定済みで実害なしだが、新規パネル追加時の書き忘れに注意
- CONFIRMED（経過観察）：旧Borderの境界線がAvalonDockへ引き継がれず右パネルと非対称な見た目回帰
（AvalonDock既定テーマが境界線非描画の設計と一次確認済み）→**T-083テーマ導入まで現状維持**
- PLAUSIBLE：GridSplitterの`IsTabStop=False`(T-021設計)相当がAvalonDock要素に無し、本家Issue#348で
類似のTabオーダー不具合既知→増分2/3でタブ複数化時に実機確認要
- PLAUSIBLE（軽微）：`LayoutAnchorablePane`の`DockWidth`未設定、PoC前例(全ペインにDockWidth明示)の
踏襲忘れで幅比率が意図と異なりうる→増分2/3着手時に留意
- **狙い撃ち観点で新規発見（AvalonDock一次資料）**：(1)`CollectGarbage()`処理により
`LayoutDocumentPane`を1つも使わない設計では、ドラッグ&ドロップ完了後・フローティングウィンドウを
閉じた後に空の`LayoutDocumentPane`が自動挿入されうる (2)`LayoutPanel`のOrientation混在ネスト処理は
v4.72.0まで既知バグ持ち(PR#431で一部修正)——いずれも**増分2/3(ドラッグ移動・フローティング切り離し・
複数パネルネストが本格化する段)の実機確認観点に追加**
(6) `Dock()`は「元の位置」に戻る保証がなく直前のペイングループへタブ統合される仕様——本実装で
「元位置復帰」UXが要るなら位置管理の設計が別途必要
(7) **忍者実機確認完了（2026-07-14、`docs-notes/ecad2-t058-poc-verification-ninja.md`）＝両方OK**：
マウスドラッグでのタブ切り離し→独立ウィンドウ化は`mouse_event`多段階ドラッグ合成で成立、
再ドッキングも正常動作確認（元位置でなく直前ペイングループへ統合＝PoCコミット記載仕様通り）。
起動直後の白フラッシュは自動連写140フレーム＋殿目視代行（複数回起動）とも「再現なし」で一致。
副次発見：本ドラッグ合成手法は他タスクのUIA自動化にも転用可能な見込み（`ecad2-ui-automation`
スキルへの反映は要否含め後日検討）。回帰・範囲外検出とも無し。
**殿方針（2026-07-14）**：隠密2のAvalonDockデバッグ手法調査（進行中）の結果を踏まえ、**忍者へ
skill作成（`ecad2-ui-automation`スキルの改修、またはドラッグ操作専用の新規skill新設）を依頼する
ことも采配候補に加える**。調査完了後、家老が采配要否を判断する。
**調査完了（2026-07-14、隠密2、`docs/ecad2-t058-avalondock-drag-debug-technique-survey-onmitsu2.md`）**：
(1) AvalonDock本家自身も実マウスドラッグの自動テストは行わず、メニュー操作・UIAパターン・内部API
直接呼び出しで代替（実ドラッグ自動化を正面突破していない）
(2) AvalonDockはUIA標準Drag/DropTargetパターン未実装（`DoDragDrop`・`IDragProvider`使用箇所0件）、
独自`CaptureMouse`+`MouseMove`機構——ゆえに合成マウス入力一択という構造に裏付けあり
(3) **忍者の`mouse_event`多段階合成は特異ではなく他ツール(pywinauto等)でも定石**。改善ガイダンス
発見＝**3〜5ステップの中間移動・各ステップ間50ms待機**（忍者の境界リサイズ実例＝60px/30ステップ/
各50msで初回失敗、距離を増やして再試行し成功、との相違点も参考になる）
(4) AvalonDock独自ログ機構は`#if TRACE`の`ConsoleDump`のみで事実上休眠、ドラッグ専用トレースは無し
(5) 不明点：SendInput合成がドラッグ閾値判定を検知できない旨の"公式"一次情報は未発見（コミュニティ
実践知見+ソース調査からの論理的推論に留まる）
(6) **派生提案（範囲外、着手せず）**：本家同様「実ドラッグではなく内部API直接呼び出しでの検証」
への設計転換も選択肢たり得る——proposed.mdへ記録
**忍者へのskill改修、采配決定（2026-07-14）**：調査結果を踏まえ`ecad2-ui-automation`スキルへ
改修を依頼する（詳細は忍者への采配メッセージ参照）。
**増分1・忍者最終実機確認完了（2026-07-14、`docs-notes/ecad2-t058-increment1-verification-ninja.md`）
＝全DoD OK、増分1（左パレットAvalonDock化＋レイアウトリセット機能）完全決着**：
(1)通常動作の再現性OK (2)モーダルダイアログ表示中のCtrl+Alt+R無反応OK（殿実機操作、シート追加
ダイアログ「維持されている」） (3)**フロートウィンドウ自身にフォーカスがある状態からのCtrl+Alt+R
復旧（殿裁定の核心課題）＝OK**（殿実機操作「戻ったよ」、可視ウィンドウ数2→1・スクショ・
ステータスバー「パネルレイアウトを既定に戻しました」いずれも確認）。次は**増分2（出力パネル）**
へ進む。
**`ecad2-ui-automation`スキル改修完了（2026-07-14、忍者）**：既存スキルへドラッグ操作機構を統合
（新規skill新設ではなくEcad2.App検証と密接なため既存改修を選択）。`helpers.ps1`へ
`Ecad2Native.DragDrop`(多段階マウス合成)+公開関数`Invoke-Ecad2Drag`を新設、`SKILL.md`へAvalonDock
ドラッグの制約（UIA標準パターン未実装・独自DragService・距離100px以上目安）とタブ切り離し・境界
リサイズの使用例を追記。既定値は忍者実測(40ステップ/40ms、隠密2調査の3-5ステップ一般ガイダンス
では届かない場合がある実例と統合)。実機でタブ切り離し動作確認済み。以後の増分2/3実機確認は
本スキル経由で行う。

**増分2完了（2026-07-14、侍、コミット5e1bbc5）**：出力パネルを案Cどおり独立した2つ目の
DockingManagerとして実装。既存T-059 GridSplitterはDockingManager外側リサイズ用として維持。
レイアウトリセット(Ctrl+Alt+R)は複数DockingManagerをまとめて処理するよう汎用化。
**往復1周目（殿実機確認で発覚・修正済み）**：AvalonDockのタブヘッダー自体がTitleを表示するため、
旧実装の手動タイトルTextBlock（左パレット「シート」・出力パネル「出力/検索結果」）が二重表示に
なっていた——**増分1（左パレット）にも同じ問題があったが増分1の検証時点では見逃されていた**。
左パレットは冗長TextBlockを削除、出力パネルはタブタイトル自体へ一本化(`FindVisibleToOutputTitleConverter`
新設)。実機(新規作成・DRC実行・二重表示解消・Ctrl+Alt+R両DockingManager)確認済み、build/test全合格。
**未検証**：検索バー(Ctrl+F)経由での「検索結果」タイトル切替は自動化で届かず、忍者実機確認へ委譲。
**隠密静的レビュー完了（2026-07-14）＝要修正級の指摘なし**。狙い撃ち観点3点(独立性・二重タイトル
修正・レイアウトリセット汎用化)いずれも妥当と確認。経過観察2件：
- PLAUSIBLE：出力パネル上部の境界線も消失（増分1と同型、T-083テーマ導入まで現状維持）
- **CONFIRMED・将来リスク（増分3着手時のチェック項目）**：`_dockingContentRegistry`は全
DockingManager横断の単一辞書でContentId一意性が暗黙の前提（重複検知コードなし）。現状
("LeftPalette"/"OutputPanel")は衝突なしだが、**増分3で右パネル追加時にContentId命名衝突すると
リセット時に誤ったコンテンツが再バインドされうる**——命名重複が無いか要確認
なお前回指摘のCanFloat/CanAutoHide既定値問題は隠密が再検証しREFUTED（「殿裁定済みの事後救済方針
Ctrl+Alt+Rが新パネルにも一貫適用されているだけで対処漏れではない」）、findingsから除外。
**忍者実機確認・中間報告で重大発覚（2026-07-14）**：出力パネルのタブタイトルが**完全に空白**
（二重表示は解消されたが「単一表示」ではなく「無表示」、UIA上もText要素のNameが空、拡大スクショ
`t058-inc2-05-output-title-crop2x.png`で確認）。侍の実機確認（コミットメッセージ「二重表示解消を
確認済み」）では見落とされていた模様。忍者の推測（断定せず）＝AvalonDockの`LayoutAnchorable`は
VisualTree外のモデルオブジェクトゆえ`{Binding Find.IsVisible, Converter=...}`のDataContext継承が
届いていない可能性。**Wチェック方式（2026-07-14家老采配）＝侍修正＋隠密独立調査を並行**。
**(4)検索バー切替の追加実機証拠（2026-07-14、忍者、殿実機操作）＝NG確定**：Ctrl+F表示後も出力
パネルのタブタイトルは空白のまま（「検索結果」表示されず）。一方**グリッドの中身は正しく切替**
（列ヘッダーが`FindResultsGrid`＝「シート」「機器名」「該当箇所」へ変化、DRC結果グリッドとは別物と
確認）——**VisualTree内のDataGrid.Visibilityバインディングは機能するが、AvalonDock内部モデルの
`LayoutAnchorable.Title`バインディングのみDataContext継承が届かず機能していない、との切り分けが
実機で裏付けられた**。(1)(2)前半(二重表示解消自体)OK、(2)後半(単一表示として機能)NG、
(3)Ctrl+Alt+Rリセットは未確認（優先度低、必要なら追って確認）。
**隠密独立調査完了（2026-07-14）＝根本原因解明**（忍者実機証拠と完全符合）：
`LayoutElement`は`DependencyObject`直継承（`FrameworkElement`ではない）。`LayoutAnchorablePane`等の
`Children`コレクションは`ObservableCollection<T>`型の素のCLRプロパティ（DependencyPropertyでは
ない）——`Children.Add(anchorable)`はWPFの自動InheritanceContext確立(`SetValue`経路)を通らない
単純なコレクション操作。ゆえに`LayoutAnchorable`インスタンスはビジュアルツリー・論理ツリー・
InheritanceContextいずれにも繋がらず、`Title="{Binding Find.IsVisible}"`は`DataContext=null`のまま
評価され空白になる。一方`Content`(DockPanel内の実UI)はAvalonDockが描画時に実ビジュアルツリー上へ
配置するため通常のDataContext継承が効く——DataGrid正常/Title不動の切り分けと完全整合。コミュニティ
既知報告あり（Dirkster99/AvalonDock Issue#282、xceedsoftware/wpftoolkit Issue#1062）。
**隠密の前回査定訂正**：「AvalonDockは独自の論理ツリーを維持しDataContextが伝播する」は誤りと
一次資料再検証で判明、率直に訂正。
**修正の技術選択肢（隠密提示、採否は侍・家老）**：(1) ElementNameバインドへ変更（改修最小・
off-tree要素への定番回避策、**隠密推奨**） (2) コードビハインドで明示的DataContext設定
(3) DockingManager標準MVVMパターンへ移行（単一固定Anchorableには過剰）。
**侍の修正完了（2026-07-14、コミット79a60b2）＝Wチェック突合結果：診断一致**。侍の独立診断
（`LayoutContent : LayoutElement : DependencyObject`——`FrameworkElement`ではなくDataContext継承の
対象外）は隠密のより精密な診断（`Children`コレクションが素のCLRプロパティでInheritanceContext
確立経路を通らない）と「AvalonDockのオフツリー構造が根本原因」という点で本質的に符合——Wチェック
の目的（誤った原因への対症療法の検出）は達成。**対処法は侍が隠密案(ElementNameバインド)ではなく
別案を採用**＝コードビハインドで`Find.PropertyChanged`を購読し`LayoutAnchorable.Title`を直接更新
する方式（理由：実装・実機確認済み・増分3の右パネル状況依存切替等への汎用性・オフツリー特性を
根本から回避する直接性）。`FindVisibleToOutputTitleConverter`は不要になり削除。実機で「出力」
テキストの正常表示・空白解消を確認、build/test全合格。**未検証**：検索バー経由の「検索結果」
切替再確認は忍者へ委譲。
**忍者最終実機確認完了（2026-07-14、`docs-notes/ecad2-t058-increment2-verification-ninja.md`）
＝全DoD OK、増分2（出力パネルAvalonDock化）完全決着**：(1)タブタイトル「出力」正常表示OK
(2)検索バー実行時「検索結果」切替OK（先のNGから解消） (3)通常DoD再確認OK (4)Ctrl+Alt+Rリセット
OK。次は**増分3（右パネル、隠密評価「最難関」）**へ進む。
**ドッキング位置制約調査完了（2026-07-14、侍、殿ご下問への回答）**：
(1) 現状レイアウト（左パレット・ツールパレット・中央キャンバス・出力パネル・右パネルの5要素、
横3分割+中央部縦2分割のネスト）完全再現は**可能**（PoCで実証済み、既存XAMLはほぼそのまま
`LayoutAnchorable`/`LayoutDocument`の中身として移植可能）
(2) ドッキング位置の構造的制限は**基本的に無し**（`LayoutPanel`のHorizontal/Vertical自由ネスト、
Grid/DockPanelに近い柔軟性）。右パネルの状況依存切替（プロパティ⇔部品選択）も既存Visibility
バインディングそのまま持ち込み可、隠密の「最難関」評価は実装コスト（移植手間）の見立てであり
実現不可能性ではない
(3) ツールバー管轄は**両方選択可能（技術的制約なし）**——可動化(LayoutAnchorable化)/固定
(現行ToolBarTray維持)いずれもPoCで実証範囲内。**殿裁定（2026-07-14）＝2段目のみ可動化**
（1段目=新規作成/開く/保存等は現行ToolBarTray固定維持、2段目=F5-F10/部品のみAvalonDock管轄内
でフロート/再ドッキング可能にする）
(4) GridSplitter(T-059)は**AvalonDock内蔵リサイザーへの置き換え対象**（重複ではない）。
DockingManager配下の全ペインは自動的にドラッグリサイズ可能になるため、本実装移行時は既存
GridSplitter XAMLの削除が設計上必要
**正直な申告（侍）**：リサイズドラッグ自体の実機動作はSendInput合成の自動化限界で未検証
（タブ切り離しドラッグと同じ制約）。忍者のタブドラッグ実機確認実績から動作すると推定されるが、
念のため追加確認が望ましいとの侍提案あり。
**忍者追加確認完了（2026-07-14）＝OK**：境界線ドラッグでのペインリサイズ、`mouse_event`多段階
ドラッグ合成（100px・60ステップ・各30ms）でThumb座標移動・幅拡大を視覚確認。これにて
**T-058のPoC確認事項3点（タブ切り離し・白フラッシュ・境界線リサイズ）全て完了**。
**殿裁定（2026-07-14）＝進め方確定**：**T-058（ドッキング機能自体）はPoCで十分実証済みにつき
先行して本実装へ進める**。**T-083（ダークモード連動）は上記(5)の課題ゆえ切り離し、AvalonDock
独自テーマ（VS2013 Dark/Light）との組み合わせ検証を別途PoC拡張してから後追いで組み込む**
（T-058本実装が一段落してから着手）。T-058本実装の実コーディング着手は、忍者の実機確認
（上記(7)、ドラッグ切り離し手触り・フラッシュ現象）の結果が出てからとする。
**.NET8対応の事前確認完了（2026-07-11、隠密2、`docs/ecad2-t058-avalondock-net8-precheck-onmitsu2.md`）**：
最新安定版4.74.1（2026-04-25）のTFMは`net5.0-windows`→`net9.0-windows`と飛び、**net8.0-windowsは
明示対象外**（メンテナ本人がIssue#467で確認済みの意図的方針、バグではない）。.NET8プロジェクト
（ecad2該当）は`net5.0-windows`アセットへのフォールバック解決見込みだが**実機動作の一次報告は
未発見**——PoC実機確認の必須性が確定。直接的な.NET8クラッシュ報告なし（不在証明の限界つき）。
テーマは別途7種必須構成（VS2013系が既存意匠と親和性高、隠密2所感）、レイアウト永続化は公式
Serializerパッケージで自前実装不要と判明。PoCで不具合が出た場合の代替選択肢=.NET9化（net9.0-windows
は明示サポート）だが大掛かりゆえ、その時点で改めて殿裁定。
**4.74.1↔5.0.0差分調査（2026-07-11、殿直接指示、隠密2、
`docs/ecad2-t058-avalondock-v5-diff-survey-onmitsu2.md`）**：一次資料=公式移行ガイド
（docs/migration/breaking-changes.md）+実運用者Issue#604/PR#607で裏取り。
**最重要=5.0.0のTFMは`net48;net9.0-windows;net10.0-windows`のみ**——4.74系にあった
`net5.0-windows`緩衝フォールバックが消滅し、ecad2がnet8のままでは5.0への追随自体が困難になる
可能性大＝**T-062（.NET 10移行）を先行させる殿判断の強い裏付け**。朗報=レイアウトXML永続化は
ほぼ完全互換（preview.96で互換レイヤー追加済み）、API面の書き直しは軽微（namespace/パッケージ
変更、公式見積10〜20分）。新機能（MVVM基底クラス・DI統合等）は任意で強制なし。開発は非常に活発
（直近1週間5コミット・実運用フィードバック対応が数日単位）だが安定版時期は不明——**結論：
4.74.1で実装開始してよい（将来の5.0移行コストは小、ただしT-062完了が事実上の前提）**。

**増分3完全決着（2026-07-15）**：
**設計叩き台**（侍、`docs-notes/ecad2-t058-increment3-design-draft-samurai.md`）＝3つ目の独立
DockingManager新設（案C踏襲）、単一`Orientation="Vertical"`の`LayoutPanel`一つのみ（Orientation
混在ネスト非該当）、ContentId="DeviceTable"/"RightPanelBottom"（既存と非衝突）、下段タイトルは
増分2の`UpdateOutputPanelTitle`と同型のコードビハインド直接更新方式を横展開。家老検分＝承認
（ContentId命名等は内部識別子でUI/UX非該当、家老裁量）。
**実装完了（コミット02d8bad・7bdd56c）**：build/test全合格(754件)、実機起動・基本動作
（機器表/プロパティ表示、部品選択切替往復）確認。
**隠密静的レビュー**（`docs/ecad2-t058-increment3-review-onmitsu.md`）＝設計どおり、致命的欠陥
なし。1件CONFIRMED：Ctrl+Alt+Rレイアウトリセット後、タイトル同期メソッドが再適用されずタイトルと
中身に不整合が生じる——**増分2（出力パネル）にも同型の既存欠陥があったと遡って判明**、パターン
再発の疑いとして`docs-notes/pattern-recurrence-log.md`へPR-15候補記帳（同一タスク内2件、他タスク
で3件目が出れば正式パターン化検討）。
**侍修正（コミット4aebaa6）**：`ResetDockingLayoutToDefault()`末尾で両タイトル同期メソッドを
呼び直す方式で増分2・3双方の不整合を同時解消。隠密再レビュークリーン。
**忍者実機確認**：通常動作・Ctrl+Alt+Rリセット（部品選択中・検索結果表示中とも）・機器表データ
反映、いずれもOK。**新規気づき**＝MinHeight未設定のため境界を大きくドラッグすると機器表が完全
不可視（BoundingRectangle=Empty）になりうる（Ctrl+Alt+Rで即座に復旧はする）。
**侍修正（コミットac55c43）**：出力パネル側の既存値`DockMinHeight="80"`を右パネル2ペインへも
追加（PoC前例の踏襲漏れ埋め合わせ、家老裁量でauto-OK判断）。実機確認で203px→53pxクランプを実証
（完全消失なし）、Ctrl+Alt+Rで203px復帰も確認。隠密軽量レビュークリーン。
**忍者最終確認＝全OK——T-058増分3、完全決着**。次は増分4（レイアウト永続化）→増分5
（ツールバー2段目のみ可動化、殿裁定済み）。

**増分4着手（2026-07-15、殿裁定＝増分優先で進める）**：
**UI/UX裁定（殿、プレビュー提示で選択2026-07-15）**：
(1) 保存タイミング＝**両方**（アプリ終了時の自動保存＋「現在のレイアウトを既定として保存」の
明示コマンドも別途用意、Ctrl+Alt+Rのリセット先を明示保存で上書き可能にする）
(2) 保存先＝**アプリ共通設定**（%AppData%配下等、全プロジェクト共通で同一レイアウトを使う、
GuiEcadも同様の方式と見込み）
**DoD**：(1) 3つのDockingManager（LeftPalette/OutputPanel/RightPanel）のレイアウトを保存/復元
する仕組みを実装（PoCで実証済みの`XmlLayoutSerializer`+`LayoutSerializationCallback`方式を
本実装へ移植、`docs/`配下PoC記録参照） (2) 自動保存＝アプリ終了時（`OnClosing`等）に現在の
レイアウトを保存 (3) 自動復元＝アプリ起動時、保存されたレイアウトがあれば復元、無ければ出荷時
既定レイアウト (4) 明示保存コマンド新設＝「現在のレイアウトを既定として保存」、キーバインド・
メニュー配置は侍案を提示 (5) **Ctrl+Alt+Rのリセット先＝明示保存済みの既定レイアウト（未保存
なら出荷時ハードコード既定）に統一**（両者の関係を明確にする） (6) 保存先＝アプリ共通設定
（既存の設定保存先があればそれに準拠、無ければ新設） (7) 破損/存在しないレイアウトファイルへの
フォールバック処理（クラッシュせず出荷時既定へ） (8) 単体テスト+build/test全合格。
**検証観点**：保存→再起動→復元の往復、明示保存→Ctrl+Alt+Rで保存済み状態に戻ることの確認、
レイアウトファイル破損時のフォールバック、既存の3パネルいずれの配置変更（フロート化・タブ化・
リサイズ）も保存対象に含まれること。

**増分4完全決着（2026-07-15）**：
**設計叩き台**（侍、`docs-notes/ecad2-t058-increment4-design-draft-samurai.md`）＝保存先
`%AppData%\Ecad2\docking-layout\{left-palette,output-panel,right-panel}.xml`（DockingManager
単位で個別ファイル）、出荷時ハードコード既定は不変維持しその上に保存済みファイル層を追加、
起動時は`SerializeDefaultDockingLayouts()`→ファイル読込の順を厳守、Ctrl+Alt+Rはファイル優先
（無ければハードコード既定）。新設コマンド＝キーバインド`Ctrl+Alt+S`・メニュー「表示」末尾
（いずれも殿確認済み）。家老検分＝承認。
**実装完了（コミット8a07472）**：build/test全合格（Core112・App655）。侍実機確認で起動時自動
復元・明示保存コマンド・Ctrl+Alt+Rリセット・破損ファイルフォールバック・終了時自動保存の5項目
とも確認。読込失敗時のステータスメッセージ通知（沈黙のフォールバック回避、家老裁量で追加採用）
も実装。
**隠密静的レビューでCONFIRMED指摘（severity中〜高）**：`ResetDockingLayoutToDefault()`の
`Deserialize`呼び出しにtry-catchが無く、ファイルは読めるが中身のXML構文が壊れているケースで
未捕捉例外によりクラッシュしうる。起動時経路`LoadDockingLayoutFromFileIfExists()`は正しく
保護されていたが、Ctrl+Alt+R経路のみ非対称に漏れていた。
**侍修正（コミット6133e6b）**：`TryDeserializeDockingLayout()`新設でDeserialize自体を保護、
失敗時ハードコード既定へ二段フォールバック。**RED/GREEN実機実測**（修正前＝破損XML+Ctrl+Alt+R
でクラッシュダイアログ実際に発生・スタックトレース特定、修正後＝同手順でクラッシュなし正常
フォールバック、殿協力）。隠密軽量再レビュークリーン（4パターン全て検算確認）。
**忍者実機確認完了**：(1)破損ファイル状態でCtrl+Alt+R＝クラッシュなし・正常フォールバック
（本題） (2)通常時のCtrl+Alt+R動作（回帰確認）——完全決着。次は増分5（ツールバー2段目可動化）。

**増分5完全決着（2026-07-15）**：
**設計叩き台**（侍、`docs-notes/ecad2-t058-increment5-design-draft-samurai.md`）＝既存
`Grid.Row="1"`をAuto/Auto2行のネストGridへ差し替え（T-059と同型の「ラッパー内で行を増やす」
手法、外側`MainContentArea`のRow構成は無改修）、1段目=`ToolBarTray`固定維持・2段目=新規
`PlacementToolBarDockingManager`（LayoutAnchorable、Title="配置ツール"）へ分離。2段目ボタン群
自体は内容無改修で配置場所のみ移動。§3-1のBinding罠は「Content内部の通常バインディングは
無関係」と正確に切り分け、増分4（レイアウト永続化）・Ctrl+Alt+Rとは`AllDockingManagers`配列・
`GetDockingLayoutFileName`への横展開のみで統合（新規ロジック不要）。家老検分＝承認（Title文言
「配置ツール」は家老裁量で承認）。
**実装完了（コミットdd974dc）**：build/test全合格（Core112・App656）。侍実機確認で「配置ツール」
パネル表示・ボタン動作・Ctrl+Alt+Rリセットを確認。
**隠密静的レビュー通過**：Grid.Row参照への無影響・`ToolBarTray.IsLocked`削除の妥当性・§3-1
切り分けの再確認・`AllDockingManagers`/`GetDockingLayoutFileName`への4つ目追加、いずれも確認、
指摘なしクリーン。
**忍者実機確認＝本題（ドラッグでのフロート化/再ドッキング）はOK、新規事象発覚**：再ドッキング
直後、配置ツールパネルの高さがタイトルバーのみ（19px）まで縮み手動では拡大不可（Ctrl+Alt+Rで
即座に復旧は可能）。
**隠密原因調査完了**（`docs/ecad2-t058-increment5-redock-height-collapse-investigation-onmitsu.md`）
＝**根本原因はAvalonDock本体の既知バグ**（GitHub Issue #337「LayoutAnchorablePane does not
honour DockHeight settings on deserialize」、2022-03-11報告・現在もOpen未解決、症状酷似）。
ecad2側4.74.1のリリースノートにも本Issueへの対応は無く、ecad2側のXAML設定は標準的用法で
設定ミスではないと確認。対処には本体フォーク・独自ビルドが必要でコスト過大。
**殿裁定（2026-07-15）＝経過観察＋proposed.mdへ記録し保留**（P-103、将来AvalonDockバージョン
アップでIssue #337が修正された際の再確認材料として維持）。実害は能動的なフロート化→再ドッキング
操作時のみに限定、既存のCtrl+Alt+R事後救済で足りると判断——**T-058増分5、完全決着**。

**T-058（パネルのドック化・フロート配置機能）全5増分、これにて完全決着（2026-07-15）**。
左パレット・出力パネル・右パネル・ツールバー2段目の4パネル群がAvalonDockドッキング化され、
自由なフロート化・再ドッキング・レイアウト保存/復元・レイアウトリセット（Ctrl+Alt+R）を
獲得した。T-083（ダークモード、AvalonDock連動）はT-058から切り離し済み、別途追加PoC検証へ。

### T-085 表示灯(Lamp)の色記号入力UI新設 — 完全Done 2026-07-15

**起票=P-057（忍者T-071実機確認、範囲外の気づき）を殿裁定でタスク化**。`ParamKeys.LampColor`は
Core・描画層に器完備（単体テストも描画ロジック自体はGREEN確認済み）だが、UIから設定する手段が
皆無（プロパティパネルは「種別」「デバイス名」のみ）。
**殿裁定（2026-07-13、GuiEcad実物スクリーンショット提示）＝仕様確定**：色は**フリーテキストで
記入する記号**（丸内にR/W/G等1〜2文字、実際の表示色ではない）。UI形式は既存「機器名」「コメント」
欄と同型（テキストボックス+クリア「×」ボタン）、ラベルは「ランプ色（中央表示）」。
**実装完了（コミットda8ea09）**：ViewModel(`IsSelectedElementLamp`/`SelectedElementLampColor`、
Params[LampColor]反映・Undo記録・2文字制限)+XAML(入力欄・クリアボタン)+コードビハインド
(Commit系統合)。単体テスト9件新規、RED先行証明済み（setter一時無効化で7件RED実測）。build/test
全合格（Core112・App651、警告0件）。
**忍者実機確認完了**：(1)Lamp要素選択→入力欄表示・"G"入力→図面上のランプ記号中央に正しく反映
（200%ズームでも位置精度良好） (2)クリアボタンで入力値・図面表示とも空欄に戻る (3)保存/読込
往復で値保持（ランプ・a接点・タイマ接点混在でも全要素・機器表とも正しく復元） (4)Lamp以外選択時
は入力欄非表示 (5)タイマ接点の「限」ラベル（機器名右肩）とランプ色記号（中央）は隣接配置でも
視覚的に重ならないこと確認——完全決着。
**隠密静的レビュー完了（2026-07-15、事後実施）**：引き継ぎ時点でレビュー記録の欠落が判明し軽量
レビューを実施。OnPropertyChanged横展開4箇所漏れなし・CommitDeviceNameEditへのLampColorBox
呼び忘れなし・境界値テスト（1-2文字反映/3文字超拒否/空文字クリア/未変化no-op）網羅、致命的欠陥
なし。軽微所見1点（`LampColorClearButton_Click`はWPF既定挙動でクリック前に`LostKeyboardFocus`が
先発火するためクリア操作のUndo記録が2段階になりうるが、既存`DeviceNameBox`等と同型の踏襲パター
ンにつき対応不要と判断）。検証パイプライン既定順への準拠を事後確認、T-085は追加対応不要で確定。

### T-096 タイマー設定時間（Setpoint）入力UI新設＋残り時間リアルタイム表示 — 完全Done 2026-07-15

**起票=P-099（隠密、殿直接指摘2026-07-15のタイマー接点調査）を殿裁定でタスク化**。
`ParamKeys.Setpoint`を設定するUIがApp層に皆無、Core層（限時/瞬時接点評価・Tick機構）は完備だが
`TimerSetpoints.TryGetValue`が常に失敗しsp=0.0固定→限時接点が固定表示のまま。
**着手前調査（隠密、`docs/ecad2-t096-t097-precheck-onmitsu.md`）＝GuiEcad原本実物確認**：
NumberBox(0〜9999)+Slider(0〜10秒・1秒刻み)の2点セット双方向同期。**殿裁定＝GuiEcad完全踏襲**
（WPFはTextBox+数値バリデーションで代替）。
**実装中に重大な設計誤りを発見・訂正**：「タイマーコイル本体(ElementKind.Timer)を配置する手段が
存在しない」と侍が発見、殿より**「タイマーコイルは仕様として存在しない」「機器名の紐づけのみで
タイマー接点があればタイマーと判断している」との訂正**。家老がEvaluator.cs（`IsConducting`）で
裏付け——限時/瞬時接点の`coilOn`判定は`s.Energized.TryGetValue(c.DeviceName, ...)`、DeviceName
一致だけで任意のコイル（通常のリレー等）の励磁状態を参照する設計。`ResolveSetpointTargetElement`
の「コイル本体検索」ロジックは不要と判明、選択要素自身のParams[Setpoint]を直接編集する設計へ
簡略化。
**殿直接要求2件が実装中に追加**：(1)瞬時接点には設定時間不要→対応（`IsSelectedElementTimerRelated`
を限時接点2種のみに限定） (2)GuiEcad仕様「タイマー接点上に経過時間(実際は残り時間)をリアルタイム
表示」→**T-096の拡張スコープとして同一タスク内で進める（家老裁可）**。
**拡張分の設計・実装**：GuiEcad原本調査（explorer委任）＝`MainPage.Drawing.cs`の
`DrawTimerCountdowns`、表示値は「残り時間」(Setpoint-Elapsed)と判明、殿確認済み。ecad2は
GuiEcadの層分離（Core/App）を踏襲せず、既存の通電色分け描画がCore層統合済みという一貫性を
優先しCore層（`DiagramRenderer.DrawTimerCountdowns`新設）へ統合。既存のリアルタイム更新機構
（`OnRealtimeTick`→`RedrawCanvas`→`Render()`）にそのまま乗るためApp層無改修。見た目（淡黄色
バッジ・"X.Xs"形式）はGuiEcad完全踏襲、殿確認済み。家老検分＝両設計とも承認。
**実装完了（コミット06d4a22）**：TextBox+Slider双方向同期・限時接点のみ表示・
`DrawTimerCountdowns`（Core層）。**殿ご自身の実機協力**でサンプル回路（押しボタン+限時接点NC+
瞬時接点NOの自己保持オンディレイ回路）による一連動作（Setpoint入力→保存反映→テストモード中の
残り時間バッジ表示→時限到達後のバッジ消失+限時接点開路による自動消磁）を確認。単体テスト25件
新規（App18・Core7）、RED先行証明済み。build/test全合格（Core119・App674、警告0件）。
**隠密静的レビュー**：軽微指摘1件（severity低、コメント記述が設計初版の名残で実装と食い違うのみ、
実装自体は正しくDoD充足、対応不要）。スコープ訂正の完全反映・瞬時接点非表示の設計判断・Core層
統合の妥当性・RED証明25件の網羅性・横展開（T-085/T-086と同型）、いずれも確認、致命的欠陥なし。
**殿裁定＝実機確認は殿ご自身が既に実施済みのため忍者確認省略、隠密レビュー通過をもって
完全決着**。

### T-084 シート削除時の後始末2件（PageNumber欠番警告+DRC結果破棄案内） — 完全Done 2026-07-15

**起票=P-066（侍、T-082修正2規約調査で発覚）+P-067（隠密、T-082再レビュー派生提案）を統合し
殿裁定でタスク化**。いずれもシート削除（`DeleteCommand`）の後始末漏れという同根：
(a) P-066＝削除時にPageNumberが再採番されず飛び番のまま残る (b) P-067＝削除時にDRC結果が古い
シート構成を指したまま破棄されない。**殿裁定（2026-07-13）＝いずれも警告文表示で対応**：
(1) P-067対応＝T-082修正2と同一パターンを横展開、診断存在時のみ`ClearResults`+案内表示
(2) P-066対応＝PageNumberの実再採番は行わず、欠番が生じる場合は警告メッセージ表示のみ。
**実装完了（コミット0548983）**：単体テスト6件新規、RED先行証明済み（DRC側・欠番側とも各
ガードを個別に一時無効化しREDを実測）。build/test全合格（Core120・App692）。
**隠密静的レビュー完了**：T-082 MoveSheetCommandパターンの横展開・独立事象の非上書き、いずれも
妥当と確認、致命的懸念なし。
**忍者実機確認＝(1)(3)OK、(2)欠番案内に条件付きNG疑い**：既に欠番が存在する状態からの末尾削除で
誤表示される再現手順を発見。
**侍の追加調査（差し戻し）で解明**：DeleteCommand側の判定ロジック自体は論理的に正しい（削除対象
PageNumberより大きいPageNumberが残存する＝新たな欠番が生じる場合のみ警告、という判定は正確）。
真因は`AddCommand`（`SheetNavigationViewModel.cs`91-101行）のPageNumber採番が`Sheets.Count+1`
固定で既存最大PageNumberを見ないこと（T-084範囲外の既存バグ）——欠番のある状態で新規追加すると
歯抜けを埋める小さい番号が新シート（表示順序上は末尾）に付き、表示順序とPageNumber数値の対応が
崩れるため、その新シートを削除すると一見「末尾削除」に見えて実際には新たな欠番が生じる、という
からくり。**殿裁定（2026-07-15）＝T-084は判定ロジック正当性が確認できたため完全決着、AddCommand
側の採番方式見直しはT-098として別途起票（P-105）**。
範囲外の気づき（忍者2026-07-15）：シート削除後も機器表に旧機器名が残存する現象を複数回観測、
P-104として記録。

### T-097 ラベル高さオフセット（LabelDy）入力UI新設＋コイル機器名の中心配置検証 — 完全Done 2026-07-15

**起票=殿直接指示2026-07-15**。`ParamKeys.LabelDy`を設定するUIがApp層に皆無と判明（隠密調査、
PR-16「1パラメータのみの書き込み経路欠落」6例目）。UI仕様＝現在の高さを0として+-で上下させる
相対オフセット方式（殿直接指示）。併せてコイル機器名の中心配置検証も要求された。
**着手前調査（隠密、理論計算）**：現状dy=-5.5でのテキスト矩形中心ベースのズレはごく僅か（0.2mm）
だが、フォント非対称性を考慮すると視覚的重心は中心から上へ0.5〜1.0mm程度ズレていると推測。まず
現状-5.5を新基準0点として実装し、実機実測でズレを確認してから補正する段取り（家老裁量）。
**実装完了（コミット9f99b28）**：プロパティパネルへLabelDy入力欄新設（相対オフセット方式）、
実体Params[LabelDy]は絶対値保持・相対0設定時はエントリ削除し既定へフォールバック。単体テスト
7件新規、build/test全合格（Core119・App687）。
**実装中の発見・対応**：DefaultLabelDyへ渡す種別に`el.Kind`を直接使うと誤動作（PartId配置要素は
Kindが常に既定値のまま）と判明——T-071の隠密指摘（生のElementInstance.Kind参照でラベル未描画）
と根本原因が同一と判明し、正式パターン**PR-17**として台帳記帳・`samurai.md`へ制度化。
`PartResolver.ComponentKind`経由で解決するよう修正済み。
**隠密静的レビューで重大所見**：T-097新規コードとは別に、既存の`DrawElementLabel`
（`DiagramRenderer.cs:1118`）がLabelDy未設定時のフォールバック計算で`resolvedKind`引数を無視し
生の`e.Kind`を参照する既存バグを発見。PartId配置のCoil要素は常にe.Kind=ContactNO固定のため、
DefaultLabelDy(Coil)=-5.5ではなくDefaultLabelDy(ContactNO)=-1.5で描画され4mmのズレが生じる潜在
バグ（DoD(4)「変更不要・器は完備」の前提が実は崩れていた）。
**差し戻し1周目（コミット27d04cd）**：1118行を`resolvedKind`参照へ修正。回帰テスト新設、RED
先行証明で期待値25.5・実測21.5（差4.0mm）を確認したうえで修正、GREEN確認。隠密軽量再確認クリーン。
**忍者実機確認1回目＝LabelDyクリアNG＋中心配置ズレ実測0.2〜0.25mm**：
(1)コイル中心配置＝実測ズレ約0.2〜0.25mm上方向（理論予測0.5〜1.0mmより小さい）
(2)LabelDyクリア（空文字列）→既定に戻らず直前値を維持するNG
**殿裁定＝基準値補正を実施、検証用一時ファイルは殿ご自身で削除**。
**差し戻し2周目（コミットe58c2d1）**：(1)クリアバグ＝空文字列を明示的に相対オフセット0扱いへ
修正 (2)DefaultLabelDy(Coil)補正＝-5.5→-5.72（実測ズレ0.22mm解消狙い、座標式検算済み）。副次
発見＝浮動小数点誤差（絶対値⇔相対値変換）をgetter/setter計3箇所にMath.Round(6桁)追加し解消。
build/test全合格（Core120・App695）。
**忍者再実機確認＝完全決着**：(1)クリア動作NG解消、空文字列クリア→既定値「0」へ正しく復帰
(2)コイル中心配置ズレほぼ解消（400%ズーム再実測、バウンディングボックス法0mm・重心法-0.018mm）
(3)副次＝絶対値⇔相対値の丸め処理も違和感なし。
### T-099 配置ツールバー2段目のドックタグ表示制御＋幅動的調整 — Done（要件(1)(2)(3)完全Done 2026-07-19コミット546b924、T-099(c)完全Done 2026-07-19コミット61ecdfd、いずれもpush済み）

**起票=殿直接指示2026-07-17**「avalondockの仕様でドッキング時はdockタグ非表示はできるか？フロート時は
表示する」「2段目ツールパネルに実装したい。またツールパネルの幅も動的に幅調整して。余白が多い」。
対象範囲＝**配置ツールバー2段目のみ**（殿確認済み、他パネルへは広げない）。実装イメージ＝
殿提示のGX Works3スクリーンショット（`c:\Users\kojif\Desktop\claude_TEMP\Dockimage.png`）の
「接続先」タブ——ドッキング時はコンテンツ幅にフィットした細身のタブ（ピン留め・閉じるアイコンの
みで文字ラベル+アイコン分だけの幅、余白なし）。

**要件**：
1. ドッキング状態の時、ドックタグ（タブヘッダー）を非表示にする
2. フローティング状態の時はタブ／タイトルを表示する
3. パネル幅をコンテンツに動的フィットさせ、現状の余白（既定の固定幅による空白）を解消する

**家老の事前調査（2026-07-17、Web一次情報）**：AvalonDock（Dirkster99/AvalonDock）の
`LayoutAnchorablePane`に`IsDirectlyHostedInFloatingWindow`／`IsHostedInFloatingWindow`という
状態判定プロパティが存在。`AnchorablePaneControlStyle`内でこれをDataTrigger条件に使い、
タブストリップ（`TabPanel`）の`Visibility`を切り替える構成が技術的な筋道と見立てる。ドッキング時
（`LayoutAnchorablePaneControl`）とフローティング時（`LayoutAnchorableFloatingWindowControl`、
`LayoutFloatingWindowControl`継承・独立ウィンドウ）は元々描画クラスが別なため、フローティング側は
既存のタイトル表示（`DropDownControlArea`）がそのまま使える見込み。出典＝
[LayoutAnchorablePane Wiki](https://github.com/Dirkster99/AvalonDock/wiki/LayoutAnchorablePane)・
[LayoutAnchorableFloatingWindowControl Wiki](https://github.com/Dirkster99/AvalonDock/wiki/LayoutAnchorableFloatingWindowControl)。

**リスク注記【重要】**：AvalonDockの`AnchorablePaneControlStyle`／`ControlTemplate`領域は、
T-083増分1（層B）で「値は正しく設定されるが実描画に反映されない」事象に遭遇した経緯がある
既知の要注意領域（最終的には検証ミスと判明したが、調査に一往復分を要した）。高リスク領域ゆえ
実装速度より検証優先＝PoC（ドックタグ表示制御のみ、最小構成）→忍者実機確認→幅調整の増分実装、
の順で進めること。

**着手（2026-07-17）**：侍へPoCから着手を采配。
**PoC難航・一旦保留（2026-07-17）**：AnchorablePaneControlStyleのHeaderPanelへDataTrigger追加
という設計自体は理論上妥当なはずだが、実機では配置ツールバー2段目全体（タブだけでなくボタン
本体まで）が幅ほぼゼロに潰れる実害が発生（殿確認済み）。Grid RowDefinitions調整でも解消せず、
原因未特定。T-100（ハッチング模様除去、同じAnchorablePaneControlStyle領域を調査）の知見を先に
得てから再挑戦する方針で、侍が新規発見3件を優先し一旦保留。**リスク注記【重要】節で予告した
「高リスク領域」の懸念が的中した形**——AvalonDockのAnchorablePaneControlStyleカスタマイズは
これで3件目の想定外挙動（層B・増分7メニュー・本件）、今後も同領域は検証優先を徹底する。
**根本原因調査、殿直接指示（2026-07-17）で隠密へ委譲**：侍が対症療法（起動時にファイルメニューの
ドロップダウンを一瞬開閉するコードを仕込む）で表示崩れ自体は解消できたが、メニューのハイライト
残留という副作用が`Keyboard.ClearFocus()`でも解消できず不完全。侍の調査引き継ぎ材料：
(1) 現象＝配置ツールバー2段目の`ContentPresenter(ContentSource="SelectedContent")`のみサイズが
潰れる（実機でBackground=Magenta化して確認、他要素=TextBlock等は正常にAuto計算される）
(2) 殿の実機操作で「メニューのドロップダウンを一度開く」ことが引き金になり表示が回復すると判明
（ダークモード切替ではなくメニュー操作が本質、殿ご指摘）
(3) 一次ソース調査＝`LayoutAnchorablePaneControl`は`TabControlEx`（独自TabControl拡張）を継承。
`TabControlEx.OnApplyTemplate`は`IsVirtualizingAnchorable`（既定true）がtrueの場合、コンストラクタの
`ItemContainerGenerator.StatusChanged`による初期選択反映ロジックが`ItemsHolderPanel==null`で早期
returnし実質無効化される、という疑わしい経路を発見（未確定）
(4) 増分7（ComponentResourceKeyでのMenuItemテンプレート派生）との競合を殿が疑っておられる。メニュー
操作（Popup生成）が何らかの形でDockingManager側のリソース解決/レイアウトパスを誘発している可能性
(5) 単純な`UpdateLayout()`・Theme再適用の複数パターンでは再現せず、「メニューの`IsSubmenuOpen`を
実際にtrue→(次のDispatcherサイクル)→false」という特殊な操作のみ効果があった。
侍が示した調査観点＝(a)なぜメニュー操作がDockingManager側のレイアウトに影響するか (b)TabControlEx/
LayoutAnchorablePaneControlのOnApplyTemplate〜SelectedContent解決の正確な処理順序 (c)増分7の
MenuItemテンプレート派生との実際の関連有無。
**着手（2026-07-17）**：隠密へ根本原因調査を委譲。侍は対症療法止まりで一旦区切り、作業ツリー整理中。
**根本原因調査完了（2026-07-17、隠密、一次ソース裏取り済み、
`docs/ecad2-t099-selectedcontent-collapse-root-cause-survey-onmitsu.md`）**：`DockingManager
.IsVirtualizingAnchorable`（既定true、ecad2は明示未設定）が`TabControlEx`のコンストラクタへ伝播、
`_IsVirtualizing=true`時は`OnApplyTemplate()`が`ItemsHolderPanel`関連初期化を全て早期returnで
スキップし標準WPFの`SelectedContent`メカニズムのみに委ねる構造と確認——**これはバグでなく設計上
意図的な分岐**。侍PoCテンプレートはAvalonDock既定`AnchorablePaneControlStyle`と構造完全一致で
コピー自体に誤りなし。**有力仮説**＝本質は標準WPFの`ContentPresenter(SelectedContent)`初期レイアウト
タイミング問題、メニュー操作のPopup生成がDispatcherのネストしたメッセージループを誘発し保留中
レイアウトを強制フラッシュする副次効果（WPF一般の経験則、完全な一次証明までは未到達）。増分7
固有の問題である可能性は低いと見立てる（既定Aero2メニューも同じPopup機構）が未検証。
**対処方針（本命、提案）**：`PlacementToolBarDockingManager`へ`IsVirtualizingAnchorable="False"`を
1行追加——TabControlEx独自のItemsHolderPanel方式（確実な初期化）が有効化され根本解決が期待でき、
これが効けば対症療法自体が不要になり副作用（メニューハイライト残留）も同時解消の見込み。タブ1つ
のみゆえ副作用（メモリ増）は実害なしと推測。**次善策**（対症療法維持の場合）＝`IsSubmenuOpen=false`
直後にキャンバス等へ`Keyboard.Focus()`を明示（`MenuItem.IsHighlighted`はClearFocusと別管理の
ため、未検証）。**不明点**＝他3つのDockingManager（左パレット/出力/右パネル）で同現象が起きるかは
未確認（理論上同じリスクを抱えるはず）、増分7との関連は既定Aero2メニューでの再現テストが必要
（侍・忍者マター）。
**着手（2026-07-17）**：本命案（`IsVirtualizingAnchorable="False"`）の検証を侍へ采配。
**本命案は根治せず、「観測者効果」と判明（2026-07-17、侍実機検証）**：`IsVirtualizingAnchorable
="False"`実装後も、外部トリガー（UIA `FindAll`・`Start-Ecad2App`内部の`MoveWindow`）に一切依存
しない起動シーケンス（`Start-Process`直叩き→`MainWindowHandle`確定待ちのみ）では起動直後・3秒後
とも配置ツールバー2段目は依然潰れたまま。従来「解決した」ように見えていたのは、外部UIAクライアント
の`FindAll`等プロセス境界を越えた問い合わせが偶然レイアウトをフラッシュしていた**観測者効果**
だった。**アプリ内でAutomationPeerツリーを明示的に構築・走査（`UIElementAutomationPeer
.CreatePeerForElement`→再帰`GetChildren()`、`ContentRendered`イベントで実行）する打開策も
効果なし**（侍実機検証、2026-07-17）——マネージド層のみで完結するAutomationPeer構築と、
外部UIAクライアントが経由する`UIAutomationCore.dll`のプロセス境界越えプロバイダコールバックは
別経路である可能性が高いと侍推定。この方向は打ち切り、作業ツリーは基準状態
（対症療法削除+`IsVirtualizingAnchorable="False"`のみ）へ復元済み、build/test再確認済み。
**殿裁定（2026-07-17）＝さらに調査を続ける**。隠密へUIAutomationCore発火源の追加調査を委譲。
侍はT-100実装へ先行して着手。
**真の発火源、特定（2026-07-17、隠密、一次ソース確認、
`docs/ecad2-t099-uiautomationcore-trigger-survey-onmitsu.md`）**：家老仮説（`DispatcherFrame
.PushFrame`によるメッセージポンプ早回し）は一次ソースで裏付けられず率直に却下——`Dispatcher
.PushFrameImpl`は既存メインループと処理内容が同一のWin32 `GetMessageW`ループに過ぎず、それ自体に
「保留中レイアウトを早回しする」特別な機構は無いと確認。**代わりに真の発火源を発見**＝
`HwndSource.cs`の`Process_WM_SIZE`（1401行）が、`WM_SIZE`受信時にルート`UIElement`へ**同期的な
`Measure()`呼び出しを無条件で強制実行**する実装と確認（コメント「Invalidating layout here ensures
that we do layout」）。侍が確認した「Win32 `MoveWindow`が効く」という実験結果と技術的に完全に
一致する一次証拠。外部UIA `FindAll`が効いた理由は完全解明には至らず推測に留まるが、侍の
「`GetChildren()`再帰では効果なし」という実験結果は本結論（本質は`WM_SIZE`経由の強制`Measure`、
managed層完結のAutomationPeer操作はWin32メッセージを一切発行しない）と論理的に整合。
**対処案（本命、提案）**：アプリ内からP/Invokeで自己の`MoveWindow`（現在の座標・サイズをそのまま
指定=実質無変化リサイズ）または`SetWindowPos`+`SWP_FRAMECHANGED`を呼び出し、意図的に`WM_SIZE`を
発火させる。外部UIAクライアントに頼らずアプリ内で完結する形。呼び出しタイミングは`ContentRendered`
後が妥当と推定（実機比較検証推奨、隠密の調査範囲では実機未検証）。
**着手（2026-07-17）**：本命案（自己`MoveWindow`/`SetWindowPos`呼び出し）の実装・実機検証を侍へ
采配。
**本命案2バリエーションとも不発（2026-07-17、侍実機検証）**：(1)自己`MoveWindow`（無変化）
(2)`SetWindowPos`+`SWP_FRAMECHANGED`、いずれも`ContentRendered`後実行・外部トリガー非依存の
起動シーケンスで確認したが潰れたまま。侍推定＝座標・サイズが実質無変化のため`WM_SIZE`自体が
OS側で送出されない（冗長操作として最適化スキップ）、または送出されても`Measure()`結果が既存
レイアウトと同一で見た目上変化がない、のいずれかの可能性（`Measure()`実際の呼び出し有無は
WndProcフック等の追加計装なしには確認できず）。同一アプローチ（`WM_SIZE`系）2バリエーション
不発につき打ち切り、作業ツリーはT-100コミット状態へ復元済み（実験コードはgit stash退避）。
**家老裁量で診断ログ方式へ切替（2026-07-17）**：確立済みの打開策
（`feedback_diagnostic_log_escalation`メモリ＝コード推論ベースの修正が複数周失敗したら診断ログ
注入で実測）を適用。殿裁定「調査続行」の範囲内の技術的手段選択のため、殿への再確認は行わず着手。
侍へ`HwndSourceHook`によるWndProc計装（`WM_SIZE`受信有無・`Measure()`呼び出しタイミング/結果の
ログ記録）を采配、外部トリガー成功時と自己トリガー失敗時のログを比較する。
**実測結果、WM_SIZE理論を覆す（2026-07-17、侍、WndProcフック+LayoutUpdated計装）**：
`[626ms] WM_SIZE自然発生（起動シーケンス中）→[763ms] ActualHeight=18.00のまま変化なし
（潰れ状態継続）`と実測——**起動シーケンス中に実際のWM_SIZEが自然発生したにもかかわらず修正
されない**ことが判明。さらに`[85782ms] UIA FindAll実行直後にActualHeight=84.00へ正常化、この間
WM_SIZEは一度も記録されず`——**FindAllによる修正はWM_SIZE経由ではないと実測で確定**。隠密の
`HwndSource.Process_WM_SIZE`理論は一次ソースとしては正確だが、本現象の実際の発火源ではなかった
と判明（侍の自己`MoveWindow`/`SetWindowPos`策2案が効かなかった理由も、標的が違っていたと裏付け）。
真の発火源は未特定のまま、推定候補＝UIAutomationCore.dllのプロバイダコールバックがWin32
メッセージを介さずAutomationPeer/UIElementのMeasureを直接誘発している等（侍提案、未検証）。
診断ログコードは作業ツリーに残置（除去せず維持）。
**殿裁定を再度仰ぐ（2026-07-17）**：理論が2周とも実測で覆り、真の発火源はUIAutomationCore.dll
という文書化されていない領域に絞られつつある。継続要否を家老から再度伺う。
**殿裁定（2026-07-17）＝もう1回だけ隠密へ追加調査、これで決着なくば打ち切り**。隠密へ
UIAutomationCoreプロバイダコールバック機構の深掘りを最終ラウンドとして委譲。
**最終ラウンド完了・結論「文書化されておらず特定不能」（2026-07-17、隠密、
`docs/ecad2-t099-elementproxy-final-survey-onmitsu.md`）**：UI Automationコールバックの
COM実装本体`ElementProxy.cs`（`MS/internal/Automation`配下、549行全文精読）にMeasure/Arrange/
UpdateLayout/InvalidateMeasure/InvalidateArrangeへの呼び出しは一切存在せず、コールバック自体に
レイアウト強制コードは無いと一次ソースで確認。**手がかり（確証には至らず）**＝外部UIAクライアント
のコールバックをUIスレッドへマーシャリングする`ElementUtil.Invoke`が
`Dispatcher.Invoke(DispatcherPriority.Send, TimeSpan.FromMinutes(3), ...)`という最高優先度の
同期呼び出しを使用——保留中レイアウトの処理順序への副次影響の可能性は排除できぬが、`ElementProxy`
/`ElementUtil`レベルでは因果関係の一次証拠は見つからず。ネイティブ`UIAutomationCore.dll`自体・
Dispatcher内部キューのさらに深い層は「深追いしすぎない」の指示どおり未調査のまま区切り。
**対症療法復元の方針に異論なし**（隠密所見＝対症療法も「UIスレッドへ高優先度処理を挟む」という
点で`DispatcherPriority.Send`経路と間接的に類似し、効果の整合性という意味では筋が通る、推測）。
**打ち切り確定（2026-07-17）**：対症療法（メニュー開閉トリック）を復元し、隠密提案の副作用対策
（`IsSubmenuOpen=false`直後にキャンバス等へ`Keyboard.Focus()`明示）を追加して仕上げる方針で
確定。真因の完全解明は打ち切り、実用上動く状態を確保してT-099本来の要件（ドックタグ表示制御・
幅動的調整）の実装へ進む。侍へ采配。
**対症療法仕上げ完了（2026-07-17、侍、コミット53aab52）**：診断ログ計装コード除去、対症療法
（ファイルメニュー時間差開閉）+副作用対策（`IsSubmenuOpen=false`直後に`Keyboard.Focus
(LadderCanvasHost)`、`ClearFocus()`から変更）実装。起動直後から配置ツールバー2段目正常表示・
複数回起動で再現性確認・ビルド/テスト回帰なし。メニューハイライト残留は「即座」でなく約2秒で
自然消滅（`ClearFocus()`単体だった前回の「残留し続ける」からは改善）。**殿裁定（2026-07-17）＝
この程度の残存挙動は許容、本題へ進む**。T-099のブロッカーはこれにて解消。
**着手（2026-07-17）**：侍へT-099本来の要件（1）ドックタグ表示制御（2）フローティング時タブ表示
（3）幅動的フィット、の実装を采配。
**忍者の「再発」報告は誤診断と判明・撤回（2026-07-17）**：忍者がダークモード切替時に配置ツール
バー2段目が潰れると観測したが、殿が実機画面（人間の目）のスクリーンショットを提供したところ
a接点等のボタン群は正常表示と確認。真因＝**忍者のPrintWindow撮影・UIA探索(FindAll)の両方が
`PlacementToolBarDockingManager`の内容を正しく捕捉できていなかった**（UIA探索でもボタン0件、
PrintWindow画像でも選択ツールのみ表示——両手法が一致して「見えない」と誤示したため手法の限界と
気づきにくかった）。T-080のPDFプレビュー撮影不正確の教訓と同型の事例。T-099対症療法(53aab52)
自体は正常に機能している可能性が高い。**家老裁量で対応**：ecad2-ui-automationスキルへ
「`PlacementToolBarDockingManager`はPrintWindow・UIA双方で正しく捕捉できないことがある」罠を
追記（技術情報のため殿確認不要）。**【報告】**T-100残り観点（配置ツールバー2段目の
DragHandleTexture確認）は、自動検証手法が本パネルで信頼できないと判明したため、殿ご自身の
実機目視での確認待ちとし、忍者にはこれ以上の自動検証手法の深追いをさせない方針とした。新規発見6
（シートパネル・部品選択パネルの一瞬ライトモード化）は別のDockingManager（左パレット側）が
対象で本問題の影響を受けないため、通常どおり検証継続してよいと判断。
**【重大】隠密2が真の根本原因（有力仮説）を特定（2026-07-17、殿直接指示「根本的な実装も疑って
調査せよ」による棚卸し調査、`docs/ecad2-t099-tanaoroshi-shinsou-chousa-onmitsu2.md`）**：終日
追ってきた「外部トリガーで直る謎」は的外れで、真因は**T-099 PoC自身が追加したDataTrigger**
（`MainWindow.xaml:151-153`、HeaderPanelをドッキング時`Visibility="Collapsed"`化）だった公算が
極めて高い。5段の因果を一次ソース（dotnet/wpf `UIElement.cs`/`Panel.cs`/`TabControl.cs`、
AvalonDock`LayoutAnchorablePaneControl.cs`）で裏取り：(1)Collapsed要素はMeasure()が早期return
(2)ItemsHostパネルのTabItemコンテナ生成はパネル初回Measure時の`InternalChildren`アクセスでのみ
発火 (3)パネルが測定されなければコンテナは永遠に生成されない (4)`TabControl`の初期選択・
`SelectedContent`解決はコンテナ生成完了が前提、AvalonDock側にも選択を救う別バインドなし
(5)よって`SelectedContent=null`のまま2段目全体が潰れる。外部UIA FindAll・メニュー開閉が偶然
コンテナ生成を誘発し「一度直れば恒久回復」という観測とも整合。**本日の実測10件全てと無矛盾**
（WM_SIZE不発・`IsVirtualizingAnchorable=False`不発・Peer走査不発の3不発も単一原因で説明）。
**確証は安価**（5分診断＝ContentRendered時にコンテナ生成状態をログ、または即効テスト＝
DataTrigger一時除去でクリーン観測起動）。**確証できれば根本修正＝HeaderPanel自体でなく
TabItemコンテナ側のVisibilityで制御する形へ置き換え**（既存Items.Count=1トリガーを
`Model.IsDirectlyHostedInFloatingWindow`条件のDataTriggerへ置換）——対症療法・
`IsVirtualizingAnchorable="False"`は全撤去可能、T-099要件(1)もこの形で直接実現される見込み。
**着手（2026-07-17、家老裁量・技術検証の範疇のため殿確認なしで進行）**：侍へ確証・根本修正の
実装を最優先割込みで采配。
**【報告・殿お戻り後の確認事項】**：根本修正案ではフロート時にタブが表示される（要件2どおり）が、
フロートウィンドウ自体のタイトルバーとタブが二重表示に見える可能性があり実機での見え方確認を
推奨（隠密2所見）。また選択タブの`Background="White"`固定（AvalonDock既定コピー由来、
`MainWindow.xaml:183`）はフロート時ダークモードで白浮きの見込み、根本修正時に併せて
DynamicResource化を検討中。
**隠密2仮説、実測で確定（2026-07-17、侍）**：DataTrigger（151-153行）コメントアウト+対症療法
コード無効化のみ（`IsVirtualizingAnchorable="False"`等は変更せず）で、外部トリガー完全なし
（`Start-Process`直叩き）のクリーン起動を2回実施、**2回とも起動直後から配置ツールバー2段目が
正常表示**（潰れなし）。「PoC DataTriggerがHeaderPanel(ItemsHost)をCollapsedにしたことが
コンテナ生成をブロックし潰れの真因」と確定。今日一日の対症療法・WM_SIZE系施策は一切不要だった
ことが実証された。
**着手（2026-07-17）**：根本修正（TabItemコンテナ側のVisibility制御へ置換）の実装を侍へ采配。
**要件(1)実装完了（2026-07-17、侍）**：DataTrigger置換・対症療法/`IsVirtualizingAnchorable`
完全撤去・選択タブ背景DynamicResource化、いずれも実装しビルド成功。実機確認＝Light/Dark双方・
複数回起動とも起動直後から正常表示、「配置ツール」タブヘッダーがドッキング時に正しく非表示化。
他パネル（T-100対象のAnchorablePaneTitle）に回帰なし。
**【重要・要殿判断】要件(2)実現に構造的矛盾を発見（2026-07-17、侍）**：フローティング化を検証
すべく試みたところ、**ユーザーがフローティング化する唯一の手段（タブをドラッグする操作）自体が、
要件(1)でタブを完全Collapsed化したことにより失われている**と判明。AvalonDockのドラッグ機構
（DragService）はタブヘッダー限定とみられ、ツールバー本体の余白領域ドラッグではフロート化しない。
DataTrigger自体（`IsDirectlyHostedInFloatingWindow=True`時は通常表示）は理論上正しいが、**そこへ
到達する経路がユーザーに存在しない**（保存済みレイアウトで既にフロート状態なら見た目上は再現
できるが、新規フロート化の手段がない）循環構造。
**【報告・殿お戻り後の判断事項】**：これは実装ミスでなく真のUI/UX設計分岐、家老裁量では決めず
殿のご判断を仰ぐ。候補案（検討材料、いずれも未実装）：(a) ドラッグ起点として機能する最小限の
ハンドル（細い帯等）をドッキング時も残す (b) T-100で扱ったAnchorablePaneTitle領域自体にドラッグ
手段を追加する (c) ドラッグ以外の代替操作（右クリックメニュー「フロート化」・キーボード
ショートカット等）を新設する (d) 完全非表示という要件(1)自体を再検討し、最小化された常時表示
要素（例=殿提示のGX Works3参考画像どおりピン留め+閉じるアイコンのみ残す形）に落ち着ける。
**現状**：作業ツリーは要件(1)実装済みの状態で維持、要件(2)(3)は保留。侍は判断待ちの間、他の
待機列（T-089再開・T-083残り3件=TextBox色/コメント追随/部品アイコン再生成）へ回す。
**副産物＝対症療法コードの取りこぼし発見・裏取り強化（2026-07-17、侍）**：T-089叩き台をstash
から復元しT-099要件(1)の未コミット変更と統合する過程で、対症療法コード（メニュー開閉+
`Keyboard.Focus`）が実は完全に削除されていなかったと判明（`git apply --3way`失敗時のロール
バックが影響と推測）。改めて完全削除し、対症療法なしのクリーン起動2回とも正常表示を再確認——
**根本修正単体での動作が改めて裏取りされ、DoD(2)の確証が強化された**。
**要件(2)実装完了（2026-07-18、侍、未コミット・殿裁可済み案a）**：TabItem全体をドッキング時
Collapsed化する従来方式から、Content(ラベル)のみCollapsed+`DockedDragHandle`(帯、Rectangle、
Height5/MinWidth20)を表示する方式へ置換。帯はT-100で除去した`DragHandleTexture`(ハッチング模様、
意匠のみ)とは別物で当たり判定は残す設計、色は既存`AnchorablePaneTitleNoDragHandleStyle`と同じ
`ToolWindowCaptionInactiveGrip`キーを流用（新規UI/UX分岐でなく既存意匠の踏襲と侍判断・家老確認
済み）。build/test全合格（App.Tests716/Core.Tests120、回帰なし）。**要実機確認**＝(a)ドッキング時
ラベル非表示+帯のみ表示になっているか(要件1再検証) (b)帯をドラッグしてフロート化できるか
(AvalonDock DragServiceがこの帯要素を拾うか未検証、本題)。検証パイプライン既定順どおり隠密静的
レビューへ采配済み（T-067(1)レビューの後）、完了後忍者へ実機確認を回す。要件(3)は要件(2)の実機
結果を見てから着手。
**要件(3)実装完了（2026-07-18、侍）**：ToolBar実幅1244px→523px(UIA実測)、ライト/ダーク両モードで
確認済み。実現方式＝DockingManagerをAuto+Star列のGridで包む（マネージャ自体を無限測定でコンテンツ
幅へ縮める）。**当初のDockWidth="Auto"案は不成立**——AvalonDock仕様（`LayoutPanelControl
.OnFixChildrenDockLengths`、v4.74.1一次ソース確認）で「水平LayoutPanel配下にLayoutDocumentPaneが
無い構成では初回描画時に全子DockWidthをStarへ強制上書き」される既知問題、診断ログ+UIA実測で
DockWidth指定・強制コードとも無意味と証明され除去（KISS）。空き領域の白浮き対策としてラッパー
Gridへ`ToolBarBackgroundBrush`（テーマ連動）設定。
**T-089回帰(2)完治（2026-07-18、侍）**：先のTransparent修正は有効ボタンのみで、**無効ボタン（起動
直後の大半）が明灰色のまま残存**していたと自己検分で発見——真因はControlTemplate内`IsEnabled`
トリガー（優先度4位）の`Button.Disabled.Background`がStyle.Triggers（優先度6位）のTransparentに
勝つ構図（PR-20系統と同型のパターン）。無効時の背景/枠線をControlTemplate.Triggersから
Style.Triggersへ移設し、派生スタイル（ToolBarButtonStyle等）が上書き可能な構成へ変更。画素採取で
無効ボタン=`#2D2D30`（地色と同化）を確認、ダイアログ等の通常ボタンは標準無効表示`#F4F4F4`を維持。
build/test exit0（716/120）、家老検分済み。**忍者へ最終確認4点を依頼**＝(1)幅フィットの見た目
(GX様式か) (2)帯ドラッグ→フロート→再ドックの一連動作+再ドック後の幅維持 (3)無効ボタンのフラット
表示(ライト/ダーク) (4)押下フィードバック(PressedOverlay)とテストモードON色。
**忍者最終確認結果（2026-07-18）**：(1)幅フィットOK（コンテンツ幅に正しくフィット、GX様式）
(3)無効ボタンフラット表示OK（ライト`#F0F0F0`/ダーク`#2D2D30`とも地色と完全一致、画素採取確認）
(2)帯ドラッグ→フロート化は成功（幅541px、移動後も維持確認）、**再ドックはUIA合成マウス操作では
不成立**（AvalonDock DragServiceのインジケーター認識起因の技術的困難と推測、殿実機操作での確認を
推奨・保留）。
**(4)テストモードON色、NG・自己訂正あり（2026-07-18、忍者）**：前回「回帰なし」と報告した判定が
目視のみで期待値との数値比較をしておらず不十分だったと自ら発見・訂正。期待値`TestModeActiveBrush`
=`#E63C00`（オレンジ、Light/Dark共通固定値）のはずが、実測はダーク/ライト両モードとも`#BCDDEE`系
（水色、支配色）——これはStack overflow修正で追加した暗黙的ToggleButtonスタイルの
`Button.Checked.Background`（Aero2既定の水色）と一致。**家老仮説**＝無効ボタン対応時と同型の
罠——ControlTemplate.Triggers内`IsChecked`トリガー（優先度4位）が、`TestModeToolBarButtonStyle`
独自のStyle.Triggers（優先度6位、`IsChecked=True→TestModeActiveBrush`）を握り潰している（侍が
T-099要件2作業時に事前警告していた範囲外懸念が的中）。証拠画像=
`docs-notes/screenshots/t089-testmode-wrong-color-5x.png`。侍へ最優先修正を采配——無効ボタン対応
（IsEnabledトリガーをControlTemplateからStyle.Triggersへ移設）と同型の対処をIsCheckedトリガーにも
適用する方向で検討されたし。
**押下フィードバック、参考所見（2026-07-18、忍者、検証一区切り）**：「新規」ボタン押下中の色を
実測（`#9DB7C5`系、青系）——通常時`#F0F0F0`からは変化しているが水色寄りでIsMouseOver色に近い
印象、テストモード色バグと同根（`IsMouseOver`トリガーが他トリガーより優先される疑い）の可能性を
指摘（参考情報、断定せず）。瞬間的視覚状態ゆえ確証には限界あり、最終確認は従前どおり殿ご自身の
実機操作に委ねる方針。忍者の一連の検証はこれにて一区切り。
**(4)テストモード色バグ、修正完了・自己確認済み（2026-07-18、侍、家老検分済み）**：真因は家老仮説
どおり（テンプレート内`IsChecked`トリガー優先度4位が`TestModeActiveBrush`[6位]を握り潰し）。対処＝
(1)`IsChecked`をStyle.Triggersへ移設（IsEnabled対応と同型） (2)**同じ罠の再発を構造的に防止**する
ためMouseOver/Pressed/IsDefaultedも同時にStyle.Triggersへ移設（テンプレート側は構造のみ=
PressedOverlay表示/無効文字色に統一。忍者所見「押下中が水色寄り」も以後は派生側で調整可能に）
(3)`TestModeToolBarButtonStyle`のIsChecked/MultiTriggerへ`BorderBrush=Transparent`追加。自己確認＝
実機で新規作成→テストモードON→支配色集計、`#E63C00`が1105px(圧倒的支配色)=`TestModeActiveBrush`
と完全一致。build/test exit0（716/120）。正規binにT-044診断ログ計装も反映済み、忍者へログ取得と
テストモード色再確認を采配。
**隠密静的レビュー完了（2026-07-18）**：ControlTemplate.Triggers内4件の重複なし、`ToolWindow
CaptionInactiveGripキー`は単色SolidColorBrush（ハッチング模様は別要素`DragHandleTexture`が担う、
T-100調査書で一次ソース確定済み）ゆえ模様混入の懸念なし、PR-20系統（値は正しいが反映されない罠）
にも該当せず（ecad2独自ControlTemplateの直接改変ゆえ）。code-reviewスキルも指摘なし。静的観点では
問題なし、忍者の実機確認（帯のドラッグでフロート化できるか、本題）をキュー追加済み。
**実機確認・本題成功（2026-07-18、忍者）**：帯（相対X=22,Y=110）を350px/50ステップでドラッグした
ところ、独立フロートウィンドウとして正しく切り離された（EnumWindowsで新規ウィンドウ検出・
キャプチャで「配置ツール」タイトル+ツールボタン群を確認）。AvalonDock DragServiceが帯要素を
正しく拾うことを実証。残る(a)ドッキング時ラベル非表示+帯のみ表示、確認中。
**要件(2)、完全決着（2026-07-18、忍者）**：(a)も確認OK——当初「配置ツールの上部青タイトルバー」を
対象と誤解していたが、これはAvalonDock標準ペインタイトル（変更対象外）と自ら気づき訂正。真の対象
はパネル下端の小さいタブ（UIA発見、rel X16,Y175,W40,H10）——4倍拡大画像で灰色の細い帯のみ・ラベル
テキストなしと確認、正しく機能。フロート化後は同位置に「配置ツール」ラベル付きタブへ変わることも
確認済み。**T-099要件(2)、本題・(a)とも全OK、完全決着**。要件(3)（パネル幅の動的フィット）へ着手
可能な段階。
**要件(1)(2)(3)、コミット完了（2026-07-19、コミット546b924、push済み）**：T-067(4)(5)と一括で
コミット（MainWindow.xaml.cs内でT-099(c)と行レベル混在のため分離不可、殿裁定で一括コミット）。

## T-099(c) 上段AnchorablePaneTitle表示制御——Done（2026-07-19、コミット61ecdfd・push済み）

**経緯要約（2026-07-19、家老記す）**：T-099要件(1)実装当初のVisibility=Collapsed化がフロート化
ドラッグ処理自体を破壊する重大回帰（引き継ぎ記載済み）と判明したのが発端。以後、Opacity=0化→
Height=1→Height=5（動作優先の殿裁定）と段階的に緩和するも、フロート化ドラッグ自体がAvalonDock
の`InternalOnActivated`タイミング問題（MouseAssistant競合ではないと確定済み）で殿の物理マウス
操作では不成立。**方式転換**：標準`MenuDropDownButton`（Float/Dock/DockAsDocument等5項目メニュー）
を活用する上段テンプレート再設計（ラベルのみHeader Collapse、ボタン群は残す）へ切替、フロート化
自体は成立。しかし「ドッキング」メニュー選択で**3種類の重大バグが連続発覚**：
(1) タブ自己複製（`ContentDocking`イベントの自前ハンドリングが招いた5段連鎖、根本原因は
`InternalDock`のフォールバック探索にフロートウィンドウ除外フィルタ漏れ）
(2) 独自再ドッキング処理後の後始末不足でツールバー中身が消失（`CollectGarbage`/
`InvalidateArrange`/`UpdateLayout`/`InvalidateVisual`と対症療法を積み重ねるも不完全解消）
(3) **起動時から配置ツールバーが完全に空白・Ctrl+Alt+Rも無効という最悪の回帰**——真因は
コードでなく`%AppData%\Ecad2\docking-layout\placement-toolbar.xml`（gitの管理外）の汚染
（実験中に壊れた状態がWindow_Closing時に自動保存されたもの）。汚染ファイル退避で復旧
（削除は殿の明示許可を得て実施、rm禁止の一時例外）。
再検証でもなお「幅44px程度の縦長タブとして左端に別追加」という別形の異常が再発、**殿より
「根本的に直して。プランの練り直しも検討して」との明確な指示**を受け、隠密が最深部の一次ソース
調査を実施。

**根本原因、完全確定（2026-07-19、隠密、
`docs/ecad2-t099-c-overlaywindow-droptarget-and-attachdrag-survey-onmitsu.md`調査5-6節）**：
AvalonDockの`LayoutRoot`は「RootPanelがnullになると空の`LayoutDocumentPane`入りパネルを自動補完
する」隠れた不変条件を持つ（`LayoutRoot.cs:91`）。フロート化→ペイン削除→`RootPanel`空→
自動補完で紛れ込んだ空`LayoutDocumentPane`は「最後のDocumentPane保護」ルールで削除もされない
——これが独自再ドッキング処理時に配置ツールと幅を分け合い縦長化する真因。**より本質的な結論
＝`ContentDocking`イベントを自前ハンドリングしモデル手術する方式自体に構造的限界がある**
（AvalonDockの隠れた不変条件全てと自前で整合を取る必要があり、直すたび新たな不変条件と衝突する
モグラ叩きに陥っていた＝実績3周）。

**殿裁定（2026-07-19）＝案Y採用**：自前モデル操作を全廃し、`e.Cancel=true`後は**ハードコード
既定レイアウトXMLのDeserialize（単一Manager版、Ctrl+Alt+Rと同じ実証済み機構）を呼ぶだけ**に
する方式へ転換。「Ctrl+Alt+Rで復活した」という殿の実測がこの方式の有効性の実証。

**詳細設計完了（2026-07-19、隠密、
`docs/ecad2-t099-c-dock-restore-by-default-xml-design-onmitsu.md`）**：コードは純減見込み。

### 実装ロードマップ（次回セッション、侍実装→隠密静的レビュー→忍者実機確認の順）

**増分1: コア実装**
- 新規private `ResetPlacementToolBarLayoutToDefault()`（約10行）——既存`TryDeserializeDockingLayout`
  （HasExpectedContent検証・RebindDockingContent込み）を再利用。saved XMLは意図的に不参照
  （常にハードコード既定＝ドッキング済み状態へ戻す、設計判断は設計書1節参照）。
- `ContentDocking`ハンドラを3行へ簡素化：ContentIdガード→`e.Cancel=true`→上記メソッド呼び出し。
- DoD: build/test exit0。

**増分2: クリーンアップ（ボーイスカウト、増分1と同時実装でも可）**
- 削除：ハンドラ内の積み上げ全部（Parent==null判定・ツリー再接続・Children.Add・IsSelected/
  IsActive設定・CollectGarbage・InvalidateArrange・遅延UpdateLayout・InvalidateVisual）。
- 削除：XAMLの`x:Name="PlacementToolBarPane"`と付随コメント（Deserializeでモデルが新規
  インスタンスへ置き換わるため、x:Name参照は孤立インスタンスを指す罠フィールドになる）。
- 残す：`ContentFloating`ハンドラ（DPI補正、無変更）・イベント購読・再発防止策
  （`HasExpectedContent`等、本設計と独立に有効）。
- DoD: build/test exit0、範囲外検出なし。

**増分3: 検証パイプライン**
- 隠密静的レビュー（コード純減の確認、削除範囲の過不足確認）。
- 忍者実機確認、設計書7節の6項目：
  (1) フローティング→ツールバー近傍表示 (2) ドッキング→元の横長位置(569×81相当)へ復帰、
  縦長44px・タブ複製・空白いずれも発生しないこと、フロートウィンドウが消える(幽霊なし)
  (3) タブ付きドキュメントとしてドッキングも(2)と同一動作 (4) Float→Dock反復2〜3周で劣化なし
  (5) Dock後にDocumentPane残骸が無いこと(UIAツリー確認) (6) 終了→再起動でレイアウト正常
  （再発防止策との複合確認）。
- 全OKでコミット・push、T-099完全決着。

**実装完了（2026-07-19、コミット61ecdfd、push済み）**：案Y（増分1+2）実装、忍者実機確認
設計書7節6項目すべてOK（フロート/ドッキング復帰・反復3周・DocumentPane残骸なし・再起動後
正常）。x:Name残置の隠密指摘は家老自ら再検分し誤検知と確定（word境界なしgrepの誤爆、隠密
自身も非を認め訂正済み）。
**追加確認（2026-07-20、隠密、`docs/ecad2-t099-c-dock-layout-load-failure-investigation-
onmitsu.md`）**：別件（T-104増分1のContentId新設に伴うレイアウト読込失敗表示、P-110）の
調査過程で、本コミット（61ecdfd）自体にDeserialize常時失敗の回帰が無いことを再確認。忍者の
上記6項目OK判定は`%AppData%`を意図的に参照しない独立経路のため無関係と判明、再精査不要と
確定。T-099完全決着。


### T-083 ダークモード搭載（AvalonDock連動、T-058と同時実装） — Done（全7増分+新規発見5件、完全Done 2026-07-17。新規発見2[ハッチング模様]はT-100へ分離）

**起票=殿直接指示2026-07-12**「AvalonDock VS2013はダーク/ライトテーマが選択できそうだが、ダークモードも
搭載できるか」→「T-058と同時にAvalonDock連動で実装をタスク起草して」。棚卸し39項目の後日判断バケット
（殿裁定2026-07-11）にあった「ダークモード」のタスク化。
**家老の事前検分（2026-07-12）＝3層構成**：
(1) **作図キャンバス色＝Core下地完備**：`src/Ecad2.Core/Rendering/DrawingTheme.cs`に`Default`（白地・黒線）/
`Dark`（暗地・明線）の2パレット定義済み。意味色（通電・接続済み・手動強制）はテーマ間固定の設計済み、
設計コメントに「画面はメニューの『ダークモード(作図色)』で切替、**PDFは常にDefault**（提出図面は白地黒線）」
の運用構想あり。**App層の切替メニュー・結線は皆無**（Core完備・App結線待ちパターン）。App層の直接色指定
15件（LadderCanvas.cs 7件ほか計6ファイル）のテーマ整合確認要。
(2) **UIクローム（メニュー・ツールバー・ダイアログ等）**＝.NET 10のWPF標準Fluentテーマ+ThemeMode
（Light/Dark/System）がネイティブ候補（新規依存なし）。成熟度・既存UIとの相性は**着手時に隠密調査で
裏取り必須**（.NET 9導入・10で改善継続の経緯ゆえ評価中機能の可能性あり）。
(3) **AvalonDockドッキングクローム**＝VS2013 Dark/Lightテーマ（T-058のテーマ7種必須構成の内）と
(1)(2)を連動切替。
**着手=T-058と同時（殿指示）**：T-058のPoC→本実装の増分計画へT-083を組み込む。PoC時に隠密へ
Fluent ThemeMode成熟度確認を併せて振る。UI/UX分岐（切替UIの形・メニュー配置・起動時既定・OSテーマ
追従の有無・切替の即時反映方式等）は着手時に殿確認【MUST】。
**メニュー配置、殿直接指示で確定（2026-07-15）**：既存「表示(_V)」メニュー
（`MainWindow.xaml:157`）へダークモードのトグル項目を追加する形とする。起動時既定・OSテーマ
追従の有無・切替の即時反映方式等、残る論点は引き続き着手時に殿確認【MUST】。
**PoC完了（2026-07-15、侍、コミット8a24318）**：作図キャンバス色のテーマ切替、忍者実機確認OK
（往復含め正常）。**範囲外の気づき（忍者2026-07-15）**：シートが0件の時のみトグルOn/Offに関わ
らずキャンバスが固定の濃紺色になる（シート追加後は正しく追従）。本実装（増分計画）着手時に
解消すべき項目として記録、PoCスコープ外のため今回は対応不要。
**本実装UI/UX論点、殿裁定（2026-07-16）＝確定**：(1) 起動時既定＝**ライトモード** (2) OSテーマ
追従＝**なし**（アプリ内トグルのみで手動切替） (3) 切替反映方式＝**即時反映**。
**本実装着手前調査完了（2026-07-16、隠密、`docs/ecad2-t083-honjissou-3layer-design-survey-onmitsu.md`）**：
(1) Fluent ThemeMode＝不採用維持（既存調査どおり、.NET 10でも`[Experimental]`のまま）、UIクローム
（層C）は自前`ResourceDictionary`（`Theme.Light/Dark.xaml`）切替方式で実現 (2) 3層連動設計＝
層A(作図キャンバス、PoC結線済み)／層B(AvalonDock、`Dirkster.AvalonDock.Themes.VS2013`新規導入、
T-058節で採用決定済み)／層C(UIクローム、自前方式) (3) 増分4分割案＝増分1:AvalonDock連動(層B)／
増分2:UIクローム基盤構築+シート0件時キャンバス色固定バグ解消(層C基盤)／増分3:UIクローム残箇所
(層C残り)／増分4:選択色系(暗背景視認性)の実機確認・調整。
**永続化要否、殿裁定（2026-07-16）＝不要**（起動時は常にライトモードへ戻す、前回設定の記憶機構は
実装しない）。
**着手（2026-07-16）**：侍へ増分1（AvalonDockテーマ連動）から着手を采配。
**増分1、層B（AvalonDockドッキングクローム）を保留（殿裁定2026-07-16）**：忍者実機確認でタブ・
タイトルバーがテーマトグルに無反応と判明（コミット0f63245+5020811+21d58fb）。侍・隠密のWチェック
で技術トライアル7回相当（Theme設定の反映機構調査、暗黙的スタイル自動再解決の既知制約検証、
ItemContainerStyle追跡等）を尽くすも根本原因未特定——ただしテーマのリソース機構自体は正常と
実証済み（`AnchorablePaneControlStyle`/`TabItem`のBackgroundともにDynamicResource解決は正しく
動作、パッケージ・AvalonDock本体・アプリ側ロジックいずれも問題なしと除外済み）。新たな手がかり
（実際に見えるタブ/タイトルバーはTabItemではなく別の描画要素＝推定`LayoutAnchorableControl`等の
可能性）はあるが、この先の調査は長期化リスクありと判断。**殿裁定＝層Bは保留し増分2・3（UI
クローム）を先に進める、後日体制を立て直して再挑戦可能**。現状のコード（Theme設定自体は機能上
害なし、視覚反映のみ未達）はそのまま残置。層A（作図キャンバス色）・観点1（起動時Light既定）・
観点3・5は確認済みOKにつき増分1はこれにて区切り。
**増分1層B、隠密2へ再調査を委譲（殿直接指示2026-07-16）**：旧隠密・侍の既存調査結果を踏まえ、
`LayoutAnchorableControl`等の実際の描画要素特定に焦点を当て独立調査中。
**増分2、メニューバー背景色も保留（殿裁定2026-07-16）**：ツールバー本体・シート0件時キャンバス
色固定バグは解消確認済み（`Theme.Light/Dark.xaml`新設・`App.xaml`統合・`MergedDictionaries`
差替え実装）。ただしメニューバー（MenuBarArea）の背景色のみダークモードに無反応と判明——値は
正しく更新される（診断ログで`#FF2D2D30`確認）が画面描画に反映されない、**増分1層B
（AvalonDock）と同型の「値は正しいが描画されない」現象**。Menu.Template書き換え・TemplateBinding
/通常Binding双方で切り分けたが解消せず（直接値=Purple等は反映される、Style/Template適用自体は
生きている）。**殿裁定＝メニューバーも保留、増分3へ進む**。層B調査（隠密2）が先に解決すれば
共通原因の可能性が高く、その知見を踏まえ再挑戦する。
**増分1層B・増分2メニュー背景色、「バグは存在しなかった」と判明（隠密2・忍者、2026-07-16）**：
隠密2が忍者撮影PNGの画素実測・GitHub一次ソース確認・4経路実測により、層B（AvalonDockドッキング
クローム）は増分1時点で既に正しく機能していたと特定（観点2・4のNG判定は目視誤読、青い帯＝
アクティブキャプション色はLight/Dark両テーマ共通値＝仕様のため変わらないのが正しい）。忍者が
画素採取で再検証し観点2・3・4を全てOKへ訂正（前回の目視誤読を撤回、`docs-notes/verification-
ninja-t083-zoubun1-recheck.md`）。侍のみ「メニューバー効果なし」を診断ログ・クリーンビルド後も
再現していたが、**真因は環境差異ではなくUIA座標取り違え**（`ControlType.MenuBar`をFindFirstすると
アプリのメニューでなくOSタイトルバーのシステムメニュー領域＝y≦31を誤って掴む罠、侍の申告座標
y≈18〜38がこの領域と重なっていた）と隠密2が特定（同一マシン5セッション体制ゆえ環境固有差異説は
構造上成立せず）。侍の最終確認（正しい座標での再測定）を待って完全決着とする。詳細・再検証用
期待値表は`docs/ecad2-t083-zoubun1-layerb-shinsou-chousa-onmitsu2.md`（追記含む）参照。
**教訓＝テーマ・配色の実機検証は目視でなく画素採取を標準とする**（ecad2-ui-automationスキルへの
追記候補）。
**増分3、TextBox入力欄の白浮き完全解消（2026-07-16）**：忍者検証でNG確定（隠密の事前指摘的中）
→侍が新規`InputBackgroundBrush`/`InputForegroundBrush`を追加し全箇所へ適用（コミット5867a51）
→忍者の往復2周目再検証で全箇所#3C3C3Cへ正しく修正・回帰なしを確認。増分3完全完了。
**増分5（新規）、ペイン内コンテンツのテーマ未対応（殿直接指摘・隠密2調査2026-07-16）**：シート
一覧（ListBox）・機器表/出力パネル（DataGrid）の中身が明示色指定なしでWPF既定の白のまま。
プロパティパネルのみ暗く見えるのは中身が透明コンテナで層Bのペイン背景(#252526)が透けているため
（忍者が「プロパティ中身の暗色化」として観測していた現象の正体）。着手前調査の色指定一覧が
「明示的な色指定のgrep」で作られたため、既定値のコントロールが増分計画から漏れていた構造的な
穴と判明。**殿裁定＝T-083の一環として増分5で対応**。詳細・対応表は同調査書「追記2」参照。
**増分6（新規）、配置ツールバー(2段目)のダークモード未対応（殿直接指摘2026-07-16）**：侍が増分1
時点で「2段目はAvalonDock管轄(層B)のため対象外」とコメント（MainWindow.xaml:195）した判断が
誤りだったと判明。層B（AvalonDock独自クローム＝タブ・タイトルバー）とパネル中身（通常WPF
コンテンツ＝`PlacementToolBarButtonStyle`/`PlacementToolBarIconStyle`等）は別物で、パネル中身は
1段目`ToolBarArea`同様に層C扱いで対応可能（侍の技術的整理、家老裁可）。**家老裁量にて対応承認**
（範囲内の見落とし修正、UI/UX新規分岐ではない）。**完了**（コミットec0707a、実機確認済み）。
**増分7（新規）、ドロップダウンメニュー背景のダーク対応 — 打ち切り（2026-07-16）**：緊急対応
（メニュー全体Light固定）を殿が実機確認、「上メニューもダーク対応にしたい」とのご指示を受け
本格対応に再挑戦。侍の調査で、サブメニューPopup内の実背景要素`SubMenuBorder`(Border)が
TemplateBinding経由で親MenuItem自身のBackgroundを参照する構造と特定（`DependencyPropertyHelper
.GetValueSource`で実測）。しかし`SystemColors.MenuBrushKey`オーバーライド・暗黙的Style適用の
いずれも試すも値操作が実描画に反映されず、**増分1層Bと同種の構造的制約**に直面。次善手段は
MenuItem用ControlTemplate（Role別=TopLevelHeader/SubmenuItem等）の完全自作だが、相当な作業量・
回帰リスクを伴う見込みのため、時間対効果を鑑みここで打ち切り。一時調査コードは除去し、コミット
ec0707a（メニュー全体Light固定、実害である白地白文字は解消済み、ダーク非連動のみ残存）の状態を
維持。**再挑戦は次セッション以降、要検討**（ControlTemplate完全自作の要否・規模を含め）。
**増分7再挑戦・方針再調査完了（2026-07-17、隠密、`docs/ecad2-t083-zoubun7-menu-dark-redesign-survey-onmitsu.md`）**：
一次ソース（dotnet/wpf本家`MenuItem.xaml`/`Menu.xaml`のAero2セクション）を精読し根本原因を特定
——`SubMenuBorder`（ドロップダウン全体の背景）は`StaticResource Menu.Static.Background`参照ゆえ
DynamicResourceオーバーライドが原理的に届かない**真の構造的制約**（増分1層Bの検証ミスとは異なる）。
侍実測の「TemplateBinding経由」要素は別役割（`SubmenuItemTemplateKey`側の`templateRoot`＝各項目
自体の背景、これは健全）を指していたと判明。**推奨案＝完全自作でなく、WPF既定Aero2の
ControlTemplateをコピーし色参照のみDynamicResource化する「派生テンプレート」**（対象2種＝
`TopLevelHeaderTemplateKey`・`SubmenuItemTemplateKey`、新規ブラシキー7〜8種、規模中）。実装順序
＝「ドロップダウン側→メニューバー本体復活」の順必須（逆順だと白地白文字再発）。代替アプローチ
（サードパーティテーマライブラリ・Fluent ThemeMode・DWM API）はいずれも非推奨で一致。
**家老裁量にて範囲確定（2026-07-17）**：現状未使用の`TopLevelItemTemplateKey`・
`SubmenuHeaderTemplateKey`（サブサブメニュー用）は今回省略——現行メニューは2階層のみで
サブサブメニューが存在せず、UI/UX上の見え方に影響しない純粋な実装スコープ判断のため
（品質哲学KISS・過剰実装回避）。将来サブサブメニュー追加時に併せて対応する。
**着手（2026-07-17）**：侍へ実装を采配。
**増分4（選択色系の暗背景視認性確認）完了（2026-07-17、忍者、画素採取＋WCAGコントラスト比、
`docs-notes/ecad2-t083-zoubun4-verify-ninja.md`）**：観点1（ダーク背景での選択ハイライト判別）・
観点3（ライトモード回帰なし）は画素採取で実測OK（背景`#202226`/選択枠OrangeRed`#FF4500`とも
理論値と完全一致）。観点2（3色の暗背景コントラスト）はOrangeRed=実測OK（コントラスト比約
4.63:1）、**DodgerBlue（約4.92:1）・White（約15.93:1）は理論値のみ**——記入中プレビュー・
画像リサイズハンドルの実機動的再現がUIA単発クリックで不安定（ツール状態が意図せずSelectへ
遷移／ボタンInvoke自体が例外）なため打ち切り。3色とも`LadderCanvas.cs`で同型の
`Pen`/`Brush`描画・テーマ非依存の固定色ゆえ、未実測分もOrangeRedと同様に理論値どおりと
推定される（実測ではない旨を明記）。NG無し、リスク低と判断し家老裁量にて増分4を決着とする。
**増分5、隠密静的レビューでCONFIRMED指摘（2026-07-17）**：DataGridRowへのIsSelectedトリガー
追加のみでは選択色が反映されぬ懸念——WPF既定DataGridCell(全テーマ共通の暗黙的Style)が
`IsSelected=True`時にSystemColors.HighlightBrushKeyでBackground/Foreground/BorderBrushを
上書きする既定Triggerを持つ(dotnet/wpf `DataGrid.xaml`一次ソース裏取り済み)。DataGridCellにも
同様の暗黙的スタイル追加が必要と判定、家老裁量で範囲内欠陥修正として侍へ差し戻し。
**要確認指摘（同、実機）**：追加したListBox暗黙的スタイルがAvalonDock既定Ctrl+Tabナビゲータ
内部の生ListBox(`PART_AnchorableListBox`等)へ意図せず波及する可能性、忍者実機確認へ回付。
軽微指摘(`PanelContentBackgroundBrush`等が`DialogBackgroundBrush`等と値完全一致=二重管理)は
修正必須とせず経過観察扱い。
**増分7、隠密静的レビュー完了（2026-07-17）**：狙い撃ち観点3点(ComponentResourceKey派生構造・
各Trigger欠落なし・実装順序遵守)はいずれも良好。新規発見(軽微〜中)＝既定Aero2
`TopLevelHeaderTemplateKey`にある7個目のTrigger(`SubMenuScrollViewer`のCanContentScroll、
サブメニューにスクロールバーが実際に表示される場合のみ影響)が派生に含まれず——現行メニューは
各6〜7項目程度でスクロールバー非表示につき顕在化しないと見立て、家老裁量で**経過観察**
（低解像度環境・将来の項目追加時に再検討）。要実機確認＝`MenuItem.SeparatorStyleKey`
オーバーライドの解決可否（9箇所実在、視覚影響あり得るためWPF内部実装依存で一次ソースからは
確定不可）、忍者の実機確認へ回付。チェックマークStroke代替は技術的制約として妥当と確認済み。
**増分5・増分7、忍者実機検証完了・完全決着（2026-07-17、画素採取実測、
`docs-notes/ecad2-t083-zoubun5-zoubun7-verify-ninja.md`）**：3件（Ctrl+Tabナビゲータ波及確認・
DataGridCell選択色回帰確認・メニューダーク対応全観点）とも全てNG無し、実測値がTheme.*.xaml
理論値と高精度で一致（半透明ブラシは背景合成計算値で照合、Dark側の差1はPNG圧縮丸め誤差の範囲）。
セパレータ色・スクロールTrigger漏れの懸念点も実害なしと確認。増分5・7ともこれにて完了。
**新規発見1（殿直接指摘2026-07-17）：プロパティパネルのダークモード時、ラベル文字色が黒固定の
バグ**：`種別`/`デバイス名`/`ラベル高さ調整`等のラベルテキストが既定黒のまま残存し、層Bペイン
背景(#252526)が透ける暗背景と衝突し可読性低下。増分5の着手前調査（対応表）は「シート一覧
(ListBox)・機器表/出力パネル(DataGrid)」の5コントロールが対象で、プロパティパネルの背景透過は
把握済みだったが中身のラベルテキスト色までは対応表に含まれていなかった見落とし。範囲内欠陥
修正として侍へ差し戻し。
**新規発見2（殿直接指摘2026-07-17）：ドッキング済みタブのヘッダー領域に常時ハッチング模様
（斜線パターン）が表示される**：タブラベルと固定アイコン(ピン留め/閉じるX)の間に余白がある際、
その余白部分にAvalonDock/VS2013テーマ既定の装飾的背景パターンが表示される。**殿確認＝全ドック
共通の事象**（特定パネル限定ではない）。T-099の要件3（幅動的フィット）とは別パネルでの観測だが
同根（タブストリップの余白部分の表示）の可能性があり、共有スタイル側の対応が妥当と見立てる。
新規タスクとして起票し侍へ調査・対応を依頼（→T-100）。
**新規発見3（殿直接指摘2026-07-17）：配置ツールバー1段目（新規/開く/保存/戻す/再実行/PDF出力/
行追加/行削除/テスト/一時停止）のアイコンがダークモードでネガポジ反転されておらず視認困難**：
2段目（配置ツール、F5/F6/F7等）は増分6で`PlacementToolBarIconStyle`によりアイコン色までダーク
対応済みだったが、1段目（`ToolBarArea`）は増分2で背景色のみ対応し、アイコンのGeometry Fill色は
theme非対応のまま据え置かれていた見落としと推定（着手時に要実測確認）。範囲内欠陥修正として
侍へ差し戻し。
**新規発見1・3、隠密静的レビュー完了（2026-07-17、コミット1648d2b）**：狙い撃ち観点（1段目全10
アイコンが`ToolBarIconStyle`経由で統一、個別Fill/Stroke指定なし）は良好、見落としなし。
**cleanup指摘2件（低severity、修正不要）**：TextBlock/CheckBox 8箇所への`Foreground`個別付与が
冗長（継承で代替可）／`PlacementToolBarIconStyle`のStroke再指定が本コミットでデッドコード化し
コメント（MainWindow.xaml:93-94「2段目専用キーのため1段目には影響しない」）が事実と矛盾——実害
なしだが保守性のため追随修正が望ましい。
**完了（2026-07-17、侍、コミット3165d49）**：コメントを事実に追随させ修正。
**T-083待機列3件、全完了（2026-07-17）**：新規発見4（TextBox5箇所色対応、8cf7e87）・新規発見5
（部品アイコンダーク対応、399019d）・cleanup（コメント追随、3165d49）、いずれも実装・実機確認・
build/test回帰なし・コミット済み。**残る未コミット分はMainWindow.xaml/App.xaml/
MainWindow.xaml.cs（T-099要件1+T-089叩き台、殿確認待ちのため意図的に留保中）のみ**。
**新規発見4（隠密発見2026-07-17、範囲内欠陥・PR-18確認）：プロパティパネルのTextBox5箇所
（DeviceNameBox/NotchPositionBox/LampColorBox/SetpointBox/LabelDyBox）がInputBackgroundBrush/
ForegroundBrush対象外のまま残存**——増分3で確立したはずの入力欄対応表からも漏れていた。殿提示の
実機画像（X1/0入力欄の白浮き）と符合する実害と確認。新規発見1と同根、`docs-notes/
pattern-recurrence-log.md` PR-18（色対応調査の網羅性不足、3例判明で正式パターン確定）として記帳
済み。範囲内欠陥修正として侍へ差し戻し。
**完了（2026-07-17、侍、コミット8cf7e87）**：`InputBackgroundBrush`/`ForegroundBrush`適用。実機
確認＝DeviceNameBox/LabelDyBoxの2箇所（暗背景+白文字で正常表示、ライト復帰も回帰なし）、残り3箇所
（NotchPositionBox/LampColorBox/SetpointBox、セレクトSW/表示灯/タイマ接点選択時限定表示）は同一
パターンでの機械的追加のためコード目視で担保。build/test回帰なし。混在していたMainWindow.xamlから
該当12行のみを`git apply --cached --recount`で的確に抽出しコミット、範囲外混入なし。
**新規発見5（殿直接指摘2026-07-17、範囲内欠陥）：部品選択パネルの部品アイコンがダークモードで
黒色のまま**：家老の一次調査で原因特定——`PartThumbnailRenderer.Render()`
（`src/Ecad2.Rendering.Wpf/PartThumbnailRenderer.cs:56`）がサムネイル描画時に固定の
`DrawingTheme.Black`を使用しており、アプリのライト/ダーク切替に反応しない構造。生成された
サムネイルはビットマップ（`ImageSource`）としてキャッシュされる設計のため、単純なブラシ差替え
では対応できず、**テーマ切替時にサムネイルの再生成が必要になる見込み**（着手時に要精査）。
PR-18とは異なるメカニズム（ビットマップ事前レンダリング由来）ゆえ別記帳。範囲内欠陥修正として
侍へ差し戻し。
**完了（2026-07-17、侍、コミット399019d）**：`PartThumbnailRenderer.Render()`/`RenderGlyph`へ
foreground引数追加（既定=従来どおり黒、後方互換維持）、`PartSelectionEntryViewModel.Thumbnail`を
可変プロパティ化、`PartPaletteViewModel.RefreshThumbnails(Color)`で全件再生成する仕組みを追加。
実機確認＝a接点/b接点/コイル等のアイコンがダーク切替で黒→明色（白系）へ正しく変化、ライト復帰も
回帰なし。build/test回帰なし。**配線待ちの注記**：呼び出し配線（`MainWindow.xaml.cs`の
`IsDarkMode`変更ハンドラ内、`RefreshThumbnails(...)`の1行）はT-099要件(1)+T-089叩き台の未コミット
分と同一ファイルのため意図的に未コミットのまま——現HEAD単体では新規発見5は「配線未接続」（実害
なし、T-099/T-089コミット時に一括で有効化される）。
**範囲外の気づき（隠密、実機確認推奨）**：1段目`ToolBarKeyLabelStyle`（ラベル用）にForeground
明示指定がなく`ToolBar.Foreground`継承依存、2段目`PlacementToolBarKeyLabelStyle`は増分6で
「継承に頼らず明示指定」と判断済みという非対称性あり——1段目でも継承だけで正しく機能するか
忍者の実機確認を推奨。
**セッション終了時点のT-083状態まとめ（2026-07-16、当時の記録。現在は増分5・7も完了し全7増分
完了、上記2件の新規発見を除く）**：増分1(層B)・増分2(メニュー含む層C基盤)・増分3(ダイアログ/
固定色パネル)・増分6(配置ツールバー2段目)＝**完了**。増分5(ペイン内コンテンツ=ListBox/DataGrid)
＝**未着手**（着手前調査済み、対応表あり）。増分7(ドロップダウンメニュー背景)＝**打ち切り**
（Light固定のまま残置、次セッション要判断）。増分4(選択色系暗背景視認性)＝**未着手**。
v0.4リリースは全増分完了後の予定ゆえ、今回は見送り。
**リリース予定、殿直接指示（2026-07-16）**：T-083全増分（4分割→層B/メニュー決着・増分5/6/7追加で
実質7分割）の検証パイプライン完了後、v0.4仮リリースビルドを作成する（T-057確立の手順＝Version
設定→publish2箇所→起動終了実測→csprojのみコミット→家老がタグ打ち・push、に準拠。侍が実行、
家老が采配時に手順詳細を伝達）。**2026-07-17時点＝全7増分の実装・検証は完了したが、上記新規
発見2件（プロパティ文字色・ハッチング模様）の対応完了後にリリースへ進む**。


### T-067 GroupFrame（グループ枠）作成・編集UI — Done（(1)〜(5)本体、完全Done 2026-07-19、コミット4e11c3a／546b924）

**起票=P-054（殿の直接指摘2026-07-11）を殿裁定でタスク化（B区分一括）**。`new GroupFrame`・
`Frames.Add`ともsrc全体0件＝生成経路がアプリに一切存在せぬ。Core層は完備（モデル・描画・RowOps
シフト・Height--詰め処理）。GuiEcad側には枠追加/削除/リネーム/移動/枠線スタイルのコマンド群が
存在。規模中〜大。関連=P-050（Visual*Mm座標未追随、枠ドラッグ実装時の潜在バグ——本タスク着手時に
併せて対処を検討）。UI/UX分岐（枠の作成操作方法等）は着手時に殿確認【MUST】。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t067-groupframe-design-onmitsu2.md`）**：
GuiEcad実物操作体系＝作成(「枠」ツール選択→**マウスドラッグ、mm連続座標のグリッド非依存自由配置**
→リリース確定、半セル未満は無視。GroupFrameは唯一グリッドに縛られない自由配置機能)・ラベル編集
(ダブルクリック→インラインテキストボックスのキャンバス上オーバーレイ)・移動(ドラッグ)・線種変更/
削除(右クリックメニュー、T-069調査で確認済み)。ecad2側Core層は完全同型で実装済み
（`DiagramRenderer.DrawFrames`のVisual*Mm優先ロジックまで）、App層は生成・編集・移動・削除いずれも
皆無。
**重要な発見**：P-050（RowOps未追随）の対処要否は独立したバグ修正ではなく、**「枠の配置単位を
グリッドセル単位にするかGuiEcad同様mm自由配置にするか」という設計分岐そのものに直結**——グリッド
セル単位限定ならP-050は構造的に非該当（そもそも追随すべきVisual座標が無い）、mm自由配置を踏襲する
ならP-050対処が必須。この分岐は作成操作方法（マウスドラッグ vs 既存`FreeLine`同型のキーボード
ステップ方式、`BeginFreeLineDraft`等が直接参考になる）とも自然に対応。Undo対応はT-064同様、既存
`RecordSnapshot`パターン流用で新規コマンドクラス不要。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①作成操作方法＝**両方対応**（マウスドラッグ・
キーボードステップいずれでも作成可能） ②配置単位＝**グリッドセル単位**（P-050は構造的に非該当と
確定、マウスドラッグ時もグリッドへスナップする実装とする） ③ラベル編集UI＝**ダブルクリック→
インライン入力（GuiEcad踏襲）** ④移動操作方式＝**ドラッグ（GuiEcad踏襲）**。
**着手時要確認（2026-07-13家老追記）**：proposed.mdの経過観察3件がいずれもGroupFrame追加を
発火条件の筆頭候補として明記している——P-071（未確定編集確定処理の呼び忘れ、新規選択状態変更
経路の追加で4回目再発なら構造格上げの殿条件つき）・P-077（左右クリックヒットテスト優先順位
ロジックの重複、別要素種別追加で2件目が出れば正式パターン化検討）・P-080（3種ドラフト全クリア
責務の分散、4種目のドラフト追加で同型のクリア漏れ再発の恐れ）。着手時は横展開チェックリスト
7項目の自己点検に加え、この3件への該当有無を隠密へ確認させること。
**着手前チェック完了（2026-07-17、隠密、`docs/ecad2-t067-pretask-check-onmitsu.md`）**：
**3件とも該当**。P-071＝該当（名指し予告そのもの、既存チェックリスト6項目目で対応済みだが
GroupFrameラベル編集[ダブルクリックインライン]は`UpdateSourceTrigger=Explicit`入力欄になる
可能性が高く要重点確認、4回目再発なら構造格上げの殿条件は維持）。P-077＝該当（左右クリック
ヒットテスト重複、**T-067実装がP-077の言う「2件目」になる可能性が高く正式パターン化格上げの
検討タイミング**）。P-080＝該当の可能性高い（GroupFrame作成がFreeLine同型の4種目ドラフト
プレビュー機構を要する設計と推測されるため）。**横展開チェックリスト7項目、7/7該当**——ただし
**項目7（矢印キーでの選択状態平行移動）のみ、殿裁定④「移動=ドラッグ」が矢印キー対応を含むか
未明記でUI/UX判断を要する**。
**【報告・殿お戻り後の確認事項】**：GroupFrameの移動操作に、ドラッグに加えて矢印キーでの平行
移動も対応させるか（他の選択可能要素との一貫性・キーボードファースト理念との整合 vs
殿裁定「移動=ドラッグ」の明示範囲）。**着手（2026-07-17）**：侍は矢印キー対応を除く範囲（ドラッグ
移動・P-071/P-077/P-080の実装時注意）で先行実装、矢印キー対応は殿裁定待ちで後日追加可能な形に
しておく。
**基盤区切り完了（2026-07-17、侍、コミット837b407）**：ViewModel層——`SelectedFrame`新設
（`SelectedCell`セッタへ排他クリア組込み、P-080対応=ドラフトクリア4種目として追加）、
`DeleteSelectedFrame`/`RenameSelectedFrame`（P-071対応、確定処理の受け皿）、
`BeginDragFrame`〜`ConfirmDragFrame`/`CancelDragFrame`（GridPos単位、Undo対応）、
`BeginFrameDraft`〜`ConfirmFrameDraft`/`CancelFrameDraft`（キーボードステップ方式）、
`IsFrameWithinGridBounds`（他要素との重複は占有判定対象外）。回帰テスト`T067GroupFrameTests.cs`
新設、build/test回帰なし（App.Tests 695→712件+17、Core.Tests 120件）。**次段階**：
(1)ヒットテスト+選択ハイライト描画 (2)キーボード/マウス配線 (3)マウスドラッグ新規作成
(4)ラベル編集UI（P-071確定処理の実配線） (5)右クリックメニュー。P-077は(1)(5)実装時に意識する
旨、侍より申告あり。
**基盤、隠密静的レビューでCONFIRMED2件・PR-01再発（2026-07-18、
`docs/ecad2-t067-foundation-static-review-onmitsu.md`）**：`ReplaceDocument`が新設
`SelectedFrame`/`_frameDraft`をクリアしない、`HasAnyDraft`が`_frameDraft`を含まない。制度化済み
チェックリスト（`samurai.md`「新規選択可能状態の横展開チェックリスト」5項目）存在下での再発ゆえ
実効性に疑義（P-106として記帳、隠密へ運用実態点検を委譲）。
**基盤欠陥2件、修正完了（2026-07-18、侍、コミット8c996fe）**：(1)`ReplaceDocument`へ
`SelectedFrame=null`/`ClearFrameDraftIfAny()`追加 (2)`HasAnyDraft`へ`_frameDraft`判定追加。
回帰テスト3件追加・RED先行証明済み。**侍の正直な報告**＝当初4件目のテスト
（「枠記入確定後はHasAnyDraftがfalseに戻る」）はConfirmFrameDraft内部でCancelFrameDraftが呼ばれ
修正前コードでも通ってしまう検出力なしのテストと判明、自ら削除（RED証明できたのは3件のみ）。
build/test全合格（App.Tests715/Core.Tests120、既知数から減少なし）。
**侍の所見（PR-01実効性への追加情報）**：基盤実装(837b407)時点では「View層未着手」の区切りで
SelectedCell setter経由のクリア連鎖は機能済みだったが、`ReplaceDocument`/`HasAnyDraft`という
「setterをバイパスする別経路」への横展開はチェックリスト対象外だった可能性——既存7項目は
選択排他setter・Esc・矢印キー等が中心で`ReplaceDocument`系は明示的に挙がっていない。P-106調査
（隠密、運用実態点検）へ追加の調査観点として申し送り済み。
**(1)ヒットテスト+選択ハイライト描画、完了（2026-07-18、侍、コミット9fd10a8）**：GuiEcad原本
（`HitTestFrame`）を直接調査し移植——枠は塗りつぶし無しゆえ境界線近傍(margin付き)のみヒット、
複数該当時は面積最小(入れ子の枠)優先、左クリック優先順位はConnectorの直後。UI/UX新規分岐でなく
GuiEcad一次資料の忠実な移植と侍判断・家老確認済み。build/test全合格（App.Tests712/Core.Tests120）。
実機確認は忍者へ委ねる。次段階(2)〜(5)は継続作業。
**(1)隠密静的レビュー完了（2026-07-18）**：GuiEcad原本（`MainPage.Pointer.cs`/`MainPage.xaml.cs`）と
数式レベルで完全一致（優先順位・margin計算・面積最小優先とも）、忠実な移植と確認。修正必須の重大
バグなし。code-reviewスキル(low)でPLAUSIBLE1件（`FrameRectMm`/`FrameRectDip`がWidth/Height非負
検証なしに`new Rect(...)`構築、旧ファイル互換・破損データでArgumentExceptionの懸念、severity低）。
気づき（範囲外）＝FreeLine/ConnectionDot判定順序がGuiEcad原本と逆転（T-041増分5由来と推測）。
**(1)忍者実機確認、NG（2026-07-18、再描画漏れ）**：境界線クリックで選択セルは変化するが、枠の
選択ハイライトが即座に反映されず、テストモードON/OFFを挟むとオレンジハイライトが現れる（再描画
漏れ）。範囲内欠陥として侍へ差し戻し。
**(1)欠陥2件、修正完了（2026-07-18、侍、コミットf648897）**：(1)真因＝`ViewModel_PropertyChanged`
の再描画トリガー列挙に`SelectedFrame`が漏れており枠クリック選択直後にRedrawCanvasが走らなかった
（忍者観測と完全整合） (2)`FrameRectMm`のWidth/Heightを非負クランプ（隠密PLAUSIBLE指摘対応）。
View層コードビハインドのためRED証明不可（既存方針）、実機確認へ委ねる。build/test exit0確認済み。
忍者の現行検証セッションは本修正込みの最新ビルドで動作中——次の境界クリック再検証で確認できる
見込み。
**基盤欠陥3件目、修正完了（2026-07-18、侍、コミットc544bde）**：`CancelResidualDraftForToolSwitch()`
へ`ClearFrameDraftIfAny()`追加（隠密P-106点検指摘、着手前チェックが名指しした3箇所全て解消）。
回帰テストは既存Theory(Connector/FreeLine/Image)へFrameケースを4種目として追加（rule of three超えの
複製回避）、RED先行証明済み。build/test全合格（App.Tests716/Core.Tests120）。**T-067基盤欠陥3件、
全て解消**。侍はT-099要件(2)(3)実装を再開。
**着手（2026-07-18）**：commit 9fd10a8（ヒットテスト+選択ハイライト描画）の隠密静的レビュー（1周目
軽量既定）を采配。完了後、忍者へ実機確認を回す（検証パイプライン既定順）。
**（1)(2)(3)、完全完了（2026-07-18、忍者実機確認全観点OK・回帰無し、コミット4e11c3a）**：
(1)ヒットテスト・選択ハイライト (2)ドラッグ移動・削除・Esc配線 (3)マウスドラッグでの新規作成
（「グループ枠記入」ボタン→PlaceFrameモード→ドラッグでグリッドセル単位伸縮）。詳細は
`docs-notes/handover-next-session.md`（2026-07-19付）参照。
**（4）ラベル編集UI、修正不要で決着（2026-07-19）**：ダブルクリック開く・Enter確定・Tab確定・
Esc取消は実機OK。「フォーカスロスト確定」が発火しない件を新規バグとして調査したが、T-080
（行コメントエディタ）で殿裁定済みの既確定仕様（ウィンドウ内側クリックでは確定せず、ウィンドウ
非アクティブ化時のみ`LostKeyboardFocus`発火）と完全に同根と判明（隠密の一次ソース調査＋忍者の
実機検証、`docs/spec/ecad2-spec-canvas-display.md`8節へ追記済み）。**殿裁定（2026-07-19）＝
修正不要**、コード修正は行わない。
**(5)右クリックメニュー、実装完了・隠密静的レビュークリーン（2026-07-19、侍）**：
`SetSelectedFrameBorderStyle(LineStyle)`新設（`RenameSelectedFrame`と同型）、右クリック
ヒットテストチェーンへGroupFrame分岐追加（Connector直後・Image前、左クリック優先順位と対称）、
削除は既存`DeleteMenuItem_Click`のOR連鎖を流用。GuiEcad一次資料（線種サブメニュー[実線/破線/点線]
+削除）通りの構成。回帰テスト5件追加、build/test全合格（App.Tests716→721、Core.Tests121）。
隠密レビュー＝要修正0件、経過観察2件（下記）、忍者実機確認中。
**経過観察1（隠密指摘、未修正・設計判断待ち）**：`GroupFrame.BorderStyle`は`LineStyle?`
（null=テーマ既定Dashed、`Element.cs:150`）のため、未設定(null)の枠へ「破線」を選ぶと見た目
無変化なのにUndo履歴+ダーティ化が起きる（同値ガード規約の趣旨をすり抜ける、実害は空Undo1個の
みで軽微）。修正するなら`(frame.BorderStyle ?? 既定値) == style`判定だが既定値はレンダラ側
テーマ知識のため設計判断を要す——優先度低のまま経過観察。
**経過観察2（忍者実機確認へ回付済み）**：`rowInRange`ガードにより、T-067(3)自由作成枠が描画
範囲境界をまたぐ場合に右クリックだけ弾かれる理論的可能性（実害未確認）。
**パターン記帳（隠密、PR-22候補）**：侍申告のP-077「2件目該当」を検証し妥当と判定、
`docs-notes/pattern-recurrence-log.md`へ記帳済み。正確には「実害の再発」でなく「P-077が予言
した手動対称性維持作業の発生」（侍は正しく対称実装）。重複チェーンは要素/Connector/Frame/Image
の4系統に拡大——正式パターン化・P-077（共通ヘルパー集約）の優先度引き上げは、実害が出ていない
現時点では見送り、次にこの種の重複が生じた際に改めて判断する。
**経過観察2、実害あり確定・修正完了（2026-07-19、隠密机上検証→侍修正→忍者実機OK）**：
`rowInRange`ガードにより最下行の枠は右クリックがほぼ不能（下辺境界ヒット帯の約83%が範囲外
判定）と机上検証で確定。Frame分岐の`rowInRange &&`条件削除＋冒頭早期returnへの
`HitTestFrame is null`条件追加、両箇所セットで修正。忍者実機確認OK。
**観点2(線種変更の画面反映)、NG→真因確定→修正完了・全観点OK（2026-07-19）**：忍者実機確認で
線種変更が画面反映されないNGを検出、当初のタイミング遅延仮説（拙速な家老所見、後に撤回）は
誤りで、**真因は選択ハイライト（`SelectedFramePen`固定色オレンジ赤・実線）が線種変更後の
枠に上塗りされ隠れていた**ことと隠密の一次ソース調査で確定（因果連鎖を全段階でコード確認、
「殿OK（選択解除後に目視）/忍者NG（選択中にキャプチャ）」の食い違いも矛盾なく説明）。忍者が
証拠画像の色（R=255 G=69 B=0）・選択解除後の再キャプチャ（点線表示）で裏付け。侍が
`SelectedFramePenFor(LineStyle?)`（BorderStyleに応じDashStyle動的生成）へ修正、隠密フル観点
レビュークリーン、忍者最終確認で全観点OK。
**T-067(5)、これにて最終確定（2026-07-19）**。build/test exit0（Core.Tests121・App.Tests721）、
コミット待ち。

T-067は(1)〜(5)本体が完全Done、矢印キー移動対応は下記T-105として分離。


### T-044 OR自動配線の冗長縦分岐抑止 — Done（新規バグ報告分、2026-07-19、コミット4e11c3a・push済み）

実装d04c9a3＋隠密再レビュー＋忍者実機とも全OK・電気的に正常だが、連鎖ORで低ズーム時に
右合流線が断線して見える描画欠陥が判明。自動ロジック精緻化は袋小路（真因はP-003/P-028の既存欠陥）
ゆえ手動配線基盤T-041で対処する方針へ転換（T-041は2026-07-08完全Done、2026-07-07裁定）。

**方針転換・再開（2026-07-19、殿直接指示）**：殿より新規スクリーンショット提示とともに
「接点C右側の配線は本来、線番2に繋がらなければいけない。手前の行4列の場所までしか縦分岐が
描画されない。この状態を線番2まで自動で結線して欲しい。描画ロジックを見直して欲しい」との
明確なご指示。忍者・隠密の再調査で真因が完全確定——`DiagramRenderer.RightTerminator`
（367-387行、T-026/P-003で意図的に`(TopRow||BottomRow)`から`BottomRow`限定へ変更した副作用）
により基準行（B接点等）の右側配線が省略され、下流の縦コネクタが視覚的に「浮いて」見える
（横線がB接点右端手前で終わり、離れた列から縦線が生える）。画像・診断ログ双方で完全一致確認済み
（忍者、`docs-notes/t044v2-diag-log-newpattern-ninja.log`・証拠画像`t044v2-*`）。殿裁定により
**自動ロジック精緻化を本命として再開**、手動配線での回避方針は撤回。対処案（「自分自身より右の
別コネクタ」と「自分を経由し下流へ分岐するコネクタ」を区別するより精密な条件、P-028時代の提案を
具体化）は隠密が設計中、まとまり次第侍へ実装采配。
**新規バグ報告（殿直接指摘2026-07-18、未調査）**：「ORを書いた場合の分岐線が必ず1段上までしか
連結しない。1段上の配線に分岐線があった場合は連結しなくなる」。当時の記録は「低ズーム時に見た目
上断線するが電気的には正常」という**描画のみの欠陥**だったが、今回の症状は「連結しなくなる」と
いう、より機能的な問題に読める——同一原因の言い換えか、別の新規バグかは未確認。隠密へ独立調査
（再現条件の特定・原因が既存P-003/P-028由来か否かの切り分け）を委譲する。
**調査中断・中間仮説あり（2026-07-18、隠密）**：Ecad2.App起動不能インシデント（下記）優先のため
一旦中断、再開待ち。中断前に有力仮説を発見済み——`PlaceElementAtSelectedCell`の
`NothingBetweenRailAndColumn`（左分岐省略ロジック）が、基準行が既に`rightColumn`側の縦コネクタを
持つ場合に`leftColumn`側の判定が及ばず誤って「何も挟まっていない」と誤判定し左分岐を省略、結果
Cの左ポートが意図せず母線直結される懸念。殿提示の実機画像とも符合する可能性あり、再開時に活用。
**調査再開・中間報告（2026-07-18、隠密）**：単純な反例（3階層、baseRow行が右側に縦コネクタを1本
持つのみ）では電気的にも描画的にも再現できず——`LeftRailReached`（NetlistBuilder、電気側）と
`LeftTerminator`（DiagramRenderer、描画側）は同じ`BottomRow==row`限定ロジックで整合しており、
コード内コメントの「省略しても電気的分断は起きない」という前提はこの範囲では成立。殿実機画像
（行1接点→行4横線がN24側に届かず浮遊→行8から縦線接続）の再現には、より複雑な多階層構成——
特に**新要素をbaseElementより左側の列に配置する**（通常の右へ広がる連鎖ORとは逆方向）ケースが
鍵と推測するが、手計算では確定的な反例を構築できず。時間対効果を鑑み実機再現へ移行、忍者へ
采配済み（(a)3階層以上・新要素を左列配置 (b)殿画像構図の再現+DRC出力確認）。文書化は忍者の
再現結果を見てから（家老裁定、仮説が外れた場合の手戻り回避）。
**Explore調査で新たな手がかり（2026-07-18、忍者）**：`NetlistBuilder`(電気側`LeftRailReached`)と
`DiagramRenderer`(描画側`LeftTerminator`)の判定ロジックが実は食い違っている疑い——隠密の「両者
整合」という記述はコード実態と不一致、電気側は旧ロジックのまま未修正の可能性。詳細は忍者の後続
報告待ち。
**実機再現成功（2026-07-18、忍者、殿ご自身の操作立会い）**：行1/4/8構図（Row1Col5にa接点→Row2Col5
にORa接点→Row3Col3[左2列]にORa接点）で、行8(baseElementが行4接点より2列左)の**母線接続線が完全に
欠落**、視覚的に接点が浮遊する現象を再現。DRC出力は0件（電気的には正常判定）——Exploreの新仮説
（電気/描画ロジック不一致）と符合。証拠画像=`docs-notes/screenshots/t044-row8-disconnect-repro-
{3x,full}.png`。殿がサンプル図面を保存予定、忍者は操作を一旦停止し待機中。
**殿サンプル図面でも再現確認（2026-07-18、忍者、`C:\ECAD2\sample\setuzoku.gcad`、
`docs-notes/ecad2-t044-realmachine-repro-ninja.md`）**：殿ご指摘「X4リレー右側配線が線番2に届いて
いない」を実機で裏付け。X4(行9)の右側配線は上方X3(行8)の縦コネクタ下端で途切れ、線番2(CRコイルへの
経路)に到達せず。DRC出力5件はいずれも「駆動元不明/死にリレー」系のみで配線欠落自体は未検出
（構図Aと同型）。**忍者作成の構図A・殿作成のサンプル図面、独立した2件で同型症状を確認**——
再現条件はほぼ確立。証拠画像=`docs-notes/screenshots/t044-x4-*`・`t044-sample-*`。原因特定を
隠密へ委譲。
**本格調査・行き詰まり、診断ログ注入へ切替（2026-07-18、隠密）**：忍者指摘の非対称性を確認・自己
訂正——`LeftRailReached`(電気側)は`(TopRow==row||BottomRow==row)`だが`LeftTerminator`(描画側)は
`BottomRow==row`のみで、以前の「両者整合」報告は誤りだったと判明。画像精査で症状を精緻化——
忍者文面「行8欠落」より正確には**行4の左側(母線N24〜接点B間)が完全に欠落**（行1・行8は母線から
正しく線が伸びる）。構図Aを手計算で追ったが、行4の左分岐は`NothingBetweenRailAndColumn`により
省略が正しい計算結果となり、欠落理由を机上で説明できず——忍者発見の非対称性も本ケースには影響
しない計算結果。**コード推論ベースの修正が複数周不発の様相**、診断ログ注入（`leftColumn`/
`NothingBetweenRailAndColumn`各判定値・`LeftTerminator`戻り値の実機ログ出力）を次善策として提案、
侍へ采配。
**診断ログ第1弾・構図Aは非再現と判明（2026-07-18、忍者、`docs-notes/t044-diag-log-koudzuA-ninja.log`）**：
構図A(Row1Col5→Row2Col5→Row3Col3)を再現しログ取得したが**バグ非再現**（視覚的にも正常）。ログ解析
——既存縦コネクタ`col6:r0-1`は新要素の列3より右にあり「column=3より左に何も無い」判定は電気的にも
描画的にも妥当（母線直結が正しい動作）、この構図はそもそもバグの発生条件を満たしていなかったと
判明。実際に再現できたのは「行1/4/8」構図（行を離して配置）と殿のsetuzoku.gcadで、いずれも既存の
縦コネクタが新要素の判定範囲と交差する位置関係だったと忍者推察——隠密の当初仮説（3階層・baseRow
行の右側縦コネクタ1本）は再現条件を捉えきれていなかった可能性。忍者は「行1/4/8」構図での再取得へ
移行。
**診断ログ第2弾・決定的発見（2026-07-18、忍者、`docs-notes/t044-diag-log-koudzu148-ninja.log`）**：
最初の再現記録と同一手順（行1列5→行4列5→行8列3、座標も同一値）で実施し、視覚的にも同じバグ
（行8左側配線完全欠落）を再現。ログでは`NothingBetweenRailAndColumn(row=7,column=3)=True`・
`LeftTerminator(row=7,lb=3)→null(母線まで延ばす)`と**判定ロジック自体は正しく「母線まで延ばす
べき」と結論している**にもかかわらず、**実際の画面には母線までの線が全く引かれていない**。
**真因の絞り込み＝判定ロジック（NothingBetweenRailAndColumn/LeftTerminator）ではなく、その判定
結果を反映する描画処理側の不具合**（もう一段階描画寄りの箇所、別の描画経路に阻まれている可能性）。
証拠画像=`docs-notes/screenshots/t044-diag-repro-148-full.png`。原因特定を侍・隠密へ委譲、次段階は
「LeftTerminatorがnullを返した後、実際に母線まで線を描画する処理はどこか、なぜ実行されないか」の
追跡。
**描画処理側の追跡完了・座標計装を提案（2026-07-18、隠密）**：`LadderCanvas.cs`は`DiagramRenderer
.Render`単一経路のみ呼んでおり画面/PDF出力とも同一経路（別経路で上書きされる懸念は排除）。
`LeftTerminator`null後の`DrawRungSegment`→`DrawWire`→`r.DrawLine`を仔細に追ったが、線を消す・
スキップする要因はコード上見当たらず（`if (xR<=xL) return;`ガードも通常成立せず、`DrawLine`自体は
`net`パラメータ有無に関わらず無条件実行）。行7のページ範囲外仮説も検討したが、これが真因なら接点C
自体も非表示になるはずのところ実際は表示されており矛盾、排除。**次の一手＝座標計装**——
`DrawRungSegment`呼び出し直前・`r.DrawLine`呼び出し直前で実際に渡される座標(xL,xR,y)・lb・rowの
値をログ出力し、判定は正しいのに実描画が起きない原因を座標計算自体(`X(lb)`等)に絞って追跡する
方針。侍へ采配。
**座標計装・決定的発見（2026-07-18、忍者、`docs-notes/t044-diag-log-koudzu148-v2-ninja.log`）**：
「行1/4/8」構図で再実測、同じバグ再現。**`DrawWire`呼び出し自体は行8の左側配線に対しても正しく
発生している**（`DrawWire: (15.50,87.50)-(47.00,87.50) net=0`←行8左側配線）——座標値(X=15.50〜
47.00,Y=87.50)は行1・行4の同種呼び出しと同型で異常なし。にもかかわらず**この呼び出しだけ実際の
画面には一切表示されない**（行1・行4は正常表示）。**真因のさらなる絞り込み＝判定ロジックでも
DrawWire呼び出しの発生有無でもなく、DrawWire呼び出し後の実描画パイプライン（内部実装・後続の
クリップ/上書き処理等）**。証拠画像=`docs-notes/screenshots/t044-diag-repro-148-v2-full.png`。
侍・隠密の継続調査待ち。
**パイプライン追跡・2仮説排除、残る筋を提示（2026-07-18、隠密）**：`WpfRenderer.DrawLine`実装自体は
単純明快で特別分岐なし。**クリッピング仮説は排除**——`PushClip`は`DrawFreeLines`/`DrawFrames`の2箇所
のみ、いずれもY方向限定・X方向は広大な範囲で行7左側を狙い撃ちで隠せず、しかも各関数内でPush直後に
Popし`DrawRungWires`実行時点では解除済み。**色(Stroke)仮説も排除**——行7左側`net=0`は行0左側
`net=0`と同一net値ゆえ色計算ロジックは同一結果になるはずで片方だけ透明化する理由なし。**残る筋＝
`DrawRungWires`実行後の`DrawConnectors`/`DrawElement`(選択ハイライト含む)等、後続描画呼び出しに
よる物理的な上書き**——行7・列3付近特有の何か(既存縦コネクタ`col6:r0-3`の描画、選択中接点Cの
ハイライト矩形等)が座標(15.50〜47.00,y=87.50)と偶然重なっている可能性。侍へ`DrawConnectors`・
選択ハイライトの実描画座標ログ計装を追加采配、DrawWire座標との突き合わせが次の一手。
**再実測・座標重なり仮説は否定（2026-07-18、忍者、
`docs-notes/t044-diag-log-koudzu148-v3-drawconnectors-ninja.log`）**：DrawWire終点X=47.00と
DrawElement始点X=47.00は隣接するが重ならず（境界一致のみ）、DrawConnectors(X=74.00)は全く別の
X座標——3者とも座標上の衝突なしと確認、隠密の物理的上書き仮説は否定された。**新たな手がかり**＝
問題のDrawWire呼び出しは「配置確定時の1回」のみログに記録され、その後（Esc押下後等）の再描画では
記録が更新されていない——**選択状態変更等で別のRedrawCanvasが走った際に、この線を含まない形で
再描画されている可能性**（最終的に画面に反映されるのは最後のRedrawCanvas呼び出し結果のはず）。
証拠画像=`docs-notes/screenshots/t044-diag-repro-148-v3-full.png`。
**症状精緻化を撤回・目視誤読と判明（2026-07-18、殿直接ご指摘）**：「行4の左側が完全に欠落」という
上記の精緻化情報は隠密の目視誤読と判明し撤回。3倍拡大画像を殿ご自身が確認され「行4には左端(母線)
から接点まで横線が明確に描画されている」とご指摘。真の断線箇所は再調査中、診断ログ注入自体（計装
内容は変わらず）の采配は有効のまま進行。正確な症状は隠密の再報告待ち。
**再訂正（2026-07-18、隠密）**：フルサイズ・3倍拡大とも再確認したところ行4・行8とも母線から接点
まで薄いグレーの線が実在（解像度・圧縮で視認しづらかっただけ）——「完全欠落」という目視判定自体が
不適切だったと認め撤回。**静止画の目視のみでは断線箇所を確定できないと判断**（`ecad2-ui-automation`
スキル既存の「画素採取が万能でない例外」節と同種の罠、新規追記は不要と判断）。以後は診断ログの
実測データが整うまで目視での断定を控える。
**新規バグ報告、決着・バグ非実在と確定（2026-07-19、忍者、3手法+殿目視の裏取り）**：診断ログで
`DrawWire`が正しく発火している（前述、行7左側配線 `(15.50,87.50)-(47.00,87.50) net=0`）にも
関わらず画面に反映されないという矛盾を追っていたところ、殿の実機目視「行8左側配線は確実に
繋がっている」との指摘を機に検証手法自体を疑い、忍者が同一状態をPrintWindow・CopyFromScreen・
PDF出力の3手法で撮り比べ。結果＝CopyFromScreen・PDF出力・殿目視の3者は「正しく繋がっている」で
一致、**PrintWindow方式のみが異常値（欠落して見える）**。WPFのDirectXハードウェアアクセラレーション
描画とGDIベースのPrintWindow APIとの間のタイミング起因の一過性不整合が原因と推定（隠密裏取り、
`PW_RENDERFULLCONTENT`フラグは`ecad2-ui-automation`スキルで既に使用済みのためフラグ未指定が原因
ではない）。**これまでの構図A・殿サンプル図面(setuzoku.gcad)での「視覚的断線」実機報告も含め、
全て検証手法(PrintWindow)側の誤検出だったと確定**——判定ロジック（`LeftTerminator`/
`NothingBetweenRailAndColumn`）・描画ロジック（`DrawWire`）自体は最初から正しかった。**この件に
関する実装修正は不要**。教訓は`ecad2-ui-automation`スキルへ制度化済み（隠密、`SKILL.md`6節・
memory`feedback_printwindow_capture_limitation_t044.md`）。
**訂正・家老の見出し「決着」は誤り（2026-07-19、殿直接ご指摘・画像提示）**：上記でPrintWindow
誤検出と確定したのは「行8左側の母線への水平配線」という**これまで忍者・隠密が追っていた症状**に
ついてのみであり、この結論自体は覆らない。しかし殿が最初にご指摘の新規バグ「分岐線が1段上までしか
連結しない」の正確な再現ではなかった可能性が高いと判明——殿が新たに提示した実機スクリーンショット
（`C:\Users\kojif\Desktop\claude_TEMP\T-044_bug.png`）が示す症状は**別物**：接点Cの右側から上へ
伸びる縦分岐線が、本来到達すべき「線番2」（コイル手前のネット）まで届かず、手前（行4付近の高さ）
で止まっている。構図＝行1(N24→A接点→分岐点→線番1→無名接点→線番2→コイル→P24、分岐点から行4へ
B接点が並列)、行7にC接点があり右の配線が縦分岐点(行5付近)から上へ伸びるべきところ短く終わる。
**この症状は未検証・未解決のまま**、忍者へ正確な構図での実機再現を再依頼中。一時診断ログ計装は
除去せず維持（侍へ指示済み）。

**実機再現・本物のバグと確定（2026-07-19、忍者、3手法一致）**：殿が実機で構築した該当構図
（A/B並列→線番1→接点→線番2→コイル、C接点をOR追加）をそのまま`C:\ECAD2\sample\T044-sample.gcad`
として保存、PrintWindow・CopyFromScreen・PDF出力の3手法**全てが一致**して「C接点右側の縦分岐線が
分岐点(行4付近)で途切れ線番2まで届かない」ことを確認——今回は3手法一致ゆえキャプチャ手法の限界
ではなく実描画バグと確定。

**原因確定（2026-07-19、隠密、診断ログ+一次ソース精読）**：真因は`DiagramRenderer.RightTerminator`
がrow(基準行)を起点(TopRow)とする下流分岐コネクタを一切見ず、`BottomRow`側のみで「B自身の右端で
終端」と誤判定していたこと。B接点は(1)A-B間結線の**入力側**(BottomRow)と(2)B-C間結線の**出力側
起点**(TopRow)の両方の役割を同時に持つが、旧ロジックは(2)を無視していたため、B-C間の縦コネクタ
(離れた列)まで横線が届かず、視覚的に「浮いて」見えていた。真因はT-026(2026-07-03、`docs/proposed.md`
P-003)で`(TopRow||BottomRow)`から`BottomRow`限定へ意図的に変更した副作用——2026-07-07に
P-028として理論予測・実証済みだった「右合流側の隙間」現象と同一と確定。

**修正完了・コミット4e11c3a・push済み（2026-07-19、侍）**：`RightTerminator`へ「rowをTopRowとする
下流分岐(Column>=rb)があれば最大列まで延ばし、無ければ従来のBottomRow側最小列へフォールバック」
という判定を追加（隠密設計）。NetlistBuilder(電気的判定)は無変更。新規回帰テスト
`DiagramRendererRightTerminatorTests.cs`（A-B縦積み+B-C横ずれ連鎖）をRED先行証明の上追加、
Core.Tests121件・App.Tests716件全合格。診断ログ計装（AppendDiagLog等）は原因確定後に除去済み
（除去後、`MainWindowViewModel.cs`はHEADと完全一致=このファイルには診断ログ以外の変更は無かった
と判明）。

**なお元々のT-044本題（OR自動配線の冗長縦分岐抑止、2026-07-07裁定）はBlockedのまま変わらず、
再開要否は引き続き殿判断待ち。今回Doneとしたのは殿新規指摘分のうち「視覚的な隙間の解消」のみ。**

**訂正・電気的な結線は未達成と判明（2026-07-19、隠密、`sample/T044-sample.gcad`直読+ネット追跡）**：
殿が実機で提示した「意図する正しい回路」（証跡=`docs-notes/screenshots/t044-tono-intended-circuit
-ninja.png`）は、C接点右側が**A/B並列ブロック全体の出力ネット（線番2、X接点〜コイル間）へ直接
合流するバイパス経路**。しかし`RightTerminator`修正は描画（横線の到達範囲）のみを変え、電気的な
接続先（`sheet.Connectors`が表すネット）は無変更——`ResolveNode`ロジック手計算追跡により、
Cの縦コネクタは`B.rightNode`=`A.rightNode`=**線番1のネットに帰着**しており、殿が意図する線番2
には到達していないと確定。忍者の追加実測（殿の意図通りに手動修正した図面でDRC再実行しても、
出力が元図面と一字一句同一）も裏付け——**DRCはこの種の未接続を検査対象にしていないため、当初
「DRC実測と符合」とした家老の伝達は誤りだった（訂正済み）**。
**根本原因＝OR自動配線の基準選択ロジック自体の設計限界（隠密所見）**：現行の`baseRow`探索は
「直近上の行を機械的に選ぶ」のみで、「どのネット・どの既存ブロックへ合流させたいか」という
操作意図を認識する手段が原理的に存在しない。描画層の修正では解決不可、基準選択ロジックそのものの
再設計を要する可能性が高い。**殿裁定（2026-07-19）：新規タスク化し後回し（他の修正を優先）**、
詳細はT-102参照。


