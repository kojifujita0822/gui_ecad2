# タスク台帳（家老が采配してよい根拠）

家老が采配してよいのは **Approved** または **In-progress** の行だけ。采配には必ずタスクIDを添える。
台帳に無い作業は、家老の裁量では着手せず `docs/proposed.md` へ記録して殿の承認を待つ
（詳細は `docs-notes/roles/karo.md` の「采配の権限線引き」）。

- 状態: Proposed → Approved → In-progress → Done / Rejected（+ Blocked＝外部要因待ち）
- 種別: auto-OK（家老の裁量で采配可） / gated（殿の承認を経たもの）
- **完了・取り止めタスクの詳細経緯は `docs/todo-archive.md` を参照**（2026-07-09軽量化＝殿指示。
  本ファイルは「生きているタスクの詳細」＋「完了・取り止めタスクの1行索引」のみを置く。
  タスクが完了したら家老が詳細をアーカイブ末尾へ移し、ここは1行索引に縮める運用）

## 現在の要望スコープ

- REQ-01: 技術スタック選定（`docs/ecad2-stack-decision-brief.md` 参照）

## 生きているタスク

### T-082 シート並び替え機能 — Approved（gated、殿裁定2026-07-12、T-080完了後に着手）

**起票=殿指摘2026-07-12**「gui_ecadにはシート並び替えがある」（家老の初期検分「両方に無い」は誤りで、
殿のスクリーンショット提示により訂正——教訓は`framework-default-features-invisible-to-grep`メモリ参照）。
**GuiEcad側の実体**：WinUI 3 `TreeView`の標準機能（`CanDragItems`/`CanReorderItems`既定true）で
ドラッグ並び替えが動く（明示コードは一行も無い）。ただし`DragItemsCompleted`等のモデル同期処理が
見当たらず、**保存順序（`Document.Sheets`）へは反映されない「見た目のみ」の可能性**（静的読解による
推測、実装時に照合。`RebuildNavTree`はモデル順で作り直すため次の再構築で見た目も戻る疑い）。
**ecad2側**：WPFのTreeViewに標準並び替え機能は無く、明示実装が必要——ドラッグ&ドロップUI+
`Document.Sheets`モデル同期+永続化+`PageNumber`採番（`SheetNavigationViewModel`）。GuiEcadと違い
保存順序へ正しく反映させる。
**着手=T-080完了後（殿裁定）**。
**殿裁定（2026-07-12）＝操作方式はドラッグ+キーボード**：ドラッグ&ドロップ（GuiEcad同様の直感操作）
に加え、キーボード操作も実装（キーボードファースト方針と整合）。**キー割当=Alt+上下で確定
（殿裁定2026-07-12、T-080クローズ後の着手前確認）**。いずれの操作でも並び替え結果は保存順序
（`Document.Sheets`）へ正しく反映させる。Undo対象はT-051 MVP現行方針どおり対象外が既定
（シート追加/削除と同種ゆえ実装中に疑義が出たら殿へ諮る）。
**采配時のDoD雛形（家老用、2026-07-12起草）**：(1)ドラッグ&ドロップ並び替え（WPF TreeViewに
標準機能無し、明示実装） (2)Alt+上下のキーボード並び替え (3)両操作ともDocument.Sheetsへ反映
(4)保存/読込で順序保持 (5)PageNumber再採番（SheetNavigationViewModel） (6)単体テスト+build/test
全合格。検証観点=端（先頭/末尾）での移動・シート1枚時・ドラッグ視覚フィードバック・保存読込往復。
ドラッグの視覚表現等のUI/UX分岐は実装前に殿へ【MUST】。参考仕様書=`docs/spec/guiecad-spec-sheet-document.md`。
**殿裁定（2026-07-12、実装着手後の分岐確認）＝ドラッグ視覚表現は案A標準フィードバック採用**：
ドラッグ中カーソル変化+ドロップ位置に挿入線表示+ドラッグ元アイテム半透明化（AdornerLayer活用。
簡易フィードバック案Bは不採用）。なお**GX Works3にはシートの概念自体が無い**（殿直接教示
2026-07-12、侍経由）——本件に「GX Works3踏襲」の参照先は存在しない。
**侍の着手時調査所見（2026-07-12）**：シート一覧の実体はListBox（SheetNavList、フラット表示）、
WPFネイティブDragDrop APIは使用実績皆無で新規導入、キーバインドはWindow_PreviewKeyDownのswitch文
（Alt+上下はKey.System/e.SystemKey分岐の同型対応が要る可能性）、GcadSerializerはリスト順そのまま
JSON化ゆえ順序変更のみで保存/読込に自動追従、PageNumber再採番ロジックは新規実装要。

### T-080 行コメント機能（右母線の右側、GuiEcad踏襲） — **完全Done（2026-07-12、往復3周+殿実機確認）**

**起票=殿直接指示2026-07-12**「gui_ecadには右母線のさらに右側に行コメントを記入できたがecad2は
記入できない。印刷にも反映されない。右母線の右側クリックで行コメント記入できるようにして」。
**印刷（PDF出力）への反映も含む**。
**着手前調査完了（2026-07-12、隠密、`docs/ecad2-t080-rung-comment-design-onmitsu.md`）**：
GuiEcad原本=右母線右側**ダブルクリック**→インラインTextBoxオーバーレイ（キャンバス上）、
Enter/Tab確定・Esc取消・フォーカスロスト確定、表示=右母線+2mm/FontSizeMm3.0/折返しなし、
PDF反映済み（画面と同一Renderer）。ecad2側=Core（モデル・RowOpsシフト）・描画（DrawRungComments）・
永続化（GcadSerializer）は**既に完備**、App層のみ皆無——「印刷に出ない」の実体は「入力手段がなく
データが常に空」。実装叩き台=ヒットテスト新規（DiagramRenderer.RightBusXのinternal化等）、
WPFネイティブClickCount==2でダブルクリック検出、入力UIはT-033配置バーの同一Windowオーバーレイ
設計・位置計算(TranslatePoint)流用（規模は単一TextBoxでかなり小）。
**殿裁定（2026-07-12、4点）**：
(1) 記入トリガー＝**ダブルクリック（GuiEcad踏襲）**
(2) 空文字列＝**削除扱い**（GuiEcadの空エントリ残留癖は踏襲しない）
(3) 印刷はみ出し対策＝**文字長20文字以内に制限し、印刷時のスペースを事前確保する**
（GuiEcadの「枠あり時未対応」は踏襲しない、殿指定）
(4) Undo＝**対象外を追認**（T-051 MVP現行方針どおり、他編集操作と同じ扱い）
**実装中間報告（2026-07-12、侍、コミット3b9080f）**：DoD(6)（スペース事前確保）を除く全DoD実装完了
（ダブルクリック→インラインTextBox・確定/取消・空は削除・20文字制限・SetRungCommentテスト6件、
build/test全合格）。DoD(6)は実装検討中に**P-060**（枠ありPDFでグリッド+コメント域が用紙に収まらない
既存欠陥、殿目視+忍者実測+隠密理論+隠密2のGuiEcad照合で確定）が発覚し一時保留→
**殿裁定（2026-07-12）＝縮小フィット採用**（常に枠あり維持、グリッド+行コメント域が用紙幅を超える
場合のみ自動縮小して収める。GuiEcad原本に収める仕組みは無く踏襲不成立、完全新規設計。
調査書=`docs/ecad2-p060-grid-overflow-investigation-onmitsu.md`ほか）。DoD(6)を縮小フィット方式で
実装続行→全DoD実装完了（コミット27d3ba9）。
**隠密静的レビュー（2026-07-12、`docs/ecad2-t080-rung-comment-review-onmitsu.md`）**：要修正・重大
8件CONFIRMED（A=CalcPageScaleが印刷枠5mm内側を未考慮、B=原点基準縮小で余白バランス崩れ、
C=複数ページ分割時に無関係ページも一律縮小、D=主回路シートのmm実座標コンテンツ未考慮で縮小フィット
実質不動、E=編集ボックス表示位置が実描画位置と約4mmずれ、F=編集中IsEnabled非連動、G=ヒットテストの
MainCircuit判定なし、H=クローズ時の無条件FocusCanvasでフォーカス強奪）。経過観察4件・REFUTED3件。
**殿裁定（2026-07-12、追加）＝キーボード経路も追加**：隠密の要判断指摘（起動経路がダブルクリック
一本のみ、キーボードファースト原則との不整合）を受け、選択セルの行に対し**F2キー**でコメント編集
ボックスを開く経路を修正往復と一括で実装（GuiEcad原本にも「コメント編集」キー割当が存在した）。
侍へ修正9件（A〜H+F2経路）を采配（往復1周目）。
**往復2周目〜クローズ（2026-07-12）**：往復1周目実装（`d7ed029`+`c4dd2b1`）・隠密再レビュー
全通過後、忍者実機で「UIA限界」と見えたマウス経路2件を殿の物理確認でNG確定→**診断ログ実測（T-038方式）で
原因確定**。(a)ダブルクリック不成立＝WPF仕様（MouseUp側の`ClickCount`は常に1、Down側でのみ2到達）の
見落としで、判定をDown側へ移設し解消（侍・隠密の独立診断が完全一致、`74d2783`）。(b)窓内クリックで
閉じない＝**現状を仕様化**（殿裁定。クローズ経路はEnter/Tab/Escのみ、`docs/spec/ecad2-spec-canvas-display.md`
へ明記）——round2で一度「閉じる」と誤実測したのは検証報告のためのチャット復帰クリック自体が
非アクティブ化を起こす「観測汚染」で、教訓はスキル`ecad2-ui-automation`とメモリへ収蔵済み。
追加往復（殿裁可）で課題2（ダブルクリック1発目の素通りによる選択巻き添えクリア＝(a)解消で表面化した
既存設計漏れ）・課題3（エディタ位置クランプ基準誤りで左端表示時に画面外描画）も修正（`516a81d`）。
殿直命でステータスバーへ操作説明「行コメント編集中: Enterで確定・Escで取消」表示を追加（`547fe59`）、
診断ログは除去済み（`f8b45d7`）。忍者実機3巡全観点OK+殿仕上げ確認済み。回帰テスト537→546件
（RED先行証明つき、配線回帰は単体で突けぬ限界を隠密が明示し実機確認を最終防波堤としてDoD化）。
範囲外の気づきはP-061〜P-065へ起票済み。

**起票=殿直接指示2026-07-11**「最終的にdocs/spec/の情報を編集して『ヘルプ』→『使い方』に転用。
ユーザー向けの為、詳しい詳細までは求めない」。

**背景**：T-075（主要機能の仕様書整備、隠密担当・進行中）で`docs/spec/ecad2-spec-{領域}.md`として
作成している技術仕様書（開発者向け、出典・コード根拠つきで詳細）を素材とし、将来的にアプリ内
「ヘルプ」メニューへ新設する「使い方」画面へ、**ユーザー向けに編集・転用**する。開発者向けの
実装詳細（コード内部・出典明記等）は求めず、平易な操作説明へ書き換える。

**前提条件**：T-075（仕様書整備）が一定領域完了してから着手（素材が無いと転用できない）。
内容（見込み、着手時に精査）：
1. 「ヘルプ」メニューへ「使い方」項目を新設
2. `docs/spec/`の各領域仕様書からユーザー向け平易版を作成・編集
3. アプリ内での表示形式（ダイアログ/パネル等）はUI/UX分岐ゆえ着手時に殿確認【MUST】
規模中、T-075完了後に着手順序を検討。

### 実装順ロードマップ（2026-07-11家老整理、T-062追加で改訂）

進行中の決着 → .NET 10移行 → 大物の順。移行を新機能実装より先に済ませ、移行時の検証（デバッグ）
範囲を最小に保つ（殿の理、2026-07-11）。

| 順 | task | 内容 | 状態・根拠 |
|---|---|---|---|
| - | T-052/T-059/T-055/T-053/T-054/T-056 | （6件） | **完全Done（2026-07-11、忍者実機6件一括全観点OK）** |
| - | T-051 | Undo/Redo基盤MVP | **完全Done（2026-07-11、往復3周+実機8観点全OK）** |
| - | T-062/T-073 | .NET 10移行・SendKeysフォーカス誤爆対策(P-056) | **完全Done（2026-07-11、mainマージ済み）** |
| - | T-072 | v0.3仮リリース | **完全Done（2026-07-11、gitタグv0.3付与済み）** |
| - | T-063/T-074 | メニュー露出2件・バージョン情報表示 | **完全Done（2026-07-11）** |
| - | T-075 | 主要機能の仕様書整備(全11領域) | **完全Done（2026-07-11、docs/spec/配下収蔵）** |
| - | T-077 | 「ヘルプ」→「使い方」画面新設(docs/spec/転用) | T-075完了済み、着手順序は後日検討 |
| - | T-071 | 経路B部品追加(10種+専用グリフ) | **完全Done（2026-07-11）** |
| - | T-065/T-066/T-079 | 文書情報・BOM編集(往復1周+緊急バグP-058対応) | **完全Done（2026-07-12、忍者実機全観点OK）** |
| - | T-060 | PDF出力のUI結線(プレビュー込み) | **完全Done（2026-07-12、往復1周、忍者実機DoD8点全OK）** |
| - | T-080 | 行コメント機能(右母線右側、殿直接指示) | **完全Done（2026-07-12、往復3周+殿実機確認）** |
| 1 | T-082 | シート並び替え機能(殿指摘起票) | **次の本丸**（T-080完了につき着手可、殿裁定2026-07-12） |
| 3 | T-064/T-069 | 画像挿入・右クリック残り4系統（中粒） | UI/UX論点**殿裁定済み(2026-07-12)**、侍実装待ち |
| 4 | T-061 | テストモードのUI結線 | UI/UX論点6点**殿裁定済み(2026-07-12)**、侍実装待ち |
| 5 | T-067/T-070 | GroupFrame作成・検索置換（中〜大） | UI/UX論点**殿裁定済み(2026-07-12)**、侍実装待ち |
| 6 | T-058 | パネルのドック化・フロート配置 | AvalonDock導入裁定済み。T-062後にPoC |
| 7 | T-068 | 自作パーツ管理・編集UI | 棚卸し起票、規模大ゆえ最後尾 |

- 順2以降の細かな先後は着手時に殿と調整（本表は家老見立て=小粒から肩慣らし）
- 棚卸し39項目の残り（クリップボード・テンプレート・オートセーブ・ショートカットキー設定・
  ダークモード・可動パレット[T-058と表裏]・不明点5件）は**後日判断（殿裁定2026-07-11）**、
  詳細=`docs/ecad2-guiecad-unwired-features-survey-onmitsu2.md`
- T-046（CI化）はStrykerスコア改善待ちの残置、T-044・T-028等の保留・Proposedは順位付け対象外
- Done6件の詳細アーカイブ移送は店じまい時にまとめて実施予定（家老）

**起票=GuiEcad未結線棚卸し（`docs/ecad2-guiecad-unwired-features-survey-onmitsu2.md`）のC区分から
殿が個別指名**。GuiEcadではApp層実装のためecad2に論理ごと無し——検索対象（デバイス名/コメント等）・
UI形式（ダイアログ/パネル）・置換の適用範囲などGuiEcad実物の詳細照合と設計調査から。規模中。
UI/UX分岐は着手時に殿確認【MUST】。

### T-069 右クリックメニュー残り4系統（コピペ/要素/縦コネクタ/枠） — Approved（gated、殿裁定2026-07-11、棚卸し起票）

**起票=棚卸しB区分**。右クリックメニュー基盤（T-055増分3のContextMenu）は行操作系のみ実装済み、
GuiEcadにあった残り4系統（コピー・ペースト系／要素操作系／縦コネクタ系／GroupFrame系）が未実装。
規模中。※コピペ系統はクリップボード機能（C区分・後日判断）に依存するため、着手時にスコープを
殿と確認（依存なしの系統から先行可）。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t069-context-menu-design-onmitsu2.md`）**：
4系統は依存関係が均質でないと判明。①コピペ系＝クリップボード機能App層に完全皆無（grep0件）、
C区分後日判断の対象そのもの、単独対応不可 ②要素操作系（3項目）＝削除は既存
（`DeleteSelectedElement`、T-063型で露出のみ可）・コメント編集は`RungComment`モデルはCore完備だが
**編集UI自体がApp層に皆無**（F2キー相当なし、右クリック以前にUI新規実装が要る）・機器名変更は
`DeviceNameBox`既存でフォーカス誘導程度（**UI/UX確認要**） ③縦コネクタ系（1項目）＝
`DeleteSelectedConnector`既存、**完全に器のみパターン・依存なし・最有力の先行着手候補** ④GroupFrame系
（3項目）＝T-067（枠作成UI皆無）と同一結論、T-067完了が前提で先行着手不可。
**隠密2の分割提案（家老承認済み）**：「即着手可能な部分」（縦コネクタ削除＋要素の削除/機器名変更＋
右クリックヒットテスト基盤新設——既存ContextMenuは行番号のみ判定のため要素/コネクタ/枠の判定分岐が
4系統共通基盤として必要）と「後続待ちの部分」（コメント編集UI新規実装・GroupFrame・コピペ）に分割。
**殿裁定（2026-07-12、プレビュー提示で選択）**：機器名変更のフォーカス誘導＝**`DeviceNameBox`へ
自動フォーカス**（新規インライン入力ボックスは不採用）。「即着手可能な部分」（縦コネクタ削除＋
要素の削除/機器名変更＋右クリックヒットテスト基盤新設）は家老裁量で侍へ采配準備中（T-071完了後）。

### T-070 検索・置換機能 — Approved（gated、殿裁定2026-07-11、棚卸し起票）

**起票=棚卸しB区分**。Core/App両層に検索・置換機能は皆無、機器名（デバイス名）変更・置換の
循環ジャンプ導線を想定。GuiEcadではApp層実装のためecad2に論理ごと無し。規模中。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t070-find-replace-design-onmitsu2.md`）**：
**最重要発見**——`DeviceRenamer.Rename(doc,from,to)`が既に全シート横断のDeviceName完全一致要素
一括置換＋機器表キー移行まで完結済み（**全置換はこれを呼ぶだけで完結**）。`Find(doc,name)`は
コメントに「検索バーのハイライト用」と明記されながら**App層のどこからも呼ばれていない未使用
メソッド**（grep0件確認）——T-064・T-071と同じ「Core完備・App結線待ち」パターンがここでも再現。
**規模見立ての再検討余地あり**（新規実装は薄い部分のみ）。
GuiEcad実物＝Ctrl+Fで作図エリア上部にFindBarオーバーレイ、機器名完全一致検索・循環Next/Prev、
ジャンプはビューパン中心化、置換1件は現在要素のみ（機器名整合性は保証しない設計）・全置換は
ループで`RenameDeviceCommand`、検索結果は下部出力パネルの別タブでDRCと同居。DRCジャンプ機構
（`OutputPanelViewModel`）は流用というよりGuiEcad自体が検索とDRCを同一パネル構造で実装済みの
パターン踏襲が自然、ecad2の`JumpTo`（SelectedCellへ選択移動の既存軽量パターン）で足り、GuiEcad
のビューパン処理の新規移植は不要。新規実装が要るのは検索状態管理(Index/循環)・トリガーUI・
置換1件時の機器表整合処理・検索結果パネル(DRCタブ同居)のみ。Undo対応は新規コマンドクラス不要
（既存`RecordSnapshot`パターン、T-064/T-067と同結論）。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①UI形式・配置場所＝**上部バー（GuiEcad踏襲、
Ctrl+FでFindBarオーバーレイ）** ②検索対象範囲＝**機器名完全一致のみ（GuiEcad踏襲）**（部分一致・
コメント等への拡大は不採用） ③置換1件の挙動＝**GuiEcad同様、現在ハイライト中の要素1件のみ置換**
（常に全置換扱いへの統一は不採用、機器名整合性は保証しない設計をそのまま踏襲）。

### T-068 自作パーツ管理・編集UI — Approved（gated、殿裁定2026-07-11、棚卸し起票）

**起票=棚卸しB区分**。`PartFolderStore`等Core層は完備だが、自作パーツの作成・編集UI（GuiEcadの
パーツエディタ相当）が皆無。**規模大**——ロードマップ最後尾（家老見立て）。着手時に増分計画から。

### T-067 GroupFrame（グループ枠）作成・編集UI — Approved（gated、殿裁定2026-07-11、P-054起票）

**起票=P-054（殿の直接指摘2026-07-11）を殿裁定でタスク化（B区分一括）**。`new GroupFrame`・
`Frames.Add`ともsrc全体0件＝生成経路がアプリに一切存在せぬ。Core層は完備（モデル・描画・RowOps
シフト・Height--詰め処理）。GuiEcad側には枠追加/削除/リネーム/移動/枠線スタイルのコマンド群が
存在。規模中〜大。関連=P-050（Visual*Mm座標未追随、枠ドラッグ実装時の潜在バグ——本タスク着手時に
併せて対処を検討）。UI/UX分岐（枠の作成操作方法等）は着手時に殿確認【MUST】。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t067-groupframe-design-onmitsu2.md`）**：
GuiEcad実物操作体系＝作成(「枠」ツール選択→**マウスドラッグ、mm連続座標のグリッド非依存自由配置**
→リリース確定、半セル未満は無視。GroupFrameは唯一グリッドに縛られない自由配置機能)・ラベル編集
(ダブルクリック→インラインテキストボックスのキャンバス上オーバーレイ)・移動(ドラッグ)・線種変更/
削除(右クリックメニュー、T-069調査で確認済み)。ecad2側Core層は完全同型で実装済み
（`DiagramRenderer.DrawFrames`のVisual*Mm優先ロジックまで）、App層は生成・編集・移動・削除いずれも
皆無。
**重要な発見**：P-050（RowOps未追随）の対処要否は独立したバグ修正ではなく、**「枠の配置単位を
グリッドセル単位にするかGuiEcad同様mm自由配置にするか」という設計分岐そのものに直結**——グリッド
セル単位限定ならP-050は構造的に非該当（そもそも追随すべきVisual座標が無い）、mm自由配置を踏襲する
ならP-050対処が必須。この分岐は作成操作方法（マウスドラッグ vs 既存`FreeLine`同型のキーボード
ステップ方式、`BeginFreeLineDraft`等が直接参考になる）とも自然に対応。Undo対応はT-064同様、既存
`RecordSnapshot`パターン流用で新規コマンドクラス不要。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①作成操作方法＝**両方対応**（マウスドラッグ・
キーボードステップいずれでも作成可能） ②配置単位＝**グリッドセル単位**（P-050は構造的に非該当と
確定、マウスドラッグ時もグリッドへスナップする実装とする） ③ラベル編集UI＝**ダブルクリック→
インライン入力（GuiEcad踏襲）** ④移動操作方式＝**ドラッグ（GuiEcad踏襲）**。

### T-064 画像挿入機能のUI結線 — Approved（gated、殿裁定2026-07-11、棚卸し起票）

**起票=棚卸しB区分**。`ImageInsert`モデル・`DrawImages`描画ともCore完備（GuiEcadでは挿入/移動/
リサイズ/トレース設定まであった機能）だが、App層に挿入・操作UIが無い。規模中。
GuiEcad実物の操作体系照合から。UI/UX分岐は着手時に殿確認【MUST】。
**着手前調査完了（2026-07-11、隠密2、`docs/ecad2-t064-image-insert-design-onmitsu2.md`）**：
GuiEcad実物操作体系＝挿入(専用ツールボタン→FileOpenPicker→ホバー位置配置、長辺120mm上限自動縮小)・
選択(クリックヒットテスト)・移動(ドラッグ、サブグリッドスナップ)・**リサイズはドラッグハンドルでなく
プロパティパネルの数値入力のみ**・削除(既存経路)・Undo(個別コマンドクラス4種)。
ecad2側は画面描画(`WpfRenderer.DrawImage`)・PDF出力(`PdfRenderSurface.DrawImage`)とも既に実装済み
（「渡されれば正しく描く」状態）、App層は挿入・選択・移動・リサイズ・プロパティ表示・削除いずれも
皆無。**朗報**：ecad2のUndo基盤(T-051、JSONスナップショット方式)はGuiEcadの個別コマンドクラス方式と
異なり、既存`RecordSnapshot`パターンを流用するだけで済む（画像操作専用Undoコマンドの新規実装は不要、
GuiEcadより実装コスト低）。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①挿入トリガー＝**メニューのみ**（ツールバーボタン
新設なし） ②配置位置決定方式＝**マウスホバー基準（GuiEcad踏襲）**——キーボードファースト方針との
整合は実装時に留意 ③リサイズ操作方式＝**ドラッグハンドル新設**（GuiEcadの数値入力のみ踏襲は不採用）
④対応ファイル形式＝**jpg等も追加**（bmp/pngのみのGuiEcad踏襲は不採用、WPF `BitmapImage`標準機能で
対応可）。

### T-061 テストモード機能のUI結線 — Approved（gated、殿裁定2026-07-11、P-051起票）

**起票=P-051（家老、殿の質問を受けた検分2026-07-11）を殿裁定でタスク化**。Core層Simulation
（`TestSession.cs`・`Netlist.cs`、T-007でGuiEcadから移植済み）は存在するが、App層に一切結線
されていない（メニュー「テストモード(_M)」もCommand未バインド）。design-brief等で「作画/テスト
モードの明確な視覚的フィードバック（色変化・右クリックメニュー分岐・タイマ実時間計時パネル）」が
既に構想されている（`docs/ecad2-ui-ux-design-brief.md`、`docs/ecad2-ladder-reference-systems-survey-onmitsu.md`）
ため、単純な結線に留まらずモード切替UI自体の新規設計を伴う可能性が高い。着手順序・スコープは
未着手（キュー整理待ち）。UI/UX分岐（モード切替の見せ方・対象操作範囲等）は着手時に都度殿確認【MUST】。
**設計調査完了（2026-07-11、隠密2、`docs/ecad2-t061-testmode-ui-wiring-survey-onmitsu2.md`）**：
Core層（TestSession/Netlist）はGuiEcadと全文一致移植済み・`DiagramRenderer.Render`もsim引数対応済み
で追加実装不要。**App層新規実装7点**（モード状態管理・切替UI結線・通電色分け設計・入力操作結線・
右クリック強制ON/OFFメニュー・実時間タイマパネル・ステータス連携）、段階導入5段階の叩き台あり。
**重要発見**：メニュー「テストモード」の`InputGestureText="F5"`表示がa接点配置（F5実利用中）と
矛盾——実装前に解消必須（P-053起票）。設計指針の隠密2所感=GuiEcadの`_testMode`単純boolを踏襲せず
上位enum（AppMode等）一元管理がToolState.csの既存教訓と整合的。
**殿裁定（2026-07-12、プレビュー提示で選択）**：①F5表示矛盾＝**メニュー表示を削除**（`InputGestureText`
除去、新規キー割当はしない） ②モード管理方式＝**上位enum新設（例：`AppMode`）**（design-briefの
状態一元化方針と整合） ③通電表示配色＝**LDmicro式（通電=赤/非通電=グレー）** ④モード切替の
見せ方＝**ツールバーToggleButton+背景色変化（GuiEcad踏襲）** ⑤シートまたぎ状態保持＝**OFF時に
全シート分クリア（GuiEcad踏襲）** ⑥タイマパネル＝**今回実装**（段階導入5段階すべてを今回スコープに
含める）。P-053（F5矛盾）はこの裁定で解決済み。

### T-058 パネル（ツールバー含む）のドック化・フロート配置機能の新規検討 — Approved（gated、殿直接指示2026-07-11）

**起票=殿直接指示2026-07-11**。当初「ツールバー右端の展開ボタンはフロート機能向けか」との問いから、
家老検分の結果、当該ボタンはWPF `ToolBar`標準のoverflow表示ボタン（ツールバー幅不足時の自動あふれ
格納UI）であり、フロート機能とは無関係と判明（`ToolBarTray`・`ToolBar`とも`IsLocked="True"`で
ドラッグ切り離しは明示的に無効化されている）。殿より「本当にツールバーフロート機能を新規検討したい」
との指示に続き、**「新規作成などのパネルもドック化してフロート配置できるようにして」と範囲拡大の
指示（2026-07-11）**——ツールバー（1段目=新規作成/開く/保存等・2段目=F5〜F10/部品）に加え、
他パネル（左パレット=シートナビ、右パネル=機器表/プロパティ、下部出力パネル）も対象候補となる、
GX Works3的な本格ドッキングペイン系の新規検討へ拡大。
現状：ゼロからの新規検討（前例・仕様とも未確定）。**WPF標準の`ToolBarTray`/`ToolBar`には真の
フロート化（別ウィンドウとして切り離す）能力が無い**（バンド間の並べ替えのみ標準対応）ため、実現には
(a)自前でWindow動的生成・再アタッチを実装する、(b)外部ドッキングライブラリ（AvalonDock等）を
導入する、のいずれかが必要と見込まれる——(b)は新規依存関係の追加でありCLAUDE.md「不要な外部依存を
追加しない」原則・karo.mdの依存関係変更ゲートに抵触するため、**採否は改めて殿へゲート**する。
第一段=隠密による調査（WPF標準機能の限界確認、外部ライブラリ候補の要否・コスト・ライセンス、
GX Works3等参考UIでの類似事例、対象パネル候補の棚卸しと優先度叩き台）。実装はまだ行わない。
**調査完了（2026-07-11、`docs/ecad2-t058-docking-float-survey-onmitsu.md`）**：
(1) WPF標準（ToolBarTray/DockPanel/GridSplitter）に真のフロート化能力なし、自前実装はドロップ先
プレビュー・タブ化・レイアウト永続化等でコスト大 (2) 外部ライブラリ5件比較で**AvalonDock
（Dirkster99フォーク、MS-PLで商用配布可・保守活発・NuGet導入容易）を最有力**と隠密推奨。
Xceed無償版=商用不可、DevExpress/Telerik/Infragistics=年$1000超の商用ライセンス。.NET8明示対応
未確認につき導入前PoC推奨 (3) GX Works3自体もWinForms+商用ドッキングコンポーネント併用と判明
（自前実装せず既製ライブラリに寄る業界先行判断） (4) 対象パネル難易度=左パレット・出力パネルが
低難易度で先行候補、ツールバー中難易度、右パネル（上下分割+状況依存切替）が最難関。
**殿裁定（2026-07-11）＝外部ライブラリ導入で進める**（「自作のコストが大きいので外部ライブラリー
導入で進めて」、依存関係ゲート通過）。採用候補=AvalonDock（Dirkster99フォーク、MS-PL）。
次段取り（実装段が来た際）：.NET8明示対応が未確認のため**導入前PoCから着手**（隠密調査の推奨
どおり。poc/配下で最小ドッキング・フロート化・レイアウト永続化を検証→OKなら本実装の増分計画へ）。
対象パネルの優先順位（左パレット・出力パネル先行が隠密叩き台）は実装段で改めて殿確認。
**.NET8対応の事前確認完了（2026-07-11、隠密2、`docs/ecad2-t058-avalondock-net8-precheck-onmitsu2.md`）**：
最新安定版4.74.1（2026-04-25）のTFMは`net5.0-windows`→`net9.0-windows`と飛び、**net8.0-windowsは
明示対象外**（メンテナ本人がIssue#467で確認済みの意図的方針、バグではない）。.NET8プロジェクト
（ecad2該当）は`net5.0-windows`アセットへのフォールバック解決見込みだが**実機動作の一次報告は
未発見**——PoC実機確認の必須性が確定。直接的な.NET8クラッシュ報告なし（不在証明の限界つき）。
テーマは別途7種必須構成（VS2013系が既存意匠と親和性高、隠密2所感）、レイアウト永続化は公式
Serializerパッケージで自前実装不要と判明。PoCで不具合が出た場合の代替選択肢=.NET9化（net9.0-windows
は明示サポート）だが大掛かりゆえ、その時点で改めて殿裁定。
**4.74.1↔5.0.0差分調査（2026-07-11、殿直接指示、隠密2、
`docs/ecad2-t058-avalondock-v5-diff-survey-onmitsu2.md`）**：一次資料=公式移行ガイド
（docs/migration/breaking-changes.md）+実運用者Issue#604/PR#607で裏取り。
**最重要=5.0.0のTFMは`net48;net9.0-windows;net10.0-windows`のみ**——4.74系にあった
`net5.0-windows`緩衝フォールバックが消滅し、ecad2がnet8のままでは5.0への追随自体が困難になる
可能性大＝**T-062（.NET 10移行）を先行させる殿判断の強い裏付け**。朗報=レイアウトXML永続化は
ほぼ完全互換（preview.96で互換レイヤー追加済み）、API面の書き直しは軽微（namespace/パッケージ
変更、公式見積10〜20分）。新機能（MVVM基底クラス・DI統合等）は任意で強制なし。開発は非常に活発
（直近1週間5コミット・実運用フィードバック対応が数日単位）だが安定版時期は不明——**結論：
4.74.1で実装開始してよい（将来の5.0移行コストは小、ただしT-062完了が事実上の前提）**。

### T-046 「必ず通過するテスト」防止の仕組み化 — In-progress（制度は運用開始済み、CI化のみ残置）

**殿裁定2026-07-08**：①RED先行証明の必須化＝バグ修正の回帰テストは修正前コードでREDになることを
git stash実測で証明し報告に含める（`samurai.md`・`karo.md`へ【MUST】追記済み、即日運用開始）
②Stryker.NET＝**手動棚卸しから段階導入で確定（殿裁定2026-07-08）**：隠密調査
（`docs/ecad2-t046-stryker-survey-onmitsu.md`、WPF相性OK・全体3分弱・当初score Core 3.76%/App 19.79%
→T-045補遺2後 App 22.15%）を受け、往復修正案件のクローズ時に隠密が手動実行してテストの穴を
棚卸しする運用から開始（`karo.md`・`onmitsu.md`へ運用追記済み）。score改善後にCI化を再検討
③**テスト設計と実装の分離**＝バグ修正・往復案件では隠密が仕様側からテスト設計（同値分割・境界値
分析・状態遷移・対称性点検・`[Theory]`活用の技法適用必須）を先に起草し、侍はコード化に徹する
（実装者バイアス対策、殿指摘2件を制度化。`onmitsu.md`「テスト設計の起草」・`samurai.md`
「テスト設計と実装の分離」・`karo.md`同名節へ【MUST】追記済み、即日運用開始）。
背景=T-041増分7で「旧実装でも新実装でも通る回帰テスト」が複数残存していた実例。
**残作業はCI化の再検討のみ（score改善待ち）**。T-050クローズ時棚卸し（2026-07-10）時点の
App score=23.88%（19.79→22.15→23.88と漸増中、`docs/archive/ecad2-t050-stryker-review-onmitsu.md`）。

### T-044 OR自動配線の冗長縦分岐抑止 — Blocked（一旦保留＝殿裁定2026-07-07）

実装d04c9a3＋隠密再レビュー＋忍者実機とも全OK・電気的に正常だが、連鎖ORで低ズーム時に
右合流線が断線して見える描画欠陥が判明。自動ロジック精緻化は袋小路（真因はP-003/P-028の既存欠陥）
ゆえ手動配線基盤T-041で対処する方針へ転換（T-041は2026-07-08完全Done）。
再開の要否・時期は殿判断待ち。

### T-028 浮動インライン入力ダイアログの「拡張表示」ボタン — Proposed（gated）

T-026段階4の浮動インライン入力ダイアログ実装時、仕様未確定のため侍が未実装のまま起票（2026-07-03）。
殿確認済み：今回は実装不要、別タスク化。位置・レイアウトの殿注文3点は殿裁定によりT-033へ統合済み
（T-033増分3で拡張表示ボタンの「配置のみ」実装済み・押下無反応・当面Tab除外。押下時の詳細画面が
本タスクの残スコープ）。

### T-022 ステータスバーの高情報密度化（機種名/局番/ステップ数等） — Proposed（gated）

T-009残課題9。機種名/局番/ステップ数など他機能への依存があり、依存元の実装状況次第。

### T-013 ツールバー/メニューアイコンの本格的な意匠制作 — Proposed（gated）

T-009段階3では簡易プレースホルダ（Path Geometry/Unicode記号）で仕組みのみ実装。
本格的な意匠（記号・単色ベースのグラフィックデザイン）は別タスクとして切り出し（家老裁量）。
T-040でツールバー配置系ボタンはGX様式グリフ化済み。

### T-032 CSV取り込みによる図面自動生成 — Proposed（将来構想、gated）

殿の将来構想（2026-07-03）。GX Works3が出力するCSV（SHIFT-JISエンコード、ニーモニック命令列：
LD/AND/ANI/OR/ORI/OUT等）を読み込んでラダー図を表示・自動生成したい。実装時はニーモニックと
回路記号のマッピングが必要（T-031の調査結果が基礎資料になる見込み）。GX Works3の実際のCSV出力
フォーマット（列構成、命令表記の実例）は未調査。詳細仕様・着手時期は未定。

## 完了・取り止めタスク索引（詳細経緯は `docs/todo-archive.md`）

並びはおおむね着手順。

- [x] T-001 技術スタック裁定（WPF本命仮確定）
- [x] T-002 フォーカス保持PoC
- [x] T-003 最終スタック確定（WPF正式確定）
- [x] T-005 WPF非技術面検証
- [x] T-006 タブ切替フォーカス喪失対策
- [x] T-004 4セッション体制・雛形/アーキ設計
- [x] T-007 GuiEcad実ソース移植（全層）
- [x] T-011 保存先フォルダ名変更（GuiEcad→Ecad2）
- [x] T-008 UI/UX全体像設計（殿裁定）
- [x] T-010 GX Works3 UI/UX調査（Web一次情報）
- [x] T-012 GX Works3実機追加調査
- [x] T-014 GX Works3技術スタック調査
- [x] T-031 ニーモニック（接点表記）の扱い方基準の調査
- [x] T-009 Ecad2.App UI実装（骨格、全8段階）
- [x] T-024 gui_ecad 新規/開く/保存フロー調査
- [x] T-025 gui_ecad 要素選択・ヒットテスト調査
- [x] T-016 要素配置ロジック本体
- [x] T-026 左パネル→ナビツリー化・ツールバー移行（2026-07-03）
- [x] T-030 グリッド線表示の有効化（殿直接依頼）
- [x] T-017 要素選択・編集フォーカス制御の本実装（2026-07-03）
- [x] T-027 選択中セルの視覚的ハイライト表示（T-017に統合）
- [x] T-018 DesignRuleCheckと下部出力パネルの接続（2026-07-04）
- [x] T-020 空状態⇔作業領域の動的切替（2026-07-04、濃紺#24325A確定は2026-07-05）
- [x] T-023 LadderCanvasアクセシビリティ強化（2026-07-04）
- [x] T-021 キーボード規約の残り（Enter配置・Esc4層・パン追従、2026-07-05 mainマージ）
- [x] T-019 ドキュメント管理（新規/開く/保存、完全Done 2026-07-05）
- [x] T-015 図形ビジュアルプレビュー（部品リストサムネイル、2026-07-05）
- [x] T-036 配置時の機器表即時反映＋デバイス名編集修正（完全Done 2026-07-05）
- [x] T-038 診断ログ連携運用の整備（2026-07-05）
- [x] T-034 App層テストプロジェクト新設（完全Done 2026-07-06）
- [x] T-035 .gcadpart読込時のID重複検出＋再採番（完全Done 2026-07-06）
- [x] T-037 部品選択リストへORa/ORb追加・固定7種化（完全Done 2026-07-06）
- [x] T-039 操作トレースログ基盤（完全Done 2026-07-06）
- [x] T-042 App層テストの実環境副作用解消（完全Done 2026-07-06）
- [x] T-040 ツールバー配置系ボタンのGX様式化（完全Done 2026-07-07）
- [x] T-043 ORa/ORbサムネイルのシンボル統一（完全Done 2026-07-07）
- [x] T-033 配置後入力の真の非モーダル浮動インライン化（全5増分、完全Done 2026-07-07）
- [x] T-041 主回路用の横配線・縦分岐線の手動記入＋消去＋修正（全7増分、完全Done 2026-07-08）
- [x] T-045 App層リファクタリング（全4増分＋補遺2、完全Done 2026-07-09、mainマージ`5f2ee6e`）
- [x] T-029 ツールバーボタン配置時のゴースト表示（**Rejected 2026-07-09**＝キーボードファースト
  理念と相反するため不要と殿裁定。先行調査書`docs/archive/ecad2-t029-presurvey-onmitsu.md`は参考資料として収蔵）
- [x] T-047 手動配線系（F9/F10系）のツールバーボタン作成（5ボタン+シート種別連動グレーアウト+
  並び替え[選択→F5〜F10→区切り→部品]+無効時半透明化、**完全Done 2026-07-09**。修正往復1周=
  フォーカス残留+接続点誤配置を制度適用[隠密設計→侍修正]で解消。グリフ変更は次回T-048へ）
- [x] T-049 デバイス名編集中の未確定編集を確定してから保存（P-013起票、**完全Done 2026-07-10**。
  隠密レビュー要修正なし・忍者実機全観点OK。範囲外の気づき=P-045）
- [x] T-048 手動配線ボタン（sF9・F10系）のグリフ変更（殿意匠提示→プレビュー承認制で往復2回調整、
  **完全Done 2026-07-10**。制御シート=矢印形・はさみ形、主回路シート=既存の棒・点を維持。
  隠密レビュークリーン・忍者実機回帰4観点OK）
- [x] T-050 TraceLogの全角ラテン文字正規化統一（P-014/P-015統合。往復3周=隠密レビュー2周+
  殿裁定のテスト補強3周目、隠密レビュー3回クリーン・忍者実機全観点OK・Stryker棚卸し3件は
  殿裁定で経過観察、**完全Done 2026-07-10**。期間中の出力破損§5離脱2回を引き継ぎ書2本で
  作業損失ゼロで完遂）
- [x] T-051 シート追加・削除操作をUndo対象に含める（Undo/Redo基盤MVP新設、往復3周、完全Done 2026-07-11）
- [x] T-052 未解決PartIdフォールバックのDRC警告追加（往復1周、完全Done 2026-07-11）
- [x] T-053 機器表「種別」列の日本語表示化（完全Done 2026-07-11）
- [x] T-054 部品選択リストの選択中部品を配置バー内に表示（完全Done 2026-07-11）
- [x] T-055 行数拡張のGuiEcad方式踏襲＋母線番号入力の同仕様化（全増分1〜3、完全Done 2026-07-11）
- [x] T-056 キャンバスのグリッド線表示切替機能（完全Done 2026-07-11）
- [x] T-057 v0.2仮リリースビルドの作成（完全Done 2026-07-10）
- [x] T-059 出力パネルの高さをドラッグで調整可能にする（完全Done 2026-07-11）
- [x] T-062 .NET 10（net10.0-windows）への移行（完全Done 2026-07-11、mainマージ済み）
- [x] T-063 「名前を付けて保存」「削除」のメニュー露出（往復1周、完全Done 2026-07-11）
- [x] T-071 経路B部品（押釦・タイマ接点等）の部品選択リスト追加（10種+専用グリフ、往復1周、完全Done 2026-07-11）
- [x] T-072 v0.3仮リリースビルドの作成（完全Done 2026-07-11）
- [x] T-073 ecad2-ui-automationスキルのSendKeysフォーカス誤爆対策（P-056対策、往復1周、完全Done 2026-07-11）
- [x] T-074 「バージョン情報」ダイアログへのバージョン番号表示（完全Done 2026-07-11）
- [x] T-075 主要機能の仕様書整備（全11領域、完全Done 2026-07-11、docs/spec/配下収蔵）
- [x] T-076 docs/配下の整理（173→37件、136件をarchiveへ移動・リンク327箇所更新、完全Done 2026-07-12）
- [x] T-078 todo.mdの軽量化（958→435行、15タスク分をarchiveへ移送、完全Done 2026-07-12）
- [x] T-065 ドキュメント情報の編集UI（往復1周、完全Done 2026-07-12）
- [x] T-066 機器表のBOM編集（型式のみ、往復1周+緊急バグP-058対応、完全Done 2026-07-12）
- [x] T-079 機器配置直後の保存操作で機器表エントリが消失するバグ修正（P-058、完全Done 2026-07-12）
- [x] T-060 PDF出力機能のUI結線（プレビュー・全シート・常に枠あり、往復1周、完全Done 2026-07-12）
- [x] T-081 GuiEcad仕様書の作成（全11領域、比較3表つき、docs/spec/guiecad-spec-*収蔵、完全Done 2026-07-12）
