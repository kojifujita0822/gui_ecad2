<Window x:Class="T110SingleDockingManagerPoc.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T110SingleDockingManagerPoc"
        xmlns:avalonDock="https://github.com/Dirkster99/AvalonDock"
        xmlns:avalonDockControls="clr-namespace:AvalonDock.Controls;assembly=AvalonDock"
        xmlns:vs2013Themes="clr-namespace:AvalonDock.Themes.VS2013.Themes;assembly=AvalonDock.Themes.VS2013"
        mc:Ignorable="d"
        Title="T-110 単一DockingManager統合PoC" Height="800" Width="1280"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>

        <!-- ============================================================
             (e)検証: タイトルスタイルのModel.ContentId分岐による統合
             本実装(src/Ecad2.App/MainWindow.xaml)のAnchorablePaneTitleNoDragHandleStyle
             (T-100、VS2013テーマAnchorablePaneTitle暗黙的スタイルの完全コピー+ハッチング除去)と
             PlacementToolBarAnchorablePaneTitleStyle(T-099(c)案E、ラベルのみ非表示)の
             2スタイルを、単一DockingManagerでは1つのStyleへ統合する必要がある(隠密プラン§4.2)。
             ControlTemplate.Triggers末尾にModel.ContentId条件のDataTriggerを追加することで
             出し分けが成立するかを本PoCで実証する。 -->
        <Style x:Key="UnifiedAnchorablePaneTitleStyle" TargetType="{x:Type avalonDock:AnchorablePaneTitle}">
            <Setter Property="Background" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionInactiveBackground}}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionInactiveText}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid Margin="2,2,3,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Rectangle
                                    x:Name="DragHandleGeometryPlaceholder"
                                    Fill="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionInactiveGrip}}"
                                    Visibility="Collapsed"/>
                                <DockPanel>
                                    <Border
                                        Padding="2,0,4,0"
                                        HorizontalAlignment="Left"
                                        Background="{TemplateBinding Background}">
                                        <avalonDock:DropDownControlArea
                                            DropDownContextMenu="{Binding Model.Root.Manager.AnchorableContextMenu, RelativeSource={RelativeSource TemplatedParent}}"
                                            DropDownContextMenuDataContext="{Binding Path=LayoutItem, RelativeSource={RelativeSource TemplatedParent}}"
                                            Style="{DynamicResource DropDownControlArea}">
                                            <ContentPresenter
                                                x:Name="Header"
                                                Content="{Binding Model, RelativeSource={RelativeSource TemplatedParent}}"
                                                ContentTemplate="{Binding Model.Root.Manager.AnchorableTitleTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                                ContentTemplateSelector="{Binding Model.Root.Manager.AnchorableTitleTemplateSelector, RelativeSource={RelativeSource TemplatedParent}}"
                                                TextElement.Foreground="{TemplateBinding Foreground}"/>
                                        </avalonDock:DropDownControlArea>
                                    </Border>
                                    <Rectangle
                                        x:Name="DragHandleTexture"
                                        Height="5"
                                        Margin="4,0,2,0"
                                        VerticalAlignment="Center"
                                        UseLayoutRounding="True"
                                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                                        Visibility="Collapsed">
                                        <Rectangle.Fill>
                                            <DrawingBrush
                                                TileMode="Tile"
                                                Viewbox="0,0,4,4"
                                                ViewboxUnits="Absolute"
                                                Viewport="0,0,4,4"
                                                ViewportUnits="Absolute">
                                                <DrawingBrush.Drawing>
                                                    <GeometryDrawing Brush="{Binding Fill, ElementName=DragHandleGeometryPlaceholder, Mode=OneWay, Converter={avalonDock:NullToDoNothingConverter}}">
                                                        <GeometryDrawing.Geometry>
                                                            <GeometryGroup>
                                                                <GeometryGroup.Children>
                                                                    <RectangleGeometry Rect="0,0,1,1"/>
                                                                    <RectangleGeometry Rect="2,2,1,1"/>
                                                                </GeometryGroup.Children>
                                                            </GeometryGroup>
                                                        </GeometryDrawing.Geometry>
                                                    </GeometryDrawing>
                                                </DrawingBrush.Drawing>
                                            </DrawingBrush>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                </DockPanel>
                                <avalonDock:DropDownButton
                                    x:Name="MenuDropDownButton"
                                    Grid.Column="1"
                                    Margin="1,1,1,0"
                                    Width="15"
                                    Height="15"
                                    VerticalAlignment="Center"
                                    DropDownContextMenu="{Binding Model.Root.Manager.AnchorableContextMenu, RelativeSource={RelativeSource TemplatedParent}}"
                                    DropDownContextMenuDataContext="{Binding Path=LayoutItem, RelativeSource={RelativeSource TemplatedParent}}"
                                    Focusable="False"
                                    Style="{DynamicResource AvalonDockThemeVs2013ToolButtonStyle}"
                                    ToolTip="メニュー">
                                    <Path
                                        x:Name="PART_ImgMenuPin"
                                        Margin="0,0,0,1"
                                        Width="8"
                                        Height="8"
                                        Data="{DynamicResource PinMenu}"
                                        Fill="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionButtonInactiveGlyph}}"
                                        Stretch="Uniform"/>
                                </avalonDock:DropDownButton>
                                <Button
                                    x:Name="PART_AutoHidePin"
                                    Grid.Column="2"
                                    Margin="1,1,1,0"
                                    Width="15"
                                    Height="15"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Command="{Binding Path=LayoutItem.AutoHideCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                    Focusable="False"
                                    Style="{DynamicResource AvalonDockThemeVs2013ButtonStyle}"
                                    ToolTip="自動的に隠す"
                                    Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={avalonDock:BoolToVisibilityConverter}}">
                                    <Path
                                        x:Name="PART_ImgAutoHidePin"
                                        UseLayoutRounding="True"
                                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                                        Width="11"
                                        Height="11"
                                        Data="{DynamicResource PinAutoHide}"
                                        Fill="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionButtonInactiveGlyph}}"
                                        Stretch="Uniform"/>
                                </Button>
                                <Button
                                    x:Name="PART_HidePin"
                                    Grid.Column="3"
                                    Margin="0,1,1,0"
                                    Width="15"
                                    Height="15"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Command="{Binding Path=LayoutItem.HideCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                    Focusable="False"
                                    Style="{DynamicResource AvalonDockThemeVs2013ButtonStyle}"
                                    ToolTip="閉じる"
                                    Visibility="{Binding Path=IsEnabled, RelativeSource={RelativeSource Self}, Mode=OneWay, Converter={avalonDock:BoolToVisibilityConverter}}">
                                    <Path
                                        x:Name="PART_ImgHidePin"
                                        Width="10"
                                        Height="10"
                                        Margin="1,0,0,1"
                                        VerticalAlignment="Center"
                                        Data="{DynamicResource PinClose}"
                                        Fill="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionButtonInactiveGlyph}}"
                                        Stretch="Uniform"/>
                                </Button>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding Model.IsAutoHidden, RelativeSource={RelativeSource Mode=Self}}" Value="True">
                                <Setter TargetName="PART_AutoHidePin" Property="LayoutTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Model.CanClose, RelativeSource={RelativeSource Mode=Self}}" Value="True">
                                <Setter TargetName="PART_HidePin" Property="Command" Value="{Binding Path=LayoutItem.CloseCommand, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <Setter TargetName="PART_HidePin" Property="ToolTip" Value="閉じる"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Model.IsActive, RelativeSource={RelativeSource Mode=Self}}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionActiveBackground}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionActiveText}}"/>
                                <Setter TargetName="DragHandleGeometryPlaceholder" Property="Fill" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionActiveGrip}}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Model.IsActive, RelativeSource={RelativeSource Mode=Self}}" Value="True">
                                <Setter TargetName="PART_ImgMenuPin" Property="Fill" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionButtonActiveGlyph}}"/>
                                <Setter TargetName="PART_ImgAutoHidePin" Property="Fill" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionButtonActiveGlyph}}"/>
                                <Setter TargetName="PART_ImgHidePin" Property="Fill" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.ToolWindowCaptionButtonActiveGlyph}}"/>
                            </DataTrigger>
                            <!-- 【本PoCの核心】(e)検証: 4Manager出し分けだった案Eのラベル非表示を
                                 Model.ContentId条件へ置き換える。ContentId="PlacementToolBar"の
                                 ペインのみラベル(Header)を非表示にし、他ペインは無変化のまま
                                 通常表示を保つ。この1トリガーが全ペイン共通の単一スタイル内で
                                 意図通り分岐するかどうかが検証対象そのもの。 -->
                            <DataTrigger Binding="{Binding Model.ContentId, RelativeSource={RelativeSource Mode=Self}}" Value="PlacementToolBar">
                                <Setter TargetName="Header" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ============================================================
             全ペイン適用時の副作用確認: 本実装のPlacementToolBarPaneControlStyle
             (T-099根本修正、TabStripPlacement=Bottom+IsDirectlyHostedInFloatingWindow
             トリガー+T-106ダークモード色対応)を全ペイン共通のAnchorablePaneControlStyleとして
             適用する。単一ペイン構成(シート/機器表/プロパティ/出力)ではVS2013テーマ標準の
             「Items.Count==1でTabItem自体がCollapse」に委ねられる想定だが、実機での副作用有無を
             本PoCで確認する。 -->
        <Style x:Key="UnifiedPaneControlStyle" TargetType="{x:Type avalonDock:LayoutAnchorablePaneControl}">
            <Setter Property="Foreground" Value="{Binding Model.Root.Manager.Foreground, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="Background" Value="{Binding Model.Root.Manager.Background, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="TabStripPlacement" Value="Bottom"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type avalonDock:LayoutAnchorablePaneControl}">
                        <Grid ClipToBounds="true" KeyboardNavigation.TabNavigation="Local" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Border Grid.RowSpan="2" Background="Transparent"/>
                            <Border x:Name="ContentPanel" Grid.Row="0" Grid.Column="0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                KeyboardNavigation.TabIndex="2"
                                KeyboardNavigation.TabNavigation="Cycle">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                    Margin="{TemplateBinding Padding}"
                                    ContentSource="SelectedContent"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <avalonDock:AnchorablePaneTabPanel x:Name="HeaderPanel"
                                Grid.Row="1" Margin="2,0,2,2" Panel.ZIndex="1"
                                IsItemsHost="true" KeyboardNavigation.TabIndex="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                        <Setter Property="IsEnabled" Value="{Binding IsEnabled}"/>
                        <Setter Property="ToolTip" Value="{Binding ToolTip}"/>
                        <Setter Property="Background" Value="{DynamicResource PanelHeaderBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource PanelHeaderForegroundBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabItem}">
                                    <Grid SnapsToDevicePixels="true">
                                        <Border x:Name="Bd"
                                            Padding="{TemplateBinding Padding}"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1,0,1,1">
                                            <Grid>
                                                <ContentPresenter x:Name="Content"
                                                    HorizontalAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                    VerticalAlignment="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                    ContentSource="Header"
                                                    RecognizesAccessKey="True"
                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="Selector.IsSelected" Value="true">
                                            <Setter Property="Background" Value="{DynamicResource PanelContentBackgroundBrush}"/>
                                            <Setter Property="Panel.ZIndex" Value="1"/>
                                            <Setter Property="Margin" Value="0,-1,-1,-2"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="true"/>
                                                <Condition Property="Selector.IsSelected" Value="false"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.GradientInactiveCaptionBrushKey}}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                            <Setter Property="Panel.ZIndex" Value="0"/>
                                        </MultiTrigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsEnabled" Value="false"/>
                                                <Condition Property="Selector.IsSelected" Value="false"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <!-- 修2(隠密静的レビュー差し戻し、2026-07-21): VS2013テーマ標準の
                             「Items.Count==1でTabItem自体をCollapse」トリガー(一次ソース
                             Generic.xaml:536-540)。本実装のPlacementToolBarPaneControlStyle
                             自体に無く2タブ固定ペインでは死文だったため省略されていたが、
                             全ペイン適用へ昇格すると単一ペイン4領域(シート/機器表/プロパティ/
                             出力)に冗長な下タブが常時表示されてしまう。全ペインPaneControl
                             適用の副作用確認(漏れ2)そのものの検出のため追加する。 -->
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TabControl}}, Path=Items.Count}" Value="1">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <avalonDock:LayoutAnchorableTabItem Model="{Binding}"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <avalonDock:LayoutAnchorableControl Model="{Binding}"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ============================================================
             (h)検証: 単一ペインタイトルバー完全非表示(裁5=案A)
             AvalonDock v4.74.1一次ソース(source/Components/AvalonDock.Themes.VS2013/
             Themes/Generic.xaml:1712-1800、scratchpad取得済み)のLayoutAnchorableControl
             既定スタイルを完全コピーし、Header(Border)を常時Collapsedにする。
             既存の「フロート単独ホスト時のみ隠す」トリガー群はテーマ公認パターンとして残しつつ、
             末尾に無条件のSetterを追加する形にする(T-099の罠=コントロール自体のCollapse化で
             マウス処理ごと殺す、とは異なりHeader Border層のみを消すため対象階層が異なる)。
             ドッキング中に常時使われるのは単一ペイン構成(シート/機器表/プロパティ/出力)であり、
             配置ツールバー(2タブ、UnifiedPaneControlStyle経由でホスト)はそもそも本スタイルの
             対象外(タブUIにタイトルバー自体が存在しない)。 -->
        <Style x:Key="TitleBarHiddenAnchorableControlStyle" TargetType="{x:Type avalonDockControls:LayoutAnchorableControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutAnchorableControl}">
                        <Border
                            x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource {x:Static vs2013Themes:ResourceKeys.PanelBorderBrush}}"
                            BorderThickness="1,0,1,1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <!-- 【本PoCの核心】(h)検証: 裁5=案A(完全非表示)を実現するため、
                                     フロート時条件を待たずHeaderを常時Collapsedにする。 -->
                                <Border x:Name="Header" Visibility="Collapsed">
                                    <avalonDockControls:AnchorablePaneTitle Model="{Binding Model, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Border>
                                <ContentPresenter
                                    Grid.Row="1"
                                    Content="{Binding LayoutItem.View, RelativeSource={RelativeSource TemplatedParent}}"
                                    ContentTemplate="{Binding LayoutItem.View.ContentTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                    ContentTemplateSelector="{Binding LayoutItem.View.ContentTemplateSelector, RelativeSource={RelativeSource TemplatedParent}}"
                                    FlowDirection="{TemplateBinding FlowDirection}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Model.IsFloating}" Value="True"/>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Model.Parent.IsDirectlyHostedInFloatingWindow}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0"/>
                            </MultiDataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ============================================================
             (f)検証: ドキュメントタブ非表示(裁2)
             AvalonDock v4.74.1一次ソースのAvalonDockThemeVs2013DocumentPaneControlStyle
             (Generic.xaml:191-299)を完全コピーし、ヘッダー領域Grid(タブストリップ+
             メニュードロップダウン)を常時Collapsedへ差し替える(元は
             Model.ShowHeaderへのBindingだったが、本PoCでは固定値での実装可否を確認する)。 -->
        <Style x:Key="DocumentTabHiddenPaneControlStyle" TargetType="{x:Type avalonDockControls:LayoutDocumentPaneControl}">
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static vs2013Themes:ResourceKeys.TabBackground}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type avalonDockControls:LayoutDocumentPaneControl}">
                        <Grid ClipToBounds="true" KeyboardNavigation.TabNavigation="Local" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.RowSpan="2" Background="Transparent"/>
                            <!-- 【本PoCの核心】(f)検証: 元はModel.ShowHeaderへのBindingだった
                                 Visibilityを固定値Collapsedへ差し替え、タブ領域を常時非表示化する。 -->
                            <Grid Margin="1,0" Grid.Row="0" Panel.ZIndex="1" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="BD" Grid.ColumnSpan="2"
                                    BorderBrush="{DynamicResource {x:Static vs2013Themes:ResourceKeys.DocumentWellTabSelectedActiveBackground}}"
                                    BorderThickness="0,0,0,2"/>
                                <avalonDockControls:DocumentPaneTabPanel x:Name="HeaderPanel"
                                    Grid.Row="0" Grid.Column="0" Margin="0"
                                    IsItemsHost="true" KeyboardNavigation.TabIndex="1"/>
                                <avalonDock:DropDownButton x:Name="MenuDropDownButton"
                                    Width="14" Height="14" Margin="0,0,-1,5"
                                    Grid.Column="1" VerticalAlignment="Bottom" Focusable="False"
                                    Style="{DynamicResource AvalonDockThemeVs2013ToolButtonStyle}">
                                    <avalonDock:DropDownButton.DropDownContextMenu>
                                        <avalonDock:ContextMenuEx ItemsSource="{Binding Model.ChildrenSorted, RelativeSource={RelativeSource TemplatedParent}}"/>
                                    </avalonDock:DropDownButton.DropDownContextMenu>
                                    <Path x:Name="MenuDropDownButtonImage" Height="8" HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Data="{DynamicResource PinDocMenu}"
                                        Fill="{DynamicResource {x:Static vs2013Themes:ResourceKeys.DocumentWellOverflowButtonDefaultGlyph}}"
                                        Stretch="Uniform"/>
                                </avalonDock:DropDownButton>
                            </Grid>
                            <Border x:Name="ContentPanel" Grid.Row="1" Grid.Column="0"
                                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource {x:Static vs2013Themes:ResourceKeys.PanelBorderBrush}}"
                                BorderThickness="1,0,1,1"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                KeyboardNavigation.TabIndex="2"
                                KeyboardNavigation.TabNavigation="Cycle">
                                <ContentPresenter x:Name="PART_SelectedContentHost" Margin="0"
                                    ContentSource="SelectedContent"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="8">
            <Button x:Name="LightThemeButton" Content="Lightテーマ" Click="LightTheme_Click" Margin="0,0,8,0" Padding="8,4"/>
            <Button x:Name="DarkThemeButton" Content="Darkテーマ" Click="DarkTheme_Click" Margin="0,0,16,0" Padding="8,4"/>
            <ToggleButton x:Name="TitleBarHiddenToggle" Content="(h)タイトルバー完全非表示" Click="ToggleTitleBarHidden_Click" Margin="0,0,16,0" Padding="8,4"/>
            <ToggleButton x:Name="DocumentTabHiddenToggle" Content="(f)ドキュメントタブ非表示" Click="ToggleDocumentTabHidden_Click" Margin="0,0,16,0" Padding="8,4"/>
            <Button Content="(c)配置ツールをFloat()" Click="FloatPlacementToolBar_Click" Margin="0,0,8,0" Padding="8,4"/>
            <Button Content="(c)配置ツールをDock()" Click="DockPlacementToolBar_Click" Padding="8,4"/>
        </WrapPanel>

        <avalonDock:DockingManager x:Name="MainDockingManager" Grid.Row="1"
                                   AnchorablePaneControlStyle="{StaticResource UnifiedPaneControlStyle}">
            <avalonDock:LayoutRoot>
                <avalonDock:LayoutPanel Orientation="Vertical">

                    <!-- 上段: 配置ツールバー相当(2タブ同居、(c)再現条件どおり
                         MainToolBar相当=CanFloat="False"・PlacementToolBar相当=フロート可、
                         両タブCanClose="False"、SelectedContentIndex="1"。
                         ContentDocking系ハンドラは意図的に付けない(標準動作のまま観察するため)。 -->
                    <avalonDock:LayoutAnchorablePane DockHeight="123" SelectedContentIndex="1">
                        <avalonDock:LayoutAnchorable Title="基本機能" ContentId="MainToolBar" CanClose="False" CanFloat="False">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <Button Content="選択" Margin="2" Padding="6,2"/>
                                <Button Content="Undo" Margin="2" Padding="6,2"/>
                                <Button Content="Redo" Margin="2" Padding="6,2"/>
                            </StackPanel>
                        </avalonDock:LayoutAnchorable>
                        <avalonDock:LayoutAnchorable Title="配置ツール" ContentId="PlacementToolBar" CanClose="False">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <Button Content="F5配置" Margin="2" Padding="6,2"/>
                                <Button Content="F6配置" Margin="2" Padding="6,2"/>
                                <Button Content="F7配置" Margin="2" Padding="6,2"/>
                            </StackPanel>
                        </avalonDock:LayoutAnchorable>
                    </avalonDock:LayoutAnchorablePane>

                    <avalonDock:LayoutPanel Orientation="Horizontal">

                        <!-- 左: シート相当(裁4=CanFloat="False") -->
                        <avalonDock:LayoutAnchorablePane DockWidth="220">
                            <avalonDock:LayoutAnchorable Title="シート" ContentId="LeftPalette" CanClose="False" CanFloat="False">
                                <ListBox>
                                    <ListBoxItem Content="シート1"/>
                                    <ListBoxItem Content="シート2"/>
                                    <ListBoxItem Content="シート3"/>
                                </ListBox>
                            </avalonDock:LayoutAnchorable>
                        </avalonDock:LayoutAnchorablePane>

                        <!-- 中央: 作図キャンバス相当(LayoutDocumentPane、(g)Tab巡回確認用に
                             フォーカス可能なTextBoxを複数配置) -->
                        <avalonDock:LayoutDocumentPane>
                            <avalonDock:LayoutDocument Title="作図キャンバス" ContentId="Canvas" CanClose="False">
                                <StackPanel Margin="16">
                                    <TextBlock Text="作図キャンバス相当(PoCダミー)" FontSize="16" Margin="0,0,0,12"/>
                                    <TextBox Text="フォーカス確認用1" Margin="0,0,0,8"/>
                                    <TextBox Text="フォーカス確認用2" Margin="0,0,0,8"/>
                                    <TextBox Text="フォーカス確認用3"/>
                                </StackPanel>
                            </avalonDock:LayoutDocument>
                        </avalonDock:LayoutDocumentPane>

                        <!-- 右: 機器表+プロパティ相当(裁4=CanFloat="False") -->
                        <avalonDock:LayoutPanel Orientation="Vertical" DockWidth="280">
                            <avalonDock:LayoutAnchorablePane DockMinHeight="80">
                                <avalonDock:LayoutAnchorable Title="機器表" ContentId="DeviceTable" CanClose="False" CanFloat="False">
                                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="機器名" Width="100"/>
                                            <DataGridTextColumn Header="種別" Width="*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </avalonDock:LayoutAnchorable>
                            </avalonDock:LayoutAnchorablePane>
                            <avalonDock:LayoutAnchorablePane DockMinHeight="80">
                                <avalonDock:LayoutAnchorable Title="プロパティ" ContentId="RightPanelBottom" CanClose="False" CanFloat="False">
                                    <StackPanel Margin="8">
                                        <TextBlock Text="機器名: SW1" Margin="0,0,0,4"/>
                                        <TextBlock Text="コメント: (PoCダミー表示)"/>
                                    </StackPanel>
                                </avalonDock:LayoutAnchorable>
                            </avalonDock:LayoutAnchorablePane>
                        </avalonDock:LayoutPanel>

                    </avalonDock:LayoutPanel>

                    <!-- 下段: 出力パネル相当(裁4=CanFloat="False") -->
                    <avalonDock:LayoutAnchorablePane DockHeight="160">
                        <avalonDock:LayoutAnchorable Title="出力" ContentId="OutputPanel" CanClose="False" CanFloat="False">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="種別" Width="100"/>
                                    <DataGridTextColumn Header="内容" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </avalonDock:LayoutAnchorable>
                    </avalonDock:LayoutAnchorablePane>

                </avalonDock:LayoutPanel>
            </avalonDock:LayoutRoot>
        </avalonDock:DockingManager>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="8"
                   Text="準備完了。(d)は各ペインの中身をクリックして青帯(アクティブ色)が1つだけ移ることを確認。"/>
    </Grid>
</Window>
